Core-Manager's Job:

Creates agent definitions (types of agents available)
Creates agent instances (specific configured instances of those types)
Manages workflows stored in the database
Creates Kafka topics for new agent types