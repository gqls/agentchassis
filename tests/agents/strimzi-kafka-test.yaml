apiVersion: v1
kind: Pod
metadata:
  name: kafka-client-test
  namespace: kafka  # Note: in the kafka namespace for easier access
spec:
  containers:
    - name: kafka-client
      image: confluentinc/cp-kafka:latest
      command: ["/bin/bash", "-c", "sleep 3600"]