You're right. Let me refactor `request_human_input.go` to align with the existing `hitl_actions.go` patterns while adding the new capabilities.Now let me check if there's an existing spawn_agent action to align `spawn_group` with:There's already a `SpawnGroupAction` - let me see what it does:Good - there's already a `SpawnGroupAction`. Let me revise my version to align with it while adding the new database lookup capabilities:

You're right. Let me refactor request_human_input.go to align with the existing hitl_actions.go patterns while adding the new capabilities.

Now I should also look at the spawn_group action and see if it should be more aligned with existing spawn_agent patterns. Let me check what patterns exist in the existing code.

Now let me check if there's an existing spawn_agent action to align spawn_group with:

bash
grep -n "func SpawnAgentAction\|func SpawnGroupAction" /mnt/project/production_agent-chassis-full_context.txt | head -10

Output
13775:func SpawnAgentAction(ctx context.Context, params ActionParams) (interface{}, error) {
14292:func SpawnAgentActionOld2(ctx context.Context, params ActionParams) (interface{}, error) {
14719:func SpawnAgentActionOld(ctx context.Context, params ActionParams) (interface{}, error) {
15050:func SpawnGroupAction(ctx context.Context, params ActionParams) (interface{}, error) {

Ah, there's already a SpawnGroupAction! Let me look at it to see what already exists.

There's already a SpawnGroupAction - let me see what it does:

There's already a SpawnGroupAction in the codebase. It:

    Takes group_type and agents from config
    Spawns each agent using SpawnAgentAction
    Creates subtree info
    Returns await_response: true

The existing one expects agents to be passed in config. My version was designed to:

    Support dynamic group_type from collected_data
    Fetch group definition from database
    Use the group's agent_configs from DB

So my version is an enhancement that:

    Fetches from agent_group_definitions table
    Supports dynamic group_type_field
    Pulls workflow and agent configs from DB

I should make my version an extension that still follows the same patterns. Let me revise spawn_group.go to align better with the existing code, or call it something different like spawn_group_by_type or just update the existing one with new capabilities.

Looking at the existing code more closely:

    Uses params.Headers["request_id"]
    Uses models.Step struct for spawn config
    Creates types.SubtreeInfo
    Uses helper functions like GenerateGroupName

My version should align with these patterns while adding:

    Database lookup capability
    Dynamic group_type from collected_data
    Questionnaire fetching

Actually, the user specifically said "spawn groups and spawn actions shouldn't be too different either". So I should make spawn_group align with the existing SpawnGroupAction, and perhaps my version becomes an enhanced/v2 version or we update the existing one to support both modes (config-provided vs DB-provided).

Let me revise the spawn_group.go to:

    Use the same patterns (SubtreeInfo, GenerateGroupName, etc.)
    Support both modes: config-provided agents OR database lookup
    Add dynamic group_type_field support

Good - there's already a SpawnGroupAction. Let me revise my version to align with it while adding the new database lookup capabilities:

bash
rm /home/claude/spawn_group.go

Output
exit code 0

I'll create a version that extends the existing SpawnGroupAction, adding support for:

    Dynamic group_type from collected_data
    Database lookup for group definition
    Questionnaire fetching

This should use the same patterns as the existing one.

The user wants me to look at the actual hitl_actions.go file they've uploaded and make request_human_input.go more closely aligned with it. Let me compare the patterns:

Key patterns in hitl_actions.go:

    Uses params.Producer.Produce() not params.KafkaClient.Produce()
    Uses datahelpers.GetInputData()
    Has specific helper functions like extractDataForApproval, buildApprovalNotification, extractApprovalResponse
    The notification structure has specific fields
    Uses params.ExecutionContext.ResponsesTopic and falls back to os.Getenv("RESPONSES_TOPIC")
    Returns await_response: true to pause workflow
    Has request_id as the approval token

I should refactor request_human_input.go to:

    Use the same Producer interface
    Follow the same function naming patterns
    Use similar data extraction patterns
    Keep the structure recognizable

Also, I notice my current request_human_input.go already imports datahelpers and uses similar patterns, but let me check if it's fully aligned. Looking at my code vs the original:

My code uses:

    params.Producer.Produce() - correct
    datahelpers.GetInputData() - correct
    Similar notification building pattern

But I should make the helper function names more consistent and the overall structure more similar.

Let me also create a cleaner version that's more clearly an extension of the same pattern.

Looking at the actual hitl_actions.go file, let me refactor request_human_input.go to align more closely with its patterns and structure:

Now let me copy the files to outputs:

Now let me copy the updated files to outputs:

bash
# Remove the old spawn_group.go which was my first attempt
rm /mnt/user-data/outputs/spawn_group.go

ls -la /mnt/user-data/outputs/

Output
total 146
drwxr-xr-x 1  999 root     0 Nov 27 16:47 .
drwxr-xr-x 4 root root  4096 Nov 22 17:24 ..
-rw-r--r-- 1  999 root  5115 Nov 27 16:47 adapter_response_functions.go
-rw-r--r-- 1  999 root 15506 Nov 27 16:47 agent_definitions_update.sql
-rw-r--r-- 1  999 root 21089 Nov 27 16:47 assemble_full_page.go
-rw-r--r-- 1  999 root  3253 Nov 27 16:47 chief_strategist_update.sql
-rw-r--r-- 1  999 root 26934 Nov 27 16:47 css_js_schema.sql
-rw-r--r-- 1  999 root  3853 Nov 27 16:47 fetch_group_questionnaire.go
-rw-r--r-- 1  999 root 28056 Nov 27 16:47 intake_and_groups.sql
-rw-r--r-- 1  999 root  7677 Nov 27 16:47 interface.go
-rw-r--r-- 1  999 root   749 Nov 27 16:47 registry_update.go
-rw-r--r-- 1  999 root 15911 Nov 27 16:47 request_human_input.go
-rw-r--r-- 1  999 root 14104 Nov 27 16:47 spawn_group_from_db.go

Good. Now let me update registry_update.go to include all the new actions:

Let me update the registry file to reflect all the new actions:

Good. Let me now give a summary of what's been created and how it all fits together.
Summary

Here's what we've created, aligned with existing patterns:
New Go Actions

File	Action	Based On	New Features
request_human_input.go	request_human_input	hitl_actions.go	Skip conditions, questionnaire support, multiple request types
spawn_group_from_db.go	spawn_group_from_db	Existing SpawnGroupAction	Database lookup, dynamic group_type resolution
fetch_group_questionnaire.go	fetch_group_questionnaire	New	Fetches questionnaire from target group definition

Alignment with Existing Code

request_human_input.go mirrors hitl_actions.go:

    Same params.Producer.Produce() pattern
    Same datahelpers.GetInputData() usage
    Same notification structure (reply_to_topic, request_id, etc.)
    Same await_response: true return pattern
    Same database stub pattern
    Helper functions follow same naming: extractInputResponse vs extractApprovalResponse, buildInputNotification vs buildApprovalNotification

spawn_group_from_db.go mirrors existing SpawnGroupAction:

    Uses same SpawnAgentAction internally
    Same GenerateGroupName() helper
    Same subtree tracking pattern
    Same result structure (group_id, agents, workflow, await_response)
    Adds: database lookup, dynamic group_type resolution

SQL Updates

File	Contents
intake_and_groups.sql	Schema update (briefing_questionnaire column), site-classifier agent, intake-orchestrator group, landing-page-builder group, content-site-builder group

Key Design Points

    Same patterns - New actions follow established conventions
    Complementary - request_human_input extends await_approval, spawn_group_from_db extends spawn_group
    Skip conditions - skip_if: "input_data.hitl_mode == auto" allows automated runs
    Group-level branching - Intake orchestrator uses spawn_group_from_db with dynamic group_type_field
    Questionnaire in group definition - Each builder group defines its own briefing questionnaire

Request human input
Code · GO
Spawn group from db
Code · GO
Fetch group questionnaire
Code · GO
Intake and groups
Code · SQL 
