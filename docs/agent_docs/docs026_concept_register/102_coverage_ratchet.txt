# Concept-register coverage ratchet — subsystems known to be uncovered.
# Generated by 102_CHECK_register_coverage.py --update-ratchet.
# A name here is ACCEPTED BACKLOG, not a resolved problem. Remove a line
# when you add its register entry; the report then stays quiet about it.
# Baseline taken 2026-07-27.

about_page_commercial
bug020_tool_recreation_data_integrity
bugfix_003_spawn_loss
bugfix_006
bugfix_019_council_truncation
bugfix_026_schema_dialect_failopen
bugfix_034
bugfix_037_needs_rebuild_guard
bugfix_040_kafka_dial
bugfix_040_partial_build
bugfix_069_chrome_locks
bugfix_079_phantom_link_gate
bugfix_082_postgres_qos
chassis_replica_scaling
content_quality2_silent_overwrite_of_dependent_pages
cta_link_integrity
dispatch_queue_serialisation
dormant_agents_inventory
durable_write_guard
empty_sections_loop_integrity
experience_loop
fabricated_stats_043
gauntlet_dead_cta
hero_tool_component_045
idea_uk_section_data_missing
idea_uk_vm_site
news_feed_pooling
per_site_ai
reasoning_dataset
review_queue_drain
robot_hands_gripper_dossier
scheduler_group_starvation
tool_suggester_phantom_links
truncation_casualties_046
truncation_contract_076
vm_estate
webdesign_couk
webdesign_uk_build_service
work_item_completion_integrity
idea_uk_demand
bugfix_161_register_ratifies    # one-off: SQL + repair script for one false register fact on one site. No reusable mechanism; the banned patterns are site data.
bugfix_125_deploy_path_from_url    # one-off: a bug-fix lane. The shared helper it ships (datahelpers.PageFilePathFromURL) is a de-duplication of five existing copies of one rule, not a new capability — no consumer gains an ability it did not have.
bugfix_165_reconciliation_deletes    # one-off: a bug-fix lane converting call sites to an ALREADY-REGISTERED rule (CTXA-025). No new mechanism — the second consumer and its three transferable lessons are recorded on the CTXA-025 entry itself, which is where a session looking for the capability would find them.
finetuning_uk_service    # workstream just opened (2026-07-31): docs-only so far. Its seams (finetune_request item type, island puller, billing lineage) get register entries in the commits that ship them — drop this line then.
bugfix_160_prose_gate_recombination    # one-off: a bug-fix lane narrowing one classifier inside verify_report_prose (one consumer, report-builder). No reusable mechanism — the transferable part is the 016b §9 pattern and the mutation-check rule, not a callable capability.
bugfix_164_bundle_body_cap    # one-off: a bug-fix lane bringing ONE loop into line with the marker convention its own file already applied at two sibling caps (:521, :605). No new mechanism — the fix is a reuse, deliberately, and the two additive artefact metadata keys are observability on an existing jsonb blob, not a capability a consumer could call. The transferable parts are the 016b §9 hard-cap pattern and a LANDMINES entry on the NULL-vs-0 trap those keys create.
retired_agents    # one-off: not a workstream and not a mechanism — a backup/restore artefact directory for agents removed from the fleet (first entry: multipage-website-builder, 2026-08-02). Deliberately outside docs/agent_docs/sql_for_agents/ so the migration runner cannot auto-apply a restore script. Nothing here is callable; a session looking for a capability would not want it, and a session about to retire an agent finds it by name.
bugfix_172_agent_state_cap    # one-off: a bug-fix lane bringing the diagnosis bundle's agent-state gather into line with the marker convention its own file and its sibling already apply (diagnose_load_runtime_action.go:491, diagnose_assemble_bundle_action.go:328). Deliberately NO shared helper — a "cap and report" mechanism spanning this family would be a platform seam, and a seam arriving inside a bug patch is what bugs_closed/124 was vetoed for. The two new functions are file-local, pure and unexported; nothing outside this action can call them. Transferable parts are the 016b §9 hard-cap pattern and a WRONG_CALLS row about mocks supplying the property under test.
domains_cloudflare_rollout    # workstream just opened (2026-08-03): ops docs only so far — bulk Cloudflare zone setup across Nominet EPP + dynadot/porkbun/spaceship registrar APIs. Nothing callable yet; if the run produces a reusable bulk-zone/NS-repoint tool it gets a register entry in the commit that ships it — drop this line then.
bugfix_177_tool_content_items    # one-off: a bug-fix lane. Its ONE arguably-registrable artefact — raiseToolContentItem, the satisfiability-gated tool-content emit — is deliberately unexported and package-local, and it is already recorded where a reader will look: TL-003 (updated to conditional emission) and TL-009 (partial-shipped note) in the same commit that ships it. Transferable parts are the 016b §9 "unsatisfiable at birth" pattern, the LANDMINES shared-tmpfs entry, and bugs_open/187 for the wider population.
docs/agent_docs/docs024_key_docs_latest/bugfix_134_optional_marker — single-bug fix lane (134, optional-marker config keys); its reusable mechanism is registered as the SCR-004 SUSPICIOUS KEYS section, this line covers only the docs dir
bugfix_116_link_check_coverage    # one-off: a bug TRIAGE lane that deliberately shipped no code. 116's fix is owner-gated (all four candidates blocked — see the bug's STATUS block), so there is no mechanism to register. Transferable parts are already where a reader will look: the LANDMINES entry (a disabled scheduled_tasks row shows a fresh last_completed_at) and the WRONG_CALLS entry (counting findings cannot measure coverage).
bugfix_087_writer_self_plans    # opened 2026-08-04, docs-only (PLAN + RUNBOOK, no code, no seed). Nothing callable exists yet; if the lane ships a mechanism it gets a register entry in the commit that ships it — drop this line then.
dartsonline_traffic    # site lane: one site's identity/briefing/nav/news SQL resets and a traffic push. Site-specific work is out of the register's scope by its own bar ("another workstream could call this and would not know it exists"). Its PREVENTION doc's transferable parts belong in 016b §9 / LANDMINES, not here.
mortgagecalculator_couk_adoption    # site lane: adopting one existing site into the framework. Site-specific; the adoption MECHANISM it uses is already registered (adopting-and-scraping.md).
loancash_couk    # site lane: one site build. Site-specific by the register's own bar.
bugfix_158_reply_drop_sizing    # REGISTERED 2026-08-04 as ADP-018 (check_silent_reply_drop). This line stays only because the coverage sensor keys on the DIRECTORY name, which the entry does not have to contain; delete it if the sensor ever matches on sources instead.
bugfix_162_fix_proposer_plan_repair    # one-off: a bug-fix lane that switched an ALREADY-REGISTERED opt-in mechanism (FIX-057, the plan repair loop) on for a second consumer. No new capability — the second consumer, the migration and the consumer-census query are recorded on FIX-057 itself, which is where a session looking for the mechanism would find them.
bugfix_179_deploy_path_override    # one-off: a bug-fix lane DELETING an override that could bypass a registered shared derivation (IMG-067, storage.DeployedAssetPath). The register entry was corrected in the same commit that shipped the fix, per the platform-seams condition (2). Nothing new is callable.
bugfix_189_split_symbol_duplicate    # one-off: a bug-fix lane collapsing the THIRD hand-rolled copy of the path:Symbol grammar onto the already-registered owner (CTXK-002, analysis.SplitSymbol). Nothing new is callable — the collapse, its proof and the declined pattern-check are recorded on CTXK-002 itself, which is where a session looking for the mechanism would find them.
bugfix_120_merge_deploy_skip    # one-off: a bug-fix lane repairing the changed-domains derivation inside two sibling repos' CI workflows (gqls/sites, gqls/vm-sites). Nothing new is callable from the platform — the fix, census and induction proof live in bugs_closed/120 and the lane docs.
bugfix_117_chrome_staleness_reference    # one-off (so far): a bug-fix lane MEASURING an already-deployed detector (StaleSiteComponentsCheck, check_integrity.go) against the signal it claims to track, and finding the reference wrong in both directions. Nothing new is callable — the lane holds evidence, queries and a handoff, no mechanism. RE-CHECK THIS LINE when the fix lands: if it ships a render-input fingerprint on site_components, that IS a reusable seam and must be registered per the platform-seams condition (2), not ratcheted.
bugfix_216_claimed_row_passthrough    # one-off: a bug-fix lane passing an already-returned claimed row through the coordinator recoverable arm (bugs_open/216). Nothing new is callable — no new symbol is exported, no seam changes; the fix, tests and induction proof live in bugs_open/216 and the lane docs.
bugfix_221_ai_disclosure_precision    # one-off: a bug-fix lane narrowing ONE pattern family inside one unexported function (checkMetaCommentary / metaCommentaryPatterns, bugs_open/221). Nothing new is callable — the Re field is a private struct member on an unexported var with a single caller, no symbol is exported and no seam changes. The design, the three mutations and the accepted residuals live in bugs_open/221 and the lane docs; the prospective trap is an addendum on the EXISTING LANDMINES entry for validate_page_content's blocker severity, not a new one.
bugfix_209_deploy_purpose_keyed_source    # one-off: a bug-fix lane that VERIFIED an existing defect (bugs_open/209) rather than shipping one. Nothing new is callable — the commit adds a characterisation test file and docs, no production code, no exported symbol, no seam. The transferable parts are already where a reader will look: the LANDMINES entry (a step with no input_fields resolves by randomised recursive search, 344/400 wrong sibling) and the verification block appended to bugs_open/209 itself. RE-CHECK THIS LINE when the fix lands: candidate 1 (asset_uris.<asset_key>, an identity-keyed in-run source) IS a reusable seam and must be registered per the platform-seams condition (2), not ratcheted.
bugfix_232_gauntlet_noindex    # one-off: a bug-fix lane. Its reusable mechanism IS registered — SEO-003 (pages.noindex -> injectRobotsNoindex), in the commit that shipped it, per the platform-seams condition (2). This line covers only the docs dir, which the coverage sensor keys on by DIRECTORY name. Also carries the two-head-producer landmine; RE-CHECK THIS LINE if that divergence is ever converged — a single head-assembly seam both producers call IS registrable and must not be ratcheted.
bugfix_198_roundtrip_writers    # one-off: a handoff dir for a SURVEY (the 012/198 round-trip-writer inventory the architecture seat asked for in council round 5249320e). Nothing new is callable — the mechanisms 198 shipped are already registered where they belong (DGH-007 commit_message_field, in the commit that shipped it). RE-CHECK THIS LINE when the inventory lands: if it produces a reusable guard or check type (a generic shrink guard, a whole-artefact-contract detector), that must be registered, not ratcheted.
noted_rebuild    # site work: rebuilding one hand-built site (noted.co.uk) on the framework. Nothing new is callable yet — no exported symbol, no new action, no seam; the commit adds a seed, workstream docs and a LANDMINES entry. RE-CHECK THIS LINE when phase 3 lands: the experience_patterns rows it will add (authenticated-note-sync, local-first-capture, media-attachment-capture, backup-export-restore) ARE a reusable vocabulary other sites could bind, and the first pattern describing an authenticated per-user data round-trip must be REGISTERED, not ratcheted. Same for any criteria check type the work has to add to close the ordering/degraded-dependency gaps recorded in PLAN §5.
council_gate_cost    # one-off: a COST-REDUCTION lane on an existing agent, not a new capability. Its reusable mechanism IS registered — LCO-008 (CacheBreakpointMarker: opt-in Anthropic prompt caching on platform/aiservice, plus the two llm_call_log cache columns from migration 376) — in the same commit that shipped it, per the platform-seams condition (2). This line covers only the docs dir, which the coverage sensor keys on by DIRECTORY name. RE-CHECK THIS LINE if the lane ever ships a SECOND mechanism: a lint for "does any template interpolate a per-seat value above its cache marker" is the named open review question on LCO-008 and would be a reusable guard — registrable, not ratchetable. Same if the per-seat model roster (17 seats currently all sonnet-5, each settable at config.ai_service.model) is ever turned into a tiering scheme other agents could adopt.
diagnosis_schema_visibility    # one-off: a commission lane repairing the SCHEMA SECTION of the diagnosis evidence bundle (owner ruling 2026-08-10, item 5) inside one unexported function, gatherSchema in diagnose_load_runtime_action.go. Nothing new is callable — every symbol touched is package-local (gatherSchema, schemaAlwaysTables, schemaFilterNotice, stringsAsIface), the new key schema_always_tables is one action's step config rather than a shared vocabulary, and nothing changes what a shared mechanism guarantees. The transferable parts are where a reader will look: the mechanism and its measurements on the lane docs, and the bundle-reading trap on the run it broke (090 074beb8a / bugs_open/236 hero-logo half). RE-CHECK THIS LINE if the always-list idea is ever generalised: a shared "an evidence gather must describe every table it renders rows from" guard, usable by the council tier or any other bundle builder, IS a reusable check type and must be REGISTERED, not ratcheted.
bugfix_242_render_audit_truncation    # one-off: a bug-fix lane on an existing audit path (bugs_open/242). Nothing new is callable — no exported symbol, no new action, no seam: the edits add fields to one action's request payload, echo them in the adapter's summary, stamp them in the drain's result, and make ONE call to the already-registered agenterrors writer (the RFC_012-B door) with a new error_code RENDER_AUDIT_TRUNCATED, which is a row vocabulary, not a mechanism. The transferable part is already where a reader will look: RFC_012 (the class), agenterrors.go's own header (the pattern), and bugs_open/242's 2026-08-11 section (the worked case). RE-CHECK THIS LINE if the request-echoes-into-summary shape is ever generalised into a helper other awaiting actions call — that IS a reusable seam and must be REGISTERED, not ratcheted.
bugfix_247_dead_agent_config_loader    # one-off: a bug-fix lane DELETING dead code (bugs_open/247). Nothing new is callable — no new exported symbol, no new action, no seam: it removes AgentConfigLoader.LoadAgentConfig (an unmutexed cache, zero live callers) plus processRequest/selectWorkflowOLD and their private helper cluster in processor.go (all zero live callers, verified fresh by grep). AgentConfigLoader the type and its other methods stay live and unregistered-but-unchanged (internal/agents/contentcreator/agent.go is the real caller). Nothing to register; this line exists only because the coverage sensor keys on the workstream DIRECTORY, and this dir holds only fix docs.
bugfix_246_shared_pool_ownership    # one-off: a bug-fix lane on an existing constructor (bugs_open/246). Nothing new is callable — no new exported symbol, no new action, no seam: it DELETES three SetMax*/SetConnMaxLifetime calls that NewMessageProcessor was making on the *sql.DB it was PASSED (a pool object, so they re-sized the caller's pool and silently discarded CHASSIS_DB_MAX_OPEN_CONNS), and sizes the pool the same constructor opens ITSELF from DATABASE_URL, which was previously created with its error discarded and left at Go's zero value (= unlimited). The rule it restores — you size what you OPEN, never what you were GIVEN — is stated in the code and enforced by a behavioural test, not by a new shared helper: there is exactly ONE non-test caller (agentbase/agent.go:341) and exactly ONE binary importing agentbase (cmd/agent-chassis), so a helper would be a seam invented for a single call site. The transferable parts are where a reader will look: bugs_open/246's 2026-08-11 section (the worked case, the blast-radius census, and the post-roll checks), and LANDMINES.md (the 090 loop cannot read a Kubernetes env var). UPDATE 2026-08-12: follow-up (a) HAS LANDED and is registered as SYS-091 (observability.RegisterDBPoolStats + agentbase registration) — this line stays because the 246 change ITSELF is still non-registrable; the instrument is the separately-registered part. RE-CHECK THIS LINE if the remaining follow-up lands: (a) [DONE, see SYS-091] surfacing db.Stats() WaitCount/WaitDuration so pool saturation is measurable at all — that IS a reusable instrument and must be REGISTERED, not ratcheted; (b) narrowing the db parameter to a query-only interface so the defect is structurally unrepresentable — that changes the signatures of orchestration.NewStateRepository and NewSagaCoordinator, which are public and platform-wide, and is architecture-scope rather than either.
silent_hero_logo_readers    # one-off: a commission lane making three EXISTING readers of a deploy_image_asset result stop failing silently (owner ruling 2026-08-10, item 2; bugs_open/236). Nothing new is callable — every symbol is unexported and package-local (deployedImageURL, mapKeysSorted, deployedImageAuditSiteID; extractLogoURLFromParams and buildRenderContextFromParams gain a ctx parameter and have one caller each), and the durable write is ONE call to the already-registered agenterrors writer (the RFC_012-B door, via LogActionError) with a new error_code DEPLOYED_IMAGE_RESULT_MISSING_URL — a row vocabulary, not a mechanism, exactly as bugfix_242_render_audit_truncation ratcheted RENDER_AUDIT_TRUNCATED. Observability only: it does NOT widen what the readers accept, which is bugs_open/236 §4 candidate 2 and is item 1's reserved owner decision. The transferable parts are where a reader will look: the lane docs (the demand-gate design and both mutation proofs), and bugs_open/236 (the worked case, plus the sibling-key lead this lane contributed). RE-CHECK THIS LINE if either follow-up lands: (a) generalising "a reader of an awaited result must record a present-but-unusable container" into a shared helper other readers call — that IS a reusable seam and must be REGISTERED, not ratcheted; (b) item 1 choosing a merge design, which changes what the awaited-response seam GUARANTEES and is architecture-scope rather than either.
bugfix_268_cta_buttons_fleet — bug-fix workstream (fix itself gets registered when built)
bugfix_252_disk_scheduler_blind    # docs-only container for the bugs_open/252 lane (SUMMARY series). The lane's reusable mechanism IS registered — BLD-021, the node-config DaemonSet (deployments/kustomize/services/node-config/) — and its bug evidence lives in bugs_open/252_*. This dir holds owner-facing summaries only; nothing here is callable. RE-CHECK THIS LINE if the dir ever grows a script another lane could run — that gets REGISTERED, not ratcheted.
inline_guide_imagery    # design-only lane as of 2026-08-14: the dir holds ONE plan (durable in-body imagery for generated guide/blog articles) and nothing has been built, so nothing is callable and there is nothing a sibling workstream could reach for. The lane exists because in-body imagery ALREADY WORKS and is simply not durable — a <figure> lives inside article-body's single llm-owned `content` field, so a prose rewrite destroys it silently (LANDMINES 2026-08-14; the correction at the foot of bugs_open/114; class is the save-REPLACES shape of 238/226/229, NOT 114's orphan class). RE-CHECK THIS LINE the moment any of the plan's phases ships: the recommended design is plan-as-truth — a locked site_plan_imagery section-scope row per figure consumed through the existing IMG-056 kind-alias resolver — and BOTH the opt-in component field that admits a figure AND any new discovery check that detects a vanished in-body image ARE reusable mechanisms other sites would call, so they must be REGISTERED, not ratcheted. The phase-3 mid-prose placement splice (style_hints.placement) is the same: a shared placement vocabulary is registrable.
bugfix_274_reply_envelope
webdesign_tool_rebuilds — site-specific lane (webdesign.co.uk ported-tool replacement via existing add_tool/tool-generator machinery); nothing reusable to register
bugfix_285_shared_template_write — bug-fix lane for bugs_closed/285 (tool-improver shared-template write). Its reusable pieces are registered under TL-042 (fence + status-update 2026-08-16); the writer-census coverage test (component_template_writer_coverage_test.go, council d8668e1f) is the second instance of the page_component_writer_coverage_test.go shape and is named in TL-042. Dir holds standing docs + seed 431's evidence only. RE-CHECK if D2(b) (per-instance writeback) is ever built — that is a new action capability and gets REGISTERED.
bugfix_280_decision_guards_chrome    # one-off: a bug-fix lane retyping an EXISTING query (bugs_open/280, sibling of bugfix_270_missing_structure). Nothing new is callable — no new exported symbol, no new action, no seam: storedPageAssemblySQL (already existed, already shared by check_decision_guards' Run and its verifier VerifyDecisionRegressionResolved) is retyped to read site_components instead of the vestigial pages.rendered_header/footer columns, same remedy shape 270 already used. Dir holds standing docs + council submission JSON only. RE-CHECK THIS LINE if the "read chrome from site_components for a site_id" pattern is ever extracted into a shared helper other checks call — that IS a reusable seam and must be REGISTERED, not ratcheted.
bugfix_282_validate_accepts_menu    # one-off DIR line only — the reusable mechanism IS registered, as PLAN-050 (validate_site_plan's `menu_field`: the acceptance surface consumes the planner's own component menu), in the commit that shipped it, per the platform-seams condition (2). The sensor keys on directory NAME, so this line covers the docs dir alone: it holds the standing five plus the council submission JSON. RE-CHECK THIS LINE if the offer-vs-accept pairing is ever generalised beyond validate_site_plan (e.g. a shared helper the gap-planner path also calls) — that IS a wider seam and gets a register entry of its own, not a ratchet line.
bugfix_297_tool_recreation_context    # one-off DIR line only — a bug-fix lane that REWRITES ONE EXISTING agent's SQL step (tool-recreation-handler.load_related_context, migration 453: drop LIMIT 10, LEFT JOIN LATERAL for one-row-per-page). Nothing new is callable: no exported symbol, no new action, no new config key, no seam. The reusable mechanism for this defect CLASS is already registered as LCO-009 (QueryDatabaseAction's row-cap WARN, committed eb137faed by the bugfix_275_silent_row_caps lane); this lane consumes it rather than adding to it. Dir holds the standing five plus the council submission JSON. RE-CHECK THIS LINE if the "newest research row per page" LATERAL is ever extracted into a shared helper other steps call, or if a column-truncation detector (LCO-009's stated blind spot) is built here — either IS registrable, not ratchetable.
loanzy_uk_example_site    # one-off DIR line only — a SITE lane, not a mechanism: one site built on loanzy.uk from a webdesign.uk customer prompt alone (owner ruling P10, 2026-08-18), to give webdesign.uk's approved lead the prompt+result pair its writer_block precondition requires. Nothing new is callable: no exported symbol, no action, no config key, no seam — it consumes the existing one-shot path (seed + 082_submit_domain_unified.sh) and changes nothing about it. Dir holds the standing five only. RE-CHECK THIS LINE if the lane ends up BUILDING the gallery mechanism (a prompt↔site pairing surface, or an intake step that dispatches a build from a chat brief — the chat box dispatches nothing today) — either IS registrable, not ratchetable.
bugfix_029_retry_kills_live_child    # PARTLY SUPERSEDED 2026-08-18: the retry-window policy this line anticipated IS built and IS registered, as RSH-010 (resilience-self-heal.md) — the condition below fired as written. This line now covers the DOCS DIR ONLY (the standing five + the 090 evidence). # one-off DIR line only — a bug-fix lane DIAGNOSING bugs_open/029 (the parent's retry replay wedging a still-running child; the retry window silently recomputed to 5 min, 3 min above a 30-min declaration). Nothing new is callable TODAY: no exported symbol, no new action, no new config key, no seam — the dir holds the standing five plus the 090 evidence. RE-CHECK THIS LINE THE MOMENT A FIX SHIPS: a retry-window policy that HONOURS a step's declared config.timeout_seconds, a liveness gate that suppresses a replay while the callee is demonstrably alive, or a re-drive/reaper arm for a row frozen in EXECUTING_STEP are each a SHARED SEAM on the coordinator's await path (blast radius: every pipeline that awaits anything) and get a REGISTER ENTRY plus architecture-scope review, not a ratchet line. This line covers the docs dir alone.
meta_description_never_backfilled    # one-off DIR line only — an INVESTIGATION lane, not a mechanism: it diagnosed why 407 of 731 live pages carry no meta_description (bugs_open/320) and BUILT NOTHING. No exported symbol, no new action, no config key, no seam; the dir holds the standing five only, and every fix candidate is deliberately left unactioned pending an owner decision (320 §8/§9, owner ruling 2026-08-06 — an absent generator is the finding to report, not a gap to fill). RE-CHECK THIS LINE THE MOMENT ANY CANDIDATE SHIPS: a `meta_description_missing` discovery check, a BACKFILL producer that writes the column on an existing page, or a guard on upsertPage's ON CONFLICT clause are each a SHARED SEAM (blast radius: every page-writing pipeline, and in the backfill case public copy on 26 sites) and get a REGISTER ENTRY plus architecture-scope review, not a ratchet line.
news_editorial_features    # one-off DIR line only — a COLLECTION lane, not a mechanism: stage 1 gathered the three prior designs for editorial/feature articles built from clustered news (027g's agent pipeline + event_timeline, 036b's readership segments + research/write/eval loop, features_open/001's shared-substrate/per-site-angle split) and BUILT NOTHING. No exported symbol, no new action, no config key, no seam; the dir holds the standing five plus the 090 evidence (run correlation 3802fb10). Everything it names as existing is ALREADY REGISTERED elsewhere — NEWS-001/005/006/009/010 (news-feed-pipeline.md), VIZ-001/002/003/006/007/009 (visualisation-and-charts.md), RES-002/003 (research-agents.md), TPI-001/002/003 (topic-intelligence.md). RE-CHECK THIS LINE THE MOMENT ANY OF STAGE 2 SHIPS: a cross-source story-grouping key (the writer duplicate_of and entity_ids have never had), a topic_packages or event_timeline table, a shared research substrate consumed by more than one site, or any publishing agent of the article-rewriter/feed-publisher/news-analyst family are each a SHARED SEAM (blast radius: every site drawing on a pool, and the claims gate for every figure a chart plots) and get a REGISTER ENTRY plus architecture-scope review, not a ratchet line.
bugfix_315_deployed_at_without_publication    # one-off DIR line only — an INVESTIGATION lane, not a mechanism (TODAY): it establishes that `pages.deployed_at` cannot be evidence of publication in ANY of its five stampers (2 of 5 stamp BEFORE the deploy is dispatched; the other 3 discard `deploy_result`), and that the two columns designed for deploy traceability — `page_components.deploy_commit` (0/1775) and `pages.content_hash` (0/786) — have NO writer anywhere in the repo including tests. It has BUILT NOTHING so far; the dir holds the standing five plus the 090 evidence. The correction it made to register DGH-001 (the false "commit SHAs are recorded on pages and work items" claim) is IN that entry, where a reader will meet it. RE-CHECK THIS LINE THE MOMENT ANY FIX SHIPS: writing a commit sha or content hash at the deploy seam, a `deployed_at` guard that reads the deploy result, a page-level served-bytes divergence check, or any change to `CommitToRepo`'s return contract are each a SHARED SEAM (blast radius: 19 `git_commit` steps across 16 agents, and every selector that reads `deployed_at` — `queryresolve.DeployedPageEligibilitySQL` and the discovery checks built on it) and get a REGISTER ENTRY plus architecture-scope review, not a ratchet line.
