# Register — public-api

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074). It said **1** and the file held **4**._ consolidated from 2 raw extractions (1 unique block, appearing twice
due to exact whole-block duplication in the cluster input file) across unit
U01. **Plus 2 post-freeze additions (PUB-002, PUB-003) made 2026-07-29** for the
two shared packages the consolidation programme built on 07-28 — both
council-approved, both with zero importers at the time of writing.

**Stage-2 duplicate resolution (2026-07-14):** PUB-001 is the same underlying
plan as `admin-dashboard-and-api.md`'s ADM-007 + ADM-008, extracted under a
distinct category tag (`new:public-api`) because P2 (its source doc) is a
later/live version of the plan that ADM-007/008's source (`007b_public_api_plan_v2.md`)
was an earlier snapshot of. Full entry retained below for its distinct source
citation (P2 vs 007b); treat ADM-007 (endpoint plan) + ADM-008 (site_ownership
table) as the canonical pair — this entry is the pointer.

### PUB-001 — Public API plan: site_ownership junction + user-facing build/HITL endpoints (duplicate — see ADM-007 + ADM-008)
- **status:** aspirational
- **status-evidence:** P2 is an implementation plan (blocks 0–6, build order); Block 3 admin subset "implemented" per its own notes — the user-facing half was never built.
- **stage2-verified (2026-07-14):** confirmed unbuilt (0 hits for site_ownership or /api/v1/sites in any .go/.sql file) and confirmed duplicate of ADM-007 + ADM-008 — see those entries for the canonical write-up.
- **what:** `site_ownership` junction table (site/client/user/role) rather than columns on `sites` (shared sites; 15+ FKs untouched); all public queries scope through it. `POST /sites` writes build_queue + ownership (seed picks it up; 409 on existing). Endpoints for sites/status (work-item progress rollup), pages, work items with the HITL review flow (needs_human_review → provide-data-and-retry / retry / dismiss; retry converts to content_rewrite), specs read+write, assets, briefing HTTP-to-Kafka bridge, WebSocket build events.
- **sources:** P2_public_api_plan.md (full, docs024_key_docs_latest)
- **relations:** duplicate-of: ADM-007 (admin-dashboard-and-api.md, public REST API plan), ADM-008 (admin-dashboard-and-api.md, site_ownership table). admin API (ADM-002); needs_human_review status; build_queue.
- **verify-later:** site_ownership table; which blocks landed

---

*Added 2026-07-28/29 by the consolidation programme (`features_open/024` A2/A3),
post-dating the 2026-07-13 extraction freeze. Both packages were built and
council-approved on 07-28 and neither was registered at the time — the omission
this file's own banner and `bugs_open/106` exist to catch, recorded here rather
than left as folklore.*

### PUB-002 — `platform/httpguard`: inbound-abuse primitives for public HTTP endpoints, with the trusted proxy stated per deployment
- **status:** built, unit-tested, council-approved (`6db59c8b`) — ~~**and called by NOTHING.** Zero importers, re-measured 2026-07-29.~~ **ADOPTED 2026-07-31: it has its first real consumer, and the adoption is LIVE and PROVEN in production.** Owner-ruled remit ("get it adopted", 07-29) is discharged for one of the two named call sites' services.
- **status-evidence:** ~~returns only the package itself~~ **re-measured 2026-07-31, as this entry's own instruction requires: `grep -rn 'agentchassis/platform/httpguard' --include=*.go . | grep -v '^./platform/httpguard/'` returns `internal/tools-api/clientip/clientip.go:59`.** Live proof, island `aqls/tools-api:v1.0.1207` swapped 2026-07-31 08:37Z: a real `/round` through Cloudflare stored `client_ip_hash = 9e464fe9fca925b099a25141f40afad5`, which is `sha256(<the caller's own egress address, as CF reported it>)[:16]` **computed independently before the request was sent** — so the value did not merely change, it changed to the one predicted. `count(DISTINCT client_ip_hash)` on `gauntlet_rounds` went **1 → 2** (95 prior rows all being the docker-gateway constant `245c0ffc…`). Pre/post on the RUNNING container with controls in both directions: `CF-Connecting-IP` and `cloudflare-tunnel` were **0/0 before** and **1/1 after**, `gin-gonic` 1186 before (so the zeros were real absences, not a broken `strings`), nonsense token 0 after. `go test ./platform/httpguard/` still passes.
- **what this settles that unit tests could not:** the `open review question` below asks whether `CF-Connecting-IP` genuinely reaches an adopting app process. **It does** — that was `[INFERRED]` in `bugs_open/139` because nobody would add a header-echo endpoint to another team's service, and it is now measured at the artefact instead. Note what is still NOT closed: the peer-gate reversion on a real *public* peer remains structurally unclosable locally, exactly as recorded below.
- **what:** One shared implementation of three things that had diverged across the estate: `ClientIP` (a per-client key the client cannot choose), `Limiter` (in-memory sliding-window, multi-band, returns retry-after — replaces a single token bucket that could express only one ceiling and no retry-after), and `CheckIntake` (honeypot + client-side timing gate, fail-open on missing JS). net/http only, no gin, so a gin service writes a three-line adapter. Written because three per-IP limiters existed and **the weakest guarded the only public endpoint**, and four different CORS postures existed.
- **the seam, and the landmine it exists to close (2026-07-29):** `ClientIP` takes a **required `FrontEnd` argument** naming which headers *this deployment's* proxy is known to **write** (as opposed to merely forward). Pre-declared: `Nginx()`, `CloudflareTunnel()`, `Direct()`. It previously hard-coded nginx's rules — prefer `X-Real-IP`, else the rightmost `X-Forwarded-For` — justified in its own docstring by nginx's `proxy_set_header` behaviour. **That justification is false on the estate's other front-end.** Measured 07-29: Caddy does not set `X-Real-IP` and forwards a client-supplied one verbatim, and it *overwrites* `X-Forwarded-For` with its own peer instead of appending. So on the island both old rules resolve to either user input or a constant, and adopting the old default into `tools-api` would have keyed every visitor on the docker bridge gateway — **83 of 83 rows, one distinct value** — while reading like a fix. The argument is required precisely so that a caller cannot inherit an assumption it never stated.
- **open review question:** the peer gate (`loopback || RFC1918`) is what keeps `CloudflareTunnel()` honest — trusting `CF-Connecting-IP` is trusting Cloudflare to be in front, and the gate is what makes the header revert to being ignored if the origin is ever reachable without the tunnel. Nobody has tested that reversion against a real direct-exposure path, only in unit tests. **The `architecture` seat ruled on this at review (2026-07-29, corr `49392838`, severity low): *"Fine to ship, but the open question should not go stale - the next thing to land against this package should close it, not add a fourth FrontEnd."* Treat that as the package's next obligation, ahead of any new front-end.**
  - **DISCHARGED as far as it can be, same day, and the residue is stated rather than left implied.** `TestPeerGateBoundaryIsStatedNotAssumed` now pins where the gate actually falls across ten realistic address forms, and `TestClientIPParsesTheRemoteAddrARealSocketProduces` drives a real `httptest` socket so the peer is parsed from what the *runtime* puts in `RemoteAddr`, not from a hand-typed string — the case where a bug would really hide, since a leaked port or bracket would fragment every limiter bucket per-connection. Both were verified to FAIL under targeted mutation (removing `IsPrivate` from the gate; returning `RemoteAddr` verbatim), so neither is a check that cannot fail.
  - **What remains structurally unclosable locally:** a genuine connection from a real *public* peer. Every address a dev machine can bind is loopback or RFC1918 and so lands on the trusted side of the gate by construction. It can only be closed by a real direct-exposure deployment, and that is a deployment question, not a test one.
  - **Boundary fact worth knowing before adopting:** Go's `net.IP.IsPrivate` covers RFC1918 and RFC4193 **only**, so a proxy behind **CGNAT (`100.64.0.0/10`)** or on a link-local address is **NOT** trusted and its headers are ignored. That fails in the safe direction — a coarse key rather than a spoofable one — but it is a real constraint on where this package can be deployed, now pinned by test rather than discovered in production.
- **council (round 1, 2026-07-29, corr `49392838-5ada-4c8e-baeb-94b01e5855b4`):** **APPROVED** — *"approved with 1 advisory objection(s) — none high-severity"*. 9 seats fired, 8 abstained on relevance, none unreadable. The `architecture` seat returned `ARCHITECTURE_SIGNAL: point_fix`, explicitly settling the venue question the submission raised itself: *"hardening a shared mechanism's own contract before its first real consumer, which is the cheapest point in its life to do it, not a shared-mechanism change smuggled in via a symptom fix."* Two medium objections, both answerable with evidence rather than argument, and both answered: `guardian` asked that the register entry be **confirmed** to ship in the same commit rather than assumed — it did, `31c684124` carries this entry and the code together; `prior_art_librarian` asked that the load-bearing zero-importer claim be re-grepped at merge time rather than trusted from the submission — re-run after the verdict, still zero. `reuse_agent` left a standing question this entry inherits: whether the three divergent per-IP limiters httpguard was built to replace are actually decommissioned. ~~**They are not — that is what adoption means, and it has not happened.**~~ **SPLIT IN TWO by the 07-31 adoption, because the original sentence conflated them.** (a) *Adoption* has happened, for one service: `internal/tools-api/clientip` calls `ClientIP` and it is live and proven. (b) *Decommissioning* has **not**, and the adoption did not attempt it — what changed is the **key** the existing token bucket is keyed on (`middleware/ratelimit.go:30`, `handlers/round.go:109`), not the limiter implementation. `httpguard.Limiter` still has **zero** consumers, and all three divergent limiters still exist. Measured 2026-07-31: `grep -rn 'httpguard\.\(Limiter\|NewLimiter\|CheckIntake\)' --include=*.go . | grep -v '^./platform/httpguard/'` → **nothing**, and `middleware/ratelimit.go:9` still imports `golang.org/x/time/rate` with its own `map[string]*rate.Limiter` — the diff swapped `c.ClientIP()` for `clientip.From(c)` at `:33` and nothing else. So `reuse_agent`'s question is still open on its own terms, and the honest reading is that `ClientIP` is adopted while `Limiter` and `CheckIntake` are not. **Do not cite the 07-31 proof as evidence for the whole package.** **UPDATE 2026-08-16 (PUB-005): `Limiter` and `CheckIntake` now HAVE a consumer in code — `internal/tools-api/middleware/bands.go` (the three-line gin adapter, per-route bands) and `handlers/gripper.go` (`/submit`). Not yet live. The gauntlet group still runs `middleware/ratelimit.go`'s token bucket; decommissioning it is still not attempted.**
- **sources:** `platform/httpguard/{clientip,limiter,intake}.go` + `httpguard_test.go`; `features_open/024` A3; `bugs_closed/090` (the proven production incident it hardens against); `bugs_open/139` (the measurement that produced the seam); `016b` §9 *"Who is the client" is decided by the PROXY CHAIN*.
- **relations:** replaces-on-adoption: `internal/tools-api/middleware/ratelimit.go` (token bucket keyed on gin's `c.ClientIP()`), idea.uk's banded limiter + honeypot/timing gate. Adoption into `tools-api` is the **gauntlet_dead_cta** thread's service — see `gauntlet_dead_cta/CONTRIB_2026-07-29_…`. Sibling: PUB-003.

### PUB-003 — `platform/mailer`: the first SMTP sender inside the built code
- **status:** built, unit-tested (8 tests), council-approved (`6db59c8b`) — ~~**and called by NOTHING.** Zero importers, re-measured 2026-07-29.~~ **FIRST CONSUMER WRITTEN 2026-08-16 (PUB-005): `internal/tools-api/gripper/poller.go` depends on `mailer.Sender`; `cmd/tools-api/main.go` constructs `mailer.New(cfg.Gripper.SMTP)` from `FromEnv("GRIPPER_SMTP")`. In code and tested with a recorder Sender; NOT yet live (island swap pending). Re-measure with the grep before citing either state.**
- **status-evidence:** before it landed, `grep -rn "net/smtp" --include=*.go platform/ internal/ cmd/` returned **nothing** — there was no mailer anywhere in the code we build and deploy. `send_notification` (`basic_actions.go:134`) is not email; it produces a Kafka message.
- **what:** SMTP send promoted into `platform/` so that "we email you a link" journeys stop depending on code outside the build. The only working sender was in idea.uk's VM app in the docs tree — outside `go build`, untested by CI, undeployable by the image pipeline.
- **landmine carried from the operational record:** see `email-infrastructure.md` — cloud boxes generally cannot use outbound 25/465, Go's `smtp.SendMail` does STARTTLS not implicit TLS, and shared-host relays content-filter legitimate transactional mail. Those constraints decide deployment, not this package.
- **sources:** `platform/mailer/mailer.go` + `mailer_test.go`; `features_open/024` A2.
- **relations:** operational constraints: `email-infrastructure.md`. First queued consumer: the gripper dossier's public half (`robot_hands_gripper_dossier/`). Sibling: PUB-002.

### PUB-004 — round publication: tools-api's first visitor-consented public read surface
- **status:** **LIVE, PROVEN, and now WIRED END TO END** on island `aqls/tools-api:v1.0.1216` (owner authorised the swap, 2026-07-31 ~15:30Z). Council APPROVED round 1. Added 2026-07-31.
  - **Updated 2026-07-31 evening — the consumer exists and the loop closes.** The line above used to end *"**Nothing is published** — the public record is empty by design and the front end does not call these yet."* That is now false and is the whole point of the change: the vonc share button calls `POST /publish`, and the record page `/tools/gauntlet/round.html?r=<slug>` (component `gauntlet-round-record`, `sql_for_agents/279`) reads `GET /round/:slug`. Proven by `drive_publish_on_share_2026-07-31.py`: one real round played in a browser, publish 200, slug `gzhhgjamdn`, a **second press returning the same slug**, and the record page rendering the position and defence byte-for-byte as typed. Three of our own harness rounds are published; no stranger's writing is public.
  - **The card carries the address, and that is checked at the CANVAS.** `buildVerdictCard(permalink)` prints the permalink as the card's last line. Verified by replacing `CanvasRenderingContext2D.prototype.fillText` and reading back every string drawn — so "the permalink is on the card" is a substring test over what the renderer emitted, not an OCR result or a pixel-width proxy. That log is also what caught the card being drawn **twice** per press (the is-there-a-round test was building and discarding a whole canvas); `roundIsComplete()` replaced it, 24 `fillText` calls down to 12.
  - **Consent is delivered where it cannot drift from behaviour.** The button label and the note naming what becomes public are written in the JS, beside the `addEventListener`, NOT in `html_template`. The page and the asset are cached separately, so a visitor can hold an old page with a new script — with the label in markup, that visitor would see "Save this verdict as a card" on a button that publishes.
  - **Publish precedes draw, deliberately.** The reverse order prints an address on a card before the address is known to exist, so a failed publish yields an image pointing at a 404. On failure the card is still drawn and saved, without a link, and the visitor is told.
- **live proof (what makes this PROVEN rather than deployed):**
  - **Running binary is byte-identical to the built one**: `sha256 7b8abbb419b1ff9a2f83f1e175795e2f335902ffd5d1ea3142e374d9b77f4c49` from `docker exec island-tools-api-1 sha256sum /tools-api` equals the local extract. Image ids are not portable across `save|load`; the binary hash is.
  - **The shared CORS change verified at the ARTEFACT, not by grep**: a live `OPTIONS` preflight returns `access-control-allow-methods: GET, POST, OPTIONS`. ⚠ **The in-container grep DISAGREED and was wrong** — busybox `grep -a -c` inside the alpine image finds `Access-Control-Allow-Methods` but returns **0** for `GET, POST, OPTIONS`, while GNU grep finds both in the *same bytes* (sha256 identical). Same family as the recorded `strings`-missing landmine: **a container's grep is not a reliable binary-inspection tool, and it fails in the direction that reads as "my change did not ship".**
  - **Every negative path, live**: nonexistent slug → 404 `no such round`; malformed slug → 404 (before any DB round-trip); `{}` → 400 `round_id is required`; `not-a-uuid` → 400 `round_id is not a valid id` (the 22P02-would-have-been-a-500 fix); unknown uuid → 404 `round not found`; **round with no verdict → 409 `round has no verdict yet`** (the completeness guard).
  - **Idempotency proven, not asserted**: two presses of publish on the same round both returned `200` with the **same** slug `ny_rf4adv5` — the property the card depends on.
  - **The read serves the whole debate and leaks nothing**: 4,169 characters across provocation/position/counter/defence/verdict, and the served keys are exactly `slug, published_at, argued_at, provocation, position, counter, defence, verdict` — **`client_ip_hash`, `site_id`, `id` and `tool` are all absent**, which is what the separate `PublicRound` type is for.
  - **The gate proven in the NEGATIVE direction**: after setting `published_at`/`public_slug` back to NULL, **the same slug returns 404**. A pass on "published rounds are readable" does not prove unpublished ones are not; this does.
  - **No regression to the game**: `POST /round` still returns a real `round_id` + provocation.
  - **Test residue reverted deliberately**: the happy path was proven on one of *our own* harness rounds (not a stranger's writing) and then unpublished, so `count(*) WHERE published_at IS NOT NULL` is back to **0**. The public record still starts empty.
  - **The rate-limit objection got live confirmation**: a verification burst of six requests hit **429 `rate limit exceeded`** on the sixth. The ceiling (1 rps / burst 5) is real and per-caller — and easily reached, exactly as four seats warned.
- **status-evidence:** `go build`/`vet`/`test ./internal/tools-api/...` clean. Router registration asserted by test (`api/server_test.go`) because gin panics at *registration* on a route conflict and `GET /round/:slug` sits beside the existing `POST /round` — that panic would be a service that will not boot, discovered after a swap. All four SQL statements **PREPAREd against the live island schema** (`go build` cannot parse SQL); the UNIQUE index proven real by forcing `23505` inside a rolled-back transaction. Migration 276 self-asserts its end state and is ledgered in `island_migrations`.
- **what:** Two endpoints plus two columns that turn a completed gauntlet round into a public, linkable record. `POST /api/v1/tools/gauntlet/publish {round_id}` → `{slug, path}`, idempotent (a second press returns the FIRST slug, because the card the visitor already holds carries that URL), site-scoped, and refusing a round with no verdict as **409 not 404** (the round exists; the front end must not tell the visitor it is gone). `GET /api/v1/tools/gauntlet/round/:slug` serves it read-only with no LLM call — the only cheap route on this service. Columns: `published_at` (NULL = private; records *when*, which a boolean cannot) and `public_slug` (a 10-char crypto/rand token, UNIQUE).
- **the guarantee it adds, stated plainly:** before this, nothing in tools-api served stored data to an unauthenticated reader. Now one row class can be read by anyone holding its slug — **but only after the visitor who wrote it pressed share.** The gate is `published_at IS NOT NULL` in the read query itself, and nothing except `PublishRound` sets it.
- **why the projection is its own type:** `store.PublicRound` is deliberately NOT `store.Round`. Adding a column to `gauntlet_rounds` therefore cannot silently widen what the public endpoint serves. `client_ip_hash` and `site_id` are absent by design.
- **landmine — one definition, not two:** `SlugAlphabet`/`SlugLength`/`ValidSlug` all live in `store` because the generator and the validator were briefly two definitions (a regexp in the handler spelling out the same characters). Drift between them fails **silently and totally** — widen the alphabet and every newly published round 404s on read while both halves still read as obviously correct. `TestNewSlugIsAlwaysAcceptedByValidSlug` pins the round trip; the 32-character length is load-bearing (256 % 32 == 0, so byte reduction is unbiased) and `TestSlugAlphabetDivides256` fails if anyone widens it. Both verified to fail under mutation.
- **shared-mechanism change, declared:** `middleware/CORSMiddleware` now advertises `GET, POST, OPTIONS` instead of `POST, OPTIONS`. Additive and inert for existing consumers — a simple cross-origin GET is not preflighted, so the endpoint would work without it; it is there so the next person adding a header to that fetch does not hit a preflight failure three files away. **Consumers measured, not assumed:** the island's CORS allowlist contains exactly one site (`vonc.com`, `status='deployed'`), and every caller of `tools.apis.uk` in the tree is this lane's own file. Told rather than merely measured: `features_open/024` holds the standing interest in this package (`middleware/ratelimit.go`) — different file, no conflict.
- **consent design, and what is NOT built:** the owner ruled 2026-07-31 that pressing share publishes, so **the button's wording is the consent mechanism** and must say so plainly. There is deliberately **no unpublish** — flagged as an owner decision, not an oversight. No backfill: measured at apply time, 98 rows / 53 complete / 2 distinct `client_ip_hash`, both ours, so seeding the record from stored rounds would manufacture a crowd from test data.
- **open review question:** whether a per-round record should be reachable by a *guessable* handle at all. 32^10 ≈ 2^50 makes enumeration impractical, and unpublished/nonexistent slugs answer identically so the endpoint cannot confirm a private round exists — but if publication ever becomes the default, that reasoning needs revisiting rather than inheriting.
- **council (round 1, 2026-07-31, corr `a24a754b-4834-4f78-9271-c76d052551ff`):** **APPROVED** — *"approved with 5 advisory objection(s) — none high-severity"*. Every objection named a claim the submission had **asserted rather than checked**, which is fair, so all five were answered with evidence afterwards rather than left as disclosure:
  - **Rate-limit posture on a public read surface** (raised by four seats independently — the strongest objection, and correct that the plan was silent). Answered: the limiter **does** cover both new routes, because `RateLimitMiddleware` is registered on `apiGroup` (`server.go:38`) before any route in it, and since PUB-002's adoption it keys on `clientip.From(c)`, i.e. the real caller — not the one global bucket the `ratelimit.go` landmine warns about. **The residual the seats were right to point at:** the ceiling is `RATE_LIMIT_RPS=1.0` / `RATE_LIMIT_BURST=5` (defaults — verified with `printenv` on the running container and against the compose env block: **no override is set**), and those numbers were chosen for 8–18s LLM POSTs, not for anonymous reads. One reader opening one record is comfortably inside it; rapid refreshes are not. **Recorded as a known ceiling rather than changed blind** — retuning a shared limiter for a route that has never served a request would be guessing.
  - **"tools-api's FIRST public read surface" was asserted, not verified.** Now checked structurally: `GET /health` is registered on the engine at `server.go:27`, *before* `apiGroup` exists at `:32`, so it carries neither `CORSMiddleware` nor the limiter. `GET /round/:slug` is genuinely the first GET inside the CORS group, so "additive and inert for existing consumers" stands on a check.
  - **No prior-art search shown for a short-token generator.** One was run before writing it (`grep -rn "func.*[Ss]lug\|randomToken\|RandomString"` across `platform/ internal/ pkg/`): the hits are name-*slugifiers* (`slugOf`, `slugifyForCompositionName`, `slugifyPathSegments`) and one password generator in `auth-service` whose `crypto/rand` failure path returns a **fixed string** — deliberately not reused, because tools-api fails loud and a predictable token would be indistinguishable from a good one in the column. Evidence existed; it belonged in `grounded_in` and was not there.
  - **No precedent check on `middleware/cors.go`.** Run afterwards: `diagnosis_artifacts kind='council_report' AND body ILIKE '%middleware/cors.go%'` returns exactly **1** row — *this* report, i.e. the objection's own text. There is no prior council precedent on the file, and the check that proves it is the loop closing on itself.
  - **`CREATE UNIQUE INDEX` without `CONCURRENTLY` takes a lock.** True and harmless here (98 rows), but the seat is right that the pattern travels: **anyone reusing migration 276's shape against a large table should use `CONCURRENTLY`** (and note it cannot run inside the `DO` block's transaction).
  - **No pod-level deploy verification listed.** Correct, and it is the step that is genuinely still outstanding: the binary was grepped *before* shipping (`v1.0.1216`, 4 new strings present, 2 positive controls present, negative control absent — the first attempt returned all zeros *including the controls*, because binary grep needs `-a`). The **running-container** grep and the `sha256sum` comparison happen after the swap, which was refused by the permission classifier and is the owner's call.
  - One correction the seats could not make, because it concerns their own input: a seat questioned whether `status='deployed'` came from the platform `sites` table (informational, and a known unsafe filter). It did not — the count is from the **island's own minimal `sites` table**, queried directly on the island, which is the CORS allowlist `ActiveSiteByOrigin` reads.
- **sources:** `internal/tools-api/store/rounds.go` (`PublishRound`, `GetPublishedRound`, `PublicRound`, `ValidSlug`), `internal/tools-api/handlers/publish.go`, `api/server.go`, `middleware/cors.go`, `sql_for_agents/276_tools_api_round_publication.sql`; ruling + measurement in `gauntlet_dead_cta/PLAN_2026-07-22_…` § *OWNER DIRECTION 2026-07-31*.
- **relations:** consumer is the vonc gauntlet share card (step 1 live 2026-07-31) and the record page it links to. Runs on the same service as PUB-002's adopted `ClientIP`. Sibling of the `gauntlet_rounds` table from migration 198.
- **two 403s that are indistinguishable by status code** (found wiring the consumer): a request with no `Origin` is refused by *our* CORS middleware with `{"error":"origin not allowed"}`; a request with no browser `User-Agent` is refused by *Cloudflare* with `error code: 1010` and never reaches the island. `curl` and `urllib` need **both** headers. Read the body before concluding the endpoint refused you.
- **the record page's own guarantees** (component `gauntlet-round-record`): every field is written with `textContent`, no allowlist and no markup-assigning property anywhere in the file — two of the fields are prose typed by a member of the public and served to every other visitor. It renders `provocation.headline` and `body` only; the same jsonb carries `stats` (an array of numbers), `primary_cta` and `secondary_cta`, and a number the page did not compute is what the honesty rail forbids. `round_record/verify_round_record.py` asserts all of that mechanically **and cross-checks the slug alphabet and length against the Go constants**, so the two implementations of `ValidSlug` cannot drift apart across languages.

### PUB-005 — the `gripper` route group in tools-api: chat intake + email delivery, and the second tool on the island
- **status:** **BUILT, unit-tested and integration-tested against a real Postgres (migration 436 applied to a throwaway postgres:16 twice — idempotent — and the store walked end to end), smoked as a real process; NOT YET SHIPPED to the island (image not built/swapped, `/opt/island/.env` not populated, seed 208 not applied, pull task still disabled).** Added 2026-08-16. Council: submitted alongside the commit (`Council-Submitted:` trailer).
- **what:** `internal/tools-api` gains a second tool at `/api/v1/tools/gripper` (proposal: `robot_hands_gripper_dossier/PROPOSAL_2026-08-05_…`; behaviour: DESIGN §2/§5). Four routes: `POST /session` (fixed greeting, no LLM), `POST /chat` (one Haiku call per turn via `aiservice.GenerateText`, reply parsed strictly, spec merged SERVER-side with jsonb `||`, `missing_fields`/`complete` computed by the server — the model's own `complete` flag is ignored), `POST /submit` (honeypot + timing gate FIRST via `httpguard.CheckIntake`, byte-identical `201 {"accepted":true}` for bot and human, request filed from the SESSION's spec never the client's, or from an inline spec in the widget's plain-form fallback), `GET /requests?since=` (the cluster's NDJSON pull, `X-Internal-Key`, **no CORS**). Plus a poller goroutine (60 s tick) that reads `https://<domain>/reports/<uuid>.json`, emails the link on `ready` (`platform/mailer`, first real consumer) or an apology on `failed`/24 h expiry, with `email_attempts` claimed BEFORE each send so a crash mid-send costs one retry, never a stream of duplicates. Three tables on the island DB (`436_tools_api_gripper_intake.sql`): `gripper_chat_sessions`, `gripper_report_requests`, `gripper_daily_turns` (the global 2,000/day cap is a conditional upsert). Feature is **opt-in by env**: no `GRIPPER_ANTHROPIC_API_KEY` → the group is not mounted and the binary boots exactly as before; with it, `GRIPPER_PULL_KEY` and `GRIPPER_SMTP_HOST/_FROM` are required (fail-loud). `cmd/tools-api/main.go` moved from `r.Run` to `http.Server` + `signal.NotifyContext` so the poller can stop cleanly.
- **the seam a third tool must copy correctly — TWO gin groups on ONE prefix.** Browser routes sit behind `CORSMiddleware` (Origin → island `sites` row, `status='deployed'`) then a per-route `httpguard.Limiter` (6/h+20/d, 60/h+200/d, 3/h+10/d — the DESIGN bands, which the gauntlet's flat RPS bucket cannot express) then the group's OWN body cap; `GET /requests` sits in a second group behind `middleware.InternalKey` INSTEAD of CORS, because a server-to-server GET carries no Origin and CORS would 403 every pull. Merge them and either the cluster is locked out or the intake is open to any page on the internet. See LANDMINES.
- **the field vocabulary is the CLUSTER's, not the design doc's — measured, and it changes what the chat records.** DESIGN §2 named `payload_kg / part_surface / environment / notes` and §5.3 said the mapping to physics inputs would happen "cluster-side, in score_grippers input handling". Live 2026-08-16: the `report-builder` workflow's `load_request` step reads `spec->>'mass_kg'`, `'travel_mm'`, `'surface_material'`, `'ip_min'`, `'cycle_rate'`, `'mounting'`, `'part_geometry'`, `'application'` (query quoted in `gripper/spec.go`), and the work-item spec IS the island's spec verbatim (`report_request_pull_action.go` adds only `request_id`/`submitted_at`). No renaming step exists between. So `internal/tools-api/gripper.Fields` uses the cluster's names from the first turn, with `surface_material` a closed enum matching `gripperMaterialAliases`, and `TestFieldNamesAreTheClustersVocabulary` pins it. A spec in the design's vocabulary would arrive, be ignored, and fail scoring on "mass_kg is required".
- **the island's `sites` table has NO `deploy_config`** (it is the minimal id/domain/status table from `island_db_prep.sql`), so proposal §4's "check `X-Internal-Key` against `sites.deploy_config->'report_island'->>'pull_key'`" cannot be done there. The key is env (`GRIPPER_PULL_KEY`), and it must equal what seed 208 writes on the cluster — two places, by hand, exactly as 208's own header always said. 436 also seeds robot-hands.com into the island `sites` (real cluster id `00ff3af5-dad8-4770-9f70-3edc267a3c92`, `deployed`) so CORS can resolve it.
- **what is deliberately NOT built:** DESIGN's `output_config` json_schema forcing (aiservice has no system prompt and no schema field — containment is `gripper.ParseReply`: one JSON object, `DisallowUnknownFields`, no trailing content, typed/enumerated spec, reply capped) · a retry on 429/5xx from Anthropic (aiservice returns a flat string error; one 503 to the widget) · the site-side widget and `/gripper-report/` page (separate lane deliverable) · a `host` filter on `/requests` (single report-island site today; a second one on the same island would cross-wire — recorded, not fixed).
- **email copy** (`gripper/email.go`) is a first draft written with the behaviour, NOT owner-reviewed — no copy existed anywhere in the lane's docs. Cheap to change; review before launch.
- **sources:** `internal/tools-api/gripper/{spec,prompt,email,poller}.go` (+ tests, incl. a fake sidecar host and fake store), `internal/tools-api/store/gripper.go` (+ `gripper_integration_test.go`, gated on `TOOLS_API_TEST_DATABASE_URL`), `internal/tools-api/handlers/gripper.go` (+ tests), `internal/tools-api/middleware/{bands,internalkey}.go`, `internal/tools-api/api/server.go` (`mountGripper`), `internal/tools-api/config/config.go` (`GripperConfig`), `cmd/tools-api/main.go`, `sql_for_agents/436_tools_api_gripper_intake.sql`, `sql_for_agents/208_…` (base_url corrected 2026-08-16), island `docker-compose.yml` (env block, `:-` defaults), `RUNBOOK_island.md` "Tenant 2".
- **relations:** consumes PUB-002 (`Limiter` + `CheckIntake` — their FIRST consumers; `ClientIP` via `clientip.From` as before) and PUB-003 (`mailer` — its first consumer). Cluster half: `report_request_pull_action.go` (`pull_report_requests`), `emit_report_status_files`, `create_report_page`, `score_grippers`, seeds 209/210. Sibling: PUB-004 (same service). Lane: `robot_hands_gripper_dossier/`.

### PUB-006 — the `playground` route group in tools-api: a streamed chat against a SELF-HOSTED model, and the third tool on the island
- **status:** **LIVE on the island since 2026-09-03 19:08:49Z** (`aqls/tools-api:v1.0.1359-playground`, from `9b540c2e6`; council APPROVED corr `63be72d1`, round 4, all reviewers). Proven from outside: 200 text/event-stream with `token`/`done{truncated:false}` events, cold 3.85 s / warm 0.96 s, 403 on a wrong Origin, 204 preflight, 400 on an empty transcript; sibling tenants alive through the restart. ~~NOT SHIPPED: …~~ (the earlier status; every item of the deployment contract below is now ✅). No page calls it yet — the widget on `/playground.html` is Phase P step 3, and the library `chat-input-box` named below turned out NOT to fit (single-turn, same-origin, JSON; see `finetuning_uk_service/NOTES` 2026-09-03 16:45Z).
- **what:** `POST /api/v1/tools/playground/chat` — one browser group, no cluster-side pull, so no internal-key group. Stateless: the browser holds the transcript and posts it back (≤12 messages, ≤1000 runes each, roles `user`/`assistant` only, last from the user; a visitor cannot inject a `system` role). Calls Ollama `/api/chat` with `stream:true`, a fixed system prompt, and `num_predict`/`num_ctx` from config, then relays NDJSON as server-sent events: `event: token` per fragment, `event: done` carrying `eval_count`/durations/`done_reason`/**`truncated`** (`done_reason=="length"` — a capped reply must not read like a finished one), `event: error` in-band once the stream has started (status 200 is already committed). Errors before the first token are ordinary JSON 400/503.
- **opt-in twice over:** `config.Playground` is nil unless `PLAYGROUND_OLLAMA_URL` is set (plus `PLAYGROUND_MODEL`, `_MAX_TOKENS` 150, `_NUM_CTX` 2048, `_MAX_BODY_BYTES` 8192, each validated positive at load), AND `main` must pass `api.WithPlayground(deps)`. Either absent and not one route is registered — asserted by `TestPlaygroundRoutesMountOnlyWhenConfiguredAndWired` over all four combinations.
- **why not `platform/aiservice`:** `OllamaClient.GenerateText` posts `/api/chat` with `"stream": false` (`ollama.go:72/78/130`) and returns one string; there is no streaming method on any aiservice type (grep, 2026-09-03). The demo's product is tokens at reading speed on a CPU that does **~14 tok/s** (`finetuning_uk_service/NOTES` 2026-09-03), so a 150-token reply is ~11 s — a single JSON body reads as a stall. Extending a chassis-shared package for one island-side consumer was the wider blast radius.
- **why not `box/chat-service`** (the webdesign.uk visitor chat): it is an Anthropic-backed INTAKE chat (spec → email), a systemd service per VM-hosted domain reading `/etc/sitechat/<domain>.env`, with server-side transcripts. finetuning.uk is cluster-served with its public surface on the island (no VM), the demo is stateless, and the product being demonstrated IS the self-hosted model — a paid-API chat would demonstrate the wrong thing. What IS reused is the front half: `content_components.chat-input-box` (`d6a8f57b`, tags chat/intake/backend/requires-backend) is the planned fork for the page.
- **the deployment contract, 4 of 4 done (2026-09-03 19:08Z):** ✅ island `sites` row (migration `737_…_ISLAND.sql` applied and ledgered) · ✅ the five env keys in `/opt/island/.env` **plus the Tenant 3 `environment:` block in the compose file — the compose maps env explicitly, so keys in `.env` alone leave the group "NOT mounted" (cost one restart; LANDMINES)** · ✅ image swap (`make build-tools-api-ref IMAGE_TAG=v1.0.1359-playground`, then `docker save | ssh | docker load`; the restart also serves robot-hands.com + vonc.com) · ✅ **a reachable model server: Hetzner `167.233.33.159` (relojistas), Ollama 0.33.2, model `finetuning-demo`, open to the island's `176.126.243.183` only on a default-deny box; measured 38–42 tok/s, ~3× the cluster's 14, warm reply 1.34 s; island→model `/api/tags` 200 in 0.036 s, third party refused with a live control.**
- **superseded deployment note:** island `sites` row (migration `737_…_ISLAND.sql`) or every browser call 403s · the five env keys in `/opt/island/.env` · image swap in `/opt/island/docker-compose.yml` · a reachable model server. `[MEASURED 2026-09-03]` the island is 1 vCPU / 1 GB and cannot host a 1.7B model beside tools-api, and it cannot reach the cluster's Ollama (cluster DNS does not resolve there; `162.209.114.65:11434` refused) — so the demo needs its own small host.
- **post-ship probe** (debug_historian, round 4 — a route-path literal can be split by the linker; use a SYMBOL): `docker exec island-tools-api-1 grep -ac 'PlaygroundChatHandler' /proc/1/exe` with `GripperChatHandler` as the present control and an impossible string as the absent control (locally: 2 / 2 / 0). Then `curl -H 'Origin: https://finetuning.uk' -X POST …/api/v1/tools/playground/chat` expecting `200 text/event-stream`, and a wrong Origin expecting 403.
- **sources:** `internal/tools-api/handlers/playground.go` (+`playground_test.go`), `internal/tools-api/api/server.go` (`mountPlayground`, `WithPlayground`, `PlaygroundDeps`/`PlaygroundLimiters` 60/h+300/d), `internal/tools-api/config/config.go` (`PlaygroundConfig`, `loadPlayground`), `cmd/tools-api/main.go`, `docs/agent_docs/sql_for_agents/737_tools_api_island_sites_finetuning_uk_ISLAND.sql`, `finetuning_uk_service/PLAN_2026-07-31` Phase P.
- **relations:** sibling PUB-005 (the two-group seam it copies) and PUB-004 (same service) · consumes PUB-002 (`httpguard.Limiter`, `clientip`) · **PUB-007** (`mountBrowserGroup`, extracted in the same change) · LANDMINES "any third tool added under `/api/v1/tools/<name>`" (proved at the router by `TestPlaygroundDoesNotShareOrShadowTheGripperGroups`) and "an ISLAND-targeted migration … IS applied to `clients_db`" (filed by this change).

### PUB-007 — `mountBrowserGroup`: the one way a browser-facing route group is built in tools-api
- **status:** built 2026-09-03, in use by TWO tools (gripper, playground), council APPROVED in the same round (`63be72d1`).
- **what:** `mountBrowserGroup(r *gin.Engine, pool *pgxpool.Pool, prefix string, maxBodyBytes int) *gin.RouterGroup` — creates the group, applies `CORSMiddleware` (deployed-site Origin allowlist) then `InputCapMiddleware`, and returns it. Per-route bands stay at the routes because they differ per route (DESIGN §2) while these two layers never do. **Any third browser-facing tool calls this** rather than re-deriving the order.
- **why it exists:** the ordering was hand-copied per tool, and a third copy drew a `reuse_agent` objection naming this estate's own case of two paths diverging quietly. Now the CORS→cap order lives in one function and cannot drift.
- **deliberately NOT migrated:** the gauntlet group keeps its flat `RateLimitMiddleware(rps, burst)` bucket — a different band shape, and outside that change's remit. It is the one hand-built chain left in the file (`grep -n 'CORSMiddleware(' server.go` → the gauntlet line and this function).
- **what it does NOT do:** cluster-facing routes. The gripper's `/requests` needs the internal-key group; putting one behind CORS (or a browser route behind the key) is the landmine on this file, and `TestPlaygroundDoesNotShareOrShadowTheGripperGroups` asserts both directions.
- **sources:** `internal/tools-api/api/server.go` (`mountBrowserGroup`, `mountGripper`, `mountPlayground`), `internal/tools-api/api/server_playground_test.go`.
- **relations:** PUB-005 (the pattern it generalises), PUB-006 (its second caller), PUB-002 (`httpguard`).
