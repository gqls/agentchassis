# Register — seo

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074). It said **1** and the file held **2**._ consolidated from 2 raw extractions across unit U21. (The cluster input file contained this category's raw block twice, back-to-back and byte-identical; merged into one entry below.)

### SEO-001 — SEO content agent
- **status:** aspirational
- **live-evidence (2026-08-10):** pod-grep over **all 5 containers** running the image (`-l app=agent-chassis` matches only 2 — label trap): `injectRobotsNoindex` **2** each, positive control `injectCanonicalLink` 4, fabricated negative 0. **Proven in BOTH directions on the same binary:** `/tools/gauntlet/round.html` (`noindex=true`) serves the tag inside `<head>`; `/about.html` (`noindex=false`), re-rendered minutes later through the same action, serves none and deploys normally — the unflagged control is the load-bearing half, because a page rendered *before* the roll would lack the tag for a trivial reason. ⚠ **Route note:** the `spawn_agent`→`call_agent` rerender wrapper HUNG and FAILED at 634s with no child spawned, on a **clear lane (LAG 0)**; the direct dispatch (`049b_deploy_single_page.sh`) completed in ~25s. Use the direct route for a single page. That failure logged **nothing** in `agent_error_log`, which is the inverse of the under-counting documented for this handshake — neither table alone is a census.
- **status-evidence:** docs017/019b "seo-content-agent | LLM for generation, algorithmic for validation | New — runs after page content is written"; seo-discovery-agent in maintenance Phase 0; slot exists in component-builder-v2 sketch.
- **what:** A post-content sweep owning meta titles/descriptions, structured data/JSON-LD, robots directives, canonical URLs and Open Graph across all pages, with algorithmic validation and LLM generation; complemented in maintenance by sitemap-sync, schema validation, and meta-freshness discovery plus sitemap-regenerator and schema-fixer fix agents. No dedicated SEO category exists in the current taxonomy despite recurring SEO responsibilities across eras.
- **sources:** docs017_legacy_agent_rules_images_design_keydocs/019b_agent_architecture_v5_with_tickets_news.md#SEO-Content-Agent, #Fix-Agents
- **relations:** meta-manager (docs018/007); link technical types; site-finalizer sitemap generation; MKT-001 marketing as work items
- **verify-later:** any seo agent definitions; sitemap.xml generation code path


### SEO-002 — Site discovery files generator (`scripts/site-discovery-files.py`)
- **status:** built and exercised — run against 3 sites 2026-07-28; relojistas.com's three files are LIVE. **Second live site 2026-08-16: leopardessconsulting.co.uk's `sitemap.xml` (27 → 36 urls, 36/36 probed 200). One defect found and fixed in the same pass — it did not filter `pages.noindex` (see below).**
- **status-evidence:** `scripts/site-discovery-files.py`; relojistas.com serves `/sitemap.xml` (18 urls), `/llms.txt` (18 pages), `/robots.txt` (own file, Cloudflare block confirmed off per-agent). Dry-run verified against oufe.com and robot-hands.com.
- **what:** Generates `robots.txt` (Content-Signal + Sitemap pointer), `sitemap.xml` (with lastmod) and `llms.txt` for ANY site, from the `pages` table. Dry-run by default; never commits or deploys. Encodes three rules learned expensively: (1) **probe every URL and list only 200s** — a sitemap advertising a 404 is worse than none, and on its first fleet run it declined to list a URL that was 404 at that moment — correctly, though that page was deployed 1.5h later, so a dropped URL means "not fetchable now", not "broken", and the tool should be run when the target site is not mid-build; (2) **llms.txt is built FROM the pages, not written ABOUT them** — each entry is the page's own `<h1>` and own first sentence, so nothing invents a claim about a site; (3) **it detects whether Cloudflare's managed robots.txt is being merged in**, and names the agents currently disallowed — because Cloudflare PREPENDS its file to the origin's rather than yielding, so shipping your own changes nothing until the dashboard setting is off.
- **CORRECTED 2026-08-16 — `live_pages()` did NOT filter `pages.noindex`, so this tool would advertise, in a site's own sitemap, a page the platform is actively telling crawlers to skip.** The column arrived after the tool (SEO-003, live chassis v1.0.1277, injects `<meta name="robots" content="noindex, nofollow">` on those pages), which is how the two came to contradict each other. **Latent, never live, when found:** exactly two rows carry `noindex` fleet-wide and the only `active` one — `vonc.com/tools/gauntlet/round.html`, meta tag confirmed served — is on a site with no `sitemap.xml` at all (404). Fixed with `AND noindex IS NOT TRUE`, proven in both directions: vonc.com counts **22 without the filter, 21 with**, and the regenerated file has 21 `<loc>` and 0 matches for `gauntlet/round.html`. **The general shape, for whoever adds the next page-level control: a generator that reads `pages` is a CONSUMER of every column that decides whether a page should be served or found, and it will not fail when a new one appears — it will silently keep answering the old question.**
- **LANDMINE — a "no such generator exists" claim in a handoff outlives its truth by days.** This tool's own arrival is the worked case: `docs/leopardessconsulting/HANDOFF_2026-08-14_services_restore.md` said *"No platform generator exists"* for sitemaps and pointed at a hand-rolled 2026-07-17 recipe. True when written, false from 2026-07-28, and the 2026-08-16 session that acted on it found this tool only by accident, in a comment in `discovery_checks/check_site_structural_validity.go`. **Grep the register before rebuilding anything** — one search for `sitemap` returns this entry. Logged in `WRONG_CALLS.md`.
- **sources:** docs024_key_docs_latest/traffic_probe/EVIDENCE_2026-07-28_crawl_budget_and_the_dead_forum.md; docs024_key_docs_latest/FLEET_GUIDANCE_discoverability.md
- **relations:** answers SEO-001's `verify-later: sitemap.xml generation code path` — **there was none**; this is it, at script tier. SEO-001's agent remains aspirational. Related: `bugs_open/131` (og:image 404 on 11 of 14 sites), and the dormant `process_html` → `AddStructuredData` path (registered, reachable, emits nothing on any site).
- **⚠ OWNER RULING 2026-08-20 — the verify-later below is ANSWERED: *"All future sites should have sitemaps."*** So the open question ("should this become a standing mechanism?") is decided: **yes**, and a script someone must remember to run does not satisfy it. **The measurement that shows the cost of it staying manual: 15 of the 25 live sites with active pages serve NO `/sitemap.xml`** (measured 2026-08-20 by fetching each) — `ai-agent-orchestration.com`, `cookly.uk`, `finetuning.uk`, `fundamentallyai.com`, `gamesdesign.co.uk`, `gaswholesalers.com`, `lendzy.co.uk`, `loanzy.uk`, `mortgagecalculator.co.uk`, `oufe.com`, `remortgagecalculator.uk`, `robot-hands.com`, `vetcomparison.uk`, `vonc.com`, `webdesign.uk`. **That list includes the newest pilot**, built four days ago with every current guard applied — which is the clearest possible statement that a manual step is not a mechanism. The shape proposed below (a Go action beside `render_rss_feed`: read DB rows, emit a file artefact, gate on `deploy_config`) still looks right; what has changed is that it is now required rather than considered. Two things whoever builds it must carry over from this entry rather than rediscover: **probe before listing** (a sitemap advertising a 404 is worse than none) and **read every column that decides whether a page should be found** — `noindex` arrived after the script and silently contradicted it for weeks.
- **verify-later:** ~~whether this should become a Go action~~ **RULED, see above — the open item is now BUILD it.** Remaining: whether the emit belongs in the deploy path (so a new page updates the file) or on a schedule (so a retracted page drops out), and the answer is probably both, since the two events are different.
- > **UPDATED 2026-08-24 — the BUILD is done and HALF the remaining question is answered: see SEO-007.** The Go action (`render_sitemap`) is live in chassis v1.0.1333, and its caller is a SCHEDULED ROTATION (migration `590`), chosen over the deploy path because only a sweep reaches the sites that have no sitemap at all, and because the probe is a GET per URL (735 listable pages across 28 live sites as of 2026-08-24). **The deploy-path half stays open** — the two events really are different, exactly as this line said. ⚠ **Two corrections to the figures above, both by ADDITION rather than error:** this entry's "15 of 25 live sites serve NO sitemap.xml" was measured 2026-08-20 by status code; re-measured 2026-08-24 by BODY the fleet is **28** live sites and **8** serve a sitemap of ours, because two of the 200s are not ours (a parking file and a 27KB `text/html` homepage) and one is a 302. And this generator has a defect SEO-007 fixes in the action but **NOT here**: line 132 emits `https://{domain}{path}` unconditionally, so every sitemap it has produced lists the homepage as `/index.html` while the page itself declares `/` canonical.


### SEO-003 — Per-page search-exclusion opt-in (`pages.noindex` → `injectRobotsNoindex`)
- **status:** **LIVE and BEHAVIOURALLY PROVEN 2026-08-10** on chassis **v1.0.1277** (was: built 2026-08-09, inert pending roll)
- **status-evidence:** migration `docs/agent_docs/sql_for_agents/352_pages_noindex_flag.sql` applied 2026-08-09 (`UPDATE 1`, guard `DO`/`RAISE` induced-then-passed); census at apply time `is_true 1 / is_false 629 / is_null 0 / total 630`. Code: `platform/orchestration/actions/rerender_single_page_action.go` (`injectRobotsNoindex`, call site at assemblePage step 5a3), `platform/orchestration/actions/helpers.go` (`PageInfo.Noindex`). Tests `inject_robots_noindex_test.go`, 5 cases, both mutation-proven (neutered injection → 5 fail; loose `name="robots"` marker → the coexistence case fails).
- **what:** An opt-in boolean column on `pages`, default `false`, that makes the single-page rerender assembly path inject `<meta name="robots" content="noindex, nofollow">` before `</head>`. The gate (`if page.Noindex`) lives at the **call site** in `assemblePage`, not inside the helper, per the owner ruling of 2026-08-02 §2 (new authority on a shared seam ships as an opt-in field with the unsafe default OFF, so a reviewer of the caller can see the decision). Idempotent on its **own exact tag**, deliberately not on any `name="robots"` match: a foreign permissive `index, follow` meta must not silently disable it, and crawlers take the most restrictive of multiple robots directives, so coexistence still noindexes. Paired with `X-Robots-Tag: noindex, nofollow` on tools-api's `PublicRoundHandler`, which covers the JSON endpoint — a different URL that the meta tag cannot reach.
- **producer set / who sets the flag:** hand-set by migration only; there is no automated writer, and no agent or check emits it. One row true fleet-wide today: `vonc.com` `/tools/gauntlet/round.html` (`4629451e-e4f2-4fe2-b258-35107b5cb51e`), the Gauntlet round-record page, which serves published visitor UGC plus an AI verdict that may name real people.
- > **CORRECTED 2026-08-09 (same day, by the author, after the council's `editquality` seat asked for the right evidence): the landmine below is TRUE IN GENERAL and OVERSTATED FOR THIS PAGE.** `AssemblePageAction` carries `bugs_open/208`'s ownership guard (`multipage_actions.go:42-86`) and **refuses a `rebuild_policy='owned'` page before assembly**, so that path cannot silently strip the tag from one — it declines to touch it. The silent-drop case is **`generic` pages**. This entry's motivating page is **`owned`**, and all three work items ever filed against it are `page_rerender`/`page-rerender`, so the shipped fix sits on its real build path and is **not inert**. Residual that keeps the landmine live: the guard **fails open** when `rebuild_policy` cannot be read (`checked=false` → generic assembly proceeds, logging `OWNED_PAGE_GUARD_UNCHECKED` at high severity), so the exposure is countable rather than absent — `SELECT count(*) FROM agent_error_log WHERE error_code='OWNED_PAGE_GUARD_UNCHECKED';`
- **LANDMINE — the other head producer does NOT read this.** There are **two** page-`<head>` producers and only one honours the flag: `assemblePage` (`rerender_single_page_action.go`, the `page-rerender` path) does; `AssemblePageAction` (`multipage_actions.go`, live behind the `assemble_page` action, used by **3 active agent types** — `site-work-orchestrator`, `pageflow-builder`, `page-rebuild`) does not. So a rebuild through that path regenerates the page **without** the tag while `pages.noindex` still reads `true` — the wrong result looking exactly like the right one. This is the same divergence that left `injectPageJSONLD` (2026-07-28) and `injectCanonicalLink` (2026-08-02) on one path only; `noindex` is the third instance and the first where the consequence is a live privacy exposure rather than a missing SEO nicety.
- **open review question:** whether the two head producers should converge (one head-assembly seam both paths call) rather than each new head fix landing on one of them. That is architecture-scope by the 2026-07-29 ruling and is deliberately NOT decided here.
- **sources:** `bugs_open/232`; migration 352 + ROLLBACK; owner rulings 2026-08-02 §2 (opt-in field) and 2026-07-29 §1 (additive-and-inert is not guarantee-changing, so this went to the normal council gate, not an RFC)
- **relations:** SEO-001 (aspirational agent owning robots directives — this is the first robots directive the platform actually emits); SEO-002 (`robots.txt`/`sitemap.xml` generator, a site-level sibling to this page-level control); `bugs_open/232`; `architecture_review/RFC_020` (the wider third-party-harm question this was split out of, deliberately independent of it)
- **verify-later:** after the next chassis roll — pod-grep `injectRobotsNoindex`, then rerender the page and `curl` for the tag; and whether the two-head-producer divergence earns its own bug file

### SEO-004 — `save_page_meta_description` (the first writer that can REPAIR a page's meta description)
- **status:** built, **not yet exercised in production** — the Go half rides the next fleet roll; the config half (migration `485`) is live from 2026-08-19. Saying "deployed" would overstate it.
- **status-evidence:** action registered in `GlobalActionRegistry` as `save_page_meta_description` (`registry.go`, category `site`); five tests in `save_page_meta_description_test.go`, each proven load-bearing by a named mutation actually run (flip the `overwrite_existing` default → 2 fail; delete the `MetaDescriptionLooksInternal` branch → 1 fails; delete the guarded WHERE clause → 3 fail; restored → green). Migration `485` applied 2026-08-19 with `UPDATE 1` and a DO/RAISE verify, ledger-recorded `--record-only`.
- **what:** `pages.meta_description` had **seven** writers and every one was a create-or-upsert path, so the column was set at page birth or never — measured 2026-08-19, **407 of 731 active pages (55.7%) across 26 of 27 sites** had none (`bugs_open/320`). This action is the missing **persist** half: given a page id (or `site_id`+`page_name`) and a text field in collected data, it writes the column. It deliberately does **not** find the pages (that is `query_database`) and does **not** write the sentence (that is `execute_llm_prompt`), so a workflow composes the three and authorship stays with the framework per the owner's 2026-08-06 ruling. It reuses `datahelpers.MetaDescriptionLooksInternal` — the `bugs_closed/103` guard, exported for exactly this — rather than re-deriving the brief-vs-public rule.
- **the opt-in field, and its default:** `overwrite_existing` defaults to **false** (owner ruling 2026-08-02 §2 — new authority on a shared seam ships opt-in with the unsafe side OFF). By default it fills a blank and refuses a page that already has copy; the policy lives in the UPDATE's own `WHERE` clause (`$3::bool OR COALESCE(meta_description,'')=''`), not in a read-then-write, so it cannot race a concurrent writer.
- **⚠ LANDMINE — the failure this exists to end is a SUCCESSFUL no-op.** `content_rewrite` items filed against missing meta descriptions ran, **completed, and wrote nothing** (two measured: `ec701bb3-85e7-40e7-bff1-2ce1ae104861`, `13522562-2392-4db9-96b5-204ab67cb999`; both targets still 0 chars, one page touched by the handler after the item closed). The cause is ownership — `llm_fields`/`llm_field_specs` are per-SECTION, and this is a `pages` column, so no writer prompt can reach it. **So this action's result map always distinguishes a write from a refusal, with a named reason** (`empty_candidate`, `candidate_looks_internal`, `candidate_too_long`, `already_has_description`). A caller that reads only "did it error" reintroduces the original defect. A missing DB connection is an **error**, not a soft `{"updated": false}`, for the same reason.
- **the sibling half (M2), same bug:** `upsertPage`'s conflict clause was `meta_description = EXCLUDED.meta_description`, unguarded, three lines below a `nav_label` clause that WAS `COALESCE(NULLIF(...))`-guarded. Since `metaDescription` defaults to `""` when the page map omits the key, **every replan wrote a blank over whatever was there** — proven to have fired on four `robot-hands.com` pages that held 97/120/169/329 chars in an April `site_snapshots` entry and read 0 today. Now `COALESCE(NULLIF(EXCLUDED.meta_description,''), pages.meta_description)`: a non-blank incoming value still wins, only the destructive direction is closed. ⚠ **CORRECTED 2026-08-19 (council round 2):** this entry first said the guard "matches" `nav_label`. It is the MIRROR IMAGE — `nav_label` is `COALESCE(NULLIF(pages.…,''), EXCLUDED.…)` so the EXISTING value wins, while this lets the INCOMING value win unless blank. Both are deliberate and deliberately different (a description is plan-owned content that must stay revisable; a nav label should not be churned by every replan), so **do not unify them**. They share the NULLIF idiom, not the polarity.
- **sources:** `bugs_open/320`; `platform/orchestration/actions/save_page_meta_description_action.go`; `platform/orchestration/actions/site_db_actions.go` (`upsertPage`); `docs/agent_docs/sql_for_agents/485_build_site_planner_asks_for_meta_description.sql`; working docs `docs024_key_docs_latest/meta_description_never_backfilled/`
- **relations:** `bugs_closed/103` (the opposite failure on the same column — tool pages publishing the BRIEF as the description; its `PublicMetaDescription` guard is reused here) · `bugs_open/309` (blocked by the empty column; its blog-listing excerpt reads `meta_description` at `rebuild_blog_listing_action.go:99`) · `bugs_closed/203` (a precedent transfers on MECHANISM — ask who owns the field before asking the framework for it) · content-quality **CQ-001**, which listed "empty meta descriptions" as an open defect in June 2026 and is the earliest record of this
- **verify-later:** the 112 non-plan-managed empty pages (a replan cannot reach them, so they are what this action is FOR); whether `485` actually makes the planner emit usable copy — check `pages` created after 2026-08-19 rather than assuming; the first production run's `updated` counts against its refusal reasons

### SEO-005 — Per-page Open Graph identity at assembly, and the document language carried by the head component (`head_assembly.go`)
- **status:** built and mutation-proven, **not yet live** — the Go rides the next fleet roll; the two migrations are authored and deliberately HELD until the binary is proven running (see the ordering trap below). Saying "deployed" would overstate it.
- **status-evidence:** `platform/orchestration/actions/head_assembly.go` (`spliceOpenGraph`, `headLangAttr`, `htmlDocumentOpen`), called from `assemblePage` (`rerender_single_page_action.go`) at two points; `head_assembly_test.go`, 10 behavioural claims, **every one proven load-bearing by a mutation actually run** (ledger in `docs024_key_docs_latest/bugfix_252_og_lang_assembly/NOTES_og_lang_assembly.md`, 2026-08-20 (c)). Full package suite green against a clean `git archive HEAD` extraction. Bug: `bugs_open/252` (og/lang slug).
- **what:** `site_components.rendered_html` holds **one `<head>` row per site**, and `assemblePage` reuses it for every page. So any page-scoped value a site-level render bakes into that row is a claim about every page at once. `spliceOpenGraph` **removes** `og:title`/`og:description`/`og:url` from the stored head and **injects** one per-page set — remove-then-inject, so it is idempotent by construction, repairs a head that already carries a wrong or duplicated value, and needs no chrome rebuild. `og:url` goes through `preferredPageURL`, the same helper `injectCanonicalLink` and `injectPageJSONLD` use, so **og:url == the canonical href == the JSON-LD `@id`** by construction rather than by three call sites agreeing. Correct-or-absent throughout (the LNK-005 idiom): no meta description means no `og:description`. Separately, `headLangAttr` + `htmlDocumentOpen` move the document language out of Go: the head component declares `lang`, sourced from `site_specs` `site_config.locale.lang` through the existing schema-driven config resolution (a second consumer of the STY-050 mechanism), and Go defaults to `en` when a site declares none — so a non-opted site's output is byte-identical to the hardcoded line this replaced.
- **the measured defect it ends:** 2026-08-19, **22 of 24** stored heads carried `og:url` pointing at the site HOMEPAGE — baked by `injectBrandHeadTags`, which has no page to ask — so all **700 assembled pages across 26 sites** declared the homepage's identity beside a canonical that correctly named the page. Verified at the artefact on `ai-agent-orchestration.com/about.html`, which also served **two `og:title` tags**, one blank and one site-level. **A missing og tag is silence and a scraper falls back sensibly; a wrong one is followed** — which is why this strips rather than fills.
- **⚠ LANDMINE — the fix candidate in the bug file would have fixed almost nothing, and the reason generalises.** `bugs_open/252` proposed the established idiom: put blank placeholders in the head, fill them at assembly (as `spliceMetaDescription` does). Placeholders existed on only **4** of 24 heads, and on those 4 they were already **shadowed by a filled duplicate** appended later in the same head by `injectBrandHeadTags`, whose idempotency guard tests `rel="icon"` OR `og:image` and therefore cannot see a blank `og:title`. The mutation that reproduces that candidate fails **five** of this entry's tests. **The general shape: an "opt-in placeholder" design assumes nothing else writes the same tag. Census the artefact for OTHER producers of your tag before choosing fill-a-blank over replace.**
- **⚠ LANDMINE — removing tags from a head changes the meaning of "first" for a neighbouring function.** `spliceMetaDescription`'s legacy fallback fills the **first** `content=""` anywhere in the head when the exact blank description tag is absent. Running `spliceOpenGraph` FIRST clears the blanks it owns, promotes `og:image` to first, and the page description lands in **og:image** — outside this function's strip set (deliberately: `og:image` is `bugs_open/322`'s, under its own landmine), so nothing cleans it. Hence the call order in `assemblePage` is description-splice **then** og-splice, which looks backwards and is pinned by `TestSpliceOpenGraphRunsAfterTheDescriptionSpliceForAReason`. **All 24 real heads take the targeted path today, so the order is currently unobservable on live data** — a fixture copied from a live head cannot show it, which is how the order was first chosen wrongly (`WRONG_CALLS.md`, 2026-08-20).
- **⚠ ORDERING TRAP for the rollout:** DB config is live the moment it applies; Go is inert until the roll. If the migrations land before the image, the OLD binary consumes the chrome-staleness edge — chrome re-renders under the old `injectBrandHeadTags`, pages re-assemble without the splice, `render_inputs` restamps, and **the detect→rebuild pipe goes quiet with the fleet still wrong**, needing a manual re-fire. Both migrations are named `_HOLD`.
- **this file is the NAMED SEAM for SEO-003's open convergence question.** `assemblePage` and `AssemblePageAction` remain two independent head producers and this fix lands on the first only — the **fourth** instance of that divergence (after `injectPageJSONLD`, `injectCanonicalLink`, `injectRobotsNoindex`). The question is still architecture-scope by the owner ruling of 2026-07-29 and is **not** decided here; what changed is that the next per-page head fix has a shared home instead of a fifth private helper inside the assembler. **Consequence for anyone verifying og:url per page: record which producer built the page**, or a correct fix reads as broken on a page rebuilt through `assemble_page`.
- **⚠ ESCALATION THRESHOLD, set by the council's `architecture` seat 2026-08-20 (corr `3b6712d4`) and binding on the next author: THIS IS THE FOURTH FIX TO LAND ON ONE HEAD PRODUCER ONLY. IF A FIFTH INSTANCE ARRIVES, IT DOES NOT GET A FIFTH BESPOKE PATCH — RAISE SEO-003 AS AN RFC FIRST.** The seat's reasoning, recorded because it is the part that decays: the cost of not converging is concrete and recurring — each new page-identity fix has to reason about which producer sees it, gets a one-sided patch, and the guard set drifts further from the code with every round (`verify_site.py`'s `OG_PER_PAGE` exemption and `check_site_structural_validity.go`'s og:url exclusion are already two rounds behind). Four dated patches have now accreted on a split flagged architecture-scope on 2026-07-29 with still no owner decision. Proceeding here was judged right; treating a fifth as routine is not.
- **council round 1 (corr `3b6712d4`): APPROVED, 5 advisory objections, none high-severity, 12 seats (6 abstained).** Four seats asked for checks rather than changes; all were run and are recorded in `507_head_components_carry_lang_HOLD.sql`'s header and the lane NOTES. The two load-bearing answers: (1) **`assemblePage` really is the only page-artefact consumer of `site_components.rendered_html`** — enumerated rather than asserted, which is what `guardian` and `prior_art_librarian` objected to; the one candidate a loose grep surfaced (`save_sections_component_floor.go:169`) reads `page_components`, not `site_components`. (2) **`head-seo-standard` declares no `og_title`/`og_description`/`og_image` fields at all**, so those template branches have never been resolvable and there is no dangling `required:true` after their removal. Residual exposure the seats named and this entry does not close: `bug_historian` is right that the untouched wholesale idempotency guard (`bugs_open/322` item 4) means **any future per-page tag added to `injectBrandHeadTags` reproduces bug 252's exact shape** — that guard, not og:url, is the generic mechanism.
- **sources:** `bugs_open/252` (og/lang slug); `platform/orchestration/actions/head_assembly.go` + `_test.go`; working docs `docs024_key_docs_latest/bugfix_252_og_lang_assembly/`; owner decisions 2026-08-11 (locale option 3) and 2026-08-20 (canary then fleet waves; opt the UK estate into `en-GB` now)
- **relations:** **SEO-003** (the two-head-producer landmine and its open convergence question — this entry is its named seam) · `bugs_open/251` (`preferredPageURL`, whose root normalisation og:url inherits — fixed, live, council-approved) · `bugs_open/322` (the emitter-side twin: this lane took item 1's assembly end plus the one-line removal of the injector's `og:url`; items 2–5 stay there, including the do-NOT-gate-on-an-assets-row landmine) · `bugs_open/320` / **SEO-004** (`pages.meta_description` empty on 55.7% of pages, so `og:description` is legitimately absent on many pages until that backfiller runs — an absent tag there is this mechanism working) · **STY-050** (the `site_specs`→template config channel `lang` reuses)
- **verify-later:** after the roll — canary two pages on a duplicate-tag site and confirm og:url == canonical == `@id` at the artefact, then the per-site sweep across all four head families (`Document Head`, `head-seo-standard`, the fork, and the no-stored-head site that keeps `buildDefaultHead`'s no-og behaviour); whether `verify_site.py`'s `OG_PER_PAGE` accepted-loss exemption and `check_site_structural_validity.go`'s og:url-exclusion comment have been retired, since **both encode the premise this entry removes and neither will fail loudly when it becomes false**

### SEO-006 — `site-locale-unset-check` (the recurring half of SEO-005's locale)
- **status:** **LIVE 2026-08-21** — CronJob deployed and a manual run completed CLEAN, writing its `doc_notes` row. Not merely built.
- **status-evidence:** `deployments/kustomize/services/site-locale-unset-check/base/` (`check.py`, `cronjob.yaml`, `kustomization.yaml`); `kubectl apply -k` 2026-08-21; manual job exit 0, output *"CLEAN — all 27 real sites declare a document language"*, `langs_in_use {"en-GB": 26, "es-ES": 1}`. Daily at **07:15 UTC**, a free slot chosen against the eleven other pipeline-integrity CronJobs.
- **what:** SEO-005 gave every site a declared document language, set **explicitly per domain** by migration `508`. That migration is a **one-off**, and new sites keep arriving — so without this, the next site created falls back to the Go default `en` and nothing anywhere says so. That is the silent default `bugs_closed/252` existed to remove, met one level up. This job is `508`'s own fail-closed guard made recurring. It reports **two** shapes: **(A) UNSET** — a real site with no `site_config.locale.lang`; and **(B) UNREACHABLE** — a site whose locale IS set and whose head component template has no `{{if .lang}}` gate to render it, so the value is set, correct, and cannot reach the page. `*.internal` pool domains excluded. One `doc_notes` row per run **including clean ones**, so a missing row means the job did not run.
- **B is the half worth having, and it is not hypothetical.** `bugs_closed/347` is exactly that shape: `webdesign.co.uk`'s head component was a hand-authored fragment with no `<head>` open tag, so `508` set its `en-GB` and **117 pages — the most of any site — kept serving `en`** until migration `529` wrapped it. **A is visible to anyone who looks at a new site; B looks FINISHED — the config is right there — and only the served page disagrees.**
- **it caught a real case before it was even deployed.** Running its predicate against live data as the test (rather than trusting a clean unit run) surfaced `buytoletcalculator.uk`, created that same day, with no locale — fixed by migration `530`, marked `[EVIDENCE-THIN]` because the site has no content yet to judge. **That is the honest way to test a check: run its predicate against production and see whether it finds something you did not already know.**
- **⚠ what it deliberately does NOT do:** judge whether a site's declared language matches its content. "What language is this site?" is a product decision — owner ruling 2026-08-20, **a non-English site must not be `en-GB`, and that generalises to future language sites**. The job surfaces the question and must never answer it; that is why `508` and `530` name domains explicitly rather than deriving from the TLD, which `bugs_closed/252` §B had already rejected (`.com` sites on this estate are mostly British, and `relojistas.com` is Spanish).
- **⚠ a finding here is not a page defect.** Setting the config repairs nothing already served: a page shows its language only when it next re-assembles (`bugs_open/346`). Verify at the artefact — `curl -s https://<domain>/<inner-page> | grep -oE '<html[^>]*>'` — and use an **inner** page.
- **sources:** `bugs_closed/252` (og/lang slug) §B; migrations `508`, `529`, `530`; `bugs_closed/347`; owner rulings 2026-08-20 (opt in now; non-English sites) and 2026-08-21 (do not force page rebuilds)
- **relations:** **SEO-005** (the mechanism this watches) · `bugs_closed/347` (finding B's worked case) · `bugs_open/346` (the page-level residual — why a clean result here does not mean a clean fleet) · `site-discovery-staleness-check` (the sibling this is modelled on: same image, secret and `doc_notes` convention)
- **verify-later:** whether finding B ever fires again — it would mean a new hand-authored head component arrived without the gate, which is the failure this exists to catch early

### SEO-007 — `render_sitemap` action + the `sitemap-refresh` rotation that calls it
- **status:** the action is **LIVE in the running chassis** (v1.0.1333, binary-probed with controls 2026-08-24 — registry description present, an invented string absent, a known older action present). The **caller** (migration `590`: a `sitemap-refresh` agent and a `sitemap-refresh-rotation` scheduled task) is authored, guard-induced and committed; it is applied when the owner releases the live-DB write. Saying "sitemaps are automatic" would overstate it until the first tick has run and a site that had none serves one.
- **status-evidence:** `platform/orchestration/actions/render_sitemap_action.go` + `render_sitemap_test.go` (`a596fa84b`, 2026-08-21); `registry.go` entry, category `feed`; migration `docs/agent_docs/sql_for_agents/590_wire_render_sitemap_into_a_rotation.sql` (+ `_ROLLBACK`), whose four verify guards were each INDUCED to fail against the live DB inside rolled-back transactions before it was committed.
- **what:** Answers SEO-002's ruling (*"All future sites should have sitemaps"*) at action tier. Renders `sitemap.xml` from `pages` and returns `render_rss_feed`'s `{files, domain}` contract, so the existing `git_commit` step publishes it unchanged. **The gate is INVERTED relative to `render_rss_feed`, deliberately:** RSS is opt-IN because most sites should not publish a feed; a sitemap is ON by default and a site opts OUT with `deploy_config.sitemap.enabled=false`. An opt-in sitemap would reproduce the exact situation the ruling exists to end. Carries over SEO-002's two expensive rules rather than rediscovering them (probe before listing; read every column that decides whether a page should be FOUND). Refuses to publish an EMPTY sitemap — that tells a crawler the site has no pages, which is worse than none.
- **the caller, and why a rotation rather than the deploy path.** SEO-002's `verify-later` asked deploy-path or schedule and answered "probably both". **The rotation goes first, and the reason is measurable:** it is the only one that reaches the sites that have NO sitemap at all (the deploy path only helps a site that gets rebuilt), and the probe is a GET per URL — **735 listable pages across 28 live sites as of 2026-08-24** (avg 26.3, max 135 on `webdesign.co.uk`), so one site per 1800s tick is ~26 requests and steady state ~245 GETs/day, where the deploy path would re-probe a whole site on every page change. The deploy-path half remains open and is deliberately a separate change.
- **⚠ LANDMINE — A 200 ON `/sitemap.xml` IS NOT EVIDENCE THE SITE HAS YOURS, AND THERE ARE AT LEAST TWO WAYS TO BE FOOLED.** Measured 2026-08-24 across all 28 live sites: `adversecreditmortgage.co.uk` answers 200 with the **parking provider's** file (171 bytes, a single `<loc>` for `/lander`, no matching `pages` row); `noted.co.uk` answers 200 with **27,414 bytes of `text/html`** — its own homepage, served for any path; `webdesign.uk` 302s to `webdesign.co.uk`. A status-code census counts all three as successes and overstates coverage by three. **The mechanical test that discriminates: extract every `<loc>`, strip the origin, and match the path against that site's `pages` rows.** Ours score 17/18 to 98/98; the parking file scores 0/1. The `noted.co.uk` case is a SECOND instance of a trap SEO-002 and the action header each record only once.
- **⚠ LANDMINE — the generator and the action both listed a NON-CANONICAL homepage, and the probe cannot catch it because `/index.html` returns 200.** `scripts/site-discovery-files.py:132` emits `https://{domain}{path}` unconditionally and the action inherited that. But the sites declare otherwise: measured 2026-08-24 by fetching the pages, **10 of 10** checked (across all three builders) declare `<link rel="canonical" href="https://<domain>/">` for the root, while a **section** index declares the extension (`https://dartsonline.com/guides/index.html`). `/` and `/index.html` served byte-identical 92,822-byte bodies, so listing the wrong form is a real duplicate signal. Fixed in `absoluteURL` (`5c9acf1bd`) by **whole-path** match — as of 2026-08-24 **27** listable rows are exactly `/index.html` (one per site) and **228** are `<section>/index.html`, so a suffix match would rewrite the second set against their own canonicals. That asymmetry looks like an inconsistency and is what the sites declare; `TestRootIndexIsCanonicalisedButSectionIndexIsNot` pins it, mutation-proven in both directions with changes that still compile. **The general shape: a probe proves FETCHABILITY, never CANONICALITY, and a duplicate URL passes it every time.**
- **the before-figure, so the close condition cannot drift:** **8 of 28** live sites serve a sitemap of ours as of 2026-08-24 (judged by body, per the first landmine). Done is not "the action is wired" — it is a site that did not have a sitemap having one, fetched and read, and the re-measure is the proof.
- **⚠ the rotation honours `sites.locked_at`, and that is load-bearing.** `adversecreditmortgage.co.uk` is locked by an owner HALT of 2026-08-18. **A sitemap commit IS a deploy**, so a sweep that ignored the lock would drive one against the halt. Same idiom as `build-pipeline-trigger`. It also skips sites with a `claimed` build work item, because a sitemap generated mid-build probes pages that are not deployed yet.
- **the OTHER consumer of the stamp table, checked rather than assumed** (owner ruling 2026-07-29 §3): the rotation stamps `site_discovery_rotation` with `agent_type='sitemap-refresh'`, for which `render-audit-agent` is already precedent as a non-discovery type. `site-discovery-staleness-check`'s coverage query CROSS JOINs its own three `DISCOVERY_AGENTS` and its findings loop iterates that same list, so a fourth type **cannot** create a false finding there; it surfaces only in the informational `stamps_last_24h` line.
- **sources:** `platform/orchestration/actions/render_sitemap_action.go`; migration `590` (+ `_ROLLBACK`); council correlation `8a004aab-be85-4d6d-bdb1-4fb114f1d64b` (round 1 REVISE, round 2 submitted 2026-08-24); working docs `docs024_key_docs_latest/portfolio_positioning/`
- **relations:** **SEO-002** (the script this promotes to a mechanism, and the source of both carried-over rules — its `verify-later` is half-answered here) · SEO-001 (whose `verify-later` asked for a sitemap generation code path) · SEO-003 (`pages.noindex`, one of the visibility columns the query must read) · `content-feed-orchestrator`'s RSS trio (the shape this copies, and `render_rss_feed`'s `{files, domain}` contract) · DGH-003 (`sites.github_repo` as deploy-target selector — `resolveGitRepoNameDB` defaults to `sites` when empty, which is how this reaches B2 domains)
- **verify-later:** after the first tick — re-measure the 8-of-28 figure by BODY, and confirm the committed file actually reaches the CDN rather than only the repo; then the deploy-path half (a new page should not wait up to 3 days), which is the remaining half of SEO-002's open question; and whether 3 days is the right staleness threshold once the real probe cost is observed rather than projected.
