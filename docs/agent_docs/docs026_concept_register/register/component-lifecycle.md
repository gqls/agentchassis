# Register — component-lifecycle

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074)._ (11 consolidated from 11 raw extractions across unit U07, + CLC-012 and CLC-013 added later — see their entries for dates). No duplicates found within this category — it arrived as a single tightly-scoped, already well-differentiated F1–F8 investigation saga (the vonc/spark shared-component clobber incident and its remediation), so all 11 raw blocks are carried through as distinct entries with only light formatting changes.

### CLC-001 — Shared content-component reuse model (one content_components row, N page_components instances)
- **status:** deployed
- **status-evidence:** Stated as settled platform fact throughout the investigation ("one content_components row, N page_components instances"); a live site's homepage arrived mid-recovery as a "healthy sixth dependent," proving live reuse.
- **what:** A section component is a single shared content_components row (keyed by `function`, with section_type, input_schema.fields, html_template, is_active, forked_from) reused across pages and sites; each page stores its own content_data in page_components. Any change to the shared row therefore has a cross-site blast radius — the structural precondition for the incidents this whole saga investigates.
- **sources:** BUNDLE(3).md §1; NOTES(43).md §1, §9z (sixth dependent); HANDOFF(7).md §Platform operating model
- **relations:** clobber failure mode; F4 fork-vs-match (CLC-006); F8 contamination; optimistic-lock co-management; tool-library's Shared component library semantics (TLIB-022, the same principle from a different incident)
- **verify-later:** content_components + page_components schemas; idx_cc_selector (section_type, component_level) partial index

### CLC-002 — StoreGeneratedComponentAction regeneration branch
- **status:** deployed
- **status-evidence:** "Correction 6 — StoreGeneratedComponentAction is the rename writer"; insert/dedup shape read from the live file.
- **what:** On storing a generated component whose `function` matches an existing row (`WHERE function=$1 AND forked_from IS NULL`, deliberately is_active-agnostic since 2026-05-06), the action snapshots the old schema/template to component_versions, UPDATEs the shared row in place (same component_id, so dependents follow), marks dependents pending (markPagesPendingRebuild — build_status only, no rendered_html), and raises one deduped needs_rerender work item per affected site via createRerenderWorkItem. Pre-fix, that item carried no `reason`, making the triggered re-render assemble-only and unable to repair anything.
- **sources:** NOTES(43).md §2 Correction 6, §9h, §9q; BUNDLE(3).md §3
- **relations:** F1 guard lives in its validation block (CLC-003); F3b re-added the reason to its spec; F4 (CLC-006, its function-keyed lookup is the fork vector); Component regeneration in place (tool-lifecycle TL-026, the same mechanism via a different real incident)
- **verify-later:** store_generated_component_action.go (existence check L198–207; regen branch; markPagesPendingRebuild; createRerenderWorkItem NOT EXISTS dedup)

### CLC-003 — F1 field-contract guard (reject regens that rename/drop retained fields)
- **status:** deployed
- **status-evidence:** "F2 COMPLETE... 3b reject live firing, three-level visibility, zero mutation" (2026-07-02); a companion runbook Part A: "Fixes deployed + proven."
- **what:** In StoreGeneratedComponentAction's Layer-1 validation, on isRegeneration the guard diffs old vs new input_schema.fields names (a schemaFieldSet helper); any retained field that disappears becomes a blockingIssue routed through recordValidationRejection into agent_error_log — additions allowed, renames/drops rejected before any snapshot/UPDATE. Converts silent stranding into a loud, queryable rejection naming the stranded fields. Design choice: a preserve-the-contract strict-reject backstop, not a per-dependent migration. This is the direct code-level fix for the system-stats field-rename incident (tool-library TLIB-005).
- **sources:** F1_store_generated_component_action.patch; NOTES(43).md §9, §9a, §9o; RUNBOOK(49).md Part A
- **relations:** F1-prompt (CLC-004, generation-time complement so name-preserving regens succeed); F5 (CLC-007, proposed extension); F8 (guard checks names only — its blind spot); Validation observability (tool-lifecycle TL-020, the logging mechanism this guard writes through)
- **verify-later:** store_generated_component_action.go guard block + schemaFieldSet; agent_error_log rows error_code component_validation_rejected; store_generated_component_guard_test.go

### CLC-004 — F1-prompt generation-time field-name preservation (loader + dormant rule + function pin)
- **status:** deployed
- **status-evidence:** "F1-prompt 1a/1b+2 done (validator passes; loader executes; verify row good)"; two regens preserved names with md5-verified template change.
- **what:** Three coupled pieces so regens preserve names instead of being rejected by CLC-003: (1) load_existing_component Go action — looks up the canonical component by section_type (is_active, forked_from IS NULL, component_level='section'), outputs existing_component.field_names + function; advisory, never errors (no match → empty map → rule dormant). (2) A snapshot-first, anchored, idempotent, drift-checked SQL migration wiring the step before generate_template and inserting a dormant `{{if .existing_component.field_names}}` prompt rule: reuse these exact names, MAY add, MUST NOT rename/drop. (3) A companion migration pins `{{.existing_component.function}}` so the store matches the same row (the mitigation for F4, CLC-006). Option A (pre-generation lookup by section_type) was chosen after confirming section_type is queryable.
- **sources:** F1prompt_component_creator_preserve_field_names(1).sql; NOTES(43).md §9c–§9f, §9m; RUNBOOK(49).md Part A
- **relations:** F1 guard (CLC-003, backstop); F4 (CLC-006, the pin is its mitigation); prompt-migration convention; deploy-ordering gate (its 9i failure); **CLC-027 (2026-08-22 — narrows the "chosen after confirming section_type is queryable" line above: queryable is not the same as ALWAYS POPULATED, and the gate never keyed on it)**
- **verify-later:** load_existing_component_action.go + registry.go (IsLocal:true); component-creator default_config (top-level prompt_template; load_existing_component step; input_fields incl. existing_component)

> **⚠ CORRECTED 2026-08-22 (`bugs_open/337`, commit `e1951c24b`) — piece (1)'s lookup key was NOT sufficient, and this entry's own closing sentence is where the reasoning went wrong.** "Option A (pre-generation lookup by section_type) was chosen after confirming section_type is **queryable**" — but queryable is not populated, and the STORE never keyed on that column at all. The advisory diverges from the gate on **three** predicates, not one: `section_type` vs `function`, plus an `is_active = true` filter and a `component_level = 'section'` filter that `lookupBaseComponent` deliberately does **not** carry (`component_storage_identity.go:157-165`: the is_active filter was removed on 2026-05-06 precisely because the paradigm regeneration target is a *deactivated* row). So the loader misses rows the gate then enforces, the dormant rule stays dormant, and **the function pin in piece (3) goes silent with it** — which is why CLC-006's mitigation was never as load-bearing as this entry implies.
> **What it cost:** `[MEASURED 2026-08-22]` 97 of 101 `component_validation_rejected` rows in eight days were the CLC-003 refusal firing on writers that had been told nothing; one 18-field component was refused **70 times with no success**, while a 4-field sibling with generic names escaped on its second attempt by reproducing them **by chance**. And the `section_type = COALESCE(...)` self-heal (CLC-020) only runs on a **successful** store, so the gap could only be repaired by the success it prevented.
> **Now:** the loader keeps the section_type query as primary (it is the selector's own, `component_selector.go:183-187`) and falls back to the gate's own `resolveStorageIdentity` on a miss — the store's resolver, not a bare by-function lookup, because a bare lookup would *manufacture* refusals on the two diversion paths. **Go, so inert until the chassis rolls.**

### CLC-005 — Store-driven retry on field-drift rejection (Option B) — abandoned alternative
- **status:** abandoned
- **status-evidence:** "Two options... user chose A"; Option B never mentioned again after being raised once.
- **what:** The rejected alternative to Option A (CLC-004): on a field-drift rejection the guard would return the existing field names, and a store_component error edge would loop back to generate_template with the names injected, retrying once. Judged heavier (reject-with-retry-data + loop-guarded workflow edge) but authoritative (no key guessing); dropped when section_type proved a stable pre-generation lookup key.
- **sources:** NOTES(43).md §9d, §9e
- **relations:** F1-prompt Option A (CLC-004, the superseding choice)
- **verify-later:** absence: component-creator workflow should have no store_component→generate_template error edge

### CLC-006 — F4 — regen-vs-create keyed on the LLM-chosen function (silent fork)
- **status:** partial
- **status-evidence:** "F4 (structural finding): regen-vs-create is keyed on the LLM-chosen function → nondeterministic. Miss case = silent FORK"; pin migration applied; "store-side advisory FLAGGED as follow-up, not built."
- **what:** Whether a store is a regeneration or a creation depends on whether the LLM happened to choose the existing function name — a miss silently creates a parallel active duplicate for a section_type (library fragmentation; the CLC-003 guard is bypassed by fork; selector nondeterminism results). Observed live in testing (fork 80222fc1). Mitigation shipped: the prompt pin of `{{.existing_component.function}}` (CLC-004); a store-side advisory (warn when function misses but an active same-section_type row exists) is deliberately advisory-only and unbuilt, since multiple components per section_type can be legitimate. A suspected live-fork case (duplicate hero rows) was later softened to old manual seeding rather than a live recurrence.
- **sources:** NOTES(43).md §9m, §9n, §9al, §9am; RUNBOOK(49).md Part E
- **relations:** F1-prompt function pin (CLC-004); StoreGeneratedComponentAction lookup (CLC-002); F2 methodology (exposed it); Component regeneration in place (tool-lifecycle TL-026, the same determinism hazard via a separate incident)
- **verify-later:** duplicate non-forked function rows in content_components; whether any store-side advisory exists

> **⚠ SUPERSEDED 2026-08-25 (`bugs_open/388`, commit `30d223291`, CLC-032) — the store-side half IS BUILT, and this entry's two verify-later items are BOTH ANSWERED, both with the bad answer.**
> **(1) Duplicate non-forked function rows:** `[MEASURED 2026-08-25]` two pairs from the `generated`
> route — `tool-archetype-taster-quiz` (`archetype-taster-quiz` 04-13 → `tool-archetype-taster-quiz`
> 05-06) and `tool-gripper-payload-calculator` (05-01 → 05-06). Both second rows carry
> `function == section_type`, the fingerprint of the section_type fallback derivation. (The `hero`
> pool's 7 rows are all `manual` and are deliberate vocabulary — `created_from` is what separates the
> finding from the noise.)
> **(2) Whether any store-side advisory exists:** it did not. `grep -n existing_component
> store_generated_component_action.go` returned two hits, both comments, no comparison.
> **What replaced the mitigation.** This entry says the prompt pin of `{{.existing_component.function}}`
> is F4's mitigation. CLC-004's own 2026-08-22 correction already records that the pin *"goes silent"*
> whenever the advisory does — and the pin is a NAME, which cannot identify a row here at all: 25 of
> 330 non-forked rows' `function` values carry more than one row. CLC-032 carries the identity by
> `content_components.id` instead, so the model's choice of name stops deciding what gets overwritten.
> **This entry's stated reason for leaving it unbuilt still stands and is honoured:** *"multiple
> components per section_type can be legitimate"* — CLC-032 changes WHICH row is written, never HOW
> MANY a section_type may have.

### CLC-027 — The birth gate's contracts, stated to the writer from the gate's own code (advisory + source vocabulary)
- **status:** **LIVE and DEMAND-PROVEN AT THE MECHANISM 2026-08-23** — rolled in build `2dbe12f1d` (agent-chassis provenance line, 11:51Z; `e1951c24b` confirmed an ancestor by `git merge-base --is-ancestor`, with a control: current HEAD is NOT an ancestor, and 10 commits have landed since the build, so the test discriminates). **The page it was filed for is still NOT repaired** — see the honest limits below; do not read this status as a repair.
- **status-evidence:** **First post-roll generation, 2026-08-23 12:12Z (work item `b0ba3e3a`), carried BOTH contracts: `existing_component.field_names` = 697 chars and `aspect_paths` = 10,292 chars, against an empty advisory and no vocabulary before the roll.** And the failure mode MOVED: the same item was refused on 08-22 for a phantom `site_specs.ctas` source, and on the first post-roll run that class is GONE — it declared only resolvable sources and was refused by a DIFFERENT, pre-existing rule (template/schema sync: 43 schema fields, 42 template variables, one orphan `score_label_high`). ⚠ **A measured figure I predicted wrongly and am correcting rather than quietly restating: I sized `aspect_paths` at ~5.1 KB from `length(aspect)+length(key)+3`; the real rendered block is 10,292 chars, because that arithmetic ignored the indent and the `(N sites)` suffix.** Package builds and all tests pass from a clean `git archive HEAD` export; the three guards are mutation-proven by running the mutations (removing the `is_active` condition fails both heal tests; removing the heal call fails both; re-inlining a separate sort in the guard message fails the one-vocabulary test); migration 565's double-apply refusal was INDUCED after applying, not merely asserted; live prompt verified 1/1/1 (guard open / placeholder / anchor survived), 17,647 → 18,816 chars.
- **what:** `load_existing_component` becomes the pre-generation half of the whole BIRTH GATE rather than of the field rule alone, and every contract it states is computed by the gate's own functions so offer and enforcement cannot drift. Three parts. (1) **Field contract:** the `section_type` lookup stays primary and byte-identical (it is the selector's query), and on a miss it calls the store's own `resolveStorageIdentity` — reporting only when `ident.IsRegeneration`, so a create and a divert-to-create advise nothing. A bare `lookupBaseComponent` was rejected: it would *manufacture* refusals on the two diversion paths (a divert-to-create has no contract; a divert-to-existing-scoped-row's contract is the SCOPED row's schema). (2) **Source vocabulary:** `known_aspects` via the guard's own `LoadKnownSpecAspects` + a newly extracted `KnownAspectsSorted` (now the single rendering used by BOTH the phantom-aspect refusal message and the advisory), `known_query_bases` via `queryresolve.KnownQueryBases`, and `aspect_paths` — resolvable `aspect.key` leaf paths carrying **site coverage**, because the gate validates only the first path segment, so bare aspect names would trade a loud refusal for a silent blank. Rides every return path including no-component-found, since a creation invents an unresolvable source as readily as a regeneration. (3) **`section_type` healed on the REJECTION path**, `is_active`-gated — healing makes a component selectable and migration 036 deactivates broken ones so pages stop choosing them, so it fills a metadata gap on rows already treated as live and never revives one.
- **the honest limits, recorded because a reader will otherwise overclaim this:** enumeration is guidance, not enforcement — the gate remains the backstop. The aspect-name half is now also reachable *reactively* since `bugs_open/345`'s migration 555 (applied 2026-08-22 11:08Z) puts the refusal text, which lists every aspect, into the next generation; **this entry's unique residual is the leaf paths and coverage, the first generation saved, and items that have already spent their attempts.** The class is RARE — `[MEASURED 2026-08-22]` 72 section components created since 2026-05-07 and none carries a phantom aspect at rest; what changed is the CONSEQUENCE, since the birth gate went live 2026-08-18 and turned a silent blank into a parked page. Deep-path validation is deliberately OUT (guarantee-altering, RFC track, the 309/362 lane's).
- **sources:** `bugs_open/337`; `docs024_key_docs_latest/bugfix_337_token_cap/` (PLAN, NOTES, RUNBOOK, README); COUNCIL_SUBMISSION_2026-08-22b, corr `9efde776-a210-42bc-aa99-899d0d301c67`
- **relations:** CLC-004 (corrected by this — its section_type-only key is the defect); CLC-003 (the field rule this feeds); CLC-018 (the source guard whose vocabulary this states); CLC-020 (`resolveStorageIdentity`, reused rather than reimplemented; its self-heal is the one now also on the rejection path); CLC-006 (the function pin, which went silent whenever the advisory did); `bugs_open/282` (offer-vs-accept drift — the same shape); `016b` §9/092 (the closed, council-approved precedent: one predicate for the writer's allow-list and the gate's accept-set)
- **verify-later:** `load_existing_component_action.go` (`resolveContractViaStorageIdentity`, `loadSourceVocabulary`, `loadAspectPathCoverage`); `component_source_guard.go` `KnownAspectsSorted`; `store_generated_component_action.go` `healRejectedComponentSectionType`; `collected_data->'existing_component'` carrying non-empty `field_names` AND `aspect_paths` on a post-roll orchestration; the live `prompt_template` containing `{{if .existing_component.aspect_paths}}` exactly once

### CLC-007 — F5 — regen-added required fallback-less fields strand renderability
- **status:** aspirational
- **status-evidence:** "Flagged F5: extend the F1 guard... Not built now"; still an open flag in a later runbook.
- **what:** The incident's second facet: a regen also ADDED a required field (Tier-C source, no fallback) that no affected site's specs could satisfy — renames strand stored content (CLC-003's concern), required additions strand renderability instead (sections permanently not-ready → carried forever). Proposed guard extension: reject, or force optional/fallbacked, any added required field on a regeneration.
- **sources:** NOTES(43).md §9v; RUNBOOK(49).md Part E F5; HANDOFF(7).md §Flags
- **relations:** F1 guard (CLC-003); section readiness model; carry-forward path
- **verify-later:** store_generated_component_action.go — absence of an added-required-field check

### CLC-008 — F7 — unguarded template swap in update_component_html (residual)
- **status:** partial
- **status-evidence:** "the snapshot INSERT is ALREADY FIXED in current code... Residual: no placeholder⇄schema sync validation on template swaps"; a later runbook flag remains open.
- **what:** update_component_html swaps a shared component's template (snapshotting versions — its old silent snapshot failure on a removed version_note column is fixed) but performs no placeholder⇄schema agreement validation and no field-contract guard, leaving a second, unguarded write path to shared components alongside StoreGeneratedComponentAction (CLC-002). The original F7 framing (an unversioned live swap on a hero component) was investigated and softened; the residual finding is specifically the missing validation, not the original unversioned-write concern.
- **sources:** NOTES(43).md §9aj, §9ak, §9am; RUNBOOK(49).md Part E F7; HANDOFF(7).md §Flags
- **relations:** F1 guard (CLC-003, candidate extension target); Component versioning (CLC-009)
- **verify-later:** update_component_html_action.go — snapshot INSERT columns; absence of schema-sync validation

### CLC-009 — Component versioning via component_versions (and unversioned-write provenance)
- **status:** partial
- **status-evidence:** "Component updated AGAIN 2026-07-03 13:22:44 with NO v2 row... unversioned write path provenance OPEN"; manual snapshots (v2/v3) were taken to backfill.
- **what:** component_versions snapshots (component_id, version_number, schema, template, change_description, changed_by, change_source) are the change history for shared components; change_source records the triggering work item's source (useful provenance). Coverage is incomplete: some write paths historically failed silently or bypass versioning entirely, so manual mirror-the-working-insert snapshots are the established compensation before risky writes, and zero-version updates are treated as an investigation smell. This is the fuller, currently-active picture of the same table an older schema-archaeology unit (tool-lifecycle TL-025) found "unclear whether any writer maintains."
- **sources:** NOTES(43).md §9k, §9an, §9ao, §9bd; RUNBOOK(49).md Part C Step 6
- **relations:** F7 (CLC-008); snapshot-before-change conventions; F8 remediation (v2/v3 snapshots); Component versioning (tool-lifecycle TL-025, the older/thinner evidence base for the same table)
- **verify-later:** component_versions rows for the incident components; snapshotComponentVersion call sites

### CLC-010 — llm_guidance as a per-field generation-steering surface
- **status:** deployed
- **status-evidence:** "Writer prompt renders per-field guidance ⇒ every writer pass on any site was instructed to write [one site's] product" — a real contamination-risk finding; writer config confirmed by direct read.
- **what:** Each input_schema.fields entry may carry llm_guidance, which page-content-writer renders into its generate_content prompt as the field's instruction (alongside name/type/required/description; fallback values notably never enter the prompt). On a shared component this is the highest-leverage contamination/steering surface — it shapes all future content on every consuming site — and therefore must be site-neutral while preserving structural guidance (word counts, an accent-markup rule).
- **sources:** NOTES(43).md §9az–§9bb; RUNBOOK(49).md Part C Step 7 (the 11 neutral strings)
- **relations:** F8 carrier 3; page-content-writer prompt assembly; F8 lint scope; Shared component library semantics (tool-library TLIB-022)
- **verify-later:** page-content-writer default_config generate_content prompt (llm_field_specs block); field attrs on the affected shared component

### CLC-011 — Superseded hypothesis: update_component_html re-renders dependents inline
- **status:** superseded
- **status-evidence:** Original working theory: "the regeneration then re-renders every dependent page... rewritten together at 15:06:12.956, roughly 16ms after the component's own update"; Correction 1: "update_component_html is clean... the synchronized timestamp is just the pending-flag, not a render."
- **what:** The initial working theory held that update_component_html performed an inline dependent re-render (inferred from ~16ms synchronized timestamps) and was the clobber writer. Disproved by reading the action: it only snapshots, swaps, and flags pending; the blame moved through RenderComponentAction and component-creator's workflow (both cleared) to StoreGeneratedComponentAction (CLC-002). Worth keeping as the exemplar of the thread's core epistemics: seven early inferences were each corrected against code/data before any fix shipped — "distrust each early inference until verified."
- **sources:** BUNDLE_component_regen_clobber.md §1 (base version); NOTES(43).md §2 Corrections 1–7, §3
- **relations:** clobber failure mode; StoreGeneratedComponentAction (CLC-002, the actual writer)
- **verify-later:** n/a (historical)

### CLC-012 — teaser-reveal-panel: a second component implementing an existing experience pattern
- **status:** deployed (registered fleet-wide, live on 4 pages of one site; images 2026-07-29 later, carousel arrows + open-state redesign 2026-07-29 evening, a CRITICAL js init bug fixed 2026-07-30 — see below)
- **CORRECTION 2026-07-30**: everything client-side (deep-link open, sibling-close, and the carousel arrows once added) had NEVER ACTUALLY RUN on the live site, since the component's first version. `<script src=".../snippets.js">` sits in `<head>`, non-deferred, executing before the panel's markup later in `<body>` exists — `behaviour.js`'s first line queried the DOM for the panel and always found none, so the whole file no-opped silently, every load. Every prior verification in this entry (`probe_reveal_open_state.py`, the render harness) exercised either static markup or DOM state set directly — none of them ever called `.click()`, so none could have caught this. Found only once the owner reported the arrows genuinely doing nothing and testing switched to simulating real clicks in a headless browser. Fixed with the same `document.readyState` → `DOMContentLoaded` guard `hero-card-carousel`'s snippet already had. Re-verified with real `.click()` simulation post-fix: arrow click now calls `track.scrollBy` (confirmed via a monkey-patched spy) and moves `scrollLeft`; opening a second card now correctly sets the first's `.open` to `false`.
- **status-evidence:** `content_components` row `function='teaser-reveal-panel'`, `component_level='section'`, `is_active`; `js_snippets` row of the same name, `applies_to:["teaser-reveal-panel"]`; render harness `docs024_key_docs_latest/brochure_component_library/scripts/render_teaser_reveal_panel.go` — 23 checks (5 added for arrows/live-region/CSS-ellipsis/open-state-hide), proven non-vacuous by seven mutants across the file (a bodyless item given a body fails 6, an ellipsis continuation fails 1, alt text echoing the hook fails 1, stripping every image fails 1, stripping the arrows fails 2, a literal ellipsis restored to the DATA fails 2, removing the open-state control-hide CSS rule fails 1). Live on fundamentallyai.com `index.html` (retrofitted with images), `capabilities.html` (replaces `services-grid`), `multi-agent-review-council.html` (replaces `info-card-grid`), and `model-fine-tuning.html` (replaces AND MERGES `info-card-grid` + `image-hover-card-grid`, which independently restated 4 of their 6 facts each — the bugs_open/151 pattern, just not caught by that bug's company-wide fact census because these were fine-tuning-specific claims, not the 9 registered `evidence_base` facts). Each panel's images are existing, already-generated site assets (`site_plan_imagery`/`assets`), reused — no new image generation.
- **what:** A section component rendering a panel of teasers — a short hook plus a deliberately unfinished continuation — whose full text opens IN PLACE and gains a shareable `?open=<key>` URL. Reachable by any site: `load_component_library` returns every active section component with no `suitable_site_types` gate, so another workstream can plan it without knowing it was built here. Three properties are the point, and each is a rule some other mechanism would otherwise break: (1) the reveal is native `<details>`/`<summary>`, so it works with JavaScript disabled and the JS snippet adds *only* addressability — with the snippet absent nothing becomes a dead control; (2) the body stays in the DOM permanently, because copy that appears only after a JS call is invisible to the claims gate and to crawlers — the same blind spot as text inside `<svg>` (`claims.go:137` `nonAssertionElements`); (3) an item with no body renders as a plain statement with NO control, and carries no cliffhanger mark — you may only tease what you can deliver. The cliffhanger is marked structurally (`data-continues="true"`), never with an ellipsis, so a truncation checker built on `output_tokens == max_tokens` can distinguish intent from damage. **Image is a fourth, optional, per-item property**: `image_url`/`image_alt`, rendered edge-to-edge above the hook, present in EITHER branch, `image_alt` required whenever `image_url` is set and must not echo the hook. **Redesigned 2026-07-29 evening on direct owner feedback about the served page**: the track is a genuine left/right-arrow carousel at every viewport width now (`grid-auto-flow: column` no longer switches to a wrapping `row` grid above 60rem — that switch was the literal cause of "six cards on two lines"), reusing `hero-card-carousel`'s exact `goTo`/`nearestIndex`/`scrollBy` navigation pattern rather than a second implementation; the visible "…" on a closed continuation is CSS-drawn (`content: "\2026"` on `::after`), never a stored character, because a literal one in the data is what the platform's own no-ellipsis rule (immediately above) forbids — a request for the same visual effect the platform's own rule blocks in the data layer, resolved by moving the effect to a layer the rule doesn't reach; once open, continuation and body read as one paragraph (continuation's colour switches from muted to full-strength, the "Read the rest" control disappears entirely rather than persisting or relabelling). `.trp__body` is capped at a fixed `max-height` with internal scroll, ALWAYS — the stated answer to "how does a dropdown behave inside a horizontally-scrolling carousel": without a cap, an open card growing taller drags the arrows (fixed-position, not computed from a variable track height) out of alignment on every open/close. **Redesigned again 2026-07-30**: opening a card now REPLACES the whole closed block (`.trp__card[open] .trp__text {display:none}` hides hook+continuation+control as one unit, not a per-element colour/visibility tweak), and what appears in its place is ONE `<p>` — `<strong class="trp__body-lead">{{hook}}</strong> {{continuation}} {{body}}`, concatenated with a template-level space so there is no line break at the old cut point; verified against the actual joined text, not just markup presence. `.trp__body` and `.trp__text` horizontal padding unified (`--spacing-lg` → `--spacing-xl` on body) — they had genuinely differed, not just looked like they did.
- **sources:** brochure_component_library/PLAN_2026-07-29_teaser_reveal_panel.md; components/teaser-reveal-panel/{template.html,input_schema.json,behaviour.js,register.sql,update_2026-07-29_images.sql,update_2026-07-29_carousel.sql,update_2026-07-29_carousel_js.sql,update_2026-07-29_textmerge.sql,update_2026-07-29_readiness_fix.sql}; NOTES 2026-07-29 + 2026-07-30 entries (same file)
- **relations:** implements the pre-existing `experience_patterns` row `teaser-detail-deeplink` (kind `micro-journey`) rather than declaring a new shape — the pattern's `section_types` does not yet name it (the experience-register thread's rows to change, proposed not taken); js_snippets delivery lane (NOT `js_content`, `bugs_open/041`); llm_guidance as a steering surface (CLC-010) carries the figure-splitting rule; claims-gate ±70-char context window (`datahelpers/claims.go`); the fine-tuning merge is the same fact-repetition shape as `bugs_open/151`; carousel navigation shares its exact pattern with `hero-card-carousel` (same site) rather than a parallel implementation
- **verify-later:** whether the planner ever SELECTS it (`features_open/017`, unobserved for every component this workstream has built); whether `teaser-detail-deeplink`'s "detail region must be emptied on close" clause should be narrowed — it is a property of vonc's JS-populated implementation, not of the shape; whether the owner prefers the bounded-body-height default over the alternative (unbounded growth, arrows repositioned live) — stated as a reversible choice, not yet confirmed either way

### CLC-013 — One chrome-eligibility predicate: `ResolveChromeComponent` (and the three answers it replaces), plus its PIN sibling `GetChromePinComponent`
- **status:** **⚠ ROLL CONDITION MET 2026-08-10, verified by build provenance.** Chassis `v1.0.1283` is built from commit `d3c09cc74` — read back from BLD-019's stamp on **both** replicas — and `git merge-base --is-ancestor` puts this entry's own cited commit(s) inside it. **The Go code IS in the running binary; every clause below about waiting for a roll is superseded.** This note makes no claim that the feature is EXERCISED — that is a separate condition on a separate clock. Original: LIVE on `v1.0.1225` for the pool/assignment half (`bugs_closed/118`, `166`, `167`). The **pin** half (`GetChromePinComponent`, `chromePinEligibleSQL`, the detector extension) is **built 2026-08-01, not yet live** — inert until a chassis image rolls. Council submissions `5bc232d6-590a-4476-a6b1-4fb6f61751c6` (original), `e242e9d3-1e5a-4b14-a16f-fbff9ca86d35` (repoint), `d73a4b06-a190-426e-bdf7-18d830d06a9d` (build path, APPROVED r3), `21bac2a2-2b46-4883-894f-19d7ec5e5b45` (pin).
- **status-evidence:** `chromeEligibleSQL` / `ChromeSlotFunction` / `ResolveChromeComponent` in `platform/orchestration/actions/component_library.go`; callers `render_site_components_action.go` (`renderAndStoreSiteComponent`) and `link_site_components_action.go`; `chrome_selection_test.go` green, with the ordering assertions proven non-vacuous by an induced fault (both ORDER BY clauses deleted → 2 red, restored → green).
- **what:** ONE predicate answering "which library component may serve chrome function F": `is_active AND forked_from IS NULL AND component_level IN ('site','header','footer','head')`, ordered eligible-first then by `name`. It exists because the question had **three** implementations with three different answers, each wrong differently (measured live 2026-07-31): `render_site_components` had no predicate at all and picked `footer-4-column`, `is_active=false`, on every unassigned slot — then INSERTed that choice into `site_components`, where it outlived every attempt to fix chrome by editing the *active* component; `link_site_components` filtered on `is_active` alone, which picks `header-leopardess`, an **active FORK** of one client's header, because a fork carries its parent's `function`; `GetComponentByFunction` filtered correctly but had no `ORDER BY` over a two-member pool and its winner, `site-header`, is `component_level='section'` — a page-section component, not chrome. The resolver returns `(component, eligible, error)` rather than erroring on an ineligible library: `head` has NO eligible component today (both candidates `is_active=false`) and a head slot that goes unrendered also loses `injectBrandHeadTags`, i.e. its favicon and og-card. An ineligible result logs at ERROR and is reported per-slot as `ineligible_chrome` in the action result — a LIBRARY defect, which no site can fix, so hiding it in a log is how it stayed hidden.
- **EXTENDED 2026-07-31 (same day, `bugs_open/166`): the seam now REPOINTS, not just selects.**
  `repointRetiredChromeSlot` (in `render_site_components_action.go`) moves a slot off a
  **RETIRED** component — `is_active = false`, and nothing else — onto the one this resolver
  names, and the `!force` idempotence exit gained `COALESCE(build_status,'') <> 'pending'` so
  the repoint actually reaches the page. **NARROWED at council round 1 (`60fd06e68`), and the
  distinction is the load-bearing part: eligibility decides what may be CHOSEN as a library
  default; retirement decides what may no longer be SERVED — only the second licenses touching
  an existing assignment.** The first version triggered on ineligibility and would have moved
  three live rows that are deliberate choices, including one site's own ACTIVE FORK, which is
  the supported way to give a site its own chrome. A fork is illegitimate as a default and
  legitimate as an assignment; the same columns answer two different questions.
  **This widens what a shared action GUARANTEES** — `render_site_components` previously
  changed `component_id` only for an UNASSIGNED slot (the `ON CONFLICT DO UPDATE` fallback)
  and now also reassigns an assigned-but-ineligible one. Flagged as risk #1 in the council
  submission (`e242e9d3-1e5a-4b14-a16f-fbff9ca86d35`) rather than slipped in: an operator who
  deliberately pins a slot to a deactivated component will have that reverted on the next
  chrome render, and the supported way to pin one is the lock columns. Measured before
  submitting: **net live change today is ZERO** — all 42 rows are `build_status='rendered'`,
  none pending-with-HTML, and the only ineligible assignments left are the 13 `head` slots,
  for which no eligible alternative exists so the repointer declines. What it changes is the
  NEXT deactivation.
- **EXTENDED 2026-08-01 (`bugs_open/170`): the seam now covers the OTHER store — the style-collection PIN — and it needs a SECOND, deliberately different predicate.**
  Everything above concerns `site_components.component_id`, the per-site *assignment*.
  "Which component serves this site's chrome?" has a second answer in the schema:
  `style_collections.header_component_id` / `footer_component_id`, the collection-level
  **pin**. Until this change the pin had no predicate, no detector and no repair — and it
  **wins**, on both paths. Three consumers dereferenced it with `GetComponentByID`
  (`WHERE id = $1`, no predicate of any kind): `RenderHeader`/`RenderFooter` rendered
  whatever was pinned; **`link_site_components` WROTE it into `site_components.component_id`**
  and NULLed `rendered_html`, so an unguarded pin silently reverts the repoint the entry
  above installs; and `fork_theme_from_site` **copied** the parent's pins into every new
  collection, propagating the bad state to collections that did not have it.
  - **New shared helper: `GetChromePinComponent(ctx, db, id, logger) (*Component, bool, error)`.**
    THE one way to dereference a chrome pin. Same `(component, eligible, error)` shape as
    `ResolveChromeComponent`, and the flag rides on the same query that fetches the row.
    All three consumers route through it or apply its predicate inline; an ineligible pin
    falls through to `ResolveChromeComponent`, i.e. the pool path this entry describes.
  - **`chromePinEligibleSQL` is NOT `chromeEligibleSQL`, and the missing clause is the point.**
    The pin predicate is `is_active AND component_level IN (…)` — it **omits
    `forked_from IS NULL`**. Same asymmetry as retirement-vs-eligibility above, one level
    up: a fork is illegitimate as a library *default* and is the entire point of a *pin*.
    Measured over all four live pins (2026-08-01) the two predicates disagree on **exactly
    one row** — `leopardessconsulting.co.uk` → `header-leopardess`, active and forked — and
    the pin predicate is the one that gets it right. Copying the pool predicate here would
    delete the single correct pin in the fleet while still catching the three wrong ones.
  - **What it GUARANTEES, changed, and stated as such:** a pin is no longer unconditionally
    honoured. `link_site_components` previously linked whatever was pinned and now links the
    library's eligible chrome when the pin is ineligible (the behaviour a NULL pin already
    had); `fork_theme_from_site` now inherits only eligible pins and NULL otherwise (what
    `install_site_composition` already does at install). Consumers named and told in council
    submission `21bac2a2-2b46-4883-894f-19d7ec5e5b45`.
  - **The detector was blind to half its own subject.** `deactivated_site_components` joins
    `site_components` only, which is why three deployed sites sat on a deactivated header and
    four on a deactivated footer with **no work item, no finding and no alert**. It now also
    reads pins, filing `deactivated_component` at `needs_human_review` with **no handler**
    (`rerender-pages` cannot write a `style_collections` row, so routing it there files an
    item unsatisfiable by construction — `bugs_open/166` on a new item type) and item_key
    `deactivated_pin_<slot>` (a shared key would be swallowed by dedup's
    `UNIQUE (site_id, item_key)`). **Measured: 7 items fleet-wide, zero for the legitimate pin.**
  - **the doors:** `TestNoConsumerDereferencesAChromePinUnguarded` (`chrome_pin_test.go`) — a
    **third** scan, matching both original forms (the Go `GetComponentByID(… HeaderComponentID)`
    dereference and a raw SQL pin read), proven by an induced fifth consumer that COMPILES
    CLEANLY and is caught at both. It deliberately does not fire on `RenderComponentAction`'s
    general by-id fetch, which renders arbitrary page sections. And
    `TestChromePinPredicateMatchesTheActionsPackage` (`discovery_checks/`) — a **lockstep**,
    because `discovery_checks` cannot import `actions` (the dependency runs the other way),
    so the detector's copy of the predicate is hand-typed and must be guarded rather than
    forbidden; mutation-proven in both directions (narrowing and widening the level list each
    compile cleanly and each fail with the reason).
  - **[UNMEASURED→MEASURED] the `component_level` clause is INERT today:** all four ineligible
    pins fail on `is_active` alone. It is a forward guard against 167's class arriving through
    a pin, not a clause doing work now.
  - **what it does NOT do:** it does not repair the four `style_collections` rows (they still
    name ineligible components — now ignored rather than served, which is what the detector
    exists to surface), and it changes no live page until the next chrome rebuild
    (`bugs_open/117`: chrome is a stored artefact and no page re-render regenerates it).
  - **third pin column, dormant:** `style_collections.header_home_component_id` is populated on
    **0 of 14** collections and read by **no** Go consumer — the `StyleCollection` struct does
    not model it. Left alone deliberately: guarding a column nothing reads would be inventing
    a consumer.
- **the landmine:** ~~**correcting the selection does NOT repoint an existing assignment.**~~
  **SUPERSEDED for header/footer 2026-07-31** — it now does, once the above is live; and the
  standing fleet was repointed by hand on the owner's ruling (21 assignments, 28/28 slots
  active). The paragraph below is kept because it is the state ANY chassis older than that
  fix still has, and because the `head` half of it is permanent until the library gains an
  active head component. Original text: 11 of 14 sites are pinned to `footer-4-column` and 7 to `header-bold-gradient` through `site_components.component_id`; this seam only decides what an *unassigned* slot gets, so its live blast radius on the day it shipped was one site (`loancalculator.co.uk`, created 2026-07-30, zero chrome rows). Worse, the platform already *detects* the pinned state (`deactivated_site_components` → `deactivated_component` items, raised since 2026-07-17) and routes the repair to `rerender-pages`, **which re-renders the same deactivated component** — so the routed repair is structurally incapable of repairing and the items age to `unresolved`. A green work item is not a repointed slot.
- **the open review question:** ~~the five build-path callers of `GetComponentByFunction` (`RenderHeader`/`RenderFooter`/`RenderHead`) were deliberately NOT switched to this resolver.~~ **ANSWERED AND CLOSED 2026-07-31 (`bugs_closed/167`, commits `8b29404d6` + `11f8b9e08` + `2605d3f92`, council **APPROVED at round 3** `d73a4b06-a190-426e-bdf7-18d830d06a9d`, 11/13 seats, 2 advisory) — the premise was already false when it was written.** All three now use this resolver, and use its answer **only when `eligible`**, falling through to the existing `RenderFallback*` renderer otherwise. Original text, kept because it is the reasoning the asymmetry rested on: *Doing so would change chrome markup on every page build fleet-wide (`site-header`/`site-footer` → the `*-theme-chrome` pair) — a visible change nobody asked for and a different defect from the one `bugs_open/118` filed. So today the build path still renders section-level components as site chrome while the assignment path no longer can. That asymmetry is deliberate and owner-facing, not an oversight.*
  **Why the premise was false:** re-running both queries verbatim, `GetComponentByFunction` and `ResolveChromeComponent` **already returned the same row** for both chrome functions — 118's own fleet repoint activated `header-theme-chrome` at `2026-07-31 12:39:53`, and `ORDER BY name` then sorts it ahead of `site-header` (and `footer-theme-chrome` ahead of `site-footer`). So the fix changed no served header or footer byte; what it removed was the **accident** that today's answer depends on alphabetical order twice over.
  **The trap in "one-line each":** this resolver ALWAYS returns a row, and for `head` — which has no eligible member live — that row is `Document Head`, an 8,523-char `component_level='section'` component. A literal swap of the lookup therefore renders a page section as `<head>`, creating this bug on the one slot that never had it. `eligible` is a **gate**, not a hint, at every build call site.
- **the door (added 2026-07-31, `bugs_closed/167`):** `TestNoBuildPathResolvesChromeByPlainFunctionLookup` in `chrome_build_path_test.go` — a **second** scan, complementing `TestNoChromeSelectionHandTypesItsOwnLookup`. The 118 scan matches hand-typed **SQL** and **skips `component_library.go`**; both exemptions are right for what it guards and both are exactly why it was blind to these three defects, which lived in that file and contained no SQL (they were Go calls handing a chrome function name to the section-shaped lookup). This one matches the **call** form — `Get(?:ComponentByFunction|ComponentWithFallback)\s*\([^)]*"(?:site-header|site-footer|head)"` — covers `component_library.go`, carries the same `scanned==0` blindness check, and was proven in both directions by inducing the original line (FAIL naming it) and removing it (green). It deliberately does not match a variable `function` argument, so the generic section lookup is untouched. **If you add a chrome function name, add it to BOTH scans' alternations.**
- **the remaining gap** ~~(filed as `bugs_open/170`, NOT fixed)~~ **— CLOSED IN CODE 2026-08-01, see the EXTENDED entry above; and the diagnosis in this paragraph was INCOMPLETE in a way worth keeping.** It describes the pin as a read path only ("a fourth path… a pin is not a selection"), which is what the census framing yields — and that framing hid the half that mattered: the pin is also a **write** source (`link_site_components` → `relinkSiteComponent`) and a **copy** source (`fork_theme_from_site`), so it did not merely render a deactivated component, it reverted the repoint installed above and propagated itself into new collections. The count was also short: **four** sites on a deactivated footer, not three. Original text, kept because the asymmetry it names is right and is the whole subtlety: `RenderHeader`/`RenderFooter` consult `coll.HeaderComponentID` **before** the by-function branch, and dereference it with `GetComponentByID` — `WHERE id = $1 LIMIT 1`, **no eligibility predicate of any kind**. So a style-collection **pin** bypasses this whole seam. Live 2026-07-31: three deployed sites (ai-agent-orchestration.com, finetuning.uk, gaswholesalers.com) are pinned to `header-professional-dark`, `is_active=false`. That is 118's class on a **fourth** path — 118's census counted code that *selects from a pool*, and a pin is not a selection. Not fixed with 167 because it moves three live sites to different markup. **Note the asymmetry any fix must respect: `forked_from IS NULL` is correct for pool selection and WRONG for a pin**, because pinning a site to its own fork is the intended use (leopardessconsulting.co.uk).
- **sources:** `bugs_open/118` (the filed case, with two corrections written into it); `docs024_key_docs_latest/bugfix_118_chrome_selection/` (the standing five); `docs024_key_docs_latest/bugfix_167_chrome_build_path/` (the build-path half, and where `chromePinEligibleSQL` was first written and then deleted); `docs024_key_docs_latest/bugfix_170_chrome_pin_eligibility/` (the pin half — RUNBOOK §R2 is the pool-vs-pin predicate check, §R6 the scan-induction recipe); `016b` §9 "Light site renders dark chrome" (recorded the `component_level=section` principle years before the predicate encoded it)
- **relations:** **LOCK-007** (`register/locks.md`) — same construction, deliberately: one named predicate string, callers built from it, and a source-scanning lockstep test that fails when someone hand-types a fourth copy. `component_selector.go` `queryCandidates` — the SECTION selector, which has had the right shape all along (active, unforked, level-filtered) and is the one nobody copied. CLC-001 (one `content_components` row, N instances) is the reuse model this selects within; CLC-006 (silent fork keyed on `function`) is why `forked_from IS NULL` is load-bearing rather than tidy. `bugs_open/083` (detected findings never reach a handler) is the queue the `deactivated_component` items sit in.
- **verify-later:** whether the owner wants the 11 pinned sites repointed (fleet-visible, needs a before/after per layout); whether a `head` component should be activated (changes the build path's `<head>` fleet-wide, since it falls through to `RenderFallbackHead` today); whether the build-path callers should follow.

### CLC-014 — Per-instance component scoping (`{{.InstanceID}}`) and the reuse-safety detector
- **status:** built and **LIVE on the fleet** (chassis `v1.0.1304`, 2026-08-16), council **APPROVED** (round 2, correlation `07635a2f-3605-4e67-9a6d-7636b07f16ca`) — but **NOT yet exercised by any live template**: 0 of 243 active components reference `{{.InstanceID}}`, so it is running and inert. Say "live and inert", not "deployed", and do not read the approval as "the defect is fixed" — the 22 calculator components still ship literal ids, which is why `bugs_open/283` stays OPEN.
  **⚠ STATUS SUPERSEDED 2026-08-24 (Fable review F9 — the line above is a dated 2026-08-16 figure sitting in a field that reads as current): 139 of 297 active templates spell `{{.InstanceID}}` and 374 non-removed `page_components` rows carry a bound `id="c-…"` token (both measured 2026-08-24). The mechanism is EXERCISED, fleet-wide, daily.**
- **status-evidence:** `component_instance_scope_test.go` — 8 tests, three mutations each killed by the test written for it (counter never advances → the repeat test and the end-to-end test fail; the single-section seam given its own shape → the agreement test fails; `TemplateNeedsInstanceID` returns false → its own test fails). `go build ./...` and `go test ./platform/orchestration/actions/` clean on a `git archive HEAD` overlay. **0 of 243** active components reference `{{.InstanceID}}` (live query, 2026-08-16), so nothing in production exercises it — deliberately stated, because "deployed" would overstate it. Deploy of the 08-15 half proven by digest chain: pod `imageID` `sha256:f208f01d…` == local `v1.0.1303` `RepoDigests`, whose `org.opencontainers.image.revision` is `5e075a6f9`, of which `03c1b0b90` is an ancestor.
- **what:** A per-INSTANCE template value, `{{.InstanceID}}`, alongside the pre-existing per-COMPONENT `{{.ComponentID}}`, plus `DetectInstanceCollisions` — a checker for the reasons a page is unsafe to carry a component twice: duplicate element ids, multiple `window.onload` assignments (a single slot; last write wins so earlier components never initialise), inline scripts declaring into global scope (a second `function calc()` replaces the first), and — **added 2026-08-24, commit `120131549`** — **`EmptyElementIDs`, an id that rendered LITERALLY EMPTY**. That fourth class is not a widening of `reElementID`, whose `+` structurally cannot match `id=""`: **ONE empty id is already a defect** (the element is addressable by nothing), so it fails `Clean()` alone, where a duplicate needs two; it is counted not listed, having no string to name; and it is kept separate because the causes differ — a duplicate is a wrong OCCURRENCE, an empty is a failed BINDING, and the live corpus proves them independent (`category-listing`'s `id="{{.category_slug}}"` is a CONTENT field in a template with no `{{.InstanceID}}` in it). ~~In the same commit `RenderTemplate` stopped logging and started REFUSING a template that spells `{{.InstanceID}}` with no token bound~~ — **WITHDRAWN the same day (council `661bcf00` round 1, guardian gating HIGH; commit `c5a0c831e`): the refusal lasted ONE commit.** `RenderTemplate` now PUBLISHES `RenderContext.UnboundInstanceToken` (an OUTPUT field, same shape as `AbsentRequiredFields` — the remedy the `bugs_open/054` ruling itself used on this function), and the authority question — may this seam refuse, and on which callers — is the owner's, in `architecture_review/RFC_050_may_the_render_seam_refuse_an_unbound_instance_token.md` with four answers costed. ⚠ **Nothing reads the field yet** (by design, pending the ruling; the field carries its own delete-me-if-not-armed instruction). The measurements that licensed the withdrawn refusal still stand as facts (2026-08-24: 0 of 972 non-test `.go` files render unbound with a 6-of-6 mutation control; 0 of 2,020 `page_components` rows carry the unbound shape against 374 bound; 0 chrome templates spell the token) — what they cannot do, per the veto'd-precedent RFC_044 shape, is bound tomorrow's caller. `RenderTemplateWithMap` (`rerender_pages_actions.go:818`), the second render path (`bugs_open/260` §13g), ~~has none of this~~ **carries the same report as of `c5a0c831e`** (log-only there — no RenderContext to publish onto), so the class is visible on both paths and enforced on neither. ~~Token is `c<position>` from `page_components.position`~~ **SUPERSEDED 2026-08-16 (commit `32d6e980a`, council round 1 REVISE): the token is the component FUNCTION plus its OCCURRENCE on the page** — `c-mortgages-repayment`, then `c-mortgages-repayment-2`. `position` is unique per page and **is not the same across pages** (measured: the LMC tool slot is position 0 on 7 pages and 1 on the other 16), so it coupled every selector to section order; `page_components.data_uuid` (1,580 rows, 1,580 distinct) is provably unique and per-row, which is the same defect harder. The chosen rule is identical on every page for a component appearing once — the property `oracle.py`'s 170 literal-id checks need — at the cost that uniqueness is DERIVED from the page's ordered section list rather than read off a column, which is what the detector pays for. `slot_name` was rejected outright: 13 active pages repeat one, and the only unique index forbids merely BYTE-IDENTICAL rows. **Revising the shape was free because nothing consumed it; re-run the 0-of-243 query before revising it again.**
- **the binding seams, and why they are the unit:** every producer goes through `BindInstanceToken(rc, token)` or, for a path that renders ONE section and cannot see the page, ~~`BindSingleSectionInstanceToken(rc, function)` (occurrence 0 into the same rule)~~ — **corrected 2026-08-24: that binder is RETIRED (`9ba3293e7`, CLC-031); the single-section seam is now `DeriveAndBindInstanceToken`, which derives the REAL occurrence (loop items on the build path, stored predecessors on the editor) and falls back to 0 only with no context at all. `pattern-check.py`'s seam regex and its remediation text both follow.** `InstanceCounter` is THE derivation, walked in position order by `assemble_from_library` and `rerender_page_sections`. Naming the seams is what makes the guarantee mechanical: `scripts/pattern-check.py`'s `check_unscoped_component_render` fires on any changed non-test `.go` that calls a `RenderTemplate*` helper and calls neither seam, unless allow-listed with a measured reason (chrome, `<head>`, and `cmd/component-render-check`, an offline lint that writes to `doc_notes` and never to a served page). Proven on the motivating case before being trusted: **4 findings at HEAD, 0 after**. ~~`RenderTemplateReportingMissing` additionally logs at Error when a template needs the token and none was bound~~ — **corrected 2026-08-24 (Fable review F4): that symbol was folded away in the 2026-08-21 one-spelling unification** (`render_seam_one_spelling_test.go` pins exactly two exported spellings); the report lives INLINE in `RenderTemplate` and, since `c5a0c831e`, also publishes `ctx.UnboundInstanceToken`. It reports and does **not** substitute, because a token invented at that layer would either collide or disagree with the one the page's other paths use for the same instance.
- **the trap this exists to close (`bugs_open/283`):** an id must be unique per document — `getElementById` returns the FIRST match and ignores the rest. 173 of 240 active components hardcode at least one element id (166 literal-only) and 100 bind by `getElementById`, so a second instance renders perfectly, accepts input, responds to its button, and reads the FIRST instance's fields while writing into the FIRST instance's results. Across the 22 active calculators, `btn-calculate` is shared by NINE different components — so "list all the calculators on one page" collides between DIFFERENT components, not merely between two copies of one.
- **⚠ why `{{.ComponentID}}` is NOT this, and must not be repurposed:** it resolves to the CONTENT COMPONENT row id on `rerender_page_sections_action.go` (:494) and `v3_site_actions.go` (:2055) — identical for every instance — while `assemble_from_library.go` (:277) builds a per-instance string from the loop index. Same placeholder, two different guarantees. Adopting it for reuse namespaces nothing on the path that actually serves re-rendered pages, and on a single-instance page the two substitutions are indistinguishable, so a canary passes either way. Full entry in `LANDMINES.md`. ~~7 live components depend on the existing semantics~~ **CORRECTED 2026-08-15 — it is 5, and the council's `prior_art_librarian` seat caught it by asking for the query I had not run.** `SELECT count(*) FROM content_components WHERE is_active AND html_template LIKE '%{{.ComponentID}}%'` returns **5** (`faq`, `generic-text-block`, `mechanism-flow`, `evidence-timeseries`, `pricing`). The 7 was a different measurement — components templating ANY id — which also catches `product-grid` and `category-listing`, and those use their own domain fields (`{{.product_id}}`, `{{.category_slug}}`), not `ComponentID`. The decision to leave `ComponentID` untouched is unchanged by this; the evidence for it was overstated. Full incident in `WRONG_CALLS.md`.
- **the residual, stated rather than absorbed:** ~~`RenderComponentAction` falls back to `InstanceTokenFromSlot`~~ **RETIRED 2026-08-16 — that helper is DELETED.** The council's `reuse_agent` seat objected that it wrote the same KEY under a weaker GUARANTEE and a different SHAPE, so one instance's ids depended on which action last rendered it — reproducing, under a new name, the `{{.ComponentID}}` trap below. It was right. The residual that remains is smaller and differently shaped: the two single-section paths (`RenderComponentAction`, the section editor) supply **occurrence 0** to the one rule, which is correct wherever the component appears once — every interactive component on every live page today — and where it is wrong the instances take the same token and **collide**, which `DetectInstanceCollisions` reports. A possibly-wrong INPUT to one rule, not a second rule; `TestSingleSectionToken_agreesWithCanonical_andCollidesDetectably` asserts both halves, so a later change cannot silently make the single-section path disagree with the canonical one. > **⚠ CORRECTED 2026-08-24 — THIS RESIDUAL IS FIXED (CLC-031, commit `364e80b7f`), AND THE LICENCE QUOTED ABOVE WAS NEVER TRUE OF THE TEMPLATES THAT MATTER.** "Correct wherever the component appears once — every interactive component on every live page today" was measured about `getElementById` TOOL components on 2026-08-15. It was never true of the RFC_032 §8 **repeatable** templates, which are precisely the ones that repeat (up to ×6), so the single-section paths re-collided every multi-instance page they touched — **any** per-section render, not just a rebuild. Both paths now derive the real occurrence: the build path counts its own loop's already-rendered items, the section editor counts stored predecessors position-exactly. Occurrence 0 survives only as the universal fallback. Filed as `bugs_open/383`; the defect was live at the served artefact on 2026-08-24 (`gaswholesalers.com/pricing-transparency.html` served `id="c-generic-text-block"` twice).
- **sources:** `bugs_open/283` (§9 is the round-2 record); `platform/orchestration/actions/component_instance_scope.go` (+ `_test.go`); the binding sites — `assemble_from_library.go`, `rerender_page_sections_action.go`, `v3_site_actions.go`, `section_editor_actions.go` ×2; `component_library.go` (`RenderTemplate`, inline — ~~`RenderTemplateReportingMissing`~~ folded away 2026-08-21); `scripts/pattern-check.py` (`check_unscoped_component_render`, `INSTANCE_TOKEN_ALLOWED`); commits `03c1b0b90`, `1e19aa6ab`, `32d6e980a`; council correlation `07635a2f-3605-4e67-9a6d-7636b07f16ca` (rounds 1 and 2); `LANDMINES.md` § `{{.ComponentID}}` is the estate's per-instance id convention on ONE render path
- **relations:** **CLC-001** (one `content_components` row, N `page_components` instances) — this is the seam that makes that reuse model actually safe for components carrying ids and scripts, which CLC-001 never addressed; owner ruling 2026-08-15 (reuse should be a genuine property of the platform, prompted by "if we chose to list all the calculators on one page we'd hope it would work"); owner ruling 2026-08-02 §2 (new authority on a shared seam ships opt-in, unsafe default OFF) governs the guard that consumes this detector; the `check_unrepaired_component_write` census in `pattern-check.py` (same idiom, same staleness problem — a set of call sites that grows while nobody is looking)
- **⚠ what this does NOT close (council `bug_historian`, round 2, recorded as a known-unresolved root cause rather than a risk):** Go's `missingkey=zero` silently blanks **any** absent field in **every** template. This adds a report for **one** field name. Every other required field on every other template is exactly as exposed as before, and the general fix — refusing or marking any bare placeholder that renders empty — is a fleet-wide render-behaviour change that this seam deliberately does not attempt. `enforce_instance_scope` also stays **off**, so a collision is recorded and nothing acts on it. ⚠ `render_guardian`: flipping it on **before** the 13 known-colliding pages are fixed would itself be a high-severity fail-loud violation. The only honest closing order is convert → re-measure → arm.
- **verify-later:** whether the 22 calculator templates get converted (the thing that would move this entry to "exercised"); whether the detector should run as a build gate and at what default; **`architecture_review/RFC_032` — unify the three render context-builders so `ComponentID` means ONE thing** (filed 2026-08-16 because the `reuse_agent` seat objected, correctly, that the previous "filed as a follow-up" claim had no locator) — **RULED 2026-08-22 (owner): converge on `{{.InstanceID}}` and RETIRE `{{.ComponentID}}`, rather than re-pointing it. Step 1 (converter pass 0) is committed; the five templates, the deletion of ComponentID's two bindings and the dead `assemble_from_library` `ReplaceAll`, and the re-render are NOT done — 18 pages still serve duplicate section ids and 11 serve `id=""` as of 2026-08-22.** RFC_032 §8 also carries a dated correction to its own §2 table: the `assemble_from_library` path it called reuse-safe has never produced a per-instance id; **the RFC_022 exception this was approved under expires the moment a live template references `{{.InstanceID}}` — and the trigger for that IS built**: `instance-token-adoption-check`, a daily CronJob (CLC-016 below), so nobody has to remember. A trip means `RFC_032` is owed a round, not that something broke

### CLC-015 — `input_schema` house dialect enforced at the table: `chk_input_schema_no_legacy_dialect` (+ `IsLegacyInputSchemaDialect`, the birth-path twin)
- **status:** **LIVE, both halves.** Constraint applied 2026-08-16 10:24Z and recorded (refusal induced, SQLSTATE 23514; census 0); Go half live on **v1.0.1304**, both chassis replicas, stamp `5de6cddbe` with `58b0111ac` confirmed an ancestor and the pre-fix tripwire sentence ABSENT as a negative control. Council `aba82416` APPROVED r1. ⚠ **One branch is unexercised and expected to stay so**: the birth-path Check 4 refusal (the component-creator has emitted 0 legacy schemas in 69 rows all-history) — proven by unit test, mutation and binary probe, NOT by a live generation. Do not read its silence as "the check never worked".
- **status-evidence:** probe run (COMMIT swapped for ROLLBACK): guards passed, `UPDATE 3`, verify NOTICE, ALTER, COMMENT; live table read back unchanged. `TestLegacyDialectConversionMatchesProjection` (live before/after fixture) + `TestIsLegacyInputSchemaDialect`, both proven able to fail by mutation. `go build ./platform/...` clean on a `git archive HEAD` overlay.
- **what:** `content_components.input_schema` has ONE dialect, `{"fields": {...}}`. The retired JSON-Schema dialect (`{"type":"object","properties":{...},"required":[...]}`) is REFUSED by a CHECK constraint on the table — `input_schema IS NULL OR NOT (input_schema ? 'properties')` — for every producer: seeds, scripts, Go, the admin UI, a restore from any `bak_*` table. The same predicate is exposed to Go as `datahelpers.IsLegacyInputSchemaDialect` and `store_generated_component` calls it before its write so an LLM that emits JSON-Schema fails with a message naming the fix rather than SQLSTATE 23514. Migration 437 converted the last three legacy rows (report-dossier, mechanism-flow, evidence-timeseries) to v2 by exactly `SchemaContentFields`' projection, so no reader saw a different field set. Nested `properties` under `fields.<x>.items` is the shape of an ITEM and is fine — only the top level is constrained.
- **why a constraint and not a Go gate (`bugs_open/265`):** a code comment declared the dialect extinct on 2026-07-21 (0 of 173); four legacy rows were seeded in the three weeks after — ALL by hand-authored SQL (`created_from='manual'`), none by the component-creator (0 in 69 rows). Components are routinely written by migrations and by hand with no commit at all (RFC_009), so the table is the only seam every producer crosses. A census goes stale the day after it is taken; a constraint is checkable at any moment (`\d content_components`).
- **⚠ what the tripwire now means:** `WarnLegacyDialect` (six call sites) is now `Error` and its firing means *the constraint is gone* or the schema came from a `bak_*`/`component_versions` copy or an in-memory value — not "someone seeded the old shape". Check `pg_constraint` first.
- **⚠ for whoever writes a component seed:** a top-level `"properties"` key is refused. Write `"fields"`. The refusal message names this constraint; the seed's `ON CONFLICT DO UPDATE` will fail too. That is the intended behaviour, not a bug to work around.
- **sources:** `sql_for_agents/437_content_components_refuse_legacy_input_schema_dialect.sql` (+ `_ROLLBACK`); `platform/orchestration/datahelpers/component_schema_fields.go` (`SchemaContentFields`, `WarnLegacyDialect`, `IsLegacyInputSchemaDialect`); `store_generated_component_action.go` Check 4; `datahelpers/legacy_dialect_conversion_test.go` + `testdata/legacy_dialect_conversion_437.json`; `bugs_open/265`; lane `bugfix_265_legacy_dialect_unrepresentable/`
- **relations:** `bugs_closed/026` (the reader's projection — the fail-open this constraint makes unreachable from the table; the projection is KEPT as defence in depth); CLC-010 (`llm_guidance`, one of the projected keys); the six existing CHECK constraints on `content_components` (house pattern); RFC_006 (daily-check pattern — deliberately NOT used here: a constraint does not drift)
- **verify-later:** apply 437 + induce a refusal (RUNBOOK); post-roll, the Check 4 literal in the binary; whether `report-dossier` `body` should be `source: renderer` (owning lane's call — its seed says never LLM-authored); `site-header`'s wrapper-less v2 shape (a third shape the reader cannot see, harmless today)

### CLC-016 — `instance-token-adoption-check`: a daily tripwire that watches for an ARCHITECTURE EXCEPTION expiring, not for a defect
- **status:** **RETIRED 2026-08-18, after firing exactly as designed** — tripped 07:40 UTC on the first converted template (adopters 0→1), expiry acknowledged by RFC_034 (DECIDED), CronJob deleted from the cluster the same day per its own retire-on-trip rule; final retirement note in `doc_notes` (`subject_key='instance-token-adoption'`, categories include `retired`). ~~Manifests remain in the repo as the worked example of the demand-control pattern.~~ > **CORRECTED 2026-08-21 (283 lane):** the manifests are REMOVED from the repo — a deleted CronJob whose kustomize overlay survives is one `apply -k` from resurrection as a daily-failing job, and the demand-control pattern now has a LIVING worked example in the same 07:40 slot: CLC-024 (`instance-scope-sweep`), which supersedes this check with the control's polarity updated for a converted estate. What was lost: nothing — `git show` serves the old files, and CLC-024's check.py credits the lesson to this one's docstring by name. Lived three days, ran daily, caught real corpus drift (243→244 components) on its second run, and its trip was the planned signal, not a defect. ~~deployed and proven — CronJob live in `ai-persona-system`, first run 2026-08-16 15:29 UTC.~~
- **status-evidence:** `kubectl create job --from=cronjob/instance-token-adoption-check` completed `SUCCEEDED 1`; job log printed the QUIET branch with `adopters 0, control 5, active 243`; the `doc_notes` row exists (`subject_type='pipeline'`, `subject_key='instance-token-adoption'`, 15:29:24 UTC). Both report branches were exercised via `--stdin` before deploy with exit codes checked (trip 1, quiet 0), so the tripped branch is known reachable rather than hoped-for; `kubectl kustomize` builds 2 resources.
- **what:** a daily CronJob (07:40 UTC) that counts active components whose `html_template` references `{{.InstanceID}}`. **0 = the RFC_022 narrow exception that licensed CLC-014 still holds; non-zero = it has EXPIRED and `architecture_review/RFC_032` is owed a round before more templates convert.** Writes one `doc_notes` row per run **including a quiet one**, so a MISSING row means the job did not run — a different thing from "the exception still holds" — and exits non-zero on a trip so the Job shows as failed. Files: `deployments/kustomize/services/instance-token-adoption-check/` (base `check.py` + `cronjob.yaml` + kustomization, production overlay `uk_001`), modelled on `optional-key-budget-check` and `single-owner-carriers-check`.
- **⚠ THE POLARITY IS INVERTED vs every sibling check, and reading it wrong causes damage.** A trip is **not** a defect: converting templates is `bugs_open/283`'s intended next phase and the thing that actually fixes the bug. The trip says *an owed review is now due*, never *someone broke something* — so **do not "fix" a trip by reverting a conversion.** The report says this in its own words, because a failed Job is read by whoever is on hand rather than by whoever built it.
- **⚠ RETIRE IT ONCE IT TRIPS.** A tripwire left failing daily is how a real signal becomes one people mute.
- **the general mechanism worth stealing — a DEMAND CONTROL inside the check itself:** this check's healthy answer is **zero**, and a broken query, a mis-escaped `LIKE` or an empty table also return zero, so a quiet result would otherwise be indistinguishable from a dead check. Every run therefore also counts `{{.ComponentID}}` — **through the same `LIKE`, the same escaping and the same statement** — and the job **REFUSES (exit 2)** if that comes back 0 rather than reporting a zero it has not earned. Any check whose expected output is an absence needs this shape; it is the mechanical form of "a post-fix zero needs a demand control".
- **why a CronJob rather than the pattern-check the council's architecture seat suggested:** the seat asked for a *written trigger* and proposed a `pattern-check.py` finding. `pattern-check.py` is a **commit-time lint over repo files**, and a component's `html_template` lives in a **database column** written by the component-creator agent, by hand-authored SQL, by migrations and by the admin UI — four routes, none of which passes through a commit. Same decisive argument as its two siblings: the number this watches changes by a route no commit can carry.
- **sources:** `deployments/kustomize/services/instance-token-adoption-check/` (the `check.py` docstring carries the full reasoning); `bugs_open/283` §10.5; `architecture_review/RFC_032` §6; council correlation `07635a2f-3605-4e67-9a6d-7636b07f16ca`, `architecture` seat's round-2 objection
- **relations:** **CLC-014** (the seam this watches); `RFC_022`'s narrowing (the exception whose third condition — zero live consumers — this measures); `optional-key-budget-check` and `single-owner-carriers-check` (same shape, same doc_notes convention, opposite polarity); MEMORY [[a-post-fix-zero-needs-a-demand-control]]
- **verify-later:** ~~it has no makefile target~~ **`deploy-instance-token-adoption-check` added 2026-08-17 (`e836afca8`) once the makefile was quiet**; whether the daily `doc_notes` row is actually READ by anything (a quiet row nobody reads is a different failure from a missing one); **the 283 canary (item `38efde3b`, dispatched 2026-08-17 evening) will TRIP this check at the next 07:40 UTC run once it converts — expected, acknowledged by RFC_034 DECIDED; observe the trip, then retire the CronJob**


### CLC-017 — `scope_component_instance`: the deterministic instance-scope converter, with a gate that REFUSES the half-state
- **⚠ EXTENDED 2026-08-22 — pass 0, and a refusal the gate could not make.** The declared-id harvest uses a brace-EXCLUDING class (`[^"{}]`), so an id written as the RETIRED per-instance placeholder `id="{{.ComponentID}}"` never entered the declared-id set and the converter refused with *"template declares no literal element ids"* — the opposite of the truth on a template that plainly declares one. **Five active templates were in that state as of 2026-08-22** (`generic-text-block`, `faq`, `mechanism-flow`, `evidence-timeseries`, `pricing`), so filing them would have drained as five polite no-op completions. Pass 0 swaps that placeholder for `{{.InstanceID}}` — the **bare** token, because the wrapper id IS the instance identity — and runs BEFORE the harvest, which now reads its output. Report field: `TemplatedIDSwaps`. **Both armed birth guards inherit it** (`ScopeToolBirthTemplate` calls this function), so `create_tool_component` and `deploy_tool_to_site` now convert a newborn spelling the old name instead of refusing it. Second half: a `{{.ComponentID}}` surviving OUTSIDE an id attribute now REFUSES the component, because it would render `id=""` on every instance and **neither the gate nor `DetectInstanceCollisions` can see that** — `reElementID` requires a non-brace character, so two instances read CLEAN while colliding on empty ids. **⚠ THAT BLINDNESS IS CLOSED AS OF 2026-08-24 (commit `120131549`) — do not repeat the claim.** `DetectInstanceCollisions` now reports `EmptyElementIDs` (CLC-014), and `GateConvertedTemplate` gained a **hard-error branch** on it — never judged-pool, because an element nothing can address is a transform defect, not a script that needs rewriting. This closes the case the pass-0 refusal could only handle by its *cause*: the gate's own `id="-` test sees only `id="{{.InstanceID}}-suffix"`, and **6 of the 140 active token-bearing templates spell `id="{{.InstanceID}}"` EXACTLY** (2026-08-24: `generic-text-block`, `faq`, `pricing`, `mechanism-flow`, `evidence-timeseries`, `illustrated-text-block`), whose unbound render is `id=""` and was invisible to it. Pre-roll blast radius, measured with `instanceaudit --gate` (CLC-021): **140 of 140 ship as-is, 0 refused by the new branch** — demand control, injecting one empty-id residue into the same export gives 139 clean / 1 EMPTY-ID / exit 3. 0 live templates are in that mixed state as of 2026-08-22 (control: 87 carry a literal id at all), so it refuses nothing today. Ships uncommitted-free at `67d34e6c1`, six tests mutation-proven; implements `architecture_review/RFC_032` §8 (owner ruling 2026-08-22). **INERT until the next chassis roll under a bumped tag.**
- **status:** LIVE since `v1.0.1307`; 69 rows converted by the 2026-08-18 batch (283 §13). > **CORRECTED 2026-08-19:** the batch's outputs carried a defect this entry's own "⚠ failure direction" paragraph believed impossible — the completeness check grepped only for the literal forms the passes rename, so bindings travelling through variables/helpers/concatenation shipped dangling on **32 of 69** rows, 14 serving (`bugs_open/324`). Pass 5 + an independent detector now close the class (CLC-021); the gate refuses it from 2026-08-19.
- **status-evidence:** full actions suite green on a `git archive HEAD` overlay; the two load-bearing fixtures are LIVE ROW BYTES pinned to `testdata/instance_conversion_*_283.html` (exported 2026-08-17); `TestConvert_realGlobalScriptComponent_isRefusedToJudgedPool` proves the §2.1 refusal on the real `mortgages-repayment` bytes (9 ids / 7 calls, the bug file's own counts, asserted); running-image check 2026-08-17 15:0x UTC: pod digest `f90a7e88…` = revision `6a782274b`, which predates this commit.
- **what:** `fix_type: scope_component_instance` (alias `instance_scope_conversion` for item_type routing) in `FixComponentTemplateAction`. `ConvertTemplateToInstanceScope` prefixes every id the template DECLARES with `{{.InstanceID}}-` and moves every reference surface with it: `getElementById` (both quote styles), `label for=`, ARIA id-reference attributes (including space-separated lists), CSS `#id` selectors, `querySelector('#id')`, `href="#id"`, and **`data-*` attributes whose value is exactly a declared id** — the last found by the real fixture, not by design: `tool-css-unit-converter`'s copy buttons carry `data-target="result-px"` feeding `getElementById(targetId)` at runtime, a reference no call-site pass can see. Foreign ids are never touched. `GateConvertedTemplate` renders TWO instances through the real render layer with the real token derivation and runs the real detector: duplicate ids across two tokens = hard error (transform defect); unscoped script = `fixed:false, action:"needs_script_scoping"`, **nothing written** — the RFC_034 §2.1 refusal, because ids-only conversion ships a page that reads clean while every button runs the last instance's logic. Snapshot to `component_versions` (`change_source='scope_component_instance'`) before the UPDATE.
- **⚠ the failure direction is the design:** every ambiguity REFUSES rather than half-converts — hex-ambiguous ids (the `#id` pass would corrupt `color:#abc`), already-converted templates (idempotency is a refusal, not a silent second rewrite), any declared id still reachable in a binding position after the passes (concatenation, unrecognised quoting). A refused component is exactly as broken as yesterday; a half-converted one is broken in a new way with the warning light removed.
- **⚠ a `fixed:true` does NOT change any page.** Pages serve stored `rendered_html`; the conversion reaches visitors on the page's next rerender+deploy, and the result payload says so. The programme's per-component sequence is convert → rerender → redeploy, and the b2_verify byte-identical baseline must be reset BEFORE the first LMC conversion.
- **sources:** `platform/orchestration/actions/component_instance_conversion.go` (+`_test.go`, + the two pinned fixtures); `fix_component_template_action.go` (`fixScopeComponentInstance`); `bugs_open/283` §11–§12; `RFC_034` (DECIDED: hybrid, LMC first, through the framework); census updated in `component_write_guard.go` header + `fanOutIntendedWriters`
- **relations:** **CLC-014** (the seam whose token this writes into templates); **CLC-016** (the tripwire that fires when the FIRST conversion ships — §5 of RFC_032: that firing means an owed review, not a defect); `repair_template_slots` (the conventions this copies); CLC-021 (the binding detector+repair that audits this converter's output); CLC-022 (the judged pipeline for the pool this refuses to)
- **verify-later:** rides the next chassis roll under a BUMPED tag (the 2026-08-17 `v1.0.1305` rebuild demonstrated the same-tag cache trap — pods restarted and served yesterday's digest); the work-item seed for `instance_scope_conversion` items (not written); the judged-pool pipeline for the 25; whether `inferFixTypeFromItemType` should map the item_type (currently the alias in the switch covers it only when spec.fix_type or item_type reaches the action's config paths)

### CLC-018 — Source-vocabulary birth gate (`component_source_guard`) + the queryresolve vocabulary made answerable
- **status:** **LIVE on v1.0.1314 (2026-08-19), verified at the binary with controls** — digest-matched (pod imageID == RepoDigests), revision `d3590ca4638d…` stamped in /proc/1/exe (present control) with a fake sha absent (negative control), and all three lane commits (`0df9f1be9` guard/vocabulary, `e21b172f0` wiring, `e5c9029dc` tests + durable fail-open record) ancestors of it. ⚠ **The refusal branch is unexercised and expected to stay quiet** (CLC-015's precedent): proven by unit test, MUTATION (wiring deleted → test failed → restored), and binary probe, NOT by a live generation — 0 rejections since the roll, and the only post-roll component writes were TOOL-path (a different birth route). Do not read its silence as "the check never worked"; the first natural component-creator generation is the live demand control, and 478's migrated component already passes the gate's predicate on all 7 fields. ~~wiring held for 303's symbol~~ superseded the same evening: 303 landed `markup_balance.go` at HEAD (`6d962bcf8`), messaged the all-clear, and the wiring committed with their store-action hunk as a declared same-file passenger. Council `fdb032c6` **APPROVED round 2** (2026-08-18, 4 advisories none high — triage in the lane NOTES). Deploy verification when a roll ships it: binary probe with CONTROLS (`kubectl exec <pod> -- grep -aq '<sha>' /proc/1/exe` for e5c9029dc-or-descendant present AND a fake sha absent — the provenance LOG line scrolls on agent-chassis, do not lead with it), then a demand control: one phantom-source generation must produce the queryable SOURCE_GUARD rejection row.
- **what:** A generated component's input_schema can declare `source` strings the platform cannot resolve — a `site_specs.<aspect>` no site has ever carried, a `query.*` name the resolver never registered, a prefix outside the vocabulary — and until this, nothing between generation and render checked: the value silently `skip_field`ed (even when `required:true`) and pages shipped complete-looking, data-less markup (bugs_open/309: an article index whose six cards had zero anchors, for four months). Two pieces: (1) `queryresolve`'s dispatch switch became ONE handler map, with `IsKnownQueryName(name)` / `KnownQueryBases()` exported and answered from that same map, so a validator and the dispatcher cannot drift; (2) `sourceVocabularyIssues(schemaJSON, knownAspects)` — pure, callable from anything — flags the three phantom classes, with `loadKnownSpecAspects` reading `SELECT DISTINCT aspect FROM site_specs` live (fail-OPEN on read error: only the aspect half goes quiet). Calibrated against every active component's schema: flags exactly the 61 fields (58 phantom-aspect + 3 junk-prefix) and 7 unknown query names that already resolve nowhere; zero refusals of anything that works.
- **sources:** `platform/orchestration/actions/component_source_guard.go` (+`_test.go`); `platform/orchestration/actions/queryresolve/queryresolve.go` (`queryHandlers`, `IsKnownQueryName`, `KnownQueryBases`) + `vocabulary_test.go`; census queries in `docs024_key_docs_latest/bugfix_309_unclickable_index_cards/RUNBOOK_309_phantom_source_components.md`; `bugs_open/309` §8
- **relations:** CLC-003 (same Layer-1 blockingIssues seam, and the interplay is deliberate: the field-contract guard keeps field NAMES while this refuses phantom SOURCES, so a regeneration of one of the 11 censused components must repoint sources or go by migration — the fabricated-fallback precedent); CLC-015 (the dialect twin: table-level + birth-path); bugs_open/309 §5 candidate 2 (a daily audit of EXISTING config is the unbuilt other half — build it ON `sourceVocabularyIssues`, not on a second predicate, or they drift)
- **verify-later:** the wiring hunk in store_generated_component_action.go once committed; whether the 309 case repair (the other lane's candidate 1) landed and passes the guard

### CLC-019 — Markup-context structural tag balance (`content.StructuralTagCounts` / `UnbalancedStructuralTags`): THE truncation-signature counter
- **status:** **LIVE + VERIFIED 2026-08-19** on chassis `v1.0.1314` (build `d3590ca46`, binary-probed both replicas; all fix commits ancestors — incl. the `store_generated_component_action.go` hunk that rode the CLC-018 lane's `e21b172f0`). Council **APPROVED round 2** (`Council-Reviewed: 70cf0da5`; round 1 REVISE, all four advisories acted on). Two tools born post-roll, zero birth refusals with demand present; the mention-class differential is pinned by tests at the shipped code, first natural live exercise pending (`bugs_closed/303` close-out).
- **what:** The single shared answer to "does this HTML bear the truncation signature (a structural tag opened and never closed)?" — used by every birth/load/rewrite guard and the discovery sweep. Replaces raw `strings.Count(folded, "<script")`-style substring counting, which read a tool's own JavaScript (`// protect <style> blocks`, `/<script[^>]*>/`) as unterminated tags and refused an entire tool category at birth (`bugs_open/303`); both live HTML-manipulating tools were passing by ZERO margin. Counts a token only when tag-shaped (name then whitespace/`>`/`/`) and in markup context: `<!-- -->` bodies skipped, `<script>`/`<style>` raw-text bodies skipped to the first close token exactly as a browser reads them — so a mid-JavaScript cut still leaves the `<script` unmatched and every true positive is kept. Lives in `platform/content` (leaf) so `discovery_checks` can import it too — which retired the import-cycle-forced hand mirror of the pair list. Calibration 2026-08-18, old-vs-new over the full live population: component_versions 264 rows, 26 flagged by both, 0 flips; 121 comparative pairs, 1 block under both (the 012 write); content_components 300 rows, new set a strict subset (8 of 11), the 3 cleared rows hand-read as CSS-comment mentions — two of which had open `truncated_component` work items that were FALSE ALARMS.
- **sources:** `platform/content/markup_balance.go` (+`_test.go`, incl. `TestSubstringCountWouldRefuseMentionTool`, which pins the OLD predicate so the central fixture cannot stop exercising the defect); consumers: `plan_sections_action.go` `toolTemplateValid`, `component_write_guard.go` `hasUnbalancedStructuralTags`/`componentRegressionIssues`, `discovery_checks/check_truncated_component.go`, `check_tool_completeness_action.go`; calibration harness + exports in the lane RUNBOOK (`docs024_key_docs_latest/bugfix_303_tool_birth_guard/`)
- **relations:** `bugs_open/303` (the defect and the fix record); `bugs_open/012`/`021`/`046` (the true-positive class the counter must keep — and does, 0 flips); `bugs_open/024` (why the tool path exists at all); CLC-018 (the held same-file wiring this unblocks); the LANDMINES "tool-birth guard counts tag SUBSTRINGS" entry (remedy note appended 2026-08-18)
- **verify-later:** after the next chassis roll — retry an `add_tool` whose description mentions tags with angle brackets (the LANDMINES workaround becomes unnecessary); the two false-alarm work items `91007600`/`6e2c9ebf` resolve as balanced via their verifier

### CLC-020 — Site-aware storage identity (`resolveStorageIdentity`): a foreign-depended base row is never another site's regeneration target
- **status:** **LIVE + DEMAND-PROVEN.** Council APPROVED round 1 (`fc3ac5f4`); rolled 2026-08-19 (v1.0.1315) and re-verified at the binary on every image since through **v1.0.1322**. **Six real diversions**, all attempt-0, all incumbents byte-identical to pre-pinned md5s: loanzy.uk car-finance (08-19; page 0→4 `<input>`) then interest-rate-stress-test / compare-loans / standard-calc / overpayment / settlement (08-20; three of those pages serve 5-6 `<input>`, one blocked by the `253` floor on an unrelated slot, one by `pages.status='archived'`). **The ORIGINATING case exercised 2026-08-21** (remortgagecalculator.uk `mortgages-repayment`): the scoped name was derived correctly — the store was then refused DOWNSTREAM by `bugs_open/345` (a retry regenerated from identical inputs), fixed separately by that lane. The verify-later below is DONE except the `loans-credit-health-check` specimen itself, which fails UPSTREAM of the store (`bugs_open/337`, the 16k output cap) and cannot exercise this seam.
- **what:** `store_generated_component` used to decide regen-vs-create purely by "does any base row hold the LLM-chosen function?" (CLC-002/CLC-006) — so a component another site depends on, which the planner could NOT reuse (template-guard-dropped at load, or no `section_type`), was still FOUND by the store and treated as the row to regenerate; the field-contract guard (CLC-003) then correctly refused, for ever. `resolveStorageIdentity(ctx, db, function, requesterSiteID, requesterDomain)` now takes a dependent census (`page_components→pages` UNION `site_components`, joined to `sites` — the ownership view the schema does not carry) and, on a foreign collision, DIVERTS the write to `<function>-<domainSlug>` as a fresh BASE row (`forked_from` NULL — fresh generation, not the incumbent's lineage, and every selection path filters `forked_from IS NULL`) with `section_type` = the REQUESTED section name, so the requesting page's rebuild resolves it via the selector and later sites can reuse it. Own-site collisions and unknown-requester (no `input_data.site_id`) keep legacy regeneration semantics; base+scoped both foreign refuses loudly. The regen UPDATE also self-heals `section_type = COALESCE(section_type, requested)`.
- **⚠ landmine closed / landmine kept:** closed — one site's component can no longer deadlock another site's build (the 311 shape: three identical field-contract refusals, page served without its section). Kept — the DIVERTED row's `function` is site-suffixed while its `section_type` is not: resolve it by section_type (the selector) or exact function, never by assuming function == section name.
- **sources:** `platform/orchestration/actions/component_storage_identity.go` (+`component_storage_identity_test.go`, mutation-proven: diversion deleted → foreign-collision test fails on the uncovered incumbent UPDATE); wiring + section_type self-heal in `store_generated_component_action.go`; `bugs_open/311` (mechanism, evidence, verification recipe); lane docs `docs024_key_docs_latest/bugfix_311_component_keys/`
- **relations:** CLC-002 (the lookup this subsumes — its query moved verbatim into `lookupBaseComponent` with the 2026-05-06 is_active-ordering note); CLC-003 (the guard that exposed the deadlock and remains the backstop on legacy paths); CLC-006 (this is F4's cross-site worst case, closed from the store side); CLC-001 (the shared-reuse model this preserves: diverted rows are base + selector-visible, shared by design); `deploy_tool_action.go` `domainSlug` (the naming convention reused); `check_unresolved_sections` (its retry loop converges instead of livelocking once a valid selector-visible row exists)
- **verify-later:** after the next chassis roll — re-drive one failed `needs_new_component` (loanzy.uk, `loans-credit-health-check`): expect a new base row `function='loans-credit-health-check-loanzy-uk'`, `section_type='loans-credit-health-check'`, incumbent `824e3309` md5(html_template) UNCHANGED, and the page linking a non-empty component_id after rebuild; the `COMPONENT_COLLISION_DIVERTED` finding row is the queryable demand signal

### CLC-021 — Binding completeness for renamed ids (`UnprefixedBindings` + pass 5 + `repair_instance_scope_bindings` + `instanceaudit --bindings`)
- **status:** built 2026-08-19, tested on pinned live bytes (mutation-proven both directions), committed this session; the detector is INSIDE `GateConvertedTemplate` from this commit, so it guards every future conversion; the repair fix_type rides the next roll; repair seed = `sql_for_agents/487_seed_bindings_repair_HOLD.sql`.
- **what:** the question "does anything in this script still refer to a renamed id" answered independently of the renamer. `BindingIDSets` recovers declared ids (bare or converted state) and the static prefixes of DYNAMICALLY-declared ids; `ApplyBindingPasses` renames class A (bare literal equal to a declared id — arrays, config objects, helper calls), class B (declaration-by-concatenation in JS-built markup), class C (lookup-by-concatenation on a declared id's `-`/`_` prefix); refuse-contexts (comparison, case label, object key, computed access) are skipped and REPORTED, never guessed. `UnprefixedBindings` is the detector (script bodies outside comments; includes the composition hazard: prefix `p` concatenated while both `x` and `p+x` are declared). `RepairConvertedTemplateBindings` = pass 5 alone, idempotent, for already-converted rows. `cmd/instanceaudit --bindings` = corpus census + done-check (exit 3 while any row dangles after repair). **`cmd/instanceaudit --gate` (added 2026-08-24, commit `120131549`) is the sibling census for the ACCEPTANCE gate**: it runs the real `GateConvertedTemplate` — tokens bound, rendered through the real render layer — over every exported row that spells the token, and reports ships-as-is / judged-pool / **hard-error-EMPTY-ID (broken out separately)** / other-hard-error, exit 3 on any hard error. Use it to measure a gate change against the LIVE corpus before shipping it, which is what the default mode structurally cannot do: that mode doubles the RAW template, and an empty id is only visible in a real render. It REFUSES a zero-considered export rather than printing a clean result over nothing. The default mode also gained an `emptyIfDoubled` column (report-only).
- **the bar it exists for:** any future mechanical RENAME over templates (ids, classes, data attributes, function names) needs its completeness asserted by an independent reference-space detector, not by re-grepping the rename's own patterns — 016b §9 (2026-08-19) is the transferable pattern; `bugs_open/324` the incident.
- **sources:** `platform/orchestration/actions/component_instance_bindings.go` (+`_test.go`, fixtures `testdata/instance_bindings_*_283.html` = live bytes incl. one pre-conversion snapshot); wiring in `component_instance_conversion.go` (pass 5 + gate) and `fix_component_template_action.go` (`fixRepairInstanceScopeBindings`); `cmd/instanceaudit`
- **relations:** CLC-017 (audits and repairs its output); CLC-019 (the truncation counter — different question: cut vs dangling); CLC-022 (consumes the detector as gate check 4 and inherits the 5 refusals)

### CLC-022 — The judged (LLM) instance-scope pipeline: `scope_component_instance_judged` + the fixer's self-routing branch
- **status:** Go half built+tested 2026-08-19 (this commit); workflow half is migration `sql_for_agents/486_judged_instance_scope_pipeline_HOLD.sql` — HOLD until the roll carrying the fix_type, then live. Nothing judged has converted yet.
- **what:** the RFC_034 shape-C LLM half, framework-native. The mechanical arm's `needs_script_scoping` refusal IS the router (its result now carries the ids-converted template + inline-handler inventory + unplaced bindings); `check_scope_route` branches to `scope_script_llm` (sonnet, 32k, NO tolerate_truncation) whose brief is script-only surgery; `apply_judged_write` re-derives its own baseline from the row and refuses on: two-instance gate not fully clean, markup changed outside `<script>` bodies (derived expectation: baseline minus on*= attributes, whitespace-normalised), id-set drift, any unprefixed binding, or the comparative collapse guard — refusals write nothing and land `needs_human_review` via `check_judged_result`. Writes snapshot `change_source='scope_component_instance_judged'`. Delivery: generic pages via the 460-vintage `template_changed` rerenders; OWNED pages via new `create_section_edit_delivery` (`apply_section_edit` binds the token — closes 283 §13.6's "owned pages deliver nothing" gap for every template fix, not just this lane's).
- **⚠ queue shape:** 25 original judged rows + 5 refusals from the CLC-021 repair = 30. LMC-first per the owner ruling; canary `loans-standard-calc`; b2_verify rebaseline + oracle selector lockstep are owed BEFORE the first LMC conversion (PLAN §5).
- **sources:** `fix_component_template_action.go` (`fixScopeComponentInstanceJudged`), `component_instance_judged.go` (+ tests), `PLAN_2026-08-18_judged_pipeline.md` (+ 2026-08-19 addendum), migration 486
- **relations:** CLC-017 (mechanical sibling; shares `writeScopedTemplate`); CLC-021 (gate check 4); the LMC lane's `oracle.py` (the independent behavioural witness, 170 checks); `update_component_html` (the OTHER llm-rewrite write path — page-scoped and fenced, deliberately NOT used here: 22/25 judged rows are section-level and one is multi-page, which that fence refuses)

### CLC-023 — Instance-scope BIRTH guard (`ScopeToolBirthTemplate`): the flow half of 283, at the generator's own write
- **status:** BUILT + TESTED 2026-08-21 (unit suite green on the archive overlay incl. both-direction controls: unconverted-doubled collides, converted-doubled clean, gutted self-converted refused); INERT until a chassis roll carries the 2026-08-21 commit; ARMED-ON-ROLL by migration 520 (`enforce_instance_scope: true` on tool-generator's `save_tool` — applied 2026-08-21, key unread by pre-roll binaries and deliberately tolerated: the spec declares it as reporting-only ConfigKeys, the 336 lesson applied in the same commit as the reader).
- **what:** `create_tool_component` runs the PROVEN deterministic converter + acceptance gate (CLC-017's `ConvertTemplateToInstanceScope` + `GateConvertedTemplate`) over every generated tool before anything persists. Armed: mechanically convertible templates are CONVERTED at birth (ids prefixed, bindings renamed, occurrence-0 rendering bound for the verbatim `rendered_html` writes — placements carry the template verbatim, so an unbound placeholder would serve literal text); unprovable scripts (global scope, dynamic unprefixable ids, dangling bindings) are REFUSED — at birth a refusal is cheap: the item retries against a prompt that teaches the rules (520's rules 21–22). Unarmed: record-only, bytes verbatim. A self-converted template (the LLM followed the prompt) is VERIFIED, never trusted. Chosen over prompt-only teaching at the owner's direction 2026-08-21: a prompt is a request, the guard is a control, and keeping the id-rewrite in Go avoids the placeholder-in-prompt-template escaping trap and keeps ONE implementation of the rule.
- **sources:** `platform/orchestration/actions/tool_birth_instance_scope.go` (+`_test.go`); wiring in `create_tool_component_action.go` (spec ConfigKeys + guard call + regen `renderedHTML`); `docs/agent_docs/sql_for_agents/520_…` (+ROLLBACK); WRONG_CALLS.md 2026-08-21 (the producer-census entry this closes)
- **relations:** CLC-017 (the transform+gate this reuses — one implementation, both seams); CLC-022 (the judged pipeline; deliberately NOT invoked at birth — regeneration beats judging when there is no live behaviour to preserve); CLC-024 (the sweep behind every OTHER door); CLC-018/CLC-019 (the sibling birth gates in the same action)
- **verify-later:** WIRING at the live seam is unproven until the first post-roll generation (the unit tests prove the helper, not the action's integration — stated honestly rather than grep-asserted); the `deploy_tool` fork path has NO birth guard (older door, sweep-covered; measure whether it still fires before building one)

### CLC-024 — `instance-scope-sweep`: the standing backstop CronJob (files conversion work daily; supersedes CLC-016)
- **status:** LIVE 2026-08-21 — deployed, first run FILED 26 items for the 28-row unconverted corpus (2 parked rows correctly dedup-skipped), all 26 drained through the proven pipeline within the hour; doc_notes row written (subject_key `instance-scope-sweep`); report branches exercised via `--stdin` pre-deploy; takes CLC-016's 07:40 UTC slot.
- **what:** daily census of active+PLACED components using `getElementById` without `{{.InstanceID}}`; files one `instance_scope_conversion` item per row (item_key `instance-scope:<8hex>`, NOT-EXISTS dedup that leaves parked rows parked). Carries NO conversion intelligence — the council-approved fixer pipeline routes mechanical/judged/refused. Demand control inverted from CLC-016 for a converted estate: the known-non-zero is now the ADOPTER count (90+); zero adopters ⇒ the matching broke ⇒ REFUSE to run (exit 2). One doc_notes row per run including clean ones.
- **sources:** `deployments/kustomize/services/instance-scope-sweep/` (check.py docstring has the full argument); owner ruling 2026-08-21 ("the sweep should exist as part of the improvement loop")
- **relations:** CLC-023 (bounds the main producer; this clock catches every other door — hand SQL, migrations, admin UI, `deploy_tool`); CLC-016 (predecessor, retired-on-trip; same doc_notes convention, opposite job); the immune-system sweeps (same files-work-items shape)
- **verify-later:** whether the daily row is READ by anything; first scheduled (non-manual) run at 07:40 UTC 2026-08-22; the filed-count parse fix (excludes psql's INSERT tally line) is deployed but its first honest count lands with the next filing run

### CLC-025 — `component-source-vocabulary-check`: the AT-REST half of the source-vocabulary rule (CLC-018's other door)
- **status:** **LIVE + PROVEN IN-CLUSTER 2026-08-22.** Image `v1.0.1326` (built by the fleet release), CronJob applied, manual Job run and read at the **pod's** `state.terminated.exitCode` = **0**, and the `doc_notes` row confirmed written (`source='component-source-vocabulary-check'`, 17:55Z) — a pod exiting 0 having written nothing would be a broken reporter, not a clean estate, so the row is the positive control on the report path. Council round 2 REVISE (8 of 12 seats approve; 3 moved object→approve); the one objection that found a real defect was acted on — see the baseline provenance note below. ~~NOT YET DEPLOYED — the image does not exist at the pinned `v1.0.1324`.~~
- **⚠ IT SHIPPED ONCE IN A STATE WHERE IT COULD NOT RUN, and every signal said otherwise.** `v1.0.1326` deployed clean, the CronJob listed, and a binary probe found `--component-source-vocabulary` compiled in **with a must-be-absent control** — all true, and every scheduled run would have exited 2: the image is `FROM alpine` + `COPY` of the binary alone, while the mode loads its baseline from a **repo-relative path**. Fixed by MOUNTING the baseline from a `configMapGenerator` rather than baking it in, because the baseline is designed to SHRINK as `bugs_open/362`'s repairs land and an image copy would go stale on the first repair. That forces the real file into the kustomize base with a symlink from the documented docs path (kustomize refuses a generator source outside its root) — the `component-fallback-check` arrangement, same reason. **No rebuild was needed: `--baseline` was in the binary all along, so this was manifest-only.** Full trap in `LANDMINES.md`.
- **what:** a daily CronJob (07:20 UTC) asking CLC-018's question of the components ALREADY in `content_components`, which the birth gate structurally cannot reach: a component is routinely inserted or altered by a hand-written migration or hand SQL and never passes through `store_generated_component_action` at all (the standing LANDMINE, and exactly how `blog-listing_pre_037` got there). `config-key-audit --component-source-vocabulary --report` reads the live library, the live `site_specs` aspect set and a `page_components→pages` live-instance join direct from Postgres (`fleetdb.go`'s route; no pods/exec RBAC here), and writes ONE `doc_notes` row per run including clean ones.
- **⚠ IT CALLS THE RULE, IT DOES NOT MIRROR IT — and that was the design constraint, set by CLC-018's own entry** ("build it ON `sourceVocabularyIssues`, not on a second predicate, or they drift"). So the guard now returns `[]SourceIssue{Field,Source,Class,Message}` via `SourceVocabularyFindings`, and `SourceVocabularyIssues` is a thin projection over it for the birth gate — whose pre-existing tests pass untouched, which is the evidence its behaviour is unchanged. The three older daily checks (`component-fallback`, `optional-key-budget`, `single-owner-carriers`) carry a Python re-implementation pinned by a parity test; that shape was **considered and rejected here** because a mirror can only DETECT drift where a call makes it unrepresentable. The owner's 2026-08-14 preference for Python answered a different objection — compiling INSIDE the job — which a pre-built image does not do, and ten such Go check images now exist.
- **⚠ THE FAIL-OPEN ASYMMETRY WITH CLC-018 IS DELIBERATE — do not "make the callers consistent".** The birth gate SKIPS the aspect half when `site_specs` is unreadable (it must not block all generation, and records `SOURCE_GUARD_ASPECT_SET_UNAVAILABLE`; `[MEASURED 2026-08-22]` 0 rows all-history). The audit exits 2 instead: an audit has nothing to block, and a daily report that silently skipped a third of its rule is the blind-pass landmine one rung higher. Two vacuity refusals, failing in OPPOSITE directions and both exit 2 — **0 components** is the silence where "0 findings" reads clean; **0 aspects** is the FLOOD, marking every `site_specs.*` source in the estate as phantom.
- **⚠ THE BASELINE IS THE RISK SURFACE, and its three defences are structural, not disciplinary.** **69** fields across **17** active components were already offending **as of 2026-08-22** (51 phantom aspect · 14 unregistered query · 4 outside vocabulary; **6** components live on **46** page instances, same date), and they cannot be zeroed in one commit — repairing ONE (migration 478) took an owner ruling and a shrink-guard incident. So they are grandfathered by `component_source_baseline.json`, which (1) is keyed on the exact `(component_id, field, source, class)` tuple, so a NEW bad field on an already-baselined component is RED — *this is the allow-list-silences-your-own-detector trap, and the narrow key is the whole answer to it*; (2) REFUSES any entry not dated `2026-08-22`, so it cannot be appended to; (3) governs the EXIT CODE and never the REPORT — the daily row names all 69 every morning, so it cannot go quiet. Grandfathering is CONDITIONAL for the eleven dormant components: deploying one turns the job red. A stale entry is also red — that is the ratchet's pawl, and **the file's shrink history is `bugs_open/362`'s burn-down.**
- **status-evidence:** controls against the REAL live library — day-one GREEN at exactly 69/17/6/46; the REAL pre-478 `blog-listing_pre_037` schema (from migration 478's own backup table, not a synthetic) re-entering the library goes RED; a second bad field on already-baselined `info-card-grid` (32 live instances) goes RED; a repaired component turns its entries stale; both vacuity refusals exit 2. Then SIX mutation proofs, each defeated in the working tree and each KILLING its test, file restored byte-identical: `baselineKey` ignoring field+source, the closure-date check removed, dormant-wake disabled, the pawl returning nothing, the audit diverging from the birth gate, and a route pointed at a non-existent file. HEAD verified by extracting `git archive HEAD` and building there, not in the working tree.
- **sources:** `cmd/config-key-audit/componentsourcevocabulary.go` (+ `_test.go`); the rule at `platform/orchestration/actions/component_source_guard.go`; `build/docker/backend/component-source-vocabulary-check.dockerfile`; `deployments/kustomize/services/component-source-vocabulary-check/`; baseline + census query + its four measurement gotchas in `docs024_key_docs_latest/bugfix_309_unclickable_index_cards/`
- **relations:** **CLC-018** (the same rule on the other door — one function, two callers, and the fail-open policy differs by caller); `bugs_open/309` §5 candidate 2 (this closes it); `bugs_open/362` (the 69 repairs, which every baseline row names by path so a grandfathered finding is ROUTED, not excused); CLC-024 / `component-fallback-check` (the daily-sweep-behind-the-birth-gate pattern this follows); `bugs_open/358` (why the findings go to `doc_notes` and not `agent_error_log`)
- **verify-later:** the first scheduled run after the image is built and pushed — until then this has never executed on the clock; whether the daily row is READ by anything (CLC-024 carries the same open question); and whether `STRUCTURAL_KEY_CARRY_MISS`, now consumed by `cmd/content-loss-check`, ever fires for one of the six LIVE components here — it cannot fire for the eleven dormant ones, because that writer only runs when a page is BUILT

### CLC-026 — Component provenance stamp: `RenderedTemplateSHA` → `page_components.component_version_id` (RFC_046 phase 1)

- **status:** **LIVE AND PROVEN WRITING 2026-08-24** — after a day inert. ~~BUILT 2026-08-22, INERT BY CONSTRUCTION, NOT YET ROLLED~~ → rolled 2026-08-22 15:10:31Z, council-approved 18:02Z, **and it wrote nothing for a day** until CLC-028 repaired the carriage. [MEASURED 2026-08-24] rows born after the ~09:00Z roll are stamped at near-100% per hour (58/58, 24/24, 117/119); **control: 0 of 987 pre-roll rows** — nothing backfills. `component_versions` gained 39 `render_stamp` rows across **39 distinct components, 1.00 each**, so it settles rather than becoming a log. **And the stamp is TRUE, not merely present:** of 245 stamped rows, 239 name their component's current template and the 6 that do not are one component edited AFTER those rows were written (version 10:17, rows 10:55, component edited 11:15) — a stale row, which is the mechanism WORKING and the first live demonstration of what it is for. The write half only. See the correction below for the day it spent inert.

> **CORRECTED 2026-08-23 — THIS MECHANISM WAS LIVE FOR A DAY AND STAMPED ZERO ROWS.**
> [MEASURED 2026-08-23] **820** `page_components` rows born since the roll, **0** stamped;
> **0** `component_versions` rows with `change_source='render_stamp'`; **0** `component version:`
> log lines in 24h — consistent with the resolver's *silent* early return, not with it refusing.
> Control on the other side: the pre-roll cohort is **0 of 1,146**, as designed (nothing backfills).
>
> **Cause: the emission described below never reached the save.** `extractSectionFromMap`
> (`v3_site_actions.go`) rebuilt each section's metadata from a hand-written six-key allow-list, and
> `rendered_template_sha` was not on it. Disconfirmable control: of **546** live `sections_metadata`
> elements since the roll, **0** carried the digest while **546** carried `component_id`. The copy
> ran; the key was dropped. Two further keys (`stripped_markdown_fields`, `copy_gate_findings`) were
> being dropped by the same list.
>
> **So every "the seam emits it / the carrier carries it / the save reads it" clause below was true
> in isolation and false as a chain** — the two ends were verified and the middle was assumed. Fixed
> 2026-08-23 by **CLC-028**, which also closes a hazard this entry created: with the digest finally
> arriving, Layer 2's splice would have stamped the *discarded* render's version onto the carried
> tool. Un-severing alone would have turned "no stamp" into a FALSE stamp on the very rows
> `bugs_open/357` is about. Nothing reads `page_components.component_version_id` (**0** readers, **0** writers, **0 of 1,930** rows populated — measured 2026-08-22, before this change), so it cannot alter what any page serves. **It does NOT fix `bugs_open/357`** — it builds the fact the fix is made of. Saying otherwise would be the "detected but never blocked" shape the council gate caught in this lane's round 2.
- **what:** `RenderTemplate` (the estate's ONE render spelling, **15** non-test call sites as of 2026-08-22) sets `RenderContext.RenderedTemplateSHA` to the SHA-256 of the template text it actually executed — an OUT-FIELD on the exact `AbsentRequiredFields` precedent (`bugs_open/342`): the seam **reports**, it does no I/O, because resolving the digest needs a database handle it does not have and must not acquire. `RenderComponentAction` emits it as `rendered_template_sha`; `extractSectionsFromMetadata` carries it onto `SectionData`; `resolveComponentVersionID` (`save_sections_component_version.go`) turns it into a `component_versions` row id at the single `INSERT INTO page_components`, in the same statement as the bytes, on the IMP-052 principle. `carryStoredSection` forwards an existing stamp so a carried row does not silently downgrade to unknown.
- ⚠ **CORRECTION 2026-08-22, same day, from the council's `reuse_agent` seat:** this entry first said `component_versions` had a single writer. It has **five INSERT sites across four files as of 2026-08-22** (`fix_component_template_action.go` ×2, `store_generated_component_action.go`, `update_component_html_action.go`, and this one). The claim was wrong and is corrected here rather than only in the lane docs. The reason `bugs_closed/277`'s nine components had NO versions is that a template edited by a hand-written SQL migration passes through **no Go writer at all** — which is precisely why the stamp is taken at RENDER time, not at edit time. Each writer allocates `version_number` by its own `MAX+1`, so version-number races are a property of the TABLE, not of this writer.
- **why the VERSION and not just the component:** `component_id` records which component was *asked*; it does not record which template text *ran*, and the two diverge the moment a template is edited. `bugs_closed/277` closed with **15** rows permanently unrepairable (as of 2026-08-22) because the templates that produced their bytes exist nowhere — `component_versions` held zero rows for any of the nine components involved. This does not repair those; it stops the class accruing.
- **REUSE, NOT ADDITION:** `page_components.component_version_id` already existed, purpose-built and never wired. No new column, no new table, no new concept. ⚠ **`site_plan_sections` has an identically-named column which IS live** and accounts for nearly every grep hit — qualify by table or the dormant one looks busy.
- **landmine — the resolver REFUSES far more often than it answers, and that is the design.** It stamps only two cases, both facts: a carried stamp, or a digest still matching the component's current template. A template edited between render and save yields **no stamp**, and it must never fall back to "the component's newest version" — that would rebuild `bugs_open/357`'s defect (a confident answer manufactured from something that merely correlates) inside its own fix. `TestComponentVersion_TemplateChangedSinceRenderIsNotInferred` pins it **with a decoy row made available and attractive**, because the obvious spelling of that test ("expect no query") passes against inferring code too — sqlmock refuses the unexpected call, the resolver errors out, and the assertion succeeds for the wrong reason. `..._DecoyControl_AnInferringImplementationTakesIt` is the paired control proving the decoy is actually on offer.
- **sources:** `platform/orchestration/actions/save_sections_component_version.go` (+ `_test.go`); `component_library.go` (`RenderContext.RenderedTemplateSHA`, the stamp in `RenderTemplate`); `v3_site_actions.go` (`RenderComponentAction` emission); `save_page_sections_action.go` (`SectionData` provenance fields, `extractSectionsFromMetadata`, the INSERT); `rerender_page_sections_action.go` (`loadStoredSections`, `carryStoredSection`)
- **relations:** `architecture_review/RFC_046` (**RULED 2026-08-22, option 1** — identity is inferred five ways as of 2026-08-22 and stamped none); `bugs_open/357` (what that costs: **22** live rows as of 2026-08-22 declaring `hero` while storing a whole tool); `bugs_open/342` (the out-field precedent and the report-not-refuse doctrine at a shared seam); IMP-052 / `bugs_open/229` (same-statement stamping, and `page_component_history`); `bugs_closed/277` §8 (the drift class this stops accruing)
- **DOWNSTREAM CONSUMER, added 2026-08-24 by the `bugs_open/378` lane — read this before changing what the stamp means.** `378` replaces `content_components.usage_count` (a counter written on one of three resolution paths and read as a merit signal) with a figure **derived from `page_components` bindings**. The stamp was offered as its substrate and **declined for now, on measurement, not on doubt about the mechanism**: `[MEASURED 2026-08-24]` a stamped-only signal would see **39** of 151 active section components against **108** for any non-removed binding, and — the deciding reason — since nothing backfills, it would measure *how recently a component was rebuilt* rather than how proven it is, which is `378`'s own defect one epoch over. **The stamp SHOULD become that signal's primary input later**, and `378` keeps the definition in ONE place so the switch is a single edit. ⚠ **The switch condition is NOT a coverage percentage.** Coverage approaches but never reaches 100% by design — a page that is never rebuilt is never stamped, so the last population to convert is the long-stable component, which is exactly what a merit signal must credit. The condition is *"the unstamped remainder is small enough to be a stated exception rather than the majority"*. Lane: `docs024_key_docs_latest/bugfix_378_usage_count_derived/`.
- **verify-later — at the artefact, with its control:** after the roll, rows created *after* it carry a non-NULL `component_version_id` while rows created *before* it stay NULL (**that asymmetry is the control**; nothing here backfills, so the old rows moving would itself be the finding). Then the stamp must be TRUE, not merely present: for a canary row, the `html_template` at the stamped version must re-render to the stored bytes **byte-identically** — the same round-trip gate CQ-029 uses. And `component_versions` should gain roughly one row per component then settle; a count climbing on every render means the get-or-create is comparing wrongly and the table has become a log.

### CLC-028 — The `sections_metadata` carry contract: one declared key list, a deny list with reasons, and a parity test that fails on drift

- **status:** **BUILT + COMMITTED 2026-08-23** (`a2e2fbac2`), council `Council-Submitted: 73a638c7-f2a0-4a69-8145-96fc9a89c7bb`. **Inert until the next chassis roll**, and it changes no page: nothing reads `page_components.component_version_id` yet.
- **what:** a section's metadata travels producer → `CompilePageSectionsAction` → `save_page_sections`, and the middle hop **rebuilds the map from scratch**. It used to copy a literal list of keys written out by hand, so a key a producer set and the list did not name was dropped in total silence. Now `sectionMetadataCarryKeys` (`section_metadata_keys.go`) is the single definition of what travels, `sectionMetadataDeniedKeys` names each deliberate drop **with its reason**, and `carrySectionMetaKey` performs the copy preserving each key's original per-key behaviour exactly (`component_id` rendered `%v` because producers supply both `string` and `uuid.UUID`; `content_data` taken whole when non-nil; every other key carried only when a **non-empty** string, because `""` means unknown and unknown must reach the database as NULL).
- **⚠ THE BAR THIS MEETS: a new producer key is invisible to its author.** Anyone adding a key to a section result needs to know this list exists, and nothing told them. That is why it is registered rather than left as a local tidy-up.
- **the class it closes, which had already recurred once:** `bugs_open/189` lost `stored_slot_name` at **this same function**. The remedy then was to add that one key and pin it with `TestExtractSectionFromMap_ForwardsStoredSlotName` — a good test, with a documented mutation, that asserts **its own key**. It could not see the next key, and the next key was RFC_046's identity stamp (CLC-026's correction above). **Three** keys were being dropped when this was written, not one. So the pin is now the CONTRACT: `section_metadata_parity_test.go` fails when a producer sets a key in neither list, and — the half that would have caught `bugs_open/357` on day one — when `extractSectionsFromMetadata` READS a key the carrier does not carry.
- **⚠ AST, NOT GREP, and the reason is in this package already.** `render_seam_one_spelling_test.go` states it: a text-matching rule makes your COMMENTS load-bearing, and the parity test file's own prose names every key in the contract. It reuses that file's `parsePackageFuncs`/`callsNamed`, whose vacuity control (fails if it parses <100 functions) is inherited too; both new halves add their own control that fails loudly if the scan finds nothing, because a renamed variable would otherwise make the test pass while checking nothing.
- **`adoptCarriedProvenance` — provenance describes the bytes you were HANDED, not the ones you arrived with.** Layer 2's splice replaces a section's HTML with stored interactive bytes; it used to leave the fresh render's digest in place, describing markup just thrown away. **⚠ It is a FUNCTION rather than two lines at the splice because the property is untestable at the action level:** `resolveComponentVersionID` swallows its own query errors and returns "no stamp", so a section carrying a stale digest and one carrying none write the SAME nil bind — the mutation that removes the clearing **PASSED** an action-level test on the first attempt (a guard in series standing in for the property under test). `TestAdoptCarriedProvenance_…` pins the decision; `TestSplice_UsesAdoptCarriedProvenance` (AST) keeps the seam calling it.
- **status-evidence:** four mutations, each defeated in an isolated `git archive HEAD` tree and each KILLING its test, file restored after: an undeclared `result["zz_undeclared_key"]` in `RenderComponentAction`; `rendered_template_sha` deleted from the carry list; the clearing line deleted from `adoptCarriedProvenance`; the splice's call re-inlined. Full `platform/orchestration/actions/...` suite green at committed HEAD (extracted and built there, not in the working tree, because another lane's WIP reddens the working-tree suite).
- **producers, named — the list a future author must join:** `RenderComponentAction` (`v3_site_actions.go`, the compile path), `RerenderPageSectionsAction` (`rerender_page_sections_action.go:746`, fresh re-render — **this one never emitted the digest at all**, though `:662` calls `RenderTemplate` and holds it 85 lines earlier), `carryStoredSection` (same file, the carry path — the only one CLC-026 wired correctly), and the copy-gate wrapper `annotateSectionNegation`. They are enumerated in `sectionMetadataProducers` in the parity test; **a producer missing from that map is checked by nothing**, which is the hole the rerender's fresh entry sat in.
- **why a declared list rather than carry-everything:** `sections_metadata` is persisted into `orchestration_states.collected_data`, which averages **210KB** per run and peaks at **2.6MB** [MEASURED 2026-08-23], and **7** live agent types consume it (`content-reviewer`, `page-build-handler`, `pageflow-builder`, `page-rebuild`, `page-rerender`, `site-work-orchestrator`, `tool-recreation-handler`, same date). Default-carry would turn every future producer key into a silent size-and-shape change on a persisted artefact, and would remove the moment where a new key's carriage is decided.
- **sources:** `platform/orchestration/actions/section_metadata_keys.go`; `section_metadata_parity_test.go`; `save_sections_layer2_provenance_test.go`; `v3_site_actions.go` (`extractSectionFromMap`); `rerender_page_sections_action.go`; `save_page_sections_action.go` (Layer 2 preload + both carry arms); lane docs in `docs024_key_docs_latest/bugfix_357_component_identity/`
- **relations:** **CLC-026** (the stamp this makes actually arrive, and whose entry is corrected above); `bugs_open/357` (the mint continues — this is not its fix); `bugs_open/189` (the first key this hop ate, and the per-key remedy that is why it ate the second); `architecture_review/RFC_046`; LANDMINES *"a hop that REBUILDS a map from a hand-written key list drops the key you just added, silently"*
- **verify-later — at the artefact, disconfirming result named:** after the next roll, `sections_metadata` elements on **both** the compile and rerender paths must carry `rendered_template_sha`, and rows created after the roll must carry a non-NULL `component_version_id` while the pre-roll cohort stays exactly **0** (that asymmetry is the control; the old rows moving would itself be the finding). Then the stamp must be TRUE: the stamped version's `html_template` must re-render the stored bytes byte-identically. **And the F2 guard must hold** — `bugs_open/357`'s population is rewritten roughly daily, so within a day its re-minted rows must still read `component_version_id IS NULL`; a population row appearing WITH a hero stamp means the splice hygiene failed and is the signal to stop.

### CLC-029 — A section-level library component cannot be BORN invisible to the selector: birth-time refusal of a NULL `section_type`

- **status:** **LIVE + PROVEN AT THE ARTEFACT 2026-08-24.** Migration `581_refuse_selector_invisible_section_birth.sql` (+ `_ROLLBACK`) applied by hand and recorded in `schema_migrations` the same minute (`--record-only`, with the by-hand provenance in its `--note`; `--apply` was avoided because it takes **every** pending file and two other sessions' `_HOLD` halves were sitting there). Council `f0cd2420` APPROVED round 1, 3 advisory objections — **two acted on, and both changed the SQL**. **Proven behaviourally on the live table, not merely present:** the birth refusal induced (ERROR), the clear refusal induced on a REAL row (`faq`, ERROR), a repair UPDATE to a standing NULL row succeeded (`824e3309`, `UPDATE 1`), and a labelled birth succeeded — all inside transactions that were rolled back, so nothing persisted. **A migration is live the moment it applies and has no image tag to roll back**, so it is held until the verdict is read. Registered now because the 2026-07-28 ruling requires the seam registered in the same commit that ships it, not later.
- **what:** a `BEFORE INSERT OR UPDATE` trigger on `content_components` (`refuse_selector_invisible_section()`, trigger `trg_cc_refuse_null_section_type`) that RAISEs — `ERRCODE 23514`, the same class the table's existing kebab CHECKs use — for a `component_level='section' AND forked_from IS NULL` row that would end up with `section_type IS NULL`. Both messages name the consequence and the remedy rather than just refusing; the UPDATE one points at `is_active = false` as the right way to retire a component.
- **⚠ THE CARVE-OUT IS THE DESIGN, and it is why this is a trigger and not a CHECK: an UPDATE of a row whose `section_type` was ALREADY NULL is ALLOWED.** That is a template repair to one of the standing rows, and every repair writer (`fix_hardcoded_colours`, `fix_forced_text_colours`, the rerender writers) leaves `section_type` untouched. Refusing it would freeze exactly the rows most in need of maintenance. Mutation M5 removes the carve-out and the migration's own control fails by refusing a repair to a real standing row (`loans-damage-checker`) — so the carve-out is proven load-bearing, not asserted.
- **the UPDATE arm exists because of a council objection, and the exposure it closes was theoretical.** `bug_historian` (medium) noted `BEFORE INSERT` alone left a mutation path: nothing stopped a later UPDATE clearing `section_type` and reproducing the identical invisibility through another door. It said it could not tell from the schema whether that was live. **Answered: theoretical as of 2026-08-24** — no Go writer and no migration nulls the column; the only UPDATE of it anywhere is `store_generated_component_action.go:1365`, `SET section_type = COALESCE(section_type, $1)`, which can only heal. Closed anyway, because it is one branch and the door is the point.
- **⚠ idempotent by construction, and the first version was the opposite.** Postgres has no `CREATE TRIGGER IF NOT EXISTS`, so a replay died on "trigger already exists" — and the pre-check made it *worse*, asserting ZERO triggers so a re-run failed at the migration's own guard. Now the guard excludes this trigger by name and a `DROP … IF EXISTS` (covering the pre-revision names too) precedes the `CREATE`. Proven by running the file **twice in one session**: two `VERIFY: PASS` lines.
- **⚠ THE BAR THIS MEETS: the bad state is INVISIBLE, not broken.** The component selector queries `WHERE section_type = $1` (`component_selector.go:184`, `:238`; and the partial indexes `idx_cc_selector` / `idx_cc_section_type` are both `WHERE section_type IS NOT NULL`). A NULL row is therefore unreachable through it — with **no error, no work item, and a perfectly healthy-looking row in every listing**. Nothing tells the author, and nothing tells the next reader.
- **what it cost, measured:** **TEN** diverted twins as of **2026-08-23** — site-suffixed near-duplicates of components the library already owned, born 08-18 ×1, 08-19 ×1, 08-20 ×5, 08-21 ×1, 08-22 ×2 — each a paid LLM generation. More directly: of the **30** `needs_new_component` work items ever filed (2026-08-05 → 2026-08-23), **27** named a `section_type` that exactly matched a live component's `function`, and **25** of those matched an incumbent carrying `section_type IS NULL`. The circularity control (require `c.created_at < w.created_at`) returns the identical 27/25, so the matches genuinely predated their items.
- **why a TRIGGER and not Go — the producer is unreachable from Go.** Every NULL-`section_type` section-level unforked row **ever** born is `created_from='manual'` (28 of 28, all-history as of 2026-08-23); the `generated` route has produced **zero**. The producer is hand-run SQL. There are exactly **three** Go INSERT sites into this table as of 2026-08-24 and **all three are provably outside the predicate**: `store_generated_component_action.go:634` (literal `'section'`, always supplies `section_type` as `$5`), `create_tool_component_action.go:369` (literal `'tool'`), `deploy_tool_action.go:326` (sets `forked_from`). So in practice this trigger fires only on hand-written SQL, which is exactly its target.
- **⚠ landmine — `forked_from IS NULL` IS LOAD-BEARING AND LOOKS LIKE DECORATION.** `deploy_tool_action.go:326`'s fork INSERT copies `component_level` from its source but **does not list `section_type`** among its 16 columns, so a section-level fork is *legitimately* born NULL. Widen this predicate and you break tool deployment at runtime, silently in review and loudly in production. Mutation-proven: removing that clause makes the migration's own fork control fail with the trigger's refusal text.
- **⚠ landmine — this is INSERT-only, and a CHECK constraint is the wrong shape here even `NOT VALID`.** A CHECK is enforced on UPDATE of pre-existing rows, so every template-repair write to the **25** standing NULL rows (as of 2026-08-23) would begin failing. A generated column is wrong outright: **35** active rows deliberately carry a `section_type` that differs from `function` (split: 89 equal / 35 differ / 25 NULL of 149). And a `COALESCE(section_type, function)` default would be worse than nothing, because `function` DEFAULTs to `'generic-text-block'` — unlabelled rows would pour into the commonest selector pool, where a wrong match is least likely to be noticed.
- **it deliberately repairs NOTHING.** The standing 25 are not backfilled, by a recorded decision (`bugs_open/351`, 2026-08-23: *"incumbents stay Path-1-only"*). A backfill adds no reachability any live caller uses — Path 1 matches `function` raw and kebab-normalised and runs first (`plan_sections_action.go:1258`), so the only key it would add to Path 2 is one Path 1 already pre-empts — and it causes two measured harms: `load_existing_component_action.go:163`'s **primary** query is keyed on `section_type` and its NULL miss deliberately routes to the store's identity resolver (`:182`), which withholds a contract on a divert-to-create; and `component_selector.go:184` ends `ORDER BY score DESC` with **no secondary key**, so a backfilled incumbent can tie exactly with its twin and resolve nondeterministically.
- **status-evidence — the verify block INDUCES the violation and is mutation-proven.** A verify of bare `SELECT`s cannot stop a `COMMIT`, so `581`'s is `DO`/`RAISE`: it attempts the refused INSERT and fails the migration if it is *accepted*, then runs four controls that must all succeed (a labelled section, a fork born NULL, a tool, and an UPDATE of a standing NULL row — the last being the NOT-VALID-CHECK trap, induced). Every probe row is written inside a subtransaction deliberately aborted, so nothing survives. Two mutations run against the live DB with `COMMIT`→`ROLLBACK`: making the predicate inert is caught by the induce probe; dropping the `forked_from` scoping is caught by the fork control. The unmutated file passes and leaves zero trigger and zero probe rows behind.
- **sources:** `docs/agent_docs/sql_for_agents/581_refuse_selector_invisible_section_birth.sql` (+ `_ROLLBACK`); `platform/orchestration/actions/component_selector.go` (the query this protects); `plan_sections_action.go` (Paths 0/1/2/3); `load_existing_component_action.go:163,182`; `deploy_tool_action.go:326` (the fork exemption); `store_generated_component_action.go:634`; `bugs_open/351` and `docs024_key_docs_latest/bugfix_351_section_template_predicate/`
- **relations:** `bugs_open/351` (the bug; its predicate half is fixed, live and demand-proven — council `7b662d65` APPROVED 2026-08-23); `bugs_open/345` (the retry loop this class fed); `bugs_open/337` (why `load_existing_component`'s NULL miss is correct and must not be "fixed"); `bugs_closed/311` (the divert-on-collision fix that produced the twins); RFC_006 / **SingleOwner** (the daily-check shape this declines in favour of refusal, because the producer is reachable by neither); `chk_section_type_kebab_case` (the sibling that refuses `''`, so NULL + `''` together leave only a valid kebab value admissible)
- **verify-later — at the artefact, with the disconfirming result named:** after apply, `SELECT count(*) FROM pg_trigger …` is 1 **and** an induced bad INSERT is refused *on the live table* (presence is not behaviour). The standing NULL set must remain **exactly** the same 25 ids, not merely count 25 — a stable count already hid a substitution once in this lane (`29e63065` left the rescued set while another row joined it). Then the disconfirming watch that matters: **no legitimate INSERT should ever be refused.** If `deploy_tool` or `store_generated_component` starts failing with `23514` naming `section_type`, the predicate is too wide and this must be rolled back, not patched around.

### CLC-030 — `illustrated-text-block`: the first section component that carries its OWN image, so imagery survives a content rewrite

- **status:** BUILT and LIVE on one site (apis.uk/index, all 6 content sections, 2026-08-24). Library row only — **no Go change**, so nothing else in the estate is affected until a page selects it.
- **what:** a `content_components` row, `function='illustrated-text-block'`, the illustrated sibling of `generic-text-block`. Same outer shape (`.section` → `.container` → `.section__title` → `.section__content`) so an existing stylesheet applies unchanged, plus a `figure` between heading and prose. **Five schema fields: `heading`, `content` (both `source:llm`), `image_url`, `image_alt` (both `source:site_assets.image`) and an optional `image_caption`.** Figure AND caption are separately gated (`{{if .image_url}}` / `{{if .image_caption}}`), so with no image it degrades to a plain heading-and-prose block with no empty figure and no blank space — the `bugs_open/111` gate-the-container-on-its-contents rule. Ships its own scoped `<style>` (figure margin, responsive image, quiet caption) using `var(--color-text-muted, …)`, and satisfies the four component-creator contracts: `data-component="illustrated-text-block"`, an `illustrated-text-block-section` class, a `--color-` var, and an `@media` rule.
- **why it exists, and this is the point:** `generic-text-block`'s template has **no image slot at all** (211 chars: section/container/h2/content). The only way to get an illustration into one is to embed `<figure><img></figure>` inside `content_data.content` as raw HTML — which is what this lane did on 2026-08-23, and **a content rewrite deleted all six images four minutes later**, because a writer that rewrites `content` necessarily discards markup a human put there. **An image expressed as a component FIELD is data the writer does not own; an image expressed as body HTML is prose the writer will overwrite.** That is the whole difference and it is why hand-embedding is not a workaround, it is a countdown.
- **how to use it:** set `pages.sections` entries to `illustrated-text-block`, point `page_components.component_id` at this row, and put `image_url` / `image_alt` in `content_data`. ⚠ **The renderer reads stored `rendered_html`, not the template** (`rerender_single_page_action.go:867`, and it drops sections whose HTML has no visible text) — so changing `component_id` alone renders nothing new. Write the rendered output too, exactly as the build step does.
- **⚠ what it does NOT fix:** subject allocation. `pages.sections` is parsed as `[]string` (`PlannedSections`), so the plan still has **nowhere to carry a per-section subject**, and a content build still gives every slot the same brief — measured four times on this page, most recently a single `content_rewrite` for the waggle dance that rewrote **all six sections about the waggle dance**. This component makes imagery survive; it does not make the six sections differ. That needs either an object-shaped section plan (a Go change to how `sections` is parsed) or the component-hierarchy work in `features_open/035`.
- **relations:** sibling of `generic-text-block`; the hierarchy plan `features_open/035_FEATURE_component_hierarchy.md` is the fuller answer. ~~**planned, not built** … `page_components.parent_instance_id` is read by **zero** Go files and set on **0 of 2,005** rows as of 2026-08-24~~ **CORRECTED 2026-09-03 by the `editorial_design_uplift` lane — half of that sentence went stale by ADDITION, which is what a bare count always does.** The reachability half is now FALSE: the column is named by SQL in **two** non-test files `[MEASURED 2026-09-03]` (`component_hierarchy_walk.go`'s two membership queries, and `rerender_page_sections_action.go:923`, which selects it and discards it into the flat list — hazard 6.9), and the walk's membership helpers are **reached in production** from `ApplySectionEditAction`, which refuses to render a composition parent alone (035 P1 direction 1) and recomposes a child's ancestors after an edit (direction 2, wired 2026-09-03). The population half still holds and is re-measured: **0 of 3,229** rows carry a `parent_instance_id` `[MEASURED 2026-09-03]` — the table grew ~1,000 rows in three days while the parented count stayed at zero, which is the point. **So the standing advice is UNCHANGED and the reason for it has changed:** prefer this row for "a section with a picture", and do not treat 035 as available — not because nothing is built, but because the READ path is not wired (no production caller of `walkComponentHierarchy`), so a row that opted in would still render flat.
- **sources:** `content_components` where `function='illustrated-text-block'`; `docs024_key_docs_latest/apis_uk_bees_homepage/NOTES_apis_uk_bees_homepage.md` (2026-08-24)
- **verify-later:** whether a framework content rewrite over a page using this component preserves `image_url`/`image_alt` (it should — they are separate keys the writer has no reason to touch — but that is reasoned, **not observed**, and the observation is the whole justification for the component)

### CLC-031 — A single-section render derives its REAL instance occurrence (`DeriveAndBindInstanceToken` + the loop-expansion contract)

- **status:** BUILT and COMMITTED `364e80b7f` (council `3fd0d026-8966-44c6-b0d8-bd8c0dfba187`, verdict pending at entry). **Go — INERT until the next chassis roll.** No migration and no workflow-config key, deliberately: it is live at the roll with no activation step.
- **what:** the two render paths that see ONE section at a time stop supplying a constant occurrence 0 to `InstanceToken(function, occurrence)`. `DeriveAndBindInstanceToken` (`platform/orchestration/actions/component_instance_occurrence.go`) derives it two ways and falls back to 0 everywhere else. **Callable by any future single-section render path** — that is the bar for this register: a new builder or repair agent that renders one section should call this, not re-derive.
  - **Build path** — `PlacementFromLoopStep(config, collected)`. It is always a loop iteration, so the occurrence is counted from the sections **already rendered in this pass**: `InstanceCounter`'s own arithmetic over the same list, one section at a time. Correct on a page's **first build**, when no `page_components` rows exist to count.
  - **Editor path** — `PlacementFromStoredRow(pcData)` → `storedPredecessorCount`, position-exact with a `(position, id)` tie arm, matched by tightening `loadStoredSections`' `ORDER BY` (that query IS the canonical walk).
- **the seam it consumes, and why that is legitimate:** loop expansion **publishes** `loop_item_index` / `loop_name` on each substep's own config and parks items at `datahelpers.LoopItemKey(loop, i)`. Those config keys are declared in `datahelpers.frameworkStepConfigKeys` ("injected by loop expansion", never unknown to the config-key audit) and already read by `LoopCompleteAction`, `call_agent` and `create_work_item_action`. **`LoopItemKey` is new here** and single-sources a spelling that existed as **three** separate literals — a typo in any of them produced a MISS, not an error, and every reader treats "key absent" as legitimate.
- **⚠ it is an INPUT-IMPROVER, never a gate.** No branch returns an error; a lookup failure Warns and binds occurrence 0. Against the constant-0 status quo it is **monotonically non-worse** within the set of sections a path renders: under constant 0 every same-function pair already collides, so a derived assignment can only MERGE pairs already merged. The one exception is the editor's errs-high carried-section case, documented on `storedPredecessorCount`.
- **⚠ landmine — the fallback is indistinguishable from success.** A `render_component` step outside the page-section loop binds 0 to every instance with no error and no warning, and a corpus count of *prefixed* ids reads green while it happens. **Exactly ONE active agent definition runs the action as of 2026-08-24**, and that count goes stale by ADDITION. Full entry in `LANDMINES.md`, footprint `component_instance_occurrence.go` / `render_component` / `process_sections_loop`.
- **what makes the coupling claim enforceable rather than asserted:** `platform/orchestration/loop_item_contract_parity_test.go` drives the **real** `handleLoopExpansion` and asserts the **real** reader agrees, ending on the derived token equalling `InstanceTokensForPage`'s for the same section. Six mutations were RUN, not asserted; five killed named tests, and **one did not** — changing `LoopItemKey`'s format leaves everything green, because single-sourcing makes writer and reader move together (a guard in SERIES with the test, not a hole). That is recorded in the test's own docstring, because a first draft claimed it as a killing mutation and running it proved the claim false.
- **~~two single-section binders exist after this change~~ — ONE. `BindSingleSectionInstanceToken` is RETIRED (`9ba3293e7`, 2026-08-24).** It survived `364e80b7f` by a few hours only because `component_instance_scope.go` was dirty in the concurrent Half B lane, and moving a function out from under a file another session is editing is how this lane earned a guardian veto on `e8c7414c`. That lane reported clean the same afternoon and the retirement was taken rather than tracked (the architecture seat on `3fd0d026` had asked for it as a tracked item, citing that RFC_032's `ComponentID` binding rotted through two deferrals — which is this lane's own history, since `364e80b7f` deletes that very binding). The one remaining caller, `component_instance_scope_test.go:126`, is retargeted onto `DeriveAndBindInstanceToken` with an EMPTY `SectionPlacement` — exactly the no-context fallback — so it still asserts the property it always asserted. ⚠ **The load-bearing edit was NOT the regex**: `pattern-check.py`'s finding TEXT still told authors to call the deleted function, printed by the very check that fires on the author who most needs working advice. Grep a retired symbol across `.go` AND `.py`, and across this register — CLC-014's own body was still naming it as a current seam a day later.
- **sources:** `platform/orchestration/actions/component_instance_occurrence.go` (+ `_test.go`); `platform/orchestration/datahelpers/loop_keys.go`; `platform/orchestration/loop_item_contract_parity_test.go`; call sites `v3_site_actions.go:2404`, `section_editor_actions.go:1104,1275`; `bugs_open/383`; `architecture_review/RFC_032` §9c/§10; `docs024_key_docs_latest/bugfix_283_component_instance_scope/`
- **relations:** **CLC-014** (the seam whose canonical rule this feeds; its stated residual is what this closes); CLC-021/CLC-022 (the conversion detectors that report a collision when this is wrong); `bugs_closed/283` (the literal-id defect that exposed this one — resolve by SLUG); the RFC_032 §8 retirement, whose **third and last** `ComponentID` binding this commit deletes (`v3_site_actions.go:2385`; census with a demand control: 0 active AND 0 inactive templates spell `{{.ComponentID}}`, against 140 spelling `{{.InstanceID}}`)
- **verify-later — at the artefact, and the check that discriminates:** after the roll, repair a page then **await or trigger one `content_rewrite` on it** and re-count distinct `c-` tokens at the SERVED page. A plain rerender proves nothing here: the canonical walk already repairs correctly on today's code (9 of 12 pages, 2026-08-23) — what this change adds is that the repair **HOLDS**. Count DISTINCT tokens per page, never prefixed ones.

### CLC-032 — The advised storage identity pin: the birth gate's contract row carried BY ID from advisory to store

- **status:** BUILT and COMMITTED `30d223291` (council `5252bee6-0e49-4e41-81fc-6acb014a4802`, round 1 REVISE on a premise this lane refuted with a one-command check, round 2 pending at entry). **Go — INERT until BOTH the next chassis roll AND migration 611**, which wires the optional key and re-gates the prompt pin. Registered one commit late: the 2026-07-28 ruling's condition 2 says same-commit, and `30d223291` shipped without this entry. Recorded rather than backdated.
- **what:** `store_generated_component` stops re-deriving which `content_components` row a regeneration overwrites, and honours the row the pre-generation advisory already resolved. `load_existing_component` routes its `section_type` winner through `resolveStorageIdentityByID` and emits `component_id` on **every** return path; the store reads it from the optional config key `advised_identity?` and, when present, resolves by primary key. Absent, every path is byte-identical to today — which is what makes the roll order free in both directions.
- **⚠ THE PIN IS BY ID BECAUSE `function` DOES NOT NAME A ROW, and that is a measurement, not a preference.** `lookupBaseComponent` filters `function = $1 AND forked_from IS NULL` with **no `component_level` filter and no `is_active` filter** (the latter removed 2026-05-06 deliberately — the paradigm regeneration target is a *deactivated* row), then takes `LIMIT 1` off `is_active DESC, updated_at DESC`. `[MEASURED 2026-08-25]` of **330** non-forked rows, **25** `function` values carry more than one row; `site-footer` and `site-header` carry **five each and span `component_level` 'section' AND 'site'**. So the resolver picks among siblings by recency and its answer moves when anything else touches one. Same conclusion, reached independently one seam along: **RFC_034 §1** — *"Convert by `content_components.id`, never by `function`"*.
- **what was actually wrong, since "two resolvers" undersells it:** the advisory and the store have keyed on different columns since CLC-004. Since `e1951c24b` (2026-08-22) they have been **bridged by a sentence in the prompt** asking the model to echo the advised function back — which nothing validated, nothing recorded, and which renders only inside `{{if .existing_component.field_names}}`, so an identity that resolved with an empty schema was never pinned at all. **A guard conditional on the thing it protects.**
- **the second defect, cured in the same change:** the advisory's PRIMARY path never ran the dependent census, so on a foreign collision it advised the incumbent's contract while the store diverted (CLC-020). This file's own header names those two cases as the ones a bare lookup gets wrong — and cured them only on the FALLBACK path. Both paths now run the same decision.
- **how it composes with CLC-020 rather than competing:** `resolveStorageIdentity` was **split**, not copied — `lookupBaseComponent` + `decideStorageIdentity`, the latter holding bugs_open/311's diversion rule verbatim. `resolveStorageIdentityByID` enters the *same* decide half, so a pinned row that other sites depend on **still diverts**. A second copy of that decision would have been this bug's own shape with better manners.
- **⚠ `decideStorageIdentity` CONTAINS NO WRITE, and a reviewer read it as one.** The council's `editquality` seat gated round 1 on the belief that its "scoped mint" is an INSERT, so calling it at advisory time would create rows. The mint mints a **name string**; the function body has no INSERT/UPDATE/DELETE/Exec of any kind, and the INSERT visible in the store test's flow belongs to `StoreGeneratedComponentAction` downstream. `TestLoadExistingComponent_ForeignDependedPrimaryRowAdvisesWhatTheStoreWillDo` drives that branch at advisory time under a mock that fails on any unexpected statement and declares **no write expectation**. If the advisory ever gains a write there, that test goes red.
- **the residual, stated rather than absorbed:** the name-keyed `resolveStorageIdentity` remains live for the un-pinned paths, and **cannot be removed** — a genuine creation has no row to pin and an unknown requester has no advisory. What changed is that it is no longer silent: `COMPONENT_PARALLEL_SECTION_BIRTH` fires exactly on it. `bug_historian` is right that this is reduced in frequency and made loud, not eliminated.
- **detection (three codes, `finding_code_registry.json`, disposition `instrumented`):** `COMPONENT_FUNCTION_PIN_DIVERGENCE` — harmless once the pin decides the row, and therefore the honest meter: pin obedience was previously an **11**-observation sample with zero failures, which bounds the disobedience rate no better than **~24%** at 95%. `COMPONENT_ADVISED_ROW_VANISHED` — the advise→store race. `COMPONENT_PARALLEL_SECTION_BIRTH` — the silent duplicate the field-contract guard cannot see (`isRegeneration` false ⇒ CLC-003 is vacuous). The three declarations physically landed in `eb7d92371` as a same-file passenger of the `bugs_open/358` lane's commit, attributed there and here.
- **CLC-006's OWN CONSTRAINT IS HONOURED, NOT OVERRIDDEN.** Its stated reason for never building this was *"multiple components per section_type can be legitimate"*. This changes **which** row a regeneration lands on, never **how many** a section_type may have: nothing is refused, renamed or reconciled.
- **sources:** `platform/orchestration/actions/component_storage_identity.go` (`decideStorageIdentity`, `lookupComponentByID`, `resolveStorageIdentityByID`); `load_existing_component_action.go` (`advisoryContract`, `advisoryRequester`); `store_generated_component_action.go` (`advisedIdentityPin`, the parallel-birth census); tests in `component_storage_identity_test.go` and `load_existing_component_action_test.go`, each naming the mutation that must turn it red and each mutation RUN; `bugs_open/388`; lane docs `docs024_key_docs_latest/bugfix_388_component_contract_identity/`
- **relations:** **CLC-006** (this is its unbuilt store-side half — its *"advisory-only and unbuilt"* line is SUPERSEDED, and both its verify-later items are now answered, both with the bad answer); CLC-004 (the prompt function pin, whose 2026-08-22 correction already recorded that it "goes silent" whenever the advisory does — this makes it not matter); CLC-020 (`resolveStorageIdentity`, split and reused verbatim rather than reimplemented); CLC-003 (the field-contract guard, which is the backstop and is vacuous on the creation branch); CLC-027 (the 337 advisory this extends from the fallback path to the primary one); RFC_034 §1 (the id-not-function precedent); `bugs_open/351` (NULL `section_type` rows — invisible to the advisory, visible to the store, which is one route into the 27)
- **verify-later — and it needs DEMAND, not a clean sweep:** after the roll AND 611, regenerate ONE component whose `section_type` is among the 27 divergent ones (`footer` → advised row `site-footer` is the cheapest). Confirm the UPDATE landed on the advised id, that no new row was born, and that `COMPONENT_FUNCTION_PIN_DIVERGENCE` recorded the writer's counterfactual choice. A post-fix zero with no demand measures nothing.
