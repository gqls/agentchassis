# Register — workflow-authoring

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074). It said **1** and the file held **11**._ consolidated from 1 raw extraction across unit U21. No duplicates
found within this category's raw material (this new category surfaced a single
raw block from the whole cluster). Note: material about workflow-authoring
mechanics that is more current/live (loop mechanisms, error_step placement,
input_mapping/ActionInputSpec, spawn/step naming conventions) was tagged
`development-guide` by the extracting units and is registered there
(development-guide.md) rather than here — read alongside this entry, since it
covers the same broad subject (how workflows are authored) from the currently
deployed angle, whereas this entry is a single abandoned historical proposal.

### WFA-001 — Workflow Builder & Validator (YAML DSL)
- **status:** abandoned
- **status-evidence:** docs006/001 full design with a roadmap claiming "[x] Phase 1: Core parser & validator, [x] Phase 2: Path resolution, [x] Phase 3: JSON generation"; no later doc references the tool, and workflows continued to be hand-written SQL/JSON thereafter.
- **what:** A validation-first system for authoring orchestration workflows in human-readable YAML instead of raw JSON: parses a DSL, validates agent types exist in agent_definitions, detects circular dependencies and invalid input references, auto-computes CollectedData paths (agent call vs local action nesting), generates the orchestration_workflow JSON, test cases, and docs, then inserts into the DB. Planned CLI (`workflow-builder build/validate/test/list/show/docs`), an HTTP API, a web UI, and a git-based CI/CD workflow-deployment pipeline — none of which are evidenced as ever having shipped or been used beyond the initial design phases.
- **sources:** docs006_workflow_builder/001_workflow_builder.md#Architecture; docs006_workflow_builder/001_workflow_builder.md#Path-Resolution; docs006_workflow_builder/001_workflow_builder.md#Roadmap
- **relations:** data-path resolution problem (agent vs local action nesting) — development-guide register; a later, differently-scoped workflow validator tool (docs017/002_standardising); superseded in spirit by the input_mapping/ActionInputSpec conventions that development-guide documents as the live mechanism
- **verify-later:** platform/workflowbuilder/ directory existence in repo history; any workflow YAML files ever produced by it

### WFA-002 — `$ctx.` execution-context parameter namespace for `query_database`
- **status:** deployed
- **status-evidence:** live on chassis v1.0.1191 (digest `sha256:2f96b795a5c4…`), pod-verified `strings /app/agent-chassis | grep -c "unknown execution-context field"` → 1; first consumer live via migration `258_diagnose_loop_stamps_run_correlation.sql`, verified end-to-end on a real diagnosis 2026-07-28 17:05 (`spec.dispatch_correlation_id = 66a65287-…`).
- **what:** A reserved parameter namespace letting any workflow's `query_database` step bind **the identity of the run executing it** — `$ctx.correlation_id`, `$ctx.orchestration_id`, `$ctx.parent_orchestration_id`, `$ctx.orchestration_name`, `$ctx.client_id`, `$ctx.request_id`, `$ctx.step_name`, `$ctx.group_id` — as ordinary `$1`-style SQL parameters. Written as `'params', jsonb_build_array('$ctx.correlation_id')` beside the query. Before it, `query_database` could only bind values found in `collected_data`, and a run's own correlation is not there: it lives in the execution context. So a dispatch loop could claim a queued row but could not record **which run took it**, and every lane that wanted that had to grow a bespoke Go action. Additive by construction — only paths beginning `$ctx.` take the branch, and no `collected_data` key can start with `$`. An unknown field or an empty value is an **error**, never a silent empty string (a silently-empty bind fills a stamp column with `''` and looks populated in every subsequent query); there is deliberately no "optional" mode.
- **why it is registerable:** "which run picked this row up" is a question every queue-driven workflow on this platform has, not a diagnose-specific one. Any dispatch loop, claim step or audit trail can use it without new Go.
- **sources:** platform/orchestration/actions/execution_context_params.go; platform/orchestration/actions/database_actions.go#QueryDatabaseAction; docs024/bugfix_124_double_dispatch/PLAN_2026-07-28_double_dispatch.md §4 P3; docs/agent_docs/sql_for_agents/258_diagnose_loop_stamps_run_correlation.sql
- **relations:** SCH-012 diagnose-dispatch-loop (first consumer); work-item claim/dedup contract; diagnosis_artifacts correlation keying (`diagnose_assemble_bundle_action.go` writes on `params.ExecutionContext.CorrelationID`, which is what made the item↔run join impossible)
- **landmine:** **ordering is load-bearing and permanent.** A config binding `$ctx.…` run against a chassis that predates this action FAILS that step (`query param path '$ctx.correlation_id' resolved to nil`) — for a claim step that stops the whole lane. Ship the image, pod-grep, then apply the config; and a rollback below the image must revert the config too.
- **review status:** council **REJECTED** it (guardian veto on SCOPE — a platform seam inside a bug patch, corr `90361922`), then **OWNER RULING 2026-07-28: Option A — keep the code, fix the precedent.** The seam stays; the rule it produced is in `CLAUDE.md` §"Platform seams and the ordering exemption". Full review + costed options: `docs024/bugfix_124_double_dispatch/REVIEW_2026-07-28_ctx_namespace.md`. **Whether it should acquire a SECOND consumer is deliberately not ruled on** — nothing depends on it today, and a reviewer asked for it to be judged on its own merits.
- **measured, not asserted (the council forced these):** 63 `params` entries across every live workflow, exactly **1** `$`-prefixed (this one) ⇒ nothing can be shadowed. Of 34 files reading `ExecutionContext.CorrelationID`, only **2** bind it into SQL and 16 use it as a Kafka partition key — so this does **not** duplicate them and there is nothing to migrate.
- **verify-later:** `strings /app/agent-chassis | grep -c "unknown execution-context field"` on the running pod; `SELECT default_config->'workflow'->'steps'->'claim_item'->'config'->'params' FROM agent_definitions WHERE type='diagnose-dispatch-loop'`

### WFA-003 — Sub-workflow validation: nested loop steps are validated, by the same traversal the offline audit uses
- **status:** deployed — live on chassis **v1.0.1203** (`sha256:afd2a4683362…`), both replicas, 2026-07-29.
- **status-evidence:** pod-verified on BOTH replicas of v1.0.1203, 2026-07-29: `"uses fan_out, which cannot work inside a sub-workflow"` → 1, `"Substep declares fields"` → 1 (the round-2 single-sourced decoder), and the DELETE-marker `"Checking disconnected step: "` → **0** while its replacement `"Checking disconnected step for cycles"` → 1. **The colon matters:** the marker first written here was the phrase without it, which the replacement string still contains — that check returned 1 on a correctly-deployed image. Functional evidence, which the binary grep cannot give: **22 orchestration runs whose `workflow_plan` carries a `sub_workflow` since the roll (18:01→18:49Z), 21 COMPLETED, 0 validation errors**, one of them executing a loop-expanded substep (`process_item_iter_2_spawn_handler`) — so the nested traversal ran in production against real definitions and passed them.
- **what:** `ValidateWorkflow` now descends into steps nested inside a loop's `sub_workflow`/`substeps`, which until this change were extracted from config and executed directly by the loop action having been checked by **nothing** — 85 live steps across 18 agents. Three pieces: (1) `subWorkflowsOf` is the single definition of where a nested step lives, mirroring `loop_actions.go`'s precedence exactly (`substeps` wins; `sub_workflow` only if it is absent or empty); (2) `models.DecodeSubWorkflowStep` is the **one** nested-step decoder, called by the executor (`parseSubsteps`) and the validator alike — seven fields, returning the keys the runtime **drops** (`dependencies`, `sub_tasks`, `timeout`, `name`, `store_memory`, `target_agent_type`), which are reported rather than pretend-enforced; (3) `WalkSteps` is exported so anything asking "which definitions carry action X?" or "which (action, key) pairs are live?" asks it of the same step set the validator enforces against. `scripts/audit-config-keys.sh` is the first consumer, via `cmd/config-key-audit --live-pairs`.
- **why it is registerable:** it changes what "validated" GUARANTEES for every pipeline on the platform, and it exports a traversal any audit, census or migration over live workflow definitions should be using. The specific trap it removes is fleet-wide: a query over `default_config->'workflow'->'steps'` silently under-reports, and 25 of the 884 live (action, key) pairs existed only below that level.
- **hard errors vs warnings, and why the split is not arbitrary:** a fault is a hard error only where it is unambiguous **at any depth** — no action, a remote action with no topic, a nested `fan_out` (the decoder drops `sub_tasks`, so it would execute with none), an empty sub-workflow, a `start_step` naming no step of that sub-workflow, a cycle. Everything validation genuinely cannot see is a **warning**: a `next_step`/`error_step` out of a sub-workflow is legitimate — loop expansion passes it through untouched and it may resolve against the enclosing plan or against `<loop>_complete`, which the expander injects and which exists in no definition.
- **sources:** pkg/models/substep_decode.go; platform/validation/subworkflow.go; platform/validation/workflow.go#ValidateWorkflow; cmd/config-key-audit/main.go#emitLivePairs; scripts/audit-config-keys.sh; platform/orchestration/actions/subworkflow_decoder_lockstep_test.go; platform/orchestration/actions/subworkflow_live_dryrun_test.go; docs024_key_docs_latest/bugfix_144_subworkflow_validation/PLAN_2026-07-29_subworkflow_validation.md
- **relations:** SCR-003 declared config-key contract (this makes it reach nested steps); SCR-004 the fleet-wide coverage report (its denominator was measured over a population that excluded every nested-only pair); bugs_closed/101 (unknown config keys); bugs_closed/086 (the same class one level up — a field the plan-builder failed to carry); bugs_open/136 (the objection that found this)
- **landmine:** **the honoured field set is a statement about the EXECUTOR, not about `models.Step`.** Adding a field to `Step` does not add it to `SubWorkflowStepFields`; `pkg/models/substep_decode_test.go` fails until you decide, in code, whether the loop reads it or knowingly drops it. "Fixing" that test by widening the honoured set makes the validator vouch for a field nothing reads — which is the defect this entry exists to close, one level down. (Until the council's reuse seat objected, there were TWO decoders held together by a lockstep test; single-sourcing them is why that test is now a re-inlining guard rather than the guarantee.) **A second landmine for any future change to what "validated" means:** run the dry-run harness first — `SUBWF_LIVE_EXPORT=<export> go test ./platform/orchestration/actions/ -run TestLiveDefinitionsPassSubWorkflowValidation -v`. It validates each live plan twice, whole and with sub-workflows stripped, and only reports the DIFFERENCE, so a pre-existing rejection cannot be misread as damage from your patch.
- **measured before shipping, not argued:** 178 live definitions replayed — **0 newly rejected**; 3 rejected before and after (`html-developer-chunked`, `multipage-wrapper`, `html-assembler`, each naming an action in no registry; `bugs_closed/044` records these as retired definitions still flagged `is_active`). 85 nested steps / 18 agents / 19 sub-workflows. All 20 distinct nested actions are `IsLocal:true` and **none of the 85 carries a topic**, so the topic rule costs nothing today. 0 of the 66 nested `(action, key)` pairs trips the strict-config rule. Live step-config census: 1,173 object, 63 null, 0 other.
- **review status:** council **APPROVED round 1**, corr `9194bc97-8475-4022-b658-2ac64f06dd63` (4 advisory objections, none high-severity). Two were answered with code before committing: the reuse seat's (two decoders pinned by a test, not single-sourced) and the debug-historian's (`is_active` assumed to bound the blast radius — `processor.go:357-360` filters on exactly those three predicates, and re-running the dry-run over ALL 183 non-deleted definitions gives the same 0-newly-rejected answer). The guardian asks a HUMAN to settle the RFC-vs-bug-patch venue question and to confirm no consumer relies on today's silent pass-through; that is open and is recorded as open. **This is a platform seam** under CLAUDE.md's 2026-07-29 ruling — it changes what a shared mechanism guarantees — so the venue question (council gate vs RFC) was put to the reviewers explicitly rather than assumed away. Registered in the same commit that ships it, per condition (2) of the ordering exemption, which is now the whole of the requirement.
- **verify-later:** the pod-grep pair above after the next roll; `./scripts/audit-config-keys.sh` printing a non-zero "inside loop sub-workflows" count (a return to zero means the traversal broke, not that the fleet tidied itself)

### WFA-004 — Offline unregistered-action detector (`config-key-audit --unregistered-actions`)
- **status:** deployed (it is a standalone CLI tool, not a chassis-served mechanism — "deployed" here means committed and runnable; there is no image or pod to roll or verify against).
- **status-evidence:** run against the live DB 2026-07-30: 178 live, `is_active`, non-snapshot agents decoded, 0 undecodable, exactly 3 findings (`html-assembler`, `html-developer-chunked`, `multipage-wrapper`) — matching WFA-003's own independent measurement of the same 3 via the dry-run harness.
- **what:** A new mode on the existing binary, `config-key-audit --unregistered-actions`, that answers offline the question `platform/validation/workflow.go`'s `validateStep` and `subworkflow.go`'s `validateSubWorkflowStep` answer only when a message arrives: is this step's action neither locally registered (`actioncheck.IsLocalAction`) nor topic-routed? It walks the same live-export shape `--live-pairs` reads with the SAME `validation.WalkSteps` traversal WFA-003 registered, top-level and nested, so it cannot go blind in a direction the runtime validator doesn't. Deliberately mirrors the rule exactly, including not exempting `fan_out` — a fixture test (`TestFindUnregisteredActions`) pins that. `scripts/audit-unregistered-actions.sh` is the first consumer, mirroring `audit-config-keys.sh`'s DB-query/report shape.
- **why it is registerable:** any workstream doing `is_active` hygiene, dead-dispatch-edge cleanup, or a pre-roll sanity check on live agent definitions needs exactly this question answered, and it is now a report rather than something to re-derive from a rejected message.
- **sources:** cmd/config-key-audit/main.go#findUnregisteredActions,#emitUnregisteredActions; cmd/config-key-audit/main_test.go; scripts/audit-unregistered-actions.sh; bugs_closed/148_HANDOFF_2026-07-29_three_live_agents_name_actions_that_exist_in_no_registry.md
- **relations:** WFA-003 (shares `WalkSteps`, the traversal this depends on); bugs_closed/044 (first identified `html-assembler` et al. as retired-but-`is_active`; this makes the check re-runnable instead of a table that goes stale); bugs_open/148 §7 scopes fix candidates 2 (retire/repoint the two live dispatch edges into these definitions) and 3 (wider `is_active` hygiene) out as an owner call this tool informs but does not settle.
- **landmine:** a step with a topic is legitimately remote and dispatched by Kafka, not the local registry — "action not in registry" ALONE is not the defect, only the pair (no topic AND not local) is. A check that drops the topic condition produces a long false-positive list against every intentionally-remote step in the fleet.
- **review status:** out of the council gate's scope by the gate's own filter (`SCOPE_RE='^(platform|internal|pkg)/'`; this touches only `cmd/` and `scripts/`) — not submitted, per the gate's own cost model (docs/site/tooling changes are refused client-side rather than spending a credit).
- **verify-later:** `./scripts/audit-unregistered-actions.sh` — a return to a non-empty finding set that isn't these same 3 names a NEW unrunnable definition; a return to 0 means candidates 2/3 above were actioned.

### WFA-006 — `SingleOwner`: an action can declare that a SECOND live carrier is a defect, and an offline check enforces it
- **status:** **deployed AND running on a clock since 2026-08-02** — CLI + spec declaration (`49ecdf4fd`, council `60f4b425` **APPROVED**), plus `single-owner-carriers-check`, a daily CronJob live in `ai-persona-system` (`20 6 * * *` UTC). The declaration is **runtime-inert by design** — nothing in the engine reads it — so the mechanism is entirely the detector, and until the CronJob existed the detector was **inert-by-omission**: it only ran when someone remembered to run it. That gap was the council `architecture` seat's objection and the owner directed it closed.
- **status-evidence:** proven as a matched pair over the SAME live fleet, same command, one variable changed — **before** migration 286: `181 agents decoded, 1 finding`, naming `design-audit-agent.steps.triage`, `improvement-loop.steps.triage_findings`, `site-review-agent.steps.triage`; **after**: `181 agents decoded, 0 findings`. Six fixture tests, and the reachability test is **mutation-proven**: deleting `SingleOwner: true` from the action makes `TestTheDeclarationIsReachableFromTheRegistry` fail, so a silently-inert declaration cannot pass.
- **what:** `datahelpers.ActionInputSpec.SingleOwner` (bool, zero value false) declares that an action's effect is scoped **wider than the run that calls it** — typically "every row on the site in state X" — so a second live agent carrying that step is a defect rather than a duplication. `ListSingleOwnerActions()` exposes the set; `config-key-audit --single-owner-actions` joins it against a live agent export and reports any declared action with more than one carrier; `scripts/audit-single-owner-actions.sh` is the runnable form (exit 1 on findings). Counts **distinct agents, not steps** — a repeat within one workflow is a different class and is deliberately not filed here. Currently one declaring action: `triage_detected_items`.
- **why it is registerable:** it is the general form of a defect class that had cost one live incident and had no detector — and any action whose effect is "take everything in state X" can now opt in with one line. The class is characterised in `016b` §9 and RFC 006 §1: such an action is *idempotent by emptying* (a second run is a success returning zero, not an error) and its result describes **what that call did** while a branch reading it wants **what is now true**. Those coincide only while exactly one caller exists.
- **why the check must be OFFLINE:** an action cannot tell at execution time whether a sibling agent also carries it — nothing inside a run can ever see the fleet. Same argument as WFA-004's, and it reuses that tool's decode and `validation.WalkSteps` traversal rather than adding a second binary (the `reuse_agent` and `editquality` seats objected to exactly that in corr `07cf67c6`, and were right).
- **landmine:** `ListSingleOwnerActions` is deliberately **NOT** gated on `checksConfig()`, unlike `ListDeclaredConfigKeys`. Coupling them would make a `SingleOwner` declaration silently inert for any action that has not adopted the unrelated config-key mechanism — a declaration that reads as enforced and enforces nothing (`WRONG_CALLS.md` 2026-07-28's shape). Pinned by `TestListSingleOwnerActionsDoesNotRequireConfigCheckingOptIn`. Second landmine: the detector **refuses to print** over an empty declaration set as well as an empty agent export, because with nothing declared every possible export yields zero findings and a clean report would be indistinguishable from a build in which registration failed.
- **sources:** `platform/orchestration/datahelpers/action_inputs.go` (`ActionInputSpec.SingleOwner`, `ListSingleOwnerActions`); `cmd/config-key-audit/singleowner.go`; `scripts/audit-single-owner-actions.sh`; `platform/orchestration/actions/triage_detect_items_action.go`; `docs024_key_docs_latest/architecture_review/RFC_006_one_promoter_one_owner_for_shared_promoting_steps.md`; `docs/agent_docs/sql_for_agents/286`, `288`
- **relations:** WFA-004 (shares the binary, the decode and the `WalkSteps` traversal); WFA-003 (registered that traversal); **WDS-015** (the site-scoped signal that fixed the symptom — this closes the class the seats said it left open); `bugs_closed/150` (the instance)
- **the automatic half (2026-08-02):** `deployments/kustomize/services/single-owner-carriers-check`. **A CronJob, not a git hook, and the first reason is decisive:** at commit time a migration has not been applied, so live definitions still look correct and a pre-commit hook would pass the very change that re-creates the fan-out — and config here is routinely changed straight in the DB with no commit at all. Modelled on `bugs-open-staleness-sweep` (same image, secret and `doc_notes` convention); daily rather than weekly because live agent config changes many times a day. It writes ONE `doc_notes` row per run **including on a clean result**, so a missing row means the job did not run — which is not the same as "nothing is wrong" and must not look like it.
- **landmine — the CronJob carries a PYTHON MIRROR of both the declared list and `validation.WalkSteps`**, because the container has no Go toolchain (running the real binary would need a clone of a 262M repo + `go mod download` + a compile with uncertain egress, and a gate that fails for infrastructure reasons gets ignored). That is the `bugs_open/144` shape — two hand-written traversals go blind the same way and then agree. **Do not edit one side alone**: `cmd/config-key-audit/cron_parity_test.go` pins the literal against `ListSingleOwnerActions()` and the traversal against the Go detector on shared fixtures, and both guards were **proven able to fail** by mutation. Parity was also checked on the LIVE fleet **on a non-empty result** — two checks agreeing on `[]` proves nothing.
- **verify-later:** `./scripts/audit-single-owner-actions.sh` on demand, or `kubectl -n ai-persona-system get jobs -l app=single-owner-carriers-check` / the `doc_notes` rows under `subject_key='single-owner-carriers'`. Any non-empty result means a shared promoter has regained a second carrier. A **0 declaring actions** message is not a pass, it is a broken build, and both the tool and the job exit 2 rather than printing one. **A missing daily row is itself a finding.**

### WFA-007 — Relay-envelope gap detector (`config-key-audit --relay-gaps`): a dispatcher must not narrow what its handler declares

- **status:** deployed as a standalone CLI mode + `scripts/audit-relay-gaps.sh` (committed and runnable; no image or pod, so nothing to roll). **NOT yet wired into `main()`'s `os.Args` dispatch** — `cmd/config-key-audit/main.go` was held by a concurrent session (WFA-006) when this landed, and committing it would have taken their then-untracked `singleowner.go` as a same-file passenger and broken the build at HEAD. **The audit script refuses rather than lying about it:** `config-key-audit` falls through to its DEFAULT report for an unrecognised argument (valid JSON, exit 0), so the script asserts the report's own keys and exits 2 if the answer did not come from this mode. Wiring it is a two-line follow-up.
- **status-evidence:** run against the live DB 2026-08-02 — 181 live agents decoded, 0 undecodable, 1 relay asserted, **0 findings, 0 unmatched, 2 uncovered**. Proven BY FIRING, not by passing: the same binary over the pre-fix config rebuilt from migration 289's own `agent_definitions_backup` snapshot exits **1** and names `seed_scope` + `runtime_page` in both `not_projected` and `not_forwarded`; an empty export exits 2.
- **what:** For a **dispatcher** — an agent that claims a work item and relays its `spec` to the handler named on it — it asserts that every key the handler's `input_contract` declares can actually be carried. It checks **two** allow-lists in series, because a dispatcher has two: the claim query's SQL `RETURNING` projection (`not_projected`) and the `call_agent` `input_mapping` (`not_forwarded`), plus the reverse direction (`maps_to_nothing` — a mapping entry sourced from a column the claim never projects, which reads as wired and resolves to nothing). Three output classes, deliberately distinct: **findings** (a defect), **unmatched registry entries** (a relay we assert about that no longer matches live config — *an assertion that stopped running*, exits 1 and is the worse one), and **uncovered relays** (dispatcher-shaped and unregistered — advisory). Shares WFA-003's `validation.WalkSteps` traversal, so it cannot go blind in a direction the runtime validator does not.
- **why it is registerable:** any workstream adding a dispatch loop, or adding an optional field to an intake that a loop relays, now has a runnable answer to "will this key actually reach the handler?" — a question that previously had to be re-derived by reading two hand-maintained lists and a contract in three different places. `bugs_open/174` is what happens when nobody does: the two lists agreed with each other, disagreed with the contract, and silently re-aimed three lanes' diagnoses.
- **why it is a REGISTRY and not a fleet-wide rule — this is the part to read before "improving" it:** the obvious general rule was written and **measured first**, and it is not sound. Live 2026-08-02: *"every `call_agent` must forward every key its callee declares"* → **31 findings from 75 resolvable call sites**, and the spot-checked ones were legitimate (`pageflow-builder.apply_site_design` omits `site_context`; `webdesign-agent` has an `else_step: load_site_context` and loads it itself). Tightened to *"…and the callee actually READS `input_data.<key>`"* → **3**, and still unable to separate "the caller dropped it" from "the caller never had it". **Both versions were blind to `bugs_open/174` itself**, because `call_handler` resolves its callee at runtime via `agent_type_field: claimed.handler_agent` and a static resolver skips it. A dispatcher is the one case where the question is answerable, because the caller's envelope IS the work item spec and the spec's shape IS the handler's contract. Population measured: **3 dispatcher-shaped relays fleet-wide.**
- **sources:** `cmd/config-key-audit/relaygaps.go` (`findRelayGaps`, `declaredRelays`, `discoverUncoveredRelays`, `projectedAliases`, `lookupStep`); `cmd/config-key-audit/relaygaps_test.go`; `scripts/audit-relay-gaps.sh`; `docs/agent_docs/sql_for_agents/289_diagnose_dispatch_loop_forwards_seed_scope.sql`; `bugs_open/174`; `docs024_key_docs_latest/bugfix_174_seed_scope_relay/`
- **relations:** WFA-003 (`WalkSteps`, the traversal this depends on); WFA-004 (same construction — an offline detector mirroring a rule the runtime only applies when a message arrives; this script's DB-query/report shape is copied from `audit-unregistered-actions.sh`); WFA-006 (the sibling that landed the same hour, and the reason this one is not wired yet); LOCK-007 (the "one named predicate + a lockstep test" construction `bugs_open/174` cited as the model); `bugs_closed/143` (dedup-index/Go-list drift, the class this belongs to).
- **landmine:** **`declaredRelays` is an assertion, so an entry that stops matching must be LOUDER than a finding, not quieter.** This is not hypothetical: the first live run matched **nothing** — `validation.WalkSteps` qualifies step paths (`steps.call_handler`) while the registry names steps as an author writes them — and would have printed "all clear" had the `unmatched` category not been added ten minutes earlier. **Second landmine: do NOT register the two uncovered relays (`report-dispatch-loop.call_handler`, `build-pipeline-trigger.call_dispatch`) to make the advisory list empty.** Their callees are resolved at runtime and their handlers' contracts have not been read; registering them unread would assert something nobody has checked, which is precisely the state `174` was already in. **Third: the export query must include `input_contract`** — copying another mode's query gives a report where every relay is "unmatched: callee declares no input_contract", which reads as broken rather than clean, but for the wrong reason.
- **review status:** the Go/config half of `bugs_open/174` went through the council gate as corr `081d98b3-75e1-4926-a17a-b0c72e5ccece` — **APPROVED at round 1**, 6 advisory objections, none high-severity, four answered with code or measurement before committing. This CLI mode itself is out of the gate's scope by its own filter (`SCOPE_RE='^(platform|internal|pkg)/'`; this touches only `cmd/` and `scripts/`), matching WFA-004's precedent.
- **verify-later:** `./scripts/audit-relay-gaps.sh` — a non-zero finding count names a relay that has drifted from its handler's contract; a non-empty **unmatched** list means a check silently stopped running and is the more urgent of the two. A drop in the **uncovered** count without a matching `declaredRelays` addition means the discovery heuristic broke, not that the fleet tidied itself.

### WFA-005 — `output_format` is a LIVE step contract, and a JSON step gets one corrective re-ask
- **status:** **⚠ ROLL CONDITION MET 2026-08-10, verified by build provenance.** Chassis `v1.0.1283` is built from commit `d3c09cc74` — read back from BLD-019's stamp on **both** replicas — and `git merge-base --is-ancestor` puts this entry's own cited commit(s) inside it. **The Go code IS in the running binary; every clause below about waiting for a roll is superseded.** This note makes no claim that the feature is EXERCISED — that is a separate condition on a separate clock. Original: built (inert until the next chassis roll) — committed `4c1a97874`, `085160a6c`, `059ae2acd`. Council `576832f3` **APPROVED at round 2** (14 reviewers, 0 unreadable, 8 advisory objections, none high). Round 1 was REVISE on a gating objection from `debug_historian` — the depth claim above was asserted, not measured; it is now measured and the census corrected upward by one step and one agent.
- **status-evidence:** `go test ./platform/orchestration/actions/` green; the two behaviours verified BY MUTATION rather than by passing — reverting the `output_format` fallback fails 4 of the 6 new tests, removing the vocabulary allow-list fails the gate test specifically, restoring passes both. NOT yet pod-verified; see **verify-later**.
- **what:** Two contracts on `execute_llm_prompt`, both previously absent. **(1) The declared output key now works.** `getOutputType` read only `output_type`; the fleet writes `output_format`. Measured 2026-08-01 across all **135** active `execute_llm_prompt` steps (134 top-level + 1 nested in a loop's `sub_workflow`): `output_type` on **6**, `output_format` on **101** — **91** of them `json`, across **33 agents**, every council seat included. **Depth measured, not assumed** (the council gated round 1 on exactly this): all 101 sit at `config.output_format`, the map `params.StepConfig.Config` is; zero under `config.ai_service`, zero at step root. So ~75% of LLM steps declared an output contract that nothing honoured and silently received `getDefaultOutputInstructions()`, which mentions JSON nowhere, instead of `getJSONOutputInstructions()` and its "Ensure valid JSON syntax (proper quotes, commas, brackets)". The fallback is **vocabulary-gated** to `json|text|html|markdown`. **(2) A JSON-declaring step whose answer will not parse gets exactly ONE corrective re-ask** before the pre-existing text path, **and (3) if it still has no JSON afterwards the result carries `__json_contract_unmet`** — a marker, deliberately not a hard error, so the residual silent success is countable instead of invisible (failing loud would break 91 steps over content they did not author, `bugs_closed/073`). Corrective, not identical, because an identical re-ask reproduces the failure; the instruction branches on *which* failure occurred — truncated → same judgement materially shorter, malformed → same content as one valid JSON value with bracket balance spelled out. `resolveOutputType` exists so the instructions and the retry ask that question through one rule rather than two.
- **why it is registerable:** any workstream authoring an LLM step now has a config key that MEANS something — writing `output_format: json` buys both the JSON instructions and the re-ask, and writing nothing buys neither. That is not derivable from the config: until this commit the key was indistinguishable from a comment. 32 agent types inherit the change without their owners doing anything.
- **sources:** `platform/orchestration/actions/ai_actions.go#getOutputType,#resolveOutputType,#correctiveReaskPrompt,#markJSONContractUnmet,#ExecuteLLMPromptAction`; `platform/orchestration/actions/ai_output_contract_test.go`; `bugs_open/119_HANDOFF_2026-07-27_one_seat_emitting_malformed_json_costs_a_whole_council_round.md`; `docs/agent_docs/docs024_key_docs_latest/bugfix_119_seat_retry/`
- **relations:** `bugs_open/119` (the case); `bugs_closed/019` + `bugs_open/138` (truncation — the re-ask deliberately does NOT touch `salvageTruncatedReview` or the `Degraded` gating rule); `bugs_closed/076` (the `__truncated` marker contract, which the re-ask re-stamps from the response actually returned); `bugs_open/134` (the same class — a config key declared and inert); LCO-005/LCO-006 (`aiservice.IsTruncated`, reused here).
- **landmine:** **`output_format` is NOT this action's key alone — `query_database` reads the same name with a different vocabulary** (`array`/`object`, `database_actions.go:26`). That is why the fallback is allow-listed rather than a pass-through, and why widening the allow-list is not a free change: an `output_format` value that means "array" to one action must never select an instruction set in the other. Second trap: the re-ask fires on `declaredOutputType == "json"` only, so **giving a step `output_format: json` now costs it a possible second LLM call** — correct for a step that needs JSON, wrong for one that declared json loosely and is happy with prose.
- **review status:** **APPROVED**, correlation `576832f3-0d39-48b7-af18-97e06297900f`, round 2 — `Council-Reviewed:` trailer on `059ae2acd`, earned by a verdict that was read. The submission names all 33 affected agent types explicitly rather than only counting them (owner ruling 2026-07-29 point 3). Two advisory objections were acted on or checked: `diagnosis_guardian` (the brevity re-ask protected findings but not the citations under them — fixed), `llm_reliability` (claimed `GenerateText` does not decode `stop_reason` — refuted, all three providers return a `*TruncatedError`). **Six of the eight objections were an artefact**: the seats cited this entry's own landmine as evidence against the change that wrote it — see `016b` §9.
- **verify-later:** pod-grep a string this change ADDs and one it does not, on every replica: `strings /app/agent-chassis | grep -c 'bugs_open/119 re-ask RECOVERED'` → 1 after the roll, and `grep -c 'YOUR PREVIOUS ANSWER WAS DISCARDED'` → 1. Then the behavioural check, which is the one that matters: `SELECT count(*) FROM llm_call_log WHERE error_message LIKE 'RETRY (bugs_open/119%';` — a non-zero count is the mechanism firing, and it should stay SMALL (2 firings per ~785 JSON-declared outputs was the measured pre-fix rate). The headline figure to move: rounds decided by an unreadable seat, **23 of 424 all-time, 15 in the week to 2026-08-01**.

### WFA-008 — A loop substep can declare its OWN `continue_on_error`, so one step's failure need not be the whole loop's policy
- **status:** deployed
- **status-evidence:** **LIVE on chassis `v1.0.1250`, pod-verified on BOTH replicas with a positive AND a negative control** (`resolveSubstepContinueOnError` → 2, the non-bool warn string → 1, pre-existing `continue_on_error is true for this loop iteration step` → 1, and a deliberately-absent marker → 0, so the probe can return zero; all four read `0/0/1/—` before the roll). **Behavioural proof, which no binary grep can give — the two-branch induction 173 demanded, run 2026-08-04:** a substep declaring `true` inside a loop with the flag UNSET skipped both failing iterations and the orchestration reached `complete` (`run_loop_iter_{0,1}_error` present, `skipped=true`, `run_loop_error_count=2`, orch `35acb827-6def-4285-92a7-395e131daa01`); a substep declaring `false` inside a loop with the flag `true` **FAILED** the orchestration at `run_loop_iter_0_boom` (orch `982bf0ce-b948-4da8-a848-184abb106b41`). Each run's loop-level flag was the OPPOSITE of its substep's, so a stamp-regression would have produced the other run's outcome — the pass is discriminating, not incidental. Committed 2026-08-04 on `platform/orchestration/loop_expansion_handler.go`; five test functions / seven subtests in `substep_continue_on_error_test.go`, and the guard is **mutation-proven** rather than merely green — reverting the production line to the unconditional stamp fails exactly the three override tests (tolerant-in-strict, strict-in-tolerant, per-substep-not-per-loop) while the two inheritance tests correctly still pass, which is the signature of a test that can tell the fixed code from the broken code. Fleet effect on the day it rolls: **none, measured** — 0 of 79 substeps across 18 active loop steps declare the key, positive control 18 loops/79 substeps from the same CTE grouped instead of filtered. **NOT yet proven end-to-end in a live orchestration** — that needs a roll; see verify-later.
- **what:** `continue_on_error` was a **loop-level** flag only. `LoopAction` read it once from the loop step's config and `handleLoopExpansion` stamped that one value onto **every** injected iteration step, so a loop had exactly two states: every substep tolerant (any failure silently skips the item) or every substep strict (any failure kills the whole orchestration). There was no way to express "a failure in *this* substep should skip the item, but a failure in *that* one should still fail the build". Now a substep declaring `config.continue_on_error` as a bool gets **its own** value — `true` makes one substep tolerant inside a strict loop, `false` makes one substep **strict inside a tolerant loop** — and a substep declaring nothing inherits the loop's flag exactly as before. The resolution happens once, at expansion, because all three failure paths (`coordinator.go:907` sync step failure, `:3239` async unrecoverable, `:3345` request timeout) consult the single `shouldContinueLoopOnError`, which reads the flag off the injected step. **The unsafe default is OFF by construction:** tolerance swallows a failure that would otherwise be loud, so a substep acquires it only by writing it down where a reviewer of that substep can see it (owner ruling 2026-08-02 §2). A declaration of the **wrong type** (the string `"true"` being the likely mistake) falls back to the loop's value **and warns** — a knob that is declared-and-inert is the exact defect this entry removes, so reproducing it inside the fix was not an option.
- **why it is registerable:** it is a new degree of freedom in the **engine**, available to every loop on the platform, and the alternative is already documented as happening: the council's `architecture` seat predicted that without it "the next nested substep that hits the same disproportionate-failure problem will invent its own ad hoc bool/sentinel". It had already happened once — `bugs_closed/165` made an action return a soft `maySync=false` so one regeneratable substep would not fail a whole site build, and four seats rejected it because the action was made to lie about its own outcome. Any workflow author who needs proportionate failure handling now has a config key instead of a Go workaround, and no reader of a loop has to guess whether a substep's tolerance is the loop's or its own.
- **sources:** `platform/orchestration/loop_expansion_handler.go#resolveSubstepContinueOnError` (and the call at the per-substep injection, ~line 157); `platform/orchestration/substep_continue_on_error_test.go`; `platform/orchestration/loop_error_handler.go#shouldContinueLoopOnError` (the single read side); `platform/orchestration/actions/loop_actions.go:66` (the loop-level read, unchanged); `bugs_open/173_HANDOFF_2026-08-01_loop_error_routing_has_no_substep_granularity.md`; `docs/agent_docs/docs024_key_docs_latest/bugfix_173_substep_error_tolerance/`
- **relations:** WFA-003 (the sub-workflow decoder — its `step.Config = config` verbatim copy is *why* this knob needs no new field, and its landmine is why it must not become one); `bugs_closed/165` (the workaround this replaces, and its four-seat rejection); `bugs_open/086` (`error_step` at step level — the *other* per-substep error mechanism, which routes to a named recovery step rather than tolerating); `bugs_closed/073` (giving a path a new way to fail over content it did not author — the trap that rules out bolting the full validate gate onto a cheap path); `datahelpers.frameworkStepConfigKeys` (which already listed `continue_on_error`, so no audit change was needed).
- **landmine:** **the key was already writable on a substep and did nothing.** Before this change, `handleLoopExpansion` deep-cloned the substep's config (so a declared `continue_on_error` was *present*) and then overwrote it three lines later with the loop's value. `config-key-audit` never objected, because `continue_on_error` is in `frameworkStepConfigKeys` — legal vocabulary on any step. So an author could write the obvious thing, get no error and no warning, and get **no effect**; any config predating this roll that declares it has been silently inert, and its author may have believed otherwise. **Second trap, and it is the one to check in review: `if v, ok := cfg["continue_on_error"].(bool); ok && v` is WRONG here.** Folding the type assertion into the truth test reads a declared `false` as *no declaration*, which silently destroys the strict-substep-in-a-tolerant-loop direction — the direction that stops a fan-out loop swallowing a failure that should be loud. Presence and truth must be tested separately. **Third:** `loop_metadata["continue_on_error"]` is loop-level and must stay so; the per-substep value belongs on the **step**, which is where the read side looks.
- **review status:** submitted to the council gate 2026-08-04, correlation `549e25fb-acc1-4806-a2a7-95bf73cca806`; committed with `Council-Submitted:` (not `Council-Reviewed:`) because the verdict had not been read at commit time. **Scope argument made explicitly in the submission**, since this council vetoed `bugs_closed/124` on exactly this ground: by owner ruling 2026-07-29 §1 the RFC trigger is whether a shared mechanism's **guarantees** change, and they do not — this adds an opt-in capability reachable by nothing until a config names it (0 of 79 substeps today), which is the ruling's own description of the normal-gate case. Consumers named rather than merely counted (§3): `pageflow-builder.build_pages_loop`, `page-rebuild.build_pages_loop` and `site-work-orchestrator.build_items_loop`, all `save_sections`/`save_page_sections` under a loop with `continue_on_error` unset.
- **verify-later:** pod-grep on **every replica**, with both controls — `strings /app/agent-chassis | grep -c 'resolveSubstepContinueOnError'` → 0 before the roll and 1 after (discriminating), and `grep -c 'continue_on_error is true for this loop iteration step'` → ≥1 throughout (positive control, proves the probe reads the binary you think it does). Then the behavioural check, which is the one that matters and which no build can give: give a loop two substeps, make the **tolerant** one fail and confirm the iteration is skipped while the orchestration continues, then make the **strict** one fail in the same loop and confirm the orchestration FAILS. Both branches, or the flag is untested in the direction that matters. The census that should stay honest as adoption starts: `SELECT type, s.key, b.key, b.value->'config'->>'continue_on_error' FROM agent_definitions a, LATERAL jsonb_each(a.default_config->'workflow'->'steps') s, LATERAL jsonb_each(COALESCE(s.value->'config'->'substeps', s.value->'config'->'sub_workflow'->'steps')) b WHERE a.is_active AND COALESCE(a.is_snapshot,false)=false AND a.deleted_at IS NULL AND s.value->>'action'='loop' AND b.value->'config' ? 'continue_on_error';` — **0 rows today**; a row appearing means somebody has taken the knob, and the loop-level value beside it is what they opted out of.

### WFA-009 — `extract_fields` gains an opt-in `required` list, so "no configured path resolved" fails where it happened
- **status:** **⚠ ROLL CONDITION MET 2026-08-10.** Chassis `v1.0.1283` is built from `d3c09cc74` (BLD-019 stamp, both replicas), which was HEAD at build time — so every commit on this branch is in it, including this one's. **Clauses below about waiting for a roll are superseded**; whether the feature is EXERCISED is a separate claim. ⚠ **This entry cites NO commit sha, so inclusion is inferred from its date rather than verified per-commit — name the commit next time; provenance can only settle an entry that names one.** Original: built (Go inert until roll); the **config half is LIVE** — `page-content-writer.select_sections` opted in via seed `308`, applied 2026-08-04 09:01:35Z.
- **status-evidence:** `go test ./platform/orchestration/actions/ -run TestExtractFields -v` → 4/4 pass, and the sibling `load_current_section_content` guard is **mutation-proven**: reverting the action to its wrapper return fails all five of its cases with the intended message, and restores clean. Live behaviour of the seed half proven end-to-end on orchestration `0511e4d1` (created 09:03:47Z, i.e. **after** the seed): `spawn_link_resolver → process_sections_loop_iter_0_generate_content → COMPLETED`, with `collected_data.sections_for_render ? 'sections_ready'` **true**, length 1 — against three FAILED runs immediately before it with the same query returning **false**. **NOT yet proven that `required` FIRES in production** — it is inert on the running binary by design; see verify-later.
- **what:** `ExtractFieldsAction` resolves each target field against an ordered list of fallback paths. When **every** path missed, it omitted the key and returned **success** — so the run continued with a silently absent field and died later, somewhere else, under an error naming the symptom. `"required": ["field", ...]` in a step's config makes that state fail **at the extraction**, with a message naming the field, the paths tried, and the `collected_data` top-level keys that were actually in scope to try them against. Checked **after** `defaults` are applied, so a supplied default legitimately satisfies a requirement. **Default OFF:** an absent `required` key preserves the historical lenient behaviour byte for byte, which is deliberate — it is the standing contract of both live consumers and must not change under anyone (owner ruling 2026-08-02 §2: new authority on a shared seam ships as an opt-in field with the unsafe default off, not as a doc comment policed by review).
- **why it is registerable:** every workflow on this platform that maps data between agents does it through an ordered fallback path list, and until now the whole class failed the same way — invisibly, and attributed to the wrong step. This is the knob that makes a data-contract break loud, and any step that genuinely cannot proceed without a field should take it. `bugs_open/192` is the worked case: it cost the fleet **every page build** for ~40 minutes, and the error everyone read pointed at `loop_actions.go`, two steps downstream of the actual fault.
- **sources:** `platform/orchestration/actions/v3_site_actions.go#ExtractFieldsAction` (the `required` block, after defaults); `platform/orchestration/actions/extract_fields_required_test.go`; `docs/agent_docs/sql_for_agents/308_select_sections_tolerates_the_wrapper_and_opts_into_required.sql`; `bugs_open/192_HANDOFF_2026-08-04_select_sections_fallback_dies_on_a_null_link_resolution.md`; `docs/agent_docs/docs024_key_docs_latest/bugfix_192_select_sections_wrapper/`
- **relations:** WFA-002 `$ctx.` (the same principle from the other side — it made an unknown field a hard error rather than a silent empty bind, and for the same reason); WFA-003 sub-workflow validation (what "validated" guarantees); `bugs_open/178` (whose fix produced the shape change this caught); `bugs_open/087` (shares this exact error signature at `process_sections_loop`, and the loop's improved message lands in its lap); `datahelpers.ExtractNestedField` (returns nil for an absent key AND a present-but-null key — the ambiguity underneath all of this, deliberately left alone: for a fallback list, treating explicit null as unresolved is the wanted semantics)
- **landmine:** **the trap this entry exists because of is NOT `extract_fields` — it is `output_field`.** A step whose `output_field` names a key an earlier step wrote does not annotate that key, it **REPLACES** it: `coordinator.go:1859-1861` (`storeActionResult`) stores an action's return value **wholesale**. So an action that returns `{the_thing, applied, reason}` while declaring `output_field: the_thing` silently demotes the real value one level, on every run, in every mode — and **the wrong result looks exactly like the right one**, because all the data is still there, just one level down. Any `SELECT collected_data->'x'->'y'` written to check it will read the path you expected and tell you nothing; **enumerate the keys** (`jsonb_object_keys`) instead. Second trap: **the shim path added by seed 308 must stay LAST**. `select_sections`' three paths are tried in order and the flat path sits ahead of the wrapper path precisely so the shim goes structurally dead on the roll; reorder them and the shim becomes permanent folklore. Third: **do not "fix" `resolve_links`' `input_mapping` the same way** — `input_mapping` has no ordered fallback, so pointing it at the wrapper path would work today and silently re-break on the roll with no error to notice.
- **review status:** submitted to the council gate 2026-08-04 alongside the commit; committed with `Council-Submitted:` because the verdict had not been read at commit time (never `Council-Reviewed:` on an unread verdict). **Scope argued explicitly**, since this council vetoed `bugs_closed/124` on precisely this ground: by owner ruling 2026-07-29 §1 the RFC trigger is whether a shared mechanism's **guarantees** change. They do not — with `required` absent, every existing step behaves exactly as before, and the capability is reachable by nothing until a config names it (1 of 2 `extract_fields` steps has, measured). Consumers **named, not merely counted** (§3): the only two `extract_fields` steps in the fleet are `page-content-writer.select_sections` (opted in, deliberately) and `research-agent.extract_topic` (untouched, still lenient) — and the `178` lane, whose action this change reshapes, is named in the bug file and the commit.
- **verify-later:** pod-grep on **every replica**, with a control: `grep -ac 'required field(s)' /app/agent-chassis` → 0 before the roll, 1 after (discriminating), alongside a pre-existing control from the same function such as `grep -ac 'ExtractFieldsAction: Found via input_data prefix'` → 1 throughout (proves the probe reads the binary you think it does; note Go compiles short literals to immediate comparisons, so pick long strings). Then the behavioural check no build can give: after the roll, confirm `collected_data->'section_plan' ? 'applied'` is **false** and `? 'sections_ready'` **true** on a fresh `page-build-handler` run — the wrapper gone at source — and then that `required` still holds by inducing it. The census that should stay honest as adoption grows: `SELECT ad.type, s.key, s.value->'config'->'required' FROM agent_definitions ad, jsonb_each(ad.default_config->'workflow'->'steps') s WHERE ad.is_active AND COALESCE(ad.is_snapshot,false)=false AND ad.deleted_at IS NULL AND s.value->>'action'='extract_fields';` — **2 rows, exactly 1 opted in** today.

### WFA-010 — `GetBoolFieldLoud`: the loud reader for an author-declared bool step-config key
- **status:** **DEPLOYED and pod-verified** — chassis `v1.0.1252`, 2026-08-05 09:10Z, both replicas (`GetBoolFieldLoud` present; `resolveSubstepContinueOnError` = 1 as the positive control). Council `51ed1257-520c-4708-900b-216bf9eb4cd9`: REVISE → **APPROVED** (round 2; the REVISE was gated on a landmine keyed to these exact symbols that the submission had not cited — read in full it confirms the design). **Deployed, NOT exercised:** no loop declares either key with a non-bool value at any nesting depth, so the warning has never fired and will not until somebody mistypes a loop config — the intended end state.
- **status-evidence:** 6 helper tests + 3 wiring tests pass, and each names the mutation it kills; three mutations run on a clean `git archive HEAD` tree, all RED (fold the assertion to `ok && value` → the declared-false case; delete the Warn → the non-bool case, *with values unchanged*; drop the caller's variadic fields → the substep case). The five pre-existing `substep_continue_on_error_test.go` tests pass **unchanged** — the inertness proof. **Not exercised live:** no loop declares either key with a non-bool type, so nothing has made it warn in production.
- **what:** `datahelpers.GetBoolFieldLoud(m, key, fallback, logger, fields...)` — reads a declared bool from a step config and **says so when the declaration is malformed** instead of silently substituting the default. Absent key → fallback, silently. Present bool → that value. Present non-bool → fallback **plus a Warn** naming the key, the declared type, the declared value and the fallback applied. The **fallback is a parameter**, which is what lets one function serve both semantics: the loop-level reads default to `false`, while the substep-level read falls back to the *loop's* resolved value (inheritance, not a default).
- **why:** `bugs_open/193`, filed by the council gate's own `bug_historian` seat while reviewing `bugs_open/173`. 173 made the SUBSTEP-level read of `continue_on_error` loud and deferred its LOOP-level twin to prose in a risk section — *"one call site of a shared judgement gets the rigorous fix; the sibling stays heuristic"*. The result was the same key warning in one place and silent in another, which is worse than uniform silence: the first thing a reader infers from "no warning" is "no problem".
- **callers (3):** `loop_actions.go` — `allow_missing` and `continue_on_error`; `loop_expansion_handler.go` — `resolveSubstepContinueOnError`, which keeps its name, signature and doc (it is 173's landmine anchor and the post-roll grep symbol) but delegates the parse.
- **PRESENCE IS NOT TRUTH.** The type assertion is tested on its own and never folded into `ok && value`. That fold is a bug, not a style choice: it reads a declared `false` as no declaration at all, so an explicit opt-out silently becomes an inherited opt-in. `loop_error_handler.go:83` may write `ok && cont` safely **only** because the value it reads was stamped by the platform as a Go bool, never by an author.
- **LANDMINE — this is the SIXTH reader of this judgement, not the second.** `datahelpers.GetBoolField` plus at least four private clones (`extractBoolConfig`, `getConfigBool`, `configBoolOrDefault`, `readBoolFromContext`) already parse config bools **silently**. This entry does NOT retire that class and does not claim to. Reach for the loud reader when the key is **author-facing** (a human writes it in a workflow definition and a mistype changes behaviour); the silent ones remain correct for machine-stamped keys, where a warning could only ever fire on a platform bug.
- **scope, measured 2026-08-04:** ~76 bare `.(bool)` config reads exist across `platform/orchestration`. Three converged — the ones whose blast radius was measured. Deliberately left: machine-written keys (`loop_iteration`, the injected `continue_on_error` read by `loop_error_handler.go`), per-action options already going through an existing helper, and `max_iterations` (`loop_actions.go:53`), which has the same defect shape with a different type (`.(float64)`) and is **unmeasured** — recorded here rather than silently deferred, which is the lesson `bugs_open/193` exists to teach.
- **live declarations, measured:** 10 loop steps declare `continue_on_error`, **all boolean**; **zero** declare `allow_missing`; 18 loop steps in total (the positive control — the predicate reaches loop configs, so a string row was reachable had one existed). The change is inert on arrival by measurement, not by hope.
- **sources:** `platform/orchestration/datahelpers/declared_bool.go` header; `declared_bool_test.go`; `bugs_open/193`; `bugs_open/173` (WFA-008, the substep twin)
- **relations:** WFA-008 (`resolveSubstepContinueOnError`, the caller this converges with); `datahelpers.GetBoolField` (the silent sibling); 016b §9 *"one call site of a shared judgement gets the rigorous fix; the sibling stays heuristic"* and *"prefer one implementation with two callers over two implementations with a test that they match"*
- **verify-later:** a chassis image containing the symbol `GetBoolFieldLoud`; the first live warning (needs somebody to mistype a loop key, so it may never fire — which is the intended end state)

### WFA-011 — `config-key-audit --shared-output-fields`: two steps writing one `output_field`, over the FULL routing graph
- **status:** deployed — CLI **and** the ONLINE half. ~~the ONLINE half (a CronJob) is NOT built~~ **CORRECTED 2026-08-08: the CronJob is BUILT, DEPLOYED and PROVEN**, so the owner's (d) ruling is fully discharged. `shared-output-fields-check`, daily **07:10 UTC**, own image (`aqls/shared-output-fields-check:v1.0.1265`) on its own tag sequence — it does NOT ride the fleet release.
- **status-evidence:** `abf5e8266` (detector), `867037f5a` (descent fix), `22ed9aa04` (the CronJob). Live run 2026-08-06 over **176 agents**: findings were EXACTLY the RFC addendum's hand census — the 2 known hazards, the 5 benign pairs correctly absent. Baseline `0 new / 2 acked / 0 stale, exit 0`. **Re-measured 2026-08-08 over 177 agents: unchanged.** First CronJob run (manual trigger, 15:45Z) **CLEAN over 177 agents via the DIRECT-POSTGRES route, container exit 0, and it wrote its `doc_notes` row** — the DB route returned the same 177 the `kubectl exec` export does, which is two independent fetch paths agreeing.
- **status-caveat, because a status line outlives its truth:** what is proven is CLEAN, EXIT 0 and REPORTS. The **exit-1-on-a-NEW-pair** path is proven against the built image (`docker run` with the ack list omitted → exit 1) and by unit test, **not** by a real new pair occurring in the cluster — there has not been one. All four exit codes verified on the image before deploy: 0 clean, 1 NEW, 2 unusable input (it refuses to print a clean report over an empty fleet).
- **how to see it, and why a missing row is not silence:** it reports on **EVERY** run, clean ones included, so an absent row means THE JOB DID NOT RUN and can never read as "nothing is wrong" — `SELECT created_at, left(body,240) FROM doc_notes WHERE source='shared_output_fields_check' ORDER BY created_at DESC LIMIT 7;`. The body states the run's SCOPE (agents scanned, routing keys read), because a finding count cannot distinguish "looked at 177 and found nothing" from "looked at 3 and found nothing".
- **what:** Reports two steps that share an `output_field` when they are **transitively reachable over the full routing graph** AND are **DIFFERENT actions** — the shape where one step's result silently overwrites another's. Graph edges are `next_step`/`error_step` plus **13 config keys**. `--ack <file>` is the ratchet: exit 0 while only acked pairs reproduce, 1 on a NEW pair, and it reports **stale acks** (an ack that outlives its finding is how a ratchet loosens). Hand-run form: `scripts/audit-shared-output-fields.sh`.
  **THE LANDMINE:** **the RFC's own "13 keys" prose is ONE KEY SHORT** — it names 11 after `then_step`/`else_step`, and the live fleet has a **config-level `error_step`** (158 occurrences) distinct from the top-level `Step.ErrorStep` field. Re-derive against the fleet; do not transcribe the list. The RUNBOOK carries the query.
  **AND THE ONE THAT MATTERS MORE:** *a green live run proves the FLEET, never the DETECTOR.* The detector's ability to fire is proven by `go test ./cmd/config-key-audit/ -run TestSharedOutputs`, which fires on `bugs_open/192`'s own pre-fix shape and LOSES the finding when the `config.then_step` edge is severed. **Never cite a clean census alone as evidence the check works.**
  **THE THIRD LANDMINE, found 2026-08-08 and the reason the descent is no longer its own:** a hand-written walk into `sub_workflow` **cannot see `substeps`**, and `substeps` is the half the loop executor PREFERS (`loop_actions.go:91`). This detector had exactly such a walk, so it was blind to that shape and, on a step carrying BOTH, audited the **inert** copy. It now goes through `validation.WalkSteps`. **Zero live definitions carry `substeps` at any depth**, so no live run could ever have revealed it — the proof is two tests that fail without the fix, plus an A/B showing the swap is byte-identical over a 177-agent export. Full entry in `LANDMINES.md` (ten footprints).
- **sources:** `cmd/config-key-audit/` (`--shared-output-fields`, `sharedoutputs.go`, `fleetdb.go`); `scripts/audit-shared-output-fields.sh`; `scripts/shared_output_fields_ack.txt`; `build/docker/backend/shared-output-fields-check.dockerfile`; `deployments/kustomize/services/shared-output-fields-check/`; makefile targets `build-/push-/deploy-shared-output-fields-check` and `shared-output-fields-check-now`; commits `abf5e8266`, `867037f5a`, `22ed9aa04`; `docs024_key_docs_latest/rfc012_await_findings/RUNBOOK_rfc012_await_findings.md`
- **relations:** WFA-004 / WFA-006 (`--unregistered-actions`, `--single-owner-actions`) — same binary, the mode pattern `singleowner.go` established; **CGV-030 (`component-render-check`) — the Go-image CronJob precedent this followed, and `fleetdb.go` mirrors its `dbConn`/`writeDocNote` convention deliberately** (`PG_CLIENTS_HOST` is how a job declares itself, because `ai-persona-app` has no `pods/exec` RBAC and a kubectl-only tool fails there LOOKING LIKE A CLEAN RUN); RFC_012 (d), owner-ruled a STANDING check, ONLINE if the framework allows — **now discharged**; RSH-008 is the other half of the same sitting.
- **council:** ~~REJECTED~~ **APPROVED 2026-08-08 as its own round, corr `7b6497d7-8147-4b15-aee4-fa6e361827f2`.** The earlier REJECTED verdict on `5c2bc265-…` was of a submission that **bundled this detector with an unrelated 34-file refactor**; two seats correctly called that scope creep (`editquality`: *"not on the causal path … makes the round judge two different bugs at once"*), and splitting the rounds is what fixed it — **no code changed to earn either approval**. Both round-2 objections are discharged: `reuse_agent` (the `relaygaps.go` comparison — **looked at, and it found the real defect described above**) and `tooling_provenance` (a `doc_notes` PLAN/NOTES row now exists at `subject_key='cmd/config-key-audit'`, plus a companion for `agenterrors`).
- **verify-later:** the **exit-1-on-a-NEW-pair** path in the live cluster — it has never fired because no new pair has occurred; it is proven on the image and in tests only. And the **routing-key list is a literal that will drift**: a new `*_step` config key and the check silently stops seeing that edge, which is the same class as the RFC's own eleven. A parity test between the literal and the live key set is the real control and is NOT built (disclosed in the approved submission's risks).

### WFA-012 — A workflow config dot-path may index an array: `search_results.results.0.url` resolves
- **status:** **⚠ ROLL CONDITION MET 2026-08-10, verified by build provenance.** Chassis `v1.0.1283` is built from commit `d3c09cc74` — read back from BLD-019's stamp on **both** replicas — and `git merge-base --is-ancestor` puts this entry's own cited commit(s) inside it. **The Go code IS in the running binary; every clause below about waiting for a roll is superseded.** This note makes no claim that the feature is EXERCISED — that is a separate condition on a separate clock. Original: **BUILT and council-APPROVED (`c961b79e-59e7-45db-a9ad-abb61aaad935`, round 1, 3 advisory objections, none high), NOT YET ROLLED** — Go changes are inert until the next chassis image, which any session's build will carry. Commit `f7111f4d8`, comment correction `6cb41ae06`.
- **status-evidence:** 11 tests in a new `extract_nested_field_test.go` — a function that had **no dedicated test at all** despite ~460 call sites across 123 files. Mutation run: deleting the array branch fails exactly the seven tests that assert an indexed path resolves and leaves the characterisation tests (`ExistingBehaviour`, `ResponseUnwrapStaysMapOnly`, `TypedNilSliceIsNotIndexable`) green — the no-op half, which is the evidence that nothing existing was disturbed. Stated honestly: `ArrayIndexEdges` also stays green under that mutation, because out-of-range/negative/non-numeric return `nil` with or without the branch; that test pins the guard, not the capability.
- **what:** `datahelpers.ExtractNestedField` (and its `…String`/`…Map`/`…Int` wrappers) now indexes a `[]interface{}` when a path segment is a bare, in-range, non-negative integer. So a step config may write `some_step.results.0.url`. **Map access is tried first and is unchanged**, so a map carrying a literal `"0"` key still resolves by key; the two branches are mutually exclusive by type, and the array branch is reachable **only** where the walk previously returned `nil` — the change can turn a `nil` into a value and can never alter an answer that resolves today. Bracket notation (`results[0].url`) is deliberately **not** parsed: the config-side convention here is the bare dot segment.
- **why:** `vet-practice-verifier`'s `scrape_website` step has configured `fallback_url_field: search_results.results.0.url` since it was written, and that fallback had **never once fired** — the walk died at the `0` segment. Doubly silent: `webscrape_actions.go` logged only on fallback *success*, and the terminal "URL not found" error never named the key. Both surfaces are now closed (a `Warn` when a configured fallback resolves to nothing, and the key named in the error).
- **blast radius, measured not asserted:** live `agent_definitions` was censused twice with different regexes — a strict whole-string path match and a coarse `token.NUMBER` match anywhere in the config text. **Both return exactly ONE row fleet-wide**, the vet path this exists to fix. Nothing else in live config can reach the new branch.
- **LANDMINE — the census that sized this is blind to a whole class of caller, and the clean answer hides it.** A config-text regex cannot see a path Go assembles at runtime. Seven live sites concatenate rather than pass a literal (`render_css_from_spec_action.go:463,466`, `thunder_ssh_exec_dispatch.go:248,348`, `v3_site_actions.go:977`, `database_actions.go:50`); checked 2026-08-09, all seven append a field **name**, never an index. The residual route that keeps the config census load-bearing is `database_actions.go:50`, whose path comes *from config*. Raised by the `guardian` seat, and the full entry is in `LANDMINES.md`.
- **THIS IS ONE RESOLVER, NOT THE CODEBASE.** About ten near-clone walkers remain **map-only** — `resolveFieldPath` (plus two copies in `entity_state_actions.go:398` and `workflow_actions.go:576`), `getFieldByPath`, `GetValueByPath`, `FindByPath`, `GetValueAtExactPath`, `getNestedValueForLoop`, `getNestedValueForSpawn`, `traverseNestedPath`, and an unexported `extractNestedField` dupe at `multipage_actions.go:1129`. So "dot paths support indexes now" is **false** as a general statement: a numeric segment resolves through this resolver and not its siblings. Converging them is real work with its own evidence burden and was deliberately not bundled.
- **sources:** `platform/orchestration/datahelpers/data_helpers.go` (`ExtractNestedField`), `extract_nested_field_test.go`, `platform/orchestration/actions/webscrape_actions.go`; `rfc012_await_findings/SUBMISSION_2026-08-09_extract_nested_field_array_index.json`; `architecture_review/CENSUS_2026-08-07_rfc012_await_step_readers.md` §6.1 (corrected in place — the walk dies at `0`, not at `results`, which resolves via the `.response` auto-unwrap)
- **relations:** WFA-009 (`extract_fields` `required` — the other "a path that resolved to nothing said nothing" fix); `bugs_closed/101` (the round that wired `fallback_url_field` into the input spec **without** making it resolvable, which is why the key looked live for weeks); RSH-008/RSH-009 (same lane)
- **verify-later:** ~~a pod-grep of the shipped chassis binary after the next roll~~ **DONE 2026-08-10 — LIVE on `v1.0.1283`, both replicas**: the `Warn` literal `fallback_url_field is configured but resolved to no URL` = 1, positive control = 2, absent-needle control = 0. The **first live firing** remains unobserved — **and it is UNOBSERVABLE for want of demand, not pending**: `vet-practice-verifier` has **0 runs** in the retained `orchestration_states` window, so the only step configured with a numeric path segment has not executed at all. ⚠ **Do not "verify" this with a zero.** A grep of the logs finding nothing means the agent did not run; it says nothing about the fix. The demand query is `SELECT count(*) FROM orchestration_states WHERE owner_agent_type='vet-practice-verifier'` — run it FIRST, and if it is 0 the correct verdict is *unobservable*, not *passing* (`LANDMINES.md` 2026-08-10, the DEMAND-control entry). Whether the resolved URL is a *useful* one is the vet lane's call, and the Warn is what will tell them.

### WFA-013 — Optional-key BUDGET census (`config-key-audit --optional-key-budget`): the counter that closes RFC_022's stated blind spot

- **status:** built + tested 2026-08-13; **budget RULED 2026-08-14 (owner): N = 10** — the wrapper now defaults to it, so a bare run enforces the ruling (`--census` keeps the no-budget census). **CronJob LIVE 2026-08-14** (owner: "build it now", "we can keep the python"): `deployments/kustomize/services/optional-key-budget-check/`, daily 06:50 UTC, Python-mirror shape copied from WFA-006's `single-owner-carriers-check` — proven by a manual Job run against the live fleet (185 agents walked, the exact three standing findings, doc_notes row written, exit 1 by design). The RFC_022 interim (migrations `381`/`383`) deliberately opened an accumulation blind spot; this closed it, and RFC_022 is now CLOSED. **Owner framing, binding on how findings are read:** sharing is estate design, not the defect — a finding means the action's accumulated optional surface owes one review as a whole, never that its reuse is a problem.
- **the acknowledged-baseline mechanism (2026-08-14):** `architecture_review/optional_key_budget_acks.json` is the source of truth — an entry records that an action's surface was reviewed and acknowledged at `count`; the Go tool (`--acks`, wired into the wrapper) and the cron's `ACKED_LEVELS` literal both honour it, so a reviewed action is quiet at its baseline and pages again only on growth PAST it. A shrunken surface marks the ack `stale_ack` (report-only). **FOUR drift surfaces, all pinned by `optional_budget_cron_parity_test.go`** (counts literal ↔ registry; acks literal ↔ acks file; `BUDGET` ↔ wrapper default; the third `walk_steps` copy ↔ the Go detector on a substeps-wins fixture) — every guard proven able to fail by mutation before first apply.
- **two traits inherited from the sibling's shape, so nobody re-diagnoses them:** on findings the Job retries once (`backoffLimit: 1`) and each attempt writes its own doc_notes row — TWO rows on a red day, one on a clean day; and updating acks or counts means editing `check.py` AND re-applying the kustomize overlay (the configMap is generated from the file), which the parity tests force you to remember at build time but nothing forces at APPLY time — after any `check.py` edit, `kubectl apply -k …/optional-key-budget-check/overlays/production/uk_001/`.
- **status-evidence:** live census 2026-08-13: **118 actions declare optional keys, 21 of them SHARED (≥2 live carriers)**; widest shared surfaces `analyse_repo_local` (12 keys, 2 carriers), `append_doc_note` (11 keys, **8 carriers** — the RFC's own motivating action), `diagnose_prepare_fix_commit` (11 keys, 2 carriers). At a trial budget of 10 those three, and only those, are flagged; the budget path exits 1 and names each with its carrier list.
- **what:** `censusOptionalKeys` joins the two numbers the RFC_022 trigger needs: `len(ActionInputSpec.Optional)` per registered action (the declaration side — exactly "a sweep over `RegisterActionInputSpec` declarations per action") against DISTINCT live carrier agents (the shared side, from the same stdin export every live-join mode reads, walked with `validation.WalkSteps` so a loop's `substeps` body cannot hide a carrier — `bugs_open/144`). `over_budget` fires only on a SHARED action past the given N; a single consumer's surface is its own business, and taxing it would recreate the per-change tax the ruling retired. Without N it is a pure census and always exits 0. `scripts/audit-optional-key-budget.sh [--json] [N]` is the runnable form; **it discriminates the tool's refusal by EMPTY STDOUT, never by exit code** — `go run` folds exit 2 into 1 (the LANDMINES entry this package already carries).
- **why it is registerable:** the architecture seat's trigger moved (owner ruling 2026-08-11) from "any new opt-in field on a shared action" to the accumulated COUNT — and until this existed nothing could produce that count, so the seat was running on a prompt clause that could only say "several". Any reviewer, seat, or lane sizing "is this shared action accumulating surface?" now has a one-command answer with the carrier list attached.
- **deliberately NOT counted:** `ConfigKeys` — settings rather than input references (the spec's own distinction); the harm the seat named (new authority arriving as opt-in fields) lands in `Optional`, where `note_body_suffix_field` (the motivating case) lives. Counting both would let a settings-heavy action trip a budget meant for accumulated authority.
- **open decision (owner):** ~~the budget **N** itself~~ **RULED 2026-08-14: N = 10** — the standing stock is exactly the top three above, each owing one review of its accumulated surface, then baselined. ~~Go-native vs Python-mirror~~ **RULED same day: the Python mirror stays** (owner: "we can keep the python") — no Go rework of the RFC_006 check, and a counter CronJob, if built, may use the same shape. Still open: whether to build that CronJob at all, or stay on-demand.
- **sources:** `cmd/config-key-audit/optionalbudget.go` (`censusOptionalKeys`, `emitOptionalKeyBudget`), `optionalbudget_test.go`, `scripts/audit-optional-key-budget.sh`; `architecture_review/RFC_022_*.md`; `docs/agent_docs/sql_for_agents/402`/`403` (the roster clauses updated to name this counter)
- **relations:** WFA-006 (the RFC_006 sibling whose shape this follows — same binary, same decode, same WalkSteps, same refuse-on-empty pair); WFA-004 (the traversal's registration); RFC_022 (what this closes); the `099`-reverts-377 landmine (why the roster clause update is a surgical migration pair, not a mirror run)
- **verify-later:** `./scripts/audit-optional-key-budget.sh` on demand (census), or with the ruled N once it exists. A "0 actions register an ActionInputSpec" refusal is a broken build, not a pass — the tool exits 2 rather than printing one, and the wrapper reads the empty stdout, not the code.

### WFA-014 — The awaited-park carries the dispatching step's own `collected_data`, additively
- **status:** built + tested 2026-08-15, **NOT yet live** — Go, so inert until the chassis image is rebuilt and rolled. Saying "deployed" would overstate it; see verify-later for the check that settles it.
- **status-evidence:** 8 tests green, **5 mutations proven** (call site unwired; carry made overwriting; forged-marker strip removed; strip unscoped from awaited steps; strip made to mutate the live map) — each fails the test that claims to cover it. Two of the eight drive the **real** `persistAwaitingStateWithRetry` through a mocked DB and assert on the `collected_data` JSON actually written; the other six call the helper directly and would pass whether or not the park is wired to it, which is exactly the gap the two exist to close.
- **what:** Parking a step to await an adapter reply used to discard everything the action computed. `persistAwaitingStateWithRetry` reloads the row from the DB and copied onto it only `AwaitedRequests`, `Status` and `LastActivity` — so `storeActionResult`'s own step-name and `output_field` writes, and any sibling key the action wrote, never reached the DB at all. `carryCollectedDataOntoFreshState` now copies the in-memory `CollectedData` across, **additively: a key already present on the fresh copy is never overwritten**, so a reply that landed while we were parking, and any other concurrent writer, still wins. It runs inside the retry loop, once per attempt, against that attempt's own freshly-loaded state, so it is idempotent. One guard rides with it: a carried key spelled `response` under a step being parked on would be indistinguishable from an arrived reply to this same function's arrival check, so it is stripped by copy and warned about.
- **why it is registerable:** this is the platform's own response plumbing, not one action's bug. **Every** action that enriches its own result before dispatching — findings, audits, counts, a computed URL — was losing that work fleet-wide, and each one that noticed invented its own escape hatch (the sibling-key pattern, which RFC_012 addendum 2 proved is a no-op on this exact path). A new findings-plus-await action no longer has to know any of that.
- **the sentence not to round up:** this fixes the **park**, which is one of three faces of the class. The reply-time write (`applyResponseToState`) still REPLACES the step's record wholesale on **two of its four branches** — the `output_mapping` branch and the default branch — so a carried key survives the park and can still die when the reply lands. Only the `.response` branch (`isAgentResponse`) is additive. **Check which branch your step takes before trusting a carried key to survive.** `storeActionResult` is untouched.
- **measured, not asserted (2026-08-15):** across the 15 files returning `await_response: true`, **no action returns a top-level `response` key** — every `"response"` spelling in them is a reader of an arrived reply or a nested topics map. The forged-marker guard is for the next action, not a live case. And the reader census this was ruled on (`architecture_review/CENSUS_2026-08-07_rfc012_await_step_readers.md` §7) scored this additive shape at **0 config-side breaks and 0 Go-side breaks**, against 3 silent Go breaks for the alternative design.
- **sources:** `platform/orchestration/coordinator.go` (`carryCollectedDataOntoFreshState`, `withoutResponseMarker`, called from `persistAwaitingStateWithRetry`); `platform/orchestration/park_carries_collected_data_test.go`; `platform/orchestration/park_persists_collected_data_test.go`; `architecture_review/RFC_012_*.md` (owner ruling 2026-08-15, third sitting); `bugs_open/236` (the case, mechanism confirmed 2026-08-14)
- **relations:** RFC_012 faces 1 and 2, both still open — `applyResponseToState` (a) and `storeActionResult` (a′), the latter explicitly outside the census's scope; WFA-011 (`--shared-output-fields`, the (d) detector for the `output_field` collision half); the LANDMINES entry "An action that RETURNS findings and AWAITS a response loses the findings", **narrowed** the same day rather than retired, because its reply-time half still stands
- **landmine:** a carried key is not a *durable* key. The only mechanism that survives BOTH the park and the reply-time replace is still a direct DB write before dispatch (`agent_error_log` via the shared writer). Do not read this entry as retiring that advice.
- **verify-later:** after the next chassis roll, `kubectl -n ai-persona-system logs -l app=agent-chassis --tail=100000 | grep -m1 'Carried the dispatching step'` — and note the demand control: that line only fires when a step actually parks with keys of its own, so an absence proves nothing unless an awaited step ran in the window. The durable check is a parked `orchestration_states` row whose awaited step's key holds the action's keys alongside `response`.

### WFA-015 — `loop_iteration_terminal`: a reserved step-config key that tells a loop's per-iteration terminal NOT to aggregate
- **status:** built + tested 2026-08-17, **NOT yet live** — Go, so inert until the chassis image is rebuilt and rolled. Council `Council-Submitted: 7a3c4fb7-e8c1-4b5f-950e-7a826d5bebbe`. See verify-later for the check that settles it.
- **status-evidence:** 5 tests green; the guard **proven by mutation** — with the early return disabled the guard test fails and prints the recursion verbatim (`results[0].done.results[0].done.nested` carrying the prior-iteration marker twice). The guard test has a **control** that strips only the two terminal signals from otherwise identical input and requires the OLD swallowing behaviour back, so it cannot pass against a `LoopCompleteAction` that merely never aggregates.
- **what:** A loop sub-workflow's terminal substep is conventionally declared `action: loop_complete` — which is ALSO the action of the loop's own end-step. `handleLoopExpansion` injects that substep once **per iteration**, so the whole-loop aggregator ran from inside every lap and, via `LoopCompleteAction`'s generic Strategy-3 scan, copied every `<loop>_iter_<i>_*` key into its own result — earlier iterations' aggregates included. `collected_data` doubled per lap. Expansion now sets `loop_iteration_terminal: true` on the injected copy and `LoopCompleteAction` returns a three-field marker (`status`/`iteration`/`loop_name`) for it, before the diagnostic key dump. Aggregation is unmoved: it still happens on the loop's own `<loop>_complete` end-step, which is what `output_field` exposes and what every consumer reads.
- **why it is registerable:** it is a **reserved key on a shared mechanism** — injected automatically onto a step in all 15 live loops, not opt-in and not addressable by a workflow author — so it is exactly the shape the guardian seat vetoed `bugs_closed/124` for shipping unregistered. It is listed in `frameworkStepConfigKeys` beside `loop_iteration`, so the unknown-config-key audit stays quiet; a future reader of an expanded plan will see the key and needs this entry to know what set it and why.
- **match on the ACTION, never the name — this is the part that bites:** the terminal substep is called `done` in only **9 of the 15** live loops. The rest are `complete_page` (4 — `pageflow-builder`, `page-rebuild`, `rerender-site`, `site-work-orchestrator.build_items_loop`), `complete_dispatch` (`maintenance-triage`) and `task_complete` (`vet-batch-processor`). A name-based fix repairs 9 and silently leaves 6 doubling, three of them page-building loops.
- **why the flag is set at INJECTION rather than inferred:** a **nested** loop's own end-step is built by its own call to `handleLoopExpansion` and is never injected as a substep, so it does not carry the flag and keeps aggregating correctly even while the orchestration sits inside an outer loop's iteration. No nested loops exist live today (measured 2026-08-17: zero sub_workflows contain an `action: loop` step), so that arm is forward-looking, not exercised.
- **the second signal, and why it is not redundant:** `isLoopIterationTerminal` also treats a bare `loop_iteration` as terminal. Workflow plans expanded and persisted **before** this ships are already in flight with no flag in them; without the fallback they would keep doubling until they died. The loop's own end-step never carries `loop_iteration`, so the fallback cannot misfire on it.
- **measured blast radius (2026-08-17), so nobody re-derives it:** all 15 `loop_complete` substeps fleet-wide declare `output_field = (none)` and `next_step = (none)`. Their result is therefore reachable only through the per-iteration runtime-minted step-name key, which no config can address and for which `platform/ internal/ pkg/` contains no Go reader. The aggregates were written, nested into each other, and read by nobody.
- **the sentence not to round up:** this stops the growth; it does **not** shrink the orchestrations already dead at 22 MB, and it fixes neither of the two residuals recorded in `bugs_open/289` — `LoopCompleteAction` still lets a step lacking its own `total_iterations` inherit the whole loop's (latent once the early return is in place), and there is still **no size tripwire** on `collected_data`, so a 22 MB orchestration remains silent.
- **sources:** `platform/orchestration/loop_expansion_handler.go` (the injection-time mark); `platform/orchestration/actions/loop_actions.go` (`isLoopIterationTerminal`, `loopConfigInt`, the early return in `LoopCompleteAction`); `platform/orchestration/datahelpers/action_inputs.go` (`frameworkStepConfigKeys`); `platform/orchestration/actions/loop_complete_iteration_terminal_test.go`; `bugs_open/289_HANDOFF_2026-08-16_a_loop_complete_substep_re_aggregates_…`
- **relations:** WFA-008 (a loop substep's own `continue_on_error` — the other per-substep override resolved at the same injection site); `bugs_closed/173` (the other loop-engine defect, error-routing granularity — different bug, same files); `bugs_closed/124` (the precedent that makes an unregistered reserved key a scope veto); the two LANDMINES added with 289 (`_iter_N_done` is a whole-loop aggregate; and a `090` REFUTED on an oversized-state target is bundle starvation)
- **landmine:** a `<loop>_iter_<N>_<terminal>` key is **not** that iteration's result — before this ships it is a whole-loop aggregate, and after it ships it is a three-field marker. Either way, read the per-iteration substep outputs (`item_created_<N>`, `<loop>_iter_<N>_<substep>`) when you want to know what one lap did.
- **verify-later:** after the next chassis roll, re-run one `tool-auditor` audit and check the series is flat rather than geometric — `SELECT key, length(value::text) FROM orchestration_states, jsonb_each(collected_data) WHERE orchestration_id='<new run>' AND key LIKE '%\_done' ORDER BY key;`. **The disconfirming result is a ratio near 2.0 between consecutive laps.** Note the demand control: this proves nothing unless a loop with more than one iteration actually ran in the window, so check `loop_metadata.total_iterations > 1` on the run you read. The durable pass is a `tool-auditor` orchestration reaching `COMPLETED` — the population had exactly one such row in 63 before this.

### WFA-016 — the `collected_data` size tripwire: the fleet can finally SEE an orchestration whose state is running away
- **status:** built + tested 2026-08-17, **NOT yet live** — Go, inert until the chassis image is rebuilt and rolled. ~~**No council trailer: the gate is unreachable** (Anthropic account quota exhausted 2026-08-17 11:08Z, regain 2026-09-01)~~ **CORRECTED 2026-08-17: that was false — the outage lasted ~3 minutes** (4 failed calls 11:08:37→11:09:53Z, successes resuming 11:13:02Z; the "regain 2026-09-01" text in the API's 400 body is not predictive, as `bugs_open/243` already recorded for the identical error on 08-10). **The gate was available throughout.** This is now in **round 2 of council correlation `7a3c4fb7-e8c1-4b5f-950e-7a826d5bebbe`** (dispatched 16:38Z), folded into WFA-015's round rather than taking its own because `bugs_open/244` measures `council-gate` at 87.8% of the fleet's August LLM spend. **Residual honesty problem, stated rather than hidden:** the commit that carries this code (`cf970b009`) has NO trailer at all and forward-only forbids an amend, so `098` cannot auto-credit it — the correlation lives here and in the lane docs instead. The lesson worth keeping: when you cannot reach the gate you have no correlation, so you cannot write even `Council-Submitted:` — which is an argument for submitting BEFORE committing whenever the two can be ordered.
- **status-evidence:** 10 tests green, **wiring proven by mutation**: commenting out the one call site leaves every direct-call test PASSING and fails only `TestTripwireIsWiredIntoTheRealPersistPath`, with "the helper is not wired in". That asymmetry is the entry's whole claim to being real — this estate has already logged "tests that passed with the guard unwired" as a wrong call, and a helper with no callers looks exactly like a finished refactor.
- **what:** `UpdateStateWithVersion` — the single path every orchestration state update goes through — now checks the size of the `collected_data` it is about to write and logs when it is abnormal: **WARN at 8 MiB, ERROR at 24 MiB**. The report names the orchestration, the owning agent type, the current step, the total size, the **key count**, and critically **the single largest key and its size**. It only ever logs; an orchestration is never failed for being large, because the size is a symptom of somebody else's defect and killing the run would destroy the evidence.
- **why it is registerable:** it is a new fleet-wide signal that did not exist. `bugs_open/289` grew `tool-auditor`'s state to 22 MB and killed 62 of its 63 runs, and it ran that way **from 2026-07-29 to 2026-08-17 with nothing noticing**, because there was no threshold anywhere — an orchestration carrying a thousand times its siblings' payload looked exactly like one carrying a normal one. Anyone who now sees this WARN in the logs needs to know what set it; and anyone diagnosing a stalled run should reach for it before theorising.
- **the thresholds are measured, not round, and a test asserts that:** fleet census 2026-08-17 — `internal-linker` 22 kB, `tool-suggester` 447 kB, `page-content-writer` 886 kB avg / 1.8 MB max, `build-dispatch-loop` 2.8 MB avg / **14 MB max**, `tool-auditor` 22 MB avg / 29 MB max. 8 MiB therefore sits above everything healthy and below both pathological cases. `TestCollectedDataThresholdsSitAboveMeasuredHealthyTraffic` fails if a later edit drops the warn line under `page-content-writer`'s real ceiling or raises it above `tool-auditor`'s pathological average — i.e. the census is pinned in code, not just in this paragraph.
- **why the LARGEST KEY is reported and not just the total:** on 289 the total said "something is wrong" and the key name (`create_items_loop_iter_9_done`) identified the mechanism outright. A total alone would have started another diagnosis; the key started the fix.
- **deliberately fires on EVERY oversized persist**, not once per run. A stuck orchestration updates rarely because it is stuck, a healthy one never crosses the line, and a tripwire that under-reports in order to stay quiet is precisely the state this replaces.
- **the sentence not to round up:** this is an INSTRUMENT, not a control. It does not cap, truncate or fail anything, so a run that is going to die at 29 MB still dies — you just find out. It also cannot see growth that never persists, and it says nothing about *why* a payload is large; `WFA-015` is the fix for the one known producer.
- **sources:** `platform/orchestration/state.go` (`collectedDataWarnBytes`, `collectedDataAlarmBytes`, `largestCollectedDataKey`, `reportOversizedCollectedData`, called from `UpdateStateWithVersion`); `platform/orchestration/collected_data_size_tripwire_test.go`; `bugs_open/289` residual (5)
- **relations:** **WFA-015** (the loop fix — that stops the one known producer, this notices the next one) · `bugs_open/289` (the case) · `bugs_open/294` (the sibling blindness: a dead orchestration was counted as ACTIVE by `monitoring.go`, so the other instrument on this class was also reporting corpses as healthy) · `bugs_open/240` (oversized state pins Kafka topics, so size has a second cost beyond the run itself)
- **verify-later:** after the next chassis roll, `kubectl -n ai-persona-system logs -l app=agent-chassis --tail=100000 | grep -c 'collected_data is unusually large'`. **Note the demand control, and note which way it cuts:** once WFA-015 is also live the expected count is **falling toward zero**, so a zero here means "no oversized state persisted", NOT "the tripwire is broken". To prove the tripwire itself works, look for a non-zero in the window BEFORE WFA-015's producer stops, or check `build-dispatch-loop`, which was still legitimately reaching 14 MB when this was written.

### WFA-017 — loop expansion suffixes ANY reference-shaped sibling-output config value (the data-reference allow-list stops being a promise)
- **status:** **LIVE + PROVEN + RATIFIED.** Live on chassis v1.0.1307 (2026-08-17 17:05Z); **owner RULING 2026-08-18 (`RFC_035`): option (1) — shape-based rewriting is the estate's convention and the `dataRefKeys` allow-list is retired as a contract.** Proven end to end with the demand control: expanded plans read `"result!": "handler_result_0"` (the pre-roll binary could not — `result` was never on the allow-list), `field=result` resolver rows **0** (from ~455/day) across 10 runs, 18 completions, **0** spawn-record results, **11/11** loop completions carrying the handler's reply. Council round 1 REJECTED on SCOPE (guardian veto, corr `cba35b35`) and **the owner overrode it on the merits** — same disposition as `bugs_closed/124`/BLD-019: the code stands and **live ≠ approved, so no `Council-Reviewed:` trailer may ever be added on that correlation.** Config counterpart: migrations `448` (12:19:59Z) and `452` (16:28:57Z), both applied and proven; historical repair `455` applied 2026-08-18 (35 rows).
- **status-evidence:** expansion-level tests green (`loop_config_reference_suffixing_test.go`: positives, loop-var/prose/expression negatives, `input_fields`-array exclusion, step-ref-key collision); **2 mutations proven** — deleting the generic pass fails the three suffixing assertions, deleting the shape gate fails the condition assertion (the prose case is guarded in series by `prefixDataReference`'s first-segment check, so the condition assertion is the one that uniquely pins the gate).
- **what:** `prefixConfigStepReferences` (coordinator.go) rewrote sibling-`output_field` references to their iteration-suffixed form only for an **allow-list** of config keys plus `input_mapping`, under a comment demanding every future reference-carrying key be listed. `complete_work_item`'s `result` was not — which is how `bugs_closed/287` (spawn_record) shipped: the un-rewritable `"result": "handler_result"` could only resolve through the whole-tree search, whose deterministic winner is the spawn record. Now a **generic pass** rewrites any OTHER top-level string config value that is *reference-shaped* (`^[A-Za-z_][A-Za-z0-9_]*(\.[A-Za-z0-9_]+)*$`) through the existing `prefixDataReference` (first-segment-match rule unchanged). Step-ref keys keep their step-name pass; `input_mapping` keeps its per-value pass; a `!`-strict key rides automatically (the pass keys on the value). The duplicated `dataRefKeys` loop is deleted.
- **why it is registerable:** it changes what a shared mechanism does for **every** loop sub-workflow (22 matching sites / 7 agents, censused live 2026-08-17 — query in `bugfix_287_spawn_record/RUNBOOK`), and it retires a maintenance contract (the allow-list) that three actions had already silently broken (`complete_work_item`, `mark_maintenance_complete`, and the `commit_sha` key on `site-work-orchestrator`).
- **the sentence not to round up:** suffixing alone fixes NOTHING for a dotless mapping — the resolver's whole-tree search still runs first and outvotes it (`RESOLVER_MAPPING_BYPASSED`). The `!` marker is the closer; this pass is what makes strict resolution read the reply's own iteration-suffixed key instead of leaning on `propagateIterationOutputs`' per-substep refresh. Conditions (`condition` expressions) are deliberately NOT rewritten — one live census row is an `OR` carrying two references, and a whole-string rewrite would half-edit it; conditional resolution stays on its own seam.
- **measured, not asserted (2026-08-17):** all 22 matching sites are read-references — zero literals, zero write-destinations; nested sibling-output strings exist only inside `input_fields` ARRAYS (Strategy-1 field NAMES — pinned untouched by the test); zero nested loops; zero `!`/`?` keys outside `input_mapping`.
- **sources:** `platform/orchestration/coordinator.go` (`prefixConfigStepReferences`, `referenceShapedConfigValue`, `prefixDataReference`); `platform/orchestration/loop_config_reference_suffixing_test.go`; `bugs_closed/287` §9a/§11; `docs/agent_docs/sql_for_agents/448_dispatch_loops_result_goes_strict.sql` + `452_build_dispatch_loop_result_goes_strict_HOLD.sql`
- **relations:** CTS-060 (the `!` marker + the resolver instrument that measured this door per-occurrence); WFA-014 (the park-carry on the same seam); WFA-015 (the other injection-time loop-expansion contract); RFC_029 §10.5–10.6a (86% of its conflict population was this bug; its Phase 2 precondition is gated on these rows zeroing); `bugs_open/248` R1 (the same search, the `asset_id` incident)
- **landmine:** a top-level string config value in a loop substep that happens to EXACTLY name a sibling `output_field` (bare or as the first dotted segment) is now a **reference**, and will be iteration-suffixed at expansion. If you genuinely need such a literal in a loop substep config, it must not collide with a sibling output name — censused zero live cases, but the rule is new.
- **verify-later:** after the chassis roll AND migration 452: `agent_error_log` rows with `error_code LIKE 'RESOLVER_%' AND context->>'field'='result'` for `build-dispatch-loop` go to **zero while the loop has traffic** (demand control: fresh `orchestration_states` rows for the agent in the window), and the `site_work_items` spawn-record census (287 §8) reads 0 spawn-record / >0 own-envelope. `work_item_id`/`current_page` conflict rows are NOT this entry's metric (search noise for already-resolved fields — 287 §11).

### WFA-018 — `config-key-audit --array-producer-conditions`: a condition that dots into a field its producer cannot carry
- **status:** built + tested 2026-08-19; runnable from the tree, and in the chassis image since v1.0.1316 (commit `5315c8a19` ∈ build revision `07eeba4a1`). Council **APPROVED round 2**, corr `aef24a7f-2992-4d4f-a6e0-422cd77fcca3` (one round with migration 490 and WFA-019). **Fleet CLEAN as of 2026-08-19 evening: 193 agents, 147 conditionals, 0 findings** — both known instances repaired by their owning lanes the same day.
- **status-evidence:** 7 tests green, the motivating case pinned as a fixture in BOTH arms (the real pre-490 internal-linker shape must flag; the repaired shape must not). First live run 2026-08-19: **191 agents decoded, 145 conditional steps checked, exactly 1 finding — `internal-linker.check_candidates`** — independently re-deriving `bugs_open/313`'s blast-radius census (7 `.count` conditionals, 6 legit, 1 mismatch).
- **what:** a new mode on the existing audit binary. For every `conditional`/`conditional_branch` step (via `validation.WalkSteps`, top-level AND nested — the `bugs_open/144` class), it takes each dotted left-side path `F.k…` in the string condition (parsed in the runtime's own probe order) and flags it when **every** producer of `output_field: F` in the same agent is a `query_database` whose effective `output_format` is `array` — explicit or defaulted (`database_actions.go` defaults to array). An array carries no named keys, so the path resolves through no strategy and the condition routes on nil for ever. Deliberate skips: numeric indexes (WFA-012 — resolvable), bare fields, fields with no known producer, and fields any object-format or non-`query_database` producer writes — the mode asserts "can never resolve", not "looks odd". Refuses (no JSON, exit 2) over an empty export AND over zero decoded conditionals, so a blind pass cannot read as clean.
- **why it is registerable:** `bugs_open/313` sat fully visible in config for four months — producer shape on one step, consumer path two lines below — while 57 runs completed "successfully" having done nothing. This is the config-time closer for that class, and any lane authoring or reviewing a workflow with a conditional can run it in seconds.
- **sources:** `cmd/config-key-audit/conditionalshape.go` (`findArrayProducerConditions`, `conditionLeftPaths`); `cmd/config-key-audit/conditionalshape_test.go`; `scripts/audit-array-producer-conditions.sh`; `bugs_open/313`
- **relations:** WFA-004/006/007/013 (the sibling modes and the shared decode/traversal); WFA-012 (why numeric indexes are exempt); WFA-019 (the run-time closer for the same class); LCO-009 (the row-cap detector on the same producer action)
- **landmine:** the finding rule is **all-producers-array**, so a field written by one array step and one object step is deliberately NOT flagged — if you split a producer into two variants during a refactor, this audit goes quiet on that field even though one path is still broken. Re-run it against the pre-change export too when splitting producers.
- **verify-later:** after migration 490 applies, a live run must return **0 findings over ≥145 conditionals** (the count line is the demand control — 0 findings over 0 conditionals is a refusal, not a pass). Not wired into any cron — a deliberate follow-up decision, stated in the 313 plan rather than smuggled in (the daily budget cron carries a Python mirror with its own parity contract, WFA-006 landmine).

### WFA-019 — opt-in `fail_on_non_numeric` on `conditional`: a numeric comparison that cannot evaluate fails the step instead of silently routing to `else_step`
- **status:** **LIVE on chassis v1.0.1316** (same day: built, council **APPROVED** round 2 corr `aef24a7f`, shipped). Proven at the artefact 2026-08-19: commit `5315c8a19` is an ancestor of the build revision `07eeba4a1` (`git merge-base --is-ancestor`), and the running pod's binary carries the new error literal — probed on `/proc/1/exe` with BOTH controls (old literal present, fake literal absent); independently probed the same way by the 320 lane before this entry was updated. **Consumers now 2**: `internal-linker.check_candidates` (migration 490) and `meta-description-backfiller.check_has_pages` (their migration 493, set while inert and stated so — armed on the 1316 roll). Original: ~~NOT live; v1.0.1315 predates the commit~~. The config half shipped ahead safely (unknown config keys ignored at execution — `bugs_open/101`).
- **status-evidence:** 7 tests green; the two load-bearing guards **mutation-proven**: flipping the default to `true` fails the frozen-lenient test; disabling the strict left-side arm fails the fail-loud and AND-propagation tests; both reverted, full suites green (`actions`, `config-key-audit`, `validation`).
- **what:** a bool step-config key, read with `datahelpers.GetBoolFieldLoud` (WFA-010 — author-facing key). When set, a numeric comparison (`>`, `>=`, `<`, `<=`) whose left or right side fails `ToFloat64` returns an ERROR naming the condition, the field, the resolved value and its type — the step fails and the workflow surfaces it — instead of `false, nil` and a silent `else_step`. **Default OFF; an absent key preserves the historical behaviour byte for byte** (the standing contract of all 145 live conditional steps, and a test freezes it). Scoped to the numeric arms only: for `==`/`!=`/truthy a nil operand is a legitimate null probe (`target_page.page_id != null`); for a numeric operator nil is always a config-or-data defect, and `bugs_open/313` is its measured cost. The exported `evaluateStringCondition(expr, data, logger)` keeps its lenient 3-arg signature (two external test files call it); `evaluateStringConditionMode` threads the flag through AND/OR recursion. Also registers the action's FIRST `ActionInputSpec` (both names, `ConfigKeys: condition/then_step/else_step/fail_on_non_numeric` — complete per a fleet census of every live conditional, nested included; no `CheckConfig`, no `StrictConfig`).
- **why it is registerable:** every string-form conditional on the platform shares this silent-false arm, and 313 shows what it costs when the left side is structurally unresolvable: four months of runs reading `complete` while the agent's only LLM step never executed. Any step whose numeric routing condition genuinely must evaluate should take this key.
- **scope, argued (WFA-009's precedent, same rulings):** owner 2026-07-29 §1 — no shared guarantee changes (absent key ⇒ identical behaviour); owner 2026-08-02 §2 — new authority on a shared seam as an opt-in field, unsafe default OFF; RFC_022 — opt-in ✓, unsafe side is the default ✓, consumers enumerated by query ✓ (exactly one: `internal-linker.check_candidates`, via 490). Optional-key budget: `conditional` moves from unregistered to 1 declared optional-class key against N=10.
- **sources:** `platform/orchestration/actions/conditional_branch_action.go` (`init()` spec registration, `failOnNonNumeric` read, the two strict arms); `platform/orchestration/actions/conditional_fail_on_non_numeric_test.go`; `docs/agent_docs/sql_for_agents/490_internal_linker_candidates_object_uncapped_fail_loud.sql` (edit 4)
- **relations:** WFA-009 (`extract_fields` `required` — the same shape on the extraction seam); WFA-010 (`GetBoolFieldLoud`, the reader); WFA-018 (the config-time closer); `bugs_open/313`
- **landmine:** the flag does NOT fire on `==`/`!=`/truthy — an unresolvable path there still evaluates as nil silently, by design (null probes are legitimate). If you opt a step in expecting it to catch a typo'd path in an equality condition, it will not; WFA-018 is the check that sees those statically.
- **verify-later:** after the next chassis roll, `check_candidates` runs with the key present: normal runs unchanged (count resolves → routing as before), and the tripwire provable by inducing — flip `output_format` back to `array` on a snapshot copy in a test workflow and the step must FAIL naming `candidate_pages.count`. Until the roll, the key is inert config (verified: unknown keys ignored at execution).

### WFA-020 — `config-key-audit --loop-sitewide-item-keys`: a loop-filed work item whose key is still per-site

- **status:** LIVE 2026-08-19, both halves — the mode (same binary, `cmd/config-key-audit/loopitemkeys.go`) and the daily CronJob (`loop-sitewide-item-key-check`, 07:55 UTC, image pattern per WFA-013's shared-output sibling; in `RELEASE_IMAGES`/`AGENT_DEPLOY_SERVICES`, and the 2026-08-19 v1.0.1316 fleet release built and retagged it unprompted, proving that membership works). Manual in-cluster run: 193 agents scanned, 0 findings, doc_notes row written on the clean run (`source='loop_sitewide_item_key_check'` — a MISSING row means the job did not run).
- **status-evidence:** proven by FIRING, both arms: the committed test fixture is the verbatim pre-migration-493 tool-suggester config (2 findings), and a live export with every suffix stripped reports exactly the 6-step loop-nested census — while the real live fleet reports 0. Registry-pinning test fails loudly if `create_work_item` is renamed or stops declaring `item_key_prefix`/`item_key_suffix_field` (the counted-as-ZERO failure RFC_022's cron walked into).
- **what:** convicts a `create_work_item` step nested inside a `loop` that carries `item_key_prefix` without an HONOURED `item_key_suffix_field` — judged by the action's exact reads, so a suffix declared as `""` or a non-string (silently ignored at execution) is convicted too, flagged `suffix_declared_but_unhonoured`. Acquits top-level steps (site-wide is the intended dedupe), no-prefix steps (`item_key` NULL, outside `idx_swi_dedup`), loops over SITES (`site_id` rooted at the loop variable), and `sub_workflow` under a non-loop parent (never executes). Traversal is `validation.WalkSteps`, one pass, parent recovered from the qualified path — no second descent (`bugs_open/144`). Wrapper: `scripts/audit-loop-sitewide-item-keys.sh [--json]` (refusal read from EMPTY stdout, never exit code — the WFA-013 gotcha).
- **why it is registerable:** the class ("a keyed work item whose KEY is coarser than its FINDING", LANDMINES 2026-08-02) was already documented as a question a session must remember to ask, and recurred anyway — `bugs_open/321`: tool-suggester lost ~72% of its suggestions (40→11) to exactly this, invisibly, because the loop reports success either way and the evidence is split across `llm_call_log` and `site_work_items`. No run of a defective agent can reveal the defect, so the check must be offline and fleet-wide. Any lane adding a loop-filed work item now gets caught the next morning instead of at the next human-noticed loss.
- **documented limitation + its net:** a suffix that resolves but is LOOP-INVARIANT passes the static check. The runtime half (same lane, `Council-Submitted: 43a7a60a`) covers it: `CreateWorkItemAction` Warns when an insert dedupes away INSIDE a loop iteration (`shouldWarnLoopDedup`, truth-table-tested; fires on legitimate cross-run dedups too, distinguished by `suffix_configured` and by whether deduped keys differ within one orchestration). Inert until the chassis image rolls past commit `b1c844abb`.
- **sources:** `cmd/config-key-audit/loopitemkeys.go` + `loopitemkeys_test.go`; `scripts/audit-loop-sitewide-item-keys.sh`; `build/docker/backend/loop-sitewide-item-key-check.dockerfile`; `deployments/kustomize/services/loop-sitewide-item-key-check/`; `docs/agent_docs/sql_for_agents/493_loop_nested_item_key_suffixes.sql` (the config fix that emptied the standing findings); lane `docs024_key_docs_latest/bugfix_321_item_key_collisions/`
- **relations:** WFA-013 (same binary, same refuse-on-empty, same doc_notes convention); WFA-004 (the WalkSteps registration); WFA-018 (sibling static checker over conditions, added the same day by another lane); `bugs_open/321` (the instance); `bugs_open/275`/`319` (upstream fixes whose value this unlocks); the LANDMINES "key coarser than its finding" entry (the prose rule this mechanises); `bugs_open/313`/migration 490 (revives internal-linker, whose collision 493 pre-empted)
- **verify-later:** `./scripts/audit-loop-sitewide-item-keys.sh` on demand; the cron's row daily. Disconfirming arm for the whole lane: a post-493 tool-suggester answer with N suggestions must produce N `add_tool` items (pre-fix baseline on record: 7→1, 2026-08-19 10:25).

### WFA-021 — `await_reconcile_enforce`: a workflow may make the advance decision adopt the awaited_requests TABLE when it disagrees with the map
- **status:** **BUILT, INERT UNTIL THE NEXT CHASSIS ROLL, and OFF everywhere.** Committed 2026-08-21. **No live workflow sets it**, and none should until a captured wedge justifies it — see the hazards below. Submitted to the council gate in the same task (`Council-Submitted:`).
- **what:** a **plan-level** boolean on `models.WorkflowPlan` (`pkg/models/contracts.go`), JSON key `await_reconcile_enforce`, default **false**. With it false — today's behaviour, byte for byte: when the in-memory `AwaitedRequests` map empties, the orchestration advances. With it true, `reconcileAllDoneAgainstTable` (RSH-012) re-adopts any rows `awaited_requests` still shows outstanding into the map, sets the status back to `AWAITING_RESPONSES`, and does **not** advance.
  **Detection does not depend on the flag.** A divergence is logged and written to `agent_error_log` (`error_code='await_divergence'`) either way. Only the DECISION is gated — which is what makes the flag's default honest: with it off, the new code cannot change what runs.
- **why an opt-in field and not a documented rule:** this is new authority on a shared seam (the park/response protocol every `await_response` step crosses), so per the owner ruling of 2026-08-02 §2 it ships as a field with the **unsafe side OFF**, not as a comment. Per the RFC_022 narrowing it is therefore **council-gate scope, not architecture-scope** — opt-in ✓, unsafe default OFF ✓, zero live consumers ✓ — with the caveat that the third condition is **tautological for a newly-named field** and should be stated as such rather than dressed as a measurement.
- **⚠ WFA-013 optional-key budget:** as a **plan-level** key this does **not** enter the budget (which counts `RegisterActionInputSpec` optional keys per action). **If a later change moves it onto an action's config, it does** — and then `cmd/config-key-audit/optional_budget_cron_parity_test.go` applies and the CronJob literal must be kept in step.
- **⚠ two hazards to weigh BEFORE switching it on for any workflow**, both real and both stated in the submission: **(a)** adopting a row means waiting out its timeout, after which the retry driver **replays the request — real side effects re-run**; **(b)** the table must remain a cross-check, never a naive replacement for the map's optimistic-lock CAS, which is what serialises two pods racing to advance. Neither is a reason the mechanism is wrong; both are reasons the default is off.
- **sources:** `pkg/models/contracts.go` (`WorkflowPlan.AwaitReconcileEnforce`); `platform/orchestration/coordinator.go` (`reconcileAllDoneAgainstTable`); `platform/orchestration/state.go` (`OutstandingAwaitedRequests`); `platform/orchestration/await_reconcile_test.go` (the flag-OFF pin is a test in its own right); `343` (silent post-abandonment freeze; lane `docs024_key_docs_latest/bugfix_029_retry_kills_live_child/`).
- **relations:** **RSH-012** is the mechanism this gates and the entry to read first. **WFA-008** is the nearest precedent for an unsafe-default-OFF workflow-level switch. **Consumers, told rather than merely measured (owner ruling 2026-07-29 §3):** every workflow with `await_response` steps — `build-dispatch-loop` named explicitly, as the agent 343 was measured on. What changed about their guarantee: *a park now either persists or visibly defers, never silently neither; a re-registration of an already-answered step name genuinely parks; and a disagreement between the table and the map is now recorded where before it was invisible.* Nothing in their behaviour changes unless they set this key.

### WFA-022 — `config-key-audit --commit-sha-exposure`: the STANDING form of migration 537's apply-time guard

- **status:** **LIVE 2026-08-22, both halves.** The mode (`config-key-audit --commit-sha-exposure`, hand-run against the live fleet: 194 agents walked, the exact 8-handler intersection the SQL guard computes, 0 findings, exit 0; positive control fires — a synthetic unexposed producer exits 1 naming it) and the CronJob `commit-sha-exposure-check` (image `v1.0.1324`, daily **06:45 UTC** in the config-integrity window), **proven in-cluster by a manual `--from=cronjob` run 09:41Z: pod `Succeeded`, container exit code **0** read at the POD (an `ImagePullBackOff` reports here as a Job still RUNNING, never FAILED), same 8/8 output from the shipped acks path, and the `doc_notes` row written (`subject_key='commit-sha-exposure'`, `source='commit-sha-exposure-check'`).** ⚠ A MISSING daily row means the job did not run — never that nothing is wrong.
- **what:** asks daily what 537's guard proved once at apply: is every handler that CAN produce a commit (its OWN `orchestration_states` carry `commit_sha`, 30 days) and IS live in dispatch (`site_work_items.handler_agent`, 7 days) exposing it at the standard path (`complete_workflow` step with `result_mapping.commit_sha`, walked by `validation.WalkSteps` — the one deliberate difference from the guard's top-level-only SQL, per `bugs_open/144`)? A miss means that handler's items silently never record `result.commit_sha` — no error, no row, because the `?` wire's absence IS the contract (bugs_closed/334). Acks file for reasoned exceptions: `architecture_review/commit_sha_exposure_acks.json` (expected empty; the right response to a finding is a 519–540-style migration). Three structural vacuity refusals (empty producers / empty handlers / zero exposing agents fleet-wide) because this lane's fixes silenced every observational control — a blind run exits 2, never prints a clean report.
- **why it is registerable:** the residual was named in 537's own header ("the guard protects the APPLY, not the future") and in the staged_component_build 2026-08-21 handoff §1 item 4; without this entry the next session would re-derive or, worse, trust the apply-time pass indefinitely.
- **sources:** `cmd/config-key-audit/commitshaexposure.go` + `commitshaexposure_test.go` (three mutation proofs RUN, each failing exactly its named test); `scripts/audit-commit-sha-exposure.sh` (hand-run; refusal read from EMPTY stdout, never exit code — the WFA-013 gotcha); `build/docker/backend/commit-sha-exposure-check.dockerfile`; `deployments/kustomize/services/commit-sha-exposure-check/`; `docs/agent_docs/sql_for_agents/537_bdl_mark_complete_declares_commit_sha.sql` (the guard this makes standing); `bugs_closed/334` §9.
- **relations:** WFA-013 / WFA-020 (same binary, same refuse-on-empty, same one-row-per-run doc_notes convention); CTS-060(5) (`--optional-explicit-wires`, the `?` adoption gate — sibling question on the same marker); migrations 519–540 (the standardisation this checks stays whole); `bugs_closed/287` (why `result!` is trustworthy at this slot).
- **verify-later:** `./scripts/audit-commit-sha-exposure.sh` on demand; the cron's daily `doc_notes` row (`subject_key='commit-sha-exposure'`). If the producers/handlers windows ever change in a revision of the guard's question, change BOTH this mode's constants and the wrapper's SQL — they are declared character-for-character shared.

### WFA-023 — `__step_errors`: a per-step failure record any downstream step can consult, so "this step failed" stops being indistinguishable from "this step was skipped"

- **status:** **LIVE ON CHASSIS `v1.0.1337` (2026-08-25), both halves, with its config half applied (mig 588).** ⚠ **NOT YET PROVEN in the discriminating case** — the negative control is discharged (the first post-migration council round reached `complete_approved`, 5 abstained, **0 unreadable**, so repointing all 17 `error_step`s did not break the ordinary path), but the positive arm needs a seat whose call actually ERRORS and **cannot be forced**; see verify-later. Previously: **BOTH HALVES COMMITTED 2026-08-24, INERT UNTIL THE ROLL.** Reader `e521cde3e`, writer `dbd865ee8`, both `Council-Reviewed: 82f07fa6-1c42-46ad-bdf6-1d58892c44a7`. The reader has been live on `v1.0.1334` since that morning and was **inert by design** (fails closed on the key's absence); the writer was blocked for the day by another lane's untracked callee and landed after an owner-approved sweep (`893a12d47`). **NOT YET LIVE and NOT YET PROVEN** — the writer needs the next roll, and the proof is a council round in which one seat errors (see verify-later). ⚠ **Probe the WRITER's own literal, never `__step_errors`**: the reader mentions that key too, so `grep -ac "__step_errors" /proc/1/exe` returns **1** whether or not the writer shipped, and reads as "it landed". The discriminating probe is the writer's cap marker — `grep -ac "step-error record capped at" /proc/1/exe`, which read **0** on v1.0.1334 (writer absent) and must read **>= 1 on EVERY replica** before the config half is applied, with a known-absent control in the same breath. **That gate was met and the migration is APPLIED** — `588_council_seat_transient_costs_one_seat.sql` (renamed off `_HOLD` at the moment of applying, both pathspecs in one commit so no stale twin survives). ⚠ **Its first apply FAILED and rolled back**: the council-reviewed sketch used `UPDATE … FROM LATERAL jsonb_each(ad.default_config …)`, which Postgres refuses because the UPDATE target cannot be referenced from a `LATERAL` in its own `FROM`. Rewritten to a correlated scalar subquery. **A seat reviews a SKETCH, which is not executable — compile-check submitted SQL; this class cannot be caught at the gate.** The **writer** (~20 lines in `routeToErrorStep`) is written, green against HEAD in isolation, and **deliberately NOT COMMITTED**: `platform/orchestration/coordinator.go` in the shared tree carries the `bugs_open/354` lane's uncommitted call to `errorRouteTermination`, whose definition is in an **untracked** file, so a pathspec commit of that file would break HEAD for everyone. Hazard and the two ways out are recorded in `bugs_open/354`. **Nothing writes this key today — do not build a consumer that assumes it is populated.**
- **what:** `routeToErrorStep` has always written `collected_data.__step_error` as a **single key that is OVERWRITTEN** on every routed failure, so a workflow with two failing steps keeps only the last, and no step can ask *"did MY predecessor fail?"*. `__step_errors` is an accumulating **map keyed by failed step name** (`{message, at}` per entry), written alongside — `__step_error` is untouched, so all of its readers are unaffected. **Bounded at 50 entries** with a `__truncated` marker entry, because `routeToErrorStep` is fleet-wide and a loop expanding into many failing iterations produces a distinct step name per iteration; the cap and its marker exist so a capped record can never silently read as "that step did not fail".
- **the question it exists to answer, and why nothing else could:** the council gate's `diagnose_council_decide` must separate a seat the relevance filter SKIPPED (an **abstention** — information, does not gate) from a seat whose call FAILED (an opinion **owed and lost** — `unreadable`, which downgrades an approval to REVISE). Both leave the seat's field absent. Without this record the two are the same observation, and the obvious fix to `bugs_open/243` — repointing each seat's `error_step` at the next seat so one transient stops discarding a whole round — would silently convert a lost seat into a considered non-objection.
- **why a sibling key and not a wider `__step_error` — MEASURED, not preferred `[2026-08-24]`:** the reuse seat objected on exactly this and asked whether `__step_error` could simply *become* the map "with a migration path for its few readers". They are not few: **33** Go references outside tests plus **6** live agent configs read `__step_error`. Changing its shape breaks all 39. Reuse was also checked upstream first — `ProcessingHistory` already accumulates an `error_routed` record per failure, but `ActionParams` (`actions/types.go`) does not carry it, so an action cannot read it, and `collected_data` is persisted and queryable where `ProcessingHistory` at that point is not.
- **⚠ WFA-013 optional-key budget:** does **not** enter it — the budget counts `RegisterActionInputSpec` **optional keys per action**, and this is a coordinator-written `collected_data` key. `diagnose_council_decide`'s input spec is unchanged at three keys. If a later change ever moves it onto an action's config, it does enter, and `cmd/config-key-audit/optional_budget_cron_parity_test.go` applies.
- **scope classification:** normal council gate, not architecture-scope. It adds a reserved key to shared state, which the 2026-07-28 ruling names as architecture-shaped, but the 2026-07-29 narrowing is decisive — **additive and inert**, changing no existing reader and reachable by nothing until a consumer opts in. Approved round 1 on that reading.
- **sources:** `platform/orchestration/actions/diagnose_council_decide_action.go` (`reviewStepFailed` + the `raw == nil` branch — the reader, and the comment at that branch explaining why conflation is unsafe); `platform/orchestration/actions/council_seat_step_failure_test.go` (both arms, **mutation-proven in both directions**: always-false fails the lost-opinion arm, always-true fails the skipped-seat arm); `platform/orchestration/coordinator.go` `routeToErrorStep` (the writer, **held**); `docs/agent_docs/sql_for_agents/588_council_seat_transient_costs_one_seat_HOLD.sql` (the config half, held on the same ordering); `bugs_open/243` (the anthropic-cap slug); lane `docs024_key_docs_latest/bugfix_243_provider_cap_resilience/`.
- **relations:** **MDL-044** is the sibling half of the same bug at the endpoint-health layer. `bugs_open/354` is the lane whose uncommitted `coordinator.go` hunk currently blocks the writer. `bugs_open/076` is the precedent for `ActionParams` gaining a field so an action can reason about its own workflow (`WorkflowSteps`), which is the alternative shape considered and rejected here.
- **verify-later:** when the writer lands, the proof is a council round in which one seat errors: the round must reach a verdict, report that seat under `unreadable` (not `abstained`), and — if the remaining seats would have approved — return **REVISE** naming the lost seat. A round approving with zero unreadable is the negative control. **Until then this entry may not claim the discrimination has ever happened in production.**

### WFA-024 — `config-key-audit --template-input-fields`: a prompt template asking for a variable its step's `input_fields` can never supply
- **status:** **BUILT + RUNNABLE 2026-09-03** (offline CLI + `scripts/audit-template-input-fields.sh`; nothing to roll — it reads live `agent_definitions` and the binary is built from source). `--report` exists, so a CronJob is config-only, but **none is wired**: hand-run today, and this estate's own record is that detection without dispatch decays.
- **what:** `bugs_open/453`'s fix candidate 1. A step renders its `prompt_template` against `ExtractFields(CollectedData, input_fields)` — a SUBSET of the run's data — so a template variable whose ROOT is outside that subset is absent at render, and Go's `text/template` renders a guarded or ranged absent key as NOTHING and an unguarded one as `<no value>`. Step succeeds, prompt looks plausible, a section is silently gone. 453 records four catches of this class, every one found by a fixture somebody happened to write.
  **Three finding kinds, one of which fails the run.** `unreachable_root` (exit 1): `input_data` is NOT among `input_fields`, so config alone decides it and the variable resolves on no row, ever. `conditional_root` (exit 0): `input_data` IS among them, so `ExtractFields` also promotes every key of the runtime `input_data` map to the root — undecidable from config, reported not convicted (`defaultshadow.go`'s `dotted_conditional` precedent). `declared_unread` (exit 0): the reverse direction — an `input_fields` entry no template reads, costing a whole-tree extraction every run.
  **Scope is the ACTION** (`actions.RendersPromptTemplate`), not the presence of a `prompt_template` key, so the key under an action that renders none is inert config rather than a finding. Covers BOTH live template tiers: the step's own, and `agent_definitions.default_config.prompt_template` (tier 2 of `getPromptWithPriority`, **6** live steps as of 2026-09-03).
- **the two new contracts, which are the reusable half:** `datahelpers.TemplateRootsFor` / `TemplateRootForInputField` / `IsSpeciallyHandledInputField` / `AlwaysEnsuredTemplateRoots` / `PromptTemplateFuncs`, and `actions.TemplateRootsAvailableTo` / `TemplateRootsInjectedBy` / `RendersPromptTemplate` / `VisionImageManifestKey`. Anything else asking "what can this step's template see?" calls these rather than re-deriving it. `ExtractFields` and `RenderPromptTemplate` were changed to call the exported forms, so declaration and behaviour are one thing.
- **first live run `[MEASURED 2026-09-03]`:** 202 agents, 1,474 steps, **139** templates, **0** parse failures → 1 `unreachable_root`, 16 `conditional_root`, 19 `declared_unread`. The single conviction is `page-content-writer`'s `process_sections_loop.sub_workflow.generate_content` wanting `{{.research_result}}` — **the same step 453 was filed about**, carrying a DIFFERENT missing root from the one migration 641 fixed by hand a week earlier.
- **⚠ LANDMINE — the injected roots are PER ACTION and are NOT a union.** `execute_llm_prompt` injects `voice_style`/`build_standard` via `injectPlatformBlocks` and NOT the manifest; `execute_vision_prompt` sets `vision_image_manifest` itself and does NOT call `injectPlatformBlocks`. This lane's own first sizing pass used one global set and convicted both live vision steps — **2 false positives in 12**. That is why the sets are read from `actions`, and why a behavioural test drives `injectPlatformBlocks` for real rather than comparing a list to itself.
- **⚠ LANDMINE — a DOTTED `input_fields` entry supplies its LAST segment.** `["a.b"]` makes `{{.b}}` work and `{{.a.b}}` NOT work. Same fact from the other side: `validateTemplateData` split on `parts[0]`, so a dotted entry that extracts SUCCESSFULLY was logged `TEMPLATE DATA VALIDATION FAILED` — ~~open~~ **FIXED 2026-09-03** (it now asks `TemplateRootForInputField`; council `54abc24b` bug_historian MEDIUM; byte-identical for an undotted entry, and `[MEASURED 2026-09-03]` **0 of 1,474** live steps declare a dotted one, so no log the fleet emits today changed). ⚠ **The extractor half is NOT fixed and is not a defect**: `{{.a.b}}` stays dead while `{{.b}}` works, for any dotted entry, for ever.
- **⚠ the export is not interchangeable with the other audit scripts'.** It needs the `agent_prompt_template` projection, and the mode **REFUSES** (exit 2) when no decoded row carries the key — because `jsonb_build_object` emits it as `null` when an agent has none, so "present on zero rows" can only mean the wrong query, and a run blind to tier 2 reports fewer findings and reads as clean.
- **what it does NOT close, stated in its own header:** the seam has three shapes and this closes shape 2. Shape 1 (no `input_fields` at all → randomised recursive search) is carried as context, never convicted. **Shape 3 (root present, SUB-FIELD absent in the row's data → `<no value>`) cannot be closed by any check over config** — the config is correct and the outcome depends on a row; the 437 lane measured it at ~65% of writer prompts, and its remedy is promoting `RenderPromptTemplate`'s existing `<no value>` scan from a `Warn` to a durable row. Unowned.
- **sources:** `bugs_open/453` (filed by `apis_uk_bees_homepage`, corrected by the `bugs_open/437` lane, built by `bugfix_453_template_input_fields_lint`); `cmd/config-key-audit/templateinputfields.go`; `platform/orchestration/{datahelpers,actions}/template_context_contract.go`; `scripts/audit-template-input-fields.sh`.
- **relations:** same offline-fleet-check shape and argument as WFA-018/WFA-020/WFA-022 — no run of the defective config can reveal the defect, so the check must be offline and fleet-wide. Uses WFA-003's `validation.WalkSteps`. The `research_result` finding's damage is currently ZERO because the branch that would supply it has never executed (`research-agent` orchestrations all time: 0) — two dead halves that hide each other, which is exactly the class a guard beats a fixture on.
- **verify-later:** `scripts/audit-template-input-fields.sh` on demand. Disconfirming arm: it must keep reporting the `page-content-writer` / `research_result` conviction until somebody fixes or deletes that block; a run reporting 0 templates refuses outright rather than printing a clean fleet.

### WFA-025 — `ClaimStaleOrchestration` / `ErrTakeoverLost`: judge staleness INSIDE the version CAS, so a takeover is claimed rather than assumed
- **status:** **BUILT + COMMITTED 2026-09-03 (`b55f837ef`), INERT until the next chassis roll** — Go, so nothing changes in production until an image carrying it is built and rolled. Council `Council-Submitted: 3beb3f54-6d51-42fd-969f-78e4ea871659`, verdict pending at the time of writing. **This entry may not claim the mechanism has ever fired in production; see verify-later.**
- **what:** `bugs_open/329`. Both takeover arms of `handleOrchestrationStatus` (`coordinator.go` `case StatusExecutingStep`, `case StatusRunning`) judged an orchestration stuck from the CALLER'S SNAPSHOT — `time.Since(state.LastActivity) > StuckOrchestrationTimeout`, 300 s — and then resumed it with nothing claiming the row. `StateRepository.ClaimStaleOrchestration(ctx, id, expectStatus, staleAfter, claimedBy)` re-judges the FRESH row INSIDE `ExecuteWithOptimisticLocking` and writes only if it is still stale; the write IS the claim, because `UpdateStateWithVersion` stamps `last_activity = now` and `version + 1` unconditionally, so every later taker's fresh read declines with `ErrTakeoverLost`. A lost version race is re-read and RE-JUDGED, never retried blind. Both arms route through the coordinator helper `takeOverStaleOrchestration`, which returns **nil** on a lost claim (the same disposition as the arms' own non-stale branch — an error there would turn a normal outcome into a retry). `ClearExecutingStep` is **removed**; its sole caller was the arm.
- **⚠ THE MECHANISM THE BUG FILE NAMED IS NOT THE DEFECT, and both `bugs_open/329` and `bugs_closed/294` assert the wrong one.** Both say these writes are unversioned — *"ends in `r.UpdateState(...)`, not `UpdateStateWithVersion`"*. **`UpdateState` IS `UpdateStateWithVersion`** (`state.go:883-885`, a one-line delegating wrapper). The version CAS was always there. The defect is a **check-then-act across TWO READS**: the arm judges on the snapshot, and the write that follows does its own fresh `GetState` → mutate → CAS that never re-tests the predicate, so two takers seconds apart both win, each CASing against the version it just read. 329's fix candidate (2) was therefore a **no-op**. Corrected in both files 2026-09-03.
- **⚠ LANDMINE — THE OBVIOUS TEST IS INVERTED, and it shows the BROKEN code passing.** Exactly SIMULTANEOUS takers never double-executed: the loser's CAS fails and the arm returns the error. So a `sync.WaitGroup` start-line test is not merely weak here, it is **backwards**. The disconfirming case is the **SEQUENTIAL interleaving** — taker A wins and refreshes `last_activity`; taker B arrives seconds later still holding a stale snapshot.
- **⚠ LANDMINE — THREE GUARDS SIT IN SERIES AROUND THIS, so a green test on the wrong path means nothing.** Outermost in: (i) the chassis intake serialisation claim (`ClaimSerialisationKey`, keyed on the orchestration_id, **`agent-chassis` only** — `intake.go` refuses the mode when `a.spawned`); (ii) **these arms**; (iii) per-path CASes below, e.g. the work-item claim in `claim_work_item_action.go` on the dispatch path. **A test on the dispatch path passes with this fix REVERTED**, because (iii) absorbs it. The tests call `handleOrchestrationStatus` directly — below (i), above (iii) — and sqlmock rejects any unexpected statement, so the expectation list IS the span's complete DB traffic.
- **why NOT `TakeOverOrchestration`, which is what both `bugs_open/329` and `bugs_closed/294` recommended:** its CAS is `WHERE processing_node = $3` from the **observed** value and deliberately leaves `version`/`last_activity` alone (its own doc: *"bookkeeping, not a state transition"*). Where a row already carries the acting pod's own name, two callers in that pod both match and both report `rowsAffected = 1` — **no exclusion at all**. And sequential takers both win regardless: taker 2 observes taker 1's name, CASes it to itself, and the row is still stale by clock.
- **council invariant, preserved trivially:** `orchestration_states` has TWO guarded-update mechanisms that must never govern the same column (`state_locks_test.go`, corr `4a227ed9`). This adds **no third**: `ClaimStaleOrchestration` contains no `UPDATE` literal of its own — it composes the existing version CAS — and `processing_node` is neither read nor written.
- **what it does NOT close, and the entry says so rather than implying otherwise:** **driver versus taker.** `defaultLocalActionTimeout = 7200 s` (`coordinator.go:1246`) and **nothing refreshes `last_activity` during a local action** — every refresh site is a step transition. A live driver inside a nine-minute council seat is "stuck" by the 300 s clock while behaving correctly, and **a driver holds nothing**, so no claim on the takeover side can exclude it. Post-fix bound per orchestration: **2** (driver + exactly one taker), from unbounded. Closing the rest needs a driver heartbeat — a separate seam, deliberately not carried.
- **sizing, which argues for a SMALLER change and is recorded so nobody inflates it later `[MEASURED 2026-09-03]`:** double-handle census over 24 h — **0** overlapping pairs across 3,044 handlers / 2,911 work items; **0** rows with a repeated step name in one `execution_path` over 7 d; **0** takeover log lines in either chassis pod's reachable window. **This is a correctness gap, not a fire.** What the fix removes is a dependence on backstops that are absent on spawned pods (the MAJORITY of orchestration drivers — `processing_node` over 14 d: `agent-chassis` 3,332 against 4,900+ from `agent-page-rerender` 2,215, `agent-build-dispatch-loop` 660 and 11 more spawned families), were never chosen as this defect's mitigation, and could be refactored away by someone who never knew they were load-bearing.
- **⚠ WFA-013 optional-key budget:** does **not** enter it — no `RegisterActionInputSpec` key is added, and no action config changes. Nothing to keep in step with the CronJob literal.
- **scope classification:** normal council gate, **not** architecture-scope on the 2026-07-29 §1 test — the shared mechanism's guarantee is **narrowed** (an unconditional resume becomes a claimed one), not widened. No opt-in field per 2026-08-02 §2: the fix *removes* authority from a branch and no caller-supplied predicate licenses a widest branch. ⚠ The pre-commit architecture signal fires anyway (exported symbol removed + ossified core site) — expected; the council is the arbiter, not the author.
- **sources:** `platform/orchestration/state.go` (`ClaimStaleOrchestration`, and the deleted `ClearExecutingStep`); `platform/orchestration/coordinator.go` (`ErrTakeoverLost`, `takeOverStaleOrchestration`, both arms); `platform/orchestration/stale_takeover_claim_test.go` (five tests, **watched to FAIL against unfixed HEAD `f9aa97a97`** via `scripts/verify-head-builds.sh --with … --test ./platform/orchestration/`, the failure messages naming the write that must not have happened); `bugs_open/329` + its 2026-09-03 update and corrections; `bugs_closed/294` (the origin of the false premise, corrected in place); lane `docs024_key_docs_latest/bugfix_329_takeover_claim/`.
- **relations:** `bugs_open/075` (why `processing_node` is inert, and why `TakeOverOrchestration` is the only writer that moves it — the reason it is NOT the primitive used here); `bugs_closed/294` + commit `e34d44f26` (which added the RUNNING arm — **not** "migration 465", a migration cannot add Go, and 465 is an ambiguous number); the `chassis_replica_scaling` lane, whose cross-worker safety inventory ("the DB claims, `UpdateStateWithVersion`, component locks, CS-1's guard") should now list this claim; the `dispatch_throughput` lane, who supplied guard (iii) and whose double-handle census is the field meter for that path. **First non-test caller of `ExecuteWithOptimisticLocking`** (0 before this, grep 2026-09-03).
- **verify-later — the entry is not allowed to claim this works until these are read at the artefact.** Post-roll: `STALE_TAKEOVER_CLAIMED` / `STALE_TAKEOVER_LOST` in the chassis logs, and `SELECT count(*) FROM orchestration_states WHERE processing_history @> '[{"action":"stale_takeover_claimed"}]'` — the first durable needle this path has ever had (before it, "have the arms ever fired?" was answerable only inside a live log window). ⚠ **An induced fault must BYPASS the intake claim or it proves nothing**, and that vehicle needs the owner's sign-off. **Negative control:** a fresh row must be taken over by neither arm.
