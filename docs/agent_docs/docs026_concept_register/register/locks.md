# Register — locks

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074)._ (6 consolidated from 18 raw extractions, + LOCK-007 added 2026-07-31 for a
mechanism built after the freeze), across units U01_docs024_numbered_core, U02_docs024_focus_handoff, U05_content_quality_linking, U09_adoption, U10_imagery, U17a_docs019_archive_discussions_and_main, U18_sql_for_agents, U19_sql_tables_components, U23_docs_root_vonc, U25_leopardess_social.

Note on source material: the assigned cluster input file contained this entire category duplicated byte-for-byte exactly twice (a mechanical bucketing artifact, verified via diff). Counts and merges below are computed on the de-duplicated set of 18 real raw blocks. Locks is the most cross-cutting category in this cluster — nearly every raw block turned out to describe some facet of the same handful of underlying mechanisms, so the merge rate here is intentionally the heaviest in the register.

---

### LOCK-001 — Pattern A (locked_at/locked_by) is the canonical lock mechanism; Pattern B (pinned) is dead
*(merged from 3 raw blocks: "Lock patterns A/B, Pattern B is dead" (U01); "Pattern A lock convention" (U19); "Locks — HITL durability across the platform" (U17a) — three restatements of the same foundational fact at different times)*
- **status:** deployed
- **status-evidence:** 041 Phase 2A: "a row is locked if locked_at IS NOT NULL"; independently confirmed 2026-05-19 verification ("Pattern B is unenforced in the current code — treat it as dead") and again in the canonical doc 031_locks(2) ("This doc is the canonical reference for lock semantics")
- **what:** Pattern A — a locked_at timestamp + locked_by identity column pair, with a partial index — is the dominant per-row lock mechanism, applied uniformly across sites, page_components, site_components, assets, and site_plan_directives. Hard locks ('admin', 'admin-removed', 'checkpoint', 'manual' upload) only humans clear; soft locks ('deploy', auditor names, 'audit-pending') agents may clear when a work item references the row. locked_by is a free-text convention, not a CHECK constraint, to allow new identifiers without a migration. Legacy Pattern B — a bare `pinned` boolean on site_specs — was never wired (no reads/writes; every spec write is supersede-then-insert with no guard) and new tables must use Pattern A instead. General lock-transfer rule during rebuilds: only the rewriting agent (e.g. write_site_plan) copies locks onto matching new rows by composite key; locked text beats an LLM rewrite; unmatched locks drop with a log. Locks and snapshots are orthogonal (prevention vs restore) — whether revert respects locks is an open question.
- **sources:** 031_locks(3).md; docs/agent_docs/sql_for_tables/041_assets.sql#Phase2A; docs/agent_docs/sql_for_tables/012_site_components.sql#phase-7a; WM/031_locks(2).md#the-two-patterns-in-use, #lock-transfer-across-rebuilds
- **relations:** Lock semantics hard/soft gate (LOCK-002); Site-level lock (LOCK-003); Timed-expiry/lock-model coherence project (LOCK-004); Adoption faithfulness via timed locks (LOCK-005, the adoption-specific instance of lock transfer)
- **verify-later:** \d site_specs pinned; write_site_plan lock-transfer code; check_component_lock.go IsHard classifier

### LOCK-002 — Lock semantics: hard gate for discovery, soft gate for execution, read-only rerender
*(merged from 2 raw blocks: the general platform doctrine (U01) and its confirmation via the assets-table rollout (U10))*
- **status:** deployed
- **status-evidence:** 013 Phase 1 shipped with four amended discovery checks; independently confirmed via the assets 2A rollout ("2A — assets.locked_at + locked_by delivered 2026-05-09")
- **what:** A lock means "human controls this", not read-only: an edit refreshes locked_at without unlocking; unlocking is a separate deliberate act. Discovery checks skip locked rows entirely (hard gate); execution agents process explicitly-targeted items regardless of lock state (soft gate); rerender reads everything unconditionally. locked_by vocabulary distinguishes human-only-unlock identities (admin/admin-removed/checkpoint) from agent-clearable ones (deploy/manual/auditor names) — no time-based expiry exists in production for this classification. Human uploads/locked assets are excluded from auditor queries and regeneration as a direct consequence.
- **sources:** 013#Three Levels of Lock, #How Agents Behave; 031(3)#rules; STATUS_imagery_2026-05-08.md#Phase-2A; PLAN_imagery_loop_closure.md#2A
- **relations:** Pattern A/B canonical mechanism (LOCK-001); Site-level lock (LOCK-003); growth budget; suppression
- **verify-later:** lock_helpers.go; four discovery checks' filters; check_component_lock.go; the store-asset lock guard `WHERE assets.locked_at IS NULL`

### LOCK-003 — Site-level lock (sites.locked_at)
- **status:** deployed
- **status-evidence:** "Phase 7: Site-level lock — prevents all automated agent activity" (012 tail); scheduled-task pre_query patched to exclude locked sites (020 site-lock section)
- **what:** locked_at/locked_by on the sites table acts as a master switch: when set, no automated agent activity (discovery, dispatch, improvement) touches the site at all. Scheduler pre_queries filter locked sites out of candidate selection, and LoadWorkItemsAction gates on it as well — the strictest of the platform's three lock levels (component, site component, whole site).
- **sources:** docs/agent_docs/sql_for_tables/012_site_components.sql#phase-7; docs/agent_docs/sql_for_tables/020_scheduled_tasks.sql#site-lock
- **relations:** Pattern A/B canonical mechanism (LOCK-001); Lock semantics hard/soft gate (LOCK-002); scheduler pre_query gating
- **verify-later:** all dispatch/discovery entry points honour sites.locked_at

### LOCK-004 — Timed lock-expiry project and lock-model coherence plan
*(merged from 5 raw blocks spanning roughly a year of the same effort: the original design-vs-implementation-gap investigation, the approved policy table, a deferral, a "collapse the model" coherence plan, and the eventual schema migration)*
- **status:** partial
- **status-evidence:** Full arc, most-recent-first: migration 115 (referencing project doc 004 v4 and 031_locks, "approved 2026-05-08") added lock_type + lock_expires_at to all four Pattern-A tables in one transaction — but the migration note is explicit that it is "SCHEMA + BACKFILL only; the Go follow-on lands as separate code changes", and that Go follow-on (an ~11-callsite filter sweep) was still pending at last dated evidence. Before that: a full lock-model coherence plan was written but explicitly marked "PLAN ONLY — NOTHING IN THIS PLAN HAS BEEN APPLIED … held deliberately" (2026-05-19); before that, the project was sequenced as "deferred" behind imagery-loop work (2026-05-08); and the original gap was that lock_type/lock_expires_at columns didn't exist in schema at all despite 004/007 describing them as if landed.
- **what:** The target model, still not fully realised: a single lock pattern everywhere (locked_by identity, lock_type permanent|timed|review, lock_expires_at), one "improvable" predicate (`locked_at IS NULL OR (lock_type='timed' AND lock_expires_at < NOW())`) replacing roughly 11 bare `locked_at IS NULL` callsites, and a single `LockPolicyFor(lockedBy)` policy function (admin/manual/checkpoint = permanent; deploy = timed +30d; auditors = timed +90d; adoption = timed +90d, new). The plan also intends to retire Pattern B (site_specs.pinned, exposed via core-manager pin/unpin HTTP endpoints but functionally dead in chassis code) and the hard/soft `locked_by` string-switch in check_component_lock.go, and to resolve the snapshot×lock interaction (does revert_site_to_snapshot clobber human locks?). A fourth `lock_class` column was considered and dropped as redundant. Migration 115 landed the schema half of this in 2026-06 timeframe; the Go-side predicate/policy-function unification remained outstanding at last dated evidence.
- **sources:** 031_LOCKS_should_locks_expire.md; FOCUS_adoption_faithfulness_via_locks(2).md#policy, #predicate, #implementation-plan; PLAN_lock_coherence.md; old/README.md; STATUS_imagery_2026-05-08.md#Lock-expiry-investigation; 115_locks.sql; 086_visual_design_auditor.sql
- **relations:** Pattern A/B canonical mechanism (LOCK-001); Adoption faithfulness via timed locks (LOCK-005, the "adoption" policy entry this coherence work formalised); Schema enforcement flexible/strict mode (CTS-015, a parallel and unrelated "approval locks the schema" idea in the same doc family, not to be confused with this)
- **verify-later:** the ~11 `locked_at IS NULL` callsites; CheckComponentLock LockType/LockExpiresAt extension; `expired_review_locks` discovery check existence; check_component_lock.go switch; site_specs.pinned column existence

### LOCK-005 — Adoption faithfulness via 90-day timed locks: design, non-functional re-plan gap, and the undeployed fix
*(merged from 5 raw blocks: the design-agreed announcement, the policy-table entry, the page_components-locking-subsystem investigation that independently found the same gap, the superseded origination design, and the written-but-undeployed fix patch)*
- **status:** partial
- **status-evidence:** "Verified landed state (2026-06-05) … 053 schema — applied … 054 partially live (first-plan branch only) … write_site_plan Changes 1-3 — not deployed. Consequence: the 90-day re-plan window is non-functional. The only working faithful↔normal boundary today is the first-plan branch." Independently re-confirmed 10 days later from a different investigation (2026-06-15): "the 90-day RE-PLAN window is non-functional" with lock columns present but observed all-NULL on the pages actually checked.
- **what:** Adopted sites are meant to stay faithful to their source for 90 days before developing normally, enforced as a timed lock rather than a permanent flag (deliberately timed even though user-initiated, so nobody "fixes" it to permanent). Because site_plan_directives are plan-scoped and adoption itself writes no plan, the lock was redesigned to originate at the FIRST write_site_plan after adoption (detected by no-current-plan + pages-exist) rather than at adoption time itself (the original adoption-writes-locked-directives design was superseded once this schema constraint was found). As landed: only this first-plan convergence branch actually works. transferDirectiveLocks copies only locked_at/locked_by (no lock_type/lock_expires_at), so re-plans within the 90-day window rely solely on the LLM's "preserve existing pages" prompt, not on any lock. A three-part fix was fully specified (emit page-scoped preserve directives; lock them adoption/timed/90d on first re-plan; extend transferDirectiveLocks to carry type+expiry and release expired locks) but was never deployed — de-prioritised after the first-plan branch proved sufficient for the faithful first pass. Coexists without contention with separate 30-day deploy locks at component scope.
- **sources:** FOCUS_adoption_faithfulness_via_locks(2).md (whole); FOCUS_adoption_faithfulness_via_locks(5).md#implementation, #how-this-drives; HANDOFF_2026-06-15(2).md#schema-corrections; running_notes_14(26).md#part-14h-14i; old2/write_site_plan_adoption_patch(1).md
- **relations:** Timed lock-expiry / lock-model coherence project (LOCK-004, the general infrastructure this is one policy-table entry of); Pattern A/B canonical mechanism (LOCK-001)
- **verify-later:** 053/054 applied state; write_site_plan first-plan lock branch and transferDirectiveLocks SELECT/UPDATE column lists; whether save_page_sections honours locked_at (open question — locking a tool section as a zero-code clobber mitigation)

### LOCK-008 — Page-component lock guard protects the ROW; the loader merge makes the section LIST honour the same lock (one predicate, one merge, two packages)
*(added 2026-08-15, postdating the extraction freeze — built to close `bugs_open/285` (the section-list-assembly-is-lock-blind case; 285 is an AMBIGUOUS number, refer by slug))*
- **status:** **deployed — LIVE (both rounds, `v1.0.1305`), PROVEN AT THE ARTEFACT, and its bug CLOSED 2026-08-17** (owner's word; case file now `bugs_closed/285_HANDOFF_2026-08-15_section_list_assembly_is_lock_blind_…`).** `LOCKED_MERGE_SKIPPED` and `locked_sections_merged` both PRESENT in `/proc/1/exe` on `agent-chassis-5657f446c7-q7b82` (pods started 2026-08-16 22:07Z), positive control present, `deadbeefcontrolstring` absent. **The merge fired on a natural rebuild** — `page-build-handler` on loancalculator.co.uk `index`, corr `b45d9965`, 2026-08-16 16:09:17Z (under round 1): `locked_merge_count=1`, `locked_sections_merged=["tool-3"]`, proposed list = the plan's 5 names **plus** the locked calculator; `pages.sections` updated to match; the locked row untouched (`updated_at 2026-08-09`, a week earlier); all five unlocked siblings re-inserted at 16:23:23Z (058's own control); **0 `remove` items fleet-wide since the roll — and this time with a demand control, because a locked page DID rebuild**; the served page carries the tool (16 × `tool-loan-repayment-section`, controls both ways). ⚠ The slot name (`tool-3`) is a DB label and appears nowhere in the markup — take the artefact grep's literal from `rendered_html`, never from the slot (WRONG_CALLS 2026-08-17). **ACCEPTANCE COMPLETE 2026-08-17** — the owner-named page went through it on a natural rebuild too: webdesign.uk `contact`, corr `352f5703`, 12:16:15Z — proposed list `[hero, contact-info, chat-input-box]`, `locked_sections_merged=[chat-input-box]`, cache updated 12:20:32Z, locked row untouched (`updated_at` still 2026-08-11), hero+contact-info re-inserted 12:20:08Z, no `remove` item fleet-wide, and the served page carries it (4 x `chat-input-box-section` at `preview.webdesign.uk/contact.html`, control 6, absent-control 0). ⚠ the APEX `webdesign.uk/contact.html` 302s to webdesign.co.uk — 143 bytes, every grep 0, looks exactly like the section is gone; only the positive control separates the two. The `a4cd5dc8` needs_human_review row is now `complete` by a direct status write (no `completed_at`/`claimed_by`/`result`) — the only way it could close, these items being handler-less by design. *(Superseded status, 2026-08-16 15:45Z: "half live, and the live half is UNEXERCISED".)* Council APPROVED round 2 (corr `79f70435`, 15:31Z; round 1 REVISE answered — see below). **Round 1 (`7d9b7334a`) IS RUNNING on `v1.0.1304`** (both chassis replicas, started 10:41Z — after that commit's 10:09Z): binary-probed on `agent-chassis-5d95ddddfd-48lv6`, `locked_sections_merged` PRESENT in `/proc/1/exe` with `deadbeefcontrolstring` ABSENT as the negative control, and `load_page_sections_from_spec` present as the positive; corroborated at the DATA — all 20 `page-build-handler` runs since the roll carry the new `spec_sections.locked_sections_merged` / `locked_merge_count` keys, which no earlier binary could emit. **Round 2 (`57336c127`, 15:10Z) is NOT in it** — `LOCKED_MERGE_SKIPPED` absent from the same probe, as expected — so today a skipped merge is still log-only. ⚠ **The merge itself has not yet FIRED on a real run: all 20 post-roll builds were on pages with no locked rows (`locked_merge_count = 0`), so `locked_sections_merged` has only ever been `[]` in production.** The `remove`-item counter reads 0 since the roll and **that zero carries no information** — 0 locked pages have rebuilt (the demand control is absent; LANDMINES "a post-fix ZERO needs a DEMAND control"). Acceptance (RUNBOOK C5, the owner's five criteria) is still owed and is the filing lane's to run.
- **status-evidence:** `platform/orchestration/datahelpers/locked_page_sections.go` + `_test.go` (merge table-driven, predicate-reuse pin), `platform/orchestration/actions/load_page_sections_from_spec_action_test.go` (first tests for the loader; MUTATION-proven — merge removed → fails alone on `sections`, facts alignment removed → fails alone on `section_facts`), `discovery_checks/check_section_source_drift_test.go` (locked-only difference is not drift; genuine drift still flags). Full `actions`, `datahelpers`, `discovery_checks` suites green against `git archive HEAD` + these files. The helper's SQL was PREPAREd by hand against the live DB and returned exactly webdesign.uk/contact's `chat-input-box` and loancalculator.co.uk's 12 locked calculators. Diagnosis: 090 `d9f97c15` CONFIRMED (the bug file).
- **what:** Two mechanisms, deliberately in `datahelpers` because `actions` → `discovery_checks` → `datahelpers` is the import direction and both outer packages need them. (1) `LoadLockedPageSlots(ctx, db, siteID, pageName)` / `LoadLockedPageSlotsForSite` answer "which rows on this page may automation NOT rewrite?" with `NOT AgentWritableSQLFor("pc.")` — the SAME string the 058 write guard's DELETE and `loadActiveLockedRows` use, negated, never re-typed (LOCK-007's rule; a test refuses a `locked_at IS NOT NULL` re-typing). `build_status <> 'removed'` is a separate MEMBERSHIP condition, stated as such. (2) `MergeLockedPageSlots(list, locked)` — pure — pairs each list entry with at most one locked row using `save_page_sections.matchLockedRow`'s arms in order (slot exact → slot kebab-normalised → component function/name standing in for the identity arm), consume-once, then inserts every unpaired row at its live `position` (clamped to the tail; ascending). `NormalizeComponentFunction` moved down here so both packages normalise with one function; `actions.NormalizeComponentFunction` delegates. **Consumers:** `load_page_sections_from_spec` calls both once a tier has served (NOT when none did — a locked-only list is neither plan nor page and a rebuild on it would delete unlocked siblings), keeps `section_facts` index-aligned, reports `locked_sections_merged`/`locked_merge_count`, and writes the FINAL list to `pages.sections` in ONE jsonb-compared sync (replacing three per-tier syncs whose `sections::text IS DISTINCT FROM $1` guard was ALWAYS true — jsonb text prints `", "`, Go marshals `","` — so every build bumped `pages.updated_at`). `check_section_source_drift` (ENABLED live) views BOTH sides through the merge, or it would have filed one drift item per fixed page (13 fleet-wide the day this shipped). `save_sections_prune_floor` reads `pages.sections` length − locked rows: it becomes exact instead of under-counting. Named readers that re-implement the tier order and STAY lock-blind by design: `page_section_satisfiability`, `ensure_page_section_layout`, `datahelpers/plan_section_counts`, `tool_content_item`, `flag_page_image_rebuild`, `check_sectionless_pages`, `check_literal_markdown` — a page with only locked rows and no plan still reads "no sections" to them, consistent with the loader's own rule.
- **landmine:** **`031_locks.md` L156 says "don't merge locks across multiple tables in a downstream reader" — this is NOT that.** The loader does not answer "is this locked?" from two tables; it answers "what sections does this page HAVE?", and `page_components` is a legitimate source for that question. The lock predicate is consulted once, from one table, through the shared string. Second: **the merge restores MEMBERSHIP, not HISTORY** — a locked row an earlier 058 tail-pass exiled to `len(sections)+1` stays where it now is (measured 2026-08-15: loancalculator `index/tool-3` moved 4→6 and `tool-settlement-calculator/tool-2` 3→5 within two hours of rebuilds, BEFORE this shipped); moving it back is a human edit, or a replan that names the component (then the plan position wins because the merge pairs by function). Third: **an in-list locked section still files an `overwrite` `lock_blocked_change` item per pass** (058's design, LANDMINES "a lock_blocked_change item does NOT mean the copy differed") — this fix stops the `remove` items, it does not make locked sections free.
- **council round 1 (corr `79f70435`, 2026-08-16): REVISE**, gated by `prior_art_librarian` (HIGH: confirm the `matchLockedRow` arm order against the symbol — read verbatim: identity → slot exact → slot kebab, consume-once, `save_page_sections_action.go` `matchLockedRow`; the merge's arms are slot exact → slot kebab → component function/name because the LIST has no ids — the identity arm is what the guard applies at save time on the entry's resolved `component_id`; the function/name arm exists so a plan naming a component the locked row renders is treated as "present" the way the guard's identity arm will treat it, else the merge inserts a second entry and the guard INSERTs a fresh copy beside the locked row — the 189 duplicate). Round-2 changes: (a) a locked-query failure now leaves a DURABLE trace — `agent_error_log` `LOCKED_MERGE_SKIPPED` via `LogActionEntryInheritingProvenance`, not log-only (bug_historian); (b) `save_page_sections`' `loadActiveLockedRows` SQL hoisted to `activeLockedRowsSQL` and a lockstep test pins that the ROW loader and the LIST loader negate the SAME predicate string (reuse_agent/architecture — the two are deliberately separate loaders because they answer for the ROW and for the LIST and differ on membership, but they may never differ on the lock test); (c) regression test: a page with NO locked rows gets exactly ONE jsonb-compared sync and no merge keys (guardian); (d) live consumer census run against `agent_definitions`: `page-build-handler.load_spec_sections` is the only step using the action (guardian); (e) travelling `doc_notes` row for the action, id `b8faa498-8234-4546-a005-6180c19f075d` (tooling_provenance). This entry names the seam's real shape per the 2026-08-11 nested-field ruling: `locked_sections_merged` / `locked_merge_count` (top-level result keys, diagnostic, no workflow step reads them) and the `section_facts` nil-insertion at merged indices (guidelines).
- **residuals named by the approving round (advisory, medium):** (1) `pages.sections` has OTHER writers (apply_gap_plan, apply_adoption_plan, create_blog_posts, deploy_tool, create_report_page, adopt_verbatim — census in save_sections_prune_floor.go's header) that can still write a lock-blind list BETWEEN builds; the loader repairs it on the next build and the drift check tolerates the difference, but a reader of the cache in that window sees the old lie (bug_historian). **MEASURED 2026-08-16, answering that seat's "your census asked who CALLS the action, not who else WRITES the state":** the Go writers of `pages.sections` are `apply_gap_plan` (`:631`, `:912`, + its INSERT), `ensure_page_section_layout` (`:159`, and only when EVERY source is empty — the one case the loader deliberately does not merge either), the loader itself, and seven INSERT-a-new-page paths (`page_role_upsert`, `site_db_actions`, `create_blog_posts`, `adopt_verbatim`, `apply_adoption_plan`, `create_tool_component`, `cmd/webdesignport`) which are **benign by construction — a page being created has no `page_components` rows yet, so there is no locked row to lose**. The sharper question is which `save_page_sections` CALLERS can re-open the class, and over the last 30 days only **two of the six ran at all**: `page-build-handler` (98 runs, all through the loader) and `page-rerender` (625 runs, none through the loader) — and `page-rerender` **cannot** reproduce it, because `rerender_page_sections` composes its proposal from the stored `page_components` rows themselves (`loadStoredSections` → `carryStoredSection`), so a locked row is in its proposal by construction. That is why the fleet's `lock_blocked_change` population is 38 `overwrite` and only 7 `remove`. `page-rebuild`, `pageflow-builder`, `site-work-orchestrator` and `tool-recreation-handler`: **0 runs in 30 days** — `page-rebuild` is the one to re-check if it wakes, since it plans from `current_page.sections` (the cache) with no loader in its chain. (2) The drift check's new loud failure applies fleet-wide via site-discovery-rotation, not only to the 13 pages (guardian) — accepted: the alternative was a silent wave of false items; the failure names its cause. (3) `MergeLockedPageSlots` + `LockedPageSlotsSQL` are a cross-package contract with no enforced call path — a future section-list consumer can still be written lock-blind (architecture); the header/this entry name today's readers, and a source-scan lockstep over readers of `pages.sections` is the mechanical answer if a fourth reader appears.
- **enforcement (RFC_033, owner-ruled 2026-08-17):** the merge is no longer a convention. `platform/orchestration/actions/section_list_reader_coverage_test.go` fails the build when a file reads `site_plan_sections` (FROM or JOIN, comments stripped) and neither calls the merge nor is declared in `lockBlindPlanReaders` **with a reason**. Population measured by running it: **7 readers — 2 merge, 5 declared** (a first pass said "6 — 2 merge, 4 declared"; that was the count before the test's own first run found the fifth declarable reader, and the total was not re-derived — corrected 2026-08-17 after council `02cb2134` flagged the mismatch) (`ensure_page_section_layout`: acts only when every source is empty, the case the loader also declines; `page_section_satisfiability`: predicts the loader so it must mirror the loader's refusal; `reconcile_site_plan`: plan-version-to-plan-version restamp, a live row is in neither version; `datahelpers/plan_section_counts`: a dedup ALLOWANCE whose victims already exclude locked rows; plus `check_sectionless_pages`, which reads the plan for the SIBLING's layout). **Cache readers are deliberately OUT of scope** — `pages.sections` now carries the merged list, so they get locked rows for free; that scoping is only sound while the loader keeps writing the merged list, and nothing notices if that write is removed (named in the test header). Anti-vacuity: fails on zero readers, and pins the two merge-callers BY NAME so deleting the merge fails here rather than leaving zero offenders. **Mutation-proven on both arms with mutations that COMPILE.** ⚠ It found a reader the author's own census had missed, on its first run — the hand census grepped `FROM`, the file uses `JOIN`. Council `02cb2134`: round 1 REVISE (editquality gating — the stale-exemption test was not shown as an edit, and the population figure said 6 while the map listed 5), **round 2 APPROVED 2026-08-17 18:3xZ, `decided_by: all reviewers approve`, 10 seats, 0 objections.**
- **open review question — MEASURED 2026-08-17, and the measurement argues for LEAVING IT.** The cost of merging a locked section into the list is that the pipeline now regenerates it and the guard then discards the copy. Measured: **26 locked rows — 14 tool-level carry ZERO non-static fields** (template render, no LLM, effectively free) **and 12 section-level carry 49 non-static fields between them**, so those DO pay an LLM call per rebuild that is thrown away. But the pages holding them rebuild **4 times in 30 days across all 8 of them** (vetcomparison/about 2, ai-agent-orchestration/privacy 1, oufe/privacy 1, the other five 0), so the waste is a handful of calls a month. The noise half is smaller than first stated too: the `overwrite` item is deduped by `item_key` and **is not refreshed** — 41 rows sit in `needs_human_review`, and **0 were touched since the fix rolled**, including on the two proven rebuilds. **So the recommendation is NOT to build the carry-stored-row shortcut**: it would buy a few LLM calls a month and a branch in the build path. **OWNER RULING 2026-08-17: "Don't build it."** So this is DECIDED, not open. **Revisit on a trigger, not a schedule** — a locked LLM-field section landing on a frequently-rebuilt page (re-run the census query in the lane RUNBOOK). The 41 stale rows are a separate, one-off queue-hygiene question for the owner.
- **superseded question (kept for the trail):** should `plan_sections` mark a merged locked section as "carry stored row" so the writer/render loop spends nothing on it and save receives a `carryStoredSection`-shaped entry (as the re-render path already does)? Cost today is one template render per locked section per pass (all 26 live locks are `static`-field components — no LLM spend measured); the benefit would be silence, not correctness. Left for the owner.
- **sources:** `platform/orchestration/datahelpers/locked_page_sections.go`; `platform/orchestration/actions/load_page_sections_from_spec_action.go` (§5a/5b); `platform/orchestration/actions/discovery_checks/check_section_source_drift.go`; `bugs_open/285_HANDOFF_2026-08-15_section_list_assembly_is_lock_blind_…`; `docs/agent_docs/docs024_key_docs_latest/bugfix_285_lock_blind_section_list/`
- **relations:** the 058 write guard (`bugs_closed/058`; `pageComponentAgentWritableSQL` → `datahelpers.AgentWritableSQLFor`, IMP-052) — the ROW half this completes; LOCK-007 (the derived-predicate rule this copies); PLAN-029/PLAN-038 (the loader's tier order and `pages.sections` as materialised cache — the cache now carries the merged list); PBP-011 (DELETE+INSERT persistence), PBP-035 (`stored_slot_name` — why the merged name is the row's slot_name); `bugs_open/189` (the duplication seam the pairing arms mirror); `bugs_open/282` (validate's resolver — NOT on this path; plan_sections resolves the merged slot by identity, Path 0); LOCK-005 / LOCK-006 `verify-later` "whether save_page_sections lock-honouring was ever resolved" → resolved for the ROW by 058 (2026-07-26) and for the LIST here.
- **verify-later:** post-roll acceptance per the bug file's five owner criteria (RUNBOOK C5 in the lane dir); the loancalculator `remove`-blocked items must stop being filed on subsequent rebuilds; `section_source_drift` must file nothing for the 13 pages.

### LOCK-007 — Shared asset-lock guard: one predicate for every writer that replaces a site asset
*(added 2026-07-31, postdating the extraction freeze — the mechanism built to close `bugs_open/143`)*
- **status:** deployed (LIVE on `v1.0.1218` 2026-07-31, pod-verified on both chassis replicas)
- **status-evidence:** `platform/orchestration/actions/asset_lock_guard.go` + `asset_lock_guard_test.go`, consumers `derive_card_asset`, `derive_brand_head_assets` (read side) and those two plus `StoreAssetAction` (write side). Council gate **APPROVED round 1**, corr `b5ff41f1-84bc-477b-a881-83e3d2e8a805` (12 seats, no high-severity). Live-verified at the pod, not the tag (`bugs_open/153`): both replicas of `v1.0.1218` return 1 for the new `"provenance write SUPPRESSED by the asset lock"` symbol and 3 for the pre-existing positive control in the same exec. The query was PREPAREd and EXECUTEd against the live schema on site `4851f6fc-71cf-4160-a270-e03d6d3e0732`, returning exactly its 2 real locks and correctly omitting an unlocked card key. Full `platform/orchestration/actions` suite green.
- **what:** `lockedAssetKeys(ctx, db, siteID, keys...) → assetLockSet` is the one place that answers "may automation overwrite this asset?" for any writer that replaces an asset — its DB row, its object in storage, or the file the row points at in a site repo. The write predicate is `assetAgentWritableSQL(alias)` and the read predicate is DERIVED from it (`assetLockedSQL = NOT(writable)`), never re-typed, because the defect being closed was a duplicated classifier where one copy sat at the wrong point in the sequence. The returned set carries `locked_by`/`lock_type`/`locked_at` and a `Describe(key)` renderer, so a refusal can name what an operator has to clear — live `locked_by` values are free-text sentences, so they are reported verbatim and never classified. **Two properties are deliberate and both fail CLOSED:** no `status` filter (`assets.status` is unconstrained text, so conditioning a safety guard on it fails OPEN the day a locked row carries an unenumerated status — this was argued and approved at council on the brand-head fix, corr `bfd73f71` round 1 HIGH), and **not expiry-aware** — bare `locked_at`, unlike the expiry-aware component predicate `pageComponentAgentWritableSQL`. Callers must treat an error as LOCKED. A source-level lockstep test pins the set of files on the `sendGitCommitRequest` path, so a NEW asset producer fails the build until it is deliberately classified in or out of the guarded population.
- **landmine:** **guarding the provenance upsert is NOT guarding the artefact.** `WHERE assets.locked_at IS NULL` on an upsert protects the DB row only; if the git commit runs first, the approved row survives and its file does not — and because the upsert's `sql.Result` is easy to discard, the suppressed write looks exactly like success (`derived: true`). The check must precede the commit, and a 0-`RowsAffected` upsert must be reported, not swallowed. Second trap: **`deploy_image_asset` deliberately has no lock check and must not be given one** — it commits bytes the named row already points at, so deploying a locked asset is publication of the approved artefact; guarding its `UPDATE assets SET url … WHERE id = $1` would leave a locked row pointing at an expiring presigned URL.
- **open review question:** should the asset predicate adopt LOCK-004's expiry-aware form? Doing so would WEAKEN three existing guards (`StoreAssetAction`, `ingest_staged_asset`, the brand-head check all test bare `locked_at`), i.e. change what the mechanism guarantees, so it is refused here as a side effect of a bug fix and left to LOCK-004's named Go predicate sweep. Measured 2026-07-31: 5 locked `assets` rows fleet-wide, `lock_expires_at` NULL on all 5 — the two readings are indistinguishable today, which is why the choice must be made on the guarantee and not on the data. **When LOCK-004 lands, `asset_lock_guard.go` is the one line it edits.**
- **sources:** `platform/orchestration/actions/asset_lock_guard.go`; `bugs_open/143_HANDOFF_2026-07-29_derive_card_asset_commits_before_lock_check.md`; `docs/agent_docs/docs024_key_docs_latest/bugfix_143_asset_lock_before_commit/`; commits `e9e345464` + `a22010eaa` (the brand-head precedent this generalises)
- **relations:** Timed lock-expiry / lock-model coherence project (LOCK-004 — this file is one of the ~11 `locked_at IS NULL` callsites that sweep must visit, now reduced to one); Pattern A/B canonical mechanism (LOCK-001); Lock semantics hard/soft gate (LOCK-002, whose "the store-asset lock guard" verify-later item this centralises)
- **verify-later:** `ingest_staged_asset` is the one asset writer still guarding by hand, and DELIBERATELY so — it already checks correctly (`:177` pre-check before the upload, `:247` `FOR UPDATE` re-check in-transaction) and its shape is a transaction, not an upsert predicate. Recorded here so that boundary cannot quietly become an undocumented duplicate. `StoreAssetAction` and both `derive_*` writers were swapped onto `assetAgentWritableSQL` in `3dba831cf` — that commit postdates the `v1.0.1218` roll, so its (byte-identical) SQL is not yet in a running binary

### LOCK-006 — auto_lock_on_deploy trigger: assumed-live consequential mechanism, later revealed as a stillborn strict-mode artifact
*(merged from 2 raw blocks describing the SAME trigger at two different dates, with the later evidence directly overturning the earlier assumption — an explicit status-reconciliation case)*
- **status:** abandoned
- **status-evidence:** Earlier evidence (2026-07-01, vonc project): schema captures "trigger_auto_lock_on_deploy auto-locks on deploy … lock_type permanent|timed|review" and treats it as operationally significant (rebuilds/re-renders must check lock state), though all rows were observed NULL-locked in that corpus so the behaviour "never actually bit". Later, more specific evidence (2026-07-09, leopardess/social project) directly overturns this: "the strict-mode subsystem it belonged to was stillborn — no Go code reads schema_mode … snapshot columns never created … fired exactly once in the system's history"; the trigger was deliberately dropped via migration 009 on 2026-07-10 with its function body backed up. Per merge instructions, the later, more specific, dated finding governs the final status.
- **what:** page_components carries locked_at/lock_type/locked_by with a BEFORE UPDATE trigger intended to auto-lock components on deploy as part of a schema_mode='strict' governance subsystem. In practice it was never functional as designed: save_page_sections INSERTs rows already in the deployed state, so the trigger's UPDATE condition essentially never fires; its companion snapshot columns (schema_snapshot/content_snapshot) were never created; and nothing downstream ever read the lock or schema_mode. It nearly caused a collision with an unrelated section-editor fix (every edit would have locked its own row) before being identified and removed. schema_mode/strict_mode_trigger columns and orphaned lock/unlock functions were deliberately retained (not dropped) even though the trigger itself was removed.
- **sources:** docs/RUNNING_NOTES_vonc(36).md#2026-07-01-~13:25, #2026-07-01-~13:55; docs/HANDOFF_2026-07-09_vonc_spark_minilobby_trim(2).md#§7; docs/social001_vonc_tiktok_social/minilobby_task/RUNNING_NOTES_minilobby_task.md#2026-07-09-the-dropped-trigger; docs/social001_vonc_tiktok_social/minilobby_task/auto_lock_on_deploy.FUNCTION_BACKUP.sql
- **relations:** Schema enforcement flexible vs strict mode (CTS-015, the same abandoned subsystem, filed in contracts-and-standards); build_status defect (the near-collision this caused)
- **verify-later:** trigger absence on page_components (confirm migration 009 applied); leftover lock/unlock functions; whether save_page_sections lock-honouring was ever resolved
