# Register — work-dispatch

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074). It said **14** and the file held **15**._ (13 consolidated from 18 raw extractions across units U01, U02, U09;
WDS-014 added 2026-07-31, post-freeze, from `bugs_open/154`).
Absorbed categories: new:dispatch-pipeline, new:work-item-system (their raw material
was largely re-derivations of the same dispatch-chain/state-machine/two-strike facts
documented independently by different units; folded in below rather than kept as
separate register files).

### WDS-001 — Work-item state machine (detected → triaged → claimed → complete/failed) + site-exclusion by stuck claim
- **status:** deployed
- **status-evidence:** "Phase 2G + 2H verified end-to-end at scale. Seven hero items … processed through the full chain … without manual intervention" (2026-05-15); re-confirmed with an ownership table and a 2026-05-14 mis-diagnosis lesson in a separate unit's numbered core doc.
- **what:** `detected` is a valid intermediate state: discovery emits at detected; design-audit-agent's `triage_detected_items` step promotes to triaged; dispatch claims only triaged/approved (partial indexes idx_swi_handler/idx_swi_site_pending); handlers mark complete/failed. There is NO automated coupling between discovery and audit — items sit in detected until an audit runs. Admin-created items insert directly at triaged. The most common "won't dispatch" symptom is a single stuck `claimed` item excluding the ENTIRE site from dispatch consideration (see WDS-002's NOT EXISTS clause) — a repeatedly-relearned debugging trap: don't infer writers from readers (indexes show the read path; grep the verb for the writer).
- **sources:** FOCUS_dispatch_diagnostic(4).md#TL;DR, #Evidence-trail; HANDOFF-pipeline-triage-april-2026.md; 016#Work-item-lifecycle, #Site-excluded-from-dispatch
- **relations:** dispatch chain + NOT EXISTS blocker (WDS-002); two-strike rule (WDS-007); silent completion pathology (WDS-004)
- **verify-later:** registry.go triage_detected_items; site_work_items partial indexes; design-audit-agent workflow

### WDS-002 — Dispatch chain (build-pipeline-trigger → find_dispatchable_site → build-dispatch-loop) + NOT-EXISTS whole-site claim blocker + one-site-per-tick throughput
- **status:** deployed
- **status-evidence:** "researched in depth this session" (2026-05-15) with the actual selection SQL quoted; independently revisited 2026-06-04: "Lever C [claim watchdog]… effectively already implemented; the remaining actions are… the guardrails (timeout alignment, fairness ORDER BY, git-adapter retry), which are the real remaining dispatch work."
- **what:** The scheduler fires `build-pipeline-trigger`, whose `find_dispatchable_site` step picks ONE site per tick (`DISTINCT ON` with no outer ORDER BY — effectively arbitrary among eligible sites, and lowest-UUID sites can starve others) and spawns `build-dispatch-loop` scoped to it, which loads up to 5 items (pipeline='build') and claims/spawns handlers — throughput cap ~5 items per site per 30s, one site at a time (tools dispatch serially within a site; ~5-min Opus builds mean 11 tools ≈ an hour minimum). `find_dispatchable_site`'s `NOT EXISTS` clause excludes ANY site with an item in status='claimed' — an absolute blocker, not a deprioritiser, by design (prevents racing claims mid-execution) but converting one dead handler into a site-wide stall (observed 47-67 min gaps until the reaper resets the claim). `build-pipeline-trigger` doesn't write orchestration_states, making its decisions untraceable. Levers considered: multi-site decoupling (lower priority — cadence already 30s/8); bounded per-site concurrency (K=2-3, gated on OOM guardrails); claim watchdog (already exists — see the claimed-item-timeout entry in the work-item-integrity register). A scheduler/handler timeout mismatch (300s task vs 900/1200/4200s work) is a latent over-spawn/OOM risk.
- **fairness lever PULLED (2026-08-02, sql_for_agents/284, owner-directed):** the "fairness ORDER BY" named above is now live — `ORDER BY wi.created_at ASC, wi.priority ASC, wi.id ASC LIMIT 1`, DISTINCT ON dropped (redundant under LIMIT 1). Two corrections to this entry's own words: (1) the old order was NOT "effectively arbitrary" — `DISTINCT ON` forces the sort to lead with `site_id`, so it was **deterministically lowest-UUID-first**, and starvation was certain, not possible (measured: gamesdesign.co.uk, 14th of 17 by UUID, held the fleet's oldest eligible item at 3d10h while 33 claims went to 5 lower-UUID sites in 45 min); (2) `priority` never influenced WHICH site won — it picked each site's representative row and was projected away, so cross-site priority was structurally unreachable, and it remains deliberately unimplemented (any priority-major order recreates starvation keyed on priority; an aging scheme needs an owner-agreed scale). ~~New selector is starvation-free by construction: a new item's `created_at` ≥ any waiter's, so bounded wait. Known bounded bias: within-site claiming is priority-major (`load_work_item_actions.go` ORDER BY priority, created_at), so a site can be re-picked while its oldest low-priority item drains — bounded by ceil(backlog/5) batches.~~ **CORRECTED 2026-08-26 (`bugs_open/413`): the bound assumed no better-priority inflow. With inflow, the old worst-priority row never comes within reach of the top-max_items, the site's representative age FREEZES on it, and every younger site waits on fall-through with no bound — measured 16 of 25 eligible sites pinned in one snapshot (webdesign.co.uk's oldest row would load 135th), waits > 11 h, invisible to every aggregate meter. Fix = the ordering-contract bullet below (657).** Evidence + design in `bugfix_154_work_item_routing_columns/NOTES`.
- **head-of-line blocking, FIXED the same day (`sql_for_agents/285`) — and 284 and 285 are only safe TOGETHER:** the selector tested three predicates; `LoadWorkItemsAction` (the loader, inside `build-dispatch-loop`) tests those three **plus two** — `(COALESCE(approval_mode,'auto')='auto' OR status='approved')` and the `depends_on` unresolved-dependency clause. So the selector could hand the loop a site whose only eligible item the loader refused: the loop loaded **0** items, reported `has_items:false`/`rows_dropped:0`, and completed **COMPLETED with no error**, having done nothing — and since no claim was made the site stayed eligible and was picked again for ever. Measured 2026-08-02: **1** blocked row out of 366 selector-eligible items across 17 sites (`93f2a3b7`, robot-hands.com, depending on an item in `needs_human_review` — a state no automation clears) stalled the **whole fleet** twice, 89 min and 68 min, while dispatch loops ran and completed cleanly throughout. **284 converts this from intermittent to permanent**: under lowest-UUID a blocked site held the head only while it sorted lowest; under oldest-waiting-first the key is `created_at`, which only ages, so an unloadable item at the head stays there. 285 gives the selector the loader's two clauses verbatim — strictly narrowing (every site removed is one where the loop would have loaded 0), proven at the artefact by loop `item_count` 0→5/5/1 across three sites in FIFO order with the blocked item left `triaged` as the negative control. ⚠ the loader's dependency subquery is **site-scoped**, so a cross-site `depends_on` can never resolve; 285 copies that deliberately (agree with the loader, don't be independently correct) — fixing it means changing Go and moving both queries together.
- **concurrency lever PULLED — N=2 via a sibling task row (2026-08-24, sql_for_agents/582+583+584, owner-authorised D1-default, dispatch_throughput lane):** "one-site-per-tick" in this entry's title is now "one site per TURN, two turns" — a second `scheduled_tasks` row (`build-pipeline-trigger-2`, same selector/loop/pre_query) gives the dispatch group a second single-flight slot, because each row is its own slot (`loadDueTasks` per-row guard; `max_concurrent` compares against a ROW count and has never bound — dispatch_throughput STARTER §2, strong first-hand verification, loop-unverified: the 090's verdict step died at max_tokens). Per-site serialisation is unchanged (`find_dispatchable_site` NOT-EXISTS + atomic claim; a same-instant same-site pick costs one wasted spawn — induction owed, NOTES). **The enabling trap, defused first:** THREE steps (trigger `notify_scheduler` + `notify_scheduler_idle` + loop `notify_scheduler`) hardcoded the stamped row name — a sibling under that hardcode fires at 300s not 60 and releases the ORIGINAL row early on every completion; 583 parameterises all three (`WHERE name = $1`, `params: ["input_data.task_name"]`, mapping via `call_dispatch.input_mapping`), 582 seeds the key (inert), 584 activates. ⚠ the PLAN named only TWO queries — the idle path was found by occurrence-counting the live config (**3** as of 2026-08-24). ⚠ occurrence-counting is NOT a verified enumeration (architecture seat, round 1) — the count that settles it is the WHOLE-TEXT census over `default_config::text` fleet-wide, which sees sub_workflow/substeps (positive control: 6 configs match the substep-level name `spawn_handler`); run 2026-08-25 post-583: **0** hardcoded stamps anywhere, **0** in `scheduled_tasks.pre_query`, the `$1` form in exactly the 2 edited agents. Re-run that census, not a step walk, if this pattern is copied to another dispatch group. Rollback = `UPDATE scheduled_tasks SET enabled=false WHERE name='build-pipeline-trigger-2'`. Consumers: every site's queue (service becomes 2-at-a-time across sites; FIFO between turns unchanged — clients-first ordering is ruled (D2, 2026-08-21) but NOT yet implemented); anything metering LLM spend (~2× turn rate while backlog exists). Do NOT add rows past N=2 before the adapter await/retry decision and the D4 spend governor — measured failure cliff at 5 concurrent items (chassis lane 07-28).
- **⚠ CORRECTED 2026-08-25 — the mechanism in the bullet above is REFUTED at the artefact; the lever is live and measured safe, but is not what it says.** (i) A `scheduled_tasks` row is NOT a single-flight slot: `cmd/scheduler/main.go` `runTick` calls `stampCompleted` (`SET last_triggered_at = NOW(), last_completed_at = NOW()`) immediately after `fireTrigger` — fire-and-forget since `892a289e9` 2026-03-17 — so the per-row guard, `countInFlight`, `max_concurrent` and `timeout_seconds` never bind for a `fire_message` row, and the agents' `notify_scheduler*` stamps are inert (583 is harmless hygiene, not a fix; the 08-24 "stamps land on ITS OWN row" verification read the SCHEDULER's stamp). Measured 2026-08-25: a row overlaps ITSELF (361 / 322 pairs in 24.5 h), cadence p50 90 s vs run p50 97 s. (ii) What 584 does: a second fire ~1 s after the first, every ~90 s. The selector cannot see a claim that lands p50 17.7 s later, so the pair co-picks ONE site **94%** of the time and interleaves an overlapping batch — **39%** of claim attempts lost, avg 2.6 claimed per loaded loop, ≈ **+10–15%** claims/h (busiest hour 227 vs ~200 single-row ceiling), and TWO handlers concurrently on that site. (iii) So "per-site serialisation is unchanged" above is FALSE — **775** same-site concurrent handler pairs across 27 sites in 24.5 h — and measured harmless: handler fail rate **1.55%** WITH a partner vs **3.85%** without; **0** overlapping handlers on one item in 2,579 (the atomic `claim_work_item` is the safety and it held on all 4,265 attempts; the 41 multi-handler items are sequential retries, `handlers = attempt_count = 3`). (iv) Named plainly (council guidelines seat): 582/583 add **`input_data.task_name`** — seeded on the trigger row, read by the trigger's `notify_scheduler` and `notify_scheduler_idle` `params`, and carried into the loop's `notify_scheduler` `params` via `call_dispatch.input_mapping.task_name`; a future key-deletion migration must handle BOTH agents. (v) **N=3 is NOT another clone** (architecture seat) — and N=2 was not necessary either: the native rate knob is **`interval_seconds`** on ONE row (30 s tick: 60 → 90 s cadence, 30 → 60 s, ≤25 → every tick `[MEASURED 2026-08-25]`), and fires ≥30 s apart steer to DISTINCT sites (p90 time-to-first-claim 24.2 s). Owner decision pending (dispatch_throughput README 2026-08-25, options A–D) on replacing the sibling with an interval change; until then N=2 stays, **provisional**, re-verified with `sql_for_agents/584_dispatch_sibling_C_insert_trigger_2_VERIFY.sql` (6 assertions: parity · identity · 0 hardcoded stamps · liveness · 0 double-handles · **no third sibling** — the clone pattern RAISEs at >2 rows; re-runnable `DO`/`RAISE`, hardened post-approval 2026-08-25: liveness keys on `input_data.task_name`, never `owner_agent_type`). **DEADLINE 2026-09-01: if options A–D are unruled by then, re-present them — council r3 architecture seat: provisional-with-no-deadline becomes permanent by default.** Council: corr `db9b7cbf` **APPROVED r3 2026-08-25** (4 advisories, none high). **RULED B 2026-08-26** ("B as you suggest and we can do C when we have the governor"), **APPLIED same day — migration 637** (08:51Z, rerun-safe guarded UPDATEs, gate-parity pre-flight, DO/RAISE post-check): sibling **DISABLED** (row kept = the rollback path; `637_dispatch_lever_B_interval_not_sibling_ROLLBACK.sql` + lockstep VERIFY re-edit), `build-pipeline-trigger` at **`interval_seconds=30`**. Measured 09:20Z (25 min steady state, meters in RUNBOOK): fire cadence p50 **60 s** (p90 65, 27 fires); 24 loops across **15 distinct sites** (the 94% co-pick is dead); lost claims **12.9%** (was 58–60% trailing-24h under the phase-locked pair) — and of the 9 losses, 3 were ONE item repeatedly refusing `ai_endpoint_unavailable` (endpoint health, not a race) and 6 were mid-batch handovers; avg loaded 3.83 / avg won 2.54 per loop; backlog at read 1,398 triaged / 31 sites. **C (interval < 30) RAISEs in the daily VERIFY (assertion 2/7) until the D4 governor lands** — the gate is mechanical. Council for 637: `Council-Submitted: 69a04e0a-8e45-4f5a-b0bb-285f00c544ee`. (vi) Sibling PARITY trap: 584 byte-copied `pre_query`/interval/topic and nothing links the rows — any `UPDATE scheduled_tasks … WHERE name = 'build-pipeline-trigger'` (e.g. the unapplied `213_dispatch_gate_matches_dispatcher.sql` — renamed `_SUPERSEDED` 2026-09-02, see the fire-gate bullet below) desyncs the sibling silently; agent-side edits (`find_dispatchable_site`, 284/285) reach both rows because they share the agent. LANDMINES 2026-08-25. (vii) No other `-N` sibling idiom exists in `scheduled_tasks` (reuse seat; 100 rows / 50 enabled as of 2026-08-25). Bug: `bugs_open/398`. D3's "timeout moves with concurrency" is moot on this binary — only the batch half of Phase 3 is real.
- **batch lever PREPARED (2026-08-26 evening, `sql_for_agents/658_dispatch_phase3_batch_8_HOLD.sql` + `_ROLLBACK`, owner-sanctioned; NOT YET APPLIED):** Phase 3 = `load_items.max_items` 5→8 + `process_item.max_iterations` 5→8, two integer jsonb writes on the one live `build-dispatch-loop` row, BOTH together (the loop action truncates at max_iterations — `loop_actions.go:197-203` — so either alone is a throughput no-op; verified at the code, harmless in both directions: the loader is a pure SELECT and claims nothing). **D3 ("batch+timeout lockstep") is satisfied VACUOUSLY** — `timeout_seconds` is inert on this binary (bugs_open/398), the timeout half is deliberately not shipped. Honest sizing `[MEASURED 2026-08-26]`: the cap binds (80.3% of 1,588 turns/24h loaded exactly 5; 26/29 sites hold ≥5 eligible, 22/29 hold ≥8 on the FULL loader predicate incl. retry_after) and the raised capacity would be consumed — but expected throughput is **single-digit % (~+7%, overhead amortisation)**: +52% items/turn is cancelled by ~60% longer turns; do not quote PLAN:195's ~1.5× (it assumed the dead timeout half). ⚠ traps encoded in the files: values must be bare jsonb NUMBERS (readers fall back to Go defaults **50**/`load_work_item_actions.go:699` and **20**/`loop_actions.go:52-55` on type mismatch); rollback sets 5 EXPLICITLY, never `#-` key-deletion (deletion = batch 50/20, ten times Phase 3); already-applied refusal runs BEFORE `snapshot_agent` (LANDMINES 2026-08-26 replay-decoy trap — 633's order is backwards). Apply gate: hand-apply ~09:30Z 2026-08-27 only after the 24h post-B read passes; the 413 fix (657, other session) applies ≥12:00Z after it and reads max_items LIVE. Watch post-apply: collected_data ~×1.6 (→ ~1.9 MB avg / ~7.3 MB max vs the 8 MiB warn tripwire, `state.go:891`) and the per-site starvation floor (413 cuts both ways). Council: `Council-Submitted: 95099f95-b32b-4656-bfe1-28f140de3717`.
- **ordering contract CLOSED — the selector may only rank a site by work the loader will TAKE (2026-08-26 evening, `sql_for_agents/657_selector_ranks_sites_by_loadable_work.sql` + `_ROLLBACK` + `_VERIFY`, `bugs_closed/413`; **APPLIED 2026-08-27 13:18:19Z** in the agreed slot, VERIFY green same minute with K=8; **measured PASS** at +2h (worst per-site wait 6–10h → ~68 min) and +3.3d (0 pins, 0 stuck claims, lost claims 3.9% best-ever); 413 CLOSED 2026-09-02; `_HOLD` suffix dropped + record-only'd same day per the bugs_closed/150 lifecycle):** 285 made the selector agree with the loader on ELIGIBILITY; 657 extends the agreement to ORDERING and WINDOW, which is the remaining seam 413 fell through. The selector now ranks sites by `min(created_at)` over each site's top-K eligible rows under the loader's own ordering (`priority ASC, created_at ASC` — `load_work_item_actions.go:789`), K read LIVE from `build-dispatch-loop > load_items > max_items` (the K agreement: 658's header defers its selector lockstep to 657, 657 reads 658's knob live, so neither owes the other an edit; a broken path COALESCEs to K=1, still pin-free, never a dispatch stop). A "pin" — a site winning selection on a row outside its loadable window — is thereby unrepresentable; the within-site meaning of `priority` ("run last") is untouched, and so is the loader (site-work-orchestrator's two un-locked steps unaffected). Divergence proven pre-apply at the live DB, same instant: OLD text picks webdesign.co.uk (pinned, oldest row loads 135th), NEW picks vetcomparison.uk (oldest work a pick would actually drain). Lockstep guards, one per side: Go `load_work_items_ordering_contract_test.go` pins the loader's ORDER BY literal (AST, mutation-proven) and routes its editor to the selector; `657_*_VERIFY` (assertion 2) pins the DB half, plus the md5, the K resolution, and an execution probe — it FAILS BY DESIGN until 657 is applied. ⚠⚠ the lock-exception rule stays spelled TWICE by design (cross-site vs per-site `$1` — `work_items_common.go:851-870`); mirror semantics, never text. What 657 does NOT do: bound the POSITIONAL wait (a young site behind genuinely old backlogs still waits its FIFO turn — that is capacity/policy, owner option per 413; the noted-lane CONTRIB is that case), and it does not serve the pinned rows themselves any sooner (priority 140 still means "run last within the site" — that is its meaning). Acceptance meter: per-site starvation floor (dispatch_throughput RUNBOOK) — no site with eligible work > ~1h unserved while pins exist elsewhere. **Council round 1 (corr ecf2e542) returned REVISE and was right:** the K subquery originally selected the loop row by `updated_at DESC`; the runtime loader selects by `version DESC` (`loadAgentDefinition`, processor.go:371-389) and `updated_at` is degenerate — fixed to mirror the loader verbatim, preflight now refuses duplicate-active shapes and pins the UPDATE by row id. **Pattern-tracking (architecture seat, same round): this is the SECOND instance of cross-agent live-config reads enforced only by written agreement + fallback + test (first: 658→657's knob deferral); a THIRD instance warrants a shared helper/schema contract — count them before adding one.**
- **fire-gate admission ALIGNED — the gate now admits a strict superset of the selector (2026-09-02, `sql_for_agents/688_fire_gate_admits_what_the_selector_admits.sql` + `_ROLLBACK`/`_VERIFY`, `bugs_open/415`; APPLIED 2026-09-02 13:28Z, VERIFY green, trigger fired 6 s post-apply):** 285 aligned selector↔loader ELIGIBILITY, 657 aligned ORDERING/WINDOW; 688 closes the seam's remaining drift, one level up — `scheduled_tasks.pre_query` (BOTH rows in one statement, sibling parity held, ROW_COUNT=2 asserted; 584 VERIFY 1/7 pins parity not text so no lockstep owed) was NARROWER than the selector it gates in three independent ways: `status='triaged'` only (selector admits `('triaged','approved')`), `wi.pipeline='build'` (neither selector nor loader filters on pipeline), and bare `locked_at IS NULL` (selector honours the two-armed lock-exception form; the gate now carries the CROSS-SITE spelling — `work_items_common.go:851-870`, never the per-site `$1` fragment). Consequence closed: an approved-only, non-build, or lock-excepted-only backlog was selector-dispatchable and the trigger never fired to ask — an absence, no error (theoretical at 2026-09-02 volume: 102 eligible rows all triaged/build). Each narrowness proven load-bearing by read-only simulation: for each, a population differing only in that dimension left the OLD gate silent and fires the NEW. **The gate deliberately stays WIDER than the selector** — no `approval_mode`/`depends_on`/busy-skip arms — because a spare fire is one cheap no-op tick (~1.3 ms gate vs ~1.3 s full selector) and narrower is the failure direction; do NOT "fix" it to match the selector or loader. Chain invariant now: **gate ⊇ selector ⊇ loader**. Prior art `213_dispatch_gate_matches_dispatcher.sql` renamed `_SUPERSEDED` the same day (it predates 633's lock arm and 657, and its by-name UPDATE misses the sibling; once tracked it was a live whole-directory `--apply` hazard). Council: corr `5f0cb450` **APPROVED r2 2026-09-02** (r1 REVISE rested on a landmine HEADLINE whose own first line corrects it — answered with run checks, no SQL change either round).
- **sources:** FOCUS_dispatch_diagnostic(4).md#Q3; HANDOFF_2026-04-23(1).md Bug 3; FOCUS_dispatch_throughput_and_claim_watchdog(3).md; CATALOGUE(9)#family-j; sql_for_agents/284 (fairness ORDER BY, 2026-08-02); sql_for_agents/285 (selector↔loader agreement, 2026-08-02); sql_for_agents/657 (+_ROLLBACK/_VERIFY, selector↔loader ordering contract, bugs_open/413, 2026-08-26); platform/orchestration/actions/load_work_items_ordering_contract_test.go (Go half of the 657 lockstep); sql_for_agents/582-584 (sibling concurrency N=2, 2026-08-24); docs024_key_docs_latest/dispatch_throughput/ (PLAN, RESEARCH, NOTES); sql_for_agents/584_dispatch_sibling_C_insert_trigger_2_VERIFY.sql (re-runnable, 2026-08-25); bugs_open/398 (scheduler fire-and-forget, 2026-08-25); cmd/scheduler/main.go stampCompleted (892a289e9); sql_for_agents/637_dispatch_lever_B_interval_not_sibling.sql (+_ROLLBACK, ruling B, 2026-08-26); sql_for_agents/658_dispatch_phase3_batch_8_HOLD.sql (+_ROLLBACK, Phase 3 prepared, 2026-08-26); sql_for_agents/688_fire_gate_admits_what_the_selector_admits.sql (+_ROLLBACK/_VERIFY, gate ⊇ selector, bugs_open/415, applied 2026-09-02)
- **relations:** work-item state machine (WDS-001); build pump and queue immune system (build-pipeline register, BLD-003); evidence-gated claimed-item-timeout reaper (work-item-integrity register)
- **verify-later:** find_dispatchable_site SQL (NOT EXISTS clause); scheduled_tasks build-pipeline-trigger row; handler pod requests/limits

### WDS-003 — pipeline column as soft routing namespace/label
- **status:** partial
- **status-evidence:** "Decision reached (2026-05-15, with user): leave the field as a soft, currently-unused routing label … Not implemented yet"; an earlier doc frames the same column as an already-settled dispatch namespace ("Everything in the initial build must be pipeline='build'").
- **what:** `site_work_items.pipeline` (renamed from `domain` — clash with the website's own domain concept; default 'build') is a coarse label allowing pipeline-specific dispatchers, but only `build-dispatch-loop` exists — 'design' and 'maintenance' items sit dormant. It duplicates what `handler_agent` already implies, with nothing keeping them in sync (a discovery check once emitted pipeline='design' and stalled). Decided: discovery checks write 'build'; loosen the dispatcher to accept any value. As of migration `688` (2026-09-02) NO arm of the dispatch chain filters on `pipeline` — the fire gate (`scheduled_tasks.pre_query`) was the last one, and a non-build backlog now fires the trigger (bugs_open/415). Historical trap: dispatch once passed the namespace to handlers as the site domain; a stale `target_domain` config keyword survives the rename.
- **sources:** FOCUS_dispatch_diagnostic(4).md#Q4; 001(5)#Work-item-pipeline-must-be-"build"; 016 Schema reminders
- **relations:** unfulfilled_imagery_plan check; dispatch chain (WDS-002)
- **verify-later:** current unfulfilled_imagery_plan INSERT; build-dispatch-loop load_items item_pipeline config

### WDS-004 — Silent completion pathology and the positive-evidence rule
- **status:** deployed
- **status-evidence:** "observed and characterised; not yet fixed" (captured 2026-04-19); one mode later "already confirmed fixed" (2026-06-09); the other two modes ran at 66x/47x per week as of 2026-04-20.
- **stage2-verified (2026-07-14):** partial → deployed — All 3 modes now confirmed fixed in code: (1) validate_page_content.go:71 routes failures to needs_human_review (not silent complete); (2) scheduled_tasks 'claimed-item-timeout' pre_query (docs/agent_docs/sql_for_agents/052_build_pipeline_trigger.sql:560-586) resets stale claims to triaged / fails at max_attempts ins...
- **what:** Three modes mark work complete that isn't: reaper auto-completion on lost responses; validate_content failures inconsistently routed to complete instead of needs_human_review; a 40-minute blind reaper marking claim-timeouts complete instead of resetting to triaged. Root flaw: "we're done trying" treated as "the work is done." Fix rule: complete only on explicit success response OR positive DB evidence (page_components rows, build_status='deployed', git commit). Symptoms attempt_count=0-on-success and updated_at<claimed_at belong to the same semantic muddle. This is the work-dispatch-side statement of the same defect family documented in full in the work-item-integrity register's silent-completion entries.
- **sources:** FOCUS_page_build_handler_silent_completion.md; HANDOFF_2026-04-20_error_investigations.md#2, #3; HANDOFF_2026-04-20_composition_deployed_design_stuck.md#C
- **relations:** silent-completion failure family (work-item-integrity register); claim-timeout mechanism (work-item-integrity register); two-strike rule (WDS-007)
- **verify-later:** reaper code paths setting status='complete'; whether the two unresolved modes were fixed

### WDS-005 — Dispatcher response-stall and missing claim/orchestration timeout cleanup
- **status:** partial
- **status-evidence:** "Bug 1 … Blocker for autonomous cascade completion" and "Bug 2 — No claim-timeout / orchestration-timeout cleanup" (2026-04-23); every cascade needed "manual dispatcher pokes."
- **stage2-verified (2026-07-14):** unknown → partial — Bug 2 (no claim/orchestration-timeout cleanup) resolved: coordinator.go:777-800 force-fails orchestrations exceeding 3x workflow timeout / 60min fallback (git blame 8c60e8f46 'timeout rationalisation'), plus the claimed-item-timeout reaper (WII-002) resets stuck claims. Bug 1 root cause (Kafka consumer reconnect fai...
- **what:** `build-dispatch-loop` orchestrations stall at `process_item_iter_N_call_handler` even when the handler response arrived (suspects: Kafka consumer reconnect failure; mark_complete not firing); with no sweeper, claimed items and AWAITING_RESPONSES orchestrations accumulate forever and block sites (compounding the NOT EXISTS blocker, WDS-002). Fix shapes: consumer reconnect detection, periodic claim-release sweeper, force-fail of timed-out orchestrations.
- **sources:** HANDOFF_2026-04-23_dispatch_reliability_and_008_validated(1).md Bugs 1-2
- **relations:** NOT EXISTS blocker (WDS-002); consumer group race; silent completion (WDS-004)
- **verify-later:** any sweeper added after 2026-04-23; kafka client reconnect handling

### WDS-006 — build-pipeline-trigger site targeting via pre_query
- **status:** aspirational
- **status-evidence:** "Bug 3 … Scheduler-driven dispatcher invocations all default to system.internal site_id … Fix shape: Add a pre_query" (2026-04-23).
- **what:** The scheduled dispatcher fires with no site targeting so it lands on `system.internal` and no-ops while real sites wait. Proposed pre_query on the scheduled_tasks row selecting sites with open build items so one dispatcher fires per site.
- **sources:** HANDOFF_2026-04-23(1).md Bug 3
- **relations:** dispatch chain (WDS-002); find_dispatchable_site arbitrariness
- **verify-later:** scheduled_tasks.build-pipeline-trigger pre_query column value

### WDS-007 — Two-strike rule for work items (dedup/anti-churn)
- **status:** deployed
- **status-evidence:** "Two-strike rule — FINAL DECISION … Decided NOT to weaken" (2026-04-23); born-unresolved pile-up pattern noted 2026-04-17; migration 006 "Two-strike dedup [check]. Deployed."
- **what:** `insertWorkItem` marks a new item `unresolved` when 2 prior items with the same item_key ended (complete + failed both count), breaking discover↔fix loops; items born unresolved accumulate, and re-cascades hit strikes from a previous run's completes. The sanctioned fix is item_key cascade_run_id scoping (deferred), not weakening the rule. A centralised `workItemTerminalStatuses` const keeps the dedup index and ON CONFLICT predicates from drifting. `wont_fix` + "superseded by active duplicate" is the dedup system working as designed, not a bug.
- **sources:** HANDOFF_2026-04-23(1).md #Two-strike; 006 issues table; 001(5)#Work-Item-Lifecycle; 016 §9 wont_fix entry
- **relations:** idx_swi_dedup (work-item-integrity register); feasibility-recheck; silent completion pathology (WDS-004)
- **verify-later:** work_items_common.go; whether cascade_run_id scoping landed

### WDS-008 — Discovery auto-triage and scheduled-audit open questions
- **status:** aspirational
- **status-evidence:** "Q1 — discovery emissions auto-triage (still open); Q2 — scheduled audit runs (still open)" (2026-05-15).
- **what:** Should low-risk discovery emissions (e.g. needs_imagery) auto-triage via a per-check `auto_triage_emissions` flag rather than waiting for an audit run? And is design-audit-agent scheduled anywhere, or is triage operator-driven? Both parked; determine before more discovery checks ship.
- **sources:** FOCUS_dispatch_diagnostic(4).md#Q1, #Q2
- **relations:** work-item state machine (WDS-001)
- **verify-later:** scheduled_tasks rows for design-audit-agent; DiscoveryCheck interface

### WDS-009 — Terminal work items: every pipeline ends with assembly + deployment
- **status:** deployed
- **status-evidence:** 001(5) rule with the minimum brochure chain; 004 step 9 sets needs_rerender priority 99.
- **what:** The planner/WriteBuildItems must create terminal items (needs_rerender, priority ~20/99) that re-render site components, assemble pages from page_components + site_components, git commit, and trigger deploy — otherwise the pipeline produces data but no website.
- **sources:** 001(5)#Every-pipeline-must-end-with-assembly; 004 step 9
- **relations:** commit-is-deploy; rerender agents (page-build-pipeline register)
- **verify-later:** WriteBuildItemsAction

### WDS-010 — Unified build & maintenance via site_work_items (single queue, same code)
- **status:** deployed
- **status-evidence:** 002(4): "Build and maintenance are the same process"; live lifecycle steps 3/7/11 "same code."
- **what:** Every piece of work is a `site_work_items` row (source, pipeline, item_type, severity, spec jsonb, handler_agent, status enum incl. needs_human_review, priority, depends_on uuid[], item_key dedup, result with commit_sha). New site = planner-written items; maintenance = discovery-written items; same orchestrator/dispatch/handlers. Cross-domain coordination happens only through the table (side_effect items with parent_item_id), never agent-to-agent calls.
- **sources:** 002(4)#Unified-Build-and-Maintenance, #site_work_items; 003(8)#Cross-Domain-Coordination
- **relations:** dispatch chain (WDS-002); site-work-orchestrator (build-pipeline register, BLD-009); side-effect rules engine (WDS-013)
- **verify-later:** site_work_items schema incl. depends_on, item_key

### WDS-011 — Work-item routing: content rebuild vs re-render (needs_page / page_rerender / needs_rerender / link_resolution_rebuild)
- **status:** deployed
- **status-evidence:** 002(4) routing table, dated with a 2026-06-22 hazard confirmation.
- **what:** Route by item_type, never item_key; the distinction is whether copy is regenerated. `needs_page`/`needs_content_page` → full LLM rebuild via page-build-handler; `page_rerender` (reason image_landed/section_data_resolved) → no-LLM re-resolve + re-render from stored content_data; `needs_rerender` → batch reassembly; `link_resolution_rebuild` is INTENDED links-only but runs the full writer (a documented hazard — see interactive-page clobber in the page-build-pipeline register). `page_rerender` on a NULL-content_data section escalates the page to `needs_page` (backfills content_data).
- **sources:** 002(4)#Work-item-routing; 003(8)#Source-of-truth-principle
- **relations:** work-item routing map (work-item-integrity register); interactive-page clobber (page-build-pipeline register, PBP-012)
- **verify-later:** rerender_page_sections action; flag_page_image_rebuild/reconcile_section_data emitters

### WDS-012 — needs_section_data: resolvable-by-query vs genuinely-human classification
- **status:** partial
- **status-evidence:** 016 §9 (2026-05-27 direction): pages_where_type implemented, pages_under_section named-but-unimplemented at the time; a reconciler direction recorded in FUTURE_section_data_handler.
- **what:** Some `needs_section_data` items are human-only (team, pricing); list/grid sections source from `query.*` and resolve mechanically once pages exist — but an unimplemented query name or pre-active timing defers them anyway. Read `spec.missing[].source` to classify. Direction: implement `pages_under_section` + a lightweight resolver (not an LLM agent) that re-attempts open items via queryresolve, closes via `closeResolvedDataRequest`, and flags re-render. This is the work-item-classification angle of the same mechanism documented architecturally in the site-plan-and-reconciler register (PLAN-009).
- **sources:** 016 §9 needs_section_data entry; 002(4) reconcile_section_data → page_rerender emitters
- **relations:** section-data deferral + reconciler (site-plan-and-reconciler register, PLAN-009); input schema v2
- **verify-later:** queryresolve vocabulary today

### WDS-013 — Side-effect rules engine (deterministic follow-on items)
- **status:** partial
- **status-evidence:** A Phase-1 table of triggers exists; some pieces are live (side_effect source appears in the 002/003 contracts), but a full Go rules engine is unconfirmed.
- **what:** After each handler completes, deterministic Go rules (not LLM) are meant to emit follow-ons: new page → needs_nav_update + needs_sitemap; deletion → redirects; CSS change → needs_rerender; milestone item types → needs_snapshot.
- **sources:** P1#Side-effects; 003(8)#Cross-Domain-Coordination
- **relations:** unified build & maintenance (WDS-010); cross-domain coordination; snapshot triggers
- **verify-later:** rules engine implementation

### WDS-014 — `current_item` exposes the first-class routing columns (`component_id`, `entity_id`, `affected_url`), column-first with a `spec.<key>` fallback
- **status:** deployed AND witnessed (2026-08-02): config half (`sql_for_agents/278`) applied 2026-07-31; first live dispatch of a column-only item observed end-to-end 2026-08-02 09:39–09:41Z — `ee745694` cleared `load_tool`, rewrote `content_components c345a76a` (10,520 chars) and completed with no step error. `bugs_closed/154`. (Original ordering note stands for re-applies: image before config.)
- **status-evidence:** `bugs_open/154` fixed 2026-07-31; census that motivated it, taken the same day: `site_work_items` had **21** rows with `component_id` set on the column, **16 of them with no `spec.component_id`**, against **235** spec-only rows — and `agent_definitions` referencing `current_item.component_id` was **0**, i.e. the path did not exist before this. Regression test `load_work_items_routing_fields_test.go`, induced-fault proven.
- **what:** `site_work_items` carries `component_id`, `entity_id` and `affected_url` as real columns, but `LoadWorkItemsAction` only ever selected `page_id` among them — so the only path a dispatcher could reference was `current_item.spec.<key>`, a copy the creating agent had to remember to duplicate into the `spec` JSONB. An item populated on the column and not the blob was **structurally undispatchable**: `build-dispatch-loop`'s optional `"component_id?"` mapping skipped silently (`ResolveInputMapping` drops an unresolved `?` path) and the handler's first `query_database` died on a nil param. That is what killed every `tool-auditor`-raised `improve_tool` item at `tool-improver`'s `load_tool` step while three other creators' items ran clean — **the creator that used the schema properly was the one whose items could not be routed.** The three columns are now exposed top-level on the item map by `setRoutingField`, resolved **column first, then `spec.<key>`**, so ONE dispatcher path is correct for both populations. The coalesce has to live in Go: `input_mapping` resolves exactly one source path per destination and has no coalesce syntax.
- **landmine — TWO, and both look like tidy-ups:** (1) **Do NOT backfill the resolved value into `spec`.** It is the obvious simplification (it needs no config change at all) and it is unsafe: `rerender-pages` reads `input_data.spec.component_id`, and `create_rerender_items_action.go:219` gates `scoped := (reason == "section_data_resolved" || reason == "image_landed") && componentIDStr != ""` on it — so a write into `spec` can silently flip a site-wide rerender into a component-scoped one. `TestSetRoutingField_NeverMutatesSpec` exists to fail if someone does. (2) **Do NOT extend the spec fallback to `page_id` for symmetry.** It is already exposed, so there is no bug to close, and **218** rows have a NULL column with a `spec.page_id` — every one would newly gain `current_item.page_id`, widening what reaches those handlers without editing them. Also: the key is deliberately left **ABSENT** rather than set to `""` when neither source has a value, because an optional mapping path that *resolves* is forwarded while one that is *missing* is skipped, and handlers gate on presence.
- **scope, NARROWED 2026-07-31 after the council's `bug_historian` seat — read this before quoting the entry above:** the Go half exposes **three** columns; the migration rewires **one** mapping. So the class is closed for `component_id` and **not** for its siblings, and my submission's "close the door on the class" was broader than what shipped. Measured the same day: `entity_id` column set on **0** rows, read by exactly one agent (`asset-deployer`, via `input_data.spec.entity_id` — through the `spec` passthrough, **not** a dispatcher mapping, so the Go coalesce cannot reach it); `affected_url` column set on 0 rows and read by nothing. Nothing is broken through them today because there is no failing population, but **a future creator that writes `entity_id` on the COLUMN hits the identical bug.** Deliberately not pre-fixed: it would be a two-part speculative change (add an `entity_id?` mapping AND repoint `asset-deployer`) against zero failing rows. When the first such row appears, that is the fix.
- **open review question:** whether the `page_id` asymmetry should be closed (and the 218 rows' handlers re-checked), or whether `spec.<key>` copies should be deprecated in favour of the columns fleet-wide. Neither is answered here; this change only makes the columns reachable. The council's `architecture` seat additionally noted (medium, not blocking) that **no RFC describes the `current_item` map as a contract** — its key surface and resolution semantics are undocumented, which is why a change like this has no obvious place to be reviewed against.
- **sources:** `bugs_open/154` → `bugs_closed/`; `docs024_key_docs_latest/bugfix_154_work_item_routing_columns/`; `platform/orchestration/actions/load_work_item_actions.go` (`setRoutingField`, `LoadWorkItemsAction`)
- **relations:** dispatch chain (WDS-002); work-item state machine (WDS-001); `input_mapping` optional-`?` semantics (`platform/orchestration/input_contracts/input_mapping.go`)
- **verify-later:** whether any later creator writes `entity_id`/`affected_url` on the column (both were 0-row when this shipped, so the fallback path for them is untested against real data)

### WDS-015 — `triage_detected_items` reports the SITE's dispatchable state (`site_dispatchable`) beside its own call-scoped `has_items`
- **status:** **DEPLOYED and PROVEN LIVE 2026-08-01** — binary `v1.0.1225` (pod-grepped on both replicas, added string 3 / positive control 1, in the same exec), migration 281 applied and recorded 08:03Z **after** that grep, and the branch induced both ways on one site (below)
- **status-evidence:** `bugs_closed/150`, closed 2026-08-01. **Both arms induced on the SAME site with the same script, one day apart, one variable changed** — the parent's own copy promoted `0` in both runs (identical input to the branch) and the branch went the other way: `911ecdd8` on v1.0.1218 → `complete_clean`, 0 closing rerenders; `21669589` on v1.0.1225 → `site_dispatchable: true / 42`, `current_step: complete`, and the priority-99 `improvement_rerender_vetcomparison.uk` item created at 08:07:31.78Z, the instant the branch fired. Reproduced twice before changing anything: orchestration `30692439` (gamesdesign.co.uk, 2026-07-29 — 67 promoted by the child, parent `promoted: 0`, terminal step `complete_clean`) and orchestration `911ecdd8` (vetcomparison.uk, 2026-07-31, fired as the control for this change — design-audit child `{"promoted": 24, "has_items": true}`, site-review child `{"promoted": 3}`, parent `{"promoted": 0, "has_items": false}`, terminal step `complete_clean`; **27 rows triaged in the run's window and ZERO `improvement_rerender%` items created**). Six regression tests in `triage_detected_items_site_state_test.go`.
- **what:** `triage_detected_items` is a step in **three** live agents — `improvement-loop` (`triage_findings`), `design-audit-agent` (`triage`), `site-review-agent` (`triage`) — and its promotion is unconditional over the site (`site_id` + `status`, no type filter). The improvement loop runs both children first, so the **first** copy takes every row and the parent's copy honestly returns `promoted: 0`. Its `check_has_findings` branched on that (`triage_result.has_items == true`), took `else_step`, and terminated on `complete_clean` — success message *"No issues found — site is clean"* — skipping `insert_rerender_item` → `spawn_dispatch` → `call_dispatch`. The action now also returns **`site_dispatchable`** (bool) and **`site_dispatchable_count`** (int): a count of `site_work_items` in a dispatchable status (`workItemDispatchableStatuses`, `work_items_common.go`) for the target pipeline, **whoever promoted them, in whatever order, including a fourth caller that does not exist yet.** A conditional step should read that; `has_items` answers a different question and is unchanged.
- **landmine — `has_items` is CALL-scoped everywhere, and only here can another caller pre-empt you.** It is a fleet-wide convention meaning "my own result set was non-empty", with three other live consumers reading it correctly about their own loaders (`build-dispatch-loop.check_has_items`, `site-work-orchestrator.check_has_items` and `.check_has_fix_items`). **Do not "fix" it by redefining `has_items` here** — that repairs one branch by making a shared word mean two things. The second landmine is ordering: `site_dispatchable` is emitted by the **binary** while the condition lives in the **DB**, which is live immediately, so applying migration 281 against an older chassis resolves the field to nil → `compareValues(nil,"true")` is false → **every** run takes the clean branch, including the ones that promote. That is strictly worse than the bug. Pinned by `TestConditionOnAPreUpgradeBinaryDoesNotSilentlyInvert` so the rule is a property of the code, not only a sentence in a SQL header.
- **scope, stated because it is narrower than it looks:** the count predicate is deliberately **narrower** than `LoadWorkItemsAction`'s selection query, which also filters `attempt_count`, `approval_mode` and `depends_on`. Those answer *"will the loader return this row on its next tick"*; this answers *"is there unfinished promoted work here"*. Anything they exclude is still work, so the difference can only err toward NOT clean — the safe direction for a branch whose false side terminates the loop. A second false-clean route is **not** closed by this and is recorded in the bug file: `improvement-loop.check_audit_pass_limit` goes straight to `complete_clean` when `get_audit_pass_count(site) >= 3`, so a capped site is told it is clean when it was simply not audited, and its `detected` pile is never promoted at all (latent 2026-07-31: **0 of 25** sites at the limit).
- ~~**open review question:**~~ **ANSWERED — OWNER RULING 2026-08-02 (RFC 006): option (a), one promoter one owner. The two child `triage` steps are GONE** (migration 286 applied + recorded; 288 fixed the `error_step` 286 left dangling). `improvement-loop.triage_findings` is now the sole carrier — asserted, not assumed, inside 286's transaction. The census that decided it: a definition scan for either child across every live agent returns exactly **2** rows, both `improvement-loop`, so there was no other parent to audit; `agent_run_stats` reads 3/3/3 identical counts, so neither child had ever run standalone. **`site_dispatchable` STAYS and stays load-bearing** — with one owner `has_items` would again be correct here, but reverting the branch would trade a robust reading for one whose correctness depends on the fan-out never coming back. The fan-out returning is now what **WFA-006** detects.
- **sources:** `bugs_open/150`; `docs024_key_docs_latest/bugfix_150_improvement_loop_false_clean/`; `platform/orchestration/actions/triage_detect_items_action.go`, `work_items_common.go` (`workItemDispatchableStatuses`, `countDispatchableWorkItems`); `docs/agent_docs/sql_for_agents/281_improvement_loop_branches_on_site_state.sql`
- **relations:** dispatch chain (WDS-002); the `detected` → `triaged` promoter and its missing cadence (`bugs_open/083`); terminal-status vocabulary (`workItemTerminalStatuses`, same file, same lockstep obligation)
- **verify-later:** the post-roll run — `current_step = 'complete'` rather than `complete_clean`, with `parent promoted: 0` and `site_dispatchable: true` in the same `triage_result`, plus the `improvement_rerender%` item the clean branch was skipping

### WDS-016 — `spec.suggestion` is the ONE item-spec steering channel; `spec.content_guidance` is aliased onto it at load time
- **status:** **DEPLOYED and PROVEN LIVE 2026-08-16** on chassis **v1.0.1304** — committed `9a7d23c49` (2026-08-15). Symbol probe on BOTH pods with a control pair (`aliasGuidanceIntoSuggestion` PRESENT · `setRoutingField` PRESENT · a nonexistent sibling ABSENT), because the `build provenance` startup line had rotated out of retained logs. Canary + negative control below. ⚠ The **v1.0.1303** roll of 2026-08-15 evening did NOT carry it (pods started 1h57m before the commit existed) — a roll is not evidence a fix shipped.
- **status-evidence:** `bugs_open/271`. Live census 2026-08-15: `content_rewrite` 56 of 231 rows and `needs_content_page` 34 of 113 carried a brief ONLY under `content_guidance`, which nothing read; no row carried both keys; every other item type (`contrast_failure` 226/226, `needs_design_review` 65, `cta_improvement` 51) uses `suggestion` exclusively. Reader enumeration the same day over every active `agent_definitions` row (`default_config::text LIKE '%spec.suggestion%'`): exactly three — `page-build-handler`, `content-gap-planner`, `css-patch-agent`. Tests `load_work_items_guidance_alias_test.go`, mutation-proven both ways (removing the call site fails only the end-to-end case; removing the precedence check fails the never-overwrite and non-string cases).
- **what:** A work item's prose brief — "state X, remove Y, add Z" — travels one wire: `site_work_items.spec.suggestion` → `LoadWorkItemsAction` parses spec to a map → `build-dispatch-loop` maps `"spec": "current_item.spec"` into the child's `input_data` → `page-build-handler`'s `call_content_writer` maps `"rewrite_guidance?": "input_data.spec.suggestion"` → `page-content-writer`'s prompt renders `{{if .rewrite_guidance}}## Rewrite Guidance (IMPORTANT: incorporate this into the content)`. Four emitters instead wrote **`content_guidance`**, which no reader reads at any layer, so their briefs were discarded before a prompt was built and the rewrite proceeded steered only by `writer_block` and the existing page — reporting `complete`. `aliasGuidanceIntoSuggestion` (same file as `setRoutingField`, same root constraint: `input_mapping` resolves exactly one source path per destination and has no coalesce syntax, so "read either key" is not expressible in config) fills `suggestion` from `content_guidance` when `suggestion` is absent, **in memory only — the DB row is never written**. The four emitters (`apply_gap_plan_action.go`, `tool_content_item.go`, `create_tool_component_action.go`, `deploy_tool_action.go`) now write `suggestion` directly, and a ratchet test bans the dead key returning to this package.
- **landmine — the alias is the ONLY exception to "the spec map is NEVER mutated" (WDS-014), and the licence is what the key DOES, not that the write is additive.** `suggestion` is a **prose** channel: all three live readers render it into a prompt and none gates scope, routing or any branch. That is precisely what `component_id` did do (`create_rerender_items` gates rerender SCOPE on its presence), which is why backfilling THAT key is still forbidden and `TestSetRoutingField_NeverMutatesSpec` still guards it. **A future key that fails the prose test belongs under the original rule.** Second landmine: the alias never materialises `""` — an optional mapping path that RESOLVES to empty is forwarded as an empty string where a MISSING one is skipped, so filling an absent key with `""` would turn "not supplied" into "supplied as empty" fleet-wide.
- **scope, stated because it is narrower than the fix sounds:** the ratchet is a source scan of this package, so it cannot see a spec built in workflow CONFIG (the generic `create_work_item` action takes its spec from step config) or by operator SQL — the same residual bug 279's minting ratchet records. Those producers are covered by the ALIAS, not the ratchet, which is why the read-side half is the one that closes the door and the emitter renames are hygiene.
- **residual, and do NOT read this entry as closing the class** (council `b24608e8`, `bug_historian` seat, accepted): the fix aliases the ONE known dead spelling. The generic root — `input_mapping` resolves exactly one source path per destination and has no coalesce, so **any** mismatched key vanishes with no error, no warning and no failed work item — is untouched here, as it was by WDS-014 before it. **A fifth producer choosing a THIRD spelling reproduces `bugs_open/271` exactly**, and only the ratchet would notice, only for this one string, and only in Go. Closing the class needs a declared vocabulary for item-spec keys, which does not exist — the same missing registry `bugs_open/279` records as its own residual.
- **what did NOT change:** `apply_gap_plan_action.go:178` still reads `content_guidance` **from the gap PLAN** — that is `content-gap-planner`'s LLM output contract, seeded in its prompt as `add_to_page.content_guidance`, and renaming the item-spec key does not touch it. The ratchet's own fixture asserts the pattern does not convict that read.
- **on why no opt-in switch** (the 2026-08-02 §2 default-OFF ruling): the loader dispatches only `status IN ('triaged','approved')` and all 90 guidance-only rows were terminal or parked (62 complete, 14 needs_human_review, 11 failed, 2 cancelled, 1 wont_fix — **zero** dispatchable), so nothing in flight changes on roll day. A default-OFF switch would have re-created the dead channel behind a flag.
- **sources:** `bugs_open/271`; `docs024_key_docs_latest/bugfix_271_content_guidance/`; `platform/orchestration/actions/load_work_item_actions.go` (`aliasGuidanceIntoSuggestion`, `LoadWorkItemsAction`)
- **relations:** routing-column coalesce and the never-mutate rule this narrows (WDS-014); dispatch chain (WDS-002); the false-affordance class — a config key written and never read (`bugs_closed/264`, `bugs_open/279`'s `work_item_type`)
- ~~**verify-later:**~~ **DONE 2026-08-16.** Canary on `pool-energy-utilities.internal` (unserved pool site), brief in the DEAD spelling only, sentinel `heliotrope kettledrum`, baseline 0: **2 of 2** writer prompts carried the sentinel AND `## Rewrite Guidance`, **2** stored `page_components` carried the phrase, item `complete`. Negative control (same site, neither key): **0 of 2** prompts gained the heading — so the heading is not unconditional. All 25 non-terminal historical rows re-triaged at the owner's instruction (`bugs_closed/271` §10).
- **landmine — `llm_call_log.work_item_id` is NULL on child-agent calls**, measured 2026-08-16: a writer call spawned under a `page-build-handler` item carries no `work_item_id`, so that column cannot scope a canary however obvious it looks. Scope by time window plus a CONTENT discriminator (the prompt's own opening line names the page) — and before filing a negative control, ask what else you are about to inject into its window: 25 guidance-carrying items re-triaged minutes later would have made a window-scoped control read as failing.

### WDS-017 — the promoter applies the CLAIM PATH's own routability test, from one shared renderer, so a flag-only finding is never promoted into the dispatch queue
- **status:** **LIVE and PROVEN IN PRODUCTION; `bugs_closed/284`; and since v1.0.1307 (2026-08-17 evening) the predicate has exactly ONE rendering fleet-wide.** The owner's tie-break on this entry's recorded seat disagreement was **option (a): unify the third copy** — done in `10fc61184`, live in build `a6d1c53c` (OCI label `org.opencontainers.image.revision`, local `RepoDigests` matched to the pods' `imageID`). `discovery_checks.HandlerRegisteredSQL` is now the single definition — it lives in THAT package, not `actions`, because `actions` imports `discovery_checks` and never the reverse, so the import direction chooses the home — and `actions.workItemHandlerRegisteredSQL`, the promoter, the claim path and `remit.go`'s `HandlerStepConfig` all render from it. Confirmed in the binary by the before/after pair: the pre-unification un-aliased `EXISTS(SELECT 1 FROM agent_definitions` is **absent**, the aliased form **present**, and no chassis-compiled source still contains the old one. `cab28cfbe` repaired a comment-attribution defect the merge introduced (the new function landed inside `HandlerStepConfig`'s doc block). **The seat disagreement recorded below is therefore RESOLVED** — `reuse_agent`'s objection was upheld. Previously: **LIVE and PROVEN IN PRODUCTION on `agent-chassis` v1.0.1305, 2026-08-17; `bugs_open/284` CLOSED** (`bugs_closed/284`, commit `238fff826`). Roll ancestry-proven per SERVICE — running digests match the local images, OCI label `revision=6a782274b`, `git merge-base --is-ancestor 7027a2801 6a782274b` exits 0 — and independently needle-probed on both replicas (three of this change's own literals PRESENT, with a long-lived positive control and a nonsense negative control). **EXERCISED, not merely deployed, and with a manufactured demand control:** a single-step `triage_detected_items` dispatch on `leopardessconsulting.co.uk` (36 flag-only `detected` rows, nothing routable, so the promoter could only hold or promote) returned `promoted: 0`, `not_promotable: 36`, `not_promotable_by_type: {"head_essentials_missing": 36}` — corr `a5be3dea-3f2c-490a-9922-22993662bc95`. All 60 damaged rows repaired by migration `442` (counted needles, `RETURNING` postcondition, `result.repair_284` stamped so none looks spontaneously fixed). **The born-dispatchable residual this entry used to carry is now CLOSED too** — migration `443`, CHECK `swi_no_handlerless_promotable` forbidding `handler_agent='' AND status IN ('triaged','approved','claimed')`, added `NOT VALID` then `VALIDATE`d, and INDUCED with two negative controls (handler-less+`triaged` refused; handler-less+`detected` allowed, which is where 77 legitimate flag-only rows live; handler+`triaged` allowed). ⚠ **A note for whoever reads a quiet census here:** `improvement-sweep`, the only SCHEDULED driver of the only carrier, is `enabled=false`, and the promoter running on its own today is the `detected-item-promoter` task created 2026-08-15 which never had the defect — so a clean census is not by itself evidence about this guard. The single-step dispatch above is. Council APPROVED round 2. Previously: **COMMITTED 2026-08-16, NOT YET LIVE** — `7027a2801` (+ `5cb063e33` gofmt), council `c22998e8-41df-4145-a7b9-f132a7c77426`, verdict read: round 1 REVISE (gated by `guardian` on an unquoted equivalence claim; `prior_art_librarian` correctly refuted the per-producer attribution, re-measured on `spec->>'check'` as 9 + 9), round 2 approved with 4 advisories, all answered — incl. a gated REPAIR/ROLLBACK SQL pair and a `doc_notes` row for the deferred work. **⚠ Two seats disagree on the same edit and it is an OWNER call, not done unilaterally:** `reuse_agent` says the unification did too little (`remit.go`'s `HandlerStepConfig` is a third, prose-coupled copy, left unmigrated), `guardian` says it did too much (the universal claim path should not carry a bug lane's refactor). Go, so inert until the next chassis roll; the 60 damaged rows are deliberately **not** repaired until it is (they re-block on the next claim). Do not read this entry as closed — check the binary's own provenance stamp against `7027a2801` before believing it.
- **status-evidence:** `bugs_open/284`. Live census 2026-08-16: **60** rows in `status='blocked'` with `error = 'No handler_agent set — item cannot be routed to any agent'`, across four item_types on 15+ sites (`image_url_404` 40, `capability_gap` 18, `needs_experience_plan` 1, `page_rerender` 1), plus **37** at `detected` with an empty handler queued to join them. Attribution on a value that discriminates: 58 carry `spec.original_pipeline` = `design`/`content`, which only `to_jsonb(pipeline)` can write — the two other writers of that key (`site_admin_handlers.go:HandleApproveWorkItem`, `tool_acceptance_actions.go:routeChromeFailures`) both hardcode `"build"`. Three mutation proofs recorded in `triage_routability_guard_test.go`.
- **what:** The platform files two kinds of finding. A **job** names a `handler_agent` and the dispatch loop routes it. A **flag** names none ON PURPOSE, because nothing on the platform can act on it — a brand palette nothing can repaint (`check_palette_contrast`), a VM nothing can restart (`check_backend_unreachable`), an image reference nothing can repoint (`check_image_url_404`), sibling copy nothing may safely rewrite (`check_content_duplication`). `TriageDetectedItemsAction` promoted BOTH, filtering on neither `item_type` nor `handler_agent`; `ClaimWorkItemAction` then claimed the flag rows and stamped them `blocked`. Two new renderers in `work_items_common.go` state the test once — **`workItemHandlerRegisteredSQL(expr)`** ("this handler is a registered agent", the exact predicate claim runs, now rendered INTO claim) and **`workItemRoutableSQL(alias)`** (that, AND a non-empty handler) — and the promoting UPDATE carries `AND workItemRoutableSQL("wi")`. `countUnroutableDetected` reports what was held back, per item_type, into `not_promotable` / `not_promotable_by_type`, so the filter is never a silent cap.
- **why the predicate is EXACTLY claim's and not the stricter one next door:** the live `detected-item-promoter` scheduled task additionally demands `is_active`, non-snapshot, and ≥1 lifetime `complete` for the `(item_type, handler_agent)` pair. That ratchet is right for an unattended background promoter and wrong here — a brand-new handler's first item would be stranded, since nothing else would promote it either. A promoter stricter than the claim it feeds holds back work nothing rescues; one looser re-creates this bug. So the two are the same function, and the coupling is pinned by a test that fails if either is hand-written back.
- **landmine — `blocked` is not terminal, so the damage is an ABSENCE as well as a wrong status.** `blocked` appears in neither `workItemTerminalStatuses` nor `idx_swi_dedup`'s excluded list, so a wrongly-blocked row **holds its `(site_id, item_key)` dedup slot for ever** and its check's later findings are silently dropped; and `feasibility-recheck` only releases a blocked row whose `handler_agent` matches a registered agent, which the empty string never does. That is why the count above understates: every re-detection since is gone.
- **landmine — a flag-only producer that OMITS `HandlerAgent` is invisible to a value-grep.** `check_image_url_404.go` never writes the field (Go zero-values it) and owns 40 of the 60 rows; a `grep 'HandlerAgent: ""'` census returns a complete-looking list without it. Census the DB, not the source.
- **residual, and this entry does NOT close the class:** 2 of the 60 rows came from a **second path** — hand-inserted by sessions, born in a dispatchable status with no handler (`created_by` = `bugfix-189-verify`, `contrast-front-113`; no `original_pipeline`, no `triaged_at`). A promoter guard cannot see those, and neither can a guard at the shared `writeWorkItem` door, because ~20 call sites use raw `INSERT INTO site_work_items` instead. What closes it is a CHECK constraint making `(handler_agent = '' AND status IN ('triaged','approved','claimed'))` unrepresentable — **which must land AFTER this binary rolls**, because DB config is live immediately while Go is inert, so a constraint arriving first makes the OLD promoter's blanket UPDATE error on any site holding a flag-only `detected` row and breaks `improvement-loop` fleet-wide. 0 rows currently violate it.
- **behaviour change, stated:** a site whose only `detected` findings are flag-only now reports `site_dispatchable=false`, so `improvement-loop.check_has_findings` takes `notify_scheduler_clean` instead of `insert_rerender_item`. Honest — there is no dispatchable work — and it replaces a rerender plus a row that could only be blocked. The branch reads `site_dispatchable` (WDS-015), not `has_items`, so `bugs_closed/150` is not reintroduced.
- **sources:** `bugs_open/284`; `docs024_key_docs_latest/bugfix_284_flag_only_items_promoted/` (PLAN, NOTES, RUNBOOK); `platform/orchestration/actions/work_items_common.go`, `triage_detect_items_action.go`, `claim_work_item_action.go`, `discovery_checks/check_palette_contrast.go`, `discovery_checks/check_content_duplication.go`; `090` run `d1477c1d-bca4-4ac9-806d-da860eb0014a`
- **relations:** the site-scoped promoter output this sits inside (WDS-015); `remit.go`'s `CapabilityGapItem` and the `deferred` convention (`bugs_closed/077`); the `detected` queue's missing cadence and its scheduled promoter (`bugs_open/083`, SCH-006 pattern); terminal-status vocabulary and the dedup lockstep (`workItemTerminalStatuses`)
- **verify-later:** post-roll — the guard live (provenance stamp ⊇ `7027a2801`), a promoter run reporting `not_promotable` on a site with flag-only findings, the 60 rows repaired per type, and then the CHECK constraint applied and induced

### WDS-018 — the shared write door demotes a dispatchable item whose NAMED handler is not a registered agent to born-`blocked`, reusing claim's own predicate
- **status:** **LIVE AND PROVEN IN PRODUCTION on `agent-chassis` v1.0.1307, 2026-08-17; `bugs_open/291` CLOSED** (`bugs_closed/291`). Roll proven three ways — RepoDigest `sha256:8339bdbd7999…` == the running pods' `imageID`; binary probe with both controls discriminating (kill-switch literal present, positive control present, negative control absent); OCI `revision=a6d1c53c…` with `git merge-base --is-ancestor` YES for `c8400e452` and `f629f4530` and correctly NO for a later commit. **EXERCISED, not merely deployed, with a demand control** (`PROBE_write_door_guard.sh`, corr `a40d69b7-b91b-431f-9187-b7c8f0ee9220`): an unregistered handler at a trigger-set status was born **`blocked` at write, never claimed**, with the exact error text, while a registered-handler control row born in the same dispatch passed through **unblocked**, and a THIRD arm carrying tool-auditor's exact post-457 shape (**no `handler_agent` key at all** + parked status) was **ACCEPTED** — the arm that converts the relaxed validation from PRESENT (established by the ancestry check on `c8400e452`) to BEHAVING, which no presence check can do: a parked config could still be refused downstream by `StrictConfig`/`ConfigKeys`, and `continue_on_error` would swallow it — the control is what makes it a proof rather than a green light, since a block-everything bug produces the same test row. Phase 3 (migration `457`) applied: **0 rows anywhere carry `hitl-review`**. Previously: BUILT + MUTATION-PROVEN (×3), council APPROVED round 2 (`4d1ed8a5`, 2026-08-17: r1 REVISE gated by guardian → kill-switch + measured probe cost shipped `f629f4530`; r2 approved, 2 advisories none high) — ⚠ historical, kept because it is the landmine: the FIRST 08-17 build DID NOT SHIP IT: pods restarted on the unchanged tag `v1.0.1305`, so the node re-served its cached image (local `sha256:6039e19c…` carries the kill-switch literal, running `sha256:f90a7e88…` does not; another lane measured 203 commits inert fleet-wide the same day). Verify by DIGEST before believing any future roll — see LANDMINES, and do not read a pod restart as delivery.** The config half of the same bug (`bugs_open/291`) is LIVE: migrations `450` (tool-auditor review items born `needs_human_review`, not the dispatchable default) and `451` (14 damaged rows re-parked, stamped `result.repair_291`) applied and verified 2026-08-17. Until the roll, a NEW config naming a phantom handler still detonates at claim, not at write — check the binary's provenance stamp before believing this guard live.
- **what:** `writeWorkItem` (`load_work_item_actions.go`) now probes `workItemHandlerRegisteredSQL` — claim's own registration predicate, rendered from the one shared helper (WDS-017) — for any item born with a non-empty `handler_agent` at a status in `workItemStatusRequiresRegisteredHandler` (triaged/approved/claimed — the Go mirror of CHECK `swi_no_handlerless_promotable`'s list, migration 443). Not registered → the item is written at `status='blocked'` with claim's exact error text (`error` rides the INSERT via the same conditional-append idiom as `parent_item_id` — widening the shared statement unconditionally is the recorded twenty-test trap), a `zap.Warn`, and `workItemWrite.BornBlocked=true` (surfaced by the `create_work_item` action as `born_blocked`). **Demote, never refuse**: a refusal loses the finding to a pod log (discovery sweeps log-and-continue; config loops run `continue_on_error`), while a blocked row is durable, visible (`idx_work_items_blocked`), and self-healing — `feasibility-recheck` (600s) promotes it the moment an agent of that name is registered, which also absorbs the platform's image-before-seeds ordering. The same commit relaxes `create_work_item`'s handler validation: an empty handler is refused only when the item is born dispatchable; a PARKED item (`status: needs_human_review` + no handler, the migration-217 idiom) is now expressible from config — it was this validation that forced tool-auditor to name SOME handler, and the name it picked (`hitl-review`) had never existed.
- **why:** `bugs_open/291`. All three prior guards were blind to config-minted handler routes BY STATED DESIGN: `handler_coverage_test.go` (077) scans only `discovery_checks` Go source; `work_item_type_minting_ratchet_test.go` (279) scans only Go and records the `create_work_item` config path as its residual; CHECK 443 covers only the EMPTY string (a CHECK cannot subquery `agent_definitions`). This probe IS the write-time registry check the 279 ratchet header said did not exist — `agent_definitions` is the registry — for the handler half of that residual.
- **landmine — the trigger set must not widen, and the counter-examples are live:** `deferred` `capability_gap` rows deliberately name UNBUILT builders (`unavailableBuilders`, `load_work_item_actions.go`); several producers park `needs_human_review` at the unregistered pseudo-handler `'human-review'` (`evidence_citations.go:438`, `refresh_evidence_base_action.go:1191,1266`, `directory_claims.go:432,729` — a recorded residual, inert-but-wrong the same way `resolve_composition_layout` was); `detected` is judged at promotion (WDS-017). A guard covering those statuses would demote them all — recreating `bugs_closed/284` inside 291's fix. Pinned by `TestStatusRequiresRegisteredHandler_ExactlyCheck443sList` and a scripted-probe tripwire in `TestWriteWorkItem_ParkedAndPrePromotionShapes_NeverProbed` (a bare no-probe-expectation CANNOT catch a widened set — the guard's own probe-failure fall-through swallows sqlmock's unexpected-query error; the tripwire scripts the probe to answer "not registered" so a wrongly-probing build demotes and fails the INSERT match).
- **limits, stated:** ~41 raw `INSERT INTO site_work_items` Go sites (28 actions / 12 core-manager admin / 1 cmd) bypass the door — claim's branch remains the universal backstop; two `query_database` steps in live config INSERT directly. `hitl-review` remains on tool-auditor's parked items until the STAGED post-roll migration flips it to `''` (`bugfix_291_hitl_review_phantom_handler/STAGED_*.sql` — it CANNOT apply before the roll: the old binary refuses an empty config handler and `continue_on_error` would silently lose every finding). Admin Retry on a parked review item recycles it to `blocked` until that flip; after it, CHECK 443 refuses the Retry instead. **The dedup KEY-GRANULARITY loss is NOT closed here and has an owner elsewhere** (council round 1, `editquality`+`bug_historian`, both correct): `audit_review_<page_id>` is one slot per PAGE, so while ANY open item holds it — blocked before this fix, parked after — the auditor's later DISTINCT findings for that page are dropped by dedup. This guard changes nothing on that axis (identical pinning to claim-time blocking); the fix is per-FINDING review keys, the named follow-on of the `bugfix 285` lane's per-finding work. **Kill-switch** (council round 1, guardian): env `DISABLE_UNREGISTERED_HANDLER_DEMOTION` disarms the door probe without a roll, falling back to claim — ships ARMED; a rollback lever, not an opt-in. **Probe cost, measured:** 0.107ms actual (index-only scan, `idx_agent_definitions_type_active`, 210 rows) × ~613 site_work_items inserts/day fleet-wide ≈ 0.07 s/day.
- **sources:** `bugs_open/291`; `docs024_key_docs_latest/bugfix_291_hitl_review_phantom_handler/` (PLAN, NOTES, RUNBOOK); `platform/orchestration/actions/{work_items_common,load_work_item_actions,create_work_item_action,resolve_composition_layout_action}.go`, `work_item_unregistered_handler_guard_test.go`; migrations `450`/`451`; `090` run `3555b514-ca8f-4f31-9f55-e105ce73e961`
- **relations:** the shared predicate and promoter guard this extends to the write door (WDS-017); the two-strike block it deliberately runs AFTER (WDS-007); `bugs_closed/077`'s `handler_coverage_test` (the Go-literal half of the same class); `bugs_closed/279`'s minting ratchet (whose recorded config-path residual this discharges for `handler_agent`); the HITL parking idiom (migration 217) and its queue with no surface (`bugs_open/033`)
- **verify-later:** post-roll — provenance stamp ⊇ the guard commit on agent-chassis, then the STAGED handler flip applied, then one tool-auditor run filing `''`+`needs_human_review` review items end-to-end

### WDS-019 — owner_critique work items (admin REQUEST CHANGES) + the dispatcher-thread routing pattern
- **status:** built 2026-08-31, committed `f2b288b72` (council trail `Council-Submitted: 9f1cb042`), **inert until the next core-manager + admin-dashboard roll**; the dispatcher half is an operating doc + tick script, exercised zero times.
- **what:** A "request changes" verb on the admin work-item surface: `POST /admin/work-items/:item_id/request_changes` (`HandleRequestChangesWorkItem`, site_admin_handlers.go) files the owner's free-text critique as a NEW `owner_critique` work item (source='owner', pipeline='delivery', status='triaged', **handler_agent EMPTY**) and touches nothing else — the origin item keeps its status, so a `needs_delivery_review` checkpoint keeps gating delivery (contrast APPROVE, which releases it, and RESOLVE, which the gate ignores). Dashboard: critique textarea + button on every needs_human_review item. **The type is deliberately NOT cluster-routed** — its consumer is stated in the row spec (`consumer: thread-dispatcher`): the owner's standing dispatcher session polls unrouted rows (`docs024_key_docs_latest/dispatcher_thread/poll_open_critiques.sh`), routes the text to live session threads via cross-session messages, reports dead lanes as "threads to open", and stamps `result.routed_at/routed_to/routed_by` with a first-stamp-wins WHERE guard (safe across multiple dispatchers/machines — the DB is the cross-machine bus). This is the deliberate inversion of the bugs_open/279 trap (a minted type EXPECTED to be cluster-routed and wasn't): here non-routing is the design, in-row.
- **why it exists:** owner request 2026-08-31 ("a place in the admin panel for a submitted site where I can input my opinions… standard procedure until we can get the quality a bit higher"), motivated the same evening by three lanes holding unread critique CONTRIBs with no live session (the first paid build's review reached the platform only via a watched chat thread).
- **sources:** internal/core-manager/admin/site_admin_handlers.go `HandleRequestChangesWorkItem`; internal/core-manager/api/server.go workItemGroup; frontends/admin-dashboard/src/App.tsx `handleRequestChanges`; docs024_key_docs_latest/dispatcher_thread/{DISPATCHER_README_start_here.md,poll_open_critiques.sh}
- **relations:** WDS-001 (state machine — this type parks at triaged with no handler by design); hitl register (approve/resolve verbs); bugs_open/279 (the unrouteable-type trap this inverts); bugs_open/420 §the first paid build's critique storm
- **verify-later:** after the roll: file one critique via the button, watch the dispatcher route it end-to-end; check the stale reaper's predicate against an open owner_critique row (risk named in the council submission).

### WDS-020 — per-site growth posture: "this site is ready" holds the tool chain at its two heads

- **status:** ~~built 2026-09-02 (Go — inert until the next roll); no site holds yet~~
  **CORRECTED 2026-09-02 by the improvement_loop lane — THE DOOR IS LIVE AND HAS A CONSUMER.**
  The roll happened the same day: probed the running `agent-chassis` binary for
  `GROWTH_DOOR_PROBE_FAILED` — **present**, with both controls in the same breath (the older
  `OWNED_PAGE_DOOR_PROBE_FAILED` present, an invented literal absent). `c2349955d` is an
  ancestor of the running stamp `ebf27c60` (verified independently by the gamedesign.uk lane).
  **First holding site: gamedesign.uk, set ~22:10Z 2026-09-02** (`bugs_open/447`) — so
  "zero live consumers" is also spent. `[MEASURED 2026-09-02]` **1 of 39** active sites holds;
  the other 38 are unset, i.e. open.
  **Why this line mattered:** it read "inert until the next roll" for as long as the door was
  running, which is the `LANDMINES.md` "a register STATUS line is a snapshot that outlives its
  truth" trap — and it nearly stopped a lane relying on a control that was working. It was
  caught by a peer probing the artefact rather than believing this entry.
  **LIVE 2026-09-03 — migration `722` APPLIED: a NEW site is BORN HOLDING.** A BEFORE INSERT
  trigger (`trg_sites_born_holding_growth`), **not** the column default the first drafts used:
  a default is bypassed by an INSERT that names `settings`, and 2 of the 15 site-creation paths
  do — including the `SEED_*.sql` shape CLAUDE.md instructs every lane to use. Council trail
  `070347dd-c410-4cf2-b5e6-8c87e568a792`, APPROVED at round 5 after four REVISE rounds.
  Verified at the artefact: trigger enabled in `pg_trigger`; **1** site carries a posture
  (gamedesign.uk, hand-set), so no existing row moved; 39 active sites unchanged.
  ⚠ **BEFORE INSERT does NOT make existing rows safe under an UPSERT** — `EXCLUDED` is the
  post-trigger row, so a `DO UPDATE SET settings = EXCLUDED.settings` would re-hold a released
  site. None of the four upserts on `sites` does; `check_sites_upsert_excluded_settings`
  (pattern-check.py) refuses one at commit time, and LANDMINES carries the induction recipe.
  ✅ **ADOPTED sites are born held too, and that is RULED, not accidental** (owner
  2026-09-03: *"yes adoption sites are held until specifically released"*). Adoption inserts
  through this path. **Do not "fix" a held adopted site by exempting adoption** — it arrives
  with someone else's tools and brand (`bugs_open/439` is that contamination the other way),
  and the human decision is the point.
  ~~⚠ **Nothing reports "held longer than N days"** — a site nobody releases stops growing
  silently. Owed by the improvement_loop lane.~~ **BUILT 2026-09-03 (improvement_loop lane):
  `growth-posture-hold-check`**, a daily CronJob (`0 8 * * *` UTC, the postgres:16-alpine
  Python family — `deployments/kustomize/services/growth-posture-hold-check/`) that lists
  every held site with its age, raises `held_overdue` for a LIVE site (active/deployed,
  unlocked) held past **N = 7 days `[ASSUMED 2026-09-03, owner to rule]`**, and raises
  `trigger_missing` if 722's trigger is dropped or disabled (otherwise a clean report lies).
  One `doc_notes` row per run, clean or not (`subject_key='growth-posture-hold'`). Hand run,
  same file: `scripts/audit-growth-posture-hold.sh [--days N] [--write]`; `--self-test` is
  16 fixture cases incl. the threshold by mutation. **Status: built + self-tested + dry-run
  against the live DB; CronJob NOT yet applied and no doc_notes row yet — "deployed" would
  overstate it until `kubectl apply -k` and the first row.** Council: see the lane's NOTES.
  **The clock it needed did not exist** `[MEASURED 2026-09-03]`: 722 stamped only
  `growth_posture`; the two hand-holds carried two shapes and zero timestamps. Migration
  **`752`** makes the trigger also stamp `growth_posture_set_at` (`to_jsonb(now())`),
  `growth_posture_set_by` (`'trg_sites_born_holding_growth'`) and `growth_posture_reason`,
  beside a posture IT stamped only — a stated `open` gets no record. **No backfill** of the
  two hand-held rows (other lanes'; a guessed timestamp is worse than none): the check
  reports those as age UNKNOWN, bounded below by the first day it saw them (its own earlier
  rows, anchored on the `HELD <domain> ` line), and self-corrects the day the lane stamps
  the row. **Hand-hold recipe, now three lines longer** (the gamedesign.uk lane's key
  names, kept): `settings = jsonb_set(jsonb_set(jsonb_set(jsonb_set(COALESCE(settings,'{}'),
  '{maintenance_profile,growth_posture}','"hold"'), '{maintenance_profile,growth_posture_reason}',
  to_jsonb('<why>'::text)), '{maintenance_profile,growth_posture_set_by}', to_jsonb('<lane> <date>'::text)),
  '{maintenance_profile,growth_posture_set_at}', to_jsonb(now()))` — ⚠ and only if
  `maintenance_profile` already exists on the row; `jsonb_set` silently no-ops on a missing
  parent (722's header, 291's before it), so materialise it first on a bare `{}`.
- **what:** `sites.settings->'maintenance_profile'->>'growth_posture' = 'hold'` makes the
  two HEADS of the tool growth chain file their items in the RECORD SHAPE — born
  `status='deferred'`, `handler_agent=''` (the promoter excludes a handler-less row from its
  scored CTE before any door runs), summary prefixed `[growth held]`, spec stamped
  `growth_held/growth_handler/growth_release_recipe`, `recurrenceExpected=true` so a held
  predecessor closing never feeds the anti-churn strikes. FILED, not skipped — the signal
  is kept; release is a one-UPDATE human verb from the spec's own recipe. The heads:
  `check_missing_tools` (files `evaluate_tools`) and tool-suggester (files `add_tool` via
  create_work_item); everything downstream (tool-generator / tool-deployer guide pages)
  runs only as a consequence of an add_tool executing, so no downstream guard exists ON
  PURPOSE. **The check lives as a DOOR in `writeWorkItem`** (the third policy door, beside
  the owned-page park and the unregistered-handler demotion; `insertWorkItem` wraps
  `writeWorkItem`, so discovery sweeps, config steps and direct Go callers ALL cross it) —
  relocated there from per-producer guards by the council's round 1 (corr 1e735fa2): a
  guard covers the producers you found, a door covers the ones you didn't. Kill-switch
  `DISABLE_GROWTH_POSTURE_DOOR`, ships armed. The held item KEEPS its item_type/item_key
  (ownedPageParkedItem's retraction-contract lesson, bugs_open/342). `source='owner-request'` bypasses — the owner asking
  is not growth to refuse. Audit-seat growth types have filed as record-mode verdicts since
  migration 624; that half needed nothing. Fail-open on a posture read error, loudly.
- **why it exists:** owner decision 5 of 2026-08-31; worked cases apis.uk (sprouting
  pages), farmerinsurance.uk (7 unresearched tools, culled 08-31), agritec.uk (ungated
  growth EXECUTED — 5 calculators live in ~26h, 0 sources; the same site's evidence-first
  route fenced+reconciled the same week — `loanzy_uk_example_site/CONTRIB_2026-08-31_agritec…`).
- **sources:** platform/orchestration/datahelpers/growth_posture.go (`SiteGrowthPosture`,
  `GrowthGateApplies`, `GrowthGatedItemTypes`); actions/growth_posture_door.go
  (`applyGrowthPostureDoor`) wired in load_work_item_actions.go's `writeWorkItem`;
  tests: growth_posture_door_test.go (hold / open+unknown / read-error fails open /
  non-gated never probed, ordered / pure-half table); design + producer census:
  loanzy_uk_example_site/PLAN_2026-08-25_…_switch_the_loop_back_on.md ADDITION 2026-08-31.
  Born-held: sql_for_agents/722 (trigger) + **752** (the when/by-whom record);
  the report: deployments/kustomize/services/growth-posture-hold-check/base/check.py
  (`classify`, `render_report`, `--self-test`), scripts/audit-growth-posture-hold.sh.
- **relations:** the record shape and its refusal proof are RFC_056 / filing_mode's
  (write_audit_findings_filing_mode_test); WDS-017/018 are the neighbouring write/promoter
  doors; set/unset recipe in loanzy RUNBOOK ("growth posture").

## WDS-021 — empty-page rerenders convert to build asks (bugs_open/315 reopen)

- **status:** built 2026-09-02 (commit `8eca969cb` + a writeWorkItem-seam refactor commit), inert until the next agent-chassis roll — "did it ship?" = `git merge-base --is-ancestor 8eca969cb <the chassis stamp>`
- **what:** A `page_rerender` on a page with **zero component rows** is a guaranteed skip that still completed the item — nine completions accumulated on one empty page (roi-estimator) while nothing routed it to the build queue. Two doors now convert instead: `RerenderSinglePageAction`'s 0-component skip files a deduped `needs_content_page` (**a new producer of that item_type** — the tool-deployer, planners, adoption and `check_sectionless_pages` are the others; named here per bugs_open/213's second-producer lesson), and `create_rerender_items`' unscoped loop stops filing guaranteed-skip rerenders and files the same ask, reporting `empty_pages_converted_to_build_asks` in the step RESULT. **Key shape is new**: `needs_content_page:<page_id>` (uuid-stable; the other producers key by name — recorded as a deliberate divergence, no cross-producer dedup intended because the asks carry different specs).
- **why the seam:** both file **through `writeWorkItem`** — the workItem struct's own comment records three hand-rolled INSERT call sites caught by council; every door in that seam (owned-page routability, growth posture, future ones) reaches these producers unremembered.
- **sources:** platform/orchestration/actions/rerender_single_page_action.go (`fileBuildAskForEmptyPage`), create_rerender_items_action.go, empty_page_build_ask_test.go (producer guard mutation-proved); `bugs_open/315` reopen section 2026-09-02.
- **relations:** the 095 skip discrimination (same branch); WDS growth-posture door (the seam argument); adoption-pipeline S2 `mark_no_ready_sections` (where a spec-less build ask honestly parks).
