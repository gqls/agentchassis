# Register — visualisation-and-charts

> **covers-through: 2026-07-28** · written 2026-07-28 from first-hand code/DB reads
> and from rendering the templates, never part of the extraction.
> Everything else dates from the 2026-07-13 extraction freeze — absence
> here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074). It said **11** and the file held **14**._ **NOT from the 2026-07-13 extraction.** Two of these components
shipped after the freeze and one shipped the day this entry was written, so none
was ever in the register.

This entry exists because of a specific, recorded failure. On 2026-07-27 a
workstream handoff asserted, in bold, *"there is no chart renderer"*, and that
claim was repeated to the owner twice and used to classify graphs as blocked
work. **Two renderers were live at the time.** The owner corrected it. The cause
was not laziness: the evidence cited was true (`go-echarts` is genuinely absent
from `go.mod`; `report_charts.go` is genuinely narrow) and the conclusion did not
follow, because **capability on this platform is mostly DATA** — `evidence-chart`
is a row in `content_components` and no grep of Go source can ever find it. That
is the reason this register entry is worth its length: the searching habit that
finds code does not find components. See `WRONG_CALLS.md` 2026-07-28.

Status vocabulary per `README.md`. Where a thing is *built but never exercised on
a live site*, that is said explicitly.

### VIZ-001 — `evidence-chart`: magnitudes, resolved through fact ids
- **status:** deployed (live on 1 site)
- **status-evidence:** **The bug's own acceptance test passes.** `[MEASURED 2026-09-02]` over three consecutive SCHEDULED runs on webdesign.co.uk the union of `audited_paths` is **151 distinct pages of 151 live — ZERO missed**, graded against the site rather than against itself; the third run cleared the cursor (`cursor_cleared=true`, final window 37 pages, last page `tool-llm-cost-calculator` at `nav_order` 201). A SECOND site, `loanandmortgagecalculator.co.uk` (61 pages), cycled to completion unaided. The reader **fired on its own schedule** `[MEASURED 2026-09-03 07:50:00Z]` and wrote its durable row 14s later: *22 rows / 4 sites / 2 callers / 0 findings / 1 dormant group named*. Four guards mutation-proven on the cursor, four on the reader, each restored green after.
- **what:** Horizontal bar chart comparing quantities. Bars are **CSS**, not SVG: the template writes `style="--v:<value>;--m:<max>"` and the browser does the division. Every plotted point resolves through `{{range $f := $facts}}{{if eq $f.id $p.fact_id}}` — **a chart point cannot carry its own number**. Each row renders `verified {{$f.verified_at}}`; the figure carries a `source_note`. Charts may be filtered per page via `$c.pages`.
- **why it matters:** this is the doctrine ("charts are code-rendered from real figures") existing as a *mechanism* rather than a rule. The unsourced state is unrepresentable, not merely discouraged.
- **sources:** `content_components` row `evidence-chart` (html_template read in full 2026-07-28); `features_open/023`
- **relations:** VIZ-002, VIZ-003, VIZ-005, CLM-001

### VIZ-002 — `evidence-timeseries`: one measurement over time
- **status:** **live and exercised** — first real use 2026-07-29, Thames page (`evidence-timeseries-leakage`, migs 265/266)
- **status-evidence:** five-observation series (Thames leakage 2020-25, restated APR figures) served on `/cases/thames-water.html`; claimscan 0 findings across 18 components with the series values resolving through `seriesSupports`; render audit clean (8 pages, 0 firm) the same hour. Scale denominator is a registered fact (the 20.5% final-year commitment), per the component's own rule.
- **first-use notes:** `as_of` renders raw as the axis tick, so year-end months read "2021-03" — honest but terse; the chart footnote explains the 31 March year end. `printf "%.10g"` renders 12.0 as "12" — accepted rather than adding a `display` key that would make content_data diverge from the register.
- **what:** The companion to VIZ-001. That compares magnitudes *between things*; this shows one measurement *over time*, one column per observation, with the `as_of` of each point as the axis tick. Resolves the series through a `fact_id`, and renders **each point's own citation and date beneath the plot**.
- **why it matters:** it is the first renderer whose x-axis is meaningful, and it is deliberately the *second* thing built — the substrate (VIZ-003) landed first, because a time-series component with no legitimate series to plot is an invitation for a writer to fill it from the model.
- **sources:** `sql_for_agents/250_evidence_timeseries_component.sql`; `docs024/oufe/DESIGN_2026-07-28_premise_branching_and_deepthink.md` §3
- **relations:** VIZ-003, VIZ-006, VIZ-007, VIZ-008
- **verify-later:** first live use; whether the column form reads as a time series to a reader, or wants a line

### VIZ-003 — series facts: the substrate a time axis needs
- **status:** deployed (in `v1.0.1185`, pod-verified); **first live series registered 2026-07-29** — `CIT-tw-leakage-series`, five observations each with its own citation (oufe register, mig 265)
- **status-evidence:** three distinctive string literals from `claims_series.go` found in the running binary 2026-07-28, with a positive control. First-series verification 2026-07-29: 5/5 observations carry their own citation (checked per-observation, the way the gate checks it).
- **what:** An `EvidenceFact` held one `Value` and three dates — `accessed`, `published`, `verified_at` — **all of which are provenance**. None is the date the value *applies to*, so a time series had no honest shape. `Observation{as_of, value, source, verified_at}` adds one. Two rules are load-bearing: **every observation carries its own source, never inherited from the parent fact**, and `as_of` is distinct from `verified_at` (re-checking a 2021 figure in 2026 moves `verified_at` and must not move the point on the axis).
- **why it matters:** a series where the first point is cited and the rest "continue from the same source" is exactly how interpolation and extrapolation enter looking like data.
- **sources:** `platform/orchestration/datahelpers/claims_series.go`; `claims.go` (`Observations` field, `numberSupported` branch)
- **relations:** VIZ-002, CLM-001, CLM-003

### VIZ-004 — the honesty gate had to learn about series, or it would have fought the chart
- **status:** deployed
- **what:** `numberSupported` skips any fact with `Value == nil`, which is every series fact by design. Without a branch for them, **every value plotted from a series would be reported as an unregistered number**. Matching is deliberately *exact* even when the fact carries a `gte` tolerance, because a `gte` series would blanket-support nearly every number on the page.
- **why it matters:** the round-1 council objection on this change found the sharper form of it — `ValidateSeries` enforced the per-observation source rule but `numberSupported` never called `ValidateSeries`, so an unsourced observation still registered its value. **A rule enforced only in a validator is not enforced**; it has to hold at the gate that decides. Both now share `observationHasResolvableSource`.
- **sources:** `claims.go` `numberSupported`; `claims_series.go` `seriesSupports`; council correlation `da40ddf0` round 1
- **relations:** VIZ-003, CLM-003

### VIZ-005 — the boundary: generated images explain, code-rendered output states
- **status:** designed, not built (the rule is stated; nothing enforces it)
- **what:** Generated (diffusion) imagery is acceptable for *explanatory* graphics — how a process runs, the shape of an architecture. It is the wrong tool for any value that must be **exact**, **selectable** or **translatable**, because a diffusion model draws a bar of approximately the right height and text baked into a JPEG cannot be copied or re-rendered in another language.
- **why it matters:** the routing fix in `bugs_closed/011` made publishable infographics real, and the first one produced was accurate *only because a human hand-wrote audited figures into the prompt*. That is prompt discipline holding a line that should be structural.
- **sources:** `features_open/023_FEATURE_infographic_figures_from_the_evidence_base.md` (R3, R4)
- **relations:** VIZ-001, VIZ-009, CLM-001

### VIZ-006 — `mechanism-flow`: drawing a process, with no numeric field at all
- **status:** deployed (live on oufe.com `/cases/thames-water.html`)
- **status-evidence:** 7 steps and 2 decision branches rendered on the live page, fetched 2026-07-28.
- **what:** A numbered vertical flow with optional decision branches, for explaining a legal or financial *mechanism* — sequence and consequence rather than magnitude. Connectors are CSS; there is no SVG and nothing to load.
- **why it matters:** it **has no numeric field by design.** On an evidence-gated site a number-shaped slot is an invitation for a writer to fill it, so the absence of the slot is the control. It is the answer to "infographics for the harder concepts" that carries no figure risk at all.
- **sources:** `sql_for_agents/247_mechanism_flow_component.sql`; `sql_for_agents/248`
- **relations:** VIZ-007, VIZ-009, VIZ-011

### VIZ-007 — there is NO arithmetic in the render funcmap, and a missing function is a PARSE error
- **status:** deployed (a constraint, not a feature)
- **status-evidence:** the only `template.FuncMap`s are `render_css_from_spec_action.go:238` and `compute_component_quality.go:354`; `executeGoTemplate` (`call_agent.go:1151`) registers `default`, `eq`, `ne`, `lower`, `upper`, `isset` and no arithmetic.
- **what:** No `inc`, `add`, or any numeric helper. A template referencing one **fails to parse**, so the component renders *nothing* — it does not degrade.
- **why it matters:** this single constraint shapes every chart on the platform. It rules out computing SVG polyline coordinates in a template, which is why both chart components pass values to CSS custom properties and let the browser divide. A first draft of VIZ-006 numbered its steps with `{{inc $i}}` and would have rendered an empty section; a CSS counter needs no funcmap at all.
- **sources:** `platform/orchestration/actions/call_agent.go:1149-1175`
- **relations:** VIZ-001, VIZ-002, VIZ-006, VIZ-008

### VIZ-008 — `$facts` is declared by the template, not supplied by the engine
- **status:** deployed
- **what:** `evidence-chart` opens with `{{- $facts := .facts -}}` and `{{- $page := .current_page -}}`. Go templates treat an **undeclared** variable as a parse error, so a component that references `$facts` without declaring it fails to render entirely.
- **why it matters:** it is a contract that is invisible unless you read the top of a working template, and its failure mode is total rather than partial. Caught on `evidence-timeseries` before shipping only by executing the template — the same class as VIZ-007.
- **sources:** `content_components` row `evidence-chart` (template head); `sql_for_agents/250`
- **relations:** VIZ-002, VIZ-007

### VIZ-009 — text inside `<svg>` is invisible to the claims gate
- **status:** deployed (a hazard, not a feature)
- **status-evidence:** confirmed the other way round on 2026-07-28 — the base64 payload `claimscan` actually received for `mechanism-flow` was decoded and found to contain the diagram's own words.
- **what:** `extractAssertions` walks HTML text nodes; SVG text is not reached. **A diagram built from `<svg><text>` could assert anything and scan clean.**
- **why it matters:** it makes "draw it in SVG" the wrong default for any graphic carrying words on an evidence-gated site, and it is the reason both new components use real HTML text with CSS-drawn furniture.
- **sources:** `claims.go` `extractAssertions:165-226`; brochure-workstream landmine
- **relations:** VIZ-001, VIZ-002, VIZ-006, CLM-002

### VIZ-010 — `scripts/render_audit.py`: the post-deploy render witness
- **status:** built (2026-07-27, brochure workstream), **run by hand only — NOT wired into anything.** SUPERSEDED IN CAPABILITY 2026-07-29: the same measurement is now dispatchable as an orchestration (VIZ-012); the script's retirement is unblocked but is the brochure workstream's call, not made here.
- **status-evidence:** checked 2026-07-28. Nothing in the Makefile, CI, any shell script, or any `agent_definitions` workflow invokes it. Run against oufe.com's Thames page that day: `contrast=0 broken-img=0`.
- **what:** Renders a live page in headless Chromium and measures what a visitor actually sees. For **every element** (`body *`) it takes the computed colour, walks up through transparent ancestors compositing alpha to find the effective background, and applies 4.5:1 / 3.0:1. A background *image* under text is reported as `overImage` so a reader discounts it rather than trusting a number the page cannot justify. It also reports images that failed to load, which the DB-only `image_url_404` check cannot see.
- **why it matters:** it is the only thing that catches 026's **family 3** — a component hard-coding an ink over a themed fill — which `check_palette_contrast` states in its own header it cannot see by construction. On fundamentallyai.com it found 101 AA failures across 5 pages in about two minutes, on a site where every page said `deployed` and none of ~50 discovery checks had objected.
- **the duplication worth recording:** on 2026-07-28 this workstream built `cmd/contrastscan`, a Go tool doing the same job, without finding this one. **The prior-art grep was `--include=*.go` and the prior art is Python.** The Go tool was deleted on discovery; its one arguably-distinct behaviour (refusing to score an unknown backdrop rather than flagging it) is a stricter variant of `overImage` and not worth a second tool. See `WRONG_CALLS.md` 2026-07-28.
- **sources:** `scripts/render_audit.py`; `platform/orchestration/actions/discovery_checks/check_palette_contrast.go:43,108`
- **relations:** VIZ-011, CLM-001

### VIZ-012 — `render-audit-agent`: the render audit as a dispatchable orchestration
- **status:** **live and exercised** (2026-07-29 — three full runs against oufe.com; the third measured a fix the first had found)
- **status-evidence:** publish `{"action":"orchestrate","config":{"agent_type":"render-audit-agent"},"input_data":{"site_id":…,"domain":…}}` on `system.agent.generic.requests`; the orchestration ran `ensure_site_record` → `request_render_audit` → complete, and findings landed in `collected_data->'render_audit'->'response'`. Recipe: oufe `RUNBOOK_oufe.md` §14.
- **what:** The whole chain, callable per site: chassis action `request_render_audit` (enumerates `pages` where `build_status='deployed'`, caps at `max_pages`, reports truncation) → topic `system.adapter.render-audit.requests` → dedicated pod `render-audit-adapter` (browser-runner image, own consumer group/logs) → Chromium renders every deployed page → `ContrastFinding`/`BrokenImage`/`OverflowFinding` + a summary separating **firm** failures from `over_image` approximations. `pages_failed` counts pages with a firm contrast failure; unreachable pages are reported, not skipped.
- **why it matters:** the only *dispatchable* detector for 026 family 3 (component hard-coding an ink over a themed fill). Proven predictive immediately: its first full-site run found a firm 2.61 white-on-gold on oufe's contact form submit — the shared `contact-form` component's hard-coded ink — fixed and re-measured clean the same morning.
- **landmines:** the seed originally wrote `initial_step` for `start_step`, which makes every dispatch fail as `WORKFLOW_INVALID` replied to a topic nobody reads (016b §9, 2026-07-29). It audits **deployed rows only** — a live page whose row says otherwise is invisible to it (oufe's contact page was, for a day). It does not raise work items from findings (deliberate — `bugs_open/122` candidate 2 needs its own design). ~~"Reports truncation" was true only in-memory: the flag rode `Metadata` on an awaited result, which never survives the park (RFC_012), so a `max_pages`-capped sweep read exactly like a complete one in the stored artefact — both rotation runs to date were silently truncated (`bugs_open/242`).~~ **FIXED 2026-08-11 (`502b6c194`, bugfix_242 lane, rides the next chassis+browser-runner roll):** the request carries `pages_total`/`truncated`, the adapter echoes them into `summary` next to `pages`, and a `RENDER_AUDIT_TRUNCATED` `agent_error_log` row lands before dispatch. `max_pages` for the rotation is 60 since migration `392` (applied 2026-08-11).
- **sources:** `docs/agent_docs/sql_for_agents/256_render_audit_agent.sql`; `platform/orchestration/actions/request_render_audit_action.go`; `internal/adapters/browserrunner/render_audit_action.go`; deployment `render-audit-adapter`
- **relations:** VIZ-010 (the hand-run predecessor), VIZ-011, CLM-001, `bugs_open/122`

### VIZ-013 — `write_render_audit_findings`: the render audit's drain
- **status:** ~~built + tested (2026-08-02, vigilant_designer_offer_analysis workstream); **inert until an image roll AND the config tail step lands on `render-audit-agent`**~~ **CORRECTED 2026-08-10 — LIVE AND OBSERVED FILING.** Both blockers cleared some time before this date; the entry outlived its truth by at least a week (bugfix_122 lane, which nearly planned around it as inert).
- **status-evidence:** 5 sqlmock tests pin the effects (`write_render_audit_findings_test.go`). **First live observation 2026-08-10 14:57:26Z**: the inaugural VIZ-015 rotation fire swept robot-hands.com and this action filed **34 `contrast_failure` rows**, all `detected`, all keyed `contrast_failure:<page-path>#<selector>`, on interior pages a homepage-only baseline never reached. Query: `SELECT item_type, item_key, status FROM site_work_items WHERE item_type='contrast_failure' AND created_at > '2026-08-10 14:54Z';`
- **what:** Chassis action that files a render audit's FIRM findings as routed `site_work_items`: firm contrast failures → `contrast_failure` at `css-patch-agent` (dedup key `contrast_failure:<page-path>#<selector>`; the NEXT render audit is the de-facto verifier); broken images that resolve to an `assets` row → `undeployed_asset` at `asset-deployer`, co-deduping with `check_undeployed_assets` on `undeployed_asset:<asset_id>` (deliberate shared dedup unit). Items are born `detected`, so promotion stays with `improvement-loop.triage_findings` (migration 286's single owner). Deliberately NOT filed, but counted loudly in the result: `over_image` approximations, overflow (no culprit attribution on this path — Tier-4 `no_horizontal_overflow` owns the attributed case), unattributed broken images (source-side planning gap; `check_content_image_missing`'s rail), and findings whose culprit class appears in a LOCKED component's markup. A run filing more than `max_items` (60) reports `findings_capped`. Since `502b6c194` (2026-08-11, live since `v1.0.1288`) a `max_pages`-truncated sweep is stamped too: `truncated`/`pages_total`/`pages_audited` appear in the result when — and only when — the audit's cap bit (`bugs_open/242`, CLOSED → `bugs_closed/` 2026-08-17). Since `89b3e582b` (**LIVE on `v1.0.1307`, 2026-08-17**) a page-less site's skipped audit (`{skipped: true}` from `request_render_audit`'s no-await path) returns an honest no-op (`{skipped, reason, inserted: 0}`) through the workflow's NORMAL edge instead of erroring onto `complete_error` — a skip is a third state, neither clean nor failed, and it now reads as itself (`bugs_closed/299_…a_skipped_render_audit…`, closed on a forced run against a zero-page pool site: normal edge, no `__step_error`, zero `agent_error_log` rows, where the same input failed on the pre-fix binary). **Retraction cannot fire on a skip** — the return precedes every DB call, because a run that measured nothing may not close standing findings.
- **why it matters:** closes the loop VIZ-012's own landmines section left open ("It does not raise work items from findings") — the design `bugs_open/122` candidate 2 asked for. Until this, the fleet's only browser-measured contrast defects stopped in `collected_data`, the bugs_open/115 shape.
- **sources:** `platform/orchestration/actions/write_render_audit_findings_action.go`; registry entry `write_render_audit_findings`
- **relations:** VIZ-012 (the measurement), VIZ-010, `bugs_open/122`, `bugs_open/115`, migration 286

### VIZ-016 — The render audit's selector contract: a filed selector is composed IN THE PAGE and VERIFIED there
- **status:** **LIVE ON BOTH IMAGES AND PROVEN AT THE ARTEFACT, 2026-08-24 19:15 UTC.** Built + committed 2026-08-24 (`ffa6e1c3d`), council-approved round 1 (`acadbe8b-f131-4d4b-b4de-5b61f0898f93`); rolled to `browser-runner-adapter` / `render-audit-adapter` / `agent-chassis` at 15:39 UTC on `v1.0.1334` and still carried after the 18:32 UTC re-roll to `v1.0.1335` (`48f55f218…`; ancestry checked with a dated control, plus a NUL-split binary probe of the running chassis). Migration `587_retire_invented_contrast_selectors_HOLD.sql` **was applied by hand at 2026-08-24 19:11:22 UTC** (`UPDATE 73`); post-checks `open_invented = 0`, `withdrawn = 73`, all carrying `pre_352_status`, `falsely_completed = 0`.
  > ⚠ **This status line replaced one that said the opposite** — "INERT until both images roll", "committed but DELIBERATELY NOT APPLIED", "[MEASURED] the 73 legacy rows are still live". Every clause was true when written on 2026-08-24 and false by that evening. It is recorded here rather than deleted because council seats read these lines as ground truth, and this entry was a live instance of the register's own stale-status landmine within hours of being filed.
  > ⚠ **The 73 are no longer findable by the census that counted them.** The open-population predicate now returns **0** for ever — staleness by SUBTRACTION, which reads as "this never happened". The query that keeps returning 73 (`result->>'cancelled_by' = 'migration_587'`, grouped by `pre_352_status`: deferred 58, unresolved 15, 13 sites) is in the lane's `RUNBOOK_invented_selector.md` §10.
- **status-evidence:** 7 sqlmock/unit tests, every guard MUTATION-PROVEN (drop the alias key, the scheme guard, the bare-tag refusal or the lock tokens and a named test fails). **The producer-side proof this entry demanded has now been supplied, and it came from the scheduled rotation rather than a driven canary** (the canary, dispatched at ~16:52 UTC, never produced an orchestration row at all). Two audits straddle the 15:39 roll: the 15:31:50 run filed **47** rows with **3** invented `TAG.TAG` selectors and no `selector_scheme`; the 17:33:16 run filed **10** rows, **0** invented, `verified/v1` and a `matches` count on every one. Settled independently in the page, not from `spec.matches`: `.ported-page-content A` counted **15** on `loancash.co.uk/guides/index.html` and **8** on `/guides/jargon-buster.html`, matching the producer exactly, all 23 being class-less `<a>`s — while two pre-roll rows' `SPAN.SPAN` / `LABEL.LABEL` count **0** against 22 real `<span>`s and 6 real `<label>`s, and both are already `complete`. Controls: invented path per domain → 404; non-existent class → 0; same selector against the 404 body → 0; `class="A"`/`class="H3"` absent from the markup.
- **what:** A `ContrastFinding` now carries `selector` / `matches` / `selector_verified`, and the reply's `summary.selector_scheme` (`"verified/v1"`) is a CAPABILITY DECLARATION stamped on every reply including a clean one. The selector is composed in-page — class → own id → nearest ancestor carrying an id or class → bare tag — and asserted with `Array.prototype.indexOf.call(document.querySelectorAll(sel), el) !== -1` to select the very element that was measured. `write_render_audit_findings` prefers it, falls back to `contrastSelector(tag, class)` when absent, and **refuses two categories with their own counters**: `skipped_unverified_selector` (the browser disagreed) and `skipped_unanchored_selector` (a bare tag is a site-wide patch, because css-patch-agent appends to the one site stylesheet). **The `class` field is now a FROZEN LEGACY ECHO and deliberately still carries the tag-name fallback** — an un-rolled chassis reads it, and removing it would make that binary compose bare-tag keys and falsely retract every legacy row.
- **the shared `item_key` shape, stated here because the producer set is named here (owner ruling 2026-08-02 §1):** `contrast_failure:<page-path>#<selector>`, where `<selector>` is the verified selector when present and `contrastSelector(tag, class)` otherwise. **The key embeds a DERIVED, VERSIONABLE value**, which is why changing the composition needed alias keys in retraction rather than a migration window — see WII-016. Producer set: exactly one, `write_render_audit_findings`.
- **why it matters:** before this, a class-less element was filed with `Class` = its TAG NAME, so the composed selector was `H3.H3` / `P.P` / `A.A` — matching nothing. [MEASURED 2026-08-24 19:10 UTC] **184 of 509 `contrast_failure` rows carried one, 111 of them already `complete`**: repairs recorded against rules that could never apply. (Earlier in the same day this read "181 of 452 … 108"; the 15:31 audit filed three more invented rows that closed `complete`, and the 452 was mis-dated — see `WRONG_CALLS.md` 2026-08-24. **111 is the permanently-quotable damage figure**: 587 never touches those rows.) ⚠ **The obvious fix is a REGRESSION**: `p.P` matches nothing and is harmless, `p` recolours every paragraph on the site, and `P.P`/`A.A` were 121 of the 181. That is why the selector is anchored and verified rather than merely lowercased.
- **⚠ TWO selector-composition schemes now live in this package, and the divergence is DELIBERATE AND PERMANENT** (council `acadbe8b`, reuse_agent seat). `contrast_check.go:86`'s `describe` lowercases the tag and joins ALL class tokens; this one preserves case, takes the FIRST class token, and anchors on an ancestor. Converging them would rekey **every** classed finding (`H2.card-title` → `h2.card-title.muted`) — ~271 rows that this bug never touched — for zero measurement benefit. **Do not "clean up" by unifying them.**
- **sources:** `internal/adapters/browserrunner/render_audit_action.go` (`auditJS`, `ContrastFinding`, `selectorSchemeVerifiedV1`); `platform/orchestration/actions/write_render_audit_findings_action.go` (`filingSelector`, `selectorLockTokens`, `candidateHasSelectorScheme`); `docs/agent_docs/sql_for_agents/587_retire_invented_contrast_selectors_HOLD.sql`; **`bugs_closed/352`** (CLOSED 2026-08-25; it was `bugs_open/352` until then); lane `docs024_key_docs_latest/bugfix_352_invented_selector/`
- **relations:** VIZ-013 (the drain that reads it), VIZ-010/VIZ-012 (the measurement), WII-016 (the retraction whose alias keys this required), WII-005 (`idx_swi_dedup`), **`bugs_closed/352`** (arm 1, closed 2026-08-25), **`bugs_closed/390`** (arm 2, split out the same day — a CORRECT selector whose appended rule is outranked; this contract does not help it and was never meant to), `bugs_open/211`, `bugs_open/296`

### VIZ-011 — chart furniture is a graphical object, so the 3.0 threshold applies to it
- **status:** deployed (applied in VIZ-002 and VIZ-006)
- **what:** Axis lines, connectors and bars are not decoration: they are required to understand the content, so WCAG's 3.0 non-text contrast minimum applies. On oufe's live stylesheet `--color-border` (#2E3F52) scores **1.66** against the page background and fails it; `--color-accent` scores **6.86**. Both new components therefore draw their furniture in the accent.
- **why it matters:** the intuitive choice (`--color-border`, because it is a border) is the failing one, and nothing on the platform checks it at build time.
- **sources:** measured 2026-07-28 against the live stylesheet; `bugs_open/122`
- **relations:** VIZ-006, VIZ-002, VIZ-010

### VIZ-014 — legible-ink companions: the renderer names BOTH directions of "is this colour readable"
- **status:** ~~code written, tested, committed 2026-08-06 — NOT live~~ **UPDATED 2026-08-10 — FULLY LIVE AND MEASURED CLOSED.** Engine live since v1.0.1266, pod-proven on both replicas across six rolls (none this lane's). Config half applied: migration `338` (4 components + 5 layouts, 2026-08-08) and `368` (`info-card-grid`, 2026-08-10). Cadence row is VIZ-015. Propagation done: 11 sites' stylesheets + 13 pages re-rendered and verified at the served artefact.
- **status-evidence:** 11 unit tests green against a clean `git archive HEAD` tree (another session's WIP was breaking the package build in place). Three mutations proved the assertions load-bearing, each with a DISTINCT failure: reorder the renderer calls → the AST ordering test fails; `grounds[:1]` → only the two-grounds test fails; delete the source-unchanged branch → only the no-op test fails. Council `c4d9c841-3658-4742-85b5-961e062ecad2` **APPROVED** at round 2 (round 1 REVISE, gated by `editquality`).
- **what:** `buildLegibleInkDefaults` (`palette_specialised_slots.go`) appends a renderer-owned `:root` block as **step 12** of `RenderCSSFromSpecAction`, after `buildSectionDefaults` (10) and `buildTokenAliases` (11). It emits ~~three~~ **four** custom properties (the fourth added 2026-08-25 — see the `--color-cta-bg-ink` bullet below), and the reason there are several is the whole point of the entry — a palette colour used as a FILL and the same colour used as an INK are different questions:
  - `--color-<x>-text` — the ink that goes **ON** an `<x>` fill. Already derived by `darkSchemeDerivations`; `accent_text` has existed since 2026-07-27 and **0 of 18 layouts declare it**, so it had never reached one stylesheet. Emitting it here is what turns that dead derivation live.
  - `--color-<x>-ink` — `<x>` **itself, made legible as an ink on the page**. New. Nothing in the platform computed this before. `--color-primary-ink` and `--color-accent-ink`.
  `legibleInkFor(src, grounds, palette, minRatio)` returns `src` unchanged when it clears `minRatio` against **every** ground, else the first palette colour that does, else the achromatic extreme with the better worst case.
- **⚠ CORRECTED 2026-08-13 — `--color-<x>-ink` is NOT "`<x>` itself made legible". It is `--color-text` under another name, on every site in the fleet, and this entry's own wording above is the thing that misled.** `legibleInkFor` walks `[]string{"text", "accent", "text_muted", "secondary", "primary"}` (`palette_specialised_slots.go:350`) and returns the **first** member clearing AA against every ground. `text` is first, and `text` is by construction the slot chosen to be read on `background` — so it clears whenever anything does, and **on every site in the fleet it wins** — stated as a measurement, not a deduction (reviewer's correction: the walk *could* return `accent` where `text` failed one ground and `accent` cleared both, since `grounds` is `{background, surface}`; no such site exists). `[MEASURED 2026-08-13 at the served artefact, all 18 palette-driven live sites: 16 divergences, 16 of them equal to that site's own --color-text, zero exceptions.]` The approved plan's justification — *"the first palette colour that does (which prefers a palette colour so the site keeps its character)"* — is therefore **false as built**: the walk cannot return a colour related to `<x>` at all. Half the description holds and is why the opt-in is safe: where the source already clears AA it IS returned unchanged, so a repoint is a genuine no-op on a healthy site. **Consequence for anyone repointing more consumers: you are writing `color: var(--color-text)`, and because `render_audit.py` measures contrast, stripping a brand colour scores a CLEAN PASS.** Do not repoint further until the derivation is fixed to try tinting the source colour before substituting a different slot — that repairs all four shipped repoints at once. Full evidence and the disconfirming test: `bugs_open/122` contribution 2026-08-13 §§1–7; `LANDMINES.md` entry of the same date.
- **⚠⚠ SUPERSEDED 2026-08-14 — THE DEFECT ABOVE IS FIXED AND LIVE. The bullet above is kept because the wording is what misled, but its instruction ("do not repoint further") is NO LONGER CURRENT and a seat quoting it as ground truth is quoting a snapshot.** `legibleInkFor` now gives `colour.LegibleVariant` first refusal ahead of the palette walk: the source colour is moved in **HSL lightness only**, hue and saturation preserved exactly, smallest sufficient change — so `--color-<x>-ink` finally means what this entry always claimed. The walk survives for the two cases it owns (achromatic source; a source no lightness can rescue). Shipped `12cf55015` (round 1) + `8ad05d01a` (round 2: composites the renderer's own `--section-surface` overlay onto both page grounds and requires **all four** to clear — round 1 emitted 3.93–4.03:1 on the ground a visitor actually sees and would have re-broken an element migration `368` repaired). **Live from `v1.0.1298`**, stamp `bc39e7bf5`, verified by ancestry with controls both directions. Council `afcec886-f84c-4fb4-8876-43502e70965b` APPROVED round 1 (11 reviewers, 6 relevance-gated abstentions, `unreadable: 0`, `gated_by_truncation: false`).
- **⚠ THE TARGET IS 5.0, NOT 4.5, AND THERE IS NOW A KILL-SWITCH (OWNER RULING 2026-08-14, second ruling of that day).** Two things a reader of the older text will get wrong:
  - **`inkMinContrast` is 5.0.** The bare AA floor is a *separate* constant, `inkFloorContrast` (4.5), and the two must not be re-merged: `legibleInkFor` (the `-ink` slots) reads the target, `pickInkOn` (the `-text` slots — labels **on** a filled control) reads the floor. They shared one constant until 2026-08-14, which meant a ruling about links would silently retune every filled control's label in the fleet. `TestPickInkOn_StillUsesTheAAFloorNotTheInkTarget` fails if they are merged. `inkCeilingContrast` (7.0, AAA) bounds the other end — above it the lightness search walks to near-white/near-black on most palettes, which is the de-branding the whole repair exists to stop.
  - **`buildLegibleInkDefaults` now takes an `inkPolicy`, and it is OPT-OUT (default ON).** Live `agent_definitions` step config, so a rollback is a config edit rather than revert + rebuild + roll + re-render — the council guardian seat's objection (medium ×2) against the round-1 change. Keys: `legible_ink_enabled` (bool, global), `legible_ink_disabled_site_ids` (`[]string`, per-site, whitespace/case tolerant), `legible_ink_min_contrast` (float, **clamped to `[4.5, 7.0]` and the clamp is logged** — config must never be able to ship sub-AA text). Disabled emits **nothing**, which is what returns consumers to the palette colour through their own `var()` fallback. Shipped `d4bbbf645`; council `d60aab29-3590-474e-898c-cd5224c9a8ee` **submitted, verdict pending at time of writing — check it before quoting this as approved.**
  - **Emissions moved.** `[MEASURED 2026-08-14, two independent implementations agreeing, palettes transcribed from the served stylesheets]` dartsonline primary-ink `#8a97bd` → `#94a0c2`; accent `#ef7060` → `#f18072`. All seven pinned sites still find a variant at 5.0 — none falls through to the walk. Pinned in **`palette_ink_policy_test.go`**, in the `actions` package, because `platform/colour`'s pin is at the fixed `AANormal` constant and **stayed green while describing seven colours that no longer ship** the moment the target moved.
- **⚠ ~~STILL TRUE~~ FALSIFIED 2026-08-14 (evening): the fix is DORMANT until a stylesheet re-renders — and routine traffic re-rendered two sites within a day of this entry claiming nothing would.** `visual-design-audit`, a third-party lane with no connection to this work, filed `needs_design_review` items that re-rendered **cookly.uk** (14:22Z) and **robot-hands.com** (15:25Z) on the then-live 4.5 binary. `[MEASURED 2026-08-15 at the served stylesheets, and each value reproduced by an independent implementation of the derivation before being read]` robot-hands now serves `--color-primary-ink: #8a97bd` / `--color-accent-ink: #f66e2f`; cookly serves `--color-accent-ink: #af4625` (its primary is the unchanged branch). Those are exactly the 4.5 emissions. **So the brand-tinted inks are LIVE on two sites, and the "dormancy is the gate" reasoning recorded in the 08-14 handoff — the `bug_historian` seat's objection was accepted on those grounds — was sound in principle and wrong in fact.** The honest form of the claim is not "nothing schedules a re-render" but **"nothing THIS LANE schedules a re-render, and other lanes re-render on their own clocks"**; the residual exposure named two days earlier ("a NEW `webdesign-agent` item from a third party") fired within a day of being written down. dartsonline, webdesign.co.uk and vonc remain untouched and still serve pre-repair values.
  > **AND THE EXPOSURE HAS NOW INVERTED, which matters more than the correction.** `v1.0.1300` (stamp `a2a691213`, 20:36Z) **carries `d4bbbf645`** — verified by ancestry against the stamp with a control, *not* by grepping the binary for the sha, which is the wrong test because a binary carries only its own stamp. So from 20:36Z **every re-render emits 5.0**, the owner-ruled target. An accidental third-party re-render now produces the *correct* colours rather than the overruled ones, so the hold protects much less than it did this morning. (`e0f239118` — the council-approved round 2 — is NOT carried; it is behaviourally identical here, since its `resolved` guard defends against *future* callers and every current call site passes a resolved policy.) The 14-site protection **halved** when `v1.0.1298` rolled: it used to rest on "not in the binary" *and* "nothing re-rendered", and now rests only on the second.
  > **CORRECTED 2026-08-14 (same day, same author) — this bullet used to end "Any lane re-rendering any of those sites, for any unrelated reason, ships the change." That is FALSE and it overstated the exposure by a wide margin.** `[MEASURED]` **`webdesign-agent` is the ONLY agent type whose workflow contains a `render_css` step** — `SELECT a.type FROM agent_definitions a, LATERAL jsonb_each(a.default_config->'workflow'->'steps') s WHERE s.value->>'action' ILIKE '%render_css%' AND a.is_active AND NOT COALESCE(a.is_snapshot,false) AND a.deleted_at IS NULL;` returns exactly one row. `page-rerender`, `rerender-pages`, `rerender-site` and `rerender-chrome` have **zero** between them, and `rerender_pages_actions.go:558` only writes the `<link rel="stylesheet">` tag rather than regenerating the file. **So the ink values move only when `webdesign-agent` runs** — not on a page re-render, a section re-render, a chrome re-render or a site re-render. Caught because a session was running `rerender_page_sections_direct.sh` against dartsonline and, on the wrong model, appeared to be one command from tripping a live owner gate; on the measured model it could not touch it. **The cost of the wrong version is not alarm, it is desensitisation:** a warning broad enough to forbid provably-harmless work is one people learn to ignore. State the handler, not "any re-render".
- **⚠ FOURTH SLOT ADDED 2026-08-25 — `--color-cta-bg-ink`, and it is the first one whose ground is NOT the page** (`finetuning_uk_service` lane, `bugs_open/398`, migrations `619`/`630`/`631`, `Council-Submitted: f0591cb2-d65d-4517-a676-0334a7ff29a8`, round 2 after a REVISE). **INERT until the next chassis roll** — until then every consumer falls through `var(--color-cta-bg-ink, var(--color-cta-bg))` to today's behaviour.
  - **Why it exists:** `cta_bg` is the ONE palette slot whose contract admits a **gradient** as well as a colour (`derive_brand_head_assets_action.go:275` documents that shape; **10 of the fleet's themes hold one** `[MEASURED 2026-08-25]`). A gradient substituted into a `<color>` position is **invalid at computed-value time**, so the declaration is discarded and the property falls back to inherit/initial — measured live as white-on-white CTA buttons at **1.00:1** and hero headings at **1.11:1** on finetuning.uk and robot-hands.com. So consumers needed a colour-typed companion, exactly as `--color-primary-ink` exists for the fill/ink split.
  - **Its ground is the INVERTED BUTTON's face (`cta_text`), not the page.** The other three slots measure against `pageGrounds`; this one must not. Note `cta_text` is the band's INK being used as a FILL — the name says text and the role is a surface.
  - **⚠ Grounding it on BOTH button faces was tried and is WRONG, not merely costly.** Before migration `630` the two consumers painted the button differently (`call-to-action` used `var(--color-cta-text, …)`, `tool-cta` hard-coded `var(--color-white, #fff)`), and **7 live themes** pair a light band with a near-black ink (`cta_bg #e9e2d3` / `cta_text #1a1a1a` — noted, idea, lendzy, loanzy, mortgagecalculator, remortgagecalculator, webdesign.co.uk), so the faces are `#ffffff` and `#1a1a1a` and **no single colour clears 4.5 against both**; `legibleInkFor` falls through to its terminal branch and emits black at a worst ratio of ~1.2. `630` converged the face instead. If a component ever paints this button a THIRD way, add its face to the slice — do not re-ground.
  - **⚠ The gradient's first STOP is not the answer, only the SOURCE.** It is a FILL colour and fails AA against a white button face on **6 of the 10** gradient themes (`#3b82f6` 3.68, `#059669` 3.82, `#8b5cf6` 4.28); only `#1e40af` (8.8) clears — and that is the theme anyone debugging this will be looking at. `legibleInkFor` re-tints it; `TestBuildLegibleInkDefaults_CTAInkRetintsAnIllegibleFirstStop` goes red if that is simplified away.
  - **⚠ A template edited by SQL ships NOTHING without a fan-out.** `619` changed five component templates and every page kept serving its stored `rendered_html` — three `page-rerender` dispatches reported `COMPLETED` while still serving the defect. Only a `page_rerender` carrying `spec.reason='template_changed'` re-renders the template (migration `460`'s reason list). That is `bugs_open/283` §13 and it applies to **any** register entry whose delivery is a component template.
- **why it matters:** 17 of 18 layouts colour an ink with `var(--color-primary)` and nothing checked it. `warnUnusablePrimary` had detected the condition since 2026-07-27 and only logged, because — in its own words — "there is no single correct repair". There is one now, and the warning names it.
- **landmines:**
  - **`grounds` is a SLICE and that is load-bearing.** A component may place one ink on the page *and* on a card — dartsonline does exactly that (`__eyebrow` 1.04 on `background`, `.tl-card-link` 1.07 on the derived `card_bg`). One variable is only right for two grounds if it is right for the WORSE of them. Simplify this to a single ground and the fix becomes a half-fix that looks correct on whichever surface you happen to open. `TestLegibleInkFor_TwoGroundsDisagree` is the only guard.
  - **SCHEME-INDEPENDENT, unlike `fillDarkSchemeSpecialisedSlots` — deliberately, and this is the architecture fork the council asked be recorded.** That function is dark-only because a *light literal* is only wrong on a dark site. "This ink is illegible on this ground" is true or false regardless of scheme, and two of the three accent-direction sites are LIGHT (gaswholesalers `#F4F1EB`, finetuning `#F5F3EF`). **If a future edit re-narrows this behind an `isDarkHex` guard, the fix silently stops covering its own measured cases.** `TestBuildLegibleInkDefaults_LightSchemeStillEmits` fails and says so.
  - **Position is load-bearing and is now asserted, not commented.** It skips any name the ASSEMBLED CSS already defines, so running it before step 11 lets a later block define the same name and win — emitting a variable that is present and inert, the dead-config shape the seam exists to avoid. `TestRenderCSS_InkCompanionsComeAfterTokenAliases` reads the **AST**, not the source text: a regex would be satisfied by the order of the *comments*.
  - **Every consumer must be written `var(--color-X-ink, <exactly today's value>)`, never bare.** An empty fallback makes the whole declaration DROP rather than degrade. `TestBuildLegibleInkDefaults_NeverEmitsAnEmptyOrIndirectValue` pins the emission side; the consumer side is the migration's job.
  - **ADDED 2026-08-10 — the ink companions are computed against `pageGrounds = {background, surface}` ONLY, so an element on a COMPONENT-PAINTED ground is out of reach and `legibleInkFor` will correctly return the source unchanged while the element stays illegible.** Measured, not inferred: gamesdesign's `.stats-eyebrow` sits on the primary section fill, accent scores 12.46:1 on the page ground, and the served post-fix eyebrow measures **1.44:1 — byte-identical to its pre-fix failure**; vonc's is the same mechanism, confirmed at 1.63:1 after two re-renders. **Do not count a closure for any element whose ground is painted by its own component** — name the ground before predicting the close. This is `bugs_open/212` §8's open architecture question inherited here, and it is why the approved plan's "12 closures" was really 10.
- **sources:** `platform/orchestration/actions/palette_specialised_slots.go` (`legibleInkFor`, `worstRatioAgainst`, `buildLegibleInkDefaults`, and from 2026-08-14 `inkPolicy` / `resolveInkPolicy` / `inkFloorContrast`); `platform/colour/contrast.go` (`LegibleVariant`, `CompositeOverGround`); `platform/orchestration/actions/render_css_from_spec_action.go` step 12; tests in `palette_specialised_slots_test.go`, `palette_ink_derivation_test.go`, **`palette_ink_policy_test.go`** (the production pin, at the live threshold) and `render_css_from_spec_action_test.go`, plus `platform/colour/legible_variant_test.go`; councils `c4d9c841-3658-4742-85b5-961e062ecad2` (original), `afcec886-f84c-4fb4-8876-43502e70965b` (derivation repair, APPROVED), `d60aab29-3590-474e-898c-cd5224c9a8ee` (5.0 + kill-switch, pending); migrations `338`, `368`; commits `12cf55015`, `8ad05d01a`, `d4bbbf645`
- **relations:** VIZ-010, VIZ-012, VIZ-013 (the drain, now live), VIZ-015 (the cadence), `bugs_open/122`, `bugs_open/211`, `bugs_open/212`

### VIZ-015 — `site-render-audit-rotation`: the render audit finally has a cadence
- **status:** **live and fired on its first tick** (2026-08-10, bugfix_122 lane; migration `369`). This is the last link — VIZ-012 made the audit dispatchable, VIZ-013 made its findings file themselves, and until this row **nothing dispatched it**: 4 `contrast_failure` items existed fleet-wide, all one site, all from a single hand run.
- **status-evidence:** applied ~14:53Z; the scheduler fired it within 70s — `site_discovery_rotation` stamped `robot-hands.com` at 14:54:23Z, orchestration `b30943e4-440c-4f7c-8221-48ded2c6a562` reached step `audit` at 14:54:24Z and `COMPLETED` at 14:57:29Z, and VIZ-013 filed **34 firm `contrast_failure` items** from it. Verified at the artefacts (rotation row, orchestration row, work items), not at `enabled` + a fresh tick.
- **what:** A `scheduled_tasks` row cloning the proven `site-discovery-rotation-*` mechanism: hourly tick; a `pre_query` CTE selects the **single** most-overdue `active`/`deployed` site not audited in 7 days and not mid-build (`NOT EXISTS` a `claimed` build item), stamps `site_discovery_rotation (site_id, agent_type='render-audit-agent')` in the same statement, and returns `site_id` + `domain` as the agent's `input_data`. Net effect: **weekly per site, one site per hour.** Own concurrency group `render-audit` (cap 1) so it never competes for the discovery agents' shared slot — the audit runs on the dedicated `render-audit-adapter` pod. `timeout_seconds` 1800 < `interval_seconds` 3600, so a hung run cannot stack fires.
- **why it matters:** the defect class returns on its own. Two days after migration 338 closed dartsonline's `image-hover-card-grid` failure, that lane replaced the component with `info-card-grid` and reintroduced the **same** defect six times over; it was found only because this lane happened to re-run the audit by hand. A one-off repair of a recurring class is a snapshot, not a fix — this row is what makes VIZ-010/012/013/014 a standing control rather than a campaign.
- **landmines:**
  - **A `pre_query` returning zero rows is a STAMPED NO-OP, not a skip** (`cmd/scheduler/main.go:198-214`) — deliberate, so a fully-audited fleet costs nothing and the row does not pin itself at the head of its concurrency group re-winning the only slot every tick (`bugs_open/048`). Do not read a quiet week as a broken schedule; read `last_completed_at` and the rotation table.
  - **The rotation table is keyed `(site_id, agent_type)` and shared with the discovery agents.** Deleting `agent_type='render-audit-agent'` rows resets the audit queue only — but a careless `DELETE ... WHERE site_id=` takes the discovery rotations with it.
  - **The far end is not yet trustworthy: `bugs_open/213` is unfixed on this exact route.** `contrast_failure` items go to `css-patch-agent`, and 213 shows items on that route stamped `complete` with nothing written. This row now feeds it at ~34 findings per site. **Grade the repairs at the artefact (the NEXT audit), never at the item status.**
  - ~~**Every site over 25 pages gets a silently truncated weekly sweep** (`bugs_open/242`): the same tail pages skipped every week, and the artefact reads complete.~~ **FIXED 2026-08-11**: cap raised to 60 (mig `392`, applied — no current site exceeds it) and the truncation is visible in summary/`findings_written`/`agent_error_log` once the fix rolls (`502b6c194`).
- **sources:** `docs/agent_docs/sql_for_agents/369_render_audit_weekly_rotation.sql`; `cmd/scheduler/main.go` (`runPreQuery`, `stampCompleted`); `scheduled_tasks` row `site-render-audit-rotation`; PLAN_2026-08-06_contrast_ink_slots.md §3 candidate 2 ("edit 8"); council `c4d9c841-3658-4742-85b5-961e062ecad2`
- **relations:** VIZ-012 (what it dispatches), VIZ-013 (what drains it), VIZ-014, VIZ-010, `bugs_open/122`, `bugs_open/213`, `bugs_open/083`/`093`/`115` (the built-then-undispatched family this closes an instance of)

### VIZ-018 — Cascade attribution: the render audit records WHICH declaration wins, and proves it by removing it
- **status:** BUILT + COMMITTED 2026-08-25 (`bugfix_390_cascade_attribution`). **INERT until BOTH images roll** — the probe is in the browser-runner/render-audit image and the routing is in agent-chassis; until the adapter ships, `summary.cascade_scheme` is absent and the filer's whole block no-ops, so the spec is byte-identical to today's. Council submission alongside the commit (`Council-Submitted:` trailer).
- **status:** > **DATED UPDATE 2026-08-26 (same lane): LIVE ON ALL THREE SERVICES AND COUNCIL-APPROVED — the "INERT until BOTH images roll" line above is SPENT.** The fleet rolled at ~23:11 UTC on 2026-08-25 (stamp `2fb40a96`); `ea64845e0` confirmed an ancestor with `HEAD` as a NO control, both browser images log the stamp in their provenance line, and the chassis was probed at the binary (`grep -aq` of the stamp in `/proc/1/exe`, absent-sha control returning absent). Council APPROVED corr `058b59b6` at 16:22 on 08-25. **Consumed by migration 635 (applied 2026-08-26)**: css-patch-agent now parks `repair_surface='unreachable'` before any LLM spend and renders `override_requirement` into its prompt. ⚠ **NOT yet exercised: zero cascade-attributed rows exist** — no render audit has run since the roll (rotation had no site past its 3-day window as of 08-26 ~08:00). A reader after ~2026-08-29 should expect attributed rows; their absence THEN is a defect, their absence NOW is the calendar.
- **status:** > **DATED UPDATE 2026-08-26 ~15:00 UTC (same lane): EXERCISED — first post-roll audit ran and the whole path fired end to end.** remortgagecalculator.uk, 13:50 UTC: 5 findings, `cascade_scheme_present TRUE`, 1 attributed (`theme`, winner `(0,1,1)` in a page block, `strictly_greater`) + 4 `unattributed`. All five repaired by 14:34 (theme v10→v14, served sha = git sha = DB sha; 6.0:1 / 4.18:1 by independent arithmetic). The attributed repair MET the requirement — `body .brief-explanation__heading EM` `(0,2,1)`, the platform's checked `override_example` verbatim. **Two limitations now MEASURED, not just designed:** (1) **inherited colour is structurally unattributable** — the failing `<p>` had no `color` rule of its own (`.footer-bottom` on the ANCESTOR decides it; the only matching candidate resolves to `inherit`, so removal cannot move the value) ⇒ `verified:false` ⇒ `unattributed`, and this is chrome, so it recurs on every page of every site (4 of 5 rows in the first audit). Candidate extension (NOT built): when no removal moves the value and the element's computed colour equals its parent's, run the same removal proof one level up. (2) **`needs_important:false` was IGNORED in its first exercise** — the agent kept `!important` despite the block's "Do NOT"; the general 616 guidance near the prompt's end outvoted the measured section near its start (**n=1 as of 2026-08-26** — if the 08-27 attributed rows repeat it, fence 616's bullet on `{{if not .override_requirement}}` so only one instruction ever renders). Lane evidence: NOTES §(n)–(p).
- **status:** > **DATED UPDATE 2026-08-31 (same lane): GRADED END-TO-END AND THE BUG IS CLOSED — `bugs_closed/390`.** P2 (repair meets the measured requirement): 16/16 theme-branch repairs post-fence, every selector strictly above its requirement, served=git=DB on every graded site. P4: ~60 attributed pairings across 7 sites, `theme`+`strictly_greater` dominant per pairing, **0 `unreachable`**, zero probe-blindness discriminators anywhere. P3 (the loop closes): the 08-29 re-audits of all three repaired sites re-filed **none** of the repaired pairings (evidence chain: lane NOTES §(aa)). Limitation (2) of the 08-26 entry is FIXED: mig 655 fenced 616's general bullet on the absence of `override_requirement`; both branches proven in production within 12 minutes of apply (16/16 discrimination since). Limitation (1) — inherited colour is structurally unattributable — STANDS as the designed under-claim; it is a per-SITE constant (chrome either inherits or it doesn't), the probe-extension sketch below remains NOT built. The `unreachable` park (`parked_by='css_cascade_unreachable_390'`, mig 635): **0 rows ever** as of 2026-08-31 — built, live, never yet needed. The counters-have-no-reader gap below still stands.
- **status-evidence:** 8 guards MUTATION-PROVEN, each reverted to its plausible-but-wrong form with the named test confirmed RED and the file then diffed byte-identical: strict→non-strict for a page-block winner; `!important` parity dropped; an important inline attribute treated as reachable; the `verified` early-return removed; the `cascade_scheme` skew gate made unconditional; the `item_key` built from the winner instead of the filed selector; the in-page restore CHECK replaced with `true`; the removal proof deleted. Full `go build ./...` clean, both suites green.
- **what:** For a text element the audit has already measured as failing, the probe finds the declaration that actually decides its `color` and records: `surface` (`linked` / `style_block` / `inline_attr` / `ua_default` / `opaque`), the winning rule's own selector text, `!important`, whether the site theme's `<link>` follows the winner in document order, the specified value, and `verified`. The chassis turns that into `spec.repair_surface` (`theme` / `unreachable` / `unattributed`) and `spec.override_requirement` (`min_specificity` computed with **cascadia**, `strictly_greater`, `needs_important`, plus an `override_example` the platform has CHECKED with `satisfiesRequirement` rather than one it hopes is right).
- **⚠ THE LOAD-BEARING IDEA, and the reason this is not just another walker: REMOVAL IS THE PROOF.** Walking `document.styleSheets` cannot see everything — cascade layers, a cross-origin sheet, `@media`/`@supports` conditions, selector lists. So the walker's ordering is NOT the claim. The probe removes the declaration it believes wins and re-reads the computed value: **removing a LOSING declaration cannot change the computed value, so if the value moves, the removed declaration WAS the winner.** That is sound whatever heuristic chose the candidate, which is why the in-page specificity guess is allowed to be rough — it only decides what to try first. Every removal is restored and the restore is re-checked; a restore that does not take latches the page dirty and attribution stops there. It **under-claims by construction**: when the winner and runner-up specify the same value, removal changes nothing and the result is `verified:false`.
- **⚠ `verified:false` IS NOT A WEAK YES.** The route is `unattributed` and no requirement is written. A guess routed on is worse than the blindness this replaces, because a router acts on it.
- **why it matters:** `write_render_audit_findings` files every `contrast_failure` with `handlerAgent: "css-patch-agent"`, which APPENDS to the site theme — a file that, for a page-level `<style>` block, is always earlier in source order by construction (`assemblePage` emits those inside `<main>`, after the `<link>`). Nothing told the platform whether the theme could govern the pixel. **[MEASURED 2026-08-25, 40 random completed real-selector findings across 7 sites]** the winning declaration was in the page block and OUT-specified the filed selector in **33**, in the page block below it in 6, **in the theme in 0**, not located in 1 — near-uniform `(0,1,1)` filed against `(0,2,0)` winning. Damage: **75 of 151** pairings that ever reached `complete` were filed again, **97** re-filings byte-identical in `fg` and `bg`. `bugs_closed/390`.
- **the `item_key` does NOT change, deliberately:** VIZ-016 records that the key embeds the selector and that changing it already forced two alias-key retrofits in the retraction path. Every new fact rides the spec, and a test pins the key byte-identical with and without attribution.
- **⚠ FOREGROUND ONLY in this cut, and that is a stated boundary rather than an oversight.** Attributing the effective BACKGROUND needs the node `effBG` stopped at, which is inside the shared `contrastMathsJS` kernel — byte-identical to the string the `contrast_ratio` check also runs, and not to be changed from here.
- **⚠ the counters have no reader yet**, the same standing gap as `skipped_unverified_selector` (352 §4(3)): `cascade_attributed` / `cascade_unattributed` / `cascade_unreachable` / `cascade_scheme_present` live in the action's result map and the log line, nowhere else. `cascade_scheme_present` is the discriminator that matters — three zeros with it FALSE means "this adapter cannot attribute", three zeros with it TRUE means "it attributed nothing".
- **sources:** `internal/adapters/browserrunner/cascade_attribution.go` (`cascadeAttributionJS`, `CascadeWinner`, `cascadeSchemeV1`); `internal/adapters/browserrunner/render_audit_action.go` (composition, per-page cap, counters); `platform/orchestration/actions/contrast_cascade_route.go` (`contrastRepairRoute`, `specificityOf`, `satisfiesRequirement`); `platform/orchestration/actions/write_render_audit_findings_action.go` (spec write, gated on `cascade_scheme`); `bugs_closed/390`; lane `docs024_key_docs_latest/bugfix_390_cascade_attribution/`
- **relations:** VIZ-016 (the selector contract this rides beside, and whose key it must not disturb), VIZ-012/VIZ-013/VIZ-015 (the measurement and its cadence), WII-016 (the retraction that closes these findings — untouched by this change), `bugs_closed/390` (the bug), `bugs_open/396` (the other way a repair goes inert — erasure, not misdirection), `bugs_open/296` §10.5

### VIZ-017 — the three GENERIC structured components: `checklist`, `period-calendar`, `comparison-table`
- **status:** **LIVE 2026-08-24** — migrations 604/605/606 applied and recorded; council `c134b0e9` APPROVED (1 advisory objection; both MEDIUM objections acted on before applying). Each has a refusing rollback sidecar that declines if any live `page_components` row uses the component. **Live, but UNEXERCISED: no page has yet been built with any of them.**
- **status-evidence:** all three dry-run applied and rolled back inside one transaction against the live DB; `component_expresses` returns `{items,list}` / `{items,list}` / `{items,table}` respectively, i.e. each is visible to the planner listing (PLAN-053) as the thing it claims to be. Rendered through `text/template` with `Option("missingkey=zero")` — the chassis's own semantics — across **11 cases**: three happy paths (checklist 4 `<li>` in one `<ul>`; period-calendar **12** `<li>` in one `<ol>`; comparison-table `table=1 tr=5 td=12 th=8`, balanced) and eight degraded (two-column table, a row omitting a declared cell, empty-string column labels, minimal checklist, a required field absent on each component, empty arrays).
- **what:** The generic structural vocabulary the library never had. `checklist` — unordered criteria, real `<ul>`. `period-calendar` — ordered NAMED periods (months, quarters, seasons, weeks), real `<ol>`. `comparison-table` — options × two-to-four writer-named criteria, real `<table>` that stacks into labelled blocks under 40rem via `data-label` + `::before`. All three follow VIZ-006's header discipline: **no numeric, price, rating, score or rank field exists in any of them** (asserted by regex over the schema in each verify block), no `<svg>` (text inside one is invisible to the claims gate), `<h3>` not `<h2>` for item headings, theme variables with neutral/translucent fallbacks, and `{{.InstanceID}}` from birth (RFC_032).
- **why it matters:** PLAN-053 made the planner able to SEE what each component can express, and that was recorded as **necessary but not sufficient** — `[MEASURED 2026-08-24]` all 44 structural components were special-purpose (directories, trackers, calculators, quizzes, spec sheets, one pricing table, two carousels, and `site-footer`, which is chrome), so an informed planner still had nothing generic to choose. These are what it can now choose. Motivating cases, all from one build: a page headed *"What your shed needs, month by month"* that shipped four prose blocks and three incidental month names; a 300-word paragraph that was a checklist written as prose; and **zero `<table>` elements across seven pages on a site whose own classification is a buying-guide comparison hub.**
- **another workstream could call this:** yes — they are ordinary library components, reachable by any planner on any site, with no vertical, product or site assumed.
- **⚠ REUSE WAS CHECKED FIRST AND IT REMOVED A COMPONENT.** A fourth, "steps", was **dropped** after reading VIZ-006: `mechanism-flow` already draws an ordered process with decision branches. A checklist is a different shape (unordered, each item independently true, no flow between them) and a calendar is a third (periods do not cause one another, and a reader jumps to the one they are in — rendering a calendar as a mechanism would imply a dependency that is not there). `features`/`differentiators` (marketing claims as cards), `faq` (Q&A pairs), `pricing` (tiers, brochure sites) and `gripper-spec-sheet` (one product) were each examined and rejected for a reason recorded in the seed headers.
- **⚠ `period-calendar` IS NOT A TIMELINE.** The boundary is **derived from** the `editorial_design_uplift` lane's own description of Phase E, and **not confirmed by them** — they have been asked (2026-08-24); an earlier draft of this entry and of migration 605 said "agreed with", which was false and is corrected here rather than quietly. Their Phase E timeline is FACT-FED: dated real-world events, each carrying its own citation, failing closed without an evidence base. This is the opposite by design — authored guidance about a RECURRING cycle ("what to do in March", not "what happened in March 2024"), no dates, no citations. A site may legitimately have both. **If you are putting a year in a period label, you want their component.**
- **⚠ THE CLAIMS RISK ON `comparison-table` IS STATED, NOT SOLVED.** VIZ-006's rule — the absence of the slot is the control — cannot be applied wholesale to a comparison component, because one with no comparable cells is not a comparison component. What IS structural: no price/rating/score/rank field exists to fill. What is NOT: the cell guidance, which is a prompt instruction and therefore not a control (owner ruling). Cells are free text under the writer prompt's rule 14, and on the **29 of 48** sites with no evidence base nothing audits them. This component makes a pre-existing estate-wide exposure easier to reach; it does not create it and does not fix it. The honest dependency is `bugs_open/380`.
- **⚠ NAMED CELLS, NOT A POSITIONAL GRID — a reliability decision, not a style one.** The obvious schema (`columns[]` + `rows[].cells[]`) misaligns silently when a model emits four columns and a row of three cells: every subsequent cell shifts and the DB row looks valid. Named cells mean a missing value renders empty **in place**. The cost is a four-column maximum, which is also the most a phone can carry.
- **sources:** `sql_for_agents/604_checklist_component.sql`, `605_period_calendar_component.sql`, `606_comparison_table_component.sql`; `bugs_open/381`; lane docs `docs024_key_docs_latest/bugfix_381_inexpressive_composition/`
- **relations:** `component_expresses` and the planner menus that surface these (site-plan-and-reconciler PLAN-053); `mechanism-flow`, the sibling whose rules these follow and whose existence removed a fourth component (VIZ-006); writer-side field guidance (content-quality CQ-031); the 581 birth gate they satisfy (component-lifecycle CLC-029)
- **verify-later:** whether a real build selects any of them; whether a model reliably emits twelve well-differentiated periods rather than twelve near-identical ones (the most likely schema weakness, named as a risk in the submission); whether the mobile stack renders correctly on a device, which markup inspection cannot prove

### VIZ-019 — The render audit's COVERAGE CURSOR: a capped sweep rotates, and the pages awaiting a grade ride every run

- **status:** **LIVE AND ACCEPTED 2026-09-03.** Both halves: the cursor (chassis `v1.0.1354`, migration **660** applied 2026-08-26 22:20:40Z) and the commissioned reader (CronJob `render-truncation-check`, 07:50 UTC). Councils `f67593f5` and `f49da30d` both APPROVED. `bugs_closed/394`.
- **status-evidence:** `go test ./platform/orchestration/actions/...` green including the two pre-existing truncation tests, whose ASSERTIONS are unchanged (only the sqlmock column shape moved, because the query now selects the ordering columns it already ordered by). Four guards mutation-proven, each restored green after: cursor written from the union instead of the rotation slice; `priority_*` context keys emitted only when non-zero; restart-from-the-top on a deleted cursor page; and the first-`#` key split (see the ⚠ below).
- **what:** `request_render_audit` caps a site's sweep at `max_pages` and took the **first** `max_pages` pages of `ORDER BY COALESCE(nav_order,999), name` **on every run**. This adds an opt-in keyset cursor (`rotate_coverage`, default **false**) in that same ordering, stored per `(site_id, agent_type)` in the new `render_audit_page_cursor` table, advanced at DISPATCH and written AFTER a successful produce. The run's window is a **UNION**: the pages carrying an open `contrast_failure` (bounded at `max_pages/2`) plus the next N−k pages from the cursor. The durable `RENDER_AUDIT_TRUNCATED` row gains `coverage_mode`, `window_first`/`window_last`, `cursor_cleared`, `audited_paths` and four `priority_*` keys, and its MESSAGE is mode-split.
- **why it matters:** the pages past the cap were not audited less often — they had **never** been audited and never would be. `[MEASURED 2026-08-26]` webdesign.co.uk was **146 live pages / 60 audited / 86 never**, and the tail was a whole CLASS: its `nav_order` bands are 0..90 (6 nav pages), 100 (94 tools, alphabetical), 200 (**48 `tool-*-guide`**), 201 (1), so the cap cut between `tool-head-architect` and `tool-html-minifier` and **every remaining guide page was unreachable at any cap below 98** — on a site that grew 15 pages in two days. `bugs_closed/242` had already raised the cap 25→60 as a stated mitigation and it had been outgrown.
- **another workstream could call this:** yes, and this is the reusable part — **any capped per-site sweep can adopt the same shape**: keyset cursor in the sweep's own ORDER BY, keyed on `(site_id, agent_type)`, advanced at dispatch, plus a priority set defined as *"whatever THIS run's reply can grade"*. Set `rotate_coverage: true` on the step. It is deliberately NOT a shared helper yet — one caller uses it — but the table and the arithmetic are general and the second adopter should lift them rather than copy them.
- **⚠ THE WINDOW IS A UNION BECAUSE A PLAIN CURSOR WOULD HAVE BROKEN AN OWNER RULING.** Migration `469` (owner instruction, 2026-08-18) cut the rotation's re-visit window 7d→3d because *"the render audit is the only thing that GRADES a contrast repair … its eligibility window is therefore the confirmation latency of the whole repair loop"*. A plain cursor takes webdesign's per-page latency to 3d × ceil(146/60) ≈ **9 days** — worse than the seven the owner ordered removed. It dissolves because the protected population is not the site but the pages awaiting a grade: `[MEASURED 2026-08-26]` **3 of 146** on webdesign (fleet maximum 17, on a 37-page site that never truncates at 60). Cost: 3 of 60 slots. **Preserved: grading latency. Traded: new-defect DETECTION on the head pages, from 3 days to one cycle — in exchange for the tail going from never to one cycle.** State both halves.
- **⚠ NEVER PARSE A `contrast_failure` `item_key` TO RECOVER ITS PAGE — this is what the council caught.** Round 1 returned REVISE naming the LANDMINE on this footprint ("the render-audit package now holds TWO selector-composition schemes"). Both halves of the hazard are LIVE: a **selector** may contain `#` (`…/index.html#BUTTON#c-tool-…`, 1 of 469 open rows; the `describe` scheme emits `tag#id.classes` by construction), and a **page URL** may contain `#` — `idea.uk` has BOTH `/tools.html#audience-check` and `/tools.html` ACTIVE with 35 open rows, so a first-`#` split attributes a finding to a *different real page* and nothing errors. Match **forward** with the grader's own construction, `workItemKey("contrast_failure", path+"#")` then `HasPrefix`, longest path first (`write_render_audit_findings_action.go:748`).
- **⚠ THE PRIORITY SET HAS EXACTLY ONE ITEM TYPE BY CONSTRUCTION, NOT BY CHOICE.** It is *"the pages whose open findings this reply can close"*, and `write_render_audit_findings_action.go:791` makes exactly one retraction call on this path. `undeployed_asset` is excluded three times over: nothing here grades it; its key is `undeployed_asset:<asset_id>` so there is no page (`[MEASURED 2026-08-26]` all 190 open rows carry a NULL `page_id`, against 111 of 111 for `contrast_failure`); and its verifier is elsewhere. **Do not widen the set without first finding a second retraction call on this path.**
- **⚠ THE BOUND IS THE DESIGN.** The priority set is capped at `max_pages/2` so the rotation always keeps at least half the window and coverage can never stall — without it, a site with more finding-bearing pages than the cap would spend every run on the same pages, which is the deterministic-prefix disease reappearing *inside its own fix*. The over-budget drop is selected cyclically from the cursor, named in the log and counted in the durable context.
- **sources:** `bugs_open/394`; `sql_for_agents/649_render_audit_coverage_cursor_HOLD.sql` (+ `_ROLLBACK`); `platform/orchestration/actions/request_render_audit_cursor.go`; lane docs `docs024_key_docs_latest/bugfix_394_render_audit_rotation_cursor/`
- **relations:** VIZ-015 (`site-render-audit-rotation`, the cadence this paginates) · VIZ-016 (the selector contract whose two schemes are why the key must not be parsed) · `bugs_closed/242` (made truncation loud; this consumes that signal) · migration 469 (the grading-latency ruling the union preserves) · DBG-075 / `RENDER_AUDIT_TRUNCATED` now `consumed` by `cmd/config-key-audit/rendertruncation.go` · WFA-013 (the optional-key budget: this took `request_render_audit` to 7 of N=10, and the CronJob literal had to move with it)
- **verify-later:** whether the union of `audited_paths` over one cycle actually reaches all of webdesign's live pages (the acceptance arm, runnable only after 649 applies); whether `design-critique-agent` should rotate or curate (owner question, deliberately not taken); and whether the ~9-day head-page detection latency is acceptable to the owner or `max_pages` should rise now that it is a pure latency dial
