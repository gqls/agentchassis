# Register — claims-verification

> **covers-through: 2026-07-28** · written 2026-07-27 from first-hand code/DB reads, never part of the extraction.
> Everything else dates from the 2026-07-13 extraction freeze — absence
> here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074). It said **18** and the file held **20**._ (CLM-001..018). **NOT from the 2026-07-13 extraction** — this whole subsystem shipped
after extraction froze (its first plan is dated 2026-07-16), so none of it was
ever in the register. Added 2026-07-27 from the oufe.com workstream, grounded in
live code and DB reads made that day; every citation below was read first-hand,
not carried from another document.

This is the **third** recorded instance of the same gap (after FIX-051/052/053 and
MDL-038/039), and by far the largest — an entire honesty subsystem, V0 through V5.
See `bugs_open/106` for why it keeps happening.

Status vocabulary per `README.md`. Where a thing is *built but never exercised*
that is said explicitly, because "deployed" would overstate it.

### CLM-001 — `evidence_base` is a `site_specs` aspect, not a table
- **status:** deployed
- **what:** The per-site fact register lives at `site_specs.aspect='evidence_base'`, one current row per site under a partial unique index (`site_id, aspect) WHERE is_current`. So it is **never UPDATEd in place** — every change is supersede-then-insert in one transaction, carrying `pinned` forward or the row silently loses human-owned status. Shape (`datahelpers/claims.go:97-103`): `audit_doc`, `governing_rule`, `facts[]`, `banned_claims[]`, `allowed_entities[]`, plus a `writer_block` string the parsed struct does not model.
- **sources:** `platform/orchestration/datahelpers/claims.go:97-130`; `docs024_key_docs_latest/claims_verification/RUNBOOK_claims_verification.md` §1-2
- **relations:** CLM-002, CLM-009, CLM-010; site-spec-and-classifier register
- **verify-later:** the `pinned` carry-forward on every supersede path

### CLM-002 — `ScanBannedClaims` is an unrestricted case-insensitive regex over prose
- **status:** deployed
- **status-evidence:** function body read 2026-07-27 at `claims.go:284-325` — blocks × patterns, `FindStringIndex`, nothing else.
- **what:** The banned-pattern scanner has **no numeric gating whatsoever**. It matches whatever patterns a site is given, about anyone, numeric or qualitative. Live registers already carry purely qualitative patterns (`leaderboard`, `live now`, `price target`, `years of experience`). Patterns compile as `(?i)`; an invalid regex degrades to a literal substring, so a typo never silently drops a ban. It scans *assertion blocks* extracted from HTML (`extractAssertions`, `claims.go:165-226`), which skips `script/style/head/code/pre` and cannot match across block boundaries.
- **why it matters:** this concept exists because its absence caused a live error. On 2026-07-26 a session concluded that qualitative claims were "invisible to every scanner", and wrote that into a live council seat, having read only the *numeric* scanner beside it. **The capability was never missing.** See CLM-003 for the limitation that was mistaken for a general one.
- **sources:** `claims.go:284-325`, `:121-128`, `:165-226`; `sql_for_agents/226`, `227`; `docs024_key_docs_latest/WRONG_CALLS.md` 2026-07-26
- **relations:** CLM-003 (the confusable neighbour), CLM-010, CLM-011

### CLM-003 — `ScanUnregisteredNumbers` is gated by an English business-noun window
- **status:** deployed, with a documented blind spot
- **what:** The unregistered-number scan inspects a number only when `businessClaimContextRe` (`claims.go:339`) matches its surrounding window — clients, customers, records, awards, uptime, years of experience and similar — and `isExcludedNumber` (`:408-494`) then drops years, dates, versions, ratios, measurements and **currency amounts**. Consequence: it is near-inert on non-English copy (recorded for relojistas.com) and on finance prose, where "£16bn of Class A debt" is never scanned at all. **Both gates apply to this function only, never to CLM-002.**
- **status-evidence:** call-site census 2026-07-27: `businessClaimContextRe` and `isExcludedNumber` are referenced only within `ScanUnregisteredNumbers` and the stat lane; zero references inside `ScanBannedClaims`.
- **sources:** `claims.go:329-349`, `:363-372`, `:408-494`; `traffic_probe/relojistas_evidence_base.sql` limitation header
- **relations:** CLM-002 — **do not generalise this entry to the sibling scanner; that is the exact error already made once**

### CLM-004 — Two enforcement surfaces: build gate (blocker) and post-deploy sweep (high)
- **status:** deployed; the post-deploy half has no cadence
- **what:** V1a runs inside `validate_page_content` check 8 over draft HTML before save — a banned-claim hit is severity **blocker** and fails the page build; an unregistered number is **error**, deliberately never a blocker. V1b (`discovery_checks/check_unverified_claims.go`) scans **stored `rendered_html` of live pages and site chrome**, skipping locked components, raising `claims_unverified` at **high**, terminating at `needs_human_review` with no handler by design. Both are opt-in by the mere presence of an `evidence_base` row: `loadEvidenceBase` returns nil and every lane silently no-ops.
- **the gap:** V1b is reachable only via `quality-discovery-agent` ← `improvement-loop` ← the `improvement-sweep` scheduled task, **disabled since 2026-05-02**. So the estate's only automatic detector of published-content drift effectively never runs.
- **UPDATED 2026-07-28 (CLM-015):** "both are opt-in by the mere presence of an `evidence_base` row" is **no longer true of the banned-claim half**. It now runs on every site, register or not, on both surfaces. The unregistered-number half is unchanged and still opt-in, so the two halves of check 8 have **different** opt-in rules by design — do not simplify that back.
- **sources:** `validate_page_content.go:274-282`, `:926-958`; `check_unverified_claims.go:135-150`, `:365-389`; `bugs_open/083` (detected-findings slug)
- **relations:** CLM-010; work-item-integrity register; improvement-loop register

### CLM-005 — V2: the writer whitelist bounds invention instead of forbidding it
- **status:** deployed
- **what:** When a site's register carries a `writer_block`, the page-content-writer prompt renders "## Verified Facts (the ONLY numbers and named entities you may assert…)", which **overrides** the writer's unbounded "never invent" rule with a bounded "state only these". A fact without a `writer_line` is omitted from a composed block entirely — never auto-phrased.
- **landmine:** a **`writer_block`-only register switches both deterministic checkers off silently**, because `ParseEvidenceBase` returns nil when `facts[]` and `banned_claims[]` are both empty.
- **sources:** `claims.go:110-130`; `refresh_evidence_base_action.go:485-489`; `robot-hands` 043 workstream notes
- **relations:** CLM-001, CLM-004

### CLM-006 — V3 `claims-auditor`: an LLM prose auditor, live, unscheduled, never demonstrated
- **status:** ~~partial~~ **SUPERSEDED 2026-08-24 by CLM-025/CLM-027 (bugs_open/380): seeded (`claims_verification/SEED_claims_auditor.sql`), scheduled (`claims-audit-rotation`, 3600s/7-day), and DEMONSTRATED on the motivating site — the cold audit's first two findings were the owner's own quoted sentences.** Before that day the auditor had ONE `llm_call_log` row in its life (2026-07-18, `[]`) and its work-item step had never fired; every `claims_unverified` item came from the Go check.
- **status-evidence:** "LIVE 2026-07-18; not on a cadence (owner call)"; "the auditor's catch path isn't yet demonstrated — I wasn't going to plant fabrications on a live site to test it".
- **what:** Classifies every prose assertion as supported / could-be-framed / unsupported against the register, citing the supporting fact id — deliberately the lane that "catches what regex can't: prose claims with no number in them". Reports at most 12, worst first. Findings go to humans; nothing is auto-fixed.
- **provenance warning:** **this agent exists only as a DB row.** No seed file created it and none exists in git history; `agent_definitions.default_config` is the sole source of truth for its prompt. Any account of its behaviour in docs is second-hand.
- **sources:** `claims_verification/NOTES_claims_verification.md:266-279`; `SPEC_claims_verification.md:167-175`; `PLAN_2026-07-16:140-141`
- **relations:** CLM-011 — it is the lane most likely to catch overclaims about ourselves, and the least exercised

### CLM-007 — V4 freshness: the one part of the layer that sweeps the fleet daily
- **status:** deployed
- **what:** `refresh_evidence_base` re-runs sql-sourced facts, updates values, raises `stale_evidence` on drift **including under-claiming**, and writes back with compare-and-swap so a human edit is never lost. Driven by the `evidence-freshness` scheduled task, enabled, daily, and when given no `site_id` it selects **every** site with a current register.
- **why it matters here:** this is the only claims-layer component with a live fleet cadence, which makes the layer *look* actively swept when only its fact-refresh half is (see CLM-004).
- **sources:** `refresh_evidence_base_action.go:172-199`, `:341-349`, `:680`; `SEED_evidence_freshness_scheduled_task.sql:45-48`

### CLM-008 — V5: citations survive only if the quote is still literally there
- **status:** deployed; first end-to-end completion 2026-07-27
- **what:** `verify_and_register_citations` re-fetches every cited URL and registers a claim only when its **verbatim quote** is present. The model proposes, the fetcher disposes. Three outcomes are held apart deliberately: quote found → stands; 200 but quote absent → `citation_lost` ("the published claim now rests on nothing"); fetch failed / 403 / PDF → `fetch_error`, **UNKNOWN, never drift** — "a paywall going up is not evidence the fact is wrong". Quote matching is forgiving on presentation (entities, curly quotes, nbsp, thousands separators) and strict on content.
- **status-evidence:** activated on v1.0.1140 (2026-07-20) but its smoke run failed on `bugs_open/047` and was never repeated; **first successful completion 2026-07-27**, 19 citations registered for oufe.com, most from `legislation.gov.uk`.
- **sources:** `platform/orchestration/actions/evidence_citations.go:9-21`; `datahelpers/citations.go:35-49`; `docs024_key_docs_latest/oufe/NOTES_oufe.md`

### CLM-009 — `EvidenceFact.Kind` is declared, documented, and read nowhere
- **status:** unknown → **confirmed dead 2026-07-27**
- **what:** The struct declares `Kind string // metric | capability | entity | attestation` (`claims.go:73`) and the spec repeats the vocabulary, but no code anywhere reads it — every `.Kind` consumer in the tree belongs to a different struct. A fact declared `capability` behaves identically to one declared `metric` or misspelled. `EvidenceSource` beside it (`:60-64`) already models exactly one of `sql | artifact | attested_by`, and CLM-007 already re-checks the sql-backed ones on a fleet cadence.
- **why it matters:** that is the shape of a **promise register** — a claim, its kind, the mechanism behind it, when last checked, and a sweep that re-checks it — sitting unused. Its invisibility (a field that governs nothing has no call sites) is why a session nearly proposed building a separate promise register in 2026-07.
- **sources:** `claims.go:60-87`; `SPEC_claims_verification.md:104,124-126`; `bugs_open/105`
- **relations:** hitl register; the EXPERIENCE_PLAN promise ledger (`sql_for_agents/167:161`)

### CLM-010 — `banned_claims` is per-site only, and the fleet-share decision is a lapsed deferral
- **status:** convention (a decision, not an artifact) — **precondition expired**
- **what:** Every reader of the register is keyed on `site_id`; there is no global row, no inheritance, no merge of a base set. `SPEC_claims_verification.md:250-252`, under *"Open questions for the owner"*, asks whether `banned_claims` should be fleet-shareable "(some patterns are universal)" and proposes **"per-site only until two sites have evidence bases"**. Correct at n=1. Measured 2026-07-27: 8 sites have registers and **only 5 of 15 live sites carry a single pattern** — the ten without include vetcomparison.uk (the fabricated-prices site) and idea.uk (the only one taking money).
- **the transferable point:** **a deferral with a numeric trigger and no watcher becomes permanent policy.** Nothing re-read it; the precondition lapsed silently.
- **precedent for the fix:** `datahelpers/voicetells.go` already solves this shape one file away — `globalTellPhrases()` (`:121-137`) unioned with the per-site list at `:109`.
- **UPDATED 2026-07-28 — the figure above has drifted and the fix now has a measured cost.** Re-run of the same query: **7 of 15** carry patterns (was 5; ai-agent-orchestration gained 10 and finetuning 3 by hand), **9** current `evidence_base` rows. vetcomparison.uk and idea.uk are **still at zero**. And the count conflates two states that decide the fix's reach: **6 sites have no row at all**, while **2 have a row with 0 patterns but non-empty `facts[]`** (robot-hands 5, gamesdesign 4) — those two parse **non-nil**, so the "union a global set into `ParseEvidenceBase`" fix reaches **9 of 15** sites, not 7.
- **the landmine, measured before the fix shipped:** dry-running the tested oufe set (10 patterns) over the stored `rendered_html` of all 15 sites (908 components, via CLM-014) produced **7 findings on 3 sites, of which 4 are FALSE POSITIVES** — every one firing on a *negated* sentence ("has **not** been independently verified", "**cannot** be independently verified", "**not** independently verified"). At severity `blocker` that fails a page build for making the honest disclosure this layer exists to encourage. One pattern (`(fully|independently|externally|properly) (verified|audited|fact.?checked)`) causes 6 of 7 hits and all 4 false positives; with it dropped the other 9 fire **exactly once** fleet-wide, on a true positive. **CLM-002's "no gating whatsoever" is safe only because each pattern was human-audited for one site — that review IS the false-positive apparatus, and going fleet-wide removes it while keeping blocker severity.** Nothing is biting today: every armed site scores 0 against its own register. No negation-guard prior art exists in the estate, and RE2 has no lookbehind, so a guard must be code.
- **sources:** `SPEC_claims_verification.md:250-252`; `PLAN_2026-07-16:138-139`; `bugs_open/104` § "Dry run 2026-07-28"; `docs024_key_docs_latest/bugfix_104_fleetwide_claim_patterns/`
- **relations:** CLM-002 (the premise that the audit was the guard), CLM-011 (the pattern family proposed for sharing), CLM-014 (how it was measured)

### CLM-011 — The overclaimed-reliability class, and the line that separates it
- **status:** deployed (2026-07-26/27)
- **what:** A promise about **our own** accuracy is a claim like any other and had never had a pattern written for it on any site. Controls now: a fleet-wide writer rule on both content writers ("never promise accuracy you cannot guarantee"), an `OVERCLAIMED RELIABILITY` contract on the compliance council seat with judge clauses, and tested banned patterns on oufe. The governing line, which generalises: **a site may describe what it DOES; it may not claim what that GUARANTEES.** "We cite every figure and date it" is a keepable process commitment; "a claim without a source does not appear here" asserts a completeness nobody can verify.
- **origin:** oufe.com shipped four such promises live, hours after the owner struck a weaker version of the same slogan.
- **sources:** `sql_for_agents/223`, `226`, `227`; `docs024_key_docs_latest/oufe/`
- **relations:** CLM-002 (the scanner that always could have caught it), CLM-006

### CLM-012 — `grounded-explainer`: a high-attention content lane that cannot publish
- **status:** deployed 2026-07-26, exercised 2026-07-27
- **what:** For pages whose facts must not come from model memory (law, regulation, safety, clinical, tax): search → extract atomic claims with verbatim quotes → CLM-008 verification → compose from survivors only → an **independent** grounding audit listing every untraceable sentence → terminate at `needs_human_review`. **There is no config flag that makes it publish** — the one property that does not depend on a model behaving well. Reuses the V5 acquisition chain wholesale.
- **status-evidence:** first full run 2026-07-27 produced a 7,546-char draft citing 3 sources with 3 declared gaps; the audit returned `needs_revision` naming two plausible legal generalisations the draft could not support, and correctly declined to flag its closing disclaimer.
- **landmine found in its own first runs:** the verification action returns a **receipt of citation ids, not the claims** — a composer handed that receipt correctly refused to assert anything and reported honest gaps. **The failure was safe**, which is the design point.
- **sources:** `sql_for_agents/224`, `225`; `docs024_key_docs_latest/oufe/NOTES_oufe.md`

### CLM-013 — series facts: many dated observations, each independently sourced
- **status:** deployed in `v1.0.1185` (pod-verified 2026-07-28) — **no live site holds a series fact yet**
- **status-evidence:** three distinctive string literals from `claims_series.go` found in the running binary, with a positive control. Added 2026-07-28, after this register file was first written.
- **what:** A fact could hold one `Value` and three dates — `accessed`, `published`, `verified_at` — **all provenance**, none of them the date the value *applies to*. `Observation{as_of, value, source, verified_at}` adds that, under two rules: **every observation carries its own source and never inherits the parent fact's**, and `as_of` is distinct from `verified_at`. `ValidateSeries` fails closed (≥2 observations, valid and unique `as_of`, a resolvable source per point); `numberSupported` gained a series branch, matching exactly even when the fact carries a `gte` tolerance.
- **why it matters:** without a series shape the only options were one fact per year (no series identity, so nothing can plot it and nothing knows a point is missing) or numbers embedded in a claim string, which is invisible to every scanner — the `bugs_open/043` failure mode. And the never-inherit rule is what stops interpolation entering as data.
- **the lesson attached to it:** round 1 of the council review found that `ValidateSeries` enforced the source rule while `numberSupported` never called it, so an unsourced observation still registered its value. **A rule enforced only in a validator is not enforced** — it must hold at the gate that decides. Both now share `observationHasResolvableSource`.
- **sources:** `platform/orchestration/datahelpers/claims_series.go`; `claims.go` (`Observations`, `numberSupported`); council correlation `da40ddf0` round 1

### CLM-015 — the fleet-wide banned-claim set: nine patterns no site may assert about itself
- **status:** **LIVE on chassis v1.0.1196 and council-APPROVED** — `Council-Reviewed: 899ed92e-1bf7-4707-96d8-24f102aa14fa`, round 3 (12 reviewers, 5 abstained, 0 unreadable, no high-severity objection). Rounds 1 and 2 were REVISE; **round 2's gating seat was `debug_historian` at HIGH on the author's measurement method, not on the design**, and the guardian de-escalated rather than vetoing. **The two rounds produced three real changes that would not otherwise exist** — the reversal lever, the malformed-pattern guards, and the disarm warning below.
- **UPDATED 2026-07-29 — "INERT until the next chassis image" is no longer true.** The original entry said committed-but-inert, which was accurate for about half an hour. Pod-verified with markers this change created: `completeness-of-exclusion` → **3**, `verification-of-everything` → **1**, positive control `banned_claim` → 2, negative control → 0. **The fleet-wide banned-claim scan is now enforcing on all 15 sites' builds, armed or not.**
- **status-evidence:** verified before commit against the real corpus, not in the abstract — the shipped set over the stored `rendered_html` of all **15 live sites / 908 components** with **no register supplied**, i.e. exactly as an unarmed site is scanned: **0 findings**. Positive control the same run: **6 of 6** overclaim shapes still blocked with no register at all, and the four live sentences that the *candidate* set falsely flagged all pass. 8 unit tests in `claims_global_test.go`.
- **what:** `globalBannedClaims()` in `datahelpers/claims_global.go` — the claims-layer answer to CLM-010. `ScanAllBannedClaims(blocks, eb)` scans the fleet-wide set **plus** the site's own, deduping a pattern present in both, and is **nil-safe**: a site with no `evidence_base` row is still protected, which is the point — otherwise site sixteen is born unarmed exactly as site fifteen was. Wired at both enforcement surfaces (CLM-004) so they cannot drift.
- **the two design constraints that shaped it, both of which rule out the obvious mirror:**
  1. **It is NOT unioned into `EvidenceBase` at parse time**, though `voicetells.go` does exactly that for the voice layer. `EvidenceBase` is marshalled **back** to `site_specs` by `refresh_evidence_base_action.go` and `evidence_citations.go`, so seeding `eb.BannedClaims` would silently persist the fleet-wide set into every site's stored register through write paths that never intended to touch it — the trap `claims.go` already documents for `EvidenceFact.Kind`. The set is held outside any parsed register and joined only at scan time; `globalEvidence` is unexported so it cannot reach a writer.
  2. **`ParseEvidenceBase`'s nil contract is unchanged.** That nil is a load-bearing opt-in signal read deliberately by several lanes. Only the banned half goes fleet-wide; **the numeric scan stays strictly opt-in**, because its false-positive rate is why it is never a blocker.
- ~~**the pattern that is deliberately ABSENT, and the landmine:**~~ **SUPERSEDED 2026-07-29 — the guard was built and the pattern is ARMED. See CLM-017.** The original text stands as the reason the guard is shaped as it is: `(fully|independently|externally|properly) (verified|audited|fact.?checked)` was **excluded** as the strongest of the ten because it caused **4 of 7** findings in the fleet dry run, every one on a *negated* sentence ("has **not** been independently verified"), and RE2 has no lookbehind so the test could not live in the pattern. The set is therefore **ten patterns, not nine** (this entry's title is left as written; the count in it is spent). **UPDATED 2026-08-27 (bugs_open/414): ELEVEN.** ⚠ *This line said TWELVE for about an hour, written the same day, by the session that made the change. Ten + one new entry is eleven; the window widening is not a pattern. Caught by re-deriving from the code (`GlobalBannedClaimCount()` = 11, which is also what `TestGlobalSetIsWired` asserts and has always asserted) instead of trusting the arithmetic in the commit message — the exact failure this file warns about two lines up, where the title's "nine" is recorded as spent.* The `every <noun> is verified` entry's window went 30 → 60 and an indefinite-subject sibling was added (`everything (on this site|here|we publish|…) is checked`), because a served compliance claim slipped through on 38 characters of compound subject and on the word "Everything" not being "every"+a listed noun. `[MEASURED 2026-08-27, 2,405 live components: at window 30 that pattern fires ZERO times fleet-wide — it was inert; at 60 it fires once, and the new entry once, each on exactly the sentence it was written for, 0 on the other 2,403.]` The 2026-07-28 NOUN narrowing is untouched and pinned by a fixture ("Every component is verified against production" still passes).
- **AND THE "0 FINDINGS" HEADLINE IN THIS ENTRY IS SPENT (2026-07-29).** The status-evidence line above — 908 components, **0 findings** — was true and was an **artefact of that pattern's absence**. Armed, the same corpus yields **2 findings**, both robot-hands.com asserting its spec data is "independently verified" (`bugs_open/147`), plus **4 suppressed by the guard**. Two components will not rebuild until that copy changes. **When you quote a clean-sweep number from this register, name the patterns that were armed when it was measured.**
- **the other landmine:** pattern 2 (`(does not|doesn't|do not|don't) appear here`) is **itself a negative construction**, so "prices do not appear here because they change daily" would match. Zero hits fleet-wide today; it is the most likely source of the next false positive. Flagged in the code beside it.
- **the lever is LOUD when pulled (added 2026-07-29, council round 3):** the compliance seat noted the lever is a **silent** disarm path — flipped false it restores the pre-104 unarmed state, *including for vetcomparison.uk or idea.uk, the two sites the fix exists to protect*, and a register-less site loses its only banned-claim protection that way. The gate now logs at **Warn** when the set is disabled, naming the site and whether it has a register. **A lever and a visible lever are different things**; the guardian asked for the first and compliance for the second, and they only conflict if pulling it is quiet.
- **the reversal lever (added 2026-07-29, council round 2):** `check_claims_fleet_wide`, a `validate_page_content` config key **defaulting TRUE**. Off restores the pre-104 scan exactly — per-site patterns only, an unarmed site scanned by nothing — and because DB config is live immediately, a bad pattern can be withdrawn **fleet-wide in seconds without a build or roll**. Mirrors `repair_internal_links` in the same function, which exists for the identical reason on `bugs_open/079`. **Default ON is deliberate**: an off-by-default set would be inert and 104 would still be live. Withdrawing it does **not** disarm a site's own audited patterns — separate test. **The lever is at the build gate ONLY**, and that asymmetry is deliberate, not drift: `DiscoveryCheckContext` carries **no config map at all**, so giving the sweep a toggle means adding a field to a shared context — a new shared seam — and the sweep is unreachable anyway (CLM-004's gap).
- **the malformed-pattern guard (added 2026-07-29, council round 2):** `globalEvidence()` keeps `ParseEvidenceBase`'s fallback — an uncompilable pattern degrades to a literal case-insensitive substring — because panicking at init over a regex would be worse than one over-narrow pattern. **But that fallback is silent: no logger, no error path**, so a typo would quietly become a near-inert literal that still looks armed from outside. Two tests are what make keeping it safe: `TestEveryGlobalPatternIsAValidRegex` fails CI on an uncompilable pattern, and `TestNoGlobalPatternIsVacuous` catches the inverse and equally invisible failure — an empty pattern compiles happily and then matches **every** block, failing every build on every site.
- **the rule for changing it:** dry-run fleet-wide first (`claimscan -no-global`), and **put the NEGATION of every pattern in the pass-list.** The original set was tested 10-for-10 on fabrications and 13-for-13 on legitimate sentences and still shipped this class, because no test sentence contradicted a banned phrase. **And drop `sites.status` as a scoping variable when you measure** — the gate never reads it, so filtering by it measures a smaller world than the one being enforced on. Unfiltered, the surface is 908 components / 14 sites, all `deployed`; pool and system sites hold **zero** stored components, so the filter happened to exclude nothing (verified 2026-07-29, council round 2's gating objection).
- **architecture review:** **RFC 003** (`architecture_review/RFC_003_fleetwide_banned_claims_at_the_build_gate.md`), filed 2026-07-29 under owner ruling answer 1 — this seam changes what check 8 GUARANTEES (skip-silently → refuse) for the 6 sites with no register, so it met the RFC trigger even though the council had already APPROVED it. Affected consumers, told in their own register categories: `page-build-handler` (page-build-pipeline), `content-reviewer` (CQ-002), `tool-recreation-handler` (tool-lifecycle). `report-builder` is unaffected — it sets `check_claims:false`.
- **sources:** `platform/orchestration/datahelpers/claims_global.go`, `claims_global_test.go`; `validate_page_content.go` check 8; `discovery_checks/check_unverified_claims.go` `scanComponentClaims`; `bugs_closed/104` § "Dry run 2026-07-28"; `docs024_key_docs_latest/bugfix_104_fleetwide_claim_patterns/`
- **relations:** CLM-010 (the decision it discharges), CLM-002 (whose "no gating whatsoever" was safe only because a human audited per site), CLM-004 (both surfaces), CLM-011 (the class), CLM-014 (how it was measured); `voicetells.go` `globalTellPhrases()` is the shape it mirrors but could not copy

### CLM-014 — `cmd/claimscan`: run the live gate's own engine over exported page HTML, offline
- **status:** deployed (committed 2026-07-16), exercised fleet-wide 2026-07-28
- **status-evidence:** `go build ./cmd/claimscan` clean against the shared tree 2026-07-28; used that day for a 15-site / 908-component dry run whose positive control blocked 6 of 6 overclaim shapes and passed 3 of 3 legitimate sentences.
- **what:** An operator CLI that calls **the same shared scan functions as the deploy gate and the post-deploy sweep** (`ParseEvidenceBase` → `ExtractAssertionText` → `ScanBannedClaims` / `ScanUnregisteredNumbers`), against a TSV export of component HTML plus an `evidence_base` JSON file. So you can ask "what would this pattern set flag, on real copy, across any number of sites" **without deploying, migrating, or touching a site**. Prints `BANNED` / `NUMBER` lines and exits 1 when findings exist, so it works as a scripted acceptance gate.
- **why it earns an entry:** this is the tool that answers "measure the blast radius before you submit" for anything touching the claims layer, and it is the only way to test a candidate pattern set against copy other than the site it was written for. **A session on 2026-07-28 was about to build a second one** — it was found only by reading `sql_for_agents/226`'s verify footer to the end, not by grep. That is `bugs_open/106`'s failure mode landing on a tool that already existed.
- **landmines:** (1) the CLI prints the prefixes `BANNED`/`NUMBER`; `banned_claim` is the JSON `check` value and appears nowhere in the output, so grepping for it returns 0 on every site — a false all-clear. (2) An evidence file with `banned_claims` but no `facts[]` makes **every number** an unregistered-number finding; filter to `^BANNED`. (3) Some site copy is non-UTF-8, and plain `grep -c` returns **empty with no error** on those outputs — use `LC_ALL=C grep -ac`. (4) The documented export query uses `kubectl exec -i`, which **eats a `while read` loop's stdin** and silently scans only the first site.
- **UPDATED 2026-07-30 — landmines (3) and (4) both fired again on a session that had not read this entry, and (3)'s stated fix is INCOMPLETE.** Recorded here because the cost was ~15 minutes each time and the failure mode is a *clean scan*:
  - **(3) `LC_ALL=C` is not sufficient in the Claude Code shell.** `grep` there is a shell **function** wrapping `ugrep` with `-I` (skip binary), so a scan output containing one non-text byte returns **zero matches and prints nothing at all — not even `0` for `grep -c`** — with `LC_ALL=C` set or not. Exit code 1, no message, indistinguishable from a clean fleet. The working form is **`command grep -a`** (bypass the function), and the tell that you are in this trap rather than genuinely clean is that **`grep -c` printed nothing**: real grep always prints a number.
  - **(5) NEW — the export itself truncates mid-stream and the short file looks complete.** Two of fourteen sites came back short on one run (`finetuning.uk` 58 of 139 rows, `gamesdesign.co.uk` 30 of 67) with `kubectl` reporting `Copying stdout failed: read message: unexpected EOF` on stderr — which is easy to miss and does **not** make the file empty. A scan of that corpus is clean-looking and wrong. **Assert the row count against the DB per site before scanning, and retry until it matches**: finetuning.uk needed three attempts (77, then 113, then 139). The count query is the same predicate as the export.
- **sources:** `cmd/claimscan/main.go` (usage block carries the export SQL); `sql_for_agents/226` verify footer; `docs024_key_docs_latest/bugfix_104_fleetwide_claim_patterns/RUNBOOK_fleetwide_claim_patterns.md`
- **relations:** CLM-002, CLM-003 (the engines it calls), CLM-004 (the two surfaces it reproduces), CLM-010 (the decision it costed); `cmd/voicescan` is the sibling for the voice layer
- **relations:** CLM-001, CLM-003; VIZ-002, VIZ-003, VIZ-004 (visualisation-and-charts)
- **verify-later:** first live series on a real site; whether `ParseCitation` per observation per scan is acceptable at fleet scale

### CLM-016 — `ClaimSurface`: the page's structural type gates the prose number heuristic
- **status:** **⚠ ROLL CONDITION MET 2026-08-10, verified by build provenance.** Chassis `v1.0.1283` is built from commit `d3c09cc74` — read back from BLD-019's stamp on **both** replicas — and `git merge-base --is-ancestor` puts this entry's own cited commit(s) inside it. **The Go code IS in the running binary; every clause below about waiting for a roll is superseded.** This note makes no claim that the feature is EXERCISED — that is a separate condition on a separate clock. Original: committed 2026-07-28 (`3ddb4ed2d`, narrowed by `955832067`) — **in code, therefore INERT until the next chassis image is built and rolled.** Council **APPROVED** round 1, correlation `de4a19f5-8f03-4e74-92cb-c23c10ab829d`, 3 advisory objections, two acted on.
- **status-evidence:** measured with CLM-014 against **each of the nine opted-in sites' own live registers** over live `rendered_html`, before and after, over an identical export: **124 unregistered-number findings → 65**. The 59 suppressed are exactly the editorial-page ones (blog-post 46, tool 7, game 4, news-index 1, guide 1) and **`comm` reports zero findings newly appearing**. All were read individually; all are false positives.
- **what:** `datahelpers.ClaimSurface{PageType}` + `ProseNumbersAreClaims()`. `ScanUnregisteredNumbers` now **requires** a surface, and returns nothing on an editorial page type (`guide`, `blog-post`, `news-index`, `tool`, `game`). `businessClaimContextRe` is a **lexical** gate — it asks whether the words near a number sound like business — and on teaching content that question cannot be answered, because an explainer's worked example is lexically identical to a sales claim ("10,000 active players farming that item"). `pages.page_type` is the structural signal that separates them, and it was already in the row both call sites read.
- **the boundary, which is the part worth copying:** **only the heuristic is surface-gated.** `ScanBannedClaims` runs on every page type — a human-authored pattern for a known falsehood has no false-positive problem to protect against, and this layer's motivating catch (CLM-004, first live run 2026-07-16) was a banned claim **on a guide**. `ScanStatClaims` likewise, per its own structural-position argument. **A precision fix belongs to the mechanism whose precision is the problem, not to the surface it was noticed on.**
- **why a required parameter and not an optional variant:** a `...OnPage` sibling that defaults to the old behaviour lets the next caller silently inherit the blind scan — `bugs_open/093`'s shape exactly (one guarded call site, one nobody remembered). Requiring it makes the compiler visit every caller; it found three production call sites and four in tests.
- **the fail direction, stated deliberately:** the **zero value is UNKNOWN and is SCANNED**. Site chrome belongs to no page; a component reviewer holds no page record; a page may not exist yet. A scanner that has gone quiet and one that is broken look identical from outside, so an absent or unrecognised page type stays noisy. A page type nobody has invented yet is scanned too.
- **landmines:** (1) **The gate only knows the page type if the workflow loaded it.** `page-build-handler` and `tool-recreation-handler` run `load_page_record` (which selects `page_type`) before `validate_content`; `content-reviewer` does not, so it resolves UNKNOWN and behaves exactly as before. A fix like this is inert wherever the signal does not arrive — check the live `agent_definitions` workflow, do not assume. (2) **The membership bar is two-part and the second half is what the council caught: a measured false positive AND a body that is never marketing.** `section-index` passes the first and fails the second — two of its twenty pages are `about-index` and `contact-index` on gamesdesign.co.uk, i.e. marketing copy under an index name — so it is scanned, at the cost of 2 known false positives. `blog-index` was dropped as an unmeasured extrapolation (3 pages, zero findings even against an empty register). `report` is excluded because its 14 false positives are model numbers inside product names ("Schunk EGP 40-N-S-B — manufacturer specification") tripping on `verified` — a different mechanism that would have been fixed by coincidence. **An `-index` suffix is a naming convention, not a content guarantee.**
- **sources:** `platform/orchestration/datahelpers/claims.go` (`ClaimSurface`, `editorialPageTypes`, `ScanUnregisteredNumbers`), `claims_surface_test.go`; `validate_page_content.go` (`resolvePageType`); `discovery_checks/check_unverified_claims.go` (`scanComponentClaims`); `cmd/claimscan/main.go` (4th TSV field); `bugs_open/102`; `bugs_open/364` (the 2026-08-24 extension); `docs024_key_docs_latest/bugfix_102_page_type_claims/`
- **EXTENDED 2026-08-24 and VERIFIED LIVE 2026-08-25 (`bugs_open/364`)** — chassis `v1.0.1337`, build commit `635f2d32`; probed at the binary with a negative control (the pre-fix regex form reads **0**, the post-fix form 1, positive control 11) and `merge-base` puts both `a9002793b` and `0f9f7f3ff` inside it. Post-roll, export asserted 118/118: ai-agent-orchestration.com **36 → 16** findings, **zero** on any tracker/directory page. ⚠ `agent_error_log` shows 0 refusals since the roll and that is NOT yet evidence — all 6 pages built since were `guide`/`tool`/`blog-post`, types already excluded before this change, so it measures absence of demand. ⚠ And the change does NOT dispose of the two false items already in the human queue (`4405fb38`, `2f8f67dd`): the revalidator's claim-granular gate correctly reports `armGateClaimsStillPresent` — *the standard moved, not the copy* — which is a deliberate integrity control, not a defect. See `bugs_open/364` §6f–6g.
- **PHASE 2 — ✅ LIVE on `v1.0.1339`, 2026-08-25, proven at the artefact** (binary probe with a negative control on both replicas, PLUS a behavioural demand control: a synthetic unsupported claim injected into live `protocol-tracker` copy is FLAGGED in `hero` and `call-to-action` and SILENT in `protocol-tracker-listing` — the design, observed). Includes the pre-roll acronym fix `35f452a0e` (a digit inside `A2A`/`W3C` would otherwise have refused that page's build at error severity). ⚠ **`service_binary_capabilities` was STALE for this roll** — its newest row named a commit without Phase 2 while the binary carried it; probe the artefact. (`RFC_053`, commits `52958897f` + `fa0b513f1` + `35f452a0e`, council `3ed2b792` APPROVED / `64d852b4` submitted) — **THE GRAIN CHANGED, and the three page types came back OUT.** `ClaimSurface` gained `ComponentFunction` (from `content_components.function`, carried as `page_components.slot_name` / `sections_metadata.component_function` — **framework-authored data, never parsed from the LLM-generated HTML**, which is the whole security of the field). A new `thirdPartyDataComponents` map holds the three listing components; an unrecognised component falls THROUGH to the page-type question and decides nothing, so unknown still scans. All five construction points now pass it, and `validate_page_content` scans **per section** where `sections_metadata` is available — safe because no assertion block spans a component boundary (measured 4 pages / 21 components, exact block-multiset match, **before** the code was written). Result: 16 findings / zero on trackers, identical suppression to the interim, with the hero and call-to-action scanned again; emptying the new map returns exactly the original 36/20. ⚠ **Go — inert until the next chassis roll**; prove it at the binary, not at git.
- **the tracker/directory three, and this extension KNOWINGLY BREAKS the two-part bar recorded in landmine (2) above.** `adoption-tracker`, `protocol-tracker` and `model-directory` were added on measurement: `cmd/claimscan` over live `rendered_html`, each site against its own current register, export asserted row-for-row against the DB (115/115 on ai-agent-orchestration.com) — **44 findings fleet-wide, 20 of them on these three page types at ZERO precision**, every one a third party's figure in an aggregated listing (`rollout_scope Over 80% of Fortune 500…`, `200,000 onboarded users` — someone else's, `JSON-RPC 2.0` — a version string, and the digit `2` inside the acronym **A2A**). After: **36 → 16** on that site, zero survivors on any tracker page, all 16 genuine first-person claims retained.
- **what it gives up, stated rather than discovered later:** these pages are **NOT "never marketing"** — each carries a `hero` AND a `call-to-action` in the site's own voice, so page-grain gating blinds those slots too. **Measured loss today is zero, and NOT for the reason the author first assumed:** every first-person numeric claim on those slots says "orchestration**s**", while `businessClaimContextRe` carries `orchestration` **singular with no `s?`** — so they are invisible to the scan whatever the page type does. That is a *second latent defect of the lexical gate*, not a safety property, and it is recorded in `bugs_open/364` rather than fixed here. `TestTrackerPagesGiveUpTheirFirstPersonClaims` pins the cost so it stays visible.
- **the right grain is the COMPONENT, and that is Phase 2 (RFC, not built):** hero and call-to-action stay scanned while the listing does not. This is precisely what landmine (2)'s `report` clause asks for when it says excluding a page type "would fix those by coincidence, not by mechanism". 4 of the 5 `ClaimSurface` construction points can pass component identity in one line; only `validate_page_content.go` (the gate that actually refuses) needs real work, and `sections_metadata` in `CollectedData` is the reachable signal there. **`entity-directory` (4 pages) and `entity-page` (21 pages) were NOT added** — zero measured findings, and analogy is not a measurement; that is the same bar that keeps `blog-index` out.
- **relations:** CLM-003 (the scan it gates), CLM-002 / CLM-015 (the banned half, deliberately NOT gated), CLM-014 (how it was measured, both directions), CLM-004 (the two surfaces); `bugs_open/093` (the one-guarded-call-site shape the signature change avoids)
- **verify-later:** pod-grep after the next roll — marker `resolvePageType` (0 on the live pod 2026-07-28), positive control `scanComponentClaims` (2); and the residual the compliance seat named: **there is no ONGOING control for future page-type misclassification**, only this one-time check.

### CLM-017 — the negation guard: a banned PHRASE is not a banned CLAIM
- **status:** **⚠ ROLL CONDITION MET 2026-08-10, verified by build provenance.** Chassis `v1.0.1283` is built from commit `d3c09cc74` — read back from BLD-019's stamp on **both** replicas — and `git merge-base --is-ancestor` puts this entry's own cited commit(s) inside it. **The Go code IS in the running binary; every clause below about waiting for a roll is superseded.** This note makes no claim that the feature is EXERCISED — that is a separate condition on a separate clock. Original: **committed 2026-07-29** (`116fdffd8`), **INERT until the next chassis roll** — Go change, so the live fleet still runs the pre-guard matcher until an image ships. Council submitted (`8a41e1a5-e670-4e50-a875-f8418ee15738`), verdict pending at time of writing.
- **UPDATED 2026-07-30 by another lane (oufe), status only:** the roll happened. Chassis **`v1.0.1211`** was built from a HEAD that contains `116fdffd8`, confirmed by the linear history plus a direct pod-verification of a LATER commit (`f61dce806`) in that same binary — so "INERT until the next chassis roll" above is spent. **[INFERRED from build ordering, not from a marker for this change specifically]** — if you need it certain, grep a `negatedClaimMatch` string in the pod rather than trusting this line. Flagged because `CLM-018`'s claims floor depends on this guard being live to be safe at blocker severity.
- **status-evidence:** dry-run with `cmd/claimscan` (CLM-014, the gate's own engine) over the **full live enforcement surface — 919 components / 14 sites, all `deployed`**, fleet-wide set plus each site's own register: **2 findings, 4 matches suppressed by the guard**, 0 findings on the other 12 sites. 21 unit tests in `claims_global_test.go`. Surface re-derived with `sites.status` **grouped, not filtered** — the gate never reads it.
- **what:** `negatedClaimMatch` in `datahelpers/claims.go`. A banned-claim pattern matches a **phrase**, but what is banned is an **assertion** — "every figure is independently verified" is a fabrication, and "where a figure has **not** been independently verified, that is stated" is the hedged disclosure the whole layer exists to encourage, containing the banned phrase verbatim. At blocker severity the second one fails a page build for being honest. The guard scans backwards from each match for a negation cue and drops the match if it finds one, so a denied claim is not reported.
  - **clause-local, and that is the whole safety property.** The backwards scan stops at the first comma or stronger boundary (`.!?;:,<>` newline, en/em dash), so a negation in a *different* clause cannot launder an overclaim: "we do not use AI, and every claim here is verified" still blocks. Window is 64 bytes; the longest real form measured is 13 ("has not been ").
  - **applies to per-site registers too, deliberately.** One matching rule, not two that drift (CLM-004). **The council raised this at medium from three seats** (corr `8a41e1a5`, round 1) — the per-site lanes never asked for it — and the answer is structural, not just "zero impact today": **all 84 per-site patterns across the 7 sites that have any were enumerated live 2026-07-29. Exactly 5 are negative constructions — the only class the guard could disarm — and all 5 are oufe.com's, all 5 verbatim copies of fleet-wide patterns, and all 5 pinned by `TestGuardDoesNotDisarmTheNegativeConstructionPatterns`** (their negation sits INSIDE the match; the guard only reads text BEFORE it). The other **79 carry no negation at all**, so the guard can only affect them on a page that denies them — the intended behaviour. Measured effect today: **0 suppressions on any per-site pattern across all 919 components**. **Affected consumers, named and told** per owner ruling 2026-07-29 §3: oufe.com (28 patterns — notice in `oufe/NOTES_oufe.md`), leopardessconsulting.co.uk (19), ai-agent-orchestration.com (10), vonc.com (9), relojistas.com (9), fundamentallyai.com (6), finetuning.uk (3). **NOT 9 sites — that was my own submission's error**: 9 have an `evidence_base` row, but robot-hands.com and gamesdesign.co.uk carry facts with **zero** patterns, so they are unaffected.
  - **it changed `ScanBannedClaims` from first-match to all-matches.** Occurrence counts are now of **surviving** matches, so a block that asserts once and denies once reports 1, not 2. A page author's count is no longer inflated by the sentences they got right.
- **the cue list is NARROW on purpose, and the near-misses are the useful part:** `without` and bare `no` are **excluded**, because both appear as intensifiers — "Without exception, every claim is verified", "There are no exceptions: every claim is verified" — and including them would silently disarm a real overclaim. `rarely`/`seldom` likewise ("our data is rarely wrong" is still an accuracy claim). **Known residual, stated rather than guarded:** "no figure here is independently verified" would still be a false positive; no sentence of that shape exists in the live corpus. Contractions are matched as `letter + n't` rather than a stem list, because the stems are irregular (`can't` is ca+n't, `won't` is wo+n't) and a stem list gets one wrong silently; curly apostrophes are included because that is what a renderer emits.
- **the suppression is logged AT THE BUILD GATE, not only in the CLI (added 2026-07-29, council round 1, architecture seat at medium).** The first version made suppression observable only in `cmd/claimscan` — a tool someone has to think to run. That is the `bugs_open/093` shape ("one call site of a shared judgement gets the rigorous fix; the sibling stays heuristic") in a change that cited 093 as a reason to care. `checkBannedClaims` now logs every suppressed match at **Info** with the site, pattern and snippet, so a guard that starts eating real findings shows up in the build log of the page it happened to. Two tests pin it, including that the lever being OFF reports no suppression for a scan that never ran.
- **call sites, enumerated rather than asserted** (the council asked; "the gate" is not singular): **2 in production** — `validate_page_content.go:1012` (V1a build gate, blocker) and `check_unverified_claims.go:405` (V1b post-deploy sweep, high) — plus `cmd/claimscan`. That is the CLM-004 pair and nothing else. **`ClaimFinding.Occurrences` has no branching consumer anywhere**: every use is display-only (a description string, a work-item field, a printf) and there is **not one comparison or threshold** on it in the tree, so counting survivors instead of raw hits is a cosmetic change in the honest direction.
- **the suppression is OBSERVABLE, which is the reason the extra API exists:** `ScanBannedClaimsIgnoringNegation` (the pre-guard behaviour, kept reachable as a control) and `ScanAllBannedClaimsWithSuppressed` (raises + suppressed, the second list derived by **subtraction** so a second guard implementation cannot disagree with the first). `claimscan -show-suppressed` prints them as `negated` lines, never counted as findings. **A silent suppressor and a dead gate are indistinguishable from outside** — this estate has been bitten by that shape repeatedly, and a guard on the strongest pattern in the set is exactly where it would hurt most.
- **THE LANDMINE, and it is about testing, not about regexes:** the test that was supposed to prove negated copy was safe had been **passing for a reason unrelated to negation** — the only pattern matching those sentences had been removed from the set. A pass from a check that cannot fail gets quoted later as "negated copy is handled". Assert the **mechanism**: `TestNegatedCopyIsSuppressedByTheGuardNotByAbsence` fails if the guard is deleted *and* fails (with "VACUOUS PASS") if the pattern is deleted. It caught a real error on its first run — a fixture filed as one of the four negated false positives that contains no negation at all.
- **the second landmine — narrowing a pattern by reasoning can make it INERT.** The restored pattern first shipped subject-anchored (content noun + `is/are` within N chars) to dodge a hypothetical false positive on "our accounts are independently audited". Over 919 live components the anchored form matched **nothing at all** — 0 findings, 0 suppressions — while the bare form found **2 real overclaims**. An inert pattern looks exactly like a well-behaved one. **Dry-run the pattern you are replacing next to the one replacing it**, in the same run.
- **sources:** `platform/orchestration/datahelpers/claims.go` (`negatedClaimMatch`, `negationCueRe`, `scanBannedClaims`), `claims_global.go` (10th pattern, `ScanAllBannedClaimsWithSuppressed`), `claims_global_test.go`, `cmd/claimscan/main.go`; `bugs_open/147`; `bugfix_104_fleetwide_claim_patterns/RUNBOOK_…` §13–14
- **relations:** CLM-015 (the set it arms, and whose "0 findings" it retires), CLM-014 (`claimscan`, how it was measured and where `-show-suppressed` lives), CLM-004 (both enforcement surfaces share the one matcher), CLM-011 (the overclaim class), `bugs_open/147` (the two live findings it surfaced). Sibling shape: `isExcludedNumber`, the structural-exclusion guard the original note pointed at as the model.

### CLM-018 — the CLAIMS FLOOR: claims checking at the persistence seam, not in workflow config
- **status:** **LIVE on chassis `v1.0.1211`, pod-verified on both replicas** (2026-07-30). Committed `f61dce806` and carried into another session's roll at 17:33 UTC — *not* deployed by its author, which is the shared-HEAD property working as documented. Markers, ASCII-only and with controls in the same exec: `CONTENT_CLAIMS_FLOOR_DETAIL` 1, `claims floor blocked` 1, positive control `CONTENT_LINK_REPAIR_DETAIL` 1, negative control 0.
- **status-evidence, and the limit of it:** the code path is **deployed and present**; it is **NOT yet exercised in production**. Zero `CONTENT_CLAIMS_FLOOR_DETAIL` rows since the roll, which is what a clean corpus predicts — only 3 of 949 components carry a banned claim and none of those pages has been rebuilt since. **That zero is the unexercised kind, not the working kind**, and this entry says so rather than letting a later reader quote it as "the floor is proven live". The behavioural evidence is the fault-injected test suite, not production.
- **council:** `2d0dbc2e-e125-41f6-876d-0f8d6cf96688`. **Round 1 REVISE** — gating objection `prior_art_librarian` (high) plus four of its five points were ONE objection: load-bearing absence claims with **no query attached**. Nothing refuted. **Round 2 answered with SQL and verbatim output, changing no code, and that seat flipped to APPROVE.** Round 2 nonetheless returned REVISE with `decided_by = "unreadable reviewer(s): review_guardian.result, review_improvement_guardian.result"` and `unreadable` 0→2 — i.e. **`bugs_open/119`'s shape, a seat's malformed result costing a round, with no high-severity objection raised at all** (3 medium, 6 low; 12 of 15 approve). **Read `unreadable`, not `abstained`, before reading a REVISE as a judgement.** Surviving mediums recorded as follow-ups in `bugs_open/149`, including one real inconsistency in the change itself: B4 reports an erroring check in the step output but records nothing durable, while this floor honours the "a pod log line is not a record" rule in the same commit.
- **status-evidence:** 7 unit tests in `save_sections_claims_guard_test.go`, **fault-injected before submission** — neutering the banned-claim arm fails 6 of the 7, so the suite demonstrably catches the guard's removal rather than passing because nothing is asked of it (the CLM-017 vacuous-pass lesson, applied to a new file on its first day). Full `./platform/...` suite green, 34 packages.
- **what:** `scanSectionClaims` / `claimsGuardBeforePersist` in `platform/orchestration/actions/save_sections_claims_guard.go`, called from `SavePageSectionsAction`. It runs the **same** engine as CLM-004's two surfaces (`ExtractAssertionText` → `checkBannedClaims` / `checkUnregisteredNumbers`) over the sections about to be written, and refuses the save when a **banned claim** is present. So this is a **third** enforcement surface and the CLM-004 pair is now a triple — but deliberately not a third *matcher*: there is one implementation, called from three places.
- **why a floor rather than more config, which is the whole point of the entry:** measured live 2026-07-30, **six** agents persist page body sections through `save_page_sections` (`page-build-handler`, `pageflow-builder`, `page-rebuild`, `page-rerender`, `site-work-orchestrator`, `tool-recreation-handler`) and only **two** run `validate_page_content`. Adding the step to the other four would work until the seventh agent is written. This is the identical argument `save_sections_link_repair.go` made for `bugs_open/079`: *"Persistence is the one chokepoint every body-section writer passes through … Putting the repair here makes an unrepaired section unsaveable whatever the workflow config says."* **A gate can be forgotten by a workflow author; a floor cannot.**
- **THE CORRECTION THAT REDIRECTED THE FIX, and the transferable bit:** `bugs_open/149` C1 named **`page-content-writer`** as the agent missing the gate. It **persists nothing** — its workflow ends `compile_page → complete_workflow{output_field: page_content}` and it is *called* by four of those six parents, so a gate there would have guarded a value in transit, not a write. `check_empty_sections.go:7` records the same route being corrected once already (`HandlerAgent: "page-build-handler" (was "page-content-writer")`). **"Which agent writes the copy" and "which agent persists it" are different questions, and the second is the one an enforcement point must answer.**
- **the severity rule is driven by SEVERITY, not by check name** — which is what keeps it correct as checks are added to the engine. `blocker` (banned claim, a known falsehood) **refuses** the save, matching the three guards already in that function (page-ownership, content-regression, interactivity). `error` (unregistered number) is **recorded and allowed**, because its false-positive rate is the stated reason it is never a blocker in the engine either. Refusing on the numeric half at a seam four agents never opted into would convert a content-quality signal into a fleet-wide build outage.
- **blast radius, measured before submission rather than left for the reviewer** (`bugs_open/124`'s REJECTED ground): `cmd/claimscan` (CLM-014) over the **complete** live corpus — **949 components / 14 sites**, each against its own register — **3 banned-claim findings on 2 sites (0.32%)**: webdesign.co.uk `tool-blueprint-compiler` ("never invents"), robot-hands.com `how-it-works` and `gripper-catalog` (both "independently verified", i.e. `bugs_open/147`). Plus **59 unregistered numbers** on the 4 armed sites, which record and never refuse. **Those 3 components cannot be re-rendered until the copy changes** — a rerender regenerates from `content_data` and would re-persist the claim. All three assert something untrue today, which is why refusing is affordable. **Re-measure, do not quote: the surface was 908 on 07-28, 919 on 07-29 and 949 on 07-30.**
- **affected consumers, named and told** per the owner ruling of 2026-07-29 §3 — the four agents that gain an enforcement they never had, for whom the honest statement is *a save can now be refused where before it always succeeded*: `pageflow-builder`, `page-rebuild`, `page-rerender`, `site-work-orchestrator`. **SIGNED OFF by the owner 2026-07-30** (*"yes they sign-off"*), discharging the council `guardian` seat's objection that naming them in a risk section was not enough. The refusing behaviour is authorised, not merely disclosed — on the measured terms above (3 of 949 components refusable, all three untrue today) and with the config kill-switch unchanged. `page-build-handler` and `tool-recreation-handler` already gated and are unchanged in effect.
- **the levers, and why they are ON:** `check_claims` and `check_claims_fleet_wide`, the **same names and same defaults as the gate's**, read from the `save_page_sections` step config. ON by default because an off-by-default floor would be inert and the defect would still be live; withdrawable in seconds because DB config is live-immediately. Disarming logs at **Warn** naming the site, for CLM-015's reason: a register-less site loses its only banned-claim protection that way and nothing else would say so.
- **it writes a work RECORD, not a work ITEM**, under a new error code `CONTENT_CLAIMS_FLOOR_DETAIL` (distinct from the gate's and the link-repair one so existing queries are untouched and "which path caught this" stays answerable). No handler can rewrite a claim — a human must — and `check_unverified_claims` already raises `needs_human_review` items for exactly this, so an item here would double-file one finding under two mechanisms.
- **scope boundary, stated rather than implied:** this covers `save_page_sections`. It is **not** fleet-wide coverage of `page_components` — `ApplySectionEditAction`, `create_report_page` and `rebuild_blog_listing` also persist LLM prose and are untouched (`create_report_page` belongs to 149 C2's open question about `report-builder`'s deliberate `check_claims:false`), and `bugs_open/123`'s content-creator path has no site and no page row so this seam cannot reach it at all.
- **sources:** `platform/orchestration/actions/save_sections_claims_guard.go`, `save_sections_claims_guard_test.go`, `save_page_sections_action.go` (call site, after the link repair and before the regression guards); `bugs_open/149` C1; council correlation `2d0dbc2e`
- **relations:** CLM-004 (the enforcement-surface pair this makes a triple), CLM-015 (the fleet-wide set it enforces), CLM-016 (`ClaimSurface`, read here from `pages.page_type`), CLM-017 (the negation guard, which is what makes refusing on the strongest pattern safe), CLM-014 (how the blast radius was measured); `bugs_open/079`'s `save_sections_link_repair.go` is the shape it copies deliberately

### CLM-019 — the SITE-LESS claims seam: a producer with no site, no page and no register, scanned by the same engine
- **status:** **⚠ ROLL CONDITION MET 2026-08-10.** Chassis `v1.0.1283` is built from `d3c09cc74` (BLD-019 stamp, both replicas), which was HEAD at build time — so every commit on this branch is in it, including this one's. **Clauses below about waiting for a roll are superseded**; whether the feature is EXERCISED is a separate claim. ⚠ **This entry cites NO commit sha, so inclusion is inferred from its date rather than verified per-commit — name the commit next time; provenance can only settle an entry that names one.** Original: **BUILT AND COMMITTED 2026-08-03; NOT LIVE.** Go changes are inert until the `content-creator-agent` image is rebuilt and rolled — this entry will overstate the position until then, and says so deliberately rather than being written as if shipped. There are **no DB config changes at all**, so there is no ordering constraint between substrates.
- **status-evidence:** 15 unit tests across `claims_attributed_test.go` (8) and `internal/agents/contentcreator/claims_guard_test.go` (7), **mutation-proved in three places before commit**: (1) delete the guard call from `handleMessage` → only `TestHandleMessageActuallyCallsTheGuard` fails, which is the point of it — the guard is annotate-only, so removing it breaks no compile and no other test; (2) neuter `DocumentCarriesCitations` → 4 of the 4 must-pass cases fail; (3) neuter the shared negation guard → the denial cases fail. Full `./platform/...`, `./internal/agents/contentcreator/`, `./cmd/claimscan/` green **against `git archive HEAD` plus these files** (the working tree carries another session's unrelated unused-import breakage in `platform/orchestration/actions`).
- **what:** three new pieces, all reusing the existing engine rather than adding a second one.
  - `datahelpers.SplitPlainAssertionText` / `datahelpers.AssertionBlocks` (`claims_plaintext.go`) — the plain-text/markdown sibling of `ExtractAssertionText`, which is an **HTML parser**. **MEASURED, not reasoned:** markdown fed to `ExtractAssertionText` returns **ONE block for the whole document**, whitespace-collapsed, fusing a heading to the following sentence with no full stop between them — so a `[^.]{0,N}` window and `negatedClaimMatch`'s backwards scan both cross a paragraph boundary the prose does not have. Same hazard the claims floor recorded at `save_sections_claims_guard.go:116-121`. `AssertionBlocks` routes by declared format and treats **unknown as plain text**, the safe direction: under-match is a missed finding, over-match is a fabricated one.
  - `datahelpers.ScanAttributedUncitedStats` / `DocumentCarriesCitations` (`claims_attributed.go`) — a **fourth** `ClaimFinding.Check` value, `attributed_uncited_stat`, for the class `bugs_open/123` observed live: a figure attributed to unnamed evidence ("Industry data shows … between 3% and 10%") in a document that cites nothing anywhere. **Needs no evidence base**, which is the point.
  - `internal/agents/contentcreator/claims_guard.go` — the wiring: `scanGeneratedText` (pure seam) plus `recordClaimFindings` (durable record), called from `handleMessage` between generation and response assembly.
- **THE UNIT IS THE DOCUMENT, AND THE CORPUS IS WHAT DECIDED IT — this is the entry's most transferable line.** With a BLOCK-scoped citation test the detector scored **0 true positives and 4 false positives** over the complete live corpus (1,130 components, 2026-08-03): every finding named its source in the same sentence — *"But Deloitte's 2026 banking research found that while 87% of banks…"*, *"Thomson Reuters' Future of Professionals research found…"*, *"Salesforce's State of the Connected Customer research found only 42%…"*, *"The same Dice survey found 30%…"*. Document-scoped, over the same corpus: **0 findings**, and the flag's non-ATTRIB output is **byte-identical** with and without it. On long-form editorial copy the citation lives in a DIFFERENT COMPONENT from the figure; only the document can see it, and content-creator's output *is* a document.
- **THE SECOND LANDMINE, and it cost more than the first: nine "true positives" evaporated when read in full paragraphs.** An earlier candidate arm matched a bare `N% of <population>` with no attribution cue at all and found nine live instances, recorded in the lane NOTES as real unsourced statistics. Read in context rather than in a 100-character snippet: *"66% of people already use AI regularly"* is immediately preceded by a full citation (University of Melbourne, *Trust, attitudes and use of artificial intelligence*, April 2025); *"at least 51% of people accepted"* is the statutory DEFINITION of a representative APR and not a statistic at all; *"the 78% of customers who…"* is anaphoric to a figure cited earlier on the page. **That arm is not in the shipped code, and its absence is the finding.** A snippet window is a measurement instrument, and it has a blind spot exactly the width of the citation you are looking for.
- **the third: a lazy cue can swallow its own negation.** `negatedClaimMatch(block, start)` scans BACKWARDS from the match start. This cue spans subject-to-verb, so "Industry data **does not** show that 40% …" matches from "Industry" and puts the negation INSIDE the matched span, invisible to a scan from `loc[0]`. CLM-017 recorded the same shape for the fleet-wide patterns, where it was harmless; here it inverted the verdict on every denial. Fixed by scanning from `loc[1]` (the verb), which keeps the guard clause-local. **Three unit tests caught it; no amount of reading did.**
- **it RECORDS and ANNOTATES; it never REFUSES, and that is a ruling rather than a preference.** The claims floor REFUSES a banned claim, and the symmetry is tempting — but the floor sits on a PERSISTENCE seam where refusing means "the existing content stays", whereas here it would mean "the caller gets an error instead of prose". The council's architecture seat has already ruled (round `7478233b`, LANDMINES.md `DeliverReply` entry) that changing these services' **caller-observable failure behaviour** "is an RFC moment"; doing it here under a different bug number would be smuggling in what that seat vetoed. Both reply-delivery sites in the package are also owned by the live `bugs_open/158` lane — untouched, and pinned untouched by `TestClaimsGuardDoesNotTouchReplyDelivery`.
- **the levers, and why their defaults differ.** `check_claims` (banned-claim scan) defaults **ON** — same name and default as the floor's lever; it only records, so it grants no new authority over any caller. `check_uncited_stats` defaults **OFF** — that IS the owner ruling of 2026-08-02 ("new authority on a shared seam ships as an opt-in FIELD with the unsafe default OFF"), because it is a heuristic over prose and whoever turns it on accepts its false positives. Pinned by `TestUncitedStatsAreOptInAndDefaultOff`, which names the ruling so a later default flip has to delete an assertion rather than change a literal.
- **the durable record:** `agent_error_log` under a distinct code `CONTENT_CREATOR_CLAIMS_DETAIL` (distinct for the floor's stated reason — a shared code makes "which path caught this" unanswerable), with **`site_id` NULL**. Not a workaround: the column is nullable and NULL-site rows are the established majority shape (283 of 484 rows in the week to 2026-08-03). `outcome` is stored explicitly as `"recorded"`, never inferred from counts. It matters more here than at the floor: content-creator writes **nothing to `llm_call_log`**, so without this row a pod log — which rolls — is the only trace that would ever exist.
- **severity vocabulary preserved** so the record still states what a refusing seam WOULD have refused: banned claim → `error`, attributed-uncited statistic → `warning`.
- **scope boundary, stated rather than implied.** Nothing routes `attributed_uncited_stat` to the build gate, the claims floor or the discovery sweep — they gain a fourth check value in the vocabulary and **no new behaviour**. Wiring it into any of them needs its own lever, its own corpus measurement and its own council round. `cmd/claimscan -attributed` is the standing measurement vehicle and is deliberately **excluded from the exit code**, so the tool's documented scripted-gate contract is unchanged.
- **severity of the bug it closes, corrected downward by measurement:** `bugs_open/123` is filed HIGH and asks for an owner call "before blog or social output is published anywhere". All three `agent_definitions` rows referencing content-creator are `is_active=false` **and** soft-deleted — **no live workflow dispatches it**; it is reachable only by direct Kafka publish, which is how the 2026-07-27 fabrication was produced. Latent, not biting. The service itself is deployed and running, so the guard is on the path any direct dispatch takes.
- **sources:** `platform/orchestration/datahelpers/claims_plaintext.go`, `claims_attributed.go`, `claims_attributed_test.go`; `internal/agents/contentcreator/claims_guard.go`, `claims_guard_test.go`, `agent.go`; `cmd/claimscan/main.go` (`-attributed`); `bugs_open/123`; workstream `docs024_key_docs_latest/bugfix_123_content_creator_claims/`
- **relations:** CLM-018 (the claims floor, whose severity vocabulary and record/refuse split this copies, and whose scope boundary named this gap), CLM-015 (the fleet-wide set this reuses nil-safely — the property that made candidate 1 wiring rather than building), CLM-017 (the negation guard, shared not reimplemented; and the vacuous-pass lesson these tests apply), CLM-014 (`claimscan`, extended with `-attributed`), CLM-004 (the enforcement surfaces this deliberately does NOT join), CLM-006 (the LLM prose auditor, site-bound and never demonstrated — not a substitute for a deterministic text-level guard)

#### CLM-019 addendum 2026-08-03 — LIVE on v1.0.1243, council APPROVED, and the four checkable objections checked
- **NOW LIVE, and proven at the artefact rather than at the tag.** Pod `content-creator-agent-ffb66cd9d-fmbqm`, image `v1.0.1243`, one exec, **both directions plus a control** (`bugs_open/153`: a positive control proves the PIPELINE, never your spelling): `content-creator claims guard: findings on generated text` → **1**, `CONTENT_CREATOR_CLAIMS_DETAIL` → **1**, `Content generation completed (claims-guard scanned)` → **1**, `and this document cites no source anywhere` → **1**; **NEGATIVE** `Content generation completed successfully` → **0**; control `system.agent.content-creator.requests` → **1**. The negative is the discriminating one and it is only available because the completion literal was changed on purpose.
- **⚠ LIVE ≠ EXERCISED, and this entry says so rather than letting a later reader quote it as proven.** The **production induced-fault run was NOT performed** — no crafted request was published to `system.agent.content-creator.requests`, so no `CONTENT_CREATOR_CLAIMS_DETAIL` row exists and the guard has never fired against a real generation. The behavioural evidence is the mutation-proved unit suite, not production. `bugs_closed/123` §"What is NOT proven" carries the exact recipe.
- **council `767e78ae-fa80-4edb-9ca2-3ac866f3500c` — APPROVED round 1**, 13 reviewers, 4 abstained, **0 unreadable**, 3 seats objecting, **no high severity, no veto**. The architecture seat approved explicitly rather than abstaining and wrote the most useful note in the round.
- **THE FOUR CHECKABLE OBJECTIONS, CHECKED — not answered by argument** (the `bugs_open/124` ground, and the `prior_art_librarian` seat's standing complaint about load-bearing absence claims with no query attached):
  1. **`guardian`, MEDIUM — "a fourth `ClaimFinding.Check` value could be silently dropped or misrouted if a consumer enumerates known values."** Checked: there is **exactly one** `switch f.Check` in the tree, `claimsSummary` (`check_unverified_claims.go:464`). It **has a `default:` arm**, so an unknown value is **mislabelled as an unregistered number, not dropped**. And it is **unreachable for this value today**: that file calls only `ScanAllBannedClaims` and `ScanUnregisteredNumbers`, never `ScanAttributedUncitedStats`. **Recorded as the landmine for whoever wires it** — see below.
  2. **`prior_art_librarian`, MEDIUM — "the severity downgrade rests on an agent_definitions fact that is checkable but not yet checked."** Re-run at close: 3 referencing rows, **all `is_active=false` AND soft-deleted**; `SELECT count(*) … WHERE is_active AND deleted_at IS NULL` → **0**. The fact holds.
  3. **`prior_art_librarian`, MEDIUM — "confirm no existing symbol already covers attributed/uncited detection."** Checked: `grep -rniE "uncited|attributed|unsourced"` across `platform/ internal/ cmd/` returns only `directory_export_action.go`'s **attributed *prices*** — an unrelated sense of the word. No prior art.
  4. **`reuse_agent`, LOW — "a generic markdown-to-lines splitter is plausible prior art; was `section_text.go`/`truncation.go` searched?"** Checked, and they were the right places to look: `section_text.go` carries `NormaliseSectionText`/`SectionTokenSet` (normalisation and token sets, **not** block splitting), and every markdown-named function in the tree (`CleanMarkdownJSON`, `cleanMarkdownJSON`, `stripMarkdownFromResponse`, `fencedJSONBlocks`) strips **JSON fences from LLM output**. No prose block splitter existed.
- **`compliance`, MEDIUM — STANDS, unfixed, and deliberately so: the detector built for this incident is OFF by default.** The seat is right that "the mechanism exists but is not switched on for the incident it was built to prevent". It is an explicit owner ruling (2026-08-02) against a producer with zero live dispatchers, and the measured false-positive history is why. **Recorded in `bugs_closed/123` as the one thing a reactivation must decide first**, so it is a named handover rather than a silent gap.
- **`architecture`, MEDIUM — carried into this entry as the seat asked.** ⚠ **WIRING `CheckAttributedUncitedStat` INTO THE CLAIMS FLOOR'S ROUTING, THE BUILD GATE OR THE DISCOVERY SWEEP IS AN ARCHITECTURE-REVIEW TRIGGER — not a follow-up point fix, and not "just adding a case to an existing switch".** Cite this review when that change arrives.
- **`architecture`, LOW — the backlog item, named here so it is not rediscovered as a fresh bug.** The claims seam has exactly ONE entry mode (`site_id` → evidence base). This fix is a bespoke, hand-wired, document-scoped guard living inside one agent's package; **the next site-less producer** (a chat/summary agent, an email drafter — anything emitting prose with no page row) **will re-derive all of it**: its own `AssertionBlocks` call, guard file, error code and register entry. The running cost of not changing the seam is a wiring pattern staying non-reusable, paid once per future producer. The seat also asked for a count of how often this council has deflected "make the evidence base reachable without `site_id`" upward — **[UNMEASURED]**, and worth having before the next one arrives.

### CLM-020 — `NegationGuard`: CLM-017's algorithm exported and made domain-parametric; first non-claims consumer is the tool-fabrication gate

- **status:** **⚠ ROLL CONDITION MET 2026-08-10.** Chassis `v1.0.1283` is built from `d3c09cc74` (BLD-019 stamp, both replicas), which was HEAD at build time — so every commit on this branch is in it, including this one's. **Clauses below about waiting for a roll are superseded**; whether the feature is EXERCISED is a separate claim. ⚠ **This entry cites NO commit sha, so inclusion is inferred from its date rather than verified per-commit — name the commit next time; provenance can only settle an entry that names one.** Original: **BUILT 2026-08-08 (`bugs_open/222`), submitted to council alongside the commit, inert until the next chassis roll** (Go change; the fabrication gate lives in `platform/orchestration/actions`, part of `agent-chassis`).
- **what:** `NegationGuard` (a struct: `Cue *regexp.Regexp`, `Boundary string`, `Window int`) and its method `NegatedAt(text string, pos int) bool`, both exported from `datahelpers/claims.go`, immediately after CLM-017's doctrine block. The body is a byte-for-byte extraction of `negatedClaimMatch`'s existing algorithm — bounded backwards window, trim to the current clause via `LastIndexAny` + the multibyte-rune step-over, test against a cue regex. `negatedClaimMatch` itself becomes a one-line wrapper (`claimNegationGuard.NegatedAt(block, start)`) over a package-private `claimNegationGuard := NegationGuard{negationCueRe, negationClauseBoundary, negationWindowBytes}` — **zero behaviour change for CLM-017's two existing consumers**, proven mechanically: the full `datahelpers` suite, including `TestBareNoIsAKnownResidualOfTheSharedGuard`, passes with **no test file touched**.
- **why an export was needed at all, not just a second regex:** `check_tool_fabrication_action.go` (bugs_open/222) had the identical false-positive shape CLM-017 was built for — a proximity match convicting a DENIAL ("no fabricated data — starts empty") as a declaration — but `negationCueRe` deliberately excludes bare `no`/`without` (the CLM-017 entry above: they are intensifiers in marketing prose, and the exclusion is pinned by a residual test). Widening the shared cue regex to fix an unrelated gate would have risked reintroducing the false negative CLM-017 exists to prevent. **Two vocabularies, one algorithm** is the resolution: the fabrication gate builds its OWN `NegationGuard{fabNegationCueRe, fabNegationClauseBoundary, 32}` — narrower window, bracket-inclusive boundary set, and a cue list that DOES include `no`/`without`/`zero`/`instead of`/`rather than` because in a code comment about a tool's own data those are plain denials, not intensifiers.
- **the qualifier-position subtlety this second consumer exposed:** CLM-019 already recorded that a cue-spanning regex can put the negation INSIDE the matched span, invisible to a scan from the match's overall start (`loc[0]`), and fixed it locally by scanning from `loc[1]` for that one pattern. The fabrication gate has THREE Tier-A regexes that disagree on which side the negatable qualifier sits — one puts it first, two put it second — so a single fixed offset does not generalise. Solved with a `declPattern{re, qualGroup}` pairing and `FindAllStringSubmatchIndex`, so the guard is always asked about the qualifier's OWN submatch position regardless of which capture group it lands in. This is the reusable shape for the *next* consumer too: know which submatch is negatable, ask the guard about THAT position, not the match start.
- **status-evidence:** `TestNegationGuardIsParameterisedByDomain` (datahelpers) proves a custom-vocabulary guard suppresses at a position `claimNegationGuard` does not, over the identical string — the mechanical proof the extraction is parametric and the claims vocabulary was not widened to make it so. On the fabrication side: 6 new tests (denied-comment reproduction, positional non-vacuity, inside-span negation for both affected regexes, a 6-row cue-vocabulary sweep, a pinned forward-negation residual) plus a mutation-and-control triple — killing the guard fails exactly the tests that should depend on it; forcing it to always-negate fails the tests that assert a real declaration must survive, while the untouched synthetic-PII arm stayed convicted through the same mutation, evidencing that arm's independence rather than merely asserting it.
- **known residual, pinned not silently absorbed:** the guard scans backwards only, so a POST-positioned denial ("Mock data is not used in this tool") is not caught by either domain's guard — same shape as CLM-017's own bare-`no` residual. Pinned by `TestPostPositionedDenialIsAKnownResidual`, written (like `TestBareNoIsAKnownResidualOfTheSharedGuard`) to fail — and to say so — the day someone adds forward scanning.
- **CORRECTED 2026-08-08, council round 2 (`compliance`, medium — caught before this entry could be cited as ground truth):** the original text here overstated the fabrication gate's coverage after this change, in words close to "every failure mode routes to human review, never to a shipped fabrication". **That is not what the plan's own risk register (R1) says, and R1 is the correct one.** A fabrication phrased defensively, below the corpus-size threshold, with no PRNG/fetch/fragment signature, now escapes Tier A **silently** — no review item is filed for that specific match — and the only remaining floor is the untouched Tier B/synthetic-PII arm, which is not exhaustive. Correct statement: this change can only *remove* Tier A convictions, never add one; a suppression that also evades Tier B ships unreviewed. Fixed in `PLAN_2026-08-08_negation_aware_declaration_tier.md` §7 the same evening the objection landed.
- **sources:** `platform/orchestration/datahelpers/claims.go` (`NegationGuard`, `NegatedAt`, `claimNegationGuard`), `claims_test.go`; `platform/orchestration/actions/check_tool_fabrication_action.go` (`fabNegationCueRe`, `fabNegationGuard`, `declPattern`, `firstAssertedDeclaration`), `check_tool_fabrication_action_test.go`; `bugs_open/222`; `docs024_key_docs_latest/bugfix_222_fabrication_negation/`
- **relations:** CLM-017 (the algorithm and vocabulary this exports without touching), CLM-019 (the inside-the-span lesson this generalises past a single fixed offset), CLM-004 (one algorithm, not a second one that drifts — now proven across TWO packages, not one)
- **⚠ LANDMINE for the next author** — *footprint: `ClaimFinding.Check`, `claimsSummary` (`check_unverified_claims.go:464`), `ScanAttributedUncitedStats`.* Routing an `attributed_uncited_stat` finding into `check_unverified_claims` makes it **count as an unregistered number** in the work item's summary line, via that `default:` arm. Nothing errors, the item is raised, and the summary reads "N unregistered number(s)" for a finding that is not one. The tell is a claims work item whose numbers count exceeds what the numeric scan could have produced. Add the case to the switch **in the same commit** that routes the value.

### CLM-021 — the register reaches the tool BUILDER, not just the copy writer (migration 366)

- **status:** **live in config since 2026-08-10 (prompt text only, no Go, no image roll); first proven on a real rebuild the same day.** Registered here 2026-08-10 by the mortgagecalculator lane — late, and it should have gone in with the commit that shipped it (`5c2e713c3`), per the platform-seams condition (2) of the owner ruling of 2026-07-28.
- **what:** `tool-recreation-handler`'s `recreate_tool` prompt now opens with a section — *"Verified facts — these OVERRIDE the original tool AND the specification"* — that injects `{{.site_specs.specs.evidence_base.writer_block}}` before `## Design Context`, and instructs the builder to take the exact figure, to put the fact's wording beside the constant in a code comment, and **not** to state a rule the register does not carry. `tool-generator` and `tool-improver` are NOT yet wired (they need a `read_site_spec` step first — still config, not Go).
- **why it was nearly free:** the facts were always in the agent's context and were being thrown away. `load_site_specs` calls `read_site_spec` with **no `aspect`**, which returns every current aspect keyed by name (`site_spec_actions.go`, "All aspects mode"), so `.site_specs.specs.evidence_base` already resolved in that very template — which mentioned `identity.industry`, `design` and `site_archetype.visual_character` and never `evidence_base`. This is `PBP-037`'s finding repeating: the data arrives, the prompt does not ask for it.
- **status-evidence, and it is attributional rather than numerical.** The obvious test — "does the rebuilt stamp-duty tool encode £500,000 instead of the original's £625,000" — **cannot come out false**: the *pre*-366 rebuild of 2026-08-08 already encoded £500,000 from the model's own knowledge. The discriminator is whether the REGISTER'S OWN WORDING reaches the artefact. Measured on 2026-08-10 over the two builds of the same tool, same agent, same spec but for one id-contract clause: each of the register's four composed `writer_line`s appears **0 times in the pre-366 artefact and exactly once in the post-366 one**, as a comment beside the constant it licenses (e.g. `// "Above £500,000 first-time buyer relief disappears entirely — the standard bands apply to the whole price"`), with a positive control (`Stamp Duty`, present in both) proving the match was capable of firing on either file. Both artefacts were compared with comment markers stripped and whitespace collapsed, because the generator wraps a long `writer_line` across two `//` lines — a naive verbatim `grep` reports 0 for BOTH and reads as "the change did nothing".
- **⚠ LANDMINE — absence from the register is an instruction too, and it can make a rebuilt tool WORSE.** *Footprint: `site_specs.aspect='evidence_base'` on any site whose tools are rebuilt by `tool-recreation-handler`.* The same prompt that says "take the exact figure" also says "do NOT state a rule that is not in the register". On 2026-08-10 the rebuilt stamp-duty calculator **dropped the £40,000 additional-property surcharge floor** (`SURCHARGE_FLOOR = 40000`, present twice in the pre-366 build, zero times after) — true law, correctly implemented before, absent from a register that then held only four SDLT facts. Nothing failed; the tool simply became wrong below £40,000. **So: register the facts a tool needs BEFORE rebuilding it under this prompt, not after.** A partial register is not a neutral one. [MEASURED 2026-08-10; n=1 build, so the mechanism is evidenced rather than proven — the register was the only changed input, but LLM output is not deterministic.]
- **what it does NOT do, and must not be written up as doing:** an LLM shown a fact may still ignore it. This lowers the odds of a tool being *built* with a stale constant (failure F1 in `PLAN_2026-08-09_facts_into_tool_acceptance.md`); it closes no door. In particular the code comment it asks for is a **trace for a human reader** — it must never become the machine declaration of which facts a tool encodes (that is Piece 2 of the plan), because a comment enforces nothing and a source-scanning consumer would make every comment in the tree load-bearing.
- **sources:** `docs/agent_docs/sql_for_agents/366_tool_recreation_reads_the_evidence_register.sql` (+ `_ROLLBACK`); `docs024_key_docs_latest/mortgagecalculator_couk_adoption/` (`PLAN_2026-08-09_facts_into_tool_acceptance.md` Piece 1, NOTES 2026-08-10)
- **relations:** CLM-001 (the register's shape and its supersede-then-insert rule — carry `pinned` forward), CLM-004 (the copy-side gates this parallels: they guard what a page *says*, this guards what a calculator *computes*), CLM-007 (the daily sweep that keeps the injected values current, so a re-verified fact flows into the next rebuild with no edit here)

### CLM-022 — the register reaches the tools that ENCODE its facts: a fence-level `facts` declaration, and the daily sweep's per-tool fan-out (bugs_open/225's class)

- **⚠ CORRECTED 2026-08-24 — THIS ENTRY, THE BUG FILE AND THE ROUND-3 COUNCIL SUBMISSION ALL CLAIMED A VALIDATOR THAT HAD NEVER RUN.** The line below reads "shape-checked by the validator's new **P11** rule". P11 is real, tested and correct — and it lives in `ValidateExperienceCriteria`, whose **only** production caller is `write_experience_pattern_action.go:217`, the **experience-pattern** register. Tool PLANs are written by `WriteDocPlanAction`, which performed **no criteria validation of any kind**. So P11 had never once seen a tool fence, and a malformed `facts` declaration was accepted in silence at the only door it actually comes through. **FIXED 2026-08-24** (`write_doc_plan_action.go`): a tool PLAN whose fence mentions `facts` is now parsed before the transaction and **refused** if any id is unreadable, reusing `criteriaFactsFromValue` — its third caller, not a third spelling. Measured the same day: **1 of 132** current tool PLANs declares facts and it is well formed, so **zero** live fences are refused by the new gate; it is additive refusal on a shape that previously produced silence. **The lesson is not the fix — it is that three documents asserted a control's existence and none of them enumerated its callers.**
- **⚠ CORRECTED 2026-08-24 — the parser FAILED OPEN on a fence that DID declare something, and that disarmed a safeguard this council asked for.** `parseCriteriaFacts` returned `(nil, nil)` on a whole-fence `json.Unmarshal` error — no ids **and no issues** — contradicting `criteria_facts.go`'s own header ("a fence that DID say something, malformed, is reported as issues so a human can see the declaration was ignored, not silently dropped"). It propagated: `planSiteFactDrift`'s zero-rows warning, added at the `bug_historian` seat's request in round 3, is gated on `issues`/`unresolved` being non-empty. **So one trailing comma in a fence edit disarmed the declaration AND the warning whose whole job is to say a declaration was disarmed** — the site then goes quiet with no signal anywhere, and against a fleet where 131 of 132 PLANs declare nothing, "silently declared nothing" is indistinguishable from normal. **FIXED 2026-08-24**, narrowed by `factsKeyMentioned`, deliberately the SAME predicate as the fan-out's SQL prefilter (`dp.body LIKE '%"facts"%'`) and the new write gate, so the three cannot disagree about which documents have anything to say about facts.
- **NEW SURFACE 2026-08-24 — `doc_notes` category `fact_declaration_broken`**, producer `refresh_evidence_base`, subject `('tool', <subject_key>)`, site-scoped, **30-day per-subject cooldown**, insert shape copied from `ToolAcceptanceCheck.noteNeedsCriteria`. It carries both failure modes: a malformed declaration, and ids this site's register does not carry. **A doc_note and NOT a work item, on measured grounds:** this mechanism's own first firing wrote 13 `fact_drift_review` items on 2026-08-17 and **all 13 were still `needs_human_review`, untouched, seven days later** (fleet-wide the same queue holds 206 `cta_names_unknown_destination` and 126 `owned_page_review` in that state; `bugs_open/033` is the tracked defect that it has no working surface). doc_notes are keyed on the subject the fence belongs to and are already loaded into every builder/improver context by `load_doc_context_action.go`, so the finding arrives where the next edit to that PLAN happens. **Writes nothing on a dry run** — `planSiteFactDrift` runs before the action's own dry-run return, so it honours `dryRun` itself, and the lane's induced-proof recipe depends on that.
- **STAGE 2b LANDED 2026-08-24 — `artifact_check` is reachable for EVERY source kind, and addressable by `subject_key`.** Closes `bugs_open/288` §5.6. Two defects, both still literally true at HEAD until this change: **(1) unreachable for the facts that matter.** `refreshOneSiteEvidence`'s per-fact loop handled `source.citation` and `continue`d **before** the `artifact_check` test seventeen lines below it — and every fact holding a legislated figure is a citation fact (**185 of 294** current facts as of 2026-08-24). So an `artifact_check` written beside an SDLT band was never evaluated; the one reader in the estate on the raw-bytes surface could not be pointed at the class of fact it existed for. The `sql` and `attested_by` arms sat below the same `continue` and were equally unreachable for a dual-source fact. Implemented as a **PRE-PASS**, so the existing branches and their `continue` are untouched and every path taken today is taken identically. **(2) the address died.** `artifact_check.component_id` is a `page_components.id`, and `bugs_closed/225`'s own component no longer exists — the page was decomposed into `prose-0`/`tool-1`/`prose-2` — so a binding written when that bug was filed would today fail closed for ever, **which reads exactly like a working check**. `subject_key` resolves through the platform's own name rule every pass (`discovery_checks.ToolSubjectKeyExpr` + the audit page predicate, the same join family as the fan-out), so it survives decomposition. Exactly one address: both, or neither, fails closed. Blast radius **0 of 294** facts (control: 185 carry `citation`).
- **⚠ LANDMINE — a SECONDARY `artifact_check` must not own `verified_at`, or a passing check re-arms a gate for a DIFFERENT fact's drift.** *Footprint: `refresh_evidence_base_action.go` `refreshArtifactCheckFact`, `shouldRaiseStaleEvidence`.* On a dual-source fact the primary arm owns `verified_at` and `changed`. If the artifact arm bumped them, a **passing** artifact check anywhere in a register would flip `changed` on a site where every citation had drifted with nothing updating — opening the stale-evidence raise for those citation drifts, which is precisely the behaviour RFC_025's ratification was scoped to leave alone. It would also bump the date on a fact whose citation was just lost, disarming the time-based 403 escalation. Hence the `ownsVerifiedAt` parameter: TRUE only for the artifact-only case.
- **⚠ LANDMINE — one fact can now produce TWO entries, and "last entry per id wins" then hides a drift.** *Footprint: `refresh_evidence_fact_drift.go` `planFactDriftFanOut` `entryIdx`.* That line was safe only while a fact could produce one entry. With a primary plus a secondary artifact check, a **fresh** artifact entry appended after a **drifted** citation entry would be the one `classifyFactDrift` sees — so a lost GOV.UK citation on a declared tax fact would stop reaching a human, silently. Selection is now by severity (`drifted` > `error` > other) and is order-independent, with a test for each direction; single-entry facts are unaffected.
- **⚠ LANDMINE — a dual-source fact was CHECKED TWICE, and no unit test could see it.** *Footprint: the `artifact_check` arm in `refreshOneSiteEvidence`.* An `attested_by` (or `source.sql`) fact carrying an `artifact_check` reached both the new pre-pass **and** the original `artifact_check` branch, appending two entries under one `FactID` and bumping `verified_at` from the second; the citation arm escaped only because its `continue` happens to intervene. The guard is `&& !factHasNonArtifactSource(fact, src)`. **Found by asking what an end-to-end test of the loop would do** — every unit test of the resolver, the gate and the selection passed throughout. Same lesson as the entry below, in the same change: **deleting the pre-pass alone left the entire suite GREEN until a test drove `refreshOneSiteEvidence` itself.**
- **⚠ ALL FOUR PHASES LIVE AND PROVEN AT THE ARTEFACT 2026-08-24 (post-roll), AND STAGE 2b FIRES BOTH WAYS ON A REAL CITATION FACT.** Binary probe with a positive AND a negative control, each string first confirmed present in the source so a zero would have meant something: `fact_declaration_broken` **4**, `fact_binding_suggested` **4**, `present_in_markup_only` **1**, the three stage-2b strings **1 each**, `stale_attestation` **5** (control), nonsense **0**. `sdlt-ftb-relief-cap` on mortgagecalculator.co.uk retyped with `artifact_check{subject_key:"stamp-duty", pattern:"FTB_RELIEF_CEILING\s*=\s*500000"}`. **Baseline dry run returned TWO entries for one fact** — `tolerance:"artifact_check"` *and* `tolerance:"citation"`, both fresh — and **before stage 2b the first could not exist**, because the citation arm `continue`d before the artifact test. **The artifact entry carries no `verified_at`**, so the `ownsVerifiedAt=false` rule holds live. **Induced** (pattern → the expired `625000`): `drifted`, detail naming the tool by `subject_key`, while the **citation arm stayed `fresh` and undisturbed**. Restore **byte-identical by md5**, armed in a `trap … EXIT` before the change; dry runs wrote nothing (13 items before and after). ⚠ **RFC_025's first consumer is NOT this lane's** — `gd-trials` on gamesdesign.co.uk was retyped by another lane at 11:07 the same day. Ours is the first on a **citation** fact and the first using **`subject_key`**; quote it that way.
- ~~**⚠ LANDMINE — THE MECHANISM HAS STILL NEVER RUN FOR REAL. The last non-dry sweep was 09:05 on 2026-08-24, which is BEFORE the roll.**~~ **CORRECTED 2026-08-25 — IT HAS NOW RUN FOR REAL, and this line was false for the whole of the day it mattered most.** The first non-dry pass with this code ran **09:06Z on 2026-08-25: 19 sites, 0 errors**, and filed **5** `fact_binding_suggested` notes across **3** sites (`bugs_open/288` §5c). So the zeros this line told you to distrust are gone: Phase 4 has produced real rows, Phase 3a has annotated a real declaration batch, and a count from these mechanisms is now evidence. *What the original warning got right, and is worth keeping as the general rule: a count of zero from a mechanism that has never run means "it has not run", not "there is nothing to find".* **The cost of leaving it standing is specific — a council seat reading this entry after 09:06Z refuses real results that exist**, and this register is read as ground truth by seats that cannot go and look. ~~⚠ **THE RESIDUAL, and it is a DIFFERENT claim from the one struck above:** two fixes committed 2026-08-25 (`bba8a892d`, the duplicate-value ambiguity guard; `6ad4a8046`, naming a misplaced top-level `artifact_check`) are **in no running binary**~~ **RESIDUAL DISCHARGED 2026-08-25 19:07Z — BOTH ROLLED on chassis `v1.0.1339` and verified at the artefact on BOTH replicas** (all three new literals present, `stale_attestation`=5 positive control, nonsense negative control 0). ⚠ **But do not now read `misplaced_artifact_checks` as a working detector on the strength of a zero:** `[MEASURED 2026-08-25, post-roll]` **0** facts fleet-wide carry a top-level `artifact_check` against a control of **6** correctly placed under `source`, out of 304 current facts — the agritec lane repaired the only instances before the code that finds them shipped. So the guard is **present and unit-tested, with no live case to fire on**, and its silence is a TRUE zero rather than a blind one *because the control says so*. The ambiguity guard (`bba8a892d`) is weaker still: it has no observable surface at all — `Ambiguous` reaches only the doc_note body, the suggester skips declaring subjects, and every noted subject is cooldown-suppressed to ~2026-09-24. **Presence at the binary plus the unit test is the whole of the claim for both; neither has been demonstrated end-to-end on live data.** — probed at the artefact with a positive control (`stale_attestation`=5) and a negative one, all three new strings 0, on pods that started 09:27Z *before* either commit was authored. So `misplaced_artifact_checks` is **absent from every sweep payload for a BINARY reason, not a data one**, and a duplicated register value is still double-proposed. Do not read those two zeros either — but read them as unrolled code, not as an unrun mechanism.

- **PHASE 3a + PHASE 4 COUNCIL-APPROVED 2026-08-24 at round 2** (`041b3026`; round 1 REVISE was gated by `debug_historian` at HIGH and was RIGHT — the probe tokenised a `string_agg` of partial fragments, so one unbalanced `<script>` would have collected the next component's prose as script text and certified a stale tool. Fixed by per-component extraction, mutation-proven).
- **PHASE 3a LANDED 2026-08-24 — the byte probe: does the tool's CODE actually carry the registered figure?** `refresh_evidence_fact_probe.go`. **ANNOTATION ONLY** — three `omitempty` fields on each emission (`evidence`, `evidence_form`, `evidence_detail`), and **no route, band, status or item body depends on them**. It exists so one full fleet sweep yields the measured present/absent/markup-only distribution a later round needs before presence may settle an item; `PLAN_2026-08-09 §3` refused a static JS scan for an unmeasured false-positive rate, and measuring before arming is that discipline applied. Five outcomes, deliberately not two: `present_in_script` · `present_in_markup_only` · `absent` · `no_surface` · `not_probed` — "I could not look" and "it is in the copy but not the code" are different findings from "it is not there".
- **⚠ LANDMINE — `verify_report_prose`'s numeric gate LOOKS like the reusable answer here and would reintroduce two measured defects.** *Footprint: `verify_report_prose_action.go` `proseNumRe` / `normaliseNumericToken` / `numericSet`, and any "reuse the existing numeric matcher" objection on a CODE surface.* Raised by the council's `reuse_agent` seat (medium, round 2, corr `041b3026`), and the seat was right that the claim *"nothing in the tree does boundary-aware numeric literal matching"* was **too absolute** — that file tokenises numbers and tests set membership, which is a real alternative design. **Measured against the live stamp-duty script rather than argued:** it finds 500000 / 300000 / 1500000 / 40000 and correctly not 625000 — and it also tokenises **`v_500000`, `width500000px` and `id500000` all as `500000`** (its `\d[\d,]*\.?\d*` has no left anchor, so an identifier-embedded number is a hit), and it **normalises `£300,000` to `300000`**, conflating the comma form with the code constant. The second is fatal here: on this estate the comma form inside a script is a comment or a string, and separating the two is the whole basis of the `present_in_script` vs `present_in_markup_only` distinction. So the two mechanisms answer different questions — **tokenise-then-set-membership over PROSE** versus **find-this-literal-with-boundaries in CODE** — and the reuse is refused on evidence, not on preference.
- **⚠ WHAT A COUNCIL SEAT JUDGES IS THE SKETCH, AND AN ELIDED SKETCH DRAWS OBJECTIONS ABOUT CORRECT CODE.** *Footprint: any `097` submission.* Round 2 came back APPROVED with two `bug_historian` objections — that an unreadable page would report `absent` rather than a distinct `no_surface`, and that the row-scan error branch logged nothing. **Both are false of the code**: `factProbeNoSurface` is returned on an empty surface and there is a test pinning it, and the scan branch logs via `zap.Warn` like its sibling. Both were true of my SKETCH, which had abbreviated the early return and the error branch away. The RUNBOOK already says reviewers judge the sketch because it is the only view of the code they get; the sharper form is that **eliding a guard from a sketch does not read as brevity, it reads as the guard's absence** — and answering such an objection costs a round whether the code was right or not.

- **⚠ LANDMINE — PROBE THE SCRIPT TEXT, NEVER THE WHOLE PAGE: a whole-page check would have CERTIFIED `bugs_closed/225` daily for sixteen months.** *Footprint: `refresh_evidence_fact_probe.go` `extractScriptText`, and any future check that asks "is the registered figure on this page".* The evidence register's own `writer_line`/`writer_block` machinery **puts the registered figure into the page's PROSE**. Measured on the live stamp-duty page 2026-08-24 (15,111 bytes: 6,132 script / 8,962 non-script): `500,000` present in the script **and** the prose; `500000` in the script only; `625000` (the expired cap) in neither. 225 was *correct register, correct copy, stale code* — so a check reading the whole page finds the figure in the copy whatever the JavaScript computes. **The generalisable rule: when a check reads a surface, ask what ELSE writes to that surface — here the answer was "the very mechanism this check reports to".**
- **⚠ LANDMINE — raw digits are CODE, the comma form is COPY, and inside a `<script>` the comma form is a comment.** *Footprint: `factValueLiterals` / `factValueDisplayLiterals`.* Measured on the same page: `300,000`, `250,000`, `925,000`, `125,000` occur **only** inside JS comments and quoted strings ("…no stamp duty up to £300,000"), while the band table the tool computes from is raw — `{ upTo: 300000, rate: 0.00 }`. Two form sets, one per surface. Matching the comma form against script text re-creates the prose problem one level down.
- **⚠ LANDMINE — the distinctiveness floor is MEASURED (1000), and a bare guard is not enough on its own.** *Footprint: `factProbeMinValue`, `valueOccursGuarded`.* Over the script text of all **161** tool pages that have any, using **invented** values so every match is a false positive by construction (40 probes per digit length, 6,440 page-probe pairs each), **2026-08-24**: **1 digit 32.75% · 2 digits 3.79% · 3 digits 0.06% · 4 digits 0.03% · 5+ 0.00%.** So `5`, `2`, `10`, `12` — and **110 of 294** current facts are under 100 — are refused rather than guessed at. ⚠ And the guard itself has a trap: **excluding every trailing comma makes `{ upTo: 1500000, rate: 0.10 }` invisible**, because a trailing comma is a list separator, not a thousands separator. The rule needs lookaround in both directions and **Go's RE2 has none**, so it is hand-rolled byte boundary checks, not a regexp.
- **⚠ LANDMINE — a fixture built to DEMONSTRATE a rule will not TEST it.** *Footprint: any "wrong surface" test.* The headline test for the script-only rule **passed under the mutation that read the whole page**: the prose carried the comma form and the code surface is searched for raw literals, so both readings failed the raw search and returned the same verdict. The fixture now carries the current figure in **markup** (`data-relief-cap="500000"`) against the expired one in code — the only shape where the two readings differ — and is guarded by three premise assertions. **Nine mutations across this change; three passed and were worthless.** The more load-bearing the claim, the likelier the fixture was written to show it rather than to break it.

- **⚠ CORRECTED 2026-08-24 — CLM-021 / Piece 1 IS TWO-THIRDS DELIVERED AGAINST ITS OWN RECORD, and this entry read as though it were finished.** Piece 1 ("show the tool BUILDER the register") was specified as a `read_site_spec` step **plus a prompt clause** for `tool-generator` and `tool-improver`, and a prompt seed for `tool-recreation-handler`. **[MEASURED 2026-08-24 on the live `agent_definitions` rows, four types compared so the result could come out either way]**: `page-content-writer` **t/t** and `tool-recreation-handler` **t/t** name `writer_block` and `evidence_base`; **`tool-generator` f/f and `tool-improver` f/f name neither.** They RECEIVE the register (their `load_*` step omits `aspect`, which is all-aspects mode) and nothing in the instruction points at it. So on the tool path there is currently **neither a clear instruction going in nor a check coming out**, while prose has both — and `tool-improver`, the agent that would EVOLVE a stale calculator, has the thinnest coverage of the four. Found by the `agritec_uk` lane 2026-08-22 and confirmed here. ⚠ What is proven is that the PROMPTS do not direct the model there; that the model ignores the data it is handed is **NOT** proven and needs a run. **This is the same failure mode as the P11 correction above: a control the written record says exists, which has never run.**
- **PHASE 4 LANDED 2026-08-24 — ADOPTION: the machine proposes the binding, a human decides it.** `refresh_evidence_fact_suggest.go`, doc_note category **`fact_binding_suggested`** (producer `refresh_evidence_base`, subject `('tool', <subject_key>)`, site-scoped, 30-day cooldown per (subject, site)). The mechanism cannot protect what nobody declares, and **1 of 132** current tool PLANs declared anything as of 2026-08-24 — that one only because this lane asked another lane to hand-seed it — against **178** tool pages on register-bearing sites. Each pass, for every **non-declaring** tool subject on a register-bearing site, it probes the tool's SCRIPT text for that site's own registered values and files one note carrying a **paste-ready `"facts": [...]` fragment**. **[MEASURED 2026-08-24, corrected method]**: of **36** probeable facts (value ≥ 1000 on a site that has tool pages), **15** are already present in a tool's script on their own site — **3 distinct tools across 3 sites**, and one of them is **`mortgages-stamp-duty` on loanandmortgagecalculator, the estate's SECOND SDLT calculator, which declares nothing today**. ⚠ An earlier figure of "15 of 27" was measured with an UNGUARDED whole-page match and is not the same quantity; quote this one.
- **⚠ LANDMINE — the suggester is NOT the static scan `PLAN_2026-08-09 §3` refused, and the difference is the DIRECTION.** *Footprint: `refresh_evidence_fact_suggest.go`, and any future "scan the tool for numbers" proposal.* §3 refused scanning tool JavaScript for **UNREGISTERED** numbers: an unbounded target set where every constant (12, 100, 0.01, a viewport width) needs whitelisting, and the false-positive rate trains people to ignore the alarm. This probes only for **REGISTERED** values — a finite per-site set a human already vouched for with a citation — so there is no whitelist to maintain, the output is a **suggestion** rather than an alarm, and a wrong one costs one ignorable doc_note. Anyone re-reading §3 as forbidding this should read the direction before the verb.
- **⚠ LANDMINE — A CALCULATOR EMBEDDED IN AN ARTICLE IS INVISIBLE TO THE SUGGESTER, AND TO THE ACCEPTANCE LADDER BEFORE IT.** *Footprint: `factSuggestToolsQuery`, and any population predicate of the form `page_type='tool' OR component_level='tool'`.* The worked case is `agritec.uk`'s SFI Revenue Stacker: it lives on `stacking-agricultural-scheme-actions`, `page_type='blog-post'`, and that site has **zero** tool-level components anywhere. **[MEASURED 2026-08-24]** fleet-wide, of **222** script-bearing pages on register-bearing sites, **172** are selected by that predicate and **50 (23%) are not**. So a calculator that is not filed as a tool gets no binding suggestion, no acceptance run, and no fan-out — three mechanisms blind on one condition. **Deliberately NOT widened here**: the predicate change alters the population every one of those mechanisms acts on and needs its own measurement and round, and widening it reflexively because one lane reported a case is how a shared seam acquires an unmeasured blast radius. Recorded as a stated limit, and the agritec lane has been told rather than left to infer it.
- **⚠ LANDMINE — `artifact_check` AT THE TOP LEVEL OF A FACT IS LIVE IN THE REGISTER AND READ BY NOTHING.** *Footprint: `source.artifact_check`, `parseArtifactCheck`, any hand-written register fence.* RFC_025 §2.2 places it **inside `source`**, as a sibling of `citation`/`artifact`/`attested_by`, and that is where the code reads it (`refresh_evidence_base_action.go:348`, `:768`). An author who instead puts it at the **top level of the fact** — a reasonable reading, since it describes the FACT rather than the source, and arguably the better data model — gets a fence with correct contents, correct pattern and correct `subject_key` that **is never evaluated, with no signal anywhere**. Found 2026-08-25 on agritec.uk: four such entries, patterns tested by their author against the live artefact, reported "live". **Since 2026-08-25 the sweep NAMES them** (`res.misplaced_artifact_checks`) instead of ignoring them; it does not relocate the key, because the register is human-owned (CLM-001). ⚠ **And note how it was found: by two lanes DISAGREEING about the same rows.** One lane counted `f->'source' ? 'artifact_check'` and reported "never landed"; the other counted `f ? 'artifact_check'` and reported "live". Both queries were right about their own path and both conclusions were wrong. **Accepting either one alone would have shipped the hole** — a confident negative from a probe pointed one level off, and a confident positive from the same mistake mirrored.

- **⚠ LANDMINE — ON A RATE-TABLE SITE, SHARED VALUES ARE NORMAL, SO THE SUGGESTER IS STRUCTURALLY QUIET AND THAT IS NOT A SIGNAL.** *Footprint: `refresh_evidence_fact_suggest.go` `sharedValue`, and any reading of a low `fact_binding_suggestions` count.* The ambiguity rule (a value carried by >1 fact is reported, never proposed) exists because the first production sweep proposed two ids for one constant. It is right. But `[MEASURED by the agritec_uk lane, 2026-08-25]` their register carries **nine further pairs sharing a value and NONE of them are duplicates** — every one is two different SFI26 actions paid the same rate (102, 115, 187, 1920, 515, 55, 707, 765, 20). Collapsing them would destroy real facts. **So a site whose register IS a rate table will get few suggestions by construction, and "few suggestions" there must not be read as "few bindings to make".** That population's remedy is a per-fact contextual `artifact_check` where the ACTION CODE discriminates (`code:'CSAM3'[^}]*rate:224`), not the value probe. ⚠ And note how the duplicate that motivated the rule had been seen and dismissed three days earlier as *"two near-duplicates — harmless"*: it was **latent**, not harmless — indistinguishable from "two true facts" until something had to choose.

- **⚠ LANDMINE — a suggestion proves CO-OCCURRENCE, not ROLE, and it can only find tools that AGREE with the register.** *Footprint: the `fact_binding_suggested` note body.* That the register carries value V and the tool's code contains V says nothing about whether the tool uses V *for the thing the fact describes*; a human confirms. And a calculator **wrong since it was built** — `bugs_closed/225`'s original shape — carries no registered value to match, so it is **invisible to this leg entirely**. The suggester grows declarations; it does not replace them, and it is not a coverage measure. Both limits are stated in the note body itself so a reader cannot take it wider.

- **⚠ LANDMINE — a test that calls the note writer directly cannot see whether ANYTHING calls it.** *Footprint: `refresh_evidence_fact_drift.go` `noteBrokenFactDeclarations`, and any "does this reach a durable surface" test.* Four tests of the writer — one note per subject, cooldown, dry-run, clean-site — all stayed **GREEN** when the call was deleted from `planSiteFactDrift`, because every one of them invoked the writer itself. The faithful test drives the real entry point and asserts the `INSERT` is issued (`TestPlanSiteFactDrift_MalformedDeclarationReachesTheDurableSurface`). This is this lane's own recorded lesson repeating in the same file (`WRONG_CALLS` 2026-08-16: three mutations to prove one fix, two of which passed and were worthless). **A mutation that passes has usually bypassed the path, not proved the guard.**

- **status:** **LIVE and PROVEN IN PART, 2026-08-17.** The fan-out fires: measured by the `mortgagecalculator_couk_adoption` lane at 16:17Z on a real declaration (`stamp-duty`, all 13 SDLT ids) — it resolves a tool the acceptance ladder **cannot see** (2 components, 0 tool-level), routes all 13 to `fact_drift_review` (correct for a `no_auto_fix` fence), and **reads the register at check time**: two dry runs differing only in the register value returned `new_value` 500000 then 550000. **FULLY PROVEN 2026-08-17 18:30Z**, once a real sweep filed the 13 items and recorded the baselines: **self-quieting** (the same dry run that produced 13 entries pre-baseline produced **0** after, 13 facts checked) and **`value_drift`** (baseline moved 500000→450000 with the register untouched → exactly 1 entry, `kind: value_drift`, `old_value: 450000`, `new_value: 500000`; the other 12 declared facts silent; nothing written; item spec restored byte-equal). **Induce by moving the BASELINE, never the register** — the comparison is symmetric, so it exercises the identical branch without changing a live tax figure on a public site. Built 2026-08-16 and council-APPROVED at round 3 (`cff364b8`, 13 of 15 seats approve, 2 advisory, none high; the seat that gated rounds 1 and 2 approves). **Go INERT until the next chassis roll**, and no live declaration yet. Two REVISE rounds each found a real defect — see the landmines below, they are the useful part. No live declaration yet — the first is mortgagecalculator.co.uk's `stamp-duty` fence (seed handed to that lane as a CONTRIB). Until a fence declares `facts`, this code costs one extra SELECT per register-bearing site per day and changes nothing.
- **what:** Pieces 2+3 of `mortgagecalculator_couk_adoption/PLAN_2026-08-09_facts_into_tool_acceptance.md`, joining the two seams the plan named and nothing joined. **Piece 2** — a tool's criteria fence (`doc_plans.body`, the ```criteria block) may carry a **fence-level** `"facts": ["<fact_id>", …]`, parsed by `parseCriteriaFacts` (`criteria_facts.go`) and shape-checked by the validator's new **P11** rule. Fence-level, not per-check: a tool encodes a fact, a check does not — a per-check `facts` stays refused by P7 as an inert field. **IDS ONLY, never values** (PBP-037's rule applied to tools): the declaration pins WHICH facts a tool encodes; the value is always resolved from the CURRENT register of the site being swept, because `doc_plans` has no `site_id`. **Piece 3** — `refresh_evidence_base` (the daily `evidence-freshness` sweep) resolves each site's declaring PLANs to its active pages and files one work item per (fact, tool) that is owed something.
- **the three finding kinds, and the council round that added the first one:** **`unreconciled_declaration`** (low/60) — a tool declares a fact and no reconciliation is on record; files ONE review item per (fact, tool), self-quieting because the item it writes becomes the baseline. **This was added at the council's insistence and it closes a hole that would have made the mechanism blind to its own motivating bug**: the baseline is defined purely from prior REGISTER state, so a tool that is stale on the day it opts in — against a fact that has not moved since — yields `baseline == current`, emits nothing, and that is *exactly* `bugs_closed/225` (correct register, stale code, no subsequent legislative change). **`value_drift`** (high/30) — the registered number is not the number the tool was last told. **`evidence_drift`** (medium/35) — the citation or artefact under it changed with no value move. The banding is deliberate and was also a council finding: the first version put every human-routed case at medium/35, ranking a confirmed move on a `no_auto_fix` tax calculator BELOW an auto-fixable one. **The harder a thing is to fix automatically, the more urgent it is, not less.**
- **the routing split, which is the whole design:** **value drift** (the fact's number is not the number the tool was last told) → `improve_tool` **only** when a tool-level component owns the page's code AND the fence does not say `no_auto_fix`; otherwise a human. **evidence drift** (citation lost, artefact changed, sql fact out of tolerance, no value move) → **always** a human: a 404 at GOV.UK is not evidence a figure moved, and pointing a rewriter at a calculator on that evidence is `bugs_open/126` with arithmetic as the target. **fetch error** → **nobody**: CLM-008's rule, unknown is not loss, and a per-tool item on every 403 day trains people to ignore the queue.
- **producer set and item_key shapes** (owner ruling 2026-08-02 §1 — named here because that is the condition on converging a new producer onto an existing type): `improve_tool` gains a **third** producer, `refresh_evidence_base`, key `fact_drift:<fact_id>:<subject_key>:<site_id>` (the existing two are `check_tool_health` and `check_tool_acceptance`); **NEW `fact_drift_review`**, sole producer `refresh_evidence_base`, key `fact_drift_review:<fact_id>:<subject_key>:<site_id>`, handler `human-review`, status `needs_human_review`, classified `catJudgement` in `discovery_checks/verifier_coverage_test.go` in the same commit as the producer. **Two key prefixes, deliberately**: `refreshOnConflict` rewrites an open item's summary and spec but never its type or handler, so one prefix would let an open review item silently absorb a later auto-fixable finding.
- **the baseline, and why it is not a register key:** "value drift" is measured against **the value the tool was last TOLD** — the most recent `fact_drift` item's `spec.fact.new_value` for that (fact, tool); failing that, the same fact's value in the site's **previous** `evidence_base` row (a human supersede and an in-place UPDATE both leave the older row behind); failing that, **nothing fires**. A per-fact `last_swept_value` key was rejected: it would force a register write on sites that never opted in, and make a consumer the author of the human's row. A register with one row ever, or a newly declared fact, is silent by design — *"is the current number RIGHT"* is Piece 4's question (an RFC), not this mechanism's.
- **⚠ LANDMINE — `fact_drift` is PER-SITE and NESTED, and the counter beside it reads ZERO.** *Footprint: `refresh_evidence_base` result payloads.* It lives at `refresh_result->'results'->N->'fact_drift'`; there is no top-level key, and top-level `total_drifted` counts **citation** drift — it read **0** on both proof runs while each carried **13** entries. The obvious query says "it did not fire" and the neighbouring counter appears to confirm it; it was reported that way out loud before the full payload was dumped (mcalc lane, 2026-08-17). Read `results[N].fact_drift`, or `jsonb_path_query_array(collected_data, '$.refresh_result.results[*].fact_drift[*]')`.
- **⚠ LANDMINE — `install_fences.py` REFUSES, silently, to install a `facts` declaration on exactly the tools this mechanism exists for.** *Footprint: `*/acceptance/install_fences.py`.* Its rule 2 keys on the acceptance ladder's eligibility (`SKIP … not ladder-eligible on this site — a PLAN here would never be read`), and a decomposed or ported tool is not eligible — which is *why* the fan-out does not key on `toolEligibilityWhere`. The refusal rests on a premise this entry made false: since Piece 3 a declaring PLAN **is** read, by the name rule. Following "just re-install" yields a clean run, no error, and no key. Fixed on the mcalc copy with `--allow-ineligible`, fenced on the document declaring `facts` AND a current row already existing under that key. **LMC's `mortgages-stamp-duty` will hit the same wall.**
- **⚠ LANDMINE — a baseline that falls back to REGISTER history silently disarms a tool-facing check.** *Footprint: `refresh_evidence_fact_drift.go` `factDriftBaselines`.* This mechanism's first-contact case fires when a (fact, tool) pair has no baseline. The original code fell back to the previous `evidence_base` row when no prior finding existed — and **the register is re-verified DAILY, so a previous row essentially always exists carrying the correct value**. The baseline was therefore never nil, the first-contact case never fired, and the check stayed blind to `bugs_closed/225`'s shape *after a whole council round spent fixing exactly that*. **Two questions were conflated: "has the register's VALUE moved" (compare register rows) and "has this TOOL ever been reconciled" (did we ever file for this pair).** Only the second is this mechanism's, and only a prior FINDING answers it. Caught by the council's `editquality` seat at round 2, gating.
- **⚠ LANDMINE — `handler_agent = 'human-review'` names an agent that DOES NOT EXIST.** *Footprint: any `needs_human_review` producer.* Copied in good faith from `createStaleEvidenceItem` in the same file. **MEASURED 2026-08-16: zero `agent_definitions` rows of type `human-review`; the fleet's dominant convention is a NULL handler — 544 rows across 21 item types, including `ported_tool_fix`, the precedent this routing was modelled on — against only 22 rows using the string.** Naming a non-existent handler invites the dispatch loop to try to spawn it. Handler-less is how you say "a person decides". Raised by the guardian seat as advisory on an APPROVED round; corrected anyway.
- **⚠ LANDMINE — the page predicate is the AUDIT one, never the LIVENESS one.** *Footprint: `refresh_evidence_fact_drift.go` `factDriftIndexQuery`.* `p.status = 'active'` is this estate's liveness predicate and is documented as WRONG for anything choosing which pages to JUDGE — an archived page that was deployed is still being served. The fan-out uses the shared `datahelpers.NeverDeployedPagePredicateFor` (skip only archived AND never deployed), the same shape `check_unverified_claims` uses. Raised by the council's `debug_historian` seat at severity high, and the point stands even though the measurement came back clean: both motivating pages are `active`/`deployed` (so the first version would not have shipped inert for them), and fleet-wide it is 198 active tool pages against 6 archived. **The seat's real objection was that the equivalent check had been run for the eligibility predicate and NOT for this join** — one trap avoided next to one unexamined, which is how a fix acquires a blind spot.
- **⚠ LANDMINE — this is NOT the acceptance ladder's population, and using the ladder's predicate would have made it inert.** *Footprint: `refresh_evidence_fact_drift.go`, `discovery_checks/tool_eligibility.go`.* The fan-out resolves PLAN→page by the platform's own name rule (`p.name IN (subject_key, 'tool-'||subject_key)`, plus a tool-level component whose function is the subject key) — the rule Tier 4 already uses to find a tool's URL — **not** by `toolEligibilityWhere`. Measured 2026-08-16 against the live DB: that predicate's sole-component clause admits **neither** of the two tools this mechanism exists for — mortgagecalculator's `tool-stamp-duty` carries two components, and loanandmortgagecalculator's `mortgages-stamp-duty` three since its B2 decomposition. A fan-out keyed on eligibility would have been a check that could never fire on the tools that motivated it, and it would have read green. **A tool need not be acceptance-eligible to encode a fact.**
- **⚠ LANDMINE — a fence-level `facts` list asserts NOTHING at acceptance time.** *Footprint: any ```criteria fence carrying `facts`.* Tier 2 (`check_tool_acceptance.go`) and Tier 4 (`run_checks_action.go`) both ignore unknown fence keys, by design and unchanged. A green acceptance run on a fence carrying `facts` does **not** mean the figures were compared against the register; only the daily sweep reads the key, and only to ask whether the fact MOVED. Reading a green fence as "the numbers were checked" is `bugs_open/225`'s own failure mode one rung higher.
- **what it does NOT do, and must not be written up as doing:** it cannot tell a correct threshold from a confidently-wrong one — the register's own correctness is unguarded here (Piece 4, and the plan's §8 says so); it cannot see a page with no `pages` row (mortgagecalculator's repo-only `stamp-duty.html` twin, which carried the same defect, stays invisible); it needs a human to author each declaration; and it fires at most once a day.
- **sources:** `platform/orchestration/actions/criteria_facts.go`, `refresh_evidence_fact_drift.go`, `refresh_evidence_base_action.go` (the two wiring points + `EncodedByTools`/`FactDrift` result fields), `experience_criteria.go` (P11), `refresh_evidence_fact_drift_test.go`, `experience_criteria_test.go`, `discovery_checks/verifier_coverage_test.go`; `docs024_key_docs_latest/register_guards_code_phase_b/` (the lane — PLAN/RUNBOOK/NOTES/README_where_we_are, and the council submission); `mortgagecalculator_couk_adoption/PLAN_2026-08-09_facts_into_tool_acceptance.md` (Pieces 2+3, §5 landmines); `bugs_closed/225`; `bugs_open/288` (the class file)
- **relations:** CLM-021 (the BUILDER side of the same join — that one shows the register to a tool being built, this one tells a built tool when a fact moves), CLM-007 (the daily sweep this rides), CLM-008 (unknown-is-not-loss, why a fetch error files nothing), CLM-001 (supersede-then-insert; the previous row is this mechanism's baseline), TL-040 (`no_auto_fix`, honoured here), TL-042 (the non-fork routing posture reused), PBP-037 (pin WHICH facts, never their values), TL-045 (the tool-lifecycle cross-reference)
- **verify-later:** the first live emission — seed the mcalc `stamp-duty` fence, then induce by superseding `sdlt-ftb-relief-cap` 500000→550000 and assert the dry run names `stamp-duty`; and whether the `improve_tool` arm is ever reachable in practice, since both live SDLT fences carry `no_auto_fix` and neither page is a fork (so today every route would be human — proven by unit test only)

### CLM-023 — `verify_cited_cardinals`: the attribution gate — a quantity in self-attributing prose must appear in the field that prose CITES (bugs_open/335)

- **⚠ LIVE FALSE POSITIVE, found by the first two production runs and FIXED IN GO BUT NOT YET ROLLED (`590fb1f5b`): `B2B` is read as the quantity 2.** `cardinalDigitRe` is not word-anchored, so `B2B`→2, `S3`→3, `IPv6`→6, `Web3`→3. A point saying *"UK B2B SaaS engineering teams"* asserts no quantity and would be **dropped** unless its cited field happened to hold a stray digit — the worst failure shape for this gate, a false positive that silently removes legitimate content. It survived on leopardess only because that premise says "B2B" too. **The running binary still has it.** Residual, pinned by a test that fails if fixed: `GPT-4` still yields 4.
- **⚠ THE ENFORCEMENT ARM HAS NEVER FIRED IN PRODUCTION.** Both live runs (2026-08-22) recorded `dropped_unsourced: []`. The motivating false claim is gone from leopardess because the **prompt** half stopped the model emitting it, not because the gate caught it. Enforcement is proven by unit test only. ~~⚠ And the prompt half may be over-suppressing …~~ **REFUTED 2026-08-24** over 4 post-537 runs on 3 sites: **robot-hands.com KEPT** its sourced word numeral (*"across six actuation types"*, 1 before → 1 after), and webdesign's loss of *"sixty-three tools"* is the model obeying the **pre-existing** *"avoid our own catalogue or page count"* instruction — its `avoid_leading_with` names a tool/article count in **all three** orderings **including the pre-537 one**, which led with the count while its own avoid-list disclaimed it. The two sites split exactly along inventory-count (dropped) vs categorical-count (kept). **Read `avoid_leading_with` from the same row before attributing a missing phrase to a prompt change.**
- **status:** **LIVE AND PROVEN 2026-08-24 — `bugs_closed/335`.** Both halves live on chassis `v1.0.1332`; **6 post-537 runs across all 5 enrolled sites as of 2026-08-24**, gate demonstrably executed on every one (the `dropped_unsourced` key is present on 5 of 5 current orderings, and only the new code writes it), **zero unsourced cardinals estate-wide**, and the motivating false claim gone from leopardess across 2 re-runs of the same premise and pages. The negative control finally passed on live data: robot-hands rank 4 KEPT *"across six actuation types"*, whose cited `value_proposition` contains it verbatim. ⚠ **The ENFORCEMENT arm has never fired — total drops estate-wide, ever: 0 as of 2026-08-24** — so it is proven by mutation-tested unit tests, not by a live firing; a run containing no cardinals passes this gate trivially, so never quote a clean run as evidence it works. Previously: LIVE 2026-08-22 11:03Z — migration 537 applied against chassis `v1.0.1323`, after a capability probe (`grep -aq "verify_cited_cardinals" /proc/1/exe`) with two controls confirmed the action is registered in the running binary. ⚠ IT HAS NEVER FIRED: no offer-analyser run has executed since (`llm_call_log` newest 2026-08-19), so the behavioural proof — and its NEGATIVE controls — remain owed. Built 2026-08-21, council-APPROVED at round 3 (`9a8f1283`, 13 seats, 2 advisory, none high), tests green, INERT.** Two REVISE rounds each found a real defect — see `bugs_open/335` for both, they are the useful part. ⚠ **Advisory left standing (`bug_historian`): the durable-record requirement belongs in the ACTION, not in this entry.** Today `drop` mode records a removal only as an in-document marker with zero automated consumers — `bugs_open/034`'s shape — and **any future caller opting into `drop` inherits that gap.** The precondition below covers `offer_ordering`; a SECOND consumer must not be added until the durable record is built into the action itself. The Go half is committed and does nothing until the next chassis roll; the config half (`offer-analyser`'s gate step + prompt line) is **held** as `516b_HOLD.sql` precisely because a seed naming an unregistered action fails at runtime. **No live run yet** — so this entry claims a mechanism that exists, not one that has fired. The re-proof and its two controls are written down in `bugs_open/335` and must be run against leopardessconsulting.co.uk *after* the roll.
- **what:** A deterministic gate for the shape "LLM emits ranked items, each carrying prose PLUS the name of the source field it was drawn from". That name (`from_field` on `offer_ordering.lead_with`) is honesty machinery — a reader is meant to be able to check the claim by reading the field it names. This action makes that checkable: every cardinal in the prose must appear in the cited field, or the item does not ship. Config: `object_field` + `items_key` (the array), `source_field` (the object whose keys are cited), `text_key`/`citation_key` (defaults `point`/`from_field`), `on_violation` (`fail` default, `drop` opt-in), `dropped_key`. Generic by construction — nothing in it knows about offers.
- **the motivating defect:** `offer-analyser` wrote, at **rank 1** on a live site, *"the same stack that runs eight live sites built by this team"*, stamped `from_field: "trust_threshold"`. True count **23**; the number came from a page `meta_description` carried in the offer surface; the cited premise field contains **no number at all**. Caught by the leopardess lane one step before a writer consumed it — not by this lane, and not by any structural check B4 makes.
- **⚠ LANDMINE — a DIGITS-ONLY numeric gate cannot see this defect, and the nearest precedent is digits-only.** *Footprint: `verify_report_prose_action.go` `proseNumRe`, any new numeric-discipline check.* `verify_report_prose` is the right precedent and its numeric idea is reused here, but its token regex is `\d[\d,]*\.?\d*` — the defect was the **word** "eight". Reusing it unmodified would have produced a gate that passed its own motivating case while reading green. Its own doc comment already names the hole from the other end ("spelled-out numerals … are lower-case English too"). `TestDigitsOnlyScanWouldHaveMissedTheDefect` pins this: it asserts the defect point contains **no digits at all**, so deleting the word vocabulary fails the suite instead of quietly widening it.
- **⚠ LANDMINE — "one" and "zero" are not quantity claims, and including them puts the gate at 17% precision.** *Footprint: `verify_cited_cardinals_action.go` `cardinalUnits`.* **[MEASURED 2026-08-21, all 30 live `lead_with` points]** with one/zero in the vocabulary: **6 flags, 5 of them false** — "one click away", "in one workflow", "one of those categories", "the one you arrived with", "a restart from zero". Every one is an article, a pronoun or an idiom. Without them: **exactly 1 flag, the real defect.** They stay admitted on the SOURCE side, so a premise that legitimately says "one" still licenses a point saying "1"; the asymmetry is deliberate and commented at the declaration.
- **⚠ LANDMINE — the negative control named in the bug file CANNOT DISCRIMINATE.** *Footprint: `bugs_open/335` "How to verify a fix".* It proposes gaswholesalers.com, "whose rank-1 point legitimately carries premise-sourced specifics, must keep them". Measured: **none of gaswholesalers' six points contains a cardinal at all**, so it passes whatever the rule is — including a rule that bans every numeral. The controls that actually bite are **webdesign.co.uk** ("sixty-three tools", present verbatim in `value_proposition`), **robot-hands.com** ("six actuation types", likewise), and **robot-hands.com** rank 5 ("2–3 articles" against a premise that writes "2-3" — an en-dash/hyphen mismatch a naive substring check fails). Two of the three legitimate specifics on this estate are **spelled-out numerals**, so "ban word numerals" both fixes the bug and guts the artefact. All three are fixtures in the test file, verbatim.
- **⚠ LANDMINE — failing the run does NOT remove the bad artefact, because `write_site_spec` merges.** *Footprint: `site_spec_actions.go` `siteSpecDeepMerge`.* An array takes the scalar-overwrite arm, so a *successful* re-run replaces `lead_with` wholesale — but a run that FAILS at the gate writes nothing, and the previous row stays `is_current`. On leopardess the false rank-1 is already persisted, so `on_violation: "fail"` would leave it live indefinitely while reporting a working gate. That is why the offer-analyser is configured **`drop`**: the surviving points are written, the bad one is removed, and the removal is recorded in the artefact under `dropped_unsourced` rather than vanishing. `fail` remains the action's default (unsafe side off, per the owner ruling 2026-08-02 §2); `drop` refuses to write an **empty** array, because a husk that reads complete is the failure this estate keeps paying for.
- **what it does NOT do, and must not be written up as doing:** it does not check that the cited field is the *right* field, only that the number is in it; it does not validate non-numeric claims (a fabricated technology name passes untouched); it does not check the `why` clause (that is `features_open/030` v2(b)); it cannot tell a stale-but-sourced number from a current one — a premise carrying its own stale figure licenses it. `features_open/034`'s claims audit over spec prose remains the layer that asks whether the premise itself is true, and **335 sharpens 034's case rather than replacing it**.
- **⚠ PRECONDITION ON THE NEXT CHANGE — before `offer_ordering` gains its FIRST automated consumer, drop-mode must surface as a WORK ITEM, not an in-document note.** Today the drop is recorded inside the artefact under `dropped_unsourced` and nothing queries it; that is tolerable only because `[MEASURED 2026-08-21]` the aspect has **no automated consumer at all** — `strpos` over every live agent config finds the literal `lead_with` in `offer-analyser` alone, and no Go file reads it, so the only reader is a human looking at the spec. The moment a writer or renderer consumes this artefact, a false positive from the closed vocabulary (years, hundred-plus, a normalisation miss) would shrink live content with the only evidence buried in a field nobody queries — the `bugs_open/056` shape, a validation blocker discarding content where the discard is technically recorded and practically invisible. Raised by the council's `bug_historian` seat (corr `9a8f1283`, round 2).
- **⚠ THE CLEAN PATH WRITES THE DROP RECORD TOO, AS AN EMPTY ARRAY — do not "tidy" that away.** `write_site_spec` deep-merges, so a key the incoming document OMITS keeps the previous run's value and reads as current (`bugs_open/327`'s mechanism). Returning the object untouched on a clean run therefore left a *previous* run's `dropped_unsourced` attached to an artefact with nothing removed — an audit record accusing a clean ordering, permanently. Writing `[]` also makes "nothing was dropped" a POSITIVE statement, so `len(dropped_unsourced) > 0` is sound for a reader who cannot know the run history. Pinned by `TestCleanRunClearsAPreviousRunsDropRecord` and a second test using a configured `dropped_key`, so the clearing follows the configured name.
- **the shared-merge question, recorded rather than absorbed:** `bug_historian` argued (round 2, high) that choosing `drop` here routes AROUND `siteSpecDeepMerge`'s array-overwrite instead of fixing it. Declined, on three grounds: wholesale array REPLACE is the *correct* semantics for a versioned spec write (element-wise merging of an ORDERED list would produce a ranking neither run wrote); the landmine cited is about `site_specs.pinned` being ignored, a different defect; and `[MEASURED 2026-08-21]` `write_site_spec` is called into **17 aspects by ~16 live agents** with top-level arrays pervasive in stored specs (`identity` 110 array keys across 25 sites, `content_direction` 102, `briefing` 52), so changing that arm is a shared-seam change belonging to its own architecture review, not a bug patch — `bugs_closed/124`'s REJECTED verdict is the precedent for what happens when one arrives that way.
- **sources:** `platform/orchestration/actions/verify_cited_cardinals_action.go`, `verify_cited_cardinals_action_test.go`, `registry.go`; `bugs_open/335`; `docs024_key_docs_latest/vigilant_designer_offer_analysis/` (the lane); migration `516b_HOLD.sql`
- **relations:** BIZ-032 (the agent this guards — its register entry says "its inputs are unverified prose"; 335 shows the ceiling is lower still, because the inputs include unverified PAGE copy), CLM-020 (`NegationGuard`, the other exported-and-parametric claims primitive), `verify_report_prose` (the digits-only precedent this deliberately diverges from), `bugs_open/161` (the opposite direction — a register ratifying a page claim), `bugs_closed/262` (same family one layer down)
- **verify-later:** the first live run after the chassis roll — leopardess must lose the "eight live sites" clause and keep five points, and webdesign/robot-hands must re-run byte-identical on their cardinal-bearing points. Also unmeasured: whether a legitimate YEAR ("updated for 2026") in a point trips the gate, since years are cardinals and premises rarely carry them.

### CLM-024 — `verify_acceptance_predicates`: a finding may carry a MACHINE-CHECKABLE half of its own acceptance test, and it is kept only if it already REFUTES the page (features_open/030 v2(d))

- **status:** **LIVE AND EXERCISED 2026-08-24 22:08Z** — migration `601` applied by hand after a capability probe found the action PRESENT on both replicas of the fresh chassis (positive + negative controls); ledger row recorded (`applied_by='record-only'`). **First live run: `checked 4, kept 3, rejected 0, subjects_loaded 137`** on webdesign.co.uk — the model wrote three predicates unprompted (the key is deliberately absent from the OUTPUT skeleton), all three refuted at emission, and one finding was left bare, so **the silence arm fired on its first outing**. All three reached `site_work_items.spec` intact, which proves the `write_audit_findings` passthrough on the hand-written map decode path — the trap the lockstep test was written for — at the artefact rather than in a test.
- **⚠ AND THE FIRST RUN ALREADY DEMONSTRATED THE FALSE GREEN, machine-checkably.** The `index` finding was dispatched to `page-build-handler`, which **rebuilt and deployed** the page (commit `ee88ba3c`, 22:25Z) and closed the item **`complete`**; the page was rebuilt again 2026-08-25 11:23Z. Its own stored predicate **still refutes** — the served `<meta name="description">` is unchanged (*"Sixty-three browser tools…"*), and the model's chosen needles (`paired`/`pairing`/`article`/`guide`) are absent, so the refutation arrives through the *"states none of `before` at all"* arm. Pinned by `TestTheFirstLiveEmittedPredicatesStillRefuteAfterTheFix`, which runs the real exported evaluator over the three live predicates and the served strings. **This is the first time this lane has had a machine verdict on a closed item rather than a reading by eye.**
- **⚠ THE REFUSAL ARM HAS STILL NEVER FIRED (`rejected: 0`), which is CLM-023's residual in a new place.** Every predicate the model wrote was already coupled to its finding, so the gate has had nothing to refuse. It is proven by mutation-tested units, **not** in production — so never quote a clean run as evidence that the refusal works, and note that a run in which the model writes NO predicate passes this gate trivially.
- **superseded status line (kept so the trajectory is visible):** ~~**BUILT AND TESTED, NOT LIVE, as of 2026-08-24.**~~ Go committed and proven against committed HEAD (`scripts/verify-head-builds.sh --test`, 26 new tests). Config is migration `601_offer_analyser_acceptance_predicates_HOLD.sql`, **deliberately held**: a step naming an action the running binary does not register is rejected whole ("requires a topic"), so the image must roll first. ⚠ **Do not read "built" as "running"** — until 601 is applied by hand, no finding anywhere carries a predicate, and the correct reading of an empty `acceptance_predicate` column is "this was never switched on".
- **what:** An optional, per-finding structured predicate carried ALONGSIDE the prose `acceptance_test`, plus the deterministic gate that decides whether it may be stored. Three shapes only — `text_absent`, `text_present` (with `min`), `text_order` — over exactly two fields, `pages.meta_description` and `pages.title`, of one named page. `text_order` accepts the reserved needle `$cardinal`, which resolves to the first quantity in the string (digits **or** words) through CLM-023's own scanner, because *"…before any count of tools"* is the commonest ordering clause in the live corpus and it names no string. The evaluator is exported as `EvaluateAcceptancePredicate` and depends on neither `ActionParams` nor a DB handle.
- **the two rules that make it safe, and they are the whole design:** (1) **refute-only** — a predicate is a NECESSARY condition of the prose test, never sufficient, so satisfying it means "not refuted" and never "the test is met"; (2) **it must refute at emission or it is discarded**, because the finding asserts the page is wrong TODAY, so a condition that genuinely expresses the finding must fail TODAY. Rule 2 is the only property of a predicate that is checkable at the moment it is written, and it is what excludes the vacuous case — a needle present nowhere, a clause already satisfied — which would otherwise be stored as honesty machinery and grade green for ever. Refusals are recorded per finding under `acceptance_predicate_rejected`; they are never dropped silently.
- **why it was built, MEASURED rather than argued:** `[MEASURED 2026-08-24, live]` of the **37** acceptance tests `offer-analyser` has written to date, **three sit on work items marked `complete`** — page rebuilt, deployed, commit sha in the item's `result` — while their own stated criterion is refuted by a one-line check over the exact field the test names: webdesign.co.uk/index twice (*"the meta description must state the zero-data or zero-account promise **before** any catalogue count"* against a served meta opening *"Sixty-three browser tools…"*, the served `<meta>` read and confirmed byte-identical to the column), and robot-hands.com/gripper-catalog-index (*"appears as a clickable link in the site header navigation"* — absent from the served header). A fourth is refuted at `wont_fix`, so it is not a false green.
- **⚠ LANDMINE — `pages.in_header` IS NOT THE RENDERED NAV, and this is why nav is NOT in the vocabulary.** *Footprint: `pages.in_header`, `pages.nav_order`, `pages.rendered_header`, any check phrased "the header nav contains…".* `[MEASURED 2026-08-24]` leopardessconsulting.co.uk has **13** pages with `in_header = true`; its **served** header renders **7** destinations (plus a logo link and a CTA). A predicate reading the column would have refuted *"the header nav contains no more than seven items"* — a test that **holds** at the artefact. That is a confident FALSE REFUTATION, the exact failure this design exists to avoid, and it was the first thing this session's own measurement got wrong. The column that looks like the escape route is empty: `rendered_header` is `''` on **all 35** active pages of robot-hands.com. Nav waits for an instrument that reads the served page.
- **⚠ LANDMINE — a plain substring search refutes a page that passes.** *Footprint: `needleRe`, any text-matching acceptance check.* The live corpus asks for *"no instance of 'we' or 'our'"*; unanchored, "we" is inside "web", "answer" and "however" — on a site about **web** design. Every needle is therefore matched at word boundaries, applied only where the needle's own edge is a word character so `"client-side"`, `"no account"` and `"(beta)"` are unaffected. Held by a test carrying both the real string and a positive control.
- **⚠ AND THE OPPOSITE LANDMINE — an empty findings array is not the same as an unresolvable findings path.** *Footprint: `write_audit_findings_action.go` `parseAuditFindings` third return.* A RECOGNISED empty list is the auditor saying "nothing is wrong here", which **arms silence retraction** (`bugs_open/213` D1 half two). So this gate omits the `findings` key entirely when its input path does not resolve, which drops `write_audit_findings` onto exactly the branch it takes today — zero items, no retraction — and passes a genuinely-empty array through AS an empty array. Held by two tests.
- **what it does NOT do, and must not be written up as doing:** it does not grade the prose test (two thirds of the corpus is judgement); it does not read page BODY copy, because the offer surface passes none (`features_open/030` v2(a)) so a predicate over body text would be a claim about something its author never saw; it does not evaluate anything at COMPLETION time, so **today it prevents no false green** — it makes one machine-checkable; and it does not touch nav, page existence, links or click distance.
- **⚠ PRECONDITION ON THE NEXT CHANGE — nothing automated reads `acceptance_predicate` yet, and the consumer that should is a COMPLETION-time check.** `complete_work_item_no_change.go`'s own comment records that grading `acceptance_test` "needs a producer-side contract change first" (that field is free prose; 10 of 15 live values name a computed property, 2 carry clauses no probe can assess). This IS that change, for one producer. Whoever builds the consumer inherits CLM-023's residual in the same shape: the requirement belongs in the **ACTION**, not in one call site, or the next caller inherits the gap.
- **the passthrough on a shared action, named because it is a seam:** `write_audit_findings` (many producers) gains two optional struct fields and two `spec` keys, both absent-by-default — the 2026-08-02 opt-in-with-the-unsafe-side-off shape. ⚠ It decodes findings **twice** — struct tags for a JSON string, a hand-written map for a native list — and the native list is the shape an upstream ACTION hands over, so a field added to the struct and forgotten in `findingsFromList` is dropped in silence on the only path this feature uses. Held by `TestFindingsFromListPopulatesEveryTaggedField`, which walks the struct's json tags by reflection.
- **COUNCIL: APPROVED 2026-08-24, corr `ef482d1c-b36d-40c0-a40c-772656116016`** — 14 seats, 3 of the 9 raised objections counted as advisory, **none high**. ⚠ Round 1 was **killed by a chassis roll** 10 minutes in (frozen `review_guardian` 18:30:18Z, new pods 18:32Z) and re-fired unchanged on the same trail; that is `LANDMINES.md`'s roll-kills-a-council entry for at least the third time.
- **What the objections changed, because two seats found the same real hole:** `editquality` and `debug_historian` independently objected that `loadAcceptancePredicateSubjects` filtering `pages` on a hand-written `status = 'active'` sits inside a named landmine — a `pages` status filter that matches nothing would make every predicate *"page not on this site's surface"* and take **the whole gate silently inert**, wearing the same face as the acceptable outcome ("the model wrote nothing storable"). **The premise is not live today** — `[MEASURED 2026-08-24]` `pages.status` is `active` **805** / `archived` **66**, and the query returns **35–137** rows for each of the five enrolled sites, never zero — **but a measurement that a hazard is not live is not a guard against it**, so both arms were taken: the lifecycle predicate is now `datahelpers.PageWantedLivePredicateFor` (the single place that vocabulary is written down, per the landmine's own *"prefer the helper"*), and an empty subject set is now **loud** — a distinct `Warn`, a `subjects_loaded` count returned on every run including clean ones, and a rejection reason that **blames this step rather than the named page**. Mutation-tested (`TestAnEmptySubjectSetIsReportedAsAFaultHere`), because a mock's own bookkeeping cannot assert that absence.
- **Two reuse objections, answered in the file's own header so the next reviewer inherits the answer:** (1) `datahelpers/claims.go` owns the nearest-looking matcher and was NOT reused — its compiler is `regexp.Compile("(?i)"+p)` over a pattern an author wrote as a REGEX, with a `QuoteMeta` fallback; the needles here are LITERAL phrases an LLM emitted, so that compiler would make `"(beta)"` a capture group, let `"3.5"` match `"375"`, and drop the word boundaries that stop `"we"` matching inside `"web"`. Same sentence, opposite contract. (2) The **revalidation family** (`revalidate_unverified_claims.go` / `check_unverified_claims.go` / `ScanDeployedClaims`) is the closer precedent, correctly named: it asks whether DEPLOYED component HTML still asserts something the site's evidence register does not support, over a fleet register, to RETRACT. This asks whether the condition a FINDING says would make the page right is false of that page's metadata today, over a per-finding clause, to decide whether it may be STORED. ⚠ **They converge at the one thing not built here** — a completion-time consumer of these predicates is asking a revalidation-shaped question, and whoever builds it should read `reviewRevalidators` before writing a third loop.
- **The third-consumer search `bug_historian` and `guardian` asked for, done rather than asserted:** the worry was a reader that unmarshals a work-item spec into its own local struct and would drop the new keys exactly as `findingsFromList` would have. `[MEASURED 2026-08-24]` there is **exactly one** such reader in `platform/`, `internal/`, `pkg/` and `cmd/` — `specAuditSource` in `write_audit_findings_retraction.go`, which decodes a single named key and carries nothing; every other consumer extracts named keys from the jsonb (`spec->>'…'`), which is additive-safe by construction. No third decoder exists to lose them.
- **⚠ ONE OBJECTION IS FACTUALLY WRONG AND IS RECORDED AS SUCH.** The `architecture` seat wrote that *"write_audit_findings gains 2 more optional keys via this migration"* against the ruled N=10 optional-key budget. It does not: the budget counts an action's `ActionInputSpec.Optional` **step-config** keys, and `WriteAuditFindingsInputSpec.Optional` is unchanged at **1** (`findings_field`). The two new keys are fields of the LLM's FINDING objects, not step config. The new action declares **1** optional key of its own. So the accumulation trigger the seat was right to reach for does not fire here — but the reasoning it applied would have counted the wrong thing, and the distinction (config surface vs data surface) is the one that keeps that budget meaningful.
- **sources:** `platform/orchestration/actions/verify_acceptance_predicates_action.go`, `verify_acceptance_predicates_action_test.go`, `registry.go`, `write_audit_findings_action.go`; migration `601_offer_analyser_acceptance_predicates_HOLD.sql` (+ `_ROLLBACK`); `features_open/030` §10 v2(d); `docs024_key_docs_latest/vigilant_designer_offer_analysis/` (the lane)
- **relations:** CLM-023 (`verify_cited_cardinals` — this reuses its cardinal scanner via `digitCardinalSpans`, extracted for the purpose, and inherits its self-attribution lesson: a predicate asserts its own checkability exactly as `from_field` did), `write_audit_findings` (the shared write this sits in front of), `complete_work_item_no_change.go` (the consumer this unblocks and does not build), DOC-068/DOC-072 (the browser-run criteria fences — the vocabulary deliberately NOT reused here, because these claims are about served metadata and need no browser; the offer from that lane is `CONTRIB_2026-08-04` in the lane dir), `features_open/030` v2(a) (widens what a predicate could address, by putting page content on the surface)
- **verify-later:** the first live run after 601 is applied — how many predicates the model actually writes (zero is possible and acceptable: the key is deliberately absent from the OUTPUT skeleton), how many are refused and for what, and whether any refusal names a page that IS on the surface (that would be a defect in `predicatePageName` or in the surface filter, not in the model). Also unmeasured: whether `min` is ever used, and whether the model reaches for `text_order` on a page whose meta it was shown truncated at 160 chars — the evaluator reads the FULL column, so a needle beyond char 160 is visible to the gate and was not visible to the author.


### CLM-025 — the COLD audit: `claims-auditor` runs on an EMPTY register, fails CLOSED, and receipts every run (bugs_open/380, migrations 597 + 601)
- **status:** **LIVE AND PROVEN 2026-08-24.** 597/601 applied and recorded; hand-dispatched at garden-tools.uk (corr bcf23316): the first two findings were the two sentences the owner quoted as invented ("We garden ourselves, and we test what we can get our hands on"; "Where we can, we buy the tool at the same price a reader would pay"), severity high, suggestion in the owner's own framing. Control at leopardessconsulting.co.uk (corr be39ddba): roster arm, one real drift finding. Council: `Council-Submitted: e684fc8d` (config slice).
- **what:** The opt-in gate `check_opted_in` (branch on `evidence_facts.facts_text`, `else_step: complete`) is DELETED. `load_evidence_facts` runs into `load_page_text` unconditionally and has NO `error_step` — a query failure FAILS the run (RFC_017). The audit prompt's register section is a two-arm branch: the roster as before, or a cold-register instruction naming the practice/possession/track-record/named-relationship classes with an explicit do-not-report list (could-framed, negations and honest disclosures, quoted third-party speech, generic industry statements). `ALLOWED ENTITIES` is nil-guarded. Per-page text cap 12,000 chars (was 3,500). Every run writes ONE `doc_notes` receipt (`pipeline`/`claims-audit`, categories `audit-ran|audit-findings`) — a MISSING row means the run did not happen. Findings still file `claims_unverified` (`claims_llm_<domain>`, HITL-terminal, `recurrence_expected:false` now explicit per migration 572).
- **why it exists — the inverted safety property:** the amount of checking a site received was proportional to how much verified material it already had; a site that knew NOTHING got no fact assignment and no audit. `facts_text` is `string_agg` over `facts[]` → NULL for no register AND an empty one, so the skipped population was 33/48 sites `[MEASURED 2026-08-24]`, not the 29 with no row. Designed 2026-07-20 (`claims_verification/PLAN_2026-07-20_gaswholesalers_second_site.md` §3, `posture: cold_audit`) and never built.
- **⚠ LANDMINE — the page-text extraction was eating most of every page (601).** *Footprint: `load_page_text.config.query`.* A PostgreSQL regex takes the greediness of its FIRST quantifier, so `<style[^>]*>.*?</style>` over an unordered `string_agg` matched first-`<style`-to-last-`</style>` across components; the owner's sentence had reached the model NOWHERE. Now stripped per component, lazy first quantifier, `ORDER BY pc.position, pc.slot_name` (8,266 chars for how-we-assess vs 3,732). Full entry in `LANDMINES.md`.
- **what it does NOT do:** it does not mint a register (owner D1: absence IS the cold posture); it does not refuse anything (HITL-terminal); the LLM item carries no `spec.page_id` and parks under the revalidator's `spec_no_page_id` arm (pre-existing; a per-page follow-up is a later slice); the `needs_human_review` queue it feeds has no working surface (bugs_open/033).
- **sources:** `docs/agent_docs/sql_for_agents/597_claims_auditor_runs_cold_and_fails_closed.sql`, `601_claims_auditor_page_text_strips_per_component.sql` (+ ROLLBACKs); `claims_verification/SEED_claims_auditor.sql` (first seed ever); `bugfix_380_claims_fail_open/` (PLAN, NOTES, RUNBOOK §2-3, `TRIGGER_claims_audit.sh`).
- **relations:** CLM-006 (superseded), CLM-027 (the clock), CLM-026 (the deterministic sibling), PBP-037 (598 makes the planner emit the object form with `facts: []` on factless sites so the writer's `facts_scoped` arm fires — proven end to end with no Go change), RFC_017, RFC_023, RFC_003 §8 Q2 (owner 2026-08-24: NO).
- **verify-later:** the first rotation tick on a never-audited site (600, hourly), and the receipt row it must leave; the writer arm (599_HOLD) after the owner's plaintext read.

### CLM-026 — the PRACTICE-CLAIMS family (`claims_practice.go`) + the `operating_history` attestation: deterministic, warning-only, NOT in the refusing union (bugs_open/380)
- **status:** **BUILT AND TESTED 2026-08-24, INERT until an image rolls.** Calibrated on the full live corpus (1,867 components): 12 findings on 6 sites — 7 on garden-tools.uk (all invented), 3 true-practice on operating sites (the attestation case), 1 clear false positive ("how we test your idea", idea.uk). Council: separate submission (S-go).
- **UPDATED 2026-08-27 (bugs_open/414) — SIX patterns, and P6 breaks two of this family's own rules on purpose.** P6 is the documentary-diligence conjunction (verification verb + `against` + a named rulebook + an exhaustive unit-by-unit idiom). It is **not first-person**, because the sentence that filed 414 is a bare participle with no subject ("…, checked against the FCA handbook, rule by rule") and a subject anchor would have been inert on the actual defect; the four-part conjunction is the substitute anchor, and it is measured — idiom alone hits **22** live components, verb+rulebook alone **13** (including a site's own correct imperative "Check your loan against the FCA rules"), both together **3**, the planted ones, 0 of the other 2,402 `[MEASURED 2026-08-27]`. It is **documentary, not physical**, which fits this family's real subject (claims about US that no register can adjudicate) rather than its verb list. `practiceHedgeRe` gained an optional intervening subject so "If we check a guide against the handbook, rule by rule, we say so" is hedged — P6 anchors on the VERB deliberately, since moving the anchor earlier would put "have NOT been checked" outside `negatedClaimMatch`'s backwards window and break suppression that works today. **Two residuals are pinned by a test that asserts they FIRE** (a third-party subject; the bare-"nothing" disclosure, which `negationCueRe` cannot see) and **the attestation coupling is pinned too** — an `operating_history` attestation exempts P6 as well, which is about different work from physical practice `[MEASURED 2026-08-27: 0 sites carry either attestation]`. Why not CLM-015's refusing set: a compliance-services client could truthfully say it, and at blocker this layer would refuse the correcting disclosure it exists to encourage. Owner decision 2026-08-27. New consumer: CLM-030 scans SPEC text with this family.
- **what:** `ScanPracticeClaims(WithSuppressed)` — five first-person patterns anchored on PHYSICAL verbs with objects (test/buy/weigh/inspect/garden/cook; measure <the>; record <the weights>; receive/are sent samples; manufacturers send US). Findings carry `Check == "practice_claim"`. Exempted only by a complete `OperatingHistoryAttestation` (`attested_by`, `attested_at`, `evidence`) stored in `evidence_base.operating_history` — a RECORD a person signs, mirroring CLM/CGV-033's `regulated`. Negated and hedged matches ("if we test…") are returned in `suppressed`, never dropped silently. `cmd/claimscan` prints `PRACTICE` lines, excluded from the exit code.
- **the contract that makes it safe, pinned by a mutation test:** `TestPracticeFamilyIsNotInTheRefusingUnion` — the family is NOT unioned into `ScanAllBannedClaimsWithSuppressed`; every consumer of that union maps a finding to a refusal, and "we test every tool" is not false-by-construction (a real reviews site does). The gate (`validate_page_content`) records it at `practice_claims_severity` (default `warning`; `warning|error|blocker`; per-step config). A fleet-wide flip to refusal is RFC_003 §8 Q1 — architecture review, not a config edit.
- **⚠ LANDMINE — `ParseEvidenceBase`'s nil rule widened, and the numeric scan's ARMING moved to register CONTENT.** *Footprint: `HasScannableRegister()`; the three `ScanUnregisteredNumbers` call sites (gate, discovery check, claimscan).* An attestation-only base (regulated OR operating_history) now parses non-nil (else the exemption never fires) but must NOT arm the unregistered-number scan — against an empty fact list it reports every figure at `error` and refuses the build (the bugs_open/364 class). CGV-033's widening carried that latent hazard; zero live attestation-only registers `[MEASURED 2026-08-24]`, so no live site changed. The guard lives at the CALL SITES, not inside `ScanUnregisteredNumbers` — a pre-existing test scans against a deliberately empty base and that contract is kept.
- **what it does NOT do:** it is not the closer — at `warning` nothing refuses; the LLM cold audit (CLM-025) is the closer and the writer arm (599) the prevention. It does not catch "Manufacturers do occasionally send review samples" (no "us"; precision first — the cold audit reported it). Not yet wired into the discovery check (slice 2b, needs the item-queue cost measured) or the save floor.
- **sources:** `platform/orchestration/datahelpers/claims_practice.go`, `claims_practice_test.go`, `claims.go` (field, nil rule, `HasScannableRegister`), `platform/orchestration/actions/validate_page_content.go` (`checkPracticeClaims`, `practiceClaimsSeverity`), `discovery_checks/check_unverified_claims.go` (call-site guard), `cmd/claimscan/main.go`.
- **relations:** CGV-033 (the template and the sibling attestation), CLM-015 (nil-safe fleet-wide set — this family is deliberately NOT that), CLM-018 (the floor argument, deferred), CLM-020 (`NegationGuard` semantics reused via `negatedClaimMatch`), RFC_003 Q1, owner 2026-08-02 §2 (opt-in escalation, unsafe default OFF).
- **verify-later:** after the image rolls, one `validate_page_content` run on garden-tools carries `practice_claim` warnings in its persisted result; the discovery-check wiring decision with the measured fleet count above.

### CLM-027 — `claims-audit-rotation`: the auditor's clock (migration 600; owner decision D2)
- **status:** **LIVE 2026-08-24** (applied after the 601 demand control). 3600s tick, one due site per tick, 7-day per-site window, never-audited first (`NULLS FIRST`).
- **what:** 590's rotation shape: `scheduled_tasks` row → `claims-auditor`, pre_query selects and STAMPS one site in `site_discovery_rotation (agent_type='claims-auditor')`; `sites.status IN ('active','deployed')`, `locked_at IS NULL` (an owner HALT must not draw audits), no claimed build item, and a page with `build_status IN ('deployed','active')` (the auditor's own read predicate). Yields `site_id, domain` of an EXISTING row — `ensure_site_record` upserts by domain, so no site is created. Cost bound ~4 sites/day, ~120 Sonnet calls/month.
- **⚠ the stamp records SELECTION, not completion** — coverage questions join the CLM-025 receipts; `RUNBOOK_claims_fail_open.md` §3 carries the query (`selected_but_no_receipt` = a FAILED run, not a clean one).
- **sources:** `docs/agent_docs/sql_for_agents/600_claims_audit_rotation.sql` (+ ROLLBACK).
- **relations:** CLM-025, WFA/590 (`sitemap-refresh-rotation`, the template), bugs_open/033 (the queue it feeds), **bugs_open/386** (a refreshed counting fact convicts every page still rendering the old value — this clock AMPLIFIES that class), **CLM-028** (the fix). ~~a rotation finding with a `nearest_fact_id` whose `verified_at` is newer than the page's render is a stale render, not an invention~~ — **CORRECTED 2026-08-25 by the bugs_open/386 lane: `nearest_fact_id` has ZERO Go hits repo-wide.** It is a field in the auditor LLM's output JSON (`claims_verification/SEED_claims_auditor.sql:70`), so the discriminator exists only on the LLM audit path, not on the Go `unregistered_number` path that actually refuses the rebuild — and at the build gate it is undecidable in principle anyway, because the gate runs NOW against the CURRENT register, so the fact's `verified_at` and "now" are both today. Cost: the claim was relayed into a handoff and this entry and read as ground truth for a day.

### CLM-028 — fact-value HISTORY: the register remembers the readings it actually held, so a page is not convicted for being a day old (bugs_open/386)
- **status:** ~~BUILT 2026-08-25, NOT LIVE~~ → ~~LIVE IN THE BINARY, ARMED ON NOTHING~~ → **LIVE AND ARMED 2026-08-25 (migration 626, owner-authorised): five facts on fundamentallyai.com, and the five convictions are GONE at the artefact — claimscan over 91 live components against the LIVE register returns 0 findings, down from 5, while the negative control (a never-held `11514` one away from a genuinely-held `11513`, same window) is STILL FLAGGED.** Council `Council-Submitted: 27a368f0-cef0-4ade-ae19-d9a518345c17` for the arming; `18dba069` APPROVED for the code. ⚠ **The WRITER half is still unexercised** — 626 SEEDS history, it does not prove `recordFactHistory` writes it; the demand control is the next `evidence-freshness` tick that MOVES one of the five, which should append the outgoing reading itself and report `FactsHistoryRecorded > 0`. Earlier status, kept because it is the shape of the thing: **LIVE IN THE BINARY 2026-08-25 (chassis `v1.0.1339`).** The first half of "inert twice over" is discharged: both replicas carry `retain_history`, probed at `/proc/1/exe` as a CAPABILITY with a present-control (`context_terms`, found) and an absent-control (found on neither). ⚠ **Do not date this roll from `service_binary_capabilities`** — it was measured STALE for this very roll (its newest `agent-chassis` row named a commit carrying none of that day's code while both binaries had it). The second half stands: `retain_history` defaults false and **zero facts carry it** `[MEASURED 2026-08-25]`, so nothing has changed for any page. **The detector half is now behaviourally PROVEN offline** — armed vs unarmed over 91 live components: 5 findings → 0, all five disappearances on the register-rendering component and none in prose; and the negative control, a never-held `11514` injected one away from a genuinely-held `11513` in the same window, is **still flagged**. So arming spares the values the register held and nothing adjacent. So a green fleet after the roll is the EXPECTED result and is **not** evidence the mechanism works — see verify-later. Council `Council-Submitted: 18dba069-c142-4828-8e59-03453d04f72b`.
- **what:** two optional fields on `EvidenceFact` — `retain_history` (opt-in, default false) and `history` (`[{value, verified_at}]`, capped at `FactHistoryMaxEntries` = 90, ~3 months at the daily refresh cadence). The refresh appends the **outgoing** reading before overwriting (`recordFactHistory`, raw-map so unmodelled human keys survive); `numberSupported` then treats an **EXACT** match against a retained reading as supported, inside the existing `context_terms` gate. A page that published a figure while the register held it did not invent it.
- **the defect it closes:** the daily `evidence-freshness` refresh overwrote `value` and DISCARDED the prior reading (`refresh_evidence_base_action.go:534-544`), so every deployed page rendering yesterday's number was convicted as `unregistered_number` at **error** severity — which refuses the very rebuild that would fix it, and is indistinguishable in the bugs_open/033 queue from a real fabrication. Self-inflicted and nightly.
- **⚠ LANDMINE — arming a fact WIDENS what a green scan means, so run the control BEFORE arming, never after.** *Footprint: `retain_history` in any `site_specs` `evidence_base`; `historySupports`; `cmd/claimscan`.* Every retained reading is a number the scan will accept near that fact's context terms. Before arming: diff `cmd/claimscan` findings baseline-vs-armed over the whole site and read every finding that DISAPPEARS in ≥300 chars of context. Anything beyond the known stale-render set means the terms are too broad. Three things bound the surface — arming is a per-fact human act, the context-term gate is unchanged, and the cap is enforced at the **reader** as well as the writer (a guard living only in the writer is not a bound).
- **why EXACT and not the bug file's proposed monotonic RANGE:** `[MEASURED 2026-08-25]` **8 of 29** sql-sourced number facts have FALLEN at least once (`aao-orchestrations` 17 falls/34 transitions, range 1,494–7,281; `C4-orchestration-state-records` 17/38, range 1,625–90,790), corroborating `218_evidence_facts_for_043_sites.sql:48`. A `monotonic` flag would encode a false premise into the schema, and a range vouches for every intermediate value nobody published. Exact-match against readings the register **actually held** needs no monotonicity assumption and cannot invent a value in between.
- **what it does NOT do:** it does not rescue a **falling lower-bound claim** ("over 1,600 a day" when today's rate is 1,494) — history is exact-only, and applying the tolerance to history instead would pin support at the all-time MAXIMUM for ever, which is CLM/bugs_open/364 §2's accidental-support gradient with time as the amplifier. That class belongs to the owner's "at least N" ruling **with the floor set below the historic LOW** (`bugs_open/386` §4b; the census query is `bugs_open/386`'s lane RUNBOOK §7). It also does not re-render anything: the stale page stays stale, it merely stops being called dishonest.
- **sources:** `platform/orchestration/datahelpers/claims.go` (`RetainHistory`, `History`, `FactHistoryEntry`, `FactHistoryMaxEntries`, `historySupports`, the arm in `numberSupported`), `claims_history_test.go`, `platform/orchestration/actions/refresh_evidence_base_action.go` (`recordFactHistory`, `siteRefreshResult.FactsHistoryRecorded`), `refresh_evidence_fact_history_test.go`. 14 tests; all 7 guards mutation-proved in an isolated worktree.
- **relations:** CLM-014/CLM-016 (the surface gate and how this layer is measured), CLM-027 (the rotation that AMPLIFIES the defect), `bugs_open/364` §2 (accidental support — the hazard this design is shaped to avoid), `bugs_open/033` (the queue whose credibility the false positives were spending), owner 2026-08-02 §2 (new authority on a shared seam ships opt-in, unsafe default OFF), RFC_022 (opt-in + unsafe-default-OFF + zero named consumers ⇒ not architecture-scope; the consumer count was enumerated, not asserted).
- **why a second store rather than reading the superseded `site_specs` rows** (council `reuse_agent` + `prior_art_librarian` asked, and the submission had not argued it): `numberSupported` is a PURE method with no DB handle, and one of its consumers — `cmd/claimscan` — imports no database package at all and scans from a hand-exported JSON file. A live read would thread a DB handle into a pure scanner an offline operator tool depends on. The superseded rows are also an audit trail rather than an index (answering "did this fact ever hold N" means unmarshalling every revision of the whole register, per number, per component) and they are UNBOUNDED — `site_specs` has no retention job — so reading them directly would accept every value the fact ever held, for ever, defeating the cap. They keep their job as the Phase 2 BACKFILL source.
- **⚠ two council advisories carried forward as standing constraints.** (1) **Arming is a one-fact-at-a-time reviewed operator act, never a bulk toggle** — the gate is per-fact DATA state, not a code-level kill switch, so a careless seed could arm many facts at once and turn a contained feature into a fleet-wide behaviour change with no code review in the loop (`guardian`). (2) **`History` is a general-purpose "formerly-true value" primitive, not counting-fact-specific.** If a second consumer class arms it for a DIFFERENT reason, that accumulation crosses into `needs_rfc` — the cap and the opt-in default police volume, not semantic scope creep (`architecture`).
- **the Phase 2 acceptance test is sharper than "read what disappears"** (`compliance`): every finding that disappears when a fact is armed must sit on the register-rendering component. **A disappearing finding in free-text PROSE is the accidental-support signal, not a stale render, and arming that fact would quietly authorise invented copy to pass.**
- **verify-later, and the demand control is the load-bearing half:** after the roll, `FactsHistoryRecorded` stays **0** until a fact is armed AND its value moves — that is correct, not a failure. The honest proof is the paired run: the same corpus scanned with an armed register (stale findings gone) and with the unarmed live one (stale findings present). Without the unarmed control a pass cannot be told from a scan that stopped scanning.

### CLM-029 — `writer_block_guidance`: human prose that SURVIVES writer-block regeneration, on both composition paths (bugs_open/387)

- **status:** built + mutation-verified 2026-08-25 (`bugs_open/387` session, as agreed with the `bugs_open/288` lane, owner of the regeneration path); INERT until an image rolls, and **zero consumers by construction at ship time** (`SELECT count(*) FROM site_specs WHERE data ? 'writer_block_guidance'` = 0, 2026-08-25) — the RFC_022 shape: opt-in, unsafe default OFF, consumers enumerated not asserted.
- **what:** an optional string key on the `evidence_base` document. When present, `composeWriterBlock` (`refresh_evidence_base_action.go`) appends it VERBATIM as the final section of every composed block, and `composeScopedWriterBlock` (`plan_sections_action.go`) carries the key into its filtered document — so the guidance rides BOTH paths: managed site-wide regeneration AND the per-section scoped block that REPLACES the site-wide block in a fact-scoped section's prompt. Absent key ⇒ byte-identical output to before the key existed (pinned by test). The nothing-phrased early return still wins: guidance alone never causes regeneration to replace a hand-written block.
- **why:** regeneration DELETING the hand-written half is why 13 of 19 writer_block sites were unmanaged on 2026-08-25 — and an unmanaged, hand-typed block is where `bugs_open/387`'s stand-in token ("NNN+") was typed and shipped to the public. The 288 lane's same-day landmine ("a generator that rebuilds a row from its source silently reverts every hand edit") is this class one table over; the fix is the carry IN the regeneration path, not another warning.
- **adoption path (each site's own call, never this lane's):** move the hand block's NEVER-STATE list into `writer_block_guidance`, give each counting fact a `writer_line` with `{value}`, set `writer_block_managed: true` — then the numbers reach the writer's prompt substituted by CODE and migration-611-style interim rewords retire. ai-agent-orchestration.com is the worked candidate (CONTRIB in its lane dir).
- **⚠ CONTRACT: NEGATIVE/PROHIBITIVE guidance ONLY** (compliance seat, council 0de22385 r1) — NEVER-STATE lists and bans, never positive writing instructions or claims about our own accuracy: the field is durable across regeneration BY DESIGN, so anything in it ships for ever. It does not widen the trust surface (an unmanaged hand-written `writer_block` is already verbatim-into-prompt and unvalidated), and wherever the field takes effect its text lands inside the composed `writer_block`, which is on `brief-negation-check`'s runtime-derived writer-visible surface.
- **relation to `banned_claims`, reconciled (approval round advisory):** one purpose, two STAGES — `banned_claims` is DETECTIVE (post-hoc scan patterns the claims gates enforce against produced copy) and `writer_block_guidance` is PREVENTIVE (prompt-time prose the writer reads before producing). Neither substitutes for the other: a prohibition that can be expressed as a scannable pattern belongs in `banned_claims` REGARDLESS of whether guidance also states it (the scan is the enforcement; the guidance only reduces how often it fires), and guidance carries the prohibitions a pattern cannot express (categories, framings, "never present X as Y"). A site opting in should hold both, not choose.
- **durability vs the typed-struct landmine, SURVEYED 2026-08-25:** all **9** `ParseEvidenceBase` callers are readers or validation guards (the admin handler validates through the struct but stores the CLIENT'S raw bytes); the two real write paths — the refresher's raw-map marshal and `write_site_spec`'s `siteSpecDeepMerge` — preserve unknown keys, each pinned by its own round-trip test. Precedent clincher: `writer_block` itself is equally unlisted in `EvidenceBase`, and no site has ever lost one to a struct write — the struct is the SCANNER'S read model. Do NOT add this key to the struct alone; the landmine guards the whole class.
- **sources:** `writer_block_guidance_387_test.go` (6 tests: byte-identity pin, scoped-loss case, guidance-alone-composes-nothing, both storage round trips); `bugs_open/387…md` CORRECTED block; `bugfix_387_deployed_and_404/{PLAN,NOTES}`; the 288 exchange (their landmine cited).
- **relations:** the `writer_block` V2 whitelist entry above · CLM-028 (fact-value history, the sibling mechanism for `bugs_open/386`) · RFC_016 fact-assignment (the scoped path this must not silently weaken) · migration 611 (the interim this retires when aiao opts in).

### CLM-030 — `spec_supplies_claim`: the claim rules applied to the INSTRUCTION, not just the output (bugs_open/414)
- **status:** **BUILT, TESTED AND COMMITTED 2026-08-27 (`fc588e445`), INERT until `brief-negation-check` is rebuilt and its next `40 7 * * *` tick.** Council `f4c144ad` submitted (`Council-Submitted:` trailer; `098` credits the commit on approval). The two pattern changes it depends on are in the same commit.
- **the hole it closes, and it is not the one 414 looks like:** every claims mechanism in the estate reads OUTPUT. Nothing read the INSTRUCTION, so a spec could lawfully order a page to say what the page gate would refuse — and did, for 24 days, on a finance site. `WriteSiteSpecAction` validates SHAPE only; the admin spec PATCH guards `evidence_base` and nothing else; `brief-negation-check`'s first detector asks one question (define-by-negation) about one agent's surface.
- **what:** a second detector inside `cmd/brief-negation-check` (`specclaims.go`). Surface = the union of `{{.site_specs.specs.<path>}}` refs across **every** live agent's prompt, with reader attribution — not the writer's alone, which is exactly what would have missed 414's surviving copy in `strategy`. Per visible string leaf: `SplitPlainAssertionText` → `ScanPracticeClaims(blocks, siteEB)`. Files `spec_supplies_claim` at `needs_human_review`, `handler_agent` **empty**, severity `high`, `item_key` `spec-claims:<site_id>:<sha256[:6] of sorted aspect|field|matched>`, `ON CONFLICT DO NOTHING`, closed only on positive re-observation, one `doc_notes` row per run including clean runs.
- **PRODUCER SET (RFC_010 §1 condition): exactly one — `cmd/brief-negation-check`.** `item_key` shape stated above. A second producer must match it exactly, so the dedup index holds one open row per site per finding-set whichever files first.
- **it scans with the PRACTICE FAMILY ONLY, and that is measured, not preference.** The fleet-wide + regulated set over all **522** current spec rows: **21** hits, effectively every one false `[MEASURED 2026-08-26]` — **15** are the estate's OWN honesty instructions ("Never invent a person, company, scheme, study, quotation, or statistic") matching CLM-015's never-invents pattern, and the negation guard cannot save them because the match *starts* at "never"; and `evidence_base` rows store each site's `banned_claims` **as data**, quoting verbatim the sentences they forbid, so a generic scan convicts every site's own immune system daily. This is also the `data::text` census `brief-negation-check`'s own header records as tried and **withdrawn within hours** on 2026-08-19. The practice family over the same text: **0 of 532** current rows, **2 of 2,782** all-history — exactly the two hops of 414's chain (the plant in `content_direction`, the strategist's paraphrase in `strategy` ten days later) `[MEASURED 2026-08-27]`.
- **`evidence_base` is CENSUSED but never SCANNED** — censused because it carries the `operating_history` attestation that exempts the family, never scanned because its content IS the ban list. Pinned by `TestSpecClaimNeverScansTheEvidenceBase`.
- **no handler agent, deliberately, and this is the whole design:** an automated handler rewriting a spec off a spec-content finding is precisely how the audit fleet canonised the planted marker in the first place (it filed a `content_rewrite` asking for a "How we verify our guides" methodology section). A person decides what a brief should say.
- **⚠ LANDMINE — a clean result here is only as wide as the practice family.** A spec mandating a COMPLETENESS claim ("make the page say everything here is checked") is CLM-015's shape, not this family's, and is missed. The page gate refuses such output at blocker so it cannot be served — but the instruction survives in the spec and refuses every rebuild until a human reads the refusal. See also the `site_specs` propagation entry in `LANDMINES.md`: stripping the aspect you found an instruction in does not retract it.
- **the demand control, because the fix silenced the detector:** the current-spec count went to 0 within an hour of the strategy strip, which on its own proves nothing. The positive case is pinned in HISTORY (2 of 2,782 all-history rows) and in `specclaims_test.go`, whose fixtures are the two real rows verbatim.
- **sources:** `cmd/brief-negation-check/specclaims.go`, `specclaims_test.go`, `briefcheck.go` (`mandateRe` widened with `(include|use|carry) the exact phrase` — the verb the plant actually used, and no existing form matched it), `platform/orchestration/actions/discovery_checks/verifier_coverage_test.go` (classified `catJudgement` + `liveItemTypes`), `bugs_open/414` §7c.
- **relations:** CQ-026/CQ-027 (the negation detector and the binary it shares — its writer-only surface is deliberately UNCHANGED, because widening it would change what every open `brief_supplies_negation` item means) · CLM-026 (the family it scans with, and P6 added to it) · CLM-015 (the family it deliberately does NOT scan with) · CLM-004 · `bugs_open/033` (the review queue this files into) · migration 629 / the `model_opinion` origin door (the downstream half of 414: the canonisation loop's own control)
- **verify-later:** after `brief-negation-check` is rebuilt, one `doc_notes` row per run carrying the second section; the first run must report **0 sites** on current specs and the run must still exit 0 (a zero from a blind scan and a zero from a clean fleet are distinguished by the `scanned_fields` count in the report).

### CLM-031 — a malformed fact costs THAT FACT: `EvidenceBase.MalformedFacts` + the `malformed_evidence_fact` item (bugs_open/456)

- **status:** **BUILT, TESTED, COUNCIL-APPROVED round 1 (`c2d1d570`, 3 advisories, all actioned) AND COMMITTED 2026-09-03** (`3f221f99f` the parse, `e5b41dc31` the reporting half, `fef2ced9c` the advisories). **INERT until the next chassis roll** — `ParseEvidenceBase` runs inside the agent-chassis binary, so post-roll verification is a provenance probe plus the register census, per `bugs_open/456` §8. **Do not read the pre-roll `regcheck` runs in that file as production evidence; they are an offline repro against live data, and the file says so.**
- **what:** `ParseEvidenceBase` decodes `facts` **one at a time** (`facts` shadowed as `[]json.RawMessage` on an embedding wire struct) instead of as one array. A fact that fails to decode is skipped and recorded on the new `EvidenceBase.MalformedFacts []MalformedFact` (`json:"-"` — produced by the parse, never round-tripped into a register). `banned_claims`, both attestations and the citation codes decode regardless. The daily sweep runs the typed parse for its report and raises one `malformed_evidence_fact` work item **per fact**.
- **why, and the number is the point:** decoding the array whole meant **one** undecodable fact returned an error for the **whole base** — and all three claims gates read that error as "this site has no register" (`validate_page_content.go` "claims checks skipped"; its stat audit; `check_unverified_claims.go`, which names the case *"a real defect on an opted-in site"* and answers it with a log line). `[MEASURED 2026-09-03, all 27 live registers, via the real parser]` **2 registers were dead this way and 10 `banned_claims` inert**: `finetuning.uk` (3 bans, since 08-24) and `noted.co.uk` (7 bans, since 08-25 — a list forbidding exactly the unearnable security absolutes a notes product must never claim). Neither had raised anything; the only trace was a Warn in a pod replaced daily.
- **the cause is a MISSING CAPABILITY, not carelessness, and it was still growing:** `EvidenceFact.Value` is a `*float64`, and authors were legitimately registering text-valued facts (`"MIT"`, `"Apache 2.0"`, `"Llama 3.3 Community License"`, `"30 days"`). finetuning.uk's count went **0 → 3 → 7 → 8** across 08-24…08-26 as one author kept doing the reasonable thing.
- **PRODUCER SET (RFC_010 §1 condition): exactly one — `refreshOneSiteEvidence` in `refresh_evidence_base_action.go`.** `item_key` shape: `malformed_evidence_fact:<site_id>:<fnv64a of "<fact_id>|<index>">` — **per FACT, not per site**, mirroring `bannedClaimPatternItemKey`'s correction under `bugs_open/091`: a site-scoped key lets a second bad fact hide behind the first, which on this defect's own growth curve is the likely path. `dropOnConflict`, `severity: high`, `priority: 20`, `handler_agent: human-review`, `status: needs_human_review`. A second producer must match that key exactly.
- **it is NOT `required_fields_missing`** (searched, on the council's reuse objection): that type has an **automated handler** (`required-fields-missing-handler`, `platform/livespec/unarmed_completers.go`) that auto-closes on convert/resolve/stale, so routing a broken register through it would hand the finding to a completer that cannot fix it. `cta_tel_malformed` is CTA-phone-specific. **No tolerant/partial array-decode helper exists anywhere in `platform/`, `internal/` or `pkg/`** — also searched, also on that objection.
- **⚠ RETIREMENT CONDITION, stated by the architecture seat and adopted:** this item type converts a silent total failure into a **recurring manual queue for a capability the schema still cannot express**. `[MEASURED 2026-09-03]` expected volume is 2. **When it is next measured, if the volume is still growing, that is the RFC trigger for a typed union (string | number) on `EvidenceFact.Value` — not another sweep tweak.** Do not let this type become permanent triage.
- **still open, and explicitly NOT cleared:** whether other `site_specs` aspects (`content_direction`, `design_intent`, `imagery_style_guide`) decode array fields through the same all-or-nothing pattern. A first look found no equivalent typed parser, but that is an absence nobody has proved, and the *mechanism* — a Go typed decode of a jsonb array voided by one bad element — is generic. Raised by the council's `bug_historian` seat.
- **sources:** `platform/orchestration/datahelpers/claims.go` (`ParseEvidenceBase`, `MalformedFact`, `rawFactID`), `claims_malformed_fact_test.go` (7 tests; 3 run RED under the restored all-or-nothing decode), `refresh_evidence_base_action.go` (`createMalformedEvidenceFactItems`, `malformedFactItemKey`, the typed-parse probe, the widened early return), `refresh_evidence_unverifiable_test.go`, `bugs_open/456`.
- **relations:** `bugs_closed/161` (this is its mechanism INVERTED — not a register that vouches for a false claim, but one that protects nothing) · CLM-007 (the sweep this rides) · CLM-032 below (the residue-arm half of the same commit) · `LANDMINES.md` "A register that fails to parse is INDISTINGUISHABLE from a site that never opted in" · RFC_025 §12.4

### CLM-032 — `FactsUnverifiable`: the sweep now COUNTS the facts nothing re-proves (RFC_025 §12.3)

- **status:** built + committed 2026-09-03 (`e5b41dc31`), same council round as CLM-031; inert until the roll.
- **what:** `refreshOneSiteEvidence`'s residue arm nudges **every** fact no machine re-proves — whatever source key it used — instead of only `attested_by` ones, and counts the whole population in `siteRefreshResult.FactsUnverifiable` whether or not the nudge is due. The `stale_attestation` item type, key and 180-day threshold are unchanged; only the producer's population and the entry's `Detail` widen.
- **why:** a fact with no `sql`/`citation`/`artifact_check`/`attested_by` previously fell through `continue` **uncounted** — not in `FactsChecked`, not in `res.Facts`, in no counter anywhere. `[MEASURED 2026-09-03]` **27 such facts on 5 sites**: the register asserted them, the writer was instructed by them, every gate vouched for them, and the one daily mechanism that could have said *"nothing has ever checked this"* could not even count them. That is `bugs_closed/161`'s "trusted once registered" on the population RFC_025 deliberately left opt-in.
- **it answers a question `AttestationsDue` cannot:** due-today vs the size of the never-checked population. A run reporting 12 unverifiable and 0 due is the honest signal that the arm ran and nothing was old enough; before this, that run and a run where the arm never looked were **indistinguishable**. RFC_025 §7 owed exactly this standing measurement and it had only ever been answered by ad-hoc SQL.
- **the `Detail` names the remedy per shape** (`unverifiableFactDetail`): `attested_by` keeps stage 1's wording verbatim; a bare `artifact` fact is told to attach an `artifact_check`; and **the 12 whose `artifact` is an external URL (all relojistas) are pointed at `citation`**, which already re-fetches the URL and re-checks a verbatim quote every sweep — the mechanism they want exists and they are simply on the wrong source type.
- **⚠ it raises nothing new today.** All 27 facts are 14–56 days old against a 180-day threshold, so the immediate effect is the counter, not the queue. **A zero in the `stale_attestation` queue is therefore NOT evidence this is inert** — read `facts_unverifiable` in the sweep result instead.
- **sources:** `refresh_evidence_base_action.go` (residue arm, `unverifiableFactDetail`, `FactsUnverifiable`), `refresh_evidence_unverifiable_test.go`, RFC_025 §12.3, `bugs_open/456`.
- **relations:** CLM-031 (same commit, same round) · CLM-007 · RFC_025 stages 1 and 2 · `bugs_open/288` §6.6 (names this population as an adoption gap; that the sweep could not COUNT it went unnoticed)
