# Register — operator-practice

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074)._ (2 consolidated from 4 raw extractions across unit U25 — each of the 2 distinct blocks
appeared byte-identically twice within the cluster input file, treated as duplicate copies of
one extraction, not independent corroboration; + OPP-003, OPP-004 and OPP-005 added later, see
their entries for dates).

### OPP-001 — In-chassis replicability requirement for operator work
- **status:** deployed
- **status-evidence:** REPLICATION_in_chassis.md (2026-07-10) maps every off-platform action to [chassis]/[human]/[gap]; RUNNING_NOTES turn 8: "everything done in this thread must be replicable inside the chassis … Either document the tool-free path or don't use the tool."
- **what:** Standing owner rule: interactive-agent work on sites must be reproducible by the chassis itself (or documented as a human-judgement or platform-gap item). The audit, spec rewrites, artifact verification and imagery deploys all map to normal platform operations; the genuinely human items (choosing a permanent logo, stating personal-history claims, setting engagement terms) are deliberately not automated; named gaps: pinned enforcement, favicon/OG derivation, chart capability.
- **sources:** docs/leopardessconsulting/REPLICATION_in_chassis.md (whole); docs/leopardessconsulting/RUNNING_NOTES.md#Turn-8
- **relations:** hitl; documentation-system; site_specs pinned gap
- **verify-later:** n/a (practice doc); checkpoint_for_review as the in-chassis review surface

### OPP-002 — Operator discipline: verify-by-artifact, dated backups, kcat generic trigger
- **status:** deployed
- **status-evidence:** PLAN standing rule 2: "Verify by artifact, never by report. (This platform has a long history of builds reporting success while building nothing.)"; RUNBOOK landmines 2/17/18; VERDICT §7 "Verified by artifact, never by item status" with md5 predictions.
- **what:** The cross-workstream operating discipline: (1) never trust a complete work item — curl the page, read the DB row, diff the bytes (strongest form: predict output bytes offline and md5-compare); (2) back up before ANY change using dated bak_*/_backup_YYYYMMDD tables and never reuse a name (CREATE TABLE IF NOT EXISTS silently no-ops); (3) trigger any agent by producing to Kafka system.agent.generic.requests via kcat with the standard header set; (4) kubectl exec heredocs need -i or silently run nothing — prefer kubectl cp + psql -f; quote heredoc delimiters.
- **sources:** docs/leopardessconsulting/RUNBOOK.md#Landmines; docs/social001_vonc_tiktok_social/minilobby_task/VERDICT_minilobby_trim_method.md#7; docs/social001_vonc_tiktok_social/minilobby_task/HANDOFF_2026-07-09_vonc_spark_minilobby_trim(2).md#8; docs/leopardessconsulting/HANDOFF.md#5
- **relations:** silent no-op success class; fleet generalisation doctrine; standing evidence rules (operating-doctrine register, OPD-001)
- **verify-later:** n/a (doctrine); bak_* table inventory in clients_db

### OPP-003 — `check_logged_model_output`: a pre-commit detector for publishing model text
- **status:** deployed (advisory; 8th check in `scripts/pattern-check.py`, run by `.githooks/pre-commit`)
- **what:** Flags a log sink (`log.*`, `logger.*`, `fmt.Print*`) that passes an
  unwrapped payload identifier (`text`, `body`, `completion`, `response`, `raw`,
  …) inside any package that calls `GenerateText`, and points at
  `aiservice.Fingerprint` as the fix. Silent when the value is wrapped in a
  derived-fact helper. Exists because whether to log model output is a *content*
  decision, not a code-review one — it took a full council round to surface once,
  and the reviewer stated it could not be closed by the council alone.
- **sources:** `scripts/pattern-check.py` (`check_logged_model_output`)
- **relations:** LCO-005; `bugs_open/083`
- **note — the check nearly shipped VACUOUS, and this is the transferable part:**
  the first version gated on **the file** containing `GenerateText`. The LLM call
  lives in `defend.go` and the log sink in its sibling `ailog.go`, so it examined
  **zero files and printed a clean result**. A clean result and an unrun check are
  byte-identical output. Caught only by a **positive control** — auditing the
  commit that introduced the defect and *requiring* a finding. Now gated on the
  package and verified three ways: fires on `a37a2037c`, silent on the fix, zero
  findings fleet-wide. **Any new detector needs its failing case demonstrated at
  the moment it is written.**

### OPP-004 — `check_register_coverage`: the register coverage cadence, on the commit path
- **status:** deployed (advisory; 9th check in `scripts/pattern-check.py`, run by `.githooks/pre-commit`)
- **what:** Fires when a commit **creates a workstream directory the concept register has never heard of**, naming both ways to go quiet — add a register entry, or add the directory to `102_coverage_ratchet.txt`. It closes `bugs_open/106`'s last open scope: the coverage SENSOR (`102_CHECK_register_coverage.py`, sensor + ratchet) already existed and worked, but ran only when a human remembered, which is the same detected-by-coincidence mechanism the bug was filed about, one step earlier. **A commit trigger was chosen over a cron deliberately**: a cron reports drift up to a week late to nobody in particular, whereas this reports it the instant it is created to the one person who can close it in ten seconds. Only NEW directories fire — the 43 already-uncovered workstreams are accepted backlog on the ratchet, and flagging active work on them every commit is how a check becomes wallpaper.
- **why it is registerable:** it is the enforcement half of the register's own coverage guarantee. Any workstream creating a new subsystem directory now learns, at creation, that the estate's capability index does not know about it.
- **measured before inclusion** (this file's own bar): **4 fires / 1,500 commits = 0.27%**, quieter than every existing check (README 0.7%, SUMMARY 2.0%, twin ~2%) — correct for a population of "commits creating a brand-new workstream". **Precision inspected because a very low rate and a dead check look identical:** all 4 genuine (`memory_index`, `bugs_sweep_2026_07`, `bugfix_066_spawn_image_tag`, `gemini_content_provider`), and the last two are exactly the pair 106's own triage records the sensor finding by hand on 2026-07-27 — the same gaps, now caught at creation.
- **verified by induced gap**, as `106` demands: two scratch workstreams staged → both fire; one added to the ratchet → only the other fires; the other given a register entry → it goes quiet too. Negative control: silent and 40 ms on a commit touching no workstream directory.
- **sources:** `scripts/pattern-check.py` (`check_register_coverage`); `docs/agent_docs/docs026_concept_register/102_CHECK_register_coverage.py`; `docs024/bugfix_106_register_coverage_cadence/` (PLAN, RUNBOOK, NOTES)
- **relations:** OPP-003 (sibling commit-path detector); `102_coverage_ratchet.txt`; the `verifier_coverage_test.go` sensor+ratchet shape it is modelled on; `bugs_closed/106`
- **landmine:** it **imports** the sensor rather than reimplementing `is_covered()` — one matching rule, one implementation, because two hand-maintained copies is the `idx_swi_dedup` ↔ `workItemTerminalStatuses` drift class. Change how coverage is decided in the sensor and the hook follows. The import is guarded: if the sensor moves, the check returns silently rather than breaking commits.
- **NOT in scope, deliberately:** it asks only whether a subsystem is *represented*, never whether an entry is *accurate*. **The register can be complete in coverage and stale in content, and nothing detects that** — two live instances hit on 2026-07-28 (a `verify-later` stating an expected answer that had been false for weeks, contributing to `bugs_closed/124` going unnoticed). Recorded, not fixed: a coverage check that starts auditing accuracy becomes slow, noisy and ignored.
- **verify-later:** does `check_register_coverage` still appear in `scripts/pattern-check.py`'s check tuple in `main()`, and does `./scripts/pattern-check.py` stay silent on a commit touching no workstream directory?

### OPP-006 — `check_register_entry_without_row`: the register's two halves ship together, or the commit says so
- **status:** deployed 2026-08-10 (advisory; 17th check in `scripts/pattern-check.py`, run by `.githooks/pre-commit`)
- **what:** Two arms on one trigger — a commit that ADDS a `### ABC-001` concept entry to a register category file. **Arm 1** fires when that id has no `| ABC-001 |` row in `000_concept_index.md` as the commit will contain it. The index table is what a session searches, so an entry with no row reads as *does not exist* and the next lane builds a second one — `bugs_open/106`'s failure mode, arriving one concept at a time. **Arm 2** fires when the id is already taken elsewhere in the register.
- **why a gate when DOC-074 already reports this daily:** because reporting it is measurably not enough, and this lane has the receipts. The watcher named a headline mismatch on **three consecutive mornings and nobody corrected it**. A report can only ask someone to repair the register afterwards; this fires in front of the one person who can close it in ten seconds, while they are still writing the entry. Same argument as OPP-004 one level up: put the check where the error is made.
- **measured before inclusion** (this file's bar), over the 14 days to 2026-08-10: **398 commits touched the register; 159 of them added at least one concept entry. 133 (84%) did it correctly and are silent. 26 commits (16%) shipped an entry with no row — 34 findings, 0 false positives on inspection.** Against the whole commit stream that is 26 / 3,324 = **0.8%**, quieter than the twin (~2%) and SUMMARY (2.0%) checks. Arm 2: **1 finding in 398 commits**, the known `LNK-031` collision.
- **the measurement that decides whether it is worth having, and it is not the fire rate.** A fire count cannot tell a real leak from a two-commit workflow the gate would merely nag. So: of the 34, **32 eventually got a row and the median entry waited 93 hours for it**; 23 of 32 took ≥24h; only 3 closed inside an hour. **21 were closed in one sweep by another session's 08-04 backfill commit** (`8f998e86b`), not by their authors. That is the shape of a leak somebody else pays for, not of a workflow.
- **it also corrects the rate this lane had been quoting.** The handoff estimated ~1 per 1.5 days from the watcher's own reports; the commit-level sweep says **~1.2/day** since the 08-04 backfill. The watcher undercounts *by construction* — it can only name entries still missing at 06:50, so a row backfilled the same afternoon (VIZ-015 at 0.0h, WII-010 at 0.8h) never appears in any daily row. **A report's count is bounded by its sampling interval; a commit sweep is not.**
- **⚠ LANDMINE — arm 2 was INERT in production while passing every test, and the shape is general.** `git grep <pattern> --cached` is not a synonym for `git grep --cached <pattern>`: git reads the trailing word as a REVISION and dies "unable to resolve revision: --cached". `sh()` captures stdout only, so that fatal arrives as an empty string — empty corpus, no collisions, no findings, **no error**. It passed every `--commit` audit, because there the sha legally follows the pattern; only staged mode was dead, and staged mode is the only mode the hook runs. Caught by a positive control that staged a known-duplicate id and REQUIRED a finding. The arm now asserts that the corpus it read contains the very headings the commit adds — true by construction — and prints an audible skip when it does not, so a broken read can never again pass as "no collisions".
- **⚠ LANDMINE — it reads `git show :<path>`, never the worktree, and that is not a detail.** The house rule here is `git commit <pathspec>`, which takes the named paths and ignores the index. An author who edited both files but named only the category file ships the entry alone, and a worktree read would call that clean — the same half-a-move failure LANDMINES records for `git mv`. There is a control for exactly this: row present in the worktree, not staged, must still fire.
- **it imports the watcher's parser** (`ENTRY_RE`, `ROW_RE`, `parse_rows` from `concept-register-drift-check/base/check.py`) rather than re-deriving them, for OPP-004's reason and one more specific to this pair: if the gate's idea of an entry heading ever diverged from the watcher's, a commit could pass the gate and then be reported by the CronJob for ever, with nothing to say which of the two was wrong. Guarded — if the watcher moves, the check returns silently rather than breaking commits.
- **NOT in scope, deliberately:** (1) it never writes the row. A row needs a one-line summary written by someone who understands the concept, which is the same reason DOC-074 reports and does not repair. (2) It does not check the reverse (a row with no entry) — the drift is one-directional by construction, since adding a concept is two edits and only the first is load-bearing for the author, and the index header's `comm` pair has never once found a row without an entry. (3) It says nothing about whether an entry is TRUE; that flank is still open and is the register lane's next question.
- **cost:** 0.12–0.16s when the register is touched, 0.05s otherwise (early return before any git call). Advisory — like everything in `pattern-check.py`, it never blocks; `PATTERN_CHECK_STRICT=1` opts a session into non-zero exits.
- **sources:** `scripts/pattern-check.py` (`check_register_entry_without_row`, `committed_content`); `deployments/kustomize/services/concept-register-drift-check/base/check.py` (the imported parser); `docs026_concept_register/RUNNING_NOTES_concept_register.md` (2026-08-10, the sweep and the five staged controls); `HANDOFF_2026-08-10_continue_here.md` item 1
- **relations:** DOC-074 `concept-register-drift-check` (the daily half — this is the same predicate moved to authoring time, and the two share one parser) · OPP-004 `check_register_coverage` (sibling commit-path detector, one level up: *is the subsystem represented at all*) · OPP-003 · `bugs_open/106` (the register-as-absence-evidence failure this drips into) · the `LNK-031`/`LNK-032` collision of 2026-08-08 (arm 2's motivating case)
- **verify-later:** does `check_register_entry_without_row` still appear in `main()`'s check tuple; does a staged entry with no row still produce a finding (the control that must never go quiet); and — the real question — **does the daily watcher's missing-row finding count fall to zero now that the class is caught at authoring time?** If it does not, the gate is being ignored rather than working, and that is a different problem with a different fix.
- > **ANSWERED 2026-08-11 — it was neither working nor ignored: it was INAUDIBLE, and the fix is OPP-007.** The gate's first live miss (`5c7b115c5`, three hours after shipping) was read back from that session's own transcript: the command ended `2>&1 | tail -8` and the recorded stdout is **exactly 8 lines**. The finding was computed correctly and thrown away by the session's own pipe. This is not one session's slip — **45% of multi-file commits since 2026-07-18 (1,199 of 2,669) never delivered the advisory block, 95% of those cut by a `| tail`, across 258 distinct sessions.** So the "is it being ignored?" branch of the verify-later above was never reachable on the evidence available in the register or the watcher; it needed the harness transcripts. **Nothing about this gate's predicate, fire rate or advisory posture changes** — the argument against giving it teeth stands, and the case for teeth was never made out. `FINDINGS_2026-08-11_advisory_delivery.md`.

### OPP-005 — Session scratchpads: versioned by a snapshot hook, reaped by content class
- **status:** deployed (2026-08-03; `CLAUDE_CODE_TMPDIR` moved to disk in `~/.claude/settings.json`, hook wired in `.claude/settings.json`, both tools in `scripts/`)
- **what:** Three coupled pieces that make the shared session-scratchpad tree survivable. (1) **`CLAUDE_CODE_TMPDIR=/home/ant/.claude-scratch`** — scratchpads move off the 16 GB `/tmp` tmpfs onto the main disk (212 GB free), ending the class where a full tmpfs makes a Bash call return **no output** (ENOSPC on the harness's own stdout capture, which reads like "the command found nothing", not like a disk error). (2) **`scripts/scratch-git-snapshot.py`** — a `PostToolUse` hook on `Write|Edit` that commits each written scratchpad file into a git repo at the scratch root, one file by pathspec, so a scratchpad stops being unrecoverable. (3) **`scripts/scratch-report.py`** — shows every session dir by age, size and content class, and reaps only what is provably reproducible.
- **why it is registerable:** any workstream on this machine writes to a scratchpad, and until now nothing versioned or bounded that tree. It is also the general answer to "how do we clean up shared temp safely", which every lane eventually asks.
- **the measurement the whole design rests on** — the two content classes have opposite risk profiles and the space is almost entirely in the safe one: **33 repo-extraction dirs = 12.13 GB = 99.3%**, versus **370 other entries = 88 MB = 0.7%**. A repo extraction is a `git archive HEAD` unpack (the shared-tree build check CLAUDE.md mandates); it is byte-for-byte reproducible from a sha, so deleting one loses nothing. The 0.7% is irreplaceable and small enough never to need deleting.
- **the separation is structural, not heuristic.** Extractions are created by `tar`/`git archive` in **Bash**; the hook fires on **Write|Edit**. So it is *incapable* of committing an extraction — no size rule is doing the work, and there is no threshold to misfire. The 5 MB cap in the hook is a backstop against a large generated artefact, not the class filter.
- **`.gitignore` is `*` and every add is `-f`.** The repo therefore contains only what was deliberately snapshotted, and `git status` next to 12 GB of untracked bulk returns **empty in 3 ms** instead of listing it. Showing untracked bulk is the report tool's job, not git's.
- **verified in both directions before wiring** (a guard that refuses everything passes a refusal test): positive — a real `Write` in a live session produced a commit, proving the hook fires without a restart; negative — a repo path outside any scratchpad and a 6 MB file were each refused, commit count unchanged; idempotence — re-firing on unchanged content adds no second commit.
- **`--reap` deletes only marker-verified extraction directories**, never a loose file, never a session dir, never anything it cannot positively identify (≥2 of `go.mod`/`platform`/`CLAUDE.md` at the directory top). It re-verifies at the moment of deletion, not just at scan time, and defaults to a dry run at a 2-day threshold.
- **sources:** `scripts/scratch-git-snapshot.py`; `scripts/scratch-report.py`; `~/.claude/settings.json` (`env.CLAUDE_CODE_TMPDIR`); `.claude/settings.json` (`PostToolUse`); `gauntlet_dead_cta/NOTES` 2026-08-03
- **relations:** `scripts/memory-git-snapshot.py` (same argument, same shape — an unversioned shared store is the hazard, and versioning it dissolves the class rather than policing writes); the `bugfix_177` LANDMINE on `/tmp` filling mid-build, which is the same outage seen from inside a build
- **landmine:** **two scratch roots are live at once after the move.** A running session keeps the tmpdir it launched with, so sessions started before 2026-08-03 keep writing to `/tmp` while new ones use the disk. `df -h /tmp` can therefore look healthy while the tree you are actually in is full, or the reverse. ~~Both tools read **both** roots for this reason; a check that inspects only one will be confidently wrong for as long as the old sessions live.~~
  > **⚠ CORRECTED 2026-08-24 — that last sentence was FALSE of `scratch-report.py` for the whole three weeks it stood, and the correction is now shipped.** `ROOTS` did list `/tmp`, but **every candidate came from `scratch_dirs()`, which requires a `<root>/claude-*/<proj>/<uuid>` layout — and `/tmp` has never had a `claude-*` directory in it.** The `/tmp` arm was structurally inert while reading as covered, which is the worst of both. Control: `ls -d /tmp/claude-*` → nothing; `ls -d ~/.claude-scratch/claude-*` → two. **The tell was an ABSENT SECTION HEADER, not a wrong number** — the report prints `=== <root> ===` per root and printed one; an early `continue` on "no session rows" skipped `/tmp` silently. Nothing errored, no figure was wrong, no zero appeared. *A missing row is the hardest refutation to notice, because there is nothing to read* — so when you change `ROOTS`, assert the header prints, not just that the totals look plausible.
  > **FIXED** by `loose_reapables(root, now, days)` (2026-08-24, `tmpfs_exhaustion` lane): scans a root's top level **and exactly one level below it** — holding directories such as `gotmp/` and `adhoc/` are not themselves scratch, but the linker dirs and extracts sit directly inside them, and descending further would start walking real work. Two shapes only, both regenerable by construction: a marker-verified extraction, and `go-build[0-9]+` (invisible to the marker test — it contains no repo files — and 3.1 GB of the 15.3 GB that filled `/tmp` on 2026-08-23). `/tmp` now reports for the first time: **7 dirs, 2.7 GB**.
- **⚠ THE TOOL WAS CORRECT AND HAD NEVER BEEN RUN — that, not any defect, is what this entry is now mainly about.** `[MEASURED 2026-08-24]` `scratch-report.py --days 2` reported **250 marker-verified extraction dirs = 97.1 GB** reapable at its own default gate, on a machine with 120 GB free and a root filesystem ~12 days from full at the then-current rate. The 130 GB did not accumulate for want of a tool; it accumulated because nothing schedules the tool. **A silent mechanism is usually UNDRIVEN, not missing** — and the `tmpfs_exhaustion` lane proved it the expensive way by building a duplicate (`scratch-janitor.sh`, deleted the same day, `0097d25de`) before reading this register. **The remaining work is a crontab line, not code.**
- **`--self-test` (added 2026-08-24)**: plants each hazard and asserts the guard refuses it. Six cases, and the pairing is the load-bearing part — a real extraction must be **found** first (control), or every refusal below it could just mean nothing was ever a candidate; the age-gate case then holds back **that same directory**, so "gated" cannot be confused with "never identified". Also: `.git` refused, one marker insufficient, unidentifiable work untouched, and the protected-name list matched against 6 system names that must hit and 5 scratch names that must not.
- **NOT in scope, deliberately:** it does not reap hand-written files at all, at any age. They are 0.7% of the volume, so reaping them buys nothing measurable and risks the only irreplaceable thing in the tree.
- **verify-later:** does `git -C /home/ant/.claude-scratch/claude-1000 log` show commits from more than one session id (proving the hook is live fleet-wide, not just in the session that wired it), and does `scripts/scratch-report.py` still report both roots?

### OPP-007 — `commit-advisory-postuse.py`: the pre-commit advisory, re-emitted where a `| tail` cannot cut it
- **status:** deployed 2026-08-11 (advisory; `PostToolUse` hook on `Bash` in `.claude/settings.json`, wired from `scripts/commit-advisory-postuse.py`). Commit `05d8b379e` (the index row itself is in `3e9d2035d`, which swept it as a same-file passenger while this entry was being written — the hazard CLAUDE.md describes, arriving unprompted during a commit about commit hygiene).
- **status-evidence (PROVEN AT THE READER, not at the hook):** commit `aa78871d8` was piped through `| tail -3`, its recorded stdout is 2 lines and carries neither block, **and the advisory arrived anyway** as a `PostToolUse` system-reminder beside the tool result. The de-duplication arm held in the same pair: `05d8b379e`, committed without a pipe, printed the block in its own output and drew no duplicate. The hook also took effect **mid-session, with no restart**, so a session that edits `.claude/settings.json` can control it immediately.
- **status-evidence, FLEET-WIDE (2026-08-12, the first full day):** of **36** multi-file commits made through the tool, **36 (100%) reached their session** — 13 in the command's own output, **23 out of band by this hook**, none missed by both. Measured with `scripts/advisory-delivery-sweep.py --since 2026-08-12`, whose controls both hold: `oob = 0` on every day 08-05→08-10 (the hook did not exist), pre-fix rates unchanged at 55–56%, and channel 1's `tail -N` separation intact. ⚠ **the sweep was BLIND to this hook until 2026-08-12** and printed 38% that morning; see the landmine below before quoting any figure from it.
- **⚠ LANDMINE — the instrument that verifies this hook could not see it, for a day.** `advisory-delivery-sweep.py` decided "delivered" by testing `"commit scope:" in toolUseResult.stdout`, and **`additionalContext` is not in `toolUseResult` at all**: the harness records it as a separate `type: "attachment"` / `attachment.type: "hook_success"` record with the text in `attachment.stdout`. So every out-of-band delivery scored as a miss and the verify-later read **38.2% — below its own 55% baseline — on a day that was 100%**. A number like that invites the opposite of the truth: that the fix failed, or that OPP-006 needs teeth. Three parts to the check: read **both** channels; join on a **9-char prefix** (the note quotes git's abbreviated sha, the population carries 40, and a set-membership test silently matches nothing); compare timestamps in **UTC** (transcripts are `…Z`, the estate writes BST, and this hook was live in the tree ~7 min before its own commit). Fixed 2026-08-12; `WRONG_CALLS.md` + `LANDMINES.md`.
- **what:** After any Bash call whose command contains `git commit` and whose output carries git's `[branch sha]` summary line, it re-runs `commit-scope-report.sh --commit <sha>` and `pattern-check.py --commit <sha>` and delivers the result through `hookSpecificOutput.additionalContext`. Out of band: it is not part of the command's stdout, so no pipe in the session's own command can remove it. Blocks the session already saw are dropped rather than repeated.
- **why it exists, and it is a delivery defect not an adoption one:** `.githooks/pre-commit` prints the commit-scope report and pattern-check's findings **first**; git prints its `[branch sha]` summary **last**. Sessions habitually run `git commit … 2>&1 | tail -5`, which is a head-cutter. **Measured 2026-08-11 across every commit made through the tool since the scope report shipped (2026-07-18) — 2,669 multi-file commits in this repo, read from the session transcripts: 1,470 (55%) delivered the block, 1,199 (45%) did not, and 1,137 of those misses (95%) were cut by the session's own `| tail`.** 258 distinct sessions did it, so it is a fleet-wide habit rather than one lane's tic. Figures move with the corpus — re-run `scripts/advisory-delivery-sweep.py`, do not quote these.
- **the control that makes it a finding and not a correlation:** among the misses `tail -N` is ≤8 in 1,031 of 1,137 cases (median 5); among commits that delivered the block **despite** a tail pipe, N is >8 in 465 of 595. Delivery tracks the pipe width, which is exactly what truncation predicts and is a result that could have come out otherwise. The sweep prints this control on every run. Second, independent control: for 666 of the misses the recorded stdout is **exactly N lines long** — output existed and was cut, arithmetic no content can fake.
- **what it settles for OPP-006, which asked this question in its own verify-later.** OPP-006 fired on `5c7b115c5` three hours after shipping and the two entries landed with no index row anyway; `HANDOFF_2026-08-10b` recorded that the honest first question was whether the session never saw the output or saw it and judged the row could wait — *"those have opposite fixes (delivery vs. enforcement), and nothing recorded so far distinguishes them."* Something did: the session transcript. That command ended `2>&1 | tail -8` and its recorded stdout is exactly 8 lines. **The gate was never ignored; it was inaudible.** No case for teeth was made out, and the argument in `pattern-check.py`'s docstring against blocking on a shared tree stands untouched.
- **why a harness hook rather than a git hook:** no git hook can win this. `post-commit` runs *before* git prints its summary (verified in a scratch repo), so its output is cut by the same `tail`. The only escape is to leave the command's stdout altogether, which is what `PostToolUse` does.
- **delivery is the whole point, so the delivery path is the load-bearing detail:** `hookSpecificOutput.additionalContext` on **stdout**, exit 0. A `PostToolUse` hook that writes to **stderr** and exits 0 reaches neither the model nor the transcript — `scripts/memory-index.py` was wired that way and was mute for six days while sessions hand-rolled the number it was computing. It is recorded in the `.jsonl` as an `attachment.stderr` record, so a grep for the banner text finds it and it looks exactly like delivery.
- **cost:** a substring test on the command and nothing else for ordinary Bash calls; ~0.1–0.5s when a commit is detected (measured: `pattern-check.py --commit` is 0.08s on a docs commit, 0.46s on a 5-file platform commit).
- **⚠ LANDMINE — it must never report against the wrong tree.** Sessions commit into other repos through the same Bash tool (the auto-memory git dir, scratch repos): 71 of 5,195 shas in the transcript corpus resolve nowhere in agentchassis. The sha is therefore checked with `git cat-file -e <sha>^{commit}` before anything is read, and an unresolvable sha exits silently. A scope report attributed to the wrong repo would be worse than no report.
- **NOT in scope, deliberately:** (1) it cannot block, and nothing here should — `PostToolUse` runs after the tool, so the commit already exists; this buys visibility one moment later, not enforcement. (2) It does not change what the checks look for; the predicates are untouched and both scripts keep their existing staged mode. (3) It does not help a commit made outside the Bash tool.
- **sources:** `scripts/commit-advisory-postuse.py`; `scripts/commit-scope-report.sh` (the new `--commit <sha>` mode); `.claude/settings.json` (`PostToolUse` matcher `Bash`); `docs026_concept_register/FINDINGS_2026-08-11_advisory_delivery.md` (the measurement, its two controls and the refuted first hypothesis)
- **relations:** OPP-006 `check_register_entry_without_row` (whose first live miss this explains, and whose verify-later this answers) · OPP-003 / OPP-004 (the other `pattern-check.py` detectors, which were being suppressed the same way and are equally repaired by this) · OPP-005 (the other `PostToolUse` hook in `.claude/settings.json`) · DOC-074 `concept-register-drift-check` (the daily half; unaffected — it never depended on a session reading anything) · `scripts/memory-index.py` (the stderr-and-exit-0 trap this deliberately avoids)
- **verify-later — HALF ANSWERED 2026-08-12, and the halves must not be conflated.** (1) **Delivery: PROVEN.** The rate rose from 55% to **100%** (36/36) on the first full day, measured at the reader, both controls holding. (2) **Behaviour: STILL OPEN, and today's evidence cannot touch it.** The register's leak signal *looks* clean — 0 OPP-006 findings across all 17 register-touching commits since the hook went live, and no entry-without-row at HEAD — but **only 4 entry-adding commits** landed in that window, and at OPP-006's measured 16% historical leak rate **P(zero leaks | nothing improved) = 0.50**. The zero is the expected outcome either way. **What would discriminate: ~14 entry-adding commits for 90% power, ~18 for 95%** — days at this cadence. Until then the enforcement question is neither closed nor reopened, and a clean daily row is not an argument. ⚠ Use the per-commit sweep, not a HEAD snapshot: this lane's own landmine is that a count at HEAD cannot see a leak repaired the same afternoon.

### OPP-008 — `verify-head-builds.sh`: the "does committed HEAD still build" check, as one script instead of nine pasted commands

- **status:** built and exercised 2026-08-23 (both modes run, both outcomes proven); replaces the pasted form in the shared debugging guide and seven lane documents.
- **what:** `scripts/verify-head-builds.sh [--with <path>]... [--test] [targets...]`. Extracts committed `HEAD` into a fresh tree **on disk**, builds it, and deletes the tree on exit. Two modes, because the pasted copies were doing two different jobs: with no flags it answers *"does committed HEAD build"* (run it **after** committing); `--with <file>` overlays your uncommitted files onto that clean checkout, which answers *"does MY change work against HEAD rather than against a working tree carrying every other session's WIP"* (run it **before**). `--test` runs `go test … -count=1` instead of a build. Exit 0 passed · 1 did not · 2 could not run the check.
- **why it exists — the drift is measured, not asserted.** The same command lived in **nine documents** as hand-typed prose. By 2026-08-23 the nine copies had drifted into **six different directory names** (`/tmp/h`, `/tmp/x`, `/tmp/chk`, `/tmp/headcheck`, `/tmp/headtree`, `/tmp/cleantree`), two different build targets, and — in `016b`, the guide every session is told to read — **a version that did not work**: it extracted into a directory it never created, so it failed first-use for anyone who followed it exactly and succeeded only for someone who had run a different variant earlier and left the directory behind. Same answer as `scripts/council-scope.sh`: one implementation, N callers.
- **⚠ LANDMINE — it writes to DISK, and that is the point, not a detail.** `/tmp` on this box is a **16 GB tmpfs — RAM**. Each checkout is ~450 MB and **none of the pasted copies cleaned up**: on 2026-08-23 `/tmp` was at 100% with 28 abandoned checkouts holding **12 GB**, which had also consumed the machine's entire 8 GB of swap. Deleting the files returned **6.9 GB of swap** and 4 GB of RAM — the disconfirmable proof that the tmpfs was the *cause* of the memory pressure and not a coincidence. The script therefore (a) writes under `CLAUDE_CODE_TMPDIR`, (b) points `GOTMPDIR`/`TMPDIR` at disk too — **Go ignores `CLAUDE_CODE_TMPDIR`**, which is why 3.1 GB of *linker* scratch was in RAM and why the failure arrives as `link: mapping output file failed` and reads like a broken toolchain — and (c) **refuses outright** if its target is on a tmpfs, checked by filesystem type rather than path spelling so a future edit cannot quietly point it back.
- **⚠ LANDMINE — Go's scratch must be a SIBLING of the checkout, never the checkout itself.** The first cut set `TMPDIR` to the extracted tree. Go then treats the module as living inside the system temp root, **ignores its `go.mod` entirely**, and reports `pattern ./x/...: directory prefix does not contain main module`. That failure is indistinguishable from a genuinely broken HEAD — i.e. the one wrong answer this script must never give — and it did give it once before being fixed.
- **proven in both directions, because a checker that cannot fail is worthless:** clean HEAD → exit 0; a checkout with an injected `undefined:` symbol → exit 1, with the injection gated on a grep count so the mutation could not silently no-op.
- **cost:** ~450 MB of disk for the life of one run, plus a full extract of HEAD (seconds). `KEEP_TREE=1` retains the tree for inspection; `VERIFY_HEAD_DIR` moves it.
- **NOT in scope:** it does not lint, vet, or run the whole test suite by default; it cannot see a defect that only appears once another session commits; and it is advisory — nothing runs it for you.
- **sources:** `scripts/verify-head-builds.sh`; `016b_debugging_guide_8_consolidated.md` (the shared prescription it replaces); `docs024_key_docs_latest/tmpfs_exhaustion/HANDOFF_2026-08-23_tmp_is_ram_not_disk.md` (the measurement, the cleanup and the four LANDMINES entries this is the fourth recurrence of); `~/.claude/settings.json` (`GOTMPDIR`/`TMPDIR`, new sessions only)
- **relations:** the shared-tree family it serves — a commit that references another session's untracked file compiles for you and breaks HEAD for everyone · `scripts/council-scope.sh` (the single-sourcing precedent this follows) · `WRONG_CALLS.md` 2026-08-23 (why the two entries there still show the old command, and must)
- ~~**verify-later:** … Re-open it if `/tmp` refills after this lands — the refill rate with the recipe fixed is the measurement that decides it.~~
  > **⚠ ANSWERED 2026-08-24, and the decision rule named the wrong meter.** `/tmp` did **not** refill (+0.5 GB/day, 4.9 G / 32%), so by this test the fix had worked. It had not: the recipe **relocated** onto the disk, where `CLAUDE_CODE_TMPDIR` points scratch, and ran there at **10.4 GB/day** — 130 GB in 308 abandoned extracts, ~12 days from a full root filesystem. **When a fix works by RELOCATION, the post-fix measurement must be taken at the DESTINATION**; a green reading at the origin is what both success and failure look like, so it is worthless as evidence either way.
  > **And "eight live documents" was the wrong denominator.** `[MEASURED 2026-08-24]` **73 documents** carry a hand-rolled `git archive HEAD | tar`, **66 with no cleanup**. Eight were fixed. `CLAUDE.md` — the file every session loads unasked — named neither the script nor the trap (**0 hits**), and **0 of the 50 extracts created since this shipped use its layout**. The script is sound and was **undriven**; `CLAUDE.md` now carries the pointer.
  > **The mechanism this entry half-saw:** every pasted copy *contains* an `rm -rf`, which is why none read as leaky — but it is the **setup** half, clearing the tree the run is about to use, so it reclaims a tree of *the same name* and each run picks a new one. One session left `headtree`, `headtree2`, `headtree3`, `headfinal`, `ht5`, `ht6` — six trees, ~2.8 GB, one morning.
  > **Still open:** the `pattern-check.py` rule for document 74. Full account: `docs024_key_docs_latest/tmpfs_exhaustion/` (the standing five).

### OPP-009 — `kafka-publish-lib.sh`: publishing with an ASSERTED receipt, and telling "never sent" apart from "never consumed"

- **status:** built and exercised 2026-08-23; self-test 11/11 offline, every runtime arm proven against live data. Adopted by **21** callers as of 2026-08-24 (the whole live `scripts/` surface; the live-queue census re-derives to zero) — originally FOUR — `082_submit_domain_unified.sh`, `scripts/trigger-landmine-verifier.sh`, `097_TRIGGER_council_review_v1.sh`, and **`scripts/initial_messages/140_tool_suggester/077_update_noted_write_tool.sh`, which a DIFFERENT LANE adopted unprompted within a day** (`caa55f04f`, *"publish via kafka_publish_checked instead of the racing kubectl-run stdin form"*) — the **~156** other runnable racing publishers are unmigrated by design, not by omission. That fourth caller is the evidence for building this as a library rather than writing the guidance down again: the safe form had been documented for a month with 2 assertions to show for it, and became callable a day before someone reached for it without being asked.
- **what:** `scripts/kafka-publish-lib.sh`, **SOURCED, NEVER EXECUTED** (mode 664, like `scripts/council-scope.sh`), providing two functions.
  `kafka_publish_checked --topic T --payload <single-line JSON> [--correlation ID] [--broker B] [--timeout-ms N] [--header k=v]…` publishes via `kubectl run … --command -- sh -c "… | kcat -P … && echo PUBLISH_OK"`, then **asserts the receipt**. Returns **0** published · **10** NOT PUBLISHED (retry now) · **11** RECEIPT INDETERMINATE (verify, do not blind-retry) · **2** usage.
  `kafka_verify_landing <correlation_id> [timeout]` polls `orchestration_states`, then discriminates: **0** landed · **12** CONSUMED AND REFUSED (quotes the `agent_error_log` code) · **13** PUBLISHED, NOT LANDED (latency guidance, no retry advice).
  Codes start at 10 because callers already own 1 and 2 — `097`'s documented contract is "1 = hard error, 2 = REFUSED out of scope".
- **why it exists — the remedy existed and was not being applied.** `kubectl run -i` attaches stdin asynchronously; lose that race and `kcat -P` sees EOF, publishes nothing and exits 0, with `--rm` deleting the evidence (measured 4-in-5 lost, 2026-07-26). The fix has been documented in `LANDMINES.md` for a month. `[MEASURED 2026-08-23]` **218** scripts publish via `kcat -P`, **201** still use the racing form, **25** print a `PUBLISH_OK` receipt — and **2** assert on it. **A receipt nobody asserts on is a log line, not a control**, and 23 scripts prove that guidance-to-copy is not a delivery mechanism. Hence a library, not another recipe.
- **what it actually buys: (a) and (b) stop looking alike.** A missing orchestration row has two causes whose correct responses are OPPOSITE — never published (retry at once) versus published-but-not-consumed (wait; a duplicate costs a round, and this is also the documented signature of ordinary latency). Until now the operator could not tell which, and CLAUDE.md's "do not retry on a missing row" is right for one and exactly wrong for the other. The exit code now says which.
- **⚠ LANDMINE — `--command` is load-bearing, not decoration.** The `edenhill/kcat` image's ENTRYPOINT *is* kcat, so without `--command` your `sh -c …` arrives as ARGUMENTS TO KCAT: usage text, nothing published — the same silent zero by another route. Verified 2026-08-23.
- **⚠ LANDMINE — `kcat -P` publishes ONE MESSAGE PER LINE.** A pretty-printed payload becomes N broken messages. The library REFUSES a multi-line payload (exit 2) rather than mangling it; the base64 hop protects the payload from the shell in the same move.
- **⚠ it is NOT a broker ack.** The receipt is scraped from pod output via `kubectl attach`, which is weaker than the acknowledgement the broker gives. `platform/kafka/producer.go` is already `RequiredAcks: RequireAll, Async: false` — an **in-cluster Go submit path would make a silent drop UNREPRESENTABLE rather than merely detected**, and would close the forensic gap below. That is the right destination; it needs an image build, a council round and a fleet roll, so it is **proposed and unbuilt** (see verify-later).
- **the forensic asymmetry that makes the receipt urgent:** `[MEASURED 2026-08-23]` `orchestration_states` retains ~**2 days**; `agent_error_log` ~**30**. "Was it refused?" stays answerable for a month; "did it land?" is unanswerable after ~48h. A dropped submission becomes permanently undiagnosable almost immediately, so the evidence has to be created at publish time.
- **it deliberately does NOT retry.** On an indeterminate receipt an automatic retry is a double-publish engine: the one case where you cannot tell whether the message landed is the case where resending is most dangerous.
- **proven in both directions, on live rows:** exit 10 induced with an unreachable broker; **12** discriminated on a real `VALIDATION_ERROR_DROPPED` correlation holding no orchestration row — the exact case a lane misdiagnosed as a kcat drop on 2026-08-20; **13** on a UUID that never existed; **0** on a real landed correlation. `[UNVERIFIED end-to-end]` exit **11** is covered at the classifier only: with the payload in the container command, an exit-0-without-receipt cannot be induced from outside the library.
- **companion detector:** `check_kcat_stdin_race` in `scripts/pattern-check.py` (advisory, pre-commit). `[MEASURED 2026-08-23]` fires on **5 of 300** commits (**1.7%**), **5/5 true positives** — every one a newly added racing publisher, which is also the evidence that the class is still growing.
- **sources:** `scripts/kafka-publish-lib.sh`; `bugs_open/327` (`…_the_build_trigger_can_publish_nothing_and_exit_zero.md` — resolve by SLUG, two bugs share 327); `docs024_key_docs_latest/bugfix_327_silent_publish_drop/`; `LANDMINES.md` "kubectl run -i --rm … kcat -P < file drops roughly 4 publishes in 5 AT EXIT 0"
- **relations:** OPP-002 (the kcat trigger convention this supersedes for new work) · `scripts/council-scope.sh` (the sourced-fragment precedent, and the only other one in this repo) · `bugs_open/326` (any-status `item_key` dedup — `kafka_verify_landing` surfaces its buried `deduped:true`, which is 326's own "make the silence loud" candidate done trigger-side) · `platform/kafka/producer.go`
- **verify-later:** (1) **the Go submit path is the unbuilt destination** — whoever builds it retires the `kubectl attach` receipt and can write a durable submission record, closing the 2-day forensic window; (2) adoption is 21 callers and the LIVE queue is empty; the ~55 racing files left under scripts/ are all dormant (30d+) — re-run the census (⚠ **strip comments first**: a migrated file's own explanatory comment keeps matching a naive grep for the racing form, so the naive count never moves — 18 files as of 2026-08-24 match on comments alone) (`RUNBOOK_silent_publish_drop.md`) before quoting any figure here, since **the racing-form count grows by ADDITION and reads as current for ever**; (3) `[UNMEASURED]` the stdin race did not reproduce on 2026-08-23 (0 drops in 10, which excludes the historical 4-in-5 at p≈1e-7 but bounds today's rate only below ~26%) — the rate is load-dependent and nobody has measured it under load.

### OPP-010 — `probe-page-url.sh`: curl a page at the URL the platform RECORDS, controls included

- **status:** built and exercised 2026-08-25 (`bugs_open/387`): self-test green (7 verdict rows + a no-composed-URL property test), live-proven on ai-agent-orchestration.com (3 targets SERVING, both controls held, exit 0) and in the refusal direction (an unknown page name exits 2 with near-match hints, never a guessed 404). The same probe logic, run as a one-off fleet sweep the same day, measured **0 of 709** deployed active pages 404 at `pages.url`.
- **what:** `scripts/probe-page-url.sh <domain> <page-name>… | <domain> --all | --self-test`. Resolves each page's URL from `pages.url` (it structurally cannot compose one from a name — asserted by its own self-test), curls it verbatim, and runs BOTH per-domain controls in the same pass: an invented URL must be non-200 (catch-all detector — "a parked domain 200s EVERY path") and a deployed non-target sibling must be 200 (URL-form/hosting control — the one whose absence filed `bugs_open/387`). Exit 0 = serving; 1 = a target is absent while both controls hold (a real finding); 2 = refusal (unknown name, DB unreachable, a control failed) — never a pass.
- **why it exists — the fourth occurrence, two of them post-warning.** A session composed `/<name>` or `/<name>.html` from `pages.name` and read the 404 as damage on 2026-07-27 (`/sale`, WRONG_CALLS), 2026-08-09 (`LANDMINES.md` "A page's served URL is NOT derivable from `pages.name`"), 2026-08-24 (`bugs_open/387` filed "deployed and 404" against pages serving 200), and 2026-08-25 (the 387 session itself, mid-investigation). Two written warnings did not stop occurrences three and four; per the WRONG_CALLS tally rule, a check that keeps appearing gets automated.
- **deliberately NOT built here:** a per-page reachability discovery check. Measured demand was zero (0/709), the linked case is `dead_internal_link_live`'s (DGH-015's recorded seam: availability belongs to `site_unreachable`, staleness to `page_content_divergence`), and a third fetcher of every page would re-open the double-filing question those two checks settled.
- **sources:** `scripts/probe-page-url.sh`; `bugs_open/387…md` (CORRECTED block); `docs024_key_docs_latest/bugfix_387_deployed_and_404/{RUNBOOK,NOTES}_387.md`; `LANDMINES.md` "A page's served URL is NOT derivable from `pages.name`" (now pointing here).
- **relations:** IMP-053 `site_unreachable` (apex-only, by design) · DGH-015 `page_content_divergence` (its non-200 skip is deliberate) · OPP-008/OPP-009 (the same drift-class argument: one implementation, N callers).

### OPP-011 — `check_register_roll_claim_without_commit`: a status that waits for a roll must name the commit
- **status:** deployed 2026-08-31 (advisory; 24th check in `scripts/pattern-check.py`, run by `.githooks/pre-commit`). Python, so live the moment it lands — no roll, no image. Council `Council-Submitted: 37b0bec4-f503-4b9a-8fc4-688ba29aa2bc`.
- **what:** Fires on a commit that ADDS a `- **status:**` / `- **status-evidence:**` line to a register category file whose claim is conditional on a roll ("inert until the next chassis roll", "rides the next roll", "not yet live") and which names no commit. A version tag (`v1.0.1322`) counts as an answer; a hex token counts only once `git cat-file -e <tok>^{commit}` resolves it.
- **why a gate and not the daily watcher:** the author is the only person who knows the sha without archaeology — it is the commit they are making. A watcher reports the gap next morning to nobody in particular. Same argument as OPP-004 and OPP-006: put the check where the error is made.
- **why it matters at all:** a status is the register's current-state claim and council seats read those as ground truth (LANDMINES, "a concept-register STATUS line is a snapshot that outlives its truth"). "Inert until the next roll" is its most perishable shape — true for hours, wrong for ever after. Since BLD-019 every binary stamps its provenance, so "did this ship?" is a query (`git merge-base --is-ancestor <sha> <the stamp>`) — but only if the entry names a commit.
- **measured before inclusion** (this file's bar), all figures `[MEASURED 2026-08-31]` at HEAD `028c3e112`: **2,027 entries; 125 statuses roll-conditional; 24 of those withdrawn via `~~strikethrough~~`; 101 live claims; 28 (27%) naming neither commit nor version tag.** Fire rate **8 of 45 register-touching commits (17%)**, 2026-08-26 → 08-31 — NAV-014, DGH-018, IMP-058, LNK-040 (×2), IMP-056, WII-036, PBP-049 — inspected, 0 false positives.
- **⚠ the design decision that carries the check, and it is not the trigger phrase:** a hex token is accepted only if git RESOLVES it to a commit. A council correlation id is 8 hex characters with digits and is indistinguishable from a short sha by pattern alone — `LNK-040` carries `corr e9bda035` today, which `git cat-file` rejects while `bf1fbc5b7` resolves. **Regex-only, the fire rate is 2% and seven of the eight real cases are silently exempted**: a check that reads as quiet and useful while missing most of its population. Strikethrough stripping (−24 wrong fires) and the version-tag exemption (−4: SEO-003, SYS-092, PLAN-027, LNK-034) carry the rest.
- **⚠ measuring this check needs a DEMAND CONTROL in the loop body.** The first fire-rate run reported **0/45** and the zero was a broken grep in the harness (`^   .roll-claim` assumes one character before the name; the ANSI prefix is four), not a quiet check. A known positive (`bf1fbc5b7`) run through the *same* loop body is what caught it.
- **only ADDED status lines fire**, so the 28-entry backlog is never mentioned and a reworded or relocated entry stays silent — the ratchet reasoning from `check_register_coverage`, whose own note says flagging accepted backlog every commit "is how a check becomes wallpaper".
- **sources:** `scripts/pattern-check.py` (`check_register_roll_claim_without_commit`, `_dates_its_claim`); commit `1efc84362`; `docs026_concept_register/HANDOFF_2026-08-17_continue_here.md` item 1 (carried across four handoffs)
- **relations:** OPP-006 `check_register_entry_without_row` (same corpus, same only-added-lines discipline, same file) · OPP-004 (put the check where the error is made) · BLD-019 build provenance, which is what makes a cited sha answerable · DOC-074 the daily drift watcher, which this deliberately does not duplicate

### OPP-012 — `sedo-api.sh`: Sedo marketplace API client that keeps credentials out of session transcripts
- **status:** built 2026-09-02; **credential-free paths exercised, credentialed path UNEXERCISED** — "deployed" would overstate it. No Sedo account exists yet: blocked on the owner obtaining a Partner ID + SignKey from Sedo (RUNBOOK §1–§3). `--self-test` 7/7 PASS and `--probe` PASS (in-cluster pod → api.sedo.com → expected `SEDOFAULT E7` on dummy creds), both `[MEASURED 2026-09-02]`.
- **what:** `scripts/domains/sedo-api.sh <Function> [k=v …] | --self-test | --probe | --check-secret`. Calls Sedo's domain-marketplace API (`https://api.sedo.com/api/v1/`, GET/POST + XML: DomainList/Insert/Edit/Delete, prices, parking) from an ephemeral `curlimages/curl` pod. Credentials live in K8s secret `sedo-api-credentials` and reach the pod via `envFrom: secretRef`, expanded **in-container** into a 0600 curl config — never in argv, overrides JSON, pod spec, container logs, or the transcript (owner ruling 2026-08-23). `--check-secret` prints key NAMES only.
- **why the shape:** BusyBox wget does no TLS cert validation (`[MEASURED 2026-09-02]` in-cluster) — wrong transport for calls carrying the account password, which Sedo's auth model sends on EVERY request. `kubectl run -i` records its command line in container logs (kubectl's own warning) and double-prints on attach failure, so the runner is run → wait → logs → delete. Sedo faults arrive in-band as `<SEDOFAULT>` XML on a readable body, so the E7 fault is usable as a credential-free transport control.
- **sources:** `scripts/domains/sedo-api.sh`; `docs024_key_docs_latest/sedo_domain_management/{PLAN_2026-09-02_sedo_domain_management,RUNBOOK_sedo_domain_management,NOTES_sedo_domain_management}.md`; api.sedo.com/apidocs/v1/Basic/
- **relations:** OPP-002 (verify-by-artifact; this is the instrument) · `never-extract-keys-probe-from-the-pod` ruling (the credential design it implements) · idea.uk + improvement-loop lanes (own the parked-at-Dan/Afternic domains; re-pointing nameservers routes through THEM, not here — PLAN "Cross-lane constraint")
- **verify-later:** (1) first credentialed call (`DomainList`) once the secret exists — until then the auth param names (`SEDO_PARTNERID` etc. → `partnerid` etc.) are doc-derived, not proven against a live account; (2) the pinned `curlimages/curl:8.10.1` will want a bump eventually; (3) if Sedo becomes load-bearing (scheduled sync, stats into traffic-analytics), the destination is a proper Go adapter under `platform/` — council scope — and this script retires to a debugging tool.

### OPP-013 — `afternic-csv.py`: Afternic portfolio-export ingest for the no-credential CSV loop
- **status:** built 2026-09-02; **mechanics proven, real export format UNSEEN** — `--self-test` 13/13 PASS and the cell-count refusal guard proven by mutation (`if False:` in its place fails the suite), both `[MEASURED 2026-09-02]`. Blocked on the owner's first portfolio export (lane RUNBOOK §1); until it arrives, the header vocabulary is `[ASSUMED]` from the owner's 2026-07-28 dashboard paste + Afternic's bulk-template vocabulary.
- **what:** `scripts/domains/afternic-csv.py ingest <export.csv> [--known FILE] [--control DOMAIN:FIELD:VALUE …] [--baseline auto|FILE] | --self-test`. Parses the owner's Afternic portfolio export into a dated snapshot (`afternic_domain_management/snapshots/`) + report: status/price coverage, leads, cross-check against a known-domains file, diff vs previous snapshot. **Afternic has no self-serve seller API** (verified 2026-09-02: partner-only Fast Transfer/DLS APIs; the seller product is an in-dashboard agent), so the owner chose a no-credential CSV loop — export in, bulk XLSX template back. Exit 1 = refused rows or a failed control; a figure from an exit-1 run is unquotable.
- **why the shape:** columns map by HEADER NAME only, a row whose cell count disagrees with the header is refused (never padded), unmapped headers are reported not interpreted, and `--control` pins a value the owner quotes off his live dashboard. Each is the mechanised form of WRONG_CALLS 2026-07-28, where a positional read of a 2-cell paste under an 11-column header invented "Minimum Offer = 0" and reached five documents.
- **sources:** `scripts/domains/afternic-csv.py`; `docs024_key_docs_latest/afternic_domain_management/{PLAN_2026-09-02_afternic_domain_management,RUNBOOK_afternic_domain_management,NOTES_afternic_domain_management}.md`
- **relations:** OPP-012 sedo-api.sh (sibling marketplace lane, credentialed route) · `classify_nameservers.py` (covers the lane's NS/verification scope, reused not rebuilt) · domains_cloudflare_rollout lane (owns registrars + re-pointing) · WRONG_CALLS 2026-07-28 Afternic-paste entry (the incident this tool retires)
- **verify-later:** (1) lock `ALIASES` against the first real export and record additions in the lane NOTES; (2) the generate half (fill `bulk_upload_sample_v3.xlsx` from desired state) is deliberately unbuilt until the template file + owner pricing decisions exist — PLAN P4.

### OPP-014 — `cf-zone-bootstrap.sh`: create + wire a portfolio Cloudflare zone, and the dangling-delegation recovery

- **status:** built 2026-09-02; **`--check` (read-only) EXERCISED live** (four NO-ZONE verdicts + a correct garden-tools.uk reference read, honest exit 1, `[MEASURED 2026-09-02]`); **the mutating half is UNEXERCISED BY A SESSION and is designed to stay that way** — the permission classifier refuses credentialed writes, so the create path runs from the owner's own prompt.
- **what:** `scripts/domains/cf-zone-bootstrap.sh [--check] <domain> [...]`. Encodes the zone recipe proven 2026-08-25 on homegarden.uk (domains_cloudflare_rollout RUNBOOK): create zone under the portfolio account → 2 proxied A records at the `192.0.2.1` placeholder → 2 `portfolio-sites-router` routes → verify by RE-READING against the garden-tools.uk reference shape (never POST receipts) → `activation_check` + status poll. Idempotent: tops up whichever records/routes are missing on an existing zone. Token = `~/.config/cloudflare/portfoliotoken` (the read-only `token` is DEAD since ≤2026-09-02, error 9109), read with `tr -d '[:space:]'`, never echoed.
- **why it exists:** the LANDMINES dangling-delegation trap fired four times at once on 2026-09-02 — an owner-side Nominet NS batch pointed advertise/designblog/seotools/websitepromotion.co.uk at alexis/leah with no zone in the account, so the registry answered while the edge REFUSED and the domains went dark as caches drained. Zone FIRST, NS second; this script is both the ordering's tool and the recovery when it is violated.
- **sources:** `scripts/domains/cf-zone-bootstrap.sh`; `docs024_key_docs_latest/nominet_domain_management/{NOTES_nominet_domain_management,RUNBOOK_nominet_domain_management}.md` (§5); `domains_cloudflare_rollout/RUNBOOK` (the proven recipe + reference zone)
- **relations:** OPP-012/OPP-013 (the `scripts/domains/` family) · domains_cloudflare_rollout lane (owns the Cloudflare half; this lane stages it when the incident is Nominet-triggered) · LANDMINES "A domain delegated to Cloudflare with NO zone…" (the trap) and "A parked domain answers 200 on EVERY path" (the verification bar after activation)
- **verify-later:** first owner run of the mutating half (the four domains above are the pending case); whether a newly created zone on this account can ever be assigned a pair other than alexis/leah (two pairs exist across the 42 zones — betty/ivan on 10 — cause unestablished).

### OPP-015 — `nominet.py`: the consolidated Nominet EPP client (the .uk member of the registrar family)

- **status:** built 2026-09-02 (owner: the Nominet lane gets "the same sort of thing as" dynadot/porkbun); **`login` PROVEN LIVE the same evening** — owner-run, result 1000 through the pod tunnel `[MEASURED 2026-09-02]`, which also re-proved the EPP allowlist for node `.37` (the 08-11 proof was from `.26`). `--self-test` 15/15; `probe` 2,527 B. **walk/list/check/info/set-ns still UNEXERCISED** (credentialed, owner-run; the session classifier refuses them by design).
- **what:** `scripts/domains/nominet.py [--direct] [--credentials FILE] <probe|login|list YYYY-MM|walk [--months N]|check DOMAIN…|info DOMAIN|set-ns DOMAIN --ns … [--apply]>`. Consolidates the proven scattered pieces (epp.pl login/list; the VMB-015/016 box siblings) into one family-style command: credentials read in-process from `~/.config/nominet/credentials` (never printed, never argv, XML-escaped — the box scripts interpolated the password raw), RFC 5734 framing done locally, transport tunnelled through `kubectl exec -i postgres-clients-0 -- openssl s_client -4` so egress is the allowlisted cluster set. `register` deliberately REFUSES and points at VMB-017 — one implementation of the money-spending verb. `set-ns` dry-runs by default, host:creates on 2303 and verifies by re-reading.
- **why the shape:** Nominet allowlists the egress IP and only the five cluster node IPs are stable, so a local-socket client can never log in from the office line — the tunnel is the 2026-08-11 login-proof mechanism made reusable. `walk --months` goes to 120 because .uk registers up to 10 YEARS: the 08-19 twelve-month recipe silently omits any longer registration, and the symptom is a plausible-looking short list.
- **sources:** `scripts/domains/nominet.py`; `docs024_key_docs_latest/nominet_domain_management/{RUNBOOK_nominet_domain_management,NOTES_nominet_domain_management}.md` (§2/§4 + the 2026-09-02 (later) entry)
- **relations:** OPP-012/013/014 (the `scripts/domains/` family) · VMB-015/016 (the proven originals, kept as fallbacks until the first credentialed nominet.py run) · VMB-017 (owns `domain:create`) · LANDMINES "the greeting is served to ANY IP" (only `login` proves the allowlist)
- **verify-later:** the owner's first `login` (also re-proves the allowlist for whichever node `postgres-clients-0` currently egresses from — it moved `.26`→`.37` between 08-11 and 09-02); the first `walk` total against the ~1,500 estimate; retire-or-keep decision on the epp.pl/box fallbacks after the credentialed paths prove out.
