# Register — build-pipeline

> **covers-through: 2026-07-13** · extraction freeze.
> Subsystems that shipped after this date may be absent from this file
> **entirely** — absence here is not evidence of absence in the platform. See `bugs_open/106`.

_Concept count retired 2026-08-09 — derived, not stored; run the drift pair in `000_concept_index.md`, or read `concept-register-drift-check`'s daily row (DOC-074). It said **15** and the file held **17**._ consolidated from 17 raw extractions across units U14, U15, U18, U21.
Absorbed categories: new:site-build-pipeline, new:site-build-orchestration-generations
(their raw material described the same builder-lineage territory as new:build-pipeline
and is folded in below rather than kept as separate near-empty register files).

### BLD-001 — Builder route method: map what exists before building (§B0 census)
- **status:** convention
- **status-evidence:** builder_route(21) "Rule honoured: map what EXISTS against what the problem statement wants BEFORE creating anything. Sources: the 147-row agent_definitions census (2026-07-03)."
- **stage2-verified (2026-07-14):** deployed → convention — This is a documented methodology/finding (census route method), not a built artifact claim; no code to check — status-evidence is a doc citation of a one-off analysis, correctly re-classed as process not code-deployed.
- **what:** The builder route's opening method: an inventory matrix of problem-statement capabilities (intake, research, planning, design, content, tools, feeds, infographics, build/deploy, improvement, observability) against the ~147 existing agent types. Findings: every section except infographics has agents; the real defect is ~8 overlapping top-tier "build the site" orchestrators; the per-section content family is already prototyped; genuine gaps are the infographics owner and a success-factor synthesis step. Liveness is judged from pump + handler references, not the status column.
- **sources:** docs019/RUNBOOK_builder_route(21).md#B0, #B0-findings
- **relations:** three builder generations + work-item relay spine (BLD-002); vertical-exemplar researcher (the gap filled)
- **verify-later:** agent_definitions census queries; duplicate-row Q1

### BLD-002 — Three coexisting builder generations + the work-item relay spine (baton/hop model)
- **status:** deployed
- **status-evidence:** builder_route(21) §B1: "Three generations coexist: GEN-1 (template era) … GEN-2 (in-memory multipage) … GEN-3 (component/spec/DB era — the LIVE architecture): pageflow-builder v20 (ACTIVE)"; §B3 "DECISION: the relay reaches page-build-handler natively ⇒ THE SPINE = THE WORK-ITEM RELAY"; MILESTONE 2026-07-06 "first end-to-end domain→deployed site through the relay" (dartsonline.com). Independently re-derived and confirmed by a second unit the same week ("THREE generations coexist... §B3 CLOSED: spine = the work-item relay").
- **what:** The archaeology of site building, and the settled current architecture. Three generations: GEN-1 template chains (strategist→architect→writer→html-assembler→site-deployer); GEN-2 in-memory multipage (chief-strategist→content loop→assemble→deployer-agent, no components/specs/review); GEN-3 component/spec/DB era — `pageflow-builder` v20's full inline build, with `site-work-orchestrator` as its queue-native sibling. Explains duplicate deployers (site-deployer serves GEN-1; deployer-agent GEN-2/3). The settled spine: work moves as a relay of `site_work_items` batons — each names a handler_agent; the 30s pump claims unclaimed batons and spawns the named agent; the agent does one job, writes findings to `site_specs` (the site's shared notebook — spec-not-message), creates the next baton, stops. Full chain: domain-submitter/adoption → classifier → (vertical research) → strategist → briefing → build-site-planner (emits needs_page/design/imagery/rerender) → page-build-handler per page → rerender/deploy. `pageflow-builder` survives as intake's initial-build convenience, demoted to "intake convenience" rather than the spine. A commented-out `"tool"` route in the relay's routing table is the mechanism gap blocking tool/infographics pages from the relay.
- **sources:** docs019/RUNBOOK_builder_route(21).md#B1, #B3, #milestone; NOTES_running_synthesis_v4(39).md 2026-07-04 "§B0" through "§B3 CLOSED"
- **relations:** builder route method (BLD-001); build pump (BLD-003); roadmap-phases scope decision gap (site-plan-and-reconciler register, PLAN-033); MVP build squad lineage (BLD-014, predecessor history)
- **verify-later:** workflow dumps of the nine builders; pageflow-builder v20 definition; load_work_item_actions.go routing table; the 37-row dartsonline item chain

### BLD-003 — Build pump and the queue immune system
- **status:** deployed
- **status-evidence:** builder_route(21) §B2: "the scheduler fires build-pipeline-trigger EVERY 30s … The queue's immune system is all ENABLED: claimed-item-timeout (evidence-based auto-complete), feasibility-recheck, stale-orchestration-reaper, stale-work-item-reaper (48h), work-item-archiver, database-cleanup. FLAG: improvement-sweep is DISABLED."
- **what:** What drives the relay: scheduled `build-pipeline-trigger` (30s, pre_query gated, concurrency dispatch/8) → `build-dispatch-loop` → atomic claim → spawn dynamic handler → complete/fail → touch scheduled_tasks. The immune system self-heals the queue (claimed-item-timeout does evidence-based auto-complete; feasibility-recheck unblocks when handlers appear; reapers and archiver bound staleness). Standing flag: improvement-sweep is disabled platform-wide, so the improvement loop is not running; content-feed-refresh is enabled 6-hourly.
- **sources:** docs019/RUNBOOK_builder_route(21).md#B2
- **relations:** work-item relay spine (BLD-002); dispatch chain + NOT-EXISTS blocker (work-dispatch register)
- **verify-later:** scheduled_tasks rows (build-pipeline-trigger, improvement-sweep enabled flags); claimed-item-timeout SQL

### BLD-004 — Two front doors and duplicate classifiers (Q5)
- **status:** partial
- **status-evidence:** builder_route(21) "Two front doors, two classifiers (overlap)"; queue item 2 "[MAIN] Q5 front-door consolidation — two classifiers, one responsibility" (queued, undecided).
- **what:** Intake exists twice: the queue door (domain-submitter → work-item relay with domain-research-classifier) and intake-orchestrator v3 (HITL: site-classifier → confirm type → questionnaire → briefing-agent → spawn dynamic builder). site-classifier and domain-research-classifier hold the same responsibility; the classifier prompt hardcodes `recommended_builder="pageflow-builder"`; intake carries orphaned rerender steps. Consolidation direction (deprecate the intake door vs align contracts) is an open decision.
- **sources:** docs019/RUNBOOK_builder_route(21).md#B1, #queue (item 2)
- **relations:** work-item relay spine (BLD-002); site_type taxonomy drift
- **verify-later:** intake-orchestrator usage evidence; site-classifier workflow

### BLD-005 — image_tag 'latest' stale-default trap
- **status:** partial
- **status-evidence:** builder_route(21): "INCIDENT 2026-07-06 — first claim STALLED … THE ONE REAL DIFFERENCE: image_tag='latest' (column default) … the registry's latest is an ANCIENT chassis build … FIX APPLIED … NEW PARKED TRAP (systemic): agent_definitions.image_tag DEFAULTS to 'latest' — every future seeded agent inherits it."
- **what:** Seeded agents inherit `image_tag='latest'`, which points at an ancient pre-architecture chassis build (boots the retired generic.process consumer regardless of env) — a newly seeded researcher stalled on it. Immediate fix: copy image columns from a live donor in every seed. Systemic options parked: repoint/retire `latest`, ALTER the column default, or a New Agent checklist line. Rollback convention is the same lever inverted: revert by repointing image_tag to the prior tag.
- **sources:** docs019/RUNBOOK_builder_route(21).md#B4 (incident), #queue (item 1)
- **relations:** stale-corpus class; standing evidence rules (seed hygiene)
- **verify-later:** agent_definitions image_tag column default; whether redeploy-agents bumps rows

### BLD-006 — Coverage baseline: guides, tools, news, curated top-N on most sites
- **status:** aspirational
- **status-evidence:** builder_route(21) queue item 7: "standing expectation going forward is most sites should carry guides + tools + news + a curated (LLM-picked, non-affiliate) top-N list … the curated-list mechanism, which IS new"; "STANDING EXPECTATION HOME: 001_development_guide."
- **what:** A platform content-coverage policy: most sites should carry guides, tools, news, and a curated non-affiliate top-N list of the vertical's best products/services with outbound links; "pages need not be original to be best-in-class." Enforcement points are the strategist/planner prompts; the curated-list mechanism is the one genuinely new build needed (reuse candidates: research-agent or the exemplar-researcher crawl pattern feeding a curation step). The mechanism for guides/tools/news already EXISTS (proven on other sites) — an absent one elsewhere is a broken route, not a missing feature.
- **sources:** docs019/RUNBOOK_builder_route(21).md#queue (item 7)
- **relations:** roadmap-phases scope decision gap (site-plan-and-reconciler register, PLAN-033); F0 guides pilot
- **verify-later:** 001 guideline amendment; strategist/planner prompt coverage clauses

### BLD-007 — pageflow-builder (component-based site build orchestration)
- **status:** deployed
- **status-evidence:** Still being patched in Phase 0 imagery work 2026-05-05 (its row backed up before a migration); a full live step chain is documented from later units too, confirming it as GEN-3's active monolith.
- **what:** The central v2-era builder, renamed from multipage-website-builder v3. Spawns planner/content-writer/reviewer/deployer, then: ensure_site_record → call_site_planner → store brief+plan → sync_pages_to_db → populate_nav → asset steps → select_style_collection → set_default_components → render_site_components → get_pages_to_build (filters by build_status) → build_pages_loop (write → review → assemble → deploy per page) → apply_site_design (CSS) → trigger_site_deploy (Cloudflare). The known hazard that sync_pages_to_db can reset page statuses is documented in-file.
- **sources:** 026_pageflow_builder.sql; sql_for_agents_v2/026_pageflow_builder.sql; 107_image_build_handler.sql (backup section)
- **relations:** three builder generations (BLD-002); site-planner (site-plan-and-reconciler register, PLAN-034); page-content-writer (BLD-008)
- **verify-later:** whether new sites still route through pageflow-builder or only via the work-item pipeline

### BLD-008 — page-content-writer (section-by-section content generation)
- **status:** deployed
- **status-evidence:** Continuously patched from v2 era through migration 069 (reads site_specs) and the 107-era imagery direction; 075 gives it idle_timeout 180.
- **what:** Writes one page section-by-section: spawn_research_agent → load_page_components → build_render_context → process_sections_loop (per-section LLM call constrained to that component's llm_field_specs) → compile_page. The prompt is a major behavioural contract: official-contact-only rule, internal-link constraint to listed pages, content_direction/imagery_direction from site_specs, admin content briefs, "Recreate Mode" for adopted sites, and an 18-rule anti-fabrication list (no invented people/testimonials/statistics/case studies). The same agent is documented in the page-build-pipeline register from the no-persistence architectural angle (PBP-009) — this entry covers its SQL/prompt-migration history and behavioural contract.
- **sources:** 023_page_content_writer_agent.sql; sql_for_agents_v2/023_page_content_writer_agent.sql; 069_blog_posts.sql
- **relations:** page-content-writer task-specialist fact (page-build-pipeline register, PBP-009); pageflow-builder (BLD-007); page-build-handler (BLD-011)
- **verify-later:** live prompt_template vs the 023 copies; llm_field_specs source in content_components

### BLD-009 — site-work-orchestrator (unified build/maintenance over site_work_items)
- **status:** deployed
- **status-evidence:** 045 definition; row backed up and patched in Phase 0 imagery work (107, 2026-05-05).
- **what:** Orchestrator that builds sites from prioritized site_work_items rows, calling appropriate handler agents per item, "compatible with pageflow-builder's planner and content writer." The first expression of the unified build/maintenance queue idea, later refined into the one-item-at-a-time build-dispatch-loop.
- **sources:** 045_site_work_orchestrator.sql; 107_image_build_handler.sql
- **relations:** site_work_items table; build-dispatch-loop / work-item build pipeline (BLD-010); unified build & maintenance (work-dispatch register)
- **verify-later:** which orchestrator the live triggers use; site_work_items schema

### BLD-010 — Work-item build pipeline: domain-submitter → dispatch loop → handler agents
- **status:** deployed
- **status-evidence:** 051/052 definitions; migration 075 sets idle timeouts across the whole handler fleet; migration 146 still adding items into the same queue in 2026-07.
- **what:** The current architecture, as reconstructed from the SQL migration history. domain-submitter (068) creates a site record + needs_domain_research item from just a domain. build-pipeline-trigger (052) is a 30-min heartbeat: seeds the build queue, finds one site with pending items, fires build-dispatch-loop (051), which loads the highest-priority claimable item, claims it, spawns+calls the handler agent, marks complete, and if items remain spawns a FRESH dispatch loop (separate orchestration, clean logs). Item chain for a new site: needs_domain_research → needs_strategy → needs_briefing → needs_site_plan → needs_content_page (per page) → images → needs_rerender. Concurrency safety via claim_work_item; health-gating via ai_endpoint_health before claiming.
- **sources:** 051_build_dispatch_loop.sql; 052_build_pipeline_trigger.sql; 068_domain_submitter_agent.sql; 085_ai_endpoint_health_checker.sql
- **relations:** every handler agent in this file; dispatch chain (work-dispatch register); replaces intake-orchestrator
- **verify-later:** LoadWorkItemsAction first_item patch; claim semantics; current item_type → handler_agent routing table

### BLD-011 — page-build-handler (content-page handler with section planning and validation gates)
- **status:** deployed
- **status-evidence:** Migration 065 documents the evolved workflow with plan_sections/validate_content and error paths; migration 070 notes the empty_sections handler switched to it.
- **what:** Wrapper solving "specialist vs handler": page-content-writer generates but doesn't persist, so this handler loads page + specs, plan_sections resolves data sources per section (creating deferred items when sections aren't ready), calls the writer, validate_content checks placeholders/templates/cross-site contamination (blockers → mark_needs_review), then save_page_sections, update_page_status, and deploys via page-rerender. An earlier version (055) was simpler (no plan/validate steps, no deploy).
- **sources:** 065_page_build_handler_wrapper.sql; 055_page_build_handler.sql; 070_blog_content_planner.sql
- **relations:** page-build-handler build path (page-build-pipeline register, PBP-008); page-content-writer (BLD-008); page-rebuild (BLD-013)
- **verify-later:** plan_sections + validate_page_content actions; deferred-item creation

### BLD-012 — MVP build squad lineage (chief-strategist → architect → content-creator → deployer)
- **status:** superseded
- **status-evidence:** docs009/001 defines the 4 MVP agents with Kafka payloads; docs015/004 shows the mature descendant ("pageflow-builder: ensure_site_record → call_site_planner → sync_pages → populate_nav → image generation → style collection → build_pages_loop").
- **what:** The builder pipeline's earliest evolutionary line, predating the GEN-1/2/3 framing in BLD-002: mvp-site-builder (4 agents, single page) → landing-page-builder / content-site-builder (specialist architects per site type) → multipage-website-builder (batching, then sequential loop) → pageflow-builder (DB-backed pages, per-page loop with review and git commit) → site-work-orchestrator (work-item driven). Each generation kept strategist/planner, writer, assembler, deployer roles while moving state from CollectedData into the database.
- **sources:** docs009_site_interrogation_and_solutions/001_gemini_discussions_multipage.md#1-Agent-chief-strategist; docs006_workflow_builder/003_current_state_of_agents.sql; docs015_data_flow_verification/004_builder_flow.md
- **relations:** three builder generations (BLD-002, later-era continuation of this lineage); sequential page generation (BLD-013); pageflow-builder (BLD-007)
- **verify-later:** which builder agent_definitions still exist and which have traffic

### BLD-013 — Sequential page generation (Phase 0 multipage fix)
- **status:** superseded
- **status-evidence:** docs010/019 "Current Problem: Multipage-website-builder tries to generate 4 pages at once — race conditions... Solution: make it work like landing-page-builder: sequential, one page at a time"; two-delay spawn fix noted FIXED in docs009/003.
- **what:** Replacing parallel batch spawning with a strategist-planned page list iterated by the loop action (research → write per page), a `wrap_multipage` action generating navigation and collecting assets, and spawn timing fixed by double initialization delays. The stabilization step that made multipage builds reliable enough for everything after.
- **sources:** docs010_multitrack_flows_persona_architecture/019_start_here_document.md; docs009_site_interrogation_and_solutions/003_claude_save_point.md#Status
- **relations:** MVP build squad lineage (BLD-012); multi-page site support (site-plan-and-reconciler register, PLAN-037, the following generation); pageflow-builder (BLD-007, successor)
- **verify-later:** wrap_multipage action; spawn_actions.go delay logic

### BLD-014 — Selective rebuild via build_status
- **status:** deployed
- **status-evidence:** docs018/004 ("get_pages_to_build filters by build_status IN ('planned','needs_rebuild')"); docs015/003 documents the stale-page trap ("If the site planner didn't include use-cases in the new plan... it shows in nav but has stale content").
- **what:** Two orthogonal page state columns — status (active/deleted/needs_attention lifecycle) and build_status (planned/needs_rebuild/deployed) — let rebuilds touch only marked pages. Known failure mode: pages absent from a new plan silently keep old content while remaining in nav. Ancestor of work-item-driven rebuild targeting (BLD-010).
- **sources:** docs018_rerendering/004_trigger_just_pages_that_need_rebuild.md; docs015_data_flow_verification/003_temp_doc_rebuild_flow.md; docs017_legacy_agent_rules_images_design_keydocs/041_page_rebuild_action.md
- **relations:** page-rebuild (BLD-015); maintenance_queue (proto-work-items); work items (work-dispatch register)
- **verify-later:** get_pages_to_build action; build_status usage today

### BLD-015 — page-rebuild (rebuild pages without re-planning)
- **status:** deployed
- **status-evidence:** 039 full definition with detailed reuse/skip lists; no later references found in the source unit.
- **stage2-verified (2026-07-14):** unknown → deployed — page-rebuild is actively wired post-dispatch-loop-refactor: maintenance-triage agent (k8s/bk_agent_definitions_backup.sql:183, status='active') workflow calls agent_type 'page-rebuild' via call_agent in a rebuild_loop step; live Go actions back this end-to-end — platform/orchestration/actions/maintenance_actions.go ...
- **what:** Rebuilds specific pages (build_status='needs_rebuild') on an existing site, loading all context from DB given a domain, explicitly skipping planner, sync_pages_to_db, asset generation, component rendering, CSS and nav (all already done) while reusing the standard build-loop agents. Documents design principles: agent owns its domain; spawnable not standalone; reuse before creating; complexity in Go.
- **sources:** 039_page_rebuild_agent.sql
- **relations:** pageflow-builder (BLD-007, same loop, different input_mapping via rebuild_context); selective rebuild via build_status (BLD-014)
- **verify-later:** whether page-rebuild survived the dispatch-loop refactor — **ANSWERED 2026-08-06**: yes, see BLD-016, first real end-to-end run confirmed the whole chain live

### BLD-016 — Operator-driven bulk page rebuild entry point (`rebuild_pages.sh`)
- **status:** deployed, first real run proven live 2026-08-06
- **status-evidence:** `maintenance_queue` sat with 2 historic rows (max `created_at` 2026-02-18) and `maintenance-triage` (BLD-015's caller) had zero `scheduled_tasks` rows targeting it — the paved road was built but nothing ever drove it. Fired a real (`DRY_RUN=0`) dispatch 2026-08-06 (`vetcomparison.uk`/`index`, correlation `093164d1-...`): claimed→complete in ~3.5 min, all `orchestration_states` rows `COMPLETED`, `pages.build_status` flipped to `deployed`, and the deployed artefact's `last-modified` header matched the run's completion to the second — checked the live page, not just the status.
- **what:** A CLI script giving an operator a supported way to say "rebuild these specific pages, for this reason" instead of hand-mutating an unrelated old `site_work_items` row (the failure mode `features_open/021` was filed over). Inserts a `maintenance_queue` row (`task_type='page_rebuild'`, `reason`, `requested_by`, `payload.pages`) then dispatches `maintenance-triage` directly, since nothing else ever claims that row. `DRY_RUN=1` (default) does no DB write and no dispatch — local report only; this had to be fixed once the script's own first test showed the workflow's native `dry_run` branch previews the automated scanner, never an operator-named page list. Pre-flight warns that `page-rebuild` sweeps in EVERY `needs_rebuild` page already on the target site, not only the named ones.
- **sources:** `features_open/021_FEATURE_operator_bulk_page_rebuild.md`; `docs024_key_docs_latest/feature_021_operator_bulk_page_rebuild/{PLAN,NOTES,RUNBOOK}_*.md`; script at `docs024_key_docs_latest/feature_021_operator_bulk_page_rebuild/scripts/rebuild_pages.sh`
- **relations:** page-rebuild (BLD-015, the pipeline this drives); selective rebuild via build_status (BLD-014, names `maintenance_queue` as "proto-work-items" — this is that table's first real driver)
- **verify-later:** intent (recompose vs rerender) is written to the payload but not read by any code yet; whether real usage volume ever justifies this its own Kafka topic (shares the general `system.agent.generic.requests` lane today, unlike council-gate)

### BLD-017 — `directory-build-handler` + `query.business_directory` (bugs_open/206)
- **status:** ~~built 2026-08-06, council submission + roll owed — NOT yet live~~ **DEPLOYED, PROVEN LIVE 2026-08-08** — council APPROVED round 3 (corr `5b8e4cf7`); code pod-grepped on v1.0.1264/1266 both replicas (5/3/1/1, negative control 0); migrations 325/326 applied **plus TWO live-fire corrections: 336** (326's `call_page_build_handler` input_mapping keys carried the `input_data.` prefix — child rejected the call) **and 337** (delegation must also forward `spec` + `current_page`; `update_status` resolves the page from `input_data.spec.page_name`/`current_page.name` and runs BEFORE `deploy_page`). Proof at the artefact: work item `715ec305` (`needs_page:directory-index`) completed via ordinary `build-pipeline-trigger` dispatch; `https://vetcomparison.uk/directory/index.html` serves HTTP 200 with 61 real practices, 49 postcodes, alphabetical, from `business_intel` (fetched 17:03Z). ⚠ **the seed file `326_...sql` does NOT match the live row** — its input_mapping is the defective original; read 336+337 with it, never re-apply 326 alone. First production `ensure_page_section_layout` run was driven by the improvement loop itself (it revived and re-routed the parked item via `incomplete_page_group` + `refreshOpenWorkItem`) — see `bugs_open/220` for the loop defect that same experiment exposed, and the lane NOTES 2026-08-08b/c/d for the full trail. Dispatch queue gotcha for operators: `LoadWorkItemsAction` orders `priority ASC` — LOWER dispatches first.
- **status-evidence:** `bugs_open/206` found `entity-directory` explicitly listed in `load_work_item_actions.go`'s own `unavailableBuilders` map (named, never implemented) and the `directory-listing` component's `entries.source` pointing at `query.directory_entries`, a name `queryresolve.Resolve` has never registered — confirmed live: zero pages fleet-wide carry `directory-listing` in `sections`. The data half was already genuinely live (`directory_export_action.go`, unrelated, unchanged).
- **what:** Two Go pieces. (1) `queryresolve.resolveBusinessDirectory` (`query.business_directory`, no static arg — looks up the SITE'S OWN `directory-export-json` scheduled_tasks config by domain to find its vertical, then runs the identical filter `loadDirectoryEntries` uses against `business_intel.businesses`, so the SSR listing and the exported JSON archive can never disagree). (2) `EnsurePageSectionLayoutAction` (`ensure_page_section_layout`) — fills a page's plan with a default layout ONLY when it has none from any source (guards against `bugs_closed/001`'s re-plan risk structurally, not by caller discipline); layout comes from `defaultSectionsForPage`, extended with an `entity-directory` case and two NAME-keyed cases (`guides-index`/`tools-index`) that mirror an already-proven fleet pattern (verified live on 4 other sites). `directory-build-handler` (new agent, seed `326_directory_build_handler_agent.sql`) chains `ensure_page_section_layout` → delegates the actual build to the EXISTING generic `page-build-handler` — no new content-writing logic. `entity-directory` moved from `unavailableBuilders` to `availableBuilders` in `load_work_item_actions.go` pointing at this agent (this one edit landed early, swept into `cb7b4d759` [bugs_open/208] as an unrelated same-file passenger — functionally fine, forward-only, noted here for the paper trail).
- **sources:** `bugs_open/206`; `docs024_key_docs_latest/bugfix_206_directory_build_handler/PLAN_2026-08-06_directory_build_handler.md`; `queryresolve/business_directory.go`; `ensure_page_section_layout_action.go`; seeds `325_directory_listing_binds_to_business_directory_query.sql`, `326_directory_build_handler_agent.sql`
- **relations:** page-rebuild / rebuild_pages.sh (BLD-015/016, this DELEGATES to page-build-handler rather than page-rebuild — different mechanism, new-page-build not rebuild); `directory_export_action.go` (the data half, unchanged, this is its first real consumer)
- ~~**verify-later:** council verdict; image tag actually carrying this code, pod-grepped both replicas; whether `vetcomparison.uk`'s `directory-index`/`guides-index` re-triage (their `handler_agent`/`status` reset to `directory-build-handler`/`page-build-handler` + `triaged`) has been done and the deployed pages checked directly — see the PLAN doc's verification section for the exact remaining steps~~ **DISCHARGED 2026-08-08** — all done (see status line), with ONE correction to the plan it restated: guides-index on bare `page-build-handler` was WRONG (no layout-filling step in that workflow; live-refuted twice — the original 07-17 no-op and the improvement loop's 08-08 re-dispatch) — BOTH pages route to `directory-build-handler`, whose `ensure_layout` is page-name-generic. guides-index deploy verification was in flight at this update; see the lane HANDOFF 2026-08-08b if it needs re-checking.

### BLD-027 — `builderForPageType` — ONE authority for "which handler builds this page_type" (`builder_routing.go`)
- **⚠ UPDATE 2026-08-25 (night) — the swap is LIVE, and BLD-027 gained a provenance stamp.**
  - **DECLARED KEY (owner ruling 2026-08-11, nested-field additions): `spec.handler`.** Written by
    BOTH page-build producers — `reconcile_site_plan_action.go`'s `needs_page` emit and
    `WriteBuildItemsAction`'s per-page item — carrying the handler that producer CHOSE, so
    `spec->>'handler' = handler_agent` separates a mint from a hand repair. **Nothing reads it**,
    and that is measured rather than asserted (the council objected, rightly, that I had asserted
    it): `[MEASURED 2026-08-25]` **0** Go readers of `spec->>'handler'`/`spec.handler` outside the
    writers; **0** live `agent_definitions` naming `spec.handler`; and **0** rows in
    `site_work_items` ∪ `site_work_items_archive`, all history, all producers, carrying a `handler`
    spec key — so there is no pre-existing key to collide with. `resolveGuardedPage`
    (`owned_page_guard.go`) consumes `current_item.spec` for build items but reads
    `spec.id`/`spec.name` **by explicit path**, so an additive key is inert there; its comment
    claiming the spec is "exactly what queryPagesForBuild returned" was corrected in the same commit
    rather than left false. Caller surface of the second door is **one** workflow
    (`site-work-orchestrator :: write_build_items`; single registration at `registry.go:712`).
    Both doors take the value from **`route.handler`** — `load_work_item_actions.go:243` is
    `handlerAgent := route.handler` — so a spelling drift between them is not representable, which
    was the guardian's remaining concern.
  **Live:** chassis `v1.0.1339`, both replicas stamping `a7459a44b68b8c67b7d7bb0ca7c064e0729d59f5`;
  `git merge-base --is-ancestor efec862f4 a7459a44b` true, **with a control** (two commits made
  after the build are correctly NOT ancestors, so the test discriminates). So `section-index` →
  `directory-build-handler` is live at both doors. `[MEASURED]` nothing has re-routed as a result and
  nothing should have: 0 reconcile runs since the roll (nothing schedules it) and a parked row blocks
  its own re-mint — the roll changes the NEXT plan, it cannot reach back.
  **Added (`1887a116b`, council `9ff151d6`, verdict pending at the time of writing):** both producers
  now stamp the handler they CHOSE into the item spec (`spec.handler`), so
  `spec->>'handler' = handler_agent` proves the column still holds the emit's own decision.
  `handler_agent` is mutable and the documented operator repair re-points it — `[MEASURED 2026-08-25]`
  all three rows fleet-wide that would have passed `bugs_open/206`'s closure test were hand repairs —
  so the column alone can detect this bug's FAILURE but never confirm its FIX. The stamp is
  permanent, unlike the interim `updated_at` gate which expires on the first legitimate dispatcher
  claim. **Council `9ff151d6` APPROVED** (9 reviewers / 8 abstained) and **LIVE on `v1.0.1341`**
  as of 2026-08-26 — image label `2fb40a960`, ancestry-verified with a control, plus a known-value
  probe confirming the label describes the running binary.
  ⚠ ~~The stamp is NOT live (`v1.0.1339` predates it)~~ **— it is live now, and the WARNING STILL
  STANDS with a different cause:** `spec ? 'handler'` is FALSE on every row (`[MEASURED 2026-08-26]`
  **0 of 231** reconcile rows) because nothing schedules `reconcile_site_plan` and none has run since
  the roll. So a query gated on it **still returns a confident zero** — and that zero is
  *indistinguishable from a broken stamp* until a plan runs. Give the first stamped row both gates,
  agreeing, before calling it proof. Gate table: the 206 lane's
  `RUNBOOK_directory_build_handler.md` §7d.
- **⚠ UPDATE 2026-08-25 — THE SWAP IS DONE, so three of the warnings below have EXPIRED. Read this first.** `WriteBuildItemsAction` now calls `builderForPageType`; its inline maps are deleted; and `section-index` was added to the shared map in that same commit, which is exactly the condition council round 4 imposed. Commit `efec862f4`, council corr `b92e624d-15c7-4ef7-a2e5-4a7f41187b38` — **APPROVED at round 1, 13 reviewers, 4 abstained, no vetoes, every objection `low`.** So, concretely: **(1) `section-index` IS routed now** (`directory-build-handler`, `needs_directory`) — the correction line immediately below is history, not current state; **(2) there is NO third copy** — a session grepping `load_work_item_actions.go` for `availableBuilders` and finding nothing has confirmed the fix, not missed it; **(3) the two `section-index` pages named below as "parked until then" are no longer blocked by this**, though see the `unresolved` caveat in `bugs_open/206` §5(b). The 08-24 round is also long since APPROVED (round 6, corr `52dbd067`) and **LIVE on `v1.0.1334`**, binary-verified on both replicas — the "round 2 in flight / do not read as ratified" line below was true for a few hours on 08-24 and has misled every reader since.
  - **What was ADDED on 2026-08-25 beyond the swap:** the `capability_gap` row's `handler_agent` at the `WriteBuildItemsAction` door went from the needed-builder's name to **EMPTY**, matching the sibling door's round-2 ruling (a deferred row naming an agent that by construction does not exist is `bugs_closed/078`'s shape). `[MEASURED 2026-08-25, live UNION archive, all history]` that arm had minted **0 rows ever** — 0 of 50 `capability_gap` rows are `created_by='site-planner'` — so no existing row changed. Five tests were added; this door had **zero** direct coverage before, and the first version of them **passed under mutation** and was rewritten (`WRONG_CALLS.md` 2026-08-25).
  - **The "is there a THIRD routing copy elsewhere?" question — asked by three seats, and now ANSWERED with a census rather than left open.** `[MEASURED 2026-08-25]` six Go sites mint a `needs_page:<name>` item_key, not two; three of them hardcode `page-build-handler` without consulting this function (`rerender_page_sections_action.go:1424`, `apply_adoption_plan_action.go:731`, `discovery_checks/check_incomplete_page_group.go:202`). **But none is a latent 206**: across all 26 typed-page rows those producers have ever minted, **zero** carry `no sections ready to build` (their failures are the owned-page guard working as designed). The mechanism explains it — 206 is the **layout-less** case, and rerender/adoption targets already have a layout; the two doors that consult this map are the two that mint at **plan** time. Full working in `bugs_open/206`, 2026-08-25 council section.
- **⚠ CORRECTED 2026-08-24 (same day, council round 4) — `section-index` is NOT routed by this function.** The entry below described it as routed; that was true for about two hours. Four council seats objected across three rounds that pointing ONE producer at a new shared authority while the other keeps its inline copy is not consolidation, and round 4's `guardian` gave it a mechanism rather than a principle: both producers mint the same `item_key` (`needs_page:<name>`) and `idx_swi_dedup` is UNIQUE over non-terminal statuses, so **whichever fires first wins and the other row is silently dropped** — a page `WriteBuildItemsAction` reached first would keep the wrong handler and the fix would never fire for it, with no signal. `section-index` was the ONLY line on which the two maps differed, so it was removed and they are now byte-identical (both sort to the same 8 entries). **Add the entry in the SAME commit that makes `WriteBuildItemsAction` call this function — one line then moves both doors, which is the whole point.** Two tests fail if you add it early, and the producer test says so in its failure message. Cost, recorded so it is not rediscovered as a defect: `loanzy.uk/guides-index` and `garden-tools.uk/buying-guides-index` are `section-index` and stay parked until then.
- **status:** built + committed 2026-08-24 (`d1aa231aa`, narrowed in `200d54bdf`) by the `bugfix_206_directory_build_handler` lane. **INERT until the next fleet roll** — Go-only. Council corr `52dbd067-10ed-4a6e-84eb-3fbf47d099dd`: round 1 **REVISE** (gating objection upheld and correct — the submission cited a `site_plan_pages.page_type` column that does not exist; the *code* never depended on it), round 2 in flight at the time of this entry. **Do not read this entry as ratified**, and check the verdict before quoting it.
- **status-evidence:** [MEASURED 2026-08-24, deduplicated] 87 work items across 16 sites carry `page-build-handler no-op: no sections ready to build`, 79 still `needs_human_review`. Of those, 9 came from `reconcile_site_plan`, whose emit hardcoded `handler_agent='page-build-handler'` and never consulted the builder map BLD-017 updated on 2026-08-08 — so the 206 fix was live at one producer and absent at another for 15 days. The decisive row: `garden-tools.uk/brand-directory-index`, `page_type='entity-directory'`, parked since 08-23 **while `directory-build-handler` was live and working**. ⚠ **69 of the 87 are tool pages** (67 layout-less, 11 sites), 42 of them `unbuilt_internal_link` rows from `bugs_open/220` — a DIFFERENT cause wearing the same error string. This mechanism reaches ~11 non-tool typed items; it does not fix the tool class.
- **what:** `builderForPageType(pageType) (info, neededBuilder, known)` in a new `platform/orchestration/actions/builder_routing.go`. Three outcomes: a route (handler + itemType), a **capability gap** (a known type whose builder does not exist — the caller files a deferred `capability_gap` naming it and mints NO dispatch item), or the generic default for an unknown type. ~~Adds `section-index` → `directory-build-handler`~~ **— see the correction above: that entry is HELD until both producers call this function.** The route itself is proven at the artefact (vetcomparison `guides-index` is `page_type='section-index'`, sections `["hero","guide-list"]`, deployed); what is held is *encoding it in one map while a second map exists*. `entity-page`/`tool` stay unavailable **by decision, not oversight** (reasoning in the file header). `reconcile_site_plan` now calls it, reading `pages.page_type` with a fallback to `site_plan_pages.role` (same vocabulary — measured across every current-plan page). `defaultSectionsForPage`'s `guides-index`/`tools-index` name keys widened from equality to **suffix** match, so `buying-guides-index` gets the proven `[hero, guide-list]`.
- **why it is registered:** the routing decision previously existed as closure-local maps inside `WriteBuildItemsAction` **plus** a hardcoded string literal in `reconcile_site_plan` — two hand-maintained copies that disagreed, with **zero test coverage in either**. Another workstream adding a page type or a builder would have found one copy and not the other. It now has six tests, four of them mutation-proven (restore the hardcoded handler / delete the gap arm / reorder the ownership guard / drop the role fallback — each kills its own test).
- **⚠ landmine — there is still a THIRD copy, deliberately, and it is the planner's.** `WriteBuildItemsAction` (`load_work_item_actions.go`) keeps its inline maps and does **not** yet call this function, so the PLANNER path lacks the `section-index` entry while reconcile has it. Nothing regresses (that path keeps its pre-08-24 behaviour and no path disagrees about an existing type), but **the swap is owed**. It was not done because that file carries an ownerless uncommitted hunk (an 8-arg `applyWorkItemFailureLadder` call against HEAD's 7-arg signature) which makes a pathspec commit of the file break HEAD — verified by git-archive overlay, and separately measured to fail three existing `UpdateWorkItemStatus` tests. **Whoever finds that file clean: do the swap, delete the inline maps, and delete this landmine.**
- **⚠ open cross-lane question — the `capability_gap` arm may be re-litigating a settled decision.** `bugs_closed/187` measured this same population on 2026-08-03 and its shipped fix states *"`reconcile_site_plan` deliberately NOT guarded (015-shape gaps are real findings)"*. The 206 lane's reading is that filing a visible deferred gap serves that intent better than a dispatch item that burns an attempt and parks under an error blaming missing data for a missing builder — but that is the 187 lane's call. Notice recorded in 187's own file, question put to the council, **revert of that arm offered**. The ROUTING half is untouched by 187's decision.
- **sources:** `bugs_open/206` (RE-VERIFICATION 2026-08-24); `docs024_key_docs_latest/bugfix_206_directory_build_handler/` (PLAN addendum 2026-08-24, NOTES, README_where_we_are); `platform/orchestration/actions/builder_routing.go` + `builder_routing_test.go` + `reconcile_site_plan_routing_test.go`; `WRONG_CALLS.md` 2026-08-24 (four entries from this lane, incl. the false-zero census that found it)
- **relations:** **BLD-017** (`directory-build-handler` — this is what routes work TO it, and the reason its 08-08 win was not fleet-wide); `bugs_closed/187` (same population, opposite decision on one arm — see above); `bugs_open/220` (owns 42 of the 87 items, tool pages, separate cause); `bugs_closed/015` (mistyped `page_type` orphans a page from its machinery — the failure mode a single typed authority makes cheaper to reason about)

### BLD-018 — `PageDescriptor.FlatURLs` — flat URL shape for tool/guide/game (opt-in seam)
- **status:** ~~built 2026-08-10 … INERT: no caller sets the field yet … Plumbing designed but NOT written~~ → **PLUMBED 2026-08-11, awaiting roll + per-site seed.** Round-1 council verdict on the field: **APPROVED** (corr `6fdb9ce6-…`, 2 advisory objections, none high). The plumbing: new shared reader `siteUsesFlatURLs` (`platform/orchestration/actions/site_url_shape.go`) reads `url_shape` from the site's `structure` spec aspect; BOTH canonicalisation surfaces (`WriteSitePlanAction`, `SyncPagesToDBAction`) thread it into their `PageDescriptor`, and `TestFlatURLFlagReachesBothCanonicalisationSurfaces` pins that neither surface can drop it alone. Council round 2 (the plumbing) submitted 2026-08-11, corr `70256656-4ada-465e-b959-096ae7225eb9` — verdict pending at the time of this entry. **Commit provenance is unusual, recorded here so the trail joins up:** the plumbing reached HEAD in `7a066dba1`, the `bugs_open/215` lane's commit, as a DECLARED same-file passenger (both lanes edited the same two surfaces; their message states the sweep and why). No `Council-Submitted:` trailer exists on that commit — the correlation above and `bugs_open/241` are the join. Still nothing sets `url_shape` automatically: it is seeded per site (loancalculator.co.uk is the first, dir-flat shape MEASURED before seeding). Auto-stamping `url_shape:"flat"` at verbatim adoption is a named follow-up candidate, not done — a verbatim crawl's URL shape is not necessarily dir-flat; measure before stamping.
- **UPDATE 2026-08-11 (evening):** round-1 code **LIVE on `v1.0.1288`** without a roll of this lane's (ancestry via the 215 lane's artefact-verified build; literal probe on both replicas with near-miss controls). Seed applied on loancalculator.co.uk, verified. Round 2 → **REVISE** (gating: adoption's supersede+INSERT drops structure-spec opt-in keys on re-adoption — the LANDMINES 2026-08-11 entry). Round 3 committed `19acfc895` (same trail corr `70256656`): `carryForwardStructureSpecKeys` fixes the drop for ALL unknown keys (PLAN-048's gates included); flag wired into apply_gap_plan / create_tool_component / deploy_tool's new-tool arm; contract test now pins per-file exact descriptor counts. Round-3 code inert until the next roll. **Open question, deliberately parked:** `siteUsesFlatURLs` + `siteIdentityPolicyFor` are two typed readers of one row; the reuse seat wants the row-read consolidated — the 215 lane owns `site_identity_policy.go`, so consolidation is theirs to absorb when they next touch it (they are notified).
- **status-evidence:** [MEASURED 2026-08-10] `CanonicalisePage` (`platform/orchestration/datahelpers/page_canonical.go:152-173` pre-change) returns `/tools/<slug>/index.html` for role=tool with NO input producing `/tools/<slug>.html`; loancalculator.co.uk serves 11 tools + 13 guides flat, so a planner run would move 24 of its 26 live URLs (`upsertPage` sets `url = EXCLUDED.url` unconditionally, `site_db_actions.go:1142`; the deployer takes the file path from `pages.url`). blog-post/entity-page already emit flat — the vocabulary existed, the three nested roles just couldn't reach it.
- **what:** One bool field on the shared `PageDescriptor` input, default false, changing ONLY the URL arm of tool/guide/game to `/<dir>/<slug>.html` via a tiny `nestedOrFlatURL` helper. Name and page_type unchanged. Ships as an opt-in field with the unsafe default OFF per owner ruling 2026-08-02 §2 (a comment is not a control). Twelve call sites, all unchanged in behaviour — the pre-existing test corpus doubles as the proof, since the zero value is false.
- **landmine (why this is registered before it is driven):** the two canonicalisation surfaces — `write_site_plan_action.go:392` and `SyncPagesToDBAction` (`site_db_actions.go:281`) — MUST read the same site-level flag or they diverge exactly the way the comment at `site_db_actions.go:245-254` records them having diverged before (flat pages row vs nested plan row, plan regressed). Whoever plumbs this: one flag, read once, passed to both.
- **open review question:** where the flag lives. Recommendation on record (bugs_open/241): a `url_shape: "flat"` key in the site's `structure` spec aspect, read at both call sites; NOT a per-page field, because mixed shapes on one site is exactly the incoherence the canonicaliser exists to prevent.
- **sources:** `bugs_open/241_HANDOFF_2026-08-10_planner_moves_every_flat_url.md`; `page_canonical.go` + `page_canonical_test.go` (five FlatURLs cases); measurement in `docs024_key_docs_latest/loancalculator_couk/HANDOFF_2026-08-10_framework_rebuild_continue_here.md`
- **relations:** sync_pages canonicalisation convergence (the doc-029/030 lineage this extends); BLD-015/016 (page-rebuild bypasses the planner and so never hits this — the trap is planner-path only)

### BLD-019 — Build provenance: the commit sha stamped into every backend binary (`pkg/buildinfo`) and image (OCI `revision` label)
- **status:** ~~built 2026-08-10, committed same day — **INERT until the fleet rolls**~~ → **LIVE and READ BACK 2026-08-10.** Chassis `v1.0.1283` carries the stamp: `strings /app/agent-chassis | grep -oE '^[0-9a-f]{40}(-tree)?$'` returns **`d3c09cc746e563b6339831cfb69576eb52135c43` on BOTH replicas**, with no `-tree` suffix (so a clean committed build, exactly as designed), and `buildinfo.GitCommit` present 4×. **It works, and the first thing it did was retire a guess:** the concept register's staleness survey had ~20 entries claiming "inert until the next roll" that could only be settled by hunting a per-fix marker — the failure `bugs_open/153` was filed about. With the stamp, `git merge-base --is-ancestor <entry's commit> d3c09cc74` settles each one exactly, and **all cited commits came back IN**. ⚠ **The stamp only settles an entry that NAMES a commit** — 13 of 29 surveyed entries cite none, so provenance can date them only by inference. **Council round 1 returned REJECTED (hard veto from `guardian`, on SCOPE not soundness — corr `44fa6a98-acaa-46b5-9ada-f0c34ca5475d`).** The guardian stated the mechanism *"is sound and well-evidenced — that part I'd approve on a single-service pilot"*; its objection is that one round bundled the shared makefile macro with all 14 Dockerfile and all 14 main.go edits. `bug_historian` and `reuse_agent` both APPROVED, so the seats disagreed — which under the owner ruling of 2026-07-28 makes the next step a **human decision, not a resubmission**. **Do not treat this entry as ratified.** The three costed options are in `bugs_open/153`'s status block. `editquality` separately found a real defect (a pipeline ending in `head -1` exits 0, so the unstamped-binary fallback could never fire) — **fixed and behaviourally proven in `8d270c68a`**. ~~The makefile half is live… no roll has happened yet… the submission died unjudged…~~ **CORRECTED 2026-08-10 evening by the `bugfix_153_build_provenance` lane — that tail was written before the roll and before the verdict, and contradicted the head of this same line.** The roll HAS happened (`v1.0.1283`) and the fleet is **not** mixed: **all 14 backend services** are stamped with `d3c09cc74…`, verified at the running binary on each — agent-chassis (both replicas), auth-service, core-manager, reasoning-agent, web-search-adapter, web-scrape-adapter, git-adapter, image-generator-adapter, thunder-adapter, analyser-adapter, browser-runner-adapter, content-creator-agent, remote-job-spawner, kafka-scheduler. The submission did NOT stay unjudged: it was resubmitted after the owner restored API credit and returned **REJECTED**, then **the owner ruled the code stands as committed** (option 1). Live and proven ≠ approved — **no `Council-Reviewed:` trailer exists or may be added.**
- **⚠ the verification recipe in this entry is unsafe as written.** `strings /app/<svc>` works on the 13 alpine services and **silently returns 0 on `browser-runner-adapter`**, the fleet's only debian-slim image, which ships no binutils — behind the customary `2>/dev/null` that is indistinguishable from "not stamped", and it briefly convinced this lane the mechanism had caught a stale image. Use `grep -aq "<expected-sha>" /proc/1/exe` (no binutils, any image base, any binary path), and **never a discovery grep** — without `strings`' line boundaries, `grep -aoE "[0-9a-f]{40}"` returns Go's internal digit table identically on every service. See `LANDMINES.md`.
- **status-evidence:** [MEASURED 2026-08-10, live pods `agent-chassis-8496665bb8-{f6svp,sskxd}` on `v1.0.1279`] pre-change, `strings /app/agent-chassis | grep -cE 'v1\.0\.1[0-9]{3}'` → **0** and a 40-hex sha grep → **0**; `grep -c ldflags` over the makefile and every `build/docker/backend/*.dockerfile` → **0**. Mechanism proven before commit against a clean `git archive HEAD` **with a negative control**: building `cmd/agent-chassis` WITH the ldflags clause puts **3** copies of the injected sha in the binary, the same source WITHOUT it yields **0** for that sha and **1** for the literal `unknown`. Repeated on the two bare-`main.go` file-builds (`git-adapter`, `remote-job-spawner`) and `cmd/scheduler`: 3 each.
- **what:** `ref_build` already computed the commit at `makefile:119` purely to echo it, then discarded it. It now passes that sha as `--build-arg GIT_COMMIT` plus OCI `image.revision`/`image.created` labels; all 14 backend dockerfiles take the ARG and stamp it through `-ldflags` into `pkg/buildinfo.GitCommit`; all 14 mains log it once at startup as `build provenance`/`git_commit`. `verify-agent-images` gains three read-only fail-soft stanzas (local image label, the sha actually in the running pod's binary, per-pod imageID+startTime). **Full 40-hex, not `--short`** — 40-hex is a measured-zero pattern in the binary, so extraction is unambiguous. Answers `bugs_open/153` candidates 1 and 4.
- **why it matters beyond its own bug:** it retires **per-fix marker hunting**, which is the fleet's universal "did my fix ship?" method and is itself defective — `bugs_open/153`'s CONTRIBUTION section records three false readings in one day across two sessions, from markers that were variously a Go comment, prose from a workstream README, and a deleted string that survived as a *prefix* of its replacement. `strings <binary> | grep <known sha>` has none of those failure modes.
- **landmine 1 — `-X` against an unlinked package is SILENTLY IGNORED.** The one-line `main.go` import+log per service is therefore load-bearing, not decoration: it is what forces `pkg/buildinfo` into the binary. A service whose main.go edit is dropped in some future refactor will build green, ship, and stamp nothing, and the only symptom is a grep returning 0 — which reads identically to "the image is stale", i.e. exactly the condition this mechanism exists to distinguish. If you add a 15th backend service, it needs BOTH halves.
- **landmine 2 — a `-tree` build is deliberately NOT given a clean sha.** `tree_build` stamps `<shortsha>-tree` **unconditionally**, including when `git status` is clean, because "porcelain was empty at build start" is a race against the other sessions sharing this tree, not a guarantee. So the marker records *how it was built*, not *whether the tree was dirty*, and a `-tree` suffix can never satisfy a 40-hex grep. Do not "improve" this into a conditional.
- **landmine 3 — the labels are applied at the docker CLI, not in the dockerfiles**, so every image built through these two macros carries provenance even where a dockerfile edit was missed — including `component-render-check` and `shared-output-fields-check`, which call `ref_build` but got no dockerfile edit and will warn harmlessly that `GIT_COMMIT` went unconsumed. Their *binaries* remain unstamped; their *images* are labelled. Do not read a labelled image as a stamped binary. **THIRD INSTANCE 2026-08-11: `removed-config-keys-check`** (added by another lane in `a9237f0c9`; builds `./cmd/config-key-audit`). [MEASURED] its dockerfile holds **0** `GIT_COMMIT` and **0** `ldflags`, against a positive control of **2** in `agent-chassis.dockerfile`. **This landmine is now predictive, not hypothetical — a new service reaches `ref_build` and stops there, because the label half is free and the binary half is opt-in.** Left unfixed on purpose: that lane is mid-council-round on the file.
- **deliberately NOT built, and named so silence is not mistaken for oversight:** `bugs_open/153` candidate 2 (make the tag imply the build — sha-suffixed tags, or refuse `push-*` on a label mismatch) and candidate 3 (gate push on a `.build/<service>.<tag>` stamp file). Both change the push/deploy contract fleet-wide and want explicit owner sign-off; **this entry is their prerequisite**, since the label a refusal would compare against did not exist until now. Also untouched on purpose: the `IMAGE_TAG`-bump-per-build requirement, because a same-tag rebuild ships the node's stale cached binary — a separate live trap.
- **how to verify once it is live** (the induced-fault test the bug file specifies): bump `IMAGE_TAG`, run push+deploy **without** build — the pod must still report the *previous* sha while wearing the new tag, i.e. the retag becomes visible. This mechanism **detects; it does not refuse.** Positive control: a full build+push+deploy cycle must make the pod report exactly `git rev-parse HEAD` of the ref built. Regression guard: `make build-<svc> REF=<older-commit>` must stamp *that* commit, not HEAD.
- **UPDATE 2026-08-11 — verified on a second roll, and the regression guard is now RUN rather than proposed.** (a) `v1.0.1284`, a release this lane did not run: **14/14 services** print `build provenance` at startup, so the stamp is the platform's normal behaviour and not an artefact of the roll it shipped in. (b) **Regression guard executed** [MEASURED 2026-08-11]: `make build-agent-chassis REF=d3c09cc74… IMAGE_TAG=scratch-153-guard` (scratch tag, never pushed, image removed) → OCI label = `d3c09cc74…`, the older sha; binary contains `d3c09cc74…`, does **not** contain live HEAD `6235beb44…` (a real but *different* commit — the discriminating control), does not contain a fabricated sha, and the positive control `orchestration` is present. So `REF=` genuinely reaches the linker, which is also the mechanism **BLD-020** relies on. (c) **The production induced-fault test is NOT owed any more** — owner decision 2026-08-11, after `v1.0.1284` produced a real, un-induced divergence (`bugs_open/249`) that demonstrated the same detection without lying to production. The local guard was chosen as the substitute; recording the decision so a later reader does not re-file it as an outstanding proof. (d) **The "did my fix ship?" claim is per SERVICE** until BLD-020 has a release under it.
- **sources:** `bugs_open/153_HANDOFF_2026-07-29_image_tag_does_not_imply_a_rebuild.md`; `docs024_key_docs_latest/bugfix_153_build_provenance/` (PLAN, NOTES, RUNBOOK, submission JSON); `pkg/buildinfo/buildinfo.go`; `makefile` (`ref_build`, `tree_build`, `verify-agent-images`)
- **relations:** BLD-005 (`image_tag 'latest'` stale-default trap — the same family, one layer down: a tag that does not imply the code); the fleet-wide pod-grep verification practice in CLAUDE.md § "Building & deploying images", which this is intended to replace; `bugs_open/243` (why its council round is unjudged)

### BLD-020 — `release` / `release-backend` pin ONE commit for the whole sweep (`pinned_sweep`), so a release is one revision rather than one tag
- **status:** built 2026-08-11 by the `bugfix_153_build_provenance` lane, committed same day. **PROVEN 2026-08-11 night, on the second release under it (`v1.0.1287`).** `v1.0.1286` ran first and came out coherent — one revision `c3b424c8e` across 14/14 — but [MEASURED] zero commits landed inside its 5m52s build window, so the old unpinned code would have produced the identical result; that check could not have come out otherwise and was recorded as coherent, not proof. `v1.0.1287` is the discriminating case: build window `14:17:39Z`–`14:24:18Z` (6m39s), and commit `d80fbf4bf` landed at `14:17:44Z` — 5 seconds after the window opened, squarely inside it, from another session. Under the pre-fix behaviour every service built after that instant (13 of 14) would have resolved `HEAD` independently and picked it up, producing ≥2 revisions exactly as `v1.0.1284` did. Instead **all 14 images carry one revision, `9b7811d4b`** — the commit that was `HEAD` 24s *before* the window opened, i.e. what `pinned_sweep` resolved once, at the start. Confirmed at both instruments: image labels (14/14) and the pods (13/14 by the `build provenance` startup log line; `agent-chassis` by the binary probe with its three controls, both replicas — its log line has scrolled past `--tail=300` again, as expected). Graded with RUNBOOK **R9b(ii)**: this is the "commit inside the window, one revision out" case it was written to wait for. `bugs_open/249` closed same commit.
- **what:** `release` and `release-backend` were prerequisite lists (`release: build-backend push-backend deploy-core …`). They are now recipes calling `$(call pinned_sweep,<goals>)`, which resolves `$(REF)` to a single commit **once**, echoes it, and then runs each goal in order as `$(MAKE) <goal> REF=$$PINNED`. An explicit `REF=<sha>` is resolved the same way, so a deliberate older-ref release is unaffected; a `REF` that is not a commit now fails the release up front instead of at the first service.
- **why:** `ref_build` resolves `$(REF)` inside **every** service's recipe (`makefile:128`), so with the default `REF=HEAD` each of the 14 builds asked git what HEAD was at the moment it started. A release takes ~6 minutes and ~40 sessions commit to this one tree. `bugs_open/249`, measured 2026-08-11: `v1.0.1284` shipped **three** revisions under one tag (5 services at `55fc8fc35`, 1 at `e2afedaaf`, 8 at `a41dec8e5`), the cut points matching two other sessions' commit times to the second. **This is not a rare race:** [MEASURED 2026-08-11] the busiest 7-minute window that day held **13** commits, and 23 landed in the 18 minutes while this fix was being written. The prior state was not "usually one revision" — it was "one revision only if nobody committed for six minutes".
- **the guarantee, stated narrowly:** one revision across the **14 backend images** (everything built through `ref_build`). `release-dashboard` builds from `frontends/admin-dashboard/` in the working tree, with no ref and no stamp, exactly as before. Frontends are outside BLD-019 and outside this guarantee — do not read the echoed sha as covering them.
- **how to verify** (needs a release run under it; the check is read-only and takes seconds): every image at the tag must carry one revision — `for S in <the 14>; do docker image inspect docker.io/aqls/$S:$IMAGE_TAG --format '{{index .Config.Labels "org.opencontainers.image.revision"}}'; done | sort -u` → **exactly one line**. At the cluster, per service: `kubectl -n ai-persona-system logs -l app=<service> --tail=300 | grep -m1 'build provenance'`. **The disconfirming result exists and has been seen** — that same command over `v1.0.1284` returns three lines, which is what filed `bugs_open/249`.
- **landmine — `make -n release` no longer previews the docker commands.** The goals are reached through a shell loop, so `-n` prints the sweep itself and stops. Use `make -n build-backend` / `make -n build-<service>` for the real preview. This is deliberate: the `+` recipe prefix would restore the old preview by executing the sub-makes under `-n` and trusting MAKEFLAGS to carry `-n` down — on an estate where releases are driven by hand, a preview command that performs a real release if that assumption ever fails is not a trade worth taking.
- **deliberately NOT built:** the companion assertion (fail or warn when the 14 images at `IMAGE_TAG` disagree on revision). The pin prevents the known cause; the assertion would catch the unknown ones (a re-push at an existing tag, a single-service rebuild between releases). Offered and not taken 2026-08-11 — **named here so its absence reads as a decision, not an oversight.**
- **sources:** `bugs_open/249_HANDOFF_2026-08-11_one_release_tag_ships_three_source_revisions.md`; `makefile` (`pinned_sweep`, `release`, `release-backend`, and `ref_build` at :111/:128 for the cause); `docs024_key_docs_latest/bugfix_153_build_provenance/` (NOTES + RUNBOOK R9)
- **relations:** **BLD-019** (the provenance stamp — this bug was invisible before it, and BLD-019's own "did my fix ship?" claim is what this makes true fleet-wide instead of per-service); `bugs_open/153`; `bugs_open/237` (`render-audit-adapter` is in no release path at all — the adjacent failure of the same assumption that "the release covers the fleet"); LANDMINES entry *"One `make release` is NOT one source revision"*.

### BLD-022 — `check-release-coverage`: the release refuses to run when an overlay on disk is in no release path
- **status:** built and committed 2026-08-17 by the `bugs_open/237` class-fix lane (`6b3524201`, `f0657b466`). ~~ARMED AND MUTATION-PROVEN, not yet exercised by a real release~~ → **EXERCISED 2026-08-17 by `v1.0.1307`, without incident.** All four of the lane's commits are ancestors of the deployed build `a6d1c53c0` (BLD-019 provenance, one revision across 5 services checked, so BLD-020 held too); 15 services retagged by the single loop; and the one ordering fact the refactor had to preserve held — `browser-runner-adapter` started 17:05:42Z and `render-audit-adapter` 17:05:44Z, on the same image. **Read that precisely: the tree was compliant, so the gate COULD ONLY HAVE PASSED — this release proves the refactor did not break a real release, NOT that the gate discriminates.** Its discriminating power rests on the mutation controls below, which is where it should rest. So: exercised, not proven. **ARMED AND MUTATION-PROVEN** — it is a make-level gate, so it takes effect the next time anyone runs `deploy-core` or `make release`; no roll is needed for it to bite, and no image carries it. Not council-gated: makefile-only touches none of `platform/`/`internal/`/`pkg/`, and the trigger refused it client-side (drawn live, no credits, no `FORCE`) — the same scope precedent `bugs_open/249` recorded.
- **what:** a `.PHONY` target that enumerates the **filesystem** — every `deployments/kustomize/services/*/overlays/$(OVERLAY_PATH)/kustomization.yaml` — reads the image each overlay pins, and fails the build, naming the service and both remedies, when that image is one the release builds (`RELEASE_IMAGES`) but the service appears in neither `AGENT_DEPLOY_SERVICES` (what `deploy-agents` retags) nor `RETAG_EXEMPT` (services retagged by their own target, each named with the target that does it). Wired as a prerequisite of `update-kustomization-images`, which `deploy-core` calls, which is in the release sweep.
- **why:** the release's service lists were **four separately hand-maintained enumerations** plus a fallback that guessed a service's image from its service name. A service present on disk but absent from a list was silently never rolled — no error, no warning, and invisible to the normal proof, because pod-grepping the image's *owner* reads live. `render-audit-adapter` sat on `v1.0.1194` while the fleet ran `v1.0.1280` — **86 tags, frozen since the pod was created** — carrying a live plaintext-credential leak (`bugs_open/233`) the whole time. The 2026-08-10 fix added the missing wiring; this closes the class that produced it.
- **the companion change, which is half the value:** the four lists are now loops over **one declaration**. `AGENT_DEPLOY_SERVICES` entries are `<service>[:<image>]`, so a service running another service's binary declares that fact **visibly, in one place** — `render-audit-adapter:browser-runner-adapter`, `vet-intel:agent-chassis`, `business-intel:agent-chassis`. The old code special-cased only the first of those three; the other two were latent. `deploy-%`'s registry pre-flight now reads the image out of the overlay instead of assuming `$(REGISTRY)/$*`, which is what let the bespoke `deploy-render-audit-adapter` rule be **deleted** rather than duplicated for the next such service.
- **how to verify — and the instrument must be shown able to fail:** `make check-release-coverage` → green. Then break it two ways and require red: (a) `make check-release-coverage AGENT_DEPLOY_SERVICES="agent-chassis"` must name `render-audit-adapter` — this **reproduces the original bug state** and is the discriminating control; (b) an overlay pinning a release-built image, dropped into `services/`, must be named. Both were run 2026-08-17 [MEASURED] and both failed as required, against a green current tree.
- **what it deliberately does NOT catch:** (i) a service in the list with **no overlay** at `$(OVERLAY_PATH)` — `deploy-agents` warns and skips (a hard error would break every non-production deploy, since only 9 services have a development overlay); (ii) ~~services on their own image lineage — `github-actions-runner` (`v1.0.948`) and `-vmsites` (`v1.0.1126`) pin images the release does not build, so the gate correctly ignores them and **their drift remains an open owner call**~~ → **CLOSED 2026-08-18 (owner ruling, `b1480f008`): all six own-lineage services — the two runners and the four daily check CronJobs — were folded INTO the release, so they now pin release-built images and the gate polices them like anything else.** Note what that says about the mechanism: the blind spot was never fixable by tightening the gate, because its predicate is "pins an image the release **builds**"; the fix was to move the services to the other side of it. The residual gap is the general form — a service whose image the release does not build is still invisible here, by design — and the door-closer for *that* is the pending content-change trigger (fail when a pinned tag predates the last commit to its own sources), **filed 2026-08-19 as `bugs_closed/318`, which also carries the (iv) hazard below**. **⚠ SUPERSEDED 2026-08-22 — the gap is CLOSED, and NOT by the content-change trigger.** See **BLD-026**: the predicate moved to `pkg/releaseset` and its admission test was INVERTED, so an overlay pinning one of our images that no release builds is now the violation rather than the skip, escapable only through an explicit `OWN_LINEAGE` entry. The content-change trigger as the owner worded it on 2026-08-18 turned out to be **uncomputable on this tree** — it dates a service's pin from the overlay's git history, and `deploy-agents` seds `newTag` in place with nobody committing it (26 overlays dirty 2026-08-22; `agent-chassis` committed at `v1.0.1239` against `v1.0.1323` on disk and in the cluster). ~~Re-aiming it at the artefact (BLD-019's stamp, BLD-023's `git_commit`) is an OWNER decision and is open.~~ **RULED 2026-08-22: the owner SKIPPED the staleness build entirely — neither the 08-18 wording nor the artefact-anchored re-aim is to be built — and took an accumulation budget on the exemption list instead (BLD-026).** CLOSED, not deferred: after the BLD-026 inversion its population is `OWN_LINEAGE`, which holds **one entry as of 2026-08-22**, and a mechanism with one possible subject is one nobody exercises. Do not re-file it as an outstanding gap. **Proven live 2026-08-19**: all six moved on `v1.0.1314`, both runners serving the SAME image, registry census 170/170 — so the `<service>:<image>` form is now demonstrated in production, not just in the declaration; (iii) whether the tag it retags to was ever actually **built** — that is BLD-019/BLD-020's territory; **(iv) ⚠ NEW 2026-08-18 — the REVERSE direction, and it now has teeth.** The gate fails a service that pins a release-built image and is in no release path. It does **not** fail a service that is in a release path but whose image stopped being built. Since `deploy-agents` now retags both runner overlays to `IMAGE_TAG`, removing `github-actions-runner` from `RELEASE_IMAGES` (or dropping `build-github-runner` from `build-backend`) while leaving the runners in `AGENT_DEPLOY_SERVICES` would point both at an image nobody pushed and `ImagePullBackOff` **both runners together**, taking CI with them. ~~The invariant that prevents it — `build-backend` builds exactly `RELEASE_IMAGES`, neither side over — was verified by set equality 2026-08-18 [MEASURED] and is policed by **nothing**.~~ **⚠ CORRECTED 2026-08-22 (`bugs_closed/318` lane), and the correction is that the invariant was FALSE FOUR DAYS LATER.** Measured that morning: **25 declared, 22 built** — `optional-explicit-wires-check`, `commit-sha-exposure-check` and `capped-schedule-ordering-check` were each added to `RELEASE_IMAGES` *and* `AGENT_DEPLOY_SERVICES` by two different lanes and to none of the build groups. Since `push-backend` loops `RELEASE_IMAGES` with `docker push … || exit 1`, and that goal precedes `deploy-core` in `pinned_sweep`, the next `make release` would have built 22 images (~6 min) and then died on the first push of an image nobody built — **deploying nothing at all**. Confirmed at the artefact, not read off the makefile: `optional-explicit-wires-check` existed locally only at `v1.0.1321` against an `IMAGE_TAG` of `v1.0.1324`, and `capped-schedule-ordering-check` had **never been built at any tag**, while the controls `agent-chassis` and `verifier-remit-check` each carried an image at v1.0.1320–1323. **FIXED by DERIVATION, not by adding three names** (`95757b6c2`): `build-backend: $(addprefix build-,$(RELEASE_IMAGES))`, so the second enumeration is gone and the set difference is 25/25 **by identity**. Do not 're-verify' it with a set difference — post-fix that could not come out otherwise; the discriminating evidence is the copy-mutation (`No rule to make target 'build-nobody-built-this-check'`, exit 2, against exit 0 on the live file). **The RETAG half of this paragraph is still real and is closed separately by BLD-026's `InvalidDeployEntries`** — deleting a name from `RELEASE_IMAGES` now stops it being built *and* pushed consistently, but the deploy loop would still retag the orphan.
- **an adjacent defect found while proving this, filed not fixed:** explicit `deploy-vet-intel`, `deploy-business-intel`, `deploy-remote-job-spawner` and `deploy-kafka-scheduler` rules **shadow the `deploy-%` pattern rule** and carry no registry pre-flight at all, so a single-service deploy of those four can still point the cluster at a never-pushed tag. Same "two hand-maintained things" family, different path (single-service deploy, not the release sweep) — recorded in `bugs_open/237` rather than swept into this change.
- **sources:** `makefile` (`RELEASE_IMAGES`, `AGENT_DEPLOY_SERVICES`, `RETAG_EXEMPT`, `check-release-coverage`, `deploy-agents`, `update-kustomization-images`, `redeploy-agents`, `push-backend`, `deploy-%`); `bugs_open/237_HANDOFF_2026-08-09_render_audit_adapter_is_in_no_release_path_so_it_has_never_been_rolled.md`; `docs024_key_docs_latest/bugfix_237_release_enumeration/HANDOFF_2026-08-17_continue_here.md` (the lane's cold-start: what shipped, the two open owner decisions, and the verification traps — read it before re-verifying anything here)
- **relations:** **BLD-020** (`pinned_sweep` — same family, opposite half: 020 makes a release ONE revision, 022 makes it reach EVERY service; and 020's own entry already warned that `make -n release` stops previewing, which is the general form of the false-negative that `LANDMINES.md` now records specifically for release-content checks); **BLD-019** (the provenance stamp — how you verify a service actually moved); `bugs_open/233` (the credential leak this drift kept alive); `bugs_open/249`; LANDMINES *"`make -n release` descends into NOTHING…"* and *"a service sharing another's image is invisible to a hand-listed release"*; `WRONG_CALLS.md` 2026-08-17.

### BLD-021 — `node-config` DaemonSet: the tenant-writable path for node-level configuration (kubelet flags, and later journald) on a provider-locked control plane
- **status:** ~~built and committed 2026-08-14 — INERT until deployed~~ → **LIVE AND PROVEN 2026-08-15, effect measured.** The owner's release deployed it via `deploy-agents`: 5/5 DS pods running, and **every kubelet reads `70/60/168h` from its own configz** (the consuming instrument, not the DS logs). Effect within 16h: four nodes reclaimed to the 60% low mark — headroom above the eviction line went **2.4–4.3 GB → 6.7–8.8 GB**, versus troughs of 0.6–0.8 GB before the lane's fixes. The fifth (…1336) is composition-bound at 80% used (in-use images + journal + OS; GC cannot reclaim in-use images) — a floor, not a failure, and the sharpened case for the journald payload. Kubelet restarts were non-disruptive as designed. Not council-gated: `deployments/` + makefile are outside `platform/`/`internal/`/`pkg/`. First payload: kubelet image-GC `85/80/0s → 70/60/168h` (252's candidates 3a+3b; **252 closed 2026-08-15 on this proof**). **Also in the fresh-install path** (owner directive 2026-08-14: the framework is installable via terraform+kustomize): terraform step `095-node-config` wraps the same overlay via the `kustomize-apply` module (`deployment_name` empty on purpose — a DaemonSet must not enter the module's Deployment set-image/rollout block) and is called by all three `deploy-infrastructure*` sequences after `090-monitoring`. Day-to-day path and install path apply the same manifest and are idempotent in either order.
- **what:** a privileged, `hostPID` DaemonSet (`deployments/kustomize/services/node-config/`) that idempotently edits `/var/lib/kubelet/config.yaml` via `chroot /proc/1/root`, restarts kubelet only when the file actually changed, verifies after the edit, and restores its own backup and exits 1 (visible CrashLoop) on any mismatch. Makefile targets: `deploy-node-config` (apply), `node-config-status` (proves at the KUBELET via `/proxy/configz`, not at the DS's own logs).
- **why it exists — the finding that forced it [MEASURED 2026-08-14]:** the kube-system `kubelet-config` ConfigMap is **provider-protected** on this hosted control plane (Rackspace ngpc): writes return 200 "patched" and are reverted before the next read — three write shapes tested (data merge patch, new data key, annotation), all accepted, all gone, resourceVersion bumping each time, zero mutating webhooks on configmaps to explain it. So kubelet settings are tenant-reachable ONLY at the node file, and on SPOT nodes (replaced without notice) the only durable carrier for a node-file edit is a DaemonSet that reapplies it on every (re)placement.
- **the bar it clears for other workstreams:** any future node-level setting — journald caps (252's candidate 5, deliberately not yet included: it trades ~70 days of node-log retention for ~10 and awaits the owner), sysctls, containerd config — belongs as a block in THIS DaemonSet's script, not as a new hand-run debug-pod procedure. A hand edit on a spot node is a time bomb with the node's own lifetime as the fuse.
- **landmine — `/proc/1/root`, not a hostPath bind of `/`.** A hostPath mount of `/` shows an empty `/run` (tmpfs submounts don't propagate by default), so `systemctl` inside a `chroot /host` fails; `chroot /proc/1/root` traverses into PID 1's real mount namespace and everything works. Verified 2026-08-14 on …1148 (`systemctl is-active kubelet` → `active`). Also: every command inside the chroot is the HOST's GNU userland, not busybox — which is why `sed -i.bak-252` is safe there.
- **sources:** `deployments/kustomize/services/node-config/base/daemonset.yaml` (full mechanism + failure direction in its header comment); `makefile` (`deploy-node-config`, `node-config-status`); `bugs_closed/252_HANDOFF_2026-08-11_disk_is_invisible_…md` (**moved to bugs_closed 2026-08-15** — the 08-14 block holds the protection measurement, the closing block the live-effect proof)
- **relations:** BLD-019/BLD-020 (same family: making "what is actually running/configured" a query); `bugs_open/252` (owner of the first payload); LANDMINES entries *"a 200 'patched' on kube-system/kubelet-config is silently reverted"* and the node-disk sawtooth entry (what the first payload fixes).

### BLD-023 — `service_binary_capabilities`: a running binary publishes what it CAN DO, so "is the new code live?" stops being a `kubectl exec` question
- **status:** ~~committed 2026-08-20, table APPLIED (migration 503), **writer INERT until the next chassis roll**~~ **CORRECTED SAME DAY — it is LIVE and it had a DEFECT, now fixed.** Live on chassis **v1.0.1319** (capability-probed: `buildcapability`, `service_binary_capabilities`, `recordBuildCapabilities` all PRESENT, control absent). First evidence: every reporting pod on `git_commit 447f3a8a8…` with **82 discovery checks, 314 actions, 1 sentinel**. **THE DEFECT ITS OWN FIRST RUN EXPOSED: the table was a LEAK** — this binary also runs as EPHEMERAL per-job pods (`agent-page-rerender-*`, `agent-page-build-handler-*`, `agent-build-dispatch-loop-*`, `agent-site-publisher-*`), ~52 pod starts/hour, each writing ~400 rows and dying. Measured 3h40m in: **75,827 rows / 191 distinct pods / 24 MB while only 82 pods existed** ⇒ unbounded without a prune. **⚠ CORRECTED same day: the '~160 MB/day' I first wrote was extrapolated from a BURST (a fleet rerender wave) and is ~17× too high — re-measured over three quiet hours it is 3/1/5 pod starts per hour, ~1,200 rows/hour. Honest range: 3–52 pod starts/hour depending on rerender activity.** Fixed by a retention prune on the write path (`RetentionWindow` 2h) plus a `Touch` heartbeat (`TouchInterval` 15m) so live pods are not swept — **the ordering between those two constants is load-bearing and pinned by a test**. 73,445 dead rows deleted by hand as the interim; **the table regrows until the next roll carries the prune** (rate is bursty — see the correction above) — manual prune is one line, in the lane RUNBOOK. Original status line (before this correction) — the Go half ships in this commit and cannot run until an image carries it. Zero rows today is the CORRECT state, not a failure. `RFC_040`, owner-ratified 2026-08-20 and **scoped to this recording half only**.
- **what:** `platform/buildcapability` (`Record`, `Touch`, `Set{Kind,Names}`) writes one row per (service, pod, kind, name) into `service_binary_capabilities`, replacing that pod's own rows in one transaction. Called from `cmd/agent-chassis/main.go` — the one place that can see both enumerable registries (`checks.Names()` at `discovery_checks/registry.go:171`, `actions.ListActions()` at `actions/registry.go:2124`) and has config in hand. Kinds today: `build` (the sentinel), `discovery_check`, `action`.
- **why it exists, in one measurement:** 32 migrations in `sql_for_agents/` assert a binary precondition in prose; **0** can verify one, because SQL cannot reach a pod. And the human check the estate prescribes frequently CANNOT BE PERFORMED — the `build provenance` line is a STARTUP line (measured gone from a FULL `kubectl logs` 3h after a roll) and `buildinfo.GitCommit` is ONE string rather than an ancestry, so grepping the binary for your own commit returns ABSENT for a binary that certainly contains it. Two lanes burned by exactly that: `bugs_open/215` (v1.0.1288, all five probes absent INCLUDING its control) and `bugs_open/299` (v1.0.1316).
- **the design decision another workstream most needs to know:** the package is DELIBERATELY DUMB — it knows nothing about what a capability is and imports nothing but `pkg/buildinfo` and `database/sql`; **callers pass the lists in.** That is what makes it importable from anywhere. `actions` imports `discovery_checks`, so any package reaching into either would inherit that direction and could never be imported by both. If you add a service, add a call site — do not teach this package about your registry.
- **⚠ LANDMINE — reading it wrong is easy and silent, in two ways.** (1) **ALWAYS filter on `last_seen_at`**: a dead pod's rows sit there looking current, and a reader that ignores staleness concludes a binary is running that is not — the same class of error this table exists to end, reintroduced by the table itself. `Touch` exists for this and **the caller must call it periodically; today nothing does** [STATED GAP, not fixed by this commit]. (2) **EVERY pod, not ANY pod** — during a partial roll replicas differ and the run that fails is the one landing on the old one (the `logs deploy/X reads one pod of N` trap). The worked read is in migration 503's header: `count(*) FILTER (…) = count(DISTINCT pod_name)`, and `pods` must equal the live replica count.
- **what is DELIBERATELY absent, so nobody files it as a gap:** (a) **no `assert_live_capability()`** and no migration calling one — the owner's ruling (RFC_040 §0) is that a fail-closed helper with exactly ONE caller is a mechanism nobody exercises, which is this estate's own documented failure mode; it waits for a second real demand, and **a future author adding it should be able to name two migrations that want it.** (b) **no `image_tag` column** — checked 2026-08-20, it is NOT obtainable from inside the pod (the container's environment carries `HOSTNAME` and `AGENT_TYPE` and no image tag), and a column that could only ever be `''` reads as "we record this" while answering nothing.
- **fails soft, on purpose:** every arm of the call site logs `Warn` and returns; a capability registry that can stop the chassis starting is a worse bargain than the problem it solves. The refusals that are HARD are the ones that would corrupt the meaning of an absence: an empty service or pod name is rejected before touching the DB (an unattributable row would satisfy a presence check for every service at once), and a failed insert rolls the whole list back (a truncated list is indistinguishable from a short one). Both mutation-proven 2026-08-20.
- **sources:** `platform/buildcapability/buildcapability.go` + `_test.go`; `cmd/agent-chassis/main.go` (`recordBuildCapabilities`); `docs/agent_docs/sql_for_agents/503_service_binary_capabilities.sql` (the read rules and the omissions are in its header); `architecture_review/RFC_040_a_migration_cannot_ask_the_binary_what_it_can_do.md`
- **relations:** BLD-019 (the stamp this supersedes as the *answer* — 019 put the commit in the binary, this puts the capability in the database, because the commit turned out to be a proxy and an unreadable one); BLD-020 (per-service vs per-fleet revision); LANDMINES *"Probing the live binary for YOUR commit returns ABSENT…"* (the trap, with the manual substitute to use until this is live everywhere); `bugs_open/299`/`bugs_open/312` (the lane that hit it)

### BLD-024 — Brief writer: an aspirational mission brief from a domain name alone, held for the owner to edit
- **status:** LIVE in config (migration `510`, applied + verified 2026-08-20; DB config is live immediately, no roll needed). **Exercised on `indoorplanters.co.uk` the same day** — see status-evidence. **Nothing dispatches it automatically**, by design.
- **what:** A `brief-writer` agent (8 steps: `read_specs → search_web → prepare_urls → scrape_pages → write_brief → persist_brief → create_review_item → complete`) that researches a subject from the domain name and writes a comprehensive `mission_brief` spec, then raises a `needs_brief_review` work item **held at `status='needs_human_review'`** so nothing builds until a person releases it. Fired by `scripts/fire-brief-writer.sh <domain> [direction]`, which reads the LIVE workflow out of `agent_definitions` rather than shipping a copy that can drift.
- **why:** Flow A is ruled — a site is always built from a brief — but the owner has ~1,500 domains and cannot write one each. This is the third brief producer beside the owner and a third-party customer, and it writes to the **same `mission_brief` aspect** a hand-written brief uses, so **nothing downstream changes**: the classifier already reads `site_specs` wholesale.
- **the two rulings that shape it, and they pull against the first design:**
  1. *"The spec is aspirational, the plan is achievable."* The prompt is told NOT to trim ambition to what the platform can build. Constraining it would bake a snapshot of the platform into a document meant to outlive it, and would delete the evidence of what is missing — which across 1,500 briefs is the best-evidenced roadmap this estate could produce. The planner already receives the live component catalogue (`build-site-planner`'s `load_components` step feeds `available_components` into `plan_site`) and degrades; the brief aims. Each `content_plan` entry carries `priority: core|valuable|aspirational` so the degradation is legible.
  2. *"I'd like to briefly look at each one. I may have a few words of direction… to add to or change."* So this is generate-then-EDIT, not approve/reject. Because it is a normal `site_specs` write, **the owner's edited version supersedes the generated one with the generated one preserved underneath** — the diff between what the machine proposed and what he changed survives for free, which is the most interesting record in the whole flow.
- **the hold: PROVEN in both directions, not assumed** (2026-08-20). `site_work_items.approval_mode` is the richer mechanism and the dispatcher honours it — `load_work_item_actions.go:709`, `AND (COALESCE(wi.approval_mode,'auto') = 'auto' OR wi.status = 'approved')`. Tested against that predicate verbatim on two disposable rows: `manual`+`triaged` is **not** returned, flipping to `approved` **is** (rolled back, nothing persisted). ⚠ **It had never been exercised — all 10,311 rows were `auto`.** BUT `create_work_item` has **no `approval_mode` config key**, so an agent cannot set it without a Go change, and Go is inert until a roll. `status` IS a supported key and the dispatcher only selects `status IN ('triaged','approved')`, so `needs_human_review` holds the item **today with zero code** — and it is the estate's existing HITL idiom (`directory_citation_unverified` does exactly this).
- **status-evidence:** applied with a DO/RAISE verify block asserting 8 steps, all 5 `write_brief` `input_fields` present (a template variable without its `input_fields` entry renders EMPTY and errors nothing — LANDMINES), `create_review_item.status = needs_human_review` (the whole review design), and `persist_brief.aspect = mission_brief` (write it anywhere else and every downstream reader misses it). First run: orchestration `1ea45228-2135-4754-b3d9-d3d792b87df0` on `indoorplanters.co.uk`, a **locked** test site so nothing could build even by accident.
- **what it deliberately does NOT do:** read the positioning register (next piece — `PLAN_2026-08-19`'s addendum §C argues the brief-writer should be the register's reader, *replacing* RFC_037's classifier input, because the owner reads briefs and so can correct positioning that lands there); screen a third-party brief for security or reasonability (a different job — this WRITES a brief, that one VETS an incoming one); or modify `domain-submitter`, so a hand-written mission still wins and no existing flow changes.
- **sources:** `sql_for_agents/510_brief_writer_agent.sql` (+ `_ROLLBACK`); `scripts/fire-brief-writer.sh`; `docs024_key_docs_latest/portfolio_positioning/PLAN_2026-08-19_one_flow_three_brief_sources.md`.
- **relations:** `domain-submitter` (writes `mission_brief` from a file — the human producer); `domain-research-classifier` (reads `site_specs` wholesale, which is how the brief reaches it); CGV-032/CGV-033 (a brief that ASKS for a regulated model is the one legitimate route to one, so this agent's output is the input to that decision); RFC_037 (whose advisory half this would absorb); `capability_gap` (where the planner records what the aspiration could not reach — 42 raised, **41 deferred**, so that queue needs an owner before 1,500 briefs multiply it).
- **verify-later:** (1) the first run's OUTPUT quality — is the brief specific to the subject, or generic with the nouns swapped? That is the failure mode the prompt argues against and the only thing that decides whether this is useful. (2) Whether the held item is a sufficient review surface at volume, or whether the owner needs a list view. (3) `approval_mode` support in `create_work_item`, which would make the hold semantically correct rather than a status convention.

### BLD-026 — `pkg/releaseset` + `cmd/releasecheck`: the coverage gate's admission test INVERTED, so an image of ours that no release builds is the violation rather than the skip
- **status:** built and committed 2026-08-22 by the `bugfix_318_release_source_coverage` lane. ~~**ARMED AND MUTATION-PROVEN, not yet exercised by a real release.**~~ → **EXERCISED 2026-08-22 by `v1.0.1326`, without incident**, and `bugs_closed/318` closed on it. `auth-service` and `core-manager` are on `v1.0.1326`, and both move only through `deploy-core`, which requires `update-kustomization-images`, which requires this gate — with a non-zero exit propagating through `pinned_sweep`'s `|| exit 1`. 20 Deployments and 11 CronJobs landed on one tag. **⚠ Read that precisely, because BLD-022's entry had to be corrected for this exact overclaim: the tree was compliant, so the gate COULD ONLY HAVE PASSED.** This proves the inverted predicate does not break a real release; it does NOT prove the gate discriminates. That rests on the six mutation controls below and nowhere else — exercised, not proven. Make-level, so it bites at each `deploy-core` / `make release`; no roll is needed and no image carries it. **Council APPROVED round 1, `Council-Reviewed: 83442a5a-e66d-4772-8872-b445f521d47b`** (3 advisory objections, none high — **two of them changed the code**, see below) — and unlike BLD-020 and BLD-022, this one COULD be submitted, because the predicates live in `pkg/`. That is the point of the layering, not a side effect: `scripts/council-scope.sh` scopes review to `platform/`/`internal/`/`pkg/` and appliable migrations, so `cmd/` is outside it and a helper written wholly there would draw no review at all.
- **what:** `pkg/releaseset` holds the release's shape (`ParseMakefileDecls` over the four `NAME := …\` blocks; `ScanOverlays` over the deployment tree) and three predicates over them; `cmd/releasecheck` is a thin CLI (`--root`, `--registry`, exit 0/1/2) that the makefile's `check-release-coverage` recipe now runs. **The 25-line shell predicate it replaces was DELETED in the same commit**, not left beside it — two implementations of one gate is the drift class `099_SYNC_gate_roster.py` exists for.
- **why — the defect is one word, `continue`.** BLD-022's recipe read `case " $(foreach i,$(RELEASE_IMAGES),$(REGISTRY)/$(i)) " in *" $$img "*) ;; *) continue;; esac`. Membership of `RELEASE_IMAGES` was therefore the gate's **own admission criterion**, so a service omitted from the list at birth was not *uncovered* — it was *out of scope*, and the gate printed "Release coverage OK" about the exact omission it existed to catch. **Eight services fell into it.** Six were folded in by owner ruling on 2026-08-18; `optional-explicit-wires-check` (born 08-21) and `commit-sha-exposure-check` (born 08-22) fell in **afterwards**, authored by sessions that had that ruling in front of them. The makefile's remedy was a comment in capitals, and CLAUDE.md's owner ruling of 2026-08-02 §2 already settles what that is worth.
- **the three predicates, and what each cannot see:**
  - `UncoveredOverlays` — an overlay pinning a `$(REGISTRY)/…` image must name a `RELEASE_IMAGES` member **or** carry an `OWN_LINEAGE` exemption. Second arm keeps BLD-022's original direction (release-built image, no retag path) intact. Blind to: services with no `overlays/` tree (base-only, applied by hand), overlays never applied to the cluster, and non-registry images (correct — ollama and postgres are not ours to roll).
  - `InvalidDeployEntries` — **the reverse direction BLD-022 §(iv) records as policed by NOTHING.** A deploy entry whose resolved image is not in `RELEASE_IMAGES` would have `deploy-agents` retag that overlay to `$(IMAGE_TAG)` for an image nobody builds; the two GitHub runners share one image and would `ImagePullBackOff` **together**, taking CI with them. Note this survived the same day's `build-backend` derivation: deleting a name from `RELEASE_IMAGES` stops it being built *and* pushed consistently, but the deploy loop still retags the orphan. Also fails a `RETAG_EXEMPT`/`OWN_LINEAGE` entry naming a service with no overlay — a mute with nothing under it, which will silently clear whatever service next takes the name.
  - the parser refuses rather than empties: a missing `RELEASE_IMAGES`/`AGENT_DEPLOY_SERVICES`/`RETAG_EXEMPT` block is **exit 2, "the check could not run"**, never 0.
- **`OWN_LINEAGE` — an opt-in field whose UNSAFE SIDE IS THE DEFAULT OFF**, per the 2026-08-02 §2 ruling; per RFC_022's narrowing (owner, 2026-08-11) that shape is **not** architecture-scope. It must stay a LIST rather than becoming a rule: a predicate that guesses which services are legitimately outside the release is one nobody can review. **It was going to ship empty and the gate's FIRST LIVE RUN found a real entry** — `admin-dashboard` pins one of our images that `build-backend` does not build (a frontend, working-tree build, no `ref_build`, no provenance stamp: BLD-019/BLD-020 scope frontends out explicitly) and yet IS released, as the last goal of `pinned_sweep` via `release-dashboard`. Genuinely own-lineage, genuinely covered, and **nothing on disk said so** — the old gate could not have said so, because its image is not in `RELEASE_IMAGES` and that was the admission test. The entry also records what it does **not** cover: `release-backend` omits `release-dashboard`.
- **two enumeration corrections, both measured:** `ScanOverlays` walks `overlays/production` to **any depth**, because `tools-api`'s real overlay is at `overlays/production/kustomization.yaml` with no region directory and BLD-022's fixed `$(OVERLAY_PATH)` glob could never see it (placeholder image, no workload — harmless today, invisible by construction); and `newName` beats `name`, which is kustomize's own semantics. Only that same placeholder uses `newName`, so the second is a fix for a **latent** case and is not claimed as a live defect.
- **how to verify — and the instrument is shown able to fail.** `make check-release-coverage` → green (2026-08-22: *"30 of 33 production overlays pin a docker.io/aqls/ image"*). Then break it, **on a COPY, never the live makefile** (`WRONG_CALLS.md` 2026-08-22: a session mutated this very file in place to prove BLD-022 discriminates and another session committed it inside the window):
  ```bash
  mkdir -p /tmp/gp && ln -s "$PWD/deployments" /tmp/gp/deployments
  sed 's/^AGENT_DEPLOY_SERVICES := agent-chassis /AGENT_DEPLOY_SERVICES := /' makefile > /tmp/gp/makefile
  go run ./cmd/releasecheck --root /tmp/gp --registry docker.io/aqls   # exit 1, "NO RELEASE PATH: agent-chassis"
  ```
  All four mutations were run 2026-08-22 `[MEASURED]`: drop `agent-chassis` from the deploy list → `NO RELEASE PATH` (this **reproduces the original `bugs_closed/237` bug state**, which is BLD-022's own discriminating control); drop `content-loss-check` from `RELEASE_IMAGES` → `OUR IMAGE, NO RELEASE BUILDS IT` (**the shape the old gate passed**); empty `OWN_LINEAGE` → names `admin-dashboard`; rename `RELEASE_IMAGES` away → *"THE CHECK COULD NOT RUN"*, never a pass. A verbatim copy → exit 0. Nine table cases in `predicates_test.go` each name, in their comment, **what a different result would mean**.
- **PHASE 2 — the CLUSTER census (`--census`, `make release-census`), added 2026-08-22, `Council-Submitted: b0883c17-32a1-434d-b0ab-114df4cb04b1`.** Everything above reads the FILESYSTEM and asks *can a release reach this service?* — preventive, and it REFUSES. The census reads the CLUSTER and asks *does what is running match what is declared?* — a detector, and it only REPORTS. It exists because **the filesystem and the cluster are two enumerations and NEITHER IS A SUPERSET OF THE OTHER**: measured 2026-08-22, `capped-schedule-ordering-check` was declared everywhere and running nowhere, while `site-discovery-staleness-check` and `site-locale-unset-check` were running with no `overlays/` tree on disk. Three comparisons — BEHIND the fleet tag (the shape the original six sat in for months), AHEAD of it (a hand deploy), DECLARED BUT NOT RUNNING, RUNNING BUT NOT DECLARED. The predicates take a `[]Workload`, **not a Kubernetes client**, so the whole census is table-testable with no cluster; `cmd/releasecheck/cluster.go` owns the only part that touches client-go, and it LISTS Deployments, CronJobs **and** DaemonSets (reading only Deployments would have missed every frozen check service, which is most of this bug's evidence). A list error aborts — a partial read is not a read.
- **⚠ THE CENSUS'S FIRST LIVE RUN FOUND A DEFECT IN ITS OWN REPORT, and this is the entry's second most useful line.** It called two services on `v1.0.1324` *"RUNNING AN OLD FLEET TAG"* against a fleet on `v1.0.1323`. They were **newer** — hand-deployed. **A report that states the opposite of the truth is worse than one that stays quiet**: a reader chasing a frozen service finds one that is, if anything, too new, and concludes the instrument works. That is the blind-pass family one rung along — not a check that misses, but one that asserts the inverse. Fixed by splitting the kind by DIRECTION (with opposite remedies: a straggler wants a release, a hand-deploy wants the tag never reused, because a same-tag re-push serves the node's cached image), by ordering tags **numerically rather than lexically** — not theoretical, this estate has already crossed `v1.0.999 → v1.0.1000` where a string comparison says 999 is newer — and by giving an unorderable tag its own kind rather than a guessed direction. **What would have caught it earlier: nothing in the unit tests, because the fixtures covered the direction I had in mind and not its mirror. When a finding has a direction, write the fixture for BOTH before running it anywhere.**
- **census, AFTER the first release under the gate** `[MEASURED 2026-08-22, post-`v1.0.1326`]`: **5 findings → 1**, on 31 of 47 workloads, and each of the five resolved exactly as the census said it would — `optional-explicit-wires-check` 1321→1326; `commit-sha-exposure-check` and `content-loss-check` 1324→1326 (and the fleet went to **1326**, so the contaminated 1324 was never re-pushed over); and the two DECLARED BUT NOT RUNNING services were **CREATED at 15:09:35Z and 15:09:37Z** by the release's own apply loop. **Those two timestamps are the strongest evidence in this entry:** the lane predicted the creation in writing, in this register and in a contribution to another lane, and the prediction could have come out otherwise — the release could have aborted at `push-backend`, which is what `95757b6c2` fixed. The one surviving finding is a service filed by another lane *since* the release (`live-declaration-drift-check`), i.e. the census catching the next one on the day it appeared.
- **census, live result BEFORE that release** `[MEASURED 2026-08-22]`: *"29 of 45 workloads run a `docker.io/aqls/` image; the fleet is on v1.0.1323"*, **5 findings, zero false positives** — two AHEAD (hand-deployed at 1324), one BEHIND (1321), two DECLARED BUT NOT RUNNING, each separately confirmed by hand or by another lane's own commit message. No `RUNNING BUT NOT DECLARED` at all, so every `aqls` workload in the cluster is accounted for.
- **⚠ THE CENSUS MEASURES TAGS, AND A TAG IS NOT THE CODE — a clean census means "every service is on the tag it should be on", NOT "every service is running the code it should be running".** A service sitting on the fleet tag reads as current here and can still be running a stale binary, because a same-tag rebuild serves the node's cached image (`LANDMINES`, *"a FRESH BUILD can ship NO NEW CODE"*). Raised by the council's `debug_historian` seat (low, corr b0883c17) against the estate's standing rule to verify at the artefact and never at a tag. **Stated rather than fixed, deliberately:** the artefact-side answer already exists and belongs to BLD-019 (the commit stamped into every image and binary), BLD-020 (one release, one revision) and BLD-023 (a running pod publishing what it can do), and duplicating it here would give one question two answers that can disagree.
- **follow-up, tracked before it calcifies (`architecture` seat, low, corr b0883c17):** `cmd/releasecheck/cluster.go` is now the **seventh** inline `kubernetes.NewForConfig` bootstrap in the estate with no shared wrapper — the others are `spawn_actions.go`, `diagnose_build_gate_action.go`, `agent_image.go`, `internal/adapters/thunder/ssh/secrets.go`, `cmd/remote-job-spawner/main.go` and this one `[MEASURED 2026-08-22]`. **Deliberately not extracted in a bug-fix round** (touching five unrelated call sites is the scope shape the guardian seat vetoes, and rightly), but the seat's warning is the right one: unwinding it across many call sites at once, later, costs an RFC. The one real difference between them is the in-cluster-only vs kubeconfig split — `agent_image.go` is `rest.InClusterConfig()` only and cannot serve a hand-run CLI, which is why this file has both paths.
- **⚠ the census is HAND-RUN ONLY and must NOT be described as a live detector.** No CronJob, no RBAC, no `doc_notes` row; the makefile comment says so in capitals. Scheduling is a separate decision with its own round, and the split is deliberate rather than unfinished — *"detection works; SCHEDULE and DISPATCH do not"* is a measured finding here, so a detector and its driver are not claimed together. **Second stated limit:** `readWorkloads` reads `containers[0]`, so a sidecar carrying one of our images would be missed. None exists today; named because it is the **same shape** the council caught in round 1 (the `images:` block cap), and if a sidecar ever appears this is where to look.
- **landmine — `go run` COLLAPSES the exit code.** `[MEASURED 2026-08-22]` a throwaway `os.Exit(2)` program gives `go run .` → **1** and its compiled binary → **2**. The makefile treats any non-zero as failure so the release stops correctly either way, but a scripted consumer cannot tell *"the release is wrong"* from *"the check is broken"* — which is why the message says which **in words**. A future non-makefile consumer must invoke the compiled binary.
- **the council found a real defect IN THE FIX, of the fix's own disease — and it is the entry's most useful line.** `bug_historian` (medium) noticed that the first cut's overlay parser stopped at the **first** element of an `images:` block. An overlay pinning two images with OURS second would produce no `Pin` at all, so the gate could never flag it: *a check reporting clean about the thing it never looked at*, reproduced in miniature inside the change written to end exactly that. Measured before acting — **no kustomization anywhere under `deployments/` has more than one element**, so the gap was LATENT, not live — and the cap was then **removed rather than warned about**, because a cap that is safe only because of today's data is the wrong kind of safe. Test **T9** is the seat's own missing fixture. Second objection acted on (`editquality`, medium): **`RETAG_EXEMPT` and `OWN_LINEAGE` are OPTIONAL, only the two JUDGING lists are required.** Both are CLEARING lists, so their absence can only ever produce MORE findings; requiring one risked a self-inflicted "could not run", and the package's own argument for `OWN_LINEAGE` applied to it word for word.
- **landmine — a green run is not evidence the gate discriminates.** BLD-022's own entry had to be corrected on exactly this ("exercised, not proven"), and `TestLiveTreeIsCompliant` says so in its own comment. The discriminating power is the mutation table and nothing else.
- **the price of the literal parser, and how it is paid:** `ParseMakefileDecls` is not a make evaluator, so a declaration rewritten as `+=` or `$(filter …)` would read differently from what make sees. `decl_parity_test.go` asks **make** for the same four lists through new echo-only `print-release-images` / `print-agent-deploy-services` / `print-retag-exempt` / `print-own-lineage` targets and requires the readings to agree, reporting a two-column diff rather than "not equal". It **SKIPS LOUDLY** when `make` is unavailable and never passes quietly — the `cron_parity_test.go` / `optional_budget_cron_parity_test.go` idiom, one language over.
- **the exemption ACCUMULATION budget — `ExemptionBudget = 3` (owner decision 2026-08-22, taken INSTEAD of the staleness build).** The inversion closes the old hiding place ("not in `RELEASE_IMAGES`") and opens a new one ("excused"). Every `OWN_LINEAGE` entry is individually reasoned and reviewable — that is the point of the form — but nothing that reviews entries one at a time notices there are now nine of them, and this bug's own history is the argument: eight services found the previous hiding place, two within three days of the ruling meant to close it, each addition individually unremarkable. So the release fails when the excused list grows past **3**, naming every entry — the same shape as the optional-key budget (**WFA-013**, RFC_022, owner-set N=10), with N lower **deliberately**, because an exemption from the release is rarer and costlier: what it permits is a service running months-old code with nothing reporting it. **N is a judgement and one line; the owner may set it otherwise.** The half that does the work is the other one: **the gate NAMES the standing exemptions and the budget on every GREEN run** (*"1 of those is EXCUSED from the release (OWN_LINEAGE, budget 3) — admin-dashboard → deploy-dashboard"*), because a threshold silent until it trips is a threshold nobody watches — so the FOURTH exemption is noticed as it arrives rather than when a number is crossed. ⚠ **It cannot fire today — one entry as of 2026-08-22.** A guard with no live subject is normally this estate's refusal (see BLD-023 on `assert_live_capability()`); what distinguishes this one is that it needs **no caller** — it runs on every `deploy-core` whether or not it fires, so it is exercised continuously while quiet. That argument is written at the constant so a later reader can disagree with it rather than guess at it. Proven by **T10** (silent AT the budget; one over reports and names the whole set, with a remedy offering a *reviewed* raise) plus a copied-tree run. Honest detail: fabricating exemptions to test the budget by hand trips the *no-overlay* guard instead — the two are independent, and the clean at-budget case is the unit test.
- **deliberately NOT built, so the absences read as decisions — and the first is now an OWNER RULING rather than a lane's judgement (2026-08-22, *"we can skip the 18 August staleness build"*), so it is CLOSED rather than pending:** no staleness detector of any kind, git-history or artefact-anchored — `bugs_closed/318`'s candidate 1 as worded is uncomputable here, because `deploy-agents` seds `newTag` in place and nobody commits it (26 overlays dirty 2026-08-22; `agent-chassis` committed at `v1.0.1239` against `v1.0.1323` on disk **and in the cluster**), so the pin's history is fiction; no gating of development overlays (only 9 services have one, and a hard failure breaks every non-production deploy — BLD-022 §(i)); no cluster-side census in this round (it is detection, it needs RBAC, and it gets its own commit and its own review); no auto-remediation.
- **sources:** `pkg/releaseset/{decl,overlays,predicates}.go` + `{predicates,decl_parity}_test.go`; `cmd/releasecheck/main.go`; `makefile` (`check-release-coverage`, `OWN_LINEAGE`, the four `print-*` targets); `bugs_closed/318_HANDOFF_2026-08-19_…md`; `docs024_key_docs_latest/bugfix_318_release_source_coverage/` (the lane's standing five — `PLAN_2026-08-22_…` holds the four predicates costed and the planning agent's adversarial pass, one of whose three corrections was checked and REJECTED with the arithmetic)
- **relations:** **BLD-022** (the gate this replaces — its `continue` is the defect, and its §(iv) reverse direction is what `InvalidDeployEntries` closes); **BLD-019**/**BLD-020** (what an image is built FROM, which this deliberately does not duplicate — this asks whether a release reaches the service at all, not whether the tag tells the truth); **BLD-023** (~~the artefact-side instrument `bugs_closed/318`'s candidate 1 should be re-aimed at~~ — **the owner RULED 2026-08-22 that the staleness build is skipped altogether**, so this entry does not depend on it and nobody is owed it; BLD-023 is also the precedent cited for why a guard with no live subject is normally refused, and why the exemption budget below is the exception); `bugs_closed/237`; `LANDMINES.md` *"A CHECK SERVICE OMITTED FROM `RELEASE_IMAGES` AT BIRTH IS INVISIBLE…"* (the trap, now closed by construction) and *"A PRODUCTION OVERLAY'S `newTag` IS EDITED BY THE RELEASE AND NEVER COMMITTED"* (added by this lane, and the reason candidate 1 was re-aimed)

### BLD-025 — The positioning register as a DATABASE table, and the brief writer reads it
- **status:** LIVE in config + schema (migrations `511` table, `512` attribution, `513` the reader; all applied + DO/RAISE verified 2026-08-20/21). **189 rows loaded.** `read_register` PROVEN returning a real entry with its siblings (orchestration `ad1ff520`, domain `buytoletcalculator.uk` → entry M9 + sibling M1).
- **what:** `positioning_register` — one row per DOMAIN (not per entry, because a twin must be findable by its own name and the brief writer is given a domain). Loaded by `scripts/domains/load_positioning_register.py`, idempotent on `lower(domain)`. The `brief-writer` agent gained a `read_register` step that returns this domain's entry plus up to 12 family siblings, feeding `write_brief`.
- **⚠ THE DESIGN DECISION THAT MATTERS — `raw_md` IS AUTHORITATIVE.** The typed columns (`proposition`, `audience`, `mode`, `stance`, `neighbours`) are a convenience INDEX; `raw_md` holds the whole original entry and wins when they disagree. The register's value is its REASONING, and that lives in prose no schema anticipates — measured: 49 entries use **18 different labelled field names**, and `owns:` is labelled in exactly ONE of them while the same idea appears as prose in dozens. **A lossy parse of a hand-written document is not a migration, it is a deletion**, and "the database is the source of truth" is only safe if nothing is lost getting there. This is the same discipline `EvidenceBase` failed at (LANDMINES: parse-and-write-back destroys every citation and `writer_line` the struct does not model) — learned there after the damage, designed in here.
- **attribution, and why it earns a column (512):** `field` (60 rows, from labelled `primary:`/`twins:`/`domains:`) · `prose` (78, swept from the entry body) · `exclusion-only` (51, no entry, the row exists to carry `exclude_from_build`). The labelled fields ALONE left **82 of the 152 portfolio domains with no row** — every one named in an entry's prose. Sweeping recovers them, which matters once the table is the source of truth (a domain with no row is invisible to anything that asks), but a domain named in `primary:` and one mentioned in a sentence are not equally certain and collapsing that would make the table look more confident than it is.
- **why the reader is in the BRIEF WRITER and not the classifier — this CHANGES `RFC_037`'s answer.** RFC_037 ruled the register goes to `classify_and_extract`. A later owner decision — *"I'd like to briefly look at each one. I may have a few words of direction… to add to or change"* — decided it differently: **the owner reads every BRIEF, so positioning that lands there is positioning he can correct; positioning fed to the classifier is invisible to him.** Plus reach (the brief is inherited by strategist/briefing/planner, which is the gap RFC_037 §4 itself names), risk (a new agent vs a shared seam every site passes), and one reader not two (the `099` drift class). **What it gives up:** the classifier no longer sees siblings AS siblings, so a BINDING collision check still needs RFC_037 — keep it open, because at 1,500 briefs review-on-collision is the only sampling rule that scales.
- **inertness is asserted, not assumed:** 189 rows against ~1,500 domains and the 50 test domains are deliberately entry-free, so most calls return zero rows. The prompt is told explicitly to say so in `open_questions` and NOT to invent a position — an absent entry means unknown, not unconstrained.
- **the trap the verify block exists for:** `register_entry` must appear in `write_brief.input_fields`. A template variable without its `input_fields` entry **renders EMPTY and errors nothing** — the migration would apply, the prompt would read correctly, and the register would silently reach nothing. 513 raises if it is absent.
- **sources:** `sql_for_agents/511_positioning_register_table.sql`, `512_…attribution.sql`, `513_brief_writer_reads_the_register.sql` (all + `_ROLLBACK`); `scripts/domains/load_positioning_register.py`; `PLAN_2026-08-19_one_flow_three_brief_sources.md` addendum §C.
- **relations:** BLD-024 (the brief writer itself); `RFC_037` (whose advisory half this absorbs and whose binding half it leaves open); `REGISTER_positioning.md` (the source document — **its fate is undecided: generated view, one-time input, or retired. Until that is settled, do NOT edit both**, because two hand-maintained copies of one roster is exactly the `099` drift class).
- **⚠ (1) ANSWERED 2026-08-21 — the model USES it, not merely receives it.** The `buytoletcalculator.uk` brief (entry M9) names its siblings by code and stays off their ground unprompted: *"The site deliberately does not serve the general homebuyer (M2 territory), does not compare rates (M3 territory), and does not go deep on company/SPV structures as a mortgage product (M12 territory)."* It also adopted M9's own vocabulary from `raw_md` — yield/ICR, "competent-to-expert landlord" — which is the clearest evidence the AUTHORITATIVE field is what carried the reasoning, not the typed columns. And it escalated the boundary case instead of deciding it: *"Should the site acknowledge the limited company / SPV route… or defer fully to M12 (smbmortgages.co.uk)? …the boundary needs deciding."* That is the prompt's "put a conflict in open_questions, a person reads every one" instruction working. **One run is one run** — this shows the mechanism functions on a well-formed entry, not that it holds on a thin or contradictory one.
- **verify-later:** (2) the **21 portfolio domains still with no row**, all `.uk` siblings of registered `.co.uk` names that the document itself never names — a register gap, not a parser gap; (3) the collision invariant as an actual check: `(family, audience, mode)` is the stated rule but the current rows do not populate those three consistently enough for a UNIQUE index to be anything but a blocker, so a daily check is the honest first step; (4) neighbour selection at 1,500 — siblings are derived from `family` and bounded at 12, which is a rule that scales but not necessarily the right one.

### BLD-028 — Listing-page item-source gate: plan validation refuses a listing page whose items cannot exist
- **status:** built, tests green (gate default-OFF in Go; enabled for `build-site-planner` by migration `720_planner_listing_source_gate.sql`; INERT until the next chassis roll AND the migration apply — either order is safe). Registered in the same commit that ships the seam, per the 2026-07-29 ordering-exemption condition 2. Council corr `c0990eb3-9f50-4e08-b578-a7e05f786945` (bugs_open/444).
- **what:** `ValidateSitePlanAction` (behind opt-in step config `enforce_listing_sources`, default false) resolves every listing-family plan page's ITEM SOURCE for the site being planned, and HOLDS any page whose source resolves to zero: the page is removed from the plan and ONE `capability_gap` work item is filed per held page — `item_key capability_gap:<page_type>:<page_name>` (co-dedups with `reconcile_site_plan`'s builder arm), `handler_agent` empty (the 078/291 rule), `spec.gap_kind='producer_missing'`, `spec.builder_needed` = the enablement slug `diagnose_triage` groups on. Listing-family = role ∈ {section-index, blog-index, news-index, entity-directory} OR any section naming a listing component. Resolvers (`listing_item_sources.go`, one registry so no second copy can drift): news → `content_features.news_feed.recommended` OR active `content_sources` rows (NEWS-001's driver); per-kind directory components → `content_features.<SpecKey>.recommended` (DIR-001 opt-in; **6 kinds as of 2026-09-02**, lockstep with `directoryCheckProfiles` — a kind added there without a row here fails OPEN); bare `directory-listing` → `queryresolve.HasBusinessDirectoryConfig` (the SAME predicate the renderer errors without, bugs_open/206); section-index → child pages under the URL prefix in plan+realised (order-safe for post-plan tool builds — the pages are IN the plan); blog-index / content-listing / blog-listing → count of blog-post-role pages (matches `query.blog_posts`, deliberately site-wide). **Fail-open on every ambiguity** (unknown component shapes e.g. category-listing, read failures, nil DB, realised pages — the bugs_open/001 preserve guard owns those, receipt still filed): a false drop silently shrinks a site; a false keep ships one more page of a now-detected class. Companion repair in `plan_sections` (rode `dbb218a41`): an ERRORED resolve of a required/min_items query field now DEFERS the section (carry-first) instead of leaving it ready — errors stay distinct from no-data (054), and 206's designed loud failure lands as a HITL item instead of one Warn line in a restarting pod.
- **the enablement contract:** post-plan, `SELECT item_key, spec->>'builder_needed', summary FROM site_work_items WHERE site_id=:site AND item_type='capability_gap' AND spec->>'gap_kind'='producer_missing' AND status NOT IN ('complete','cancelled','rejected')` is the machine half of "what does this site still need enabled" (adopted into `portfolio_positioning/RUNBOOK_remake_release.md` §6). Pre-enable to get pages PLANNED: feeds via `content_features.news_feed` (idea.uk 2026-08-25 worked example) or direct source seeds; directories via DIR-001's seven-place checklist or a `directory-json-exporter` config row (vetcomparison pattern).
- **stated blind spot (scope, not defect):** a listing page typed `content` with generic sections — both shipped glossaries — is invisible to the gate; that half is the prompt conditionality in 720 plus the copy lane's title-promise design (their NOTES record the split). Durable evidence code `LISTING_PAGE_HELD_NO_ITEM_SOURCE` (finding_code_registry.json) — one row per held page, because the gate's Warn lines do not survive pod restarts (how 444's own build evidence was lost).
- **sources:** `platform/orchestration/actions/listing_item_sources.go` (+`_test.go`); `v3_site_actions.go` (ValidateSitePlanAction tail); `plan_sections_action.go` (error→defer, committed via 443's `dbb218a41`); `queryresolve/business_directory.go` (`HasBusinessDirectoryConfig`); `sql_for_agents/720_planner_listing_source_gate.sql` (+`_ROLLBACK`); `bugs_open/444…md`; `bugfix_444_empty_listing_pages/PLAN_2026-09-02_listing_source_gate.md`.
- **relations:** DIR-001 (kind opt-in is the directory resolver; the seven-place checklist is what a `directory_kind_opt_in:*` gap asks for); NEWS-001 (the news resolver IS its driver); bugs_open/054 + 206 (the two-guards-in-series finding this closes the composition of); bugs_open/001 (preserve guard — why realised pages are never dropped); 016b §9 "listing page whose item producer is missing" (the symptom pattern); RFC_022 (the opt-in-default-OFF shipping shape).
