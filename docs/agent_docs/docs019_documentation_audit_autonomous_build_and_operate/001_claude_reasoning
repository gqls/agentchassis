I'm parsing the request: they want me to build the call graph analyzer first to extract caller/callee relationships, then work on framing the imagery brief. They've also shared three manifests from different projects—handoffs they generated by asking a chat to list required files for new conversations—and want me to compare these manually-created manifests.
against what our tool would assemble to validate our approach and spot gaps. The real value here is using these expert-curated lists as ground truth to test whether our framing and bundling strategy captures what actually matters for each task. I should prioritize the comparison since it directly answers whether we're missing critical context, then update the running notes with everything. I'm weighing whether to tackle everything in one turn or split it up. The call graph build is the priority they emphasized, so I should focus on that properly—extending the analyzer to capture function calls and the assembler to construct the call-graph neighborhood. The comparison analysis can follow, grounded in what the tool actually produces, and then I'll update the notes. The imagery framing is a separate next step, so this turn should be call graph build plus comparison plus notes. I'll prioritize the call graph build first since that's what they explicitly asked for, then validate it against the three manifests to see what we'd catch and miss, and finally update the notes. For the call graph design, I need to shift from treating the neighbourhood as the whole in-scope package to making it the actual functions and types that are directly referenced.
The key insight is that full type resolution via go/types is too heavy and fragile for incomplete codebases, so I'm going with a name-based approach instead. I'll extract identifiers from function bodies, build a global symbol index across the repo, and match callees and callers by name. It'll have some false positives and negatives—like two Get functions in different packages looking connected, or missing interface method calls—but it's a solid improvement over the current approach.
For the implementation, I need to extend the analyser to track which function names each function calls, then have the assembler build the call-graph neighbourhood by matching those names. The concern is that adding a calls field per function will bloat the analysis output, but for 459 files it should still be manageable.
The assembler will then construct a symbol index mapping names to their declarations, and use it to resolve callees (looking up the names in a function's calls list) and callers (scanning all functions to find those that call an in-scope symbol). I can also extract type names from the signatures of in-scope symbols to include their type declarations in the context.
I'll add a -neighbour flag to switch between call-graph mode (the new focused approach) and package mode (the fallback), then implement the analyser to walk function bodies and collect CallExpr nodes to populate the calls field.
For the assembler, I'm building a symbol index that maps names to their definitions, then for each in-scope symbol I'll compute its neighbourhood by finding callees (looking up each name in its calls list), callers (scanning all functions to see who calls it), and types used (extracting identifiers from the signature's params and results). Writing the call extraction logic...
I'm planning to test this incrementally: first verify that the call extraction works in the analyzer, then add the call-graph neighbourhood computation and flag to the assembler. The real validation will come from comparing the manifests—that's where I'll see if this approach actually captures the right dependencies. I'm keeping the comparison focused on what matters rather than exhaustive, since the manifests are already in context. The call-graph extraction they did by hand—pulling whole functions with line ranges—is exactly what our automated approach would generate, which validates that mechanism. But there's a critical gap: they're pulling fresh code directly from the running deployed system because the disk copies are stale, whereas our analyzer works from the checked-out tree. If the authoritative source is the live deployment rather than what's checked in, we'd be bundling outdated code.
Beyond that, we're missing live database state and schema introspection—the three key chassis tasks all need current table content and fresh schema definitions, plus runtime evidence like recent error logs and pod states. These are the operational data and runtime evidence pieces we designed but haven't implemented yet, and the comparison confirms they're genuinely essential for debugging these kinds of issues. The schema extractor needs to pull live data, not stale snapshots. We're also seeing multi-tenant patterns emerge—idea.uk is a separate body of work from the chassis project, so our tool needs to handle multiple repos/tenants. The manifests show what we're still missing: live table content, fresh deployed code (not just disk), runtime state from clusters, and the curation logic to pick relevant docs. We'd add the constitution layer (standing rules) that the manifests assume are already known.
Actually, looking closer at the handoffs, they DO restate the standing rules every time—"Snapshot before any DB change," "Check schema before writing SQL," "Reuse existing funcs," "No logger.Debug"—which is exactly what our constitution would capture. The fact that they manually reproduce these rules in every handoff is strong validation that automating this consistently belongs in every bundle. The comparison shows we're automating what experts already do by hand: call-graph slices, constitution rules, reference docs, and code-by-line-range extraction.
The gaps are all deferred already—live table content, runtime evidence from logs and pod state, schema freshness, target resolution, and code freshness. But there's one new angle: for their multi-repo deployed reality, the authoritative code sometimes isn't what's on disk ("pull fresh from the chassis"). For dogfooding on our own repo this isn't an issue, but it flags that the analyser must run against the current source tree, not a stale checkout.
Now I'm planning the concrete next steps: build the call graph with the analyser and assembler, test it, then distill the comparison findings and update the notes. I'm thinking about whether to tackle both the build and comparison in one turn given the length, or split them.
The build requires several tool calls and the assembler edit is substantial, while the comparison is pure analysis with no tool calls. I think the disciplined move is to do all three—build, comparison, and notes—but keep each tight. The build is the explicit priority, so I'll start there and edit the analyser to add the call graph functionality.

