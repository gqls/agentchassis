# NOTES — Running Synthesis: Framings and Principles

**Status:** running. A list of synthesis points and design framings that have crystallised across the conversation, captured as reusable lenses for further design work, not as a final theory. Added to as new ones surface.

Organised by theme. Each item is a principle with a short rationale, brief enough to be a reusable lens rather than a paragraph of philosophy.

---

## Context substrate

- **Documentation is code.** Standards, intent, and trajectory are operational inputs to code generation, not passive reference. Versioned, drift-tested, composed deterministically.
- **Authored vs derived.** Two epistemic categories: owner + lifecycle (can be wrong, needs maintenance) vs no-owner true-right-now readout (can't be wrong, only current or superseded). Source code sits on the line — authored, but also ground truth.
- **The change layer between.** Diffs of code and logs are derived (auto-generated, true by being actual) but narrative (tell the story of what changed) — the natural audit and learning surface.
- **References, not copies.** A chat full of pasted code holds copies; a chat that fetches the file when it needs it holds references. Authored layers point at derived rather than paraphrase, so they don't drift when reality moves.
- **Salience over presence.** LLMs lose the bigger picture because local detail is more salient mid-reasoning, not because text fell out of the window. The lever is salience at the moment of decision.
- **Right altitude at the right moment.** Depth is a virtue, not only a failure mode. Blanket-mission-everywhere degrades implementation steps; selective surfacing serves both correctness and bigger-picture fidelity.
- **Two staleness modes, two fixes.** Authored drift: keep authored thin and pointer-rich. Derived snapshot-staleness: fetch at reasoning time, not paste-time.

---

## Trust, reliability, and the ratchet

- **Bottleneck is trust, not capability.** Reframes "make it autonomous" as a reliability problem: bound uncertainty per step to progressively remove the human.
- **Automation is a per-capability ratchet, not a switch.**
- **Bidirectional ratchet.** Trust can be lost, not only gained. This is the safety property that lets the ratchet advance at all — graduating a capability isn't an irreversible bet.
- **Trust ledger as the master knob.** One control surface governing both how conservatively something is produced (cascade floor) and whether it applies without a human (gate).
- **Verifiability + containment.** Two factors; both needed to climb; the weaker holds the ceiling. Verifiability = ground truth; containment = blast radius / reversibility.
- **Ceiling vs maturity.** High ceiling + low maturity = invest (it converts to ratchet progress). Low ceiling regardless of maturity = invest in good HITL ergonomics, not in removing the gate.
- **Reliability cascade per task:** reuse → generate+verify → compete+judge → HITL. Highest-reliability tier first. **Reuse-before-recreate is load-bearing for reliability, not just tidiness.**
- **Apparent contradictions can be graduated states, not bugs.** When two rules seem to conflict ("everything is gated" vs "the mechanical layer is confirmed by reality"), check whether one is the default starting position and the other is what a trust mechanism eventually permits. They may be the same rule at different maturity levels — the gate is the start; graduation relaxes it.
- **Cold start at the most conservative level.** A new (tenant, capability) pair starts at `confirm_every`, never inherited-higher. Trust is earned per tenant from the most cautious starting point.
- **A capability is a unit of trust, not a unit of agent.** Capabilities aren't 1:1 with agents — "propose a schema migration" or "roll back to known-good" may span several. So the capability (with its ceiling) lives in its own catalog, referenced by the agents that exercise it, rather than as an attribute of any one agent. Trust attaches to the capability; agents are how it's exercised.

---

## "Right" as balance, not a single answer

- **"Right" is a requirement-relative balance** among conflicting dimensions (fast/secure/generic/simple/functional). Not pick, not merge.
- **Authored solutions are extremes that bound the space.** The mediator finds the requirement's point inside it.
- **Priority is not global.** It comes from the why-chain's priority profile, modulated temporally by direction-of-travel.
- **Order, not numeric weights.** Avoid false precision; external events arrive as "X now outranks Y," not as weight tweaks.
- **Drop-out as mode change.** A satisfied concern demotes from author to checker (passively verifying); re-promotes if a later change breaks it. **Unifies the checker and multi-author models as two modes of one process.**
- **Convergence separates resolvable tensions from the irreducible one.** Non-convergence is the escalation signal — it identifies the genuine tradeoff that needs a judgement, with everything else settled around it.
- **Multi-author surfaces tradeoffs vividly; it cannot resolve value-laden conflicts.** Option-generation engine, not decision engine. Decision still lands in the authority model.
- **Candidate owned by no concern.** Shared artifact + change log, seeded deliberately from the profile's dominant dimension. Rounds = premised diffs the mediator adjudicates. Path-dependent — converges near the seed; rival-base proposals are the gated escape for structurally different solutions.

---

## Governance and HITL

- **Confirm-not-initiate.** Decision reasoning is agent-led; the human confirms via a decision package (summary + tradeoffs + explicit choices). The hard job becomes "agent explains well enough that a human can confirm responsibly," not "human authors from scratch."
- **Every decision publishes its reasoning, not just its outcome.** Foundational. Drift detection, the N-round process, and the trace all stand on it. "Chose X because [premise]" is reusable; "chose X" teaches nothing later.
- **Drift detection = gap between a decision's logged premise and the current premise.** Possible only because reasoning is published.
- **Heuristic decisions emit decision + provenance.** Information availability, not information presence. A heuristic that decides invisibly hides drift; a legible one lets later judgement start from "here's what was already decided and on what basis."
- **Provenance informs weighting, not deference.** Knowing which concern authored a solution shapes how to read it; authority comes from the requirement-relative priority, not the loudest voice.
- **Two precedence directions in inheritance.** Normal entries are child-wins (local refinement); sealed constraints are ancestor-wins (legal floors, mission non-negotiables). Sealing prevents a leaf's prior relaxation defeating a new law.
- **Three resolutions to a disagreement: code drifted / doc drifted / legitimate exception.** The default presumption is set by the source-of-truth choice (docs-authoritative → default = code drifted), but the human can pick any. **Default ≠ only.** This is where docs-authoritative earns its keep without becoming rigid.
- **The user-rep is a different *kind* of advocate.** Curators advocate for concerns (correctness, consistency); the user-rep advocates for intent (fidelity to what the user wants). The coordinator arbitrates between them; the user-rep is the check on the coordinator's framing power.
- **"Sufficiently adopted" via both self-judge and mediator-judge**, with the residual recorded if the mediator dismisses an unsatisfied concern. Override doesn't erase; it logs.
- **One path to the privileged state.** When a transition must be gated (here: `proposed → active`), route it through a single component, not reimplemented in each producer. Multiple implementations of a gate are multiple chances to weaken it; one path (the central confirmer) makes confirm-not-initiate airtight and auditable in one place rather than a discipline spread across agents.
- **Newer supersedes pending.** When a new proposal arrives for a target that already has an unresolved one, the newer wins and the older expires. Better never to confirm something reality has overtaken than to block on a stale proposal. Prefer freshness over queue order when the world can move under a pending decision.
- **A system that records its own changes shouldn't re-investigate the ones it made on purpose.** The tool watches for changes so it stays honest about its own effects — but when it deliberately applies a change a human just approved, it should note that change without treating it as something new to re-check. Tell apart "I just did this on purpose, already reviewed" from "something changed, go look." The first needs no re-work; the second does. (Changes the tool generates but that haven't been reviewed still count as the second kind.)

---

## Build vs operate

- **The two routes unify at the context layer, not at "website is code."** Both run on thin authored + thick live derived; vertical (infra vs web) changes which slices, not the machinery.
- **Build vs operate asymmetry.** Build is isolatable (competition safe); operate is live/stateful (competition risky, lean on known-good + canary + rollback).
- **Rollback can be trusted autonomously earlier than roll-forward**, because returning to a known-good state is the inherently safe direction.
- **Cross-cluster: boundary made invisible by design** (same Kafka talk-back, same DNS for remote DB) → theory barely expands. Mostly new capabilities, not new theory.
- **Defect-vs-partition disambiguation.** Quarantine infrastructure-attributed failures from the trust signal, or transient flakiness will de-graduate capabilities that are actually fine.

---

## Onboarding and config

- **The config has three layers with different derivability:** mechanical (discovered + probed), conventions (inferred or doc-sourced — confirmed), intent (elicited). They are not one onboarding problem; they are three uneven ones.
- **Progressive onboarding.** Mechanical value first, ramp not cliff; never "done" — config tracks the repo.
- **Config is a maintained artifact** with the standards' lifecycle. Drift detection, confirm-not-initiate, per-entry provenance (discovered / inferred / supplied).
- **Inference quality scales with codebase quality.** The messier the repo, the less it teaches; the more a tenant needs the tool, the less their repo can teach it. Surface uncertainty rather than silently picking; inconsistency detected is itself output.
- **Extraction is inferred-then-confirmed even in docs-authoritative mode.** The docs are the source, not extraction's authority. Hallucinated conventions would manufacture drift.
- **Coverage honesty.** Don't imply a clean audit when half the conventions weren't checkable. Report what was checked and what couldn't be — three buckets (deterministic violations, heuristic candidates, unchecked judgement), not one number.
- **Conventions have two roles: audited and guiding.** Some atoms appear in the drift audit; some load into generation context; some both. Un-auditable conventions are not lost — they contribute on the input side rather than the output side.
- **LLM as judgement-checker is itself only a candidate flag, never a verdict.** The same LLM-uncertainty problem the wider cascade exists for. Don't create a weaker authority channel by trusting it as a verdict — narrow where to look, leave the verdict to a human.
- **Exceptions feedback.** Marking a disagreement as a legitimate exception is recorded against location + convention, so the audit doesn't re-flag — it becomes incremental.
- **Our own setup is the template, not a special case.** The docs-vs-code disagreement list from our own onboarding is the drift detector's first run, on us — a free audit of where the code drifted from its own documented standards.
- **Inspection is fact, interpretation is proposal.** Reading a file's contents is fact; deciding what it *means* (which Makefile target is "the test command"; that a directory called `actions/` holds the actions in this codebase's sense) is interpretation, which is inferred-then-confirmed even at the mechanical layer. The pattern applies where it is least expected.
- **Probing is declared.** No execution without a plan emitted first, with rationale. Necessary for sandboxing tenants (the security contract); useful as audit for our own use (what did the agent actually do).
- **Confirmation by reality.** The mechanical layer can be confirmed by probe success — the recorded commands actually running is the strongest confirmation any config layer can carry. This is why the mechanical layer climbs the ratchet fastest of the three: conventions and intent cannot be confirmed by reality and depend on human confirmation as the only available authority.
- **The interview is progressive, never finite.** Up-front elicitation captures the top of the tree; leaves are captured just-in-time as work happens. The intent layer fills out with usage rather than as a setup tax. Initial onboarding stops asking when the top is covered; the rest fills as the tool is used.
- **Value-return per exchange.** Each elicited piece changes the next interaction immediately (capturing the mission tethers the next generation to it; capturing an area's priority order is what the mediator uses in the next tradeoff). The tenant should never feel they have given several answers and gotten nothing back.
- **Proposal-confirmation vs free elicitation.** Lean proposal-confirmation where evidence exists (low friction); free elicitation where it does not. Pure either is wrong — free is the blank-page problem; pure proposal-confirmation anchors the tenant on the agent's guess. Mitigation: proposals cite their evidence so a wrong inference is contestable.
- **Capture vs use are separate roles, separate agents.** The intent-elicitation agent captures intent (onboarding); the user-rep advocate uses intent (runtime decision). Same concept, two roles. Keeps elicitation unpressured by live decisions and advocacy undistracted by capture.
- **The dependency graph is the flow plan.** Sequencing follows dependencies, not process preference: run in series where outputs feed downstream, run in parallel where they don't. Reading the dependencies *is* designing the flow.
- **Onboarding never reaches "fully done."** Active-with-pending is the steady state. Maintenance takes over from "enough to use" rather than "complete." This is the progressive principle applied to the lifecycle, not just to the interview.
- **The orchestrator coordinates and surfaces; it does not extract.** Same boundary applies to any layer-coordinating orchestrator: dispatch to the specialists, route gates to the human, surface state — but do not start doing the specialists' jobs, or boundaries blur and the orchestrator inflates.
- **Same code for our use and for tenants.** Only the entry point and isolation context differ. The engine/config split principle applied to the orchestrator itself.
- **Maintenance is where the ratchet gets its long-run signal.** Drift detection is not a separate upkeep function from the bidirectional trust ratchet — it is the source of evidence the ratchet acts on. Without something playing this role, the ratchet has nothing to move on. Immediate verification pass/fail is the wrong timescale for trust; sustained no-drift and repeated-drift are the right ones.
- **Drift triggers follow the change layer.** Diffs to docs/code/objective-tree nodes are the natural event source; the periodic sweep catches what events missed. No separate sensor — the change layer is the source.
- **Targeted re-validation, not full sweeps.** When something changes high in a tree, check only the descendants whose entries reference or override the changed dimension. Efficient maintenance is implicated-only-recheck — the priority-profile maintenance pattern generalised across the config.
- **Surface drift in priority order, not all at once.** Over-surfacing causes alert fatigue and the signal is ignored; under-surfacing lets drift accumulate. Critical drift immediately; low-impact drift periodically; the tenant's attention is earned, not consumed.
- **Contracts are the bottleneck for coherent implementation.** Agents specced individually can each look fine while quietly disagreeing on the shape of the artifact they all produce or consume. A system-view check is what surfaces these — and the shared contracts have to be settled before implementation, not as you go. The load-bearing one is usually the artifact every agent touches (here: the active config).
- **Compute on read for freshness; log the result at point of use for retrospect.** Derived values (the effective priority profile, the constitution view, the applicable-standards bundle) are computed fresh on each read so ancestor changes propagate automatically without stored derivations going stale. But the result *at the moment it was used* is recorded in the decision log so the historical state is reconstructable. The active value is always fresh; the log is audit, not source — it wasn't authoritative when written and isn't authoritative now. Combined with versioned atoms, this gives full retrospective reconstructability without poisoning the present.
- **When compute-on-read is too expensive, store the value but keep its inputs authoritative.** The middle path between "store (goes stale)" and "recompute every read (costly)": store the derived value for cheap reads (e.g. a capability's ceiling), but treat its inputs (verifiability, containment) as the source of truth, so a change to an input triggers a gated re-proposal of the stored value rather than silent drift. Same discipline as compute-on-read — the inputs are authoritative — differing only on whether the value is recomputed each read or stored and refreshed on input change.
- **State vs history are different artifacts.** State is mutable, derived from history, and is what's *used* at runtime (the trust ledger). History is immutable, append-only, and is what's *audited* (the decision log). Same source, different access patterns. Mixing them contaminates both — reading old state means walking history; reading history means walking history; the present is always the present.
- **The bidirectional ratchet is asymmetrically governed.** Tightening (de-graduation, lowering trust) may auto-apply with notification when evidence is severe. Loosening (graduation, raising trust) is always confirm-not-initiate. The asymmetry is the safety property: losing trust is reversible; falsely gaining trust permits mistakes to apply unsupervised. Make it easy to be cautious, hard to be incautious.
- **The ceiling is structural; the level is built.** A capability's ceiling (the max trust it can ever reach) is determined by its verifiability + containment — a property of the capability, not the tenant. The trust level a tenant has reached for it is built by use and lives on the per-tenant ledger row. Same capability, different levels for different tenants, but the same ceiling for all.
- **In-band events close the loop on self-modification.** When the tool itself causes a change (the bundle builder applies a confirmed code change; a layer agent flips a row to active), it emits a change event so the maintenance agent sees its own effects. Otherwise the tool's own changes evade the drift detector and the decision log — the system would become blind to itself. State changes emit; computed-view refreshes don't.
- **The trigger filter is computed from the mechanical config.** Compute-on-read applied to routing — when the mechanical config changes (docs move, new code root added), the filter updates automatically on next event. No separate maintained mapping to drift out of sync with reality.

---

## Process and adoption

- **Two goals on one ramp.** The verification harness and bundle that improve today's workflow are literally the first rungs of the autonomy ratchet — no fork between "fix the workflow" and "automate."
- **Walk one capability through the whole machine before generalising.** Vertical slice first; horizontal expansion on proven rails.
- **Dogfood the ratchet.** Each new piece is itself adopted under the trust model it implements: starts at confirm-every, graduates on evidence.
- **Continuous with today, not a replacement.** The current chat workflow is the starting trust level; adoption progressively automates it.
- **Design for the optimal, deliver incrementally.** Early phases expose the interfaces the cascade / checkers / mediator will plug into, so reaching the optimal is adding components, not rebuilding. The seams are the way to get to optimal sooner.
- **Review contracts against each other, not in isolation.** Each contract can read fine alone while quietly disagreeing with another — a reference shape that fits two tables but not the third, a status used in one place and absent from the enum. The cross-contract reading is the check that catches these; do it before code.
- **Verify reuse claims against live schemas (schema-before-SQL, applied to contracts).** A contract that claims to mirror or extend an existing table is an assumption until the real schema is inspected. Correct the contract to match reality, not the reverse. Design-level reuse claims need the same verification as the SQL would.
- **Checking reuse claims reveals reuse you didn't assume.** Verification isn't only validation — inspecting the real `site_work_items` turned up `approval_mode`, `pipeline`, `depends_on`, retry counters, and `item_key` dedup that the contract had reinvented or omitted. The act of checking often finds *more* to reuse than the claim assumed; budget the inspection as discovery, not a rubber stamp.
- **Reuse-before-recreate applies to control mechanisms, not just functions.** The confirm-not-initiate gate was being designed as new machinery when `approval_mode` already encoded exactly that in the existing table. Before building a gate, a queue, a status lifecycle, check whether the system already encodes it — control mechanisms hide in existing schemas as readily as helper functions do.
- **Design artifacts conform to the house style too.** The chassis uses text+CHECK (not native enums), `version`+`previous_version_id` (not `supersedes`), and `deleted_at` (not `status=archived`). A new contract must match these storage-layer conventions, the same way new code matches the code conventions — the conventions agent's remit extends to the schema layer.
- **Scope a shared shape to what it describes.** The common provenance shape belongs on config-entry tables, not on events, logs, state, or queues. Don't universalise a shared structure onto things it doesn't describe — confirming intentional scoping prevents a later "fix" that forces false uniformity.
- **Checking the real system can prove an assumption wrong, not just a name.** Looking at the real `agent_definitions.capabilities` showed it holds loose descriptive tags, not the references the contract assumed — so the whole idea of how two things connected was wrong, not just a label. Check the real thing before wiring anything to it; you may be wrong about the connection itself.
- **If one shared shape is undefined, look for its neighbour.** The bundle shape was missing in exactly the way the active-config shape had been — one step further along the same path. When you find one undefined shared artifact, check the things just upstream and downstream of it; the same gap tends to repeat.
- **Fail loudly; don't quietly tolerate the unexpected.** The house rule (from the naming work) is that readers should not paper over odd inputs and carry on — that just hides the real problem. New parts (the bundle builder, the confirmer, the trigger filter) should stop and surface an unexpected shape, not normalise it away.
- **A strong rule usually has a scope; don't stretch it over everything.** "One path to active" covers the config tables; the trust ledger is a separate domain with its own gated flow. The asymmetric de-graduation isn't a hole in the rule — it's outside the rule's scope. When a rule feels universal, name what it actually covers.
- **In a system where behaviour is stored as data, "the code" is partly in the database.** Here workflows are jsonb on `agent_definitions`, tools are rows in `content_components` at tool level, prompts are text columns. So the context a task needs includes definition rows, fetched like source files, and the reuse-search has to cover them — the common duplication isn't a repeated function, it's a near-copy of an existing workflow or agent. (And as ever, these live where the schema says, not where you'd guess — there's no `workflows` table.)
- **Reading the system's own design is not sensitive; reading content or customer data is.** Split database reads by kind: definition data (the design) and operational data (the telemetry) are routine context for our own use; content/customer data is the smaller, gated set where privacy matters, mainly in the service. Gate the minority, not the common case.
- **Probe before you fetch; check the result against the gates.** You can't know a query's result size until you run it, and results are often bigger than expected. So the flow is multipass: probe (count + shape), check against size and sensitivity gates, then fetch the right form — full, a sample, an aggregate, or just a pointer ("N rows matched, here's the query"). An oversized result becomes a summary or a pointer, never an unbounded dump. This is "inline what's needed, point at the rest" applied to data.
- **For debugging, the run narrative beats the row state.** What a task usually needs is not "what does this row say now" but "what happened on this run" — the spawn tree, the step sequence, the errors, the logs, the messages. Tie it all together by the run's correlation key (here, `orchestration_id`) so the evidence is one coherent run, not scattered lines.
- **A healthy-run signature turns log-reading into diff-reading.** Capture the expected step sequence and spawn-tree shape of a good run, then on failure diff the actual run against it and show where they diverged. It localizes the break instead of making someone scan everything — verification applied to runtime behaviour, not just to code output.
- **The debugging guides you already wrote are authored knowledge to capture, not just docs to read.** Known failure fingerprints (a signature + the commands that confirm it + the fix) have the same shape as standards and should be surfaced for the matching task the same way. Seed them from the existing guides; grow them as run-signature diffs reveal new ones.
- **A capability's availability scales with what the codebase provides; discover the structure, don't assume it.** Many of the useful capabilities (definition-data fetch, run traces, run signatures, log correlation) only work if the codebase has a particular structure — a correlation key, telemetry tables, named logged steps, data-stored behaviour. Those are rich on our own codebase, so build the optimal version against it first — but they are facts to discover per codebase, recorded in the config, with each capability degrading (to a weaker form, or "unavailable because no X") rather than breaking where the fact is missing. Don't bake your own codebase's shape into what should be generic engine capabilities.
- **A capability that rests on a manual convention is only as reliable as the convention's coverage.** The run trace depends on every agent logging the run id — a convention that's mostly followed but never audited. So the reliability question isn't "is this the rule" but "how completely is it actually followed," and the gap is fixable (add the missing log lines), not a hard limit. This holds even on the home codebase, where the structure exists but the enabling convention isn't verified everywhere — which makes convention coverage a thing the drift audit measures, not assumes.

---

## Building discipline (edge cases caught before building)

- **Self-referential structures need a cycle guard.** Trees (parent links), version chains, and related-links can loop if one bad edit gets in, and a root-to-leaf walk over a loop hangs forever. Check for cycles on write, and make the walk detect-and-fail as a backstop. A human confirming the edit doesn't prevent this — they can confirm a loop by mistake.
- **A multi-step apply must be all-or-nothing.** When applying one change means several writes plus emitting an event, a crash in the middle leaves a half-done state (a live row with no log entry; no event, so nothing downstream notices). Put the writes in one transaction and the event in an outbox written in the same transaction, so a crash leaves a clean state and a retry finishes the job.
- **Read a consistent snapshot when you assemble from several tables.** If you read table A, then table B, and a change lands between, you get a mix that never coherently existed. Read the set at one point-in-time.
- **One live thing per target, all the way down.** A dedup on the queue isn't enough if the underlying proposed rows can still pile up. Extend "at most one live X per target" to every layer, and clean up the underlying row when the queue item is expired.
- **Bulk operations need bulk confirmation.** A gate that asks for one confirmation per item becomes a wall when something produces a large batch at once (the first onboarding). Keep the gate, but let a whole reviewed batch be confirmed together; per-item confirmation is for the steady trickle, not the initial flood.
- **Don't let transient or infrastructure failures lower trust.** A flaky test or an infra blip isn't a regression. Filter those out before any evidence is allowed to reduce a capability's trust, or one blip collapses trust and triggers a confirmation flood.
- **Tell "not yet" apart from "broken."** Missing data because something hasn't been onboarded yet is normal and should degrade gracefully; missing data because something is malformed is a fault and should fail loudly. The fail-loud rule is for faults, not for not-yet-authored.
- **Derived indexes and caches go stale silently.** A search index over code or data gives wrong answers, not errors, when its source changes. Refresh it on change and stamp it with a freshness time so it can say how current it is rather than imply it's live.
- **Config and deliverables are different gated paths.** Changing the tool's own knowledge of a codebase goes through one gate (confirm-not-initiate); the tool's actual outputs — code, and here workflow/agent edits — go through a different one (trust level + verification). Don't build one gate for two jobs.
- **Scope a guard to exactly its target.** The "don't re-check what was just confirmed" guard must exempt re-checking *that entry*, not the legitimate downstream effects of it (confirming a new rule should still trigger checking existing code against the rule).
- **Probe by limit, not by count.** To learn whether a result is too big, fetch one more than the cap and see if you hit it — counting a filtered query is as expensive as running it.
- **Log what was actually shown, not what was assembled.** If the thing handed to the model is a rendering of the structured context, take the record of "what it saw" from the rendering, or an audit will claim it saw things the rendering dropped.
- **Don't force a reasoning premise on a mechanical step, and don't log the logging.** A mechanical assembly has a rule-trace, not a reasoned premise; record what it actually has. And writing a log entry is a side-effect of a decision, not itself a decision — no recursion.
- **Recovery must not depend on the thing being recovered.** A tool that can change the platform it runs on can break its own ability to run. The rollback path must work without the tool — no routing through the agents it would be rolling back.
- **Author the vocabulary before classifying into it.** The fixed sets things get sorted into (the concern taxonomy, the priority dimensions) must exist before the agents that sort into them can run. Seed them first.
- **Validate the premise with a thin slice before building the infrastructure for it.** A pile of well-specified machinery can all rest on one unproven assumption. Before building it, run the cheapest end-to-end slice that tests the assumption directly (here: hand-write a flat constitution, assemble one bundle, paste it in by hand — no contracts). If the premise holds, build the infrastructure to scale it; if not, you've saved the build. Good specs keep; the slice de-risks.
- **The loop runs on outcomes — make sure outcomes are recorded, not just inputs and decisions.** You can log every input and every decision and still have no feedback signal. A trust ratchet or improvement loop needs the *result* (did it pass, get reverted, need a human fix) as a first-class record, separate from the decision and its inputs. Without an outcome record the loop has nothing real to move on.
- **Blast radius caps trust.** The more a capability can break, the lower its ceiling — regardless of how verifiable it is. A capability that edits the system itself (low containment, can break everything including the tool) should never be fully autonomous; one whose effects are contained can graduate higher. Containment, not just verifiability, sets the ceiling.
- **Self-modification is a residual risk you manage, not solve.** A system that changes itself can drift past its own checks — gradual degradation that still passes verification, not a crash. Manage it with conservative early trust, a human in the loop, external rollback, and a low ceiling where the blast radius is the system itself; don't assume the safeguards close it completely.
- **Record what actually went in, don't plan to reconstruct it later.** You can often rebuild "what context did this have" by replaying the assembly from the logged inputs — but only while the assembly logic is unchanged and the sources still exist. Change the logic later and the replay gives you today's result, not the one that was used. A manifest captured at the time (the filenames, ids, versions, queries that actually went in) is more robust and cheaper to read than reconstruction. (Same root as "log what was shown, not what was assembled.")
- **A record of what was included can't tell you what was missing.** "Missing" is relative to what should have been there, which you only know in hindsight. So a context manifest makes the "did it have what it needed" diagnosis *possible* (you can see X wasn't there) but doesn't perform it — you still supply the knowledge that X was relevant. Necessary, not sufficient; don't expect it to flag its own gaps.
- **A record earns its keep only when the artifact isn't already in front of you.** In the thin slice the bundle is rendered and pasted by hand, so it's fully visible every time — a separate record of it is redundant. Records pay off once the thing becomes automated and unattended. Don't log what you're already looking at.
- **Within a thin slice, build toward the piece that produces the thing under test, and hand-feed the rest.** The assembler (which produces the bundle being evaluated) comes before the schema extractor; feed the schema by hand for the first runs rather than block on building it. Sequence to the artifact you're trying to judge; stub or hand-supply its neighbours.
- **A bigger context window is not the fix for too much context.** Models don't use a full window evenly — accuracy and recall degrade as it fills (context rot), and the very large windows are premium-priced and tier-gated. So a large bundle is a smell, not a goal: include less, don't buy a bigger window. The cheapest, most relevant context usually beats the most context. (Aim to stay inside the standard window; treat needing the giant one as a signal the selection is too broad.)
- **The call graph is a quality lever, not just a size one.** Pulling the functions actually involved (callers/callees) instead of whole packages or directories shrinks the bundle *and* keeps the model in the zone where it uses context well. Size and quality improve together.
- **For stable, reused context, caching changes the economics.** When a large prefix is reused across a session, prompt caching makes re-sends cheap (reads a fraction of base price). Cache the stable parts (rules, reference docs, code context); leave the changing task/question uncached. This shifts the question from "what can I afford to send" to "what should it see" — which loops back to: send what's relevant, not everything.
- **Some features carry liability — treat it as a first-class review concern.** When a generated feature could create legal exposure (claims, advice, regulated or sensitive content), that needs its own review, the way correctness and house-style do — a concern in the standards taxonomy and possibly a dedicated review agent. Consider it when developing new features, not after.
- **A fuzzy brief must be expanded before targets can be resolved.** You can't pick the in-scope files for an under-specified task until the brief is consolidated into something concrete. So the first pass on a vague task is a framing pass (intent and the relevant existing material at a high level) to expand the brief into a spec; only then can implementation targets be named. Target resolution depends on a clear brief.
- **Hand-built context is the spec for automated context.** When experts pack context by hand for a task, that package is ground truth for what the tool should assemble. Checked against real hand-built manifests: the call-graph slice the tool builds is the same artifact they extract by hand (the relevant whole functions, with line ranges, chosen over the full dump); the standing rules they restate at the top of every handoff are the constitution; the docs they attach are the reference layer. Matching expert hand-curation is the validation that the approach is right — and where the hand-built version has something the tool doesn't, that's precisely the next thing to build.
- **The authoritative source must be the one analysed — a stale checkout ships wrong code confidently.** In a deployed, multi-repo reality the current version of some code can live in the running system, not the on-disk tree, and a stale snapshot looks identical to a fresh one. So analyse the current source of truth and record what was analysed (the commit/SHA), so staleness is visible rather than silent. Same family as the stale search index: derived-from-stale-source is silent wrongness.
- **Live state and run evidence are confirmed-essential for debugging, not speculative.** Every real debugging task's hand-built manifest reached for live table content, a fresh schema dump, and run/error/pod state — the operational and runtime-evidence layers. They are core to a debug bundle, not nice-to-haves; the thin slice defers them, but the need is real and proven.
- **A past failure mode can become the intended mechanism when the goal changes.** The image model answering "several icons" with one gridded image was a defect when you wanted separate icons; for "make one coherent sheet and slice it" that same grid is exactly what's wanted. Before working around a tendency, check whether a reframing of the goal turns it into the feature. What you fight under one objective you can harness under another.
- **Prefer the option that deletes a component, not just the one that works.** Choosing CSS `background-position` over physically cropping the sheet didn't merely pick a styling approach — it removed the need for any image-cropping step at all. When comparing routes, weight the one that subtracts a part to build and maintain; the cheapest code is the code you don't write.
- **Build the general structure, exercise the simple case.** Support the fuller shape in the data model and code paths (many sheets, finer scope) but run only the one simple instance (a single site sheet) to start. This keeps the first version simple to ship and reason about while leaving the growth path open — extension without the cost of building every branch up front. (A sharpening of "design for the optimal, deliver incrementally": the abstraction is cheap now, the unused branches are not.)

### Reuse-checking (finding code that already solves the problem)
- **Checking for reuse is a retrieval problem with a judgement tail, not a generation problem.** So most of it is algorithmic: with a maintained index (signatures, types, call graph — the analyser already half-builds this), "have we solved this?" is a query, and the model sees the query's results, not the whole codebase. The real question isn't "how much code can we afford to send", it's "how good is the index and retrieval". That dissolves most of the "we need access to huge swathes of code" premise — you need a query interface, right up until the final call.
- **Split "identical" from "similar" — they're different mechanisms.** Near-duplicate detection is algorithmic and high-precision (token/AST fingerprinting, structural hashing) — no model needed. "Similar / something to build on" is semantic and fuzzy: same problem, different names and structure, which lexical and structural matching miss — that's where embeddings (cheap, self-hostable, a vector op not a generation call) earn their place. One mechanism doesn't serve both.
- **The cheapest reuse check is a maintained capability catalog.** A vocabulary of what the system can already do turns the first reuse question into a lookup, not a code search. Cost is keeping it current; payoff is short-circuiting most of the work.
- **Recall is the whole game; a pre-filter that drops the real match is worse than no check.** The model then never sees the match and writes a confident duplicate — exactly what the rule exists to prevent. So tune every narrowing layer for recall over precision (fifteen candidates including the right one beats five that exclude it), and treat a reuse check with silent false negatives as actively harmful (it manufactures false confidence), not merely unhelpful.
- **A cheap/self-hosted model should narrow, not decide.** Its job is recall-preserving compression of the candidate set; if it makes the reuse *decision*, the hard judgement has moved to the weaker model and its drops are unrecoverable downstream. Narrow with the cheap layers, decide centrally with the strong model on a small shortlist seen in full.
- **Split the check by timing: fuzzy retrieval before generation, precise clone-check after.** Near-duplicate detection works far better against a concrete draft than a fuzzy intent (a real artifact to fingerprint), so run it post-generation as a pre-merge guardrail; run the fuzzy "is there something to build on" retrieval pre-generation to inform the work. Some of the check belongs after the model writes, not before.
- **A reuse index rots like any derived-from-source artifact.** (Same family as code-freshness and the stale search index.) Stale index → false negatives → can't find code added recently. It needs incremental refresh on change; the heuristic approach trades context cost for index-maintenance cost.
- **You can't tune a reuse finder's recall without ground truth, and the dangerous error is invisible by construction.** The missed match leaves no trace. Get a test set of known reuse cases (past duplications caught in review are the obvious source) or you're blind on the one number that matters.
- **Justify the pre-LLM heuristic layer on quality, not token cost.** Caching and large windows make raw volume cheaper than it feels, but a stuffed window is used worse (context rot), so the case for narrowing is recall + less rot. And don't reach for a model where a deterministic score already separates candidates — sequence the layers (catalog → lexical/structural → embeddings → cheap rerank) and stop as soon as one is good enough.
- **Signature+docstring embeddings are a retrieval substrate, not a dedup optimisation.** Their biggest use is target resolution — task → candidate in-scope set, the largest manual step in the tool; reuse-finding is just one query against the same vectors. They also catch semantic neighbours the call graph misses (related problem, no call edge between them), give "where do we handle X?" navigation, and feed capability-catalog curation (cluster symbols; flag code that maps to no cluster). So build it once to serve all of these, not narrowly for duplication — and "leave it as an optimisation for later" is the right timing but the wrong category: it enables retrieval the deterministic layers can't do, it isn't speeding up something that already works.
- **Defer embeddings as the *next retrieval layer*, not as a nice-to-have, and add them only when measurable.** Sequencing: get the deterministic baseline in first (name/lexical + structural + call graph) — it may cover much of target resolution and candidate-finding for free; add embeddings when it demonstrably misses, with a ground-truth task→files set so you can tell they actually beat the baseline. The trigger that flips it from "later" to "next" is starting to automate scope selection.
- **The cheap signature+docstring embedding is only as good as the docstrings.** A task phrased by behaviour won't sit near a function whose docstring doesn't describe that behaviour, and much code has thin/what-not-why docs. If retrieval underperforms for that reason, the fix is richer input (the body, or an LLM-generated one-line what/why per symbol) at higher cost — decide that from measured recall, not upfront.
- **At this scale embeddings need no vector database.** A few thousand symbols is a small matrix; brute-force cosine in memory over a stored vector set suffices. The standing-infrastructure cost is smaller than it sounds — the real costs are the embedding model and keeping the vectors fresh as code changes (the index-staleness problem again).

### Testing the tool (evidence, not just design)
- **Design has outrun evidence — the next high-value step is a real run, not more design.** The approach is validated by reasoning: against the hand-built manifests, and by writing the context packs by hand. But the assembler has never been run on a real task and its output read, which is the thin slice's entire purpose ("does the assembled bundle beat hand-curation, and what does it miss"). Past this point more design has diminishing returns until a bundle is actually produced and judged.
- **With a hand-built baseline and a tool, the test is the diff between them.** Run the tool on a task you also have a hand pack for, and compare. The places you'd add or drop scope are the target-resolution signal; gaps in the neighbourhood are the call-graph's limits (e.g. interface dispatch); wanting live rows over schema is the data signal; a bloated bundle is the "scope too broad" signal. The delta points to where to invest far better than more upfront design does.
- **Keep the tool as the subject and use a live task as the probe.** A real task (imagery) earns its place as the example that exercises the tool and surfaces its gaps — not as a feature to finish. The task's job is to make the tool's weaknesses visible.
- **The existing hand-built gather script is a coarse version of the whole bundle.** `package_page_build_debug.sh` concatenates a hand-picked set of directories + files *and* appends a read-only live capture (schema, the decisive queries, the agent-definition workflows, pod state). That is code + schema + live data + definitions + runtime in one file — almost exactly what the tool is meant to assemble automatically. The tool is the leaner, automated version of the same artifact; the one piece the script omits is the constitution (it leans on the handoff for standing rules). The hand script is the spec *and* the working fallback.
- **The first real comparison cut both ways — and that's the finding.** Run against skinner-box, the coarse whole-directory gather pulled 110 files when the task turned on ~14 (over-includes: test files, an example doc, unrelated utilities → 1.7 MB), but it also **caught wiring the narrow scope missed** (`registry.go`, the page-build action neighbours) because it deliberately keeps the shared/registration layers for reuse-discovery. So completeness and leanness genuinely trade off; neither broad-directory nor narrow-naming alone is right. This is exactly the scope-delta and neighbourhood-adequacy signal predicted for a first real run — evidence, not design.
- **The call graph's specific blind spot is registration/wiring code.** A name-matched call graph won't reach `registry.go` and similar, because registration happens through an init/registry mechanism, not a call from the in-scope symbol. So the assembler needs a force-include for wiring/shared files (the way the script keeps them for reuse-discovery), or the user adds them by hand with `-scope`. A concrete, durable instance of "the call graph misses things", alongside interface dispatch.

### Shape of the eventual tool (so trial pieces graduate, not get thrown away)
- **A bundle has two halves; one tool makes each.** The gather script produces the code + data + runtime + definitions half; the handoff/pack produces the framing half (task, next action, the decisive fork, standing rules, what's already resolved). Neither alone is sufficient context for a fresh chat — together they are the bundle. This is the constitution+task vs code+data split, seen in practice: a fresh chat with only the gathered code/state could re-derive the diagnosis but would lack the prescribed move, the fork, and the rules.
- **Two goals diverge; name which you're serving.** Getting a task done (use the ready script + handoff) and advancing the tool (run the assembler on the same task and learn from the diff) are different objectives. The assembler is currently the *experiment*, not the shortcut — leaner but less complete (misses wiring). Don't conflate "do the task" with "test the tool"; decide which the current move is for.
- **The eventual tool is workflow + spawned-agents + optional HITL + front-end + generic — so anchor the thin-slice CLIs as prototypes of specific actions.** The mapping: analyser → an indexer action (+ a refresh trigger on change); assembler → a code-context action, with target-resolution as a sibling and the first natural HITL stop (confirm the scope before assembling); dbcontext → a data-context action (the multipass probe-gate-fetch); the pre-merge near-duplicate review, the reuse review, and liability review → build-time review contributors; the constitution → the active-config (tenant rules/standards/objectives). Each responsibility is a separate spawned agent/container so the logs and responsibilities stay distinct, replying on the caller's responses topic, as the platform already does. Genericity comes from per-tenant config plus pluggable per-language analysers (Go now via go/ast, others later through the same bundle contract — the way deploy already has github-now / other-adapters-later). Build each trial piece in this shape so what a trial teaches (e.g. the wiring-include) lands as a feature of the action that ships, not as throwaway scaffolding.
- **Two layers, both multi-agent — keep them distinct.** The website-builder (the chassis: adoption, planner, content, research, imagery, rerender, deploy) is the subject the tool helps develop, and a product in its own right. The context tool is the development layer that assembles task context and verifies changes to that builder (and, generically, to any codebase). The builder builds sites; the tool builds reliable changes to the builder. Neither replaces the other; focusing on the tool hasn't dropped the builder — it sits one layer down.
- **The remaining gaps are all "find", not "include".** The tool can include any code (`-scope`) or any doc (`-doc`), but it can't yet *find* the relevant one for a task automatically. Target-resolution (which code), matched-standards retrieval (which guidelines), and the wiring-include (the code a call graph can't reach) are the same missing capability from different sides — selection/retrieval, not inclusion. Concretely for documentation: the analyser reads code only; guidelines enter via the always-on constitution plus manual `-doc`; auto-selecting the relevant guideline is the deferred standards/docs contributor — a named-but-hand-filled slot in the bundle.
- **"Different angles" are all contributors; "checkers" are a separate concept.** The build's different angles are two kinds of *contributor*: context contributors each assemble one slice of the bundle (code, data, runtime, standards), and build-time review contributors each review the proposed change from one angle (reuse, duplication, liability, morality, correctness), raising a concern that revises the change or gates via HITL. Separate containers keep each angle's logs and responsibility distinct. **Checkers are not these:** in the web tool a checker is an improvement-loop agent that continuously monitors *deployed* sites against the plan/spec (the existing `check_*.go` family), usually with a fixer — it runs post-deployment, on a loop, in the builder/operate layer, not in the build path. Whether the initial builds reuse some of the same checks is unconfirmed and is to be investigated separately. (The website-builder layer also has its own context-angle agents — research, content, design, imagery — same pattern, lower layer.)
- **A morality review is a distinct angle from liability, and must apply a configured standard, not a baked-in view.** Liability asks "will this get us sued or fined"; morality asks "is this right" — they overlap but some legal things are still wrong. The tool must not hard-code one moral view: the morality review (a build-time contributor) applies a *layered, configured* standard held in the active-config — a base framework the operator chooses (for marketing sites, advertising/consumer-protection standards like the UK ASA/CAP code and CMA guidance, plus recognised AI-ethics frameworks for the generation angle), the operator's own judgement on top with priority, and later jurisdiction/government guidance and a current-focus overlay as higher-priority dated sources. It runs at two altitudes (per-output, and a vertical-level gate at intake on the domain/objective), and because moral calls are contested it routes hard cases to a human (HITL) rather than auto-resolving. The tool applies the configured standard and flags; it is not the moral authority.
- **A trial finding can land as a tool feature, as intended.** The first adoption run showed the call graph misses wiring/registration files (`registry.go`); that taught the rule, and the rule shipped as the assembler's `-include` flag (force-include named wiring/shared files regardless of the call graph). The trial's output was throwaway; the capability it specified is now in the tool — the "build trial pieces so they graduate" principle, demonstrated.
- **First real assembler run, on the actual adoption code, confirmed the thesis and the limits.** Scoped to the two decisive functions (`LoadPageSectionsFromSpecAction`, `PlanSectionsAction`) with `-include registry.go`, it produced a ~30 KB bundle against the script's ~1.7 MB (≈57×), and the call graph surfaced exactly the readiness/section-resolution helpers that matter (`planSection`, `createDeferredItems`, `loadComponentSchemas`, `resolveSectionComponent`, …) — neighbourhood adequacy confirmed for this task, with the `registry.go` wiring now present via `-include`. Two honest caveats: the neighbourhood is only as complete as the analysed tree (six files here, so it's a floor — a full-tree run would also pull callees living in other files), and the genuinely-missing contributor for a debug task is runtime evidence (the run trace/errors the script's live-capture block carried but the assembler still stubs).
- **Runtime evidence is built, by extending dbcontext rather than adding a tool.** `dbcontext -runtime-site <domain>` (optionally `-runtime-page`) pulls recent `agent_error_log` rows and the `site_work_items` lifecycle and renders a "Runtime evidence" block; the assembler's `-runtime <file>` slots it into that section. It reused dbcontext's psql/CSV/table machinery (reuse before recreate). On the skinner-box data the lifecycle it pulls *is* the diagnosis — the timed-out original item and the empty-but-`complete` retry. The two tables it queries (`agent_error_log`, `site_work_items`) are the ones whose schema is confirmed; the run-state table (`orchestration_states`) is left for `-rows` until its schema is verified.
- **Target resolution has a deterministic baseline before any embeddings — and it shows its own recall gap.** `resolve_targets.go` scores symbols against the task by name/path/doc word-overlap and proposes the `-scope` set (plus wiring `-include`s by detecting registry-like files). On the skinner-box task it ranked `PlanSectionsAction` first and surfaced the whole section-planning cluster and `registry.go` — but it ranked `LoadPageSectionsFromSpecAction` (also decisive, name echoing "load/spec" not the task's "plan/ready/count") below the cut. That miss is the exact recall gap embeddings fill, and the baseline says so itself. Sequencing held: lexical first (cheap, no infra, and useful), embeddings later when there's a ground-truth set to prove they beat it.
- **Omitting bodies shifts the burden of completeness onto the assembler — the complement is giving the model a retrieval tool, not abandoning curation.** Signatures-not-bodies buys focus and token-efficiency but removes the model's ability to grep the codebase itself; if the bundle omits a body it needs, it's stuck, and the onus is on us to have included the right files. The fix isn't to dump more — it's an on-demand search/read tool the model can call to fetch what the seed bundle lacks (the index, call graph, body-by-line-range, `dbcontext`, and the lexical resolver are already its backends). This is how capable coding agents work: a small seed plus tools to pull the rest. Not a bad idea — a good one — with caveats: it complements curation rather than replacing it (a good seed means fewer fetches); the same size discipline applies to fetched results (probe, signatures first, bodies only when needed) or context rot returns; it should offer both lexical and semantic search (so it's the same investment as the embedding layer, seen from the agent's side); read-only and tenant-scoped; and record what was fetched (provenance). It also shifts the shape from "assemble-and-paste a bundle" to "assemble a seed and serve tools" — which is exactly the agentic/workflow form the tool is heading toward, so it's a direction to take when the tool becomes agentic, not a contradiction of the bundle approach.
- **Stay foundation-model-agnostic; the embedder (and any model call) sits behind an interface, and we have our own models to serve it.** The platform has a small Ollama CPU model (the `ollama-adapter` / `ollama-eval` pods) for cheap, local embedding and inference, and Thunder GPU (the finetuning-flywheel project) for heavy/bulk work. So the model-dependent layers can use a real self-hosted model (e.g. `nomic-embed-text` via Ollama) rather than a stand-in or an external API — and because it's behind an `Embedder` interface, the model stays swappable. Model-agnosticism is a hard constraint, not a preference: don't couple the tool to any one provider's mechanisms.
- **The embedding layer is built (`embed.go`) — mechanism verified, real recall pending a model run.** `embed build` indexes the analyser's symbols (name + kind + signature + doc + path, embedded then L2-normalised) and `embed query` returns cosine top-k for a task; two embedders — an Ollama client (POST `{model,input[]}` to `/api/embed`) and an offline token-hashing stand-in. Tested here with the stand-in (proves index→cosine→rank→suggested-scope) and against a stub HTTP server (proves the Ollama request/response handling, batched build + single-text query). The stand-in is *not* semantic, so the recall gain over the lexical baseline is only demonstrable by running `query -ollama` against the real model, and only *measurable* once a ground-truth task→files set exists. Index and query must use the same embedder (same vector space). Next: merge the lexical and semantic candidate sets into one `-scope` proposal.
- **PARKED for a dedicated discussion: provider-native tool use vs a model-agnostic second round-trip.** The agentic search-tool direction would normally use the model's native tool-calling, but that is provider-specific and breaks model-agnosticism. The model-agnostic alternative: the model states what it needs in its text/JSON output, the orchestrator fulfils it via our tools, and feeds it back on the next API turn — no provider tool-calling. Attractive because it keeps agnosticism and full control over what's fetched (so the size discipline still applies); the tradeoffs are added latency (full round-trips vs streamed tool calls) and needing the model to emit a reliably-parseable need-spec. Flagged, not resolved — to compare properly when the tool goes agentic.
- **The merge and the measurement harness are built — and the measurement immediately corrected an assertion.** `fuse.go` combines candidate lists by reciprocal-rank fusion (rank-based, so the lexical integer scores and semantic cosine scores need no common scale); `eval_targets.go` scores any candidate list against `groundtruth_targets.json` (recall@N, MRR, hits/misses), with suffix-tolerant path matching so one ground-truth file works for both a flat dir and the full tree. Running the whole chain on the adoption analysis showed three things worth keeping: (1) the lexical resolver actually surfaces *both* decisive symbols within top-12 (`PlanSectionsAction` #1, `LoadPageSectionsFromSpecAction` #12) — so my earlier "it misses it" was too strong; it gets it but borderline, which is the real thing the semantic layer should improve (rank it robustly, not at the cut). (2) The offline stand-in is non-semantic noise (recall 0/2), confirming it is only good for plumbing, never for judging quality. (3) **Fusing a strong list with a weak one degrades the result** (fused recall 0/2): plain RRF rewards agreement, so a decisive item only one list ranks high gets diluted. The consequence: the merge cannot be validated with the stand-in — it needs the real Ollama list, and if one list dominates we should weight the fusion rather than use plain RRF. This is the "measure rather than assert" principle paying for itself on the first run.
- **The toolset is now one module with the contracts defined once, not seven files re-declaring them.** The duplicate names (`analysis` ×3, `funcDef`/`typeDef` clashing between analyser and assembler, `candFile` ×2) were the visible symptom; the real issue was the same two contracts — the analyser's output shape and the ranked-candidate shape — hand-redefined in every consumer. Fix: a `contextkit` module with `internal/analysis` and `internal/candidates` holding each contract once, and each tool as its own `cmd/<name>` command (which also dissolves the `main`/`funcDef`/`typeDef` clashes, since each command is a separate package). Consumers reference the shared types by qualified name (`analysis.FuncDef`, `candidates.Candidate`) — no per-tool aliases, one unambiguous source. `dbcontext` shares neither contract, so it imports neither. When these graduate into the chassis, the same two packages move under the chassis module path; nothing else changes. Run shifts from `go run x.go` to `go run ./cmd/x` / `go build ./...`. (An intermediate version used local type aliases to keep the diffs tiny; the qualified-name form was chosen as the final state for a single, explicit reference to each contract.)
- **The target shape in the chassis is an index/build split, recorded in its own living plan (`PLAN_workflows_and_actions_migration.md`) — discussion-first, unknowns before code.** When `contextkit` moves into the chassis: a durable **indexing** workflow (commit-triggered, SHA-keyed: code structure + embeddings) feeds a per-task **build** workflow (resolve targets → optional HITL scope confirm → gather context slices → assemble → generate → review → emit). The action-vs-agent rule: something is an **agent** when it should be a separately-logged orchestrator that can recurse or gate at HITL (the context contributors and the build-time review contributors), and an **action** when a step-level log entry over pure compute is enough (`resolve_targets` with lexical+semantic+fusion folded in, `assemble_bundle`). No subworkflows in SQL — separable concerns are spawned sub-agents replying on the parent topic. Reuse the chassis DB layer, the ollama-adapter, the registry, the spawn/topic wiring, the generation adapter, and artifact upload rather than the thin-slice stand-ins; `eval_targets` stays offline as the measurement harness. The plan deliberately front-loads the doubts — the workflow-definition and orchestration schema are the gate (we've only inferred them from step names), and the parked on-demand-context-vs-assemble-then-send fork has to be settled because it decides whether gathering is a fixed sequence or a loop. Nothing is built; the next move is confirming the live-system schema/APIs, not writing workflows.
- **The workflow/agent model is now grounded in the guide docs (001/002), and it corrected the migration plan.** Workflows are declarative JSON in `agent_definitions.default_config` (templates_db); steps are `{action, config, next_step, output_field}`; there's a generic action library to reuse before writing Go (`query_database`, `spawn_agent`, `call_agent`, `loop`, `conditional`, `rag_lookup`, the work-item lifecycle). Two corrections to what I'd proposed: the chassis deliberately **groups** LLM-assisted checks into group-agents by shared context (one context load, one call per group) rather than making each its own agent — so the build-time reviews are a group agent, not four separate agents — with a **promotion pattern** to spawn a sub-agent only when one check needs independence; and much of "gather/generate" maps to existing generic actions, so there are fewer new Go actions than first listed. The most important reuse check is `rag_lookup`: a retrieval action already exists and may overlap with `embed`/`resolve_targets`, so confirm-and-reuse before building a parallel index. The remaining genuine unknowns shrank to the run-trace tables (for runtime evidence) and whether a HITL pause/resume step exists. Lesson reinforced: read the guides before proposing structure — half my "open questions" were already answered there.
- **Real `agent_definitions` rows + the action code made "what is an agent" concrete, and produced two decisions.** An agent is a DB row whose `default_config.workflow` is a step graph; the Go code holds *actions* (`SpawnAgentAction`, `CallAgentAction`, `ExecuteLLMPromptAction`), never agents — which is why it isn't obvious from a code read. `spawn_agent`+`call_agent` are a pair (spawn creates the Job and wires the child's parent-responses topic; call sends the work), and an agent's description can contradict its config (`build-dispatch-loop` claims "no loops or sub_workflows" but its config has a `loop`+`sub_workflow`) — folded into debug guide §6.0. Decisions: (1) the build's **gather phase is a loop**, not a fixed sequence — tasks get long, and the `loop` action + existing `call_agent`-substep loops + `rag_lookup` are precedent; the model-agnostic "ask for more" is still the parked second-round-trip, now scoped to the loop with a budget/termination to define. (2) **CPU Ollama must be feasibility-checked early** on speed (embed the whole chassis; per-query latency) and quality (beat the lexical baseline on the ground-truth set) before the embedding layer leans on it — Thunder GPU or a different model if it falls short, and reuse `rag_lookup`'s embedder if it already has a workable one.

## DB discipline

- **Snapshot before any `agent_definitions` change; check `\df` for a helper before hand-writing SQL.** The DB carries helper functions — `snapshot_agent(p_agent_type)` and `snapshot_agent(p_agent_type, p_reason)` (both return uuid), with `revert_agent` to roll back. Run a snapshot before *any* change to `agent_definitions` (config patch, migration, manual edit); it's cheap insurance against the patch-contamination / failed-revert footguns. More generally, reuse-before-recreate applies at the DB layer too: before writing raw SQL for something routine, run `\df` and see whether a function already does it. Recorded in debug guide §0 (item 24) and §6.2. **To do (long-term):** capture the full live `\df` output into §6.2 — it currently holds only the confirmed helpers as a scaffold; we should keep a real inventory in the docs so helpers are discoverable rather than re-derived each time.
- **pgvector appears installed.** The same `\df` snippet listed `sparsevec` (a pgvector artefact), so pgvector is likely present — confirm with `\dx`. If so, embedding storage should use pgvector columns + similarity operators from the start rather than in-Go cosine, which also resolves the at-scale storage question (migration plan B4/B4a).
- **DB capabilities (`\dx`/`\df`) are captured *context*, not memory — and the capture point is `dbcontext`, not the analyser.** Reuse-before-recreate at the DB layer only works if generation can *see* what exists: installed extensions (pgvector available) and helper functions (reuse `snapshot_agent`, don't hand-roll a backup table — the migration-110 footgun). So the bundle should carry `\dx`+`\df` for DB-touching tasks. Built in the harness: `dbcontext -capabilities` runs both (same psql path it already uses for `\d`), and the assembler gained a `-dbfacts` **Database capabilities** section that includes the capture, with a reuse nudge when absent. Clear division: the **analyser is code only** (Go-AST, language-agnostic contract); **DB facts live in `dbcontext` + the assembler section** — don't conflate them. The durable step (recorded as plan B2a): fold the capture into the indexing workflow on a *migration cadence* (these change on migrations, not every commit) so the bundle always carries current capabilities without anyone remembering a flag; gate by task type for size. Debug-guide §6.2 is the human-readable twin.
- **The chassis already has the embedding/retrieval layer I was prototyping — reuse it, don't parallel it.** `rag_actions.go` shows `rag_index`/`rag_lookup` running `nomic-embed-text` via the `ollama-adapter` (with the asymmetric `search_document:`/`search_query:` nomic prefixes) into a **pgvector** store (`knowledge_base`, cosine `<=>`, idempotent dedup on `content_hash`), with a trigram fallback. So pgvector is confirmed in use (closes the storage question), and `embed`/`fuse`/`resolve_targets`/`eval_targets` are repositioned as the lexical layer + fusion + offline measurement, not a parallel vector store. The one genuine misfit for code: `rag_index`'s character chunker fragments functions — code must be chunked at **symbol boundaries**, which the analyser already gives. So contextkit indexing = analyser symbols → nomic prefix → the *same* embedder → a `knowledge_base`-shaped store. Made `embed` faithful (added the nomic prefixes mirroring `applyNomicPrefix`) so the CPU-Ollama feasibility run measures nomic the way the chassis actually uses it. The two checks merge: running feasibility through the analyser-symbol path also proves the embedder reuse works for code. One input still needed to finalise: `\d knowledge_base` (columns, vector dim, ANN index).
- **The embedding stack is confirmed end-to-end and reusable — this closes the storage/embedder questions and narrows feasibility to one real risk.** Ground truth from `\d knowledge_base` (clients_db) + `aiservice/interface.go` + `ollama.go`: a model-agnostic embedder seam already exists (`AIService.GenerateEmbedding`, `OllamaClient`, `nomic-embed-text`; the caller applies the nomic `search_document:`/`search_query:` prefix, which `embed.go`'s `nomicPrefix` mirrors), and the store is pgvector `knowledge_base.embedding vector(768)` with an **IVFFlat cosine** ANN index AND a **gin_trgm** index on `content` in the same table — i.e. the chassis already does the lexical+semantic hybrid I built, and the fusion can run in SQL (so the in-Go `fuse` may not graduate). Consequences: reuse the embedder + pgvector + the hybrid pattern + the dedup (`unique(collection, content_hash)`); code symbols get a **sibling table** mirroring that shape (not a KB collection — different columns and lifecycle), preferring **HNSW** over IVFFlat because the code index updates incrementally per commit (IVFFlat centroids go stale and need REINDEX as rows churn). `contextkit`'s `embed`/`resolve_targets`/`fuse`/`eval_targets` are confirmed as the offline lexical-prototyping + measurement layer with a clean graduation path (swap my embedder for `AIService.GenerateEmbedding`, swap in-memory cosine for the pgvector table). The one remaining embedding risk is **code-domain quality** — nomic is prose-trained, so measure code recall with `eval_targets` against the real adapter; per-query latency is de-risked by the ANN index, and "is it wired" is answered (the model serves the KB; `ollama-eval` 47d uptime). Endpoint nuance: `GenerateEmbedding` uses `/api/embeddings` (single text); my stand-in used `/api/embed` (batch) — reuse `AIService`, batch via a loop.
- **The code index store is now a concrete artifact: a `code_symbols` sibling table (DDL drafted), reusing the KB shape, keyed for code.** Decisions realised: sibling table (not a `knowledge_base` collection — different columns/lifecycle); identity `unique(repo, path, symbol)` with `content_hash` to skip re-embedding unchanged symbols; SHA-versioned via `commit_sha`; HNSW primary (incremental-friendly) with IVFFlat fallback. Two deliberate, flagged departures from the usual conventions, because this is a rebuildable cache not a system-of-record entity: no `version`/`previous_version_id` (versioned by commit), and no `deleted_at` (stale symbols are pruned/hard-deleted, not soft-deleted). The migration honours the §6.1 footgun (plain `CREATE TABLE`, not `IF NOT EXISTS`, so a pre-existing table errors) and carries the parameterised retrieval/upsert/prune queries — including the hybrid RRF done in SQL, which is what lets the in-Go `fuse` retire. Still gated on `\dx` (HNSW needs pgvector >= 0.5.0) and a `\dt` reuse check before apply.
- **Pre-apply gates passed; `code_symbols` is clear to apply on `clients_db`.** `\dt` found no existing code/symbol table; `\dx` shows pgvector **0.8.0** with HNSW (so the primary HNSW index stands, no IVFFlat fallback), pg_trgm 1.6, and pgcrypto for `gen_random_uuid()`. Storage questions (A5/B4) fully closed. The one open embedding question is now B4a — whether nomic retrieves code well enough to beat the trigram baseline — which the table plus the offline harness (`eval_targets` against the real adapter, nomic prefix applied) are set up to answer. Discipline that paid off: gating the apply on `\dt` (reuse) + `\dx` (HNSW availability) rather than assuming, and using plain `CREATE TABLE` so a pre-existing table would have surfaced.
- **Decided: text-embedding and code-embedding are separate responsibilities — share the mechanism, separate the policy.** Shared (don't duplicate): the `AIService` embedder seam (model-agnostic, per-agent `ai_service` config), provider impls, pgvector, the semantic+trigram hybrid. Separate per domain so each improves independently: the model (prose vs code-specific — the main lever and the answer to B4a's domain shift), the vector dimension (follows the model; isolated by separate tables), preprocessing (nomic prefixes vs a code model's), retrieval tuning (HNSW vs IVFFlat, lexical-heavier hybrid for code), the row definition (symbol vs prose chunk), and evaluation (code vs text ground-truth). The sibling table started this; the `embedding_model` column lets one domain be re-embedded after a model upgrade without touching the other. Chassis form: separate indexer agents (knowledge over `knowledge_base`, code over `code_symbols`), each with its own `ai_service`. Open: parameterise `rag_lookup` by table+model+weights vs a sibling code-retrieval action (lean sibling if ranking diverges). Caution: separation only pays if the shared mechanism stays shared — policy separate, plumbing shared. (`code_symbols` applied cleanly this turn.)
- **Harness embedder realigned to the chassis contract so the B4a measurement is faithful and turnkey.** `embed.go`'s Ollama embedder now does single-call `/api/embeddings` `{model,prompt}` (was batch `/api/embed` `{model,input[]}`), matching `ollama.go` and the `AIService.GenerateEmbedding` graduation seam exactly — same model, same `search_document:`/`search_query:` prefixes as `rag_*`. Why: it predicts production behaviour, hits the endpoint the chassis provably serves (de-risks the run against the adapter), and measures realistic single-call indexing latency (the SPEED half of B4a). Trade-off: single-call is slower for bulk than batch — but that slowness *is* the speed signal; batch `/api/embed` is a later optimisation only if the adapter exposes it. Only `ollamaEmbedder` changed; no variable names elsewhere. Built + vetted + smoke-tested against a fake `/api/embeddings` server; tarball refreshed. The runbook now carries the full measurement procedure (lexical baseline vs semantic-against-real-adapter vs fused, the decision rule, and the "which model for code" repeat with `-model`). The measurement remains the user's to run (needs the chassis repo + live adapter); it is the one open embedding risk and the gate before committing the code-indexing path to a model.
- **Code-indexer reuse target grounded from `rag_actions.go` — reuse the mechanism, differ on what's code-specific.** The chassis already has `rag_index` (chunk→embed→store in `knowledge_base`) and `rag_lookup` (embed→vector search→trigram fallback). Reuse as-is (same `actions` package, direct calls): `createRAGEmbeddingClient`, `applyNomicPrefix`, `pgvectorString`, and the pattern (sha256 content_hash dedup; embedding non-fatal so trigram still works; `ON CONFLICT`). Differ: input is analyser symbols not a blob and is NOT chunked (`rag_index`'s `chunkContent` breaks Go mid-function — the prose chunker A5 flagged); target is `code_symbols`; conflict is identity upsert `(repo,path,symbol) DO UPDATE WHERE content_hash IS DISTINCT` not content `DO NOTHING` (KB is content-keyed, code is identity-keyed); retrieval is its own query because `vectorSearchKB`/`trigramSearchKB` bake in KB's table+columns. So build a thin `index_code_symbols` + a `lookup_code_symbols` (mirrors rag_lookup) reusing the helpers; the indexer agent is a one-step-each orchestrator (analyser → index → prune), needing the analyser-as-library. Resolved the parked B4b retrieval sub-decision → sibling action, not a parameterised rag_lookup. Separately: **docs reuse the existing rag path as-is** (prose → a `knowledge_base` collection like `standards`, retrieved by `rag_lookup`) — flat files stay the source of truth, the DB copy is a derived retrieval index; much lighter than code, keep it a distinct workstream. Constraint: chassis Go compiles in the user's env (module `github.com/gqls/agentchassis/...`), not in-container — these actions get drafted for review, then built/deployed via their GitHub Actions → Backblaze pipeline.
- **Production is in-cluster; the contextkit harness is a prototyping/measurement scaffold (framing corrected).** Parsing, indexing, retrieval, and context assembly are in-cluster chassis capabilities — no desktop dependency. The harness binaries proved the approach and measure recall (a dev/tuning activity for the flywheel); their production-relevant parts graduate in-cluster (analyser → adapter; embed → `index_code_symbols`; hybrid → SQL over `code_symbols`; assembler → a build action), leaving `eval_targets` + ground truth as the measurement tool. My earlier "analyses locally and offline" was imprecise: the Go walk is a library because it's the parsing *primitive the in-cluster adapter wraps*, not because anything is desktop-bound.
- **Decided: build the polyglot analyser adapter NOW.** Reason (the right test): the JS tools already exist and need code-symbol indexing/provenance — tech debt, not a foreseeable-future plan, so build polyglot-ready once. Three layers, decision applied: (1) Go AST walk = a **library** (`analysis.Analyse`), extracted from `cmd/analyser`, still needed because it runs *inside* the adapter (a polyglot adapter imports it for Go, doesn't replace it); (2) an **`Analyser` seam** the code-indexer depends on (interface `Analyse(repo, ref, language) → Output`), mirroring how callers depend on `AIService` not `OllamaClient`; (3) the **adapter = a service**, fronting per-language parsers like the ollama-adapter fronts models, taking `(repo, ref, language)` and returning symbols over HTTP. The seam means `index_code_symbols` can be drafted against the interface now, independent of the adapter build.
- **JS's three needs are distinct — keep the adapter's scope precise.** Code-symbol *indexing* needs a JS parser → the adapter (JS drop-in). *Documentation* is prose → the language-agnostic `rag_index` path, no parser. *Provenance* is recorded by `code_symbols` (commit_sha, path, symbol) for any language. Separately and far-future (NOT an analyser concern): the *performance* of the end-user quant tools the chassis builds (TCP tricks, faster languages) is product code in the generated sites, handled when those tools are built — the analyser only parses those languages well enough to index them.
- **Parsing input = git checkout in-cluster, not raw files in B2/Postgres.** The repo is the editable source of truth; the checkout is ephemeral; Postgres holds only the derived index (symbols/chunks + embeddings + searchable content); B2 is for built deliverables. Flow: checkout repo@SHA into a container workspace → parse/read → derived index to Postgres (`code_symbols`; a `knowledge_base` collection for docs) → discard checkout. The adapter owns the checkout for code (clones repo@ref, parses, returns symbols); docs are read from a checkout and fed to `rag_index`. Precondition: the docs must live in a git repo — if they're loose files (desktop/chat project), step one is putting them in git; "flat files as source of truth" = "files in a versioned repo", not on a machine. Do NOT store raw docs in Postgres (that's the index) or lean on B2 for the editable source (artifacts only). Access choice to settle: cluster clones from GitHub (a read token/deploy key as a k8s secret) or fetches a B2 source snapshot from the existing pipeline — cloning is the simpler default, B2-snapshot avoids cluster git creds.
- **Adapter build grounded in the real git/thunder adapters — and the transport was Kafka, not HTTP (correction).** Custom adapters (git, thunder) are Kafka workers: consume `AdapterRequest{Headers, Body{Action, Data}}` from a requests topic, dispatch by action, reply with a `ResponseMessage{Headers, Body{Success, Data, Error}}` on the caller's `responses_topic` (shapes in `platform/models`; `ToKafkaHeaders()` flattens). Only the *stock* ollama-adapter is HTTP — which is why my earlier "analyser adapter = HTTP service" was wrong. "Output messages pass validation" = this ResponseMessage contract. Dispatcher pattern from thunder/adapter.go (kafka consumer+producer, `Run` loop, `handleMessage` → dispatch → reply); handler shape from thunder action handlers (`NewXAction(deps)` + `Execute(ctx, req)`, stateless/idempotent).
- **Adapter progress this turn:** extracted + compiled the layer-1 `analysis.Analyse(root)` (contextkit `internal/analysis/analyse.go`; CLI now thin) — moves to the chassis as `internal/analysis`. Drafted (chassis, for their build) `github_source.go` (read-only **tarball** fetch — one request, no git binary, recovers `commit_sha` from the archive top-dir, traversal-guarded) and `analyse_action.go` (validate → fetch → Analyse → result; Go-only, JS parser drop-in behind the same action). Next: dispatcher + manifest (mirror thunder), then the `index_code_symbols` consumer.
- **Security/repo-access decided direction:** least-privilege, per-adapter — the analyser gets a fine-grained, **repo-scoped, read-only** GitHub token (k8s Secret, mounted only on its pod); the git adapter keeps its separate **write** credential. Not broad cluster access, not one shared token. Reuse the git adapter's GitHub-API client pattern; fetch via tarball. Harden with rotation, encrypted secrets, network policy; the indexed code stays in the existing in-cluster Postgres trust boundary. No-static-secret variant: a GitHub App installation token (short-lived, auto-rotating).
- **JS tools documentation flagged as its own focus (`FOCUS_js_tools_documentation.md`).** The existing JS tools need prose docs + provenance and have neither; only the site/plan spec they were built from exists (a seed, not documentation). Two mechanisms: prose docs → the language-agnostic `rag_index`/`knowledge_base` path (no parser); code-symbol provenance → the analyser adapter's JS parser drop-in (records via `code_symbols`). Docs must live in git to be indexable. Tech debt (the tools exist now), not a future plan.
- **Import-reuse confirmed for the message envelope (user was right).** Canonical types are in `platform/orchestration/types` (context.go): RequestMessage/RequestHeaders (Action + ResponsesTopic on the HEADERS), ResponseMessage/ResponseHeaders/ResponseBody{Success,Body,Error}/ErrorInfo, AgentIdentity, and the helpers `FromRequestHeaders(headers)→*ExecutionContext` + `(ec)ToResponseHeaders()→ResponseHeaders`. The adapter IMPORTS these and builds responses via FromRequestHeaders→set status/sender→ToResponseHeaders, so the platform constructs the envelope (best guarantee of passing validation). git mirrored the types locally = the duplication to avoid. Cyclic-import handling here (interfaces.go: MessageTracer local "to avoid cyclic import") is for behavioral interfaces, not data types — importing the data types is clean.
- **Adapter naming settled: `analyser`** (`system.adapter.analyser.requests`, group `analyser.adapter.group`). Convention = name for the capability fronted (thunder/git/ollama → analyser fronts parsers). Product names (contextkit/contexttool/doctool) name the consumer; renamable.
- **Dispatcher built (`adapter.go`, chassis draft).** Mirrors thunder/adapter.go (kafka consumer+producer, NewAdapter with env topic/group + read-only GitHubSource, Run fetch-loop + small /health,/ready server, handleMessage). Reuses orchestration/types for the contract + ToResponseHeaders. Manifest built (`analyser-adapter.yaml`): Deployment + read-only repo-scoped Secret `analyser-github-read` + health probes. Reconcile only: (1) kafka pkg path + NewConsumer/NewProducer/FetchMessage/Produce/CommitMessages signatures from thunder; (2) the ExecutionContext fields ToResponseHeaders needs (Sender/Status/IsComplete/IsError) + AgentIdentity field names.
- **Analyser adapter now complete as drafts** (fetcher + handler + dispatcher + manifest + the compiled analysis library). Next: consumer side — index_code_symbols + lookup_code_symbols + the indexer agent (call analyser adapter → upsert code_symbols → prune), then end-to-end (index chassis → B4a → assemble).
- **Adapter Response Envelope Contract (doc 003) — the silent-timeout trap, and my draft had it.** An adapter reply must set, in the KAFKA MESSAGE HEADERS: message_type=response, **request_id = the incoming request_id reused verbatim** (the router matches awaited_requests on this), fresh message_id, in_response_to_request_id, orchestration_id, correlation_id, status, sender_agent_type, in_response_to_step_id/name; and be sent via **ProduceWithValidation** (not Produce). Body headers unmarshal into types.ResponseHeaders where is_complete/is_error are real bool — so the body MUST use a typed struct (ToResponseHeaders), never map[string]string string-bools. Get any of these wrong and the reply routes to process-as-work and the awaited row times out with no error logged (real multi-day thunder fault). Reference adapters: webscrape (map builder) + git (typed ResponseHeaders + ToKafkaHeaders).
- **Correction: import-reuse alone is insufficient for the adapter response envelope.** Canonical types.ResponseHeaders has NO request_id/message_id field and NO ToKafkaHeaders method (verified in context.go). So: body reuses the canonical type (real bools via ToResponseHeaders); the adapter ADDS a local Kafka-header builder (git's ToKafkaHeaders analog) + ProduceWithValidation. That's why git mirrored locally — not arbitrary duplication. Fixed adapter.go accordingly.
- **Consumer side scoping (per 001/003):** index_code_symbols + lookup_code_symbols are ACTIONS (mirror rag_index/rag_lookup; platform/orchestration/actions/code_symbols_actions.go; ActionInputSpec+ExtractActionInputs; parameterised SQL; reuse rag embedding helpers). code-indexer is an AGENT (agent_definitions row, category integrator) — orchestrator: request_analysis (call analyser adapter) → index → complete. Field names prefixed (repo, not site_id/domain) to dodge ExtractActionInputs nested-source collisions (current_page/rerender_pages/site_record/input_data).
- **To build the consumer side right first time, need 4 source files** (same discipline as the user sending the kafka package for the dispatcher): rag_actions.go (reuse exact helpers + signatures), an adapter-calling action (webscrape/git's chassis-side caller, for the analyser request-reply), action_registry.go (registration), one sample agent_definitions row (workflow + contracts + docker_image/tag + category). Building blind risks 001's resolveRAGFieldPath / universal-topic-fallback anti-patterns.
- **ProduceWithValidation signature (verified, webscrape_actions.go):** `ProduceWithValidation(ctx, topic string, headers map[string]string, key []byte, value []byte) error` — headers BEFORE key. My adapter had key/value/headers order; fixed.
- **Adapter-call pattern (verified, WebscrapeAction):** an action that calls a fixed-topic adapter builds JSON `{headers, body}`, derives the responses topic from `ExecutionContext.ResponsesTopic` (fallbacks: env RESPONSES_TOPIC, then `system.agent.<type>.responses`), builds a Kafka headers map mirroring the JSON headers, sends via ProduceWithValidation, and returns `{AwaitResponse:true, RequestID, ...}`. The engine registers an awaited request and lands the reply in the step's output_field. request_repo_analysis follows this exactly.
- **rag helpers are package-level and reusable as-is:** createRAGEmbeddingClient (ollama, defaults nomic-embed-text + the cluster api_url, overridable via config.embedding_service), applyNomicPrefix (search_query/search_document; only for nomic-* models), pgvectorString, getEmbeddingModel, resolveRAGConfigField, nullIfEmpty, truncateForLog. vectorSearchKB/trigramSearchKB/scanKBResults HARDCODE knowledge_base → wrote code_symbols equivalents.
- **code_symbols upsert/prune pattern:** identity upsert ON CONFLICT(repo,path,symbol) DO UPDATE; keep existing embedding via `COALESCE(EXCLUDED.embedding, code_symbols.embedding)` so unchanged symbols aren't re-embedded; embed only when stored content_hash != new (load existing hashes first); prune `WHERE repo=$1 AND commit_sha IS DISTINCT FROM $current` (skip + log when no commit). content = kind+symbol+signature+doc+path (embedded AND trigram-matched). Method symbols carry the receiver.
- **agent_definitions real shape (from 3 live rows):** columns incl category (freeform: code-driven/orchestrator/specialist) AND agent_category (CHECK: strategist/executor/analyst/integrator/coordinator/specialist, often empty); image_repository 'docker.io/aqls/agent-chassis' tag 'v1.0.1059'; topics `{error,process,response}` templated `{type}`; health_config port 8080 /health /ready; default_config = {workflow{start_step,steps}, processing_mode:"orchestrator", timeout_seconds}; input_contract {required,optional,expects}; output_contract {produces}. code-indexer modelled on these (category code-driven, agent_category integrator). CHECK schema (\d) before applying.
- **Two run-time confirmations for the consumer side:** (1) \d agent_definitions before the INSERT; (2) the collected_data shape of the awaited adapter reply (index_code_symbols reads repo_analysis.output/.commit_sha/.repo; ExtractNestedField tolerates a .response wrapper) — confirm from the first run and adjust configs.
- **Adapter audited against FOCUS_adapter_design (2026-06-11). Conforms:** topic Convention A (system.adapter.analyser.requests + analyser.adapter.group + REQUESTS_TOPIC/CONSUMER_GROUP overrides); ProduceWithValidation arg order (headers before key); sequential handleMessage (no goroutine); NewAdapter manual cleanup on failure; credentials from env not config; in_response_to_request_id from incoming; adapter is the correct pattern (one external system=GitHub, multiple callers, credential boundary).
- **Adapter audit — DIVERGES (fix):** (1) HIGH — envelope: the doc + webscrape + the doc's smoke test put `action` and `reply_to_topic` in the BODY and ids in the Kafka MESSAGE headers (kafka.HeadersToMap(msg.Headers)); my adapter reads action/reply_to_topic from the JSON envelope's typed headers and my sender writes them there. Pair is self-consistent (indexer path works) but a doc-shaped request lands with empty action / no reply topic. Fix both adapter.go (read body.action/body.reply_to_topic; ids from kafka headers; payload at body.data) and analyser_request_action.go (put action/reply_to_topic/data in body). (2) no cmd/analyser-adapter/main.go and no configs/analyser-adapter.yaml (checklist requires both; struct needs cfg → service_info.name=sender_agent_type, server.port, kafka_brokers). (3) graceful shutdown absent — need Shutdown() (sync.Once) + ctx/cancel in struct + held healthServer + shutdownWg; my Run takes ctx and can't stop the health server. (4) missing adapterID (sender_agent_id) + cfg-derived sender_agent_type. (5) minor: blanket error_unrecoverable (doc wants error_recoverable for transient e.g. failed fetch); /ready static 200; run-loop error backoff+shutdown check; I skip commit on produce failure (doc commits unconditionally); kustomize layout vs my flat deploy/.
- **Adapter audit — DOC CONTRADICTION (FOCUS_adapter_design vs 003_contracts_and_standards), needs the user to resolve:** (A) body header bool type — 003: typed struct, REAL bools (string bools fail the chassis unmarshal, verified thunder fault); FOCUS skeleton: map[string]string, STRING bools. (B) send call — 003: ProduceWithValidation (never Produce); FOCUS skeleton: Produce. (C) correlation — 003: reuse incoming request_id in response request_id (matcher on request_id); FOCUS: response request_id = NEW uuid + in_response_to_request_id = incoming (matcher on in_response_to_request_id); FOCUS's own smoke test asserts in_response_to_request_id==request's id (supports FOCUS). My adapter followed 003 (typed body via ToResponseHeaders, ProduceWithValidation, request_id reused).
- **Recommended reconciliation (pending user):** in_response_to_request_id=incoming (both agree); ProduceWithValidation (strict superset; validates real responses, still sends error responses); REAL bools in body but as a map[string]interface{} so it ALSO carries request_id/in_response_to_request_id (canonical types.ResponseHeaders has NEITHER field — the recurring root cause). Open question to user: does the chassis match the awaited row on request_id (003) or in_response_to_request_id (FOCUS)? If in_response_to_request_id → response request_id=fresh uuid; if request_id → keep reused; if unsure → set both to incoming (can't miss either matcher). Also confirm the response body payload field name (FOCUS/git "data" vs canonical types.ResponseBody "body").
- **Meta-finding:** the canonical orchestration/types, git's local mirror, 003, and FOCUS have drifted; the adapter RESPONSE envelope is the area of maximum drift. The user should pick ONE canonical response-envelope spec and align the docs + the validator (FOCUS's own "TODO — tighten validator coverage" notes Tier-2 fields incl in_response_to_request_id aren't validated → silent hangs).
- **003-vs-FOCUS contradiction RESOLVED empirically (2026-06-11) from coordinator.go + git/thunder/dynamic/websearch adapters.** Bools: REAL bools — orchestrator consumes typed types.ResponseMessage; thunder comment (~L634-643) records the string-bool map fails the unmarshal into IsComplete bool; git/thunder/dynamic all typed. websearch is the lone outlier (string-bool map + plain Produce) = the deprecated path the FOCUS skeleton describes. ProduceWithValidation: git/thunder/dynamic use it; only websearch uses Produce; user confirms it's the better validator. request_id: coordinator.go ProcessResponse (L192-197) claims the awaited request on execCtx.InResponseTo.RequestID (=in_response_to_request_id) FIRST, falling back to execCtx.RequestID only if empty → in_response_to_request_id is the load-bearing matcher; a fresh request_id is fine. git's concrete choice: reuse request_id from incoming + add fresh message_id + set in_response_to_request_id (covers both match paths). My adapter's buildResponseKafkaHeaders already does exactly this → response side was already correct.
- **Request-envelope finding (REVISES the prior audit): action is read from the BODY by ALL adapters** (thunder body["action"], git req.Body.Action, image-generator req.Body.Data) — my adapter read headers.action; FIXED. Reply topic is genuinely MIXED: git+websearch read headers (responses_topic/reply_to_topic), thunder+image-generator read body.reply_to_topic — support both (my responses_topic-from-headers was already right; the FOCUS skeleton's body.reply_to_topic was the outlier, NOT my adapter as the prior audit implied). Payload nests under body.data.
- **APPLIED (2026-06-11):** adapter.go — incoming.Body changed from json.RawMessage to a typed struct {Action, Data json.RawMessage, ReplyToTopic}; dispatch on in.Body.Action; unmarshal in.Body.Data; reply topic = headers.ResponsesTopic||headers.ReplyToTopic||body.ReplyToTopic; header comment rewritten to the empirically-grounded contract. analyser_request_action.go (sender) — body now {action, reply_to_topic, data:{owner,repo,ref,language}}; headers keep responses_topic+reply_to_topic+action (belt-and-braces); comments fixed. Response side (buildResponseBody via ToResponseHeaders [real bools], buildResponseKafkaHeaders [request_id reused + message_id new + in_response_to_request_id], ProduceWithValidation) LEFT AS-IS — confirmed correct.
- **Still open (separate structural gaps from the FOCUS audit, NOT the contradiction):** cmd/analyser-adapter/main.go (signal handler → Shutdown()); configs/analyser-adapter.yaml (service_info.name → sender_agent_type, server.port, kafka_brokers, custom.adapter_settings, github read-token env var NAME); graceful Shutdown() (sync.Once + ctx/cancel in struct + held healthServer + shutdownWg); adapterID uuid; status vocab (error_recoverable for transient fetch via a github_source sentinel); meaningful /ready; kustomize layout. Also confirm at first run the response body payload field name (canonical types.ResponseBody "body" vs git/FOCUS "data") since index_code_symbols reads the awaited reply via ExtractNestedField.

## Doc restructure: adapter docs + 003 (thinking + plan, 2026-06-11)

**Diagnosis (precise, after reading 003 §832-890 + coordinator.go).** The "003 vs FOCUS contradiction" is NOT 003 being wrong. 003 already has a dedicated "Adapter Response Envelope Contract" (§832) that is correct on the two things FOCUS's skeleton got wrong: it MANDATES the typed-struct/real-bool pattern (§867-880, the bool trap, citing the 2026-05-22 thunder fault) and ProduceWithValidation (§852/863/888), and it documents the deprecated anti-pattern (§882, which is exactly what FOCUS's skeleton showed). So the contradiction = FOCUS_adapter_design DUPLICATED the envelope in a code skeleton that used the deprecated map/string-bool/Produce path 003 warns against. Root cause is DUPLICATION of normative content that then drifted, not a genuine disagreement of intent.

**The one real 003 imprecision (needs clarification).** 003 §843/856 says "the router matches request_id" and "reuse the incoming request_id in BOTH request_id and in_response_to_request_id; a fresh request_id misses the lookup." coordinator.go ProcessResponse (L192-197) actually claims on execCtx.InResponseTo.RequestID (= in_response_to_request_id) FIRST, falling back to execCtx.RequestID (= request_id) only if empty. So the load-bearing field is in_response_to_request_id, NOT request_id. 003's RECOMMENDED pattern (reuse request_id in both) still WORKS — it sets in_response_to_request_id=incoming (the real key) and request_id=incoming (the fallback) = belt-and-braces. But its MECHANISM wording over-constrains: a fresh request_id is in fact acceptable (the user's instinct), because the match is on in_response_to_request_id. This also harmonises 003 and FOCUS: FOCUS had the mechanism right (match on in_response_to_request_id, request_id=new) but its skeleton used the deprecated bools/Produce; 003 has the safe pattern (reuse request_id, git-style) but mis-describes the mechanism.

**Doc taxonomy (theirs).** Numbered docs = canonical/permanent (001 dev guide, 002 architecture, 003 contracts, 019 tool_library, 020 tool_lifecycle). FOCUS_* = focused design/investigation notes (often transient). Adapters are a first-class component type like tools → they deserve a NUMBERED guide, not a FOCUS_ note. "We need an adapter doc rather than a focus doc" = promote, don't fold.

**Recommendation.**
1. KEEP 003 as the single source for the response-envelope CONTRACT. Apply a SMALL clarification (~15 lines), do NOT expand it with adapter how-to:
   - Correct the matcher wording: the coordinator claims on in_response_to_request_id FIRST (coordinator.go ProcessResponse), request_id is the fallback. The strictly-required rule is in_response_to_request_id = incoming request_id. request_id MAY be a fresh UUID; the favoured pattern (git) reuses the incoming request_id AND adds a fresh message_id, covering both the primary and fallback match — recommend that for new adapters. Drop the "a fresh request_id misses the lookup" claim (true only if in_response_to_request_id is also wrong/absent).
   - Add a short REQUEST-envelope note (currently 003's section is response-only): adapters read action from body.action (NOT headers) — all working adapters do; reply topic is accepted from headers (responses_topic / reply_to_topic) OR body (reply_to_topic) (mixed across adapters); payload at body.data.
   - Add "last verified against code: 2026-06-11 (coordinator.go, git/thunder/dynamic/websearch)" + "if docs and code disagree, code wins."
2. PROMOTE the adapter CONSTRUCTION material to a NUMBERED canonical doc — e.g. 021_adapter_guide.md (sits beside 019_tool_library / 020_tool_lifecycle). Carry over from FOCUS: decision (adapter vs agent vs inline), responsibilities, file/package layout, the struct, NewAdapter cleanup ordering, Run loop (sequential default), graceful Shutdown (sync.Once), health endpoints, topic conventions (A), config YAML field names, deployment/kustomize, credentials/secrets, end-to-end test, pre-merge checklist, common mistakes. For the envelope, LINK to 003 §Adapter Response Envelope Contract and show ONLY the skeleton helper that IMPLEMENTS it — FIXED to typed struct + real bools + ProduceWithValidation (so the skeleton stops contradicting 003), ideally lifted from git-adapter (the reference impl 003 names). Retire FOCUS_adapter_design.
3. Do NOT fold the recipe into 003: it mixes "what must be true" (contract) with "how to build this component" (recipe) and worsens 003's length. The cure for the contradiction is single-sourcing + linking, not merging.
4. 003-too-long is a SEPARATE axis. If slimming: split 003 by contract DOMAIN into numbered sub-docs (message/response-envelope contract; DB+SQL parameterisation; component/theme contracts; agent-definition contract). The response-envelope contract is the prime candidate to extract into its own short numbered doc that BOTH 003 and 021 reference. Optional/later; not needed for this fix.

**Durable anti-drift fix (the meta-point).** The contradiction recurred because a contract lived as PROSE in two places. Promote it to CODE: tighten platform/validation ValidateOutgoingMessage to enforce the Tier-2 routing fields (esp. in_response_to_request_id, message_type, status) when message_type=="response", with the is_error=true escape hatch (this is FOCUS's own "TODO — tighten validator coverage"). An enforced contract can't silently rot into a 30-minute hang, and the doc skeleton can't drift from a validator that rejects the deprecated shape. This is the strongest fix and should be filed as a tracked task.

**Open decisions for the user:** (a) the number for the adapter guide (021_adapter_guide.md?); (b) whether to extract the response-envelope contract into its own numbered doc now or keep it as a 003 section; (c) whether to slim 003 by domain (separate, larger effort). Execution order once decided: apply the small 003 clarification → draft the numbered adapter guide from FOCUS (envelope as a link, skeleton fixed) → retire FOCUS_adapter_design → file the validator-tightening task.

## DONE 2026-06-11: envelope contract extracted to 035_adapter_guide.md
- Created 035_adapter_guide.md. Section 1 = the message envelope contract (NORMATIVE, applies to any chassis-message replier, not only adapters), extracted from 003 §832-890 with corrections: (a) matcher precision — coordinator.go ProcessResponse claims on in_response_to_request_id FIRST, request_id fallback; in_response_to_request_id=incoming is the required field, request_id may be fresh (git reuses + adds fresh message_id = belt-and-braces); explicitly retracts the old "a fresh request_id misses the lookup" wording. (b) request-envelope parse added (action from body.action, payload body.data, reply topic from headers OR body — mixed across adapters). (c) header tiers (1/2/3) folded in from FOCUS. (d) canonical-type gap documented (types.ResponseHeaders lacks request_id/message_id → git mirror; fix path = add the fields). (e) removed 003's internal tension: §865 said "mirror the webscrape map-based builder" while §867 said never map[string]string — 035 states the body-typed / Kafka-headers-map split cleanly and names git/thunder (verified) as the references. (f) deprecated anti-pattern names the web-SEARCH adapter (distinct from web-SCRAPE) as the current example.
- The validator-tightening TODO is placed in 035 §1.10 ("promote this contract from prose to validator enforcement") — the natural home, right after the contract it concerns. Marked "not yet filed — raise as a tracked task." Also still in this NOTES file.
- Section 2 (construction recipe) is an OUTLINE only (2.1-2.15) with a "to be migrated from FOCUS_adapter_design" banner; FOCUS's deprecated send skeleton is explicitly NOT to be carried over (use 1.8). NEXT STEP: migrate the FOCUS recipe detail into Section 2, then retire FOCUS_adapter_design.
- 003 change PENDING USER APPLICATION (003 is read-only here): replace §832-890 "## Adapter Response Envelope Contract" with a short pointer to 035 §1. Replacement text supplied in the turn response. This completes the extraction (contract now single-sourced in 035; 003 references it). 003 shrinks by ~58 lines, helping the length concern.
- Open decisions still outstanding: whether to also slim 003 by splitting other domains into numbered docs (separate, larger); the adapter guide is now 035 (not 021 as earlier mooted).

## DONE 2026-06-11: FOCUS_adapter_design merged into 035 — FOCUS retired
- 035_adapter_guide.md is now the single canonical adapter doc (~894 lines): Section 1 = envelope contract (normative), Section 2 = full construction recipe consolidated from FOCUS_adapter_design (§2.1 decision table, 2.2 responsibilities, 2.3 layout, 2.4 struct, 2.5 NewAdapter cleanup, 2.6 Run loop, 2.7 handleMessage, 2.8 graceful shutdown sync.Once, 2.9 health, 2.10 topics A/B, 2.11 config YAML field names, 2.12 deployment [full manifest + cluster resources + RBAC + Makefile + pre/post-deploy], 2.13 credentials, 2.14 common mistakes, 2.15 kcat end-to-end test, 2.16 pre-merge checklist). All code blocks/tables/YAML carried verbatim.
- Deliberately NOT carried over from FOCUS: its "Sending responses" skeleton (map[string]string respHeaders, string is_complete/is_error, request_id via uuid.New(), plain Produce) — that is the deprecated shape; §1.9 marks it so and §1.8 gives the favoured typed-struct pattern. FOCUS's Tier-1/2/3 header tables had string bools + request_id=new; the corrected tiers are in §1.3. FOCUS's validator TODO (lines 319-328) is captured in §1.10. The response BODY envelope shape ({headers, body:{success,data,error}}, both Kafka + body headers set, body headers are the type-sensitive ones) was added to §1.3 (it was not previously explicit); result field is "data" per git/thunder, with a runtime-confirm caveat for types.ResponseBody's tag.
- FOCUS_adapter_design can now be RETIRED/DELETED — its content is fully in 035. (Upload copy was /mnt/user-data/uploads/FOCUS_adapter_design_2_.md, 891 lines.)
- STILL PENDING (user applies): replace 003 §832-890 "## Adapter Response Envelope Contract" with the short pointer to 035 §1 (text supplied in the prior turn). Until then the contract is duplicated 003↔035; that pointer completes the single-sourcing and trims ~58 lines from 003.
- Validator-tightening TODO still open (035 §1.10): enforce Tier-2 routing fields (esp. in_response_to_request_id) in ValidateOutgoingMessage when message_type==response, is_error escape hatch. Not yet filed as a tracked task.

## DONE 2026-06-11: analyser adapter structural gaps — main.go, config YAML, graceful shutdown
- adapter.go reworked to the thunder/git lifecycle (grounded in adapter_thunder_.go L82-806 re-read this turn): struct now holds ctx/cancel/cfg/adapterID/senderType/healthServer/shutdownOnce/shutdownWg; NewAdapter does adapterCtx+cancel first and full cleanup ordering on every failure path — FIXED a real producer LEAK (old github-source failure path closed only the consumer); Run() (no args) with thunder's exact fetch/error/backoff/shutdown shape; StartHealthServer(port) exported, called by main, healthServer held on struct; /ready = 503 once shutdown begins (drain signal) else 200, with a comment that pinging GitHub per probe would burn rate limit (the analyser has no cheap pingable dep); Shutdown() sync.Once → cancel → healthServer.Shutdown(5s) → wait shutdownWg (10s cap) → close consumer+producer; commit(msg) helper on Background ctx.
- handleMessage(msg) (ctx param dropped): Execute on a.ctx (shutdown aborts long analyses); NEW shutdown-mid-work guard — if execErr && a.ctx done, no reply + no commit → redelivery (an error_unrecoverable reply there would wrongly stop retries); produce on Background+10s (replies land during drain); commits via a.commit.
- buildResponseKafkaHeaders: func → METHOD; sender_agent_type now cfg-derived (service_info.name, fallback const), ADDED sender_agent_id (adapterID), sender_pod_name, time_sent (Tier-3, thunder parity). ec.Sender now also sets AgentID (AgentIdentity field names remain a header reconcile point).
- NEW cmd/analyser-adapter/main.go: -config flag (default /app/configs/analyser-adapter.yaml), config.Load (RECONCILE: exact loader name — copy from thunder/git main), newLogger from cfg.Logging.Level (RECONCILE: swap for platform logger helper if one exists), StartHealthServer(cfg.Server.Port||8080), SIGINT/SIGTERM → Shutdown, Run, Shutdown again (idempotent).
- NEW configs/analyser-adapter.yaml per 035 §2.11 (service_info name analyser-adapter, server.port "8080", logging, kafka_brokers, databases {} — adapter uses none, custom.adapter_settings + custom.github DOCUMENTARY like thunder: code reads REQUESTS_TOPIC/CONSUMER_GROUP/GITHUB_READ_TOKEN/GITHUB_API_BASE env directly; keep the three topic values in sync).
- Deployment manifest patched (coupling with main.go): command ["./analyser-adapter","-config","/app/configs/analyser-adapter.yaml"], configMap volume analyser-adapter-config (kustomize configMapGenerator note), serviceAccountName ai-persona-app, imagePullSecrets docker-hub-creds, strategy Recreate, image docker.io/aqls/analyser-adapter:vX.Y.Z, AGENT_VERSION. RENAMES FLAGGED: HOSTNAME fieldRef → POD_NAME (code reads POD_NAME then HOSTNAME fallback); HEALTH_PORT env REMOVED (port now cfg.Server.Port via main); Run(ctx)→Run(); startHealthServer→StartHealthServer(port); handleMessage(ctx,msg)→handleMessage(msg).
- gofmt-parse clean (adapter.go 463 lines, main.go); both YAMLs parse.
- STILL OPEN (next steps, in order): (1) error_recoverable for transient fetch — needs an ErrTransient sentinel in github_source + errors.Is in handleMessage status choice; (2) KafkaTopic CRD for system.adapter.analyser.requests (auto-create OFF — without it the consumer hangs on every fetch, §2.12 gotcha); (3) kustomize layout (base/overlays + service.yaml + kustomization + configMapGenerator) + Makefile four insertions; (4) main.go reconcile points (loader + logger names) when user uploads an existing adapter main.

## DONE 2026-06-11: Makefile structure analysis, main.go reconciled, directory map updated
- Makefile facts (uploaded, 2054 lines): REGISTRY=docker.io/aqls, IMAGE_TAG (v1.0.1060 current), KUSTOMIZE_DIR=deployments/kustomize, OVERLAY_PATH=production/uk_001 when ENVIRONMENT=production else $(ENVIRONMENT). Per-adapter pattern (5 existing adapters, one each): build-<name>-adapter target with dockerfile build/docker/backend/<name>-adapter.dockerfile; build-adapters aggregate; push-backend line; deploy-agents block = sed newTag in services/<name>/overlays/$(OVERLAY_PATH)/kustomization.yaml + kubectl apply -k, guarded by if-dir-exists. release-backend = build-backend push-backend deploy-core deploy-agents deploy-agent-cleanup. ollama-adapter is the exception (stock image, not IMAGE_TAG-managed).
- main.go RECONCILE points RESOLVED from the real mains (git main.go + main_go.thunder, structurally identical): config.Load(*configPath) ✓ (as assumed) and logger.New(cfg.Logging.Level) from platform/logger (replaces my inline zap newLogger — helper REMOVED). main.go REWRITTEN to the canonical pattern: Run() in goroutine → runComplete chan, select(signal|runComplete), cancel() then adapter.Shutdown() then bounded 10s wait on runComplete. FLAGGED: -config default changed /app/configs/... → "configs/analyser-adapter.yaml" (matches git/thunder; Deployment passes the absolute path explicitly so no in-cluster change). gofmt clean.
- Directory map: contextkit/README.md CREATED in outputs (the attached copy was stale — no analyse.go) and tarball rebuilt with it. Map covers: verified contextkit tree (internal/analysis/{types.go,analyse.go} + 7 cmds); chassis-drafts→destination mapping; still-to-create list (dockerfile, kustomize base+overlay production/uk_001, 4 Makefile insertions, analyser-github-read Secret, KafkaTopic CRD). Consumer-action destinations marked "alongside rag_actions.go / webscrape_actions.go" (package actions) — exact dir not asserted, unverified from here.
- ANALYSER vs OTHER ADAPTERS (the user's structure question): NO different structure needed. Identical in every per-adapter respect (own image/dockerfile/cmd/internal pkg/config/kustomize dir/4 Makefile insertions; main.go now pattern-identical). Three genuine differences, none structural: (1) imports the SHARED chassis-root internal/analysis package (copied from contextkit; no other adapter imports a chassis-root internal/* beyond platform/*; deliberate — the JS-parser seam + indexer tests import it without importing the adapter; contextkit keeps its own copy for the CLI; same module so zero build implications); (2) dedicated least-privilege secret analyser-github-read via secretKeyRef instead of the shared personae-default-secrets envFrom; (3) no DB / no object storage (leaner than thunder, like web-scrape — configs databases are {}).
- NEXT (mechanical, conventions now confirmed): draft the kustomize base+overlay files + the dockerfile + the 4 Makefile insertion snippets + the KafkaTopic CRD.

## DONE 2026-06-11: registry insertions presented (registry_insertions.md)
- Three entries reformatted to the verified house style (multi-line aligned literals in GlobalActionRegistry = map[string]ActionDefinition): lookup_code_symbols + index_code_symbols (Category "storage" — exact match with siblings rag_lookup/rag_index) and request_repo_analysis. Placement anchor: after training_data_export's closing }, before the FEED banner, under a new CODE SYMBOLS section banner. Handler names + (ctx, ActionParams)(interface{},error) signatures verified against the drafts.
- IsLocal:true VERIFIED as the adapter-calling convention (call_agent, generate_image, web_search, scrape_web all true; firecrawl_map is the registry's lone IsLocal:false outlier).
- FLAGGED to user: Category "code" is NEW (taxonomy: site/data/web/storage/training/feed/agent/hitl/core/maintenance/med_pricing/image/analysis/webscrape/external/validation/system/llm/content). Consistent with the small-domain-category pattern (validation/system/llm have 1 each); nearest existing alternative "analysis" (3 entries). User decides; snippet keeps "code" with an inline comment.
- ActionDefinition type itself not found in any uploaded file (lives in an un-uploaded platform file) — field set confirmed by usage only.

## DONE 2026-06-11: destination map re-anchored to the REAL repo tree (user uploaded tree -d)
- Tree facts that CORRECT/CONFIRM earlier notes: internal/analysis ALREADY EXISTS in the repo (verify analyse.go/types.go match contextkit's copies — same files); actions' home is platform/orchestration/actions/ (registry.go/rag_actions.go/webscrape_actions.go; my draft headers already said this — the README's "alongside rag_actions.go" phrasing was the confusion); migrations home is platform/database/migrations/; contextkit's in-repo home is docs/agent_docs/docs019_documentation_audit_autonomous_build_and_operate/go_files/contextkit/ (already present — sync the tarball there); adapters' kustomize overlays confirmed at services/<name>/overlays/production/uk_001 (thunder has production-only; git also has development).
- README.md (the directory map) REWRITTEN: single tree-anchored section with (existing)/(NEW)/(EDIT) markers per destination; tarball rebuilt (first tar attempt hit a transient I/O error on cmd/assembler — retry clean).
- NEW dirs needed: cmd/analyser-adapter/, internal/adapters/analyser/, deployments/kustomize/services/analyser-adapter/{base,overlays/production/uk_001}. EDITs: registry.go (paste registry_insertions.md), Makefile (4 insertions). NEW files: build/docker/backend/analyser-adapter.dockerfile, kustomize base/overlay set.
- Unplaceable from tree -d (user's call): 035_adapter_guide.md home (wherever 001/003 live); KafkaTopic CRD home — wherever system.adapter.thunder.requests is declared (candidates: deployments/kustomize/jobs/kafka-topics/ or terraform production/uk001/080-kafka-topics/); analyser-github-read Secret applied via secret manager.

## DONE 2026-06-11: kustomize set + Makefile for analyser-adapter (modelled on uploaded thunder files)
- Thunder kustomize facts: base = deployment+service+rbac+configMapGenerator (config IN BASE, with a sync note to configs/ root); overlay = namespace ai-persona-system + resources ../../../base + images {name: docker.io/aqls/thunder-adapter, newTag: vX} — NAME+NEWTAG form, NOT newName (corrects 035/README's earlier newName claim); deployment image :latest in base, tag pinned per overlay; deployment carries POD_NAME/AGENT_VERSION/topic envs + envFrom three secrets (default/platform/storage — the deployment comments record that B2 keys live in personae-storage-secrets NOT platform, a verified correction).
- Analyser set DRAFTED (chassis-drafts/…/deployments/kustomize/services/analyser-adapter/): base = deployment.yaml (thunder-shaped: SA, imagePullSecrets, command -config, POD_NAME, topic envs, GITHUB_API_BASE + GITHUB_READ_TOKEN secretKeyRef analyser-github-read, probes thunder-timed, 256Mi/1Gi memory — parses repos in-pod) + service.yaml + kustomization (resources only). NO rbac (analyser manages no k8s resources). OVERLAY-HEAVY per user preference: configMapGenerator + configs/analyser-adapter.yaml live in overlays/production/uk_001/ (precedent: auth-service/core-manager/content-creator-agent overlay configs/ dirs in the real tree); base is cluster-agnostic; root configs/analyser-adapter.yaml = local-run copy, overlay copy = deployed source of truth (headers say so).
- SED TRAP caught+fixed: deploy-agents runs sed 's/newTag:.*/…/' on the WHOLE overlay kustomization — comment lines containing the literal "newTag:" get mangled every release. Overlay now has exactly ONE newTag occurrence (verified grep -c = 1). Worth adding to 035 §2.12 later.
- KafkaTopic CRD drafted (deployments/kafka/analyser-requests-topic.yaml, kafka namespace, strimzi cluster label, 7d retention) — deliberately NOT a kustomize resource (overlay namespace: ai-persona-system would override the kafka namespace); apply directly like the thunder/provisioner topics (user confirmed that's how they were set up). No responses topic needed (replies go to the caller's existing topic).
- Makefile UPDATED (workspace root `makefile`, copied from the latest upload — IMAGE_TAG now v1.0.1061): 4 insertions applied by cloning thunder's exact tab-preserved blocks (build target L140, build-adapters aggregate L169, push line L207, guarded deploy-agents block L911). Verified: make -n expands build-analyser-adapter correctly (with a stub .env; the repo's real .env satisfies the include). kustomize build NOT run here (release CDN off the container allowlist) — static cross-ref checks all pass (generator name = deployment configMap name; ../../../base resolves; images.name matches deployment image; generator source exists). User: run `kubectl kustomize deployments/kustomize/services/analyser-adapter/overlays/production/uk_001` as the 30-second pre-apply check.
- REMAINING to-create: build/docker/backend/analyser-adapter.dockerfile — NOT guessed; copy thunder-adapter.dockerfile and swap the binary/cmd name (upload it if you want the swap drafted). Then: secret, topic apply, registry paste, agent INSERT, end-to-end.

## ═══ STATE OF PLAY — 2026-06-11, end of session (consolidates today's diary) ═══

**Where everything is.** PLAN_workflows_and_actions_migration.md carries the full analyser-thread history + the deployment sequence; RUNBOOK_thin_slice.md "In-cluster path" has the command-level deploy/first-run procedure; contextkit/README.md is the tree-anchored destination map (in the rebuilt contextkit.tar.gz); 035_adapter_guide.md is the canonical adapter doc (envelope contract §1 + construction §2; FOCUS_adapter_design retired); registry_insertions.md has the paste-ready registry block; the updated `makefile` is at the workspace root. All chassis drafts: chassis-drafts/analyser-adapter/ (adapter.go, analyse_action.go, github_source.go, code_symbols_actions.go, analyser_request_action.go, cmd/…/main.go, configs/…, deployments/kustomize/…, deployments/kafka/…, NNN_create_code_indexer_agent.sql, registry_insertions.md, the staged flat manifest analyser-adapter.yaml now superseded by the kustomize set except as the Secret reference).

**Decisions locked today:** envelope = in_response_to_request_id matcher / real bools / ProduceWithValidation / body.action (empirical, from coordinator.go + git/thunder/dynamic; websearch deprecated); contract single-sourced in 035 §1; overlay-heavy kustomize (per-cluster config in the overlay, auth-service precedent); no rbac for the analyser; topic CRD standalone (namespace-override gotcha); registry IsLocal:true for adapter-calling actions.

**Ordered next steps (user-side):** dockerfile name-swap copy → the RUNBOOK §In-cluster sequence (secret → topic → registry paste [decide Category "code" vs "analysis"] → \d agent_definitions + INSERT → kustomize dry-build → make release-backend → smoke → first index → code_symbols verification → B4a live).

**Open decisions / pending items:** (1) code_symbols.repo naming convention — BEFORE indexing in anger; (2) 003 §832-890 → pointer to 035 §1 (text supplied 2026-06-11, not yet applied); (3) validator-tightening task (035 §1.10) not yet filed; (4) Category "code" is new — or reuse "analysis"; (5) runtime confirmations on first run: agent_definitions columns; awaited-reply nesting (analysis_field/commit_field/repo_field); (6) B4a code-domain embedding quality — the one unmeasured risk; (7) 035's own home in the repo (wherever 001/003 live); (8) later: error_recoverable for transient fetch (github_source sentinel); sed-trap note for 035 §2.12; canonical types.ResponseHeaders gaining request_id/message_id so adapters can drop the local mirror.

## Design direction — thousands of domains: tools (JS+backend), provenance, docs (discussed 2026-06-11)
- THREE concerns, THREE existing homes (do not unify into one table): code STRUCTURE → code_symbols via the analyser (JS parser behind the same analyse action); prose DOCS → knowledge_base via rag_index (collection e.g. tool_docs, metadata {client_id, domain, tool_id, version}); generation PROVENANCE → relational columns on the tool/site rows (version/previous_version_id style; created-by agent/orchestration, template/prompt ref). Join keys across the three: repo label + path + commit_sha (+ content_hash as code-level provenance).
- LABEL at scale: domain/site-keyed and DERIVABLE FROM THE SITE RECORD (site:<site_id> stable vs domain:<name> readable; chassis stays owner/repo). Store the label ON the site/tool row; indexer triggers + lookup configs read it from there (kills silent label mismatch). Tenancy at thousands of client domains → explicit client_id COLUMN (parameterised isolation/deletion) over string-prefixing; the DDL's pencilled prefix note (plan B5) is superseded by this lean.
- SOURCE SEAM generalises: repoSource (FetchToDir) is the interface — a DB-backed/S3-backed source materialises tool code from rows into a temp dir; "repo" = any labelled, versioned code corpus; commit_sha carries tool version/content digest for non-git corpora.
- SCALE: per-domain slices are small (hundreds of symbols) → low millions of rows total; CAVEAT: global HNSW + highly selective repo filter can degrade filtered ANN (pgvector 0.8.0 iterative scans mitigate; fallback = exact cosine scan per small slice under the (repo,…) index — measure before engineering). Batch indexing = the existing dispatch-loop pattern (orchestrator spawns code-indexer children); trigger = index-on-deploy (deploy already flows git adapter → GitHub Actions → B2); content_hash makes re-index incremental.
- DOCS RULE (carries FOCUS_js_tools_documentation forward): the workflow that GENERATES a tool also generates its prose doc and indexes BOTH (adjacent steps: rag_index + index_code_symbols); spec/history is a seed not docs; never plan to retro-document thousands of tools — the JS parser recovers structural surface where prose is missing.
- SCHEMA CHECKS before drafting any of this (\d first): where tool code persists (tools table vs per-domain repos vs both); the tools table's provenance columns; knowledge_base metadata filterability for {client_id, domain, tool_id}.

## GROUNDED 2026-06-11 (019_tool_library + 020_tool_lifecycle + \d clients_db + \d knowledge_base): tools/provenance/docs design corrected
- TOOLS ARE DB ROWS, NOT REPOS: content_components, component_level='tool', FULL html_template (style+main+script, self-contained IIFE, no external deps); FORK-ON-DEPLOY — library originals forked_from IS NULL, per-site forks forked_from=<uuid>, name={tool}-{domain-slug} (function NOT slugged); no cascading library→fork updates (deliberate). js_snippets is a SEPARATE thing (the JS sweep), not the tool store. "tool db" question answered.
- PROVENANCE LARGELY EXISTS: forked_from lineage; created_from CHECK (manual/generated/adopted/tool/forked); component_versions snapshots (update_component_html + change_description); is_active. knowledge_base ALREADY carries source_agent_type, source_orchestration_id, source_type/url, and first-class domain + industry columns + collection + (collection,content_hash) dedup. ONE residual check: does content_components stamp the creating orchestration/agent (\d content_components) — add one column if not.
- DOCS HALF-BUILT: deploy_tool_to_site AND create_tool_component already create a COMPANION GUIDE page per tool → the prose exists; the gap is INDEXING it for agents. Plan: add a rag_index step to tool-generator/tool-deployer workflows (collection 'tool_docs', domain column set, source_* stamped) + one batch backfill of existing guides (retrofit is cheap here because the writing already happened — softens the never-retrofit warning).
- CODE_SYMBOLS FOR TOOLS: DEFERRED deliberately. Each tool = one IIFE → thin symbol pickings; tool discovery is already solved by semantic_tags + display_name + metadata-only listing rule (019: NEVER load html_template in listing/discovery queries — applies to any future indexer too). Index tools only when a concrete consumer arrives (cross-tool code search for auditor/improver; assets-split JS). Then: DB-backed repoSource materialises script blocks; label domain:<domain> for forks / library:tools for originals; commit_sha ↦ component_versions id or content hash.
- IMMEDIATE WIN INSTEAD: FORK-DIVERGENCE detection — pure SQL discovery check (tier-1, zero cost) comparing fork html_template hash vs library original via forked_from → answers "which forks are unmodified / safe to bulk-push a library change" (019's per-site-triage pain). Analyser later upgrades to symbol-level WHAT diverged.
- Chassis code-indexing path UNCHANGED by all of this.
- Ordered plan: (1) rag_index step in the two tool workflows + guide backfill; (2) \d content_components creator-stamp check; (3) divergence SQL check; (4) chassis indexing as planned; (5) tools→code_symbols only on a real consumer.

## DECISION DIRECTION 2026-06-11: in-code documentation for new tools (grounded in \d content_components)
- Schema facts: NO creator stamp on content_components (created_from = mechanism enum only; agent_type/agent_workflow/data_sources = dynamic-component render hooks, NOT provenance); `description` column EXISTS and is the idle machine-readable home; `js_content` column exists separate from html_template — check what uses it (019 says tools are all-in html_template; possible precedent for the assets-split).
- KEY CONSTRAINT others would miss: html_template IS the shipped product — the script block lands in rendered_html → deployed public HTML. So in-code docs SHIP to visitors. Verbose JSDoc = pointless public bytes; provenance ids in code = publishing internals. NEVER put orchestration/agent ids in tool code.
- RECOMMENDATION (yes-but-small): ONE standardised 4-6 line header in the tool script — purpose one-liner, behavioural invariants (units, ranges, no-external-calls), library function + version marker. Payoffs: (1) tool-auditor audits AGAINST a stated spec (tier-2 upgrade: "does code match stated behaviour"); (2) anti-drift anchor across update_component_html / tool-improver LLM rewrites; (3) becomes code_symbols.doc when the JS parser arrives — the only meaningful symbol doc an IIFE has → embedded retrieval text.
- SUBSTANTIVE doc lives where it doesn't ship + is queryable: same generated text written ONCE by the creating workflow into `description` (listings/discovery, respects never-load-html_template) AND knowledge_base tool_docs (the rag_index step). Four layers (header/column/KB/companion guide), one author step, zero hand-maintenance — docs-at-creation holds.
- PROVENANCE: add created_by_agent_type + created_by_orchestration_id columns to content_components (mirrors knowledge_base's existing pattern; set by create_tool_component/deploy_tool_to_site). FIRST check whether system_events/entity_state_log already capture component creation (may cover existing rows).
- ENFORCEMENT (existing machinery): header baked into tool-generator prompt + create_tool_component validation; tier-1 tool_health gains zero-cost `no_doc_header` WARNING (converges the inventory on the normal sweep — no retrofit campaign); tool-improver prompt told to preserve/update the header.

## DONE 2026-06-11: tool-doc header set built (chassis-drafts/tool-docs/)
- STRIP DECISION (user yes/no answered): YES — but SENTINEL-strip of our standardised block only, NEVER general JS comment stripping (regex destroys "https://…" strings + regex literals; safe general stripping = a JS minifier dependency on the hot render path with silent code-corruption as the failure mode). Sentinel scan = no parsing, idempotent, cannot corrupt sentinel-free code; malformed block (no closer) left UNTOUCHED (ship inert comment > truncate a script) and flagged by tool_health. Strip at rendered_html production for EVERY component (no-op when absent). Because it no longer ships, the header can be 6-12 lines (invariants/inputs/outputs) not 4-6. Internal consumers unaffected (auditor/improver/parser read DB html_template); fork-divergence hash (DB column) unaffected.
- tool_doc_header.go — package content (relocate beside compile_page_sections): ToolDocOpen="/* === tool-doc ===" / ToolDocClose="=== /tool-doc === */" consts + HasToolDocHeader + StripToolDocHeader (loops multiple blocks, swallows one trailing newline, leaves malformed untouched). COMPILED + 12 behaviour tests PASS in-container (URL-with-// intact, regex literal intact, idempotent, no-op absent, malformed untouched, multi-block).
- NNN_add_component_provenance.sql — ADD COLUMN source_agent_type varchar(100) + source_orchestration_id varchar(255) on content_components, names MIRRORING knowledge_base's pair (one mental model); load-bearing COMMENTs disambiguate from the unrelated render-hook agent_type column; no backfill (NULL = honest absence; pre-check queries for system_events/entity_state_log trail included); no index (forensic pattern); set-on-insert guidance for create_tool_component_action.go + deploy_tool_action.go (values from the execution context, no new plumbing).
- 019_addition_tool_doc_header.md — the contract as a SPLICE into 019 (canonical-home discipline, the 035 lesson): format + exact sentinels + 6-12 line example; rules (function: matches column; NO ids/names/dates in code; no */ inside); lifecycle table (generator prompt requires + create_tool_component validates HasToolDocHeader; forks copy free; improver preserves/updates behaviour: lines; auditor audits AGAINST the stated invariants; strip at render; tool_health no_doc_header WARNING + malformed_doc_header ERROR); the generator-prompt snippet verbatim; the 072 lesson restated (locate nested prompt path, snapshot_agent first); 020 table-row additions listed.
- INTEGRATION still user-side (sources not in hand): splice points in compile_page_sections (strip call), check_tool_health.go (two checks), create_tool_component_action.go (validation + source_* stamping), deploy_tool_action.go (source_* stamping), tool-generator/tool-improver agent_definition prompt UPDATEs. Upload those files for precise patches if wanted.
- js_content column still unexplained (one grep user-side: what reads/writes content_components.js_content) — relevant to the assets-split future.

## GROUNDED 2026-06-11 (project mount + check_tool_health.go/check_tool_completeness_action.go uploads): splice-point map + js_content RESOLVED
- **js_content RESOLVED — the assets-split EXISTS** (003 "JS Content Separation Contract" + rerender_single_page_action.go): for interactive components (games/feeds/explorers), store_generated_component_action.go separateInlineJS() extracts inline <script> bodies → content_components.js_content; html_template keeps `<script src="/tools/assets/{function}.js">`; RerenderSinglePageAction.collectJSAssets() (project file, L137-170) builds the files map {page.html, tools/assets/{function}.js,...} → page-rerender git_commit multi-file → CDN. js_snippets = SHARED design effects (separate, applies_to). 019's "split isn't built" refers narrowly to library TOOLS staying inline. TWO JS carriers confirmed.
- **DESIGN IMPLICATION:** a tool-doc header inside js_content would SHIP in the public .js asset → StripToolDocHeader must ALSO run at the asset path (one line in collectJSAssets: assets[assetPath] = content.StripToolDocHeader(jsContent)). And js_content is the CLEANER future JS-parser source (raw JS body, no <script> extraction needed) for the DB-backed repoSource.
- **rendered_html writer/assembly map (the strip splice points):** (1) save_page_sections_action.go L351-354 INSERT INTO page_components(...rendered_html...) VALUES(...section.HTML...'deployed') — the primary build-path write; (2) RerenderSinglePageAction assemblePage() (L87/L214+) reads site/area/page component rendered_html → page html → files map (deploy-time choke point, html + collectJSAssets); (3) rerender_pages_actions.go reads rendered_html (L585) — check for writes when integrating; (4) create_tool_component_action.go + deploy_tool_action.go set rendered_html at tool creation (020) — FILES STILL NOT IN HAND. Recommendation stands: strip at the OUTBOUND points (assemblePage output + collectJSAssets) AND/OR at rendered_html writes; minimal correct set today = assemblePage html + collectJSAssets + the two tool actions' creation writes (pending upload).
- **Validation + source_* stamping splice points:** create_tool_component_action.go and deploy_tool_action.go are NOT in the project mount or any upload — named in registry/docs only. UPLOAD NEEDED for precise patches (validation HasToolDocHeader gate + source_agent_type/source_orchestration_id stamping per NNN_add_component_provenance.sql).
- **tool-generator / tool-improver prompts:** NOT in files — they are agent_definitions ROWS in clients_db. Pull via SELECT type, default_config FROM agent_definitions WHERE type IN ('tool-generator','tool-improver'); locate the NESTED prompt path (072 lesson: prompts sit in loop sub_workflows), snapshot_agent first, targeted jsonb_set. The snippet text is in 019_addition_tool_doc_header.md.
- **PRECEDENT STRENGTHENS THE DESIGN:** check_tool_completeness_action.go ALREADY does marker-strip-before-downstream (toolCompleteMarker "<!-- tool-recreation-complete -->", strings.Replace at L86) — an in-pipeline sentinel-strip precedent; the tool-doc strip follows an established pattern, not a new idea.
- **BUILT:** chassis-drafts/tool-docs/check_tool_health.go — the uploaded file PATCHED with checks 9 (no_doc_header, warning) + 10 (malformed_doc_header, error — "would ship"), TEMPLATE-ONLY (rendered_html is post-strip; guarded on templateHTML!="" since empty_template blocker covers that), import of platform/content sentinels (path reconcile note), doc-comment checklist updated. gofmt clean. Rides the existing improve_tool flow + 7-day cooldown automatically. Noted existing inefficiency (auditTool computed twice per tool, tier1+tier2 loops) — not mine to fix in this patch.
- tool_health only audits DEPLOYED forks (joins pages on a site) — library originals are gated at create_tool_component validation instead; 019 addition wording aligned.

## VERIFIED 2026-06-11: 019's "isn't built yet" — precise verdict (sources now in hand)
- VERDICT: FALSE as implied (a template/JS split exists, fully built), TRUE in two narrow senses: (a) the mechanism 019 sketched (assets TABLE → S3) is NOT the one built — the built one is js_content column → collectJSAssets → multi-file git commit, no assets table; (b) the TOOL pipeline specifically never uses it (generator prompt mandates one inline <script>; create_tool_component INSERT has no js_content; improver prompt mandates inline; fork SELECT omits js_content). Correction splice drafted: chassis-drafts/tool-docs/019_correction_js_split.md.
- separateInlineJS verified (store_generated_component_action.go L815+): matches ONLY attribute-less `<script\s*>` (src=/type= untouched — deliberate safe scope); combines multiple blocks; inserts the src ref after the LAST </section>; no-op when no inline JS. js_content written on BOTH the UPDATE (L383-389) and INSERT (L443-449) paths via nullIfEmpty.
- LANDMINE (unarmed today): deploy_tool fork INSERT…SELECT copies html_template but NOT js_content → a library tool adopting the split would fork with the <script src> reference and no asset → deployed page 404s its own script. Fix the fork copy BEFORE tools ever adopt the split. Second gap (self-documented at L920): component_versions lacks a js_content column → snapshots of split components lose the JS.
- PROMPT PATHS NOW KNOWN (agent_definitions dump in hand): tool-generator → workflow.steps.generate_tool_html.config.prompt_template; tool-improver → workflow.steps.improve_tool.config.prompt_template. BOTH TOP-LEVEL steps — simpler than the 072 nested-path worry (which applied to page-content-writer). snapshot_agent before UPDATE still mandatory. Header snippet must be reconciled with each prompt's existing Rules list (generator rule 3 "single <script> block at the bottom" + the Structure example; improver rule 8 "inline <script>") — the header block goes at the TOP of that single script block.
- Also observed: tool-generator's workflow (ensure_site_record → load_brand_context → generate_tool_html → save_tool → complete) has NO check_tool_completeness step — that action rides the tool-RECREATION flow only. The HasToolDocHeader validation gate therefore belongs in create_tool_component (Go), which all generator output passes through.
- The strip-at-collectJSAssets point reconfirmed by reading separateInlineJS: any doc header inside an extracted script body lands in js_content verbatim → would ship in the public .js.

## DONE 2026-06-11: the four tool-doc patch sets BUILT (chassis-drafts/tool-docs/, all gofmt clean)
- STRIP PLACEMENT DECIDED: at the OUTBOUND deploy assembly, not at rendered_html writes — DB rendered_html deliberately RETAINS the header (audit/parse parity with the template); only what ships is stripped. THREE strip sites: (1) RerenderSinglePageAction html after assemblePage, (2) collectJSAssets emitted values, (3) rerender_pages_actions.go bulk finalHTML (independent deploy path — added to its cleanup chain). check_tool_health.go comments CORRECTED to match (earlier wording wrongly claimed rendered is post-strip).
- create_tool_component_action.go: platform/content import + HasToolDocHeader HARD GATE after StripCodeFences (error names both sentinels; notes the generator workflow has no completeness step) + source_agent_type/source_orchestration_id stamped on the component INSERT ($8,$9 via nullIfEmpty from ExecutionContext.Sender.AgentType / .OrchestrationID).
- deploy_tool_action.go: fork INSERT…SELECT gains the two source_* columns as $4,$5 (the DEPLOYING actor; forked_from keeps lineage). FLAGGED-NOT-CHANGED in a code comment: forks default created_from='manual' (CHECK allows 'forked' — behaviour change beyond this patch); js_content still not copied (the known landmine, harmless while tools stay inline).
- rerender_pages_actions.go observation (comment in code): the bulk path has NO collectJSAssets equivalent — js_content assets only ship via single-page rerenders. Pre-existing; recorded, not fixed.
- NNN_update_tool_prompts_doc_header.sql: anchored replace() DO blocks (NOT whole-prompt overwrite — survives unrelated edits, ABORTS LOUDLY on anchor drift), idempotence guards (skip if '=== tool-doc ===' present), snapshot_agent first, paths verified TOP-LEVEL from the live dump (072 trap n/a here), VERIFY query at the end. Generator gets rule 13 + the Structure example's script block rewritten with a literal header (no new {{template}} variables — deliberately); improver gets rule 10 (preserve/update behaviour: lines; add if missing).
- DEPLOYMENT ORDER (load-bearing, recorded in file comments too): (1) NNN_add_component_provenance.sql — columns must exist before the stamping binaries; (2) NNN_update_tool_prompts_doc_header.sql — prompts must teach the header before the gate enforces it; (3) one release with the five Go files (gate + stamps + 3 strips + checks). Then: generate one tool end-to-end; verify header in html_template, ABSENT in the committed page/asset, source_* populated, tool_health quiet on it.

## DONE 2026-06-11 (close of thread): docs reconciled to the final strip-at-deploy design
- 019_addition_tool_doc_header.md (two spots) + tool_doc_header.go's own comments (two spots) corrected: the strip runs at deploy assembly (three named call sites), rendered_html RETAINS the header — earlier wording said "where rendered_html is produced", contradicting the built patches. gofmt re-verified.
- PLAN changelog: tools thread marked apply-ready (supersedes the "Remaining: uploads" line). RUNBOOK gains "Tool-doc header rollout": the three-stage order (provenance migration → prompts SQL → one binary release), end-to-end verification queries (header in template / provenance stamped / 'tool-doc' absent from committed page+asset / tool_health quiet), and the diagnostic for a failing gate (check stage-2 NOTICEs before blaming the LLM).
- Transient str_replace read error on the 019 addition observed (file intact via bash; retry clean) — same class as the earlier tar I/O hiccup; outputs FS occasionally flaky, retry before concluding loss.

## DONE 2026-06-11: 019/020 CONSOLIDATED — splice files retired
- Produced full updated canonicals at chassis-drafts/tool-docs/: 019_tool_library.md (804 lines) + 020_tool_lifecycle.md (473 lines). These REPLACE the repo docs wholesale; the two staging splices (019_addition_tool_doc_header.md, 019_correction_js_split.md) are deleted from staging — consolidated in.
- 019 edits (8, all anchor-asserted): template-structure brevity pointer; full Tool Doc Header section after Design conventions (strip-at-deploy wording, three call sites; prompt-application paragraph REWRITTEN — top-level paths verified, NNN_update_tool_prompts_doc_header.sql codifies the rules, 072 trap n/a here, snapshot-first stands — the uploaded addition variants both still carried the stale nested-path advice); Step-1 checklist bullet (gate); BOTH SQL placeholders (library INSERT + modify-library SET) now show the header — the second occurrence was a genuine catch, the modify path must preserve it; tier-1 quality table + no_doc_header/malformed_doc_header rows; when-to-split subsection REPLACED (split exists for component-creator, not tools, not via assets table; the two gaps incl. the fork js_content landmine); deploy behaviour step 3 + create_tool_component behaviour paragraph updated (gate + source_* stamps).
- 020 edits (5, anchor-asserted): tool_health tier-1 bullets + the two header checks (with the template-only/rendered-retains note); Go Actions table — both tool-action rows amended + HasToolDocHeader/StripToolDocHeader row added; Content Components Model + provenance paragraph (source_* mirror kb, disambiguated from render-hook agent_type); Implemented list + the contract bullet.
- chassis-drafts/tool-docs/ now: 2 consolidated canonicals + 6 Go files + 2 SQL migrations. Apply = replace 019/020 in the repo + the RUNBOOK rollout order.

## DISCUSSED 2026-06-11: doc up-to-dateness — the two staleness events and the layered fix
- Two distinct events in one day: (1) 019's stale NEGATIVE claim ("isn't built yet") survived because nothing couples doc claims to code — nearly caused a reuse-before-recreate violation (designing the split from scratch); (2) my OWN same-day docs went stale within hours when the strip-placement decision changed — proving scheduled freshness sweeps structurally cannot keep up. Staleness is a COUPLING property, not an age property.
- Claim taxonomy by checkability: code existence/behaviour (grep/symbols), schema (\d), DB rows (SELECT) = mechanically checkable; design rationale = not; NEGATIVE claims ("X isn't built") = hardest — existence checks can't falsify them unless stated in checkable form.
- POINT-OF-USE principle (the thing that actually saved us, twice): schema-before-SQL generalised to source-before-design — verify a claim when it becomes load-bearing, not on a schedule. Writing side: falsifiable claims carry evidence pointers (file/line/date, the 035 §1 model); negative claims carry the falsifying command + date. One-line rule for 001/016, zero code.
- Workflow step (later, reuses existing machinery): tier-1 `doc_drift` discovery check — backtick-reference extraction → existence vs repo tree/information_schema → update_doc items; HONEST LIMIT: catches renames/removals only, NOT semantic drift or stale negatives. Right trigger is COMMIT-DRIVEN not scheduled (drift is created by commits) — needs the doc→code reference index, which the analyser/code_symbols + tool_docs KB indexing already on the roadmap provide; tier-2 LLM doc-auditor (cooldown like tool-auditor) over contextkit-assembled code context = the contextkit dogfood case and the configuration that WOULD catch "isn't built yet".
- Deepest fix is upstream: docs that RESTATE code rot silently, docs that POINT rot detectably; single-sourcing (035/FOCUS, pending 003 pointer) shrinks the rot surface; docs update IN THE SAME CHANGE as the decision (docs-at-creation applied to ourselves).
- Deliberate non-action: NO whole-doc "verified" stamps — verification attaches to CLAIMS, never documents (false-trust risk).
- Ordered: (1) NOW the 001/016 discipline rule (offered to draft); (2) SOON tier-1 doc_drift on the weekly sweep; (3) LATER commit-triggered flagging + optional doc-auditor once analyser + tool_docs indexing are live.

## DONE 2026-06-11: claim-verification discipline CODIFIED (016 v2_34 item 24 + 001 pointer + dated-claim convention)
- CANONICAL HOME: 016 §0 Assumption Checklist gains item 24 (single-sourced; 001's "Rule: grep before using" gets a pointer paragraph, not a copy). Consolidated copies produced: chassis-drafts/tool-docs/016_debugging_guide_v2_34.md (changelog line added in the doc's italic-version style) + 001_development_guide.md.
- THE CONVENTION (user's dating requirement, designed greppable for future tooling): falsifiable load-bearing claims carry `[checked YYYY-MM-DD: <evidence>]` — evidence = the command or source location; re-check updates the date IN PLACE (one date = last checked, not a history); negative claims carry their falsifying command; rationale/intent untagged; whole-doc "verified" stamps BANNED (claim-level only). Fixed `[checked ` token = mechanically extractable by the planned doc_drift check and commit-triggered flagging — which catch renames/removals only; point-of-use verification stays the always-current control. Item 24 carries today's two concrete instances + disciplines (a)-(e) incl. docs-in-the-same-change.
- DEMONSTRATED in the consolidated 019's When-to-split section: 3 tags on claims genuinely verified today (split-is-built; tools-stay-inline; the landmine's negative claim — tagged honestly as code-path-verified with the row-state SELECT left for the user to run, since we never ran it). The Tool Doc Header section deliberately UNTAGGED — it describes the not-yet-applied patch set; tags attach to claims about the live system.
- Apply: replace 016/001 in the repo alongside the 019/020 consolidations. Honest naming note: 001 emitted as 001_development_guide.md (the upload was a "(3)" download-suffixed copy — reconcile with the repo's actual filename).

## DONE 2026-06-11: analyser thread resumed — pre-apply consistency sweep + repo convention IMPLEMENTED
- SWEEP (cross-file, scripted): topic string in all 5 places ✓; consumer group in 3 ✓; config path chain (main.go default ↔ deployment command/mount ↔ overlay generator) ✓; env names consumed (REQUESTS_TOPIC, CONSUMER_GROUP, GITHUB_READ_TOKEN, GITHUB_API_BASE, POD_NAME, HOSTNAME-fallback) ⊆ declared ✓; AGENT_VERSION declared-but-unread by the adapter — matches thunder's deployment pattern, harmless, left as is; port 8080 chain ✓; secret name analyser-github-read verbatim in deployment+staged manifest ✓; the three registry handlers exist with correct names ✓; image name chain (deployment :latest / overlay name+newTag / makefile target+push) ✓; complete step uses output_fields PLURAL (016 item 23) ✓.
- SWEEP FIND: the agent SQL's index step carried repo_field "repo_analysis.repo" → would have labelled code_symbols.repo with the BARE name "agentchassis" — exactly the colliding form the convention discussion rejected.
- CONVENTION IMPLEMENTED (option c — structural, complexity-in-Go): IndexCodeSymbolsAction now COMPOSES the label as owner+"/"+repo from the analyser reply when config.repo/repo_field is unset — the label always matches what was actually fetched; no trigger-supplied label to mismatch. FLAGGED changes: two NEW config keys owner_field/repo_name_field (paths, defaults "repo_analysis.owner"/"repo_analysis.repo"); input_data.repo fallback now AFTER composition; error message updated. gofmt clean. Resolution order: explicit override → composed → input_data.repo.
- SQL updated: repo_field dropped from index step (composition is the default path); convention comment block added with a [checked 2026-06-11: …] tag (the new 016 item-24 convention in use); image_tag → v1.0.1061 with set-at-apply note. RUNBOOK: settle-before paragraph REPLACED with the decided convention; verification SQL concretised to WHERE repo = 'gqls/agentchassis'.
- 035 §2.12 gains the deploy-agents sed-trap lesson (queued micro-item closed).
- DOCKERFILE remains the one undrafted item — 035 names the pattern but not the body; copy thunder-adapter.dockerfile + name swap per RUNBOOK step 0, or upload it for the swapped draft.

## FIXED 2026-06-12: code-indexer INSERT — domain_tags type error (the \d finally in hand)
- Apply attempt failed at planning: domain_tags is JSONB (default '[]'::jsonb) but the SQL passed text[] (ARRAY['…']) — fixed to '["code-context","indexing"]'::jsonb. Single-statement INSERT → nothing partial landed; re-run is clean.
- Column-by-column verification against the supplied \d agent_definitions [checked 2026-06-12]: NOT NULLs covered (type/display_name/category explicit; id/created_at/updated_at/idle_timeout_seconds defaulted); agent_category 'integrator' ✓ CHECK; status 'experimental' ✓ CHECK; all other jsonb casts correct; topics + image_repository values happen to EQUAL the column defaults (kept explicit for readability — flagged, not trimmed, per item 16: change only what the error indicts); default_config.workflow placement matches the live tool-generator/improver rows (not the task_workflow/orchestrator_workflow columns); is_snapshot defaults false ✓.
- Re-run guard: UNIQUE (type, version) rejects a duplicate code-indexer v1 — revision path is snapshot_agent then UPDATE. Recorded in the file header with the [checked 2026-06-12] tag.
- NOTE: the session crossed into 2026-06-12 — prior entries dated -11 stand as written.

## DONE 2026-06-12: code-indexer agent row APPLIED (INSERT 0 1) + runbook steps 0-1e expanded to command level
- The corrected INSERT applied cleanly. Runbook "In-cluster path" steps 0-1e rewritten with full granularity at the user's request: file-copy manifest with exact repo destinations (+ the internal/analysis diff check + makefile diff-before-replace), registry paste as a 4-substep procedure (anchor, block, the one-word Category decision, go build compile check), dockerfile copy-swap with the grep-for-thunder verification + read-once instruction + make -n dry-run, agent INSERT marked DONE with the existence SELECT, dry-build with the expected three objects (hash-matched ConfigMap reference) and the two named failure modes.
- Remaining sequence: 1a/1b check-offs (secret + topic) if not yet applied → 0/1c/1c'/1e → make release-backend → §3 smoke → §4 first index (watch the awaited-reply nesting caveat) → §5 code_symbols verification (WHERE repo='gqls/agentchassis') → §6 B4a.

## CLARIFIED + EXTENDED 2026-06-12: analyser config family + overlay-heavy kustomize
- CONFIG FAMILY [checked 2026-06-12: uploaded thunder/agent-chassis/content-creator/git configs + main.go]: there are TWO config loaders. NESTED family (service_info/server/logging/observability/infrastructure[/custom]) = thunder, agent-chassis, content-creator — and cmd/analyser-adapter/main.go reads cfg.ServiceInfo.*/Logging.Level/Server.Port, so the analyser belongs HERE. FLAT family (kafka:/github:/health:/monitoring:/env:) = git-adapter ONLY, a different loader. My drafted analyser config was ALREADY in the correct nested family — the "wrong format" impression was it being the deployed-source copy, not a flat-shape error. Self-corrected mid-turn: nearly mis-advised reshaping to git's flat layout; verified before acting. Only the config HEADER was changed (documents the family + the [checked] tag); shape untouched.
- OVERLAY-HEAVY restructure (user wants more config in overlays for cross-deployment flexibility): base deployment env REDUCED to cluster-agnostic identity only (POD_NAME, AGENT_VERSION, GITHUB_READ_TOKEN secretKeyRef). The three cluster-varying values (REQUESTS_TOPIC, CONSUMER_GROUP, GITHUB_API_BASE) MOVED to a new overlay strategic-merge patch overlays/production/uk_001/deployment-env.yaml, wired via patchesStrategicMerge. Plus the existing per-cluster configMapGenerator + configs/ copy. Net: a new cluster copies the overlay, edits deployment-env.yaml + its config copy, NEVER touches base.
- VERIFIED by Python simulation of the two kustomize mechanics that changed (no kustomize binary reachable — CDN + go proxy both off-allowlist; pip pkg not a binary): strategic-merge env (patch targets base Deployment + container by name; merge-by-name yields all 6 env vars; base no longer hardcodes the 3); configMapGenerator name ↔ config-volume reference; patchesStrategicMerge wired; exactly one newTag (sed-trap guard). All parse.
- CLUSTER STATE confirmed by user: analyser-github-read secret NOT yet created; analyser requests topic NOT yet present. Both are RUNBOOK steps 1a/1b — still to do before release.
- Files now in deployments/: kustomize base (deployment/service/kustomization) + overlay (kustomization/deployment-env.yaml/configs/analyser-adapter.yaml) + kafka/analyser-requests-topic.yaml.

## DONE 2026-06-12: AGENT_VERSION moved base → overlay (per-cluster release-tag tracking)
- Decision (user): AGENT_VERSION should track each cluster's release tag, so it left the base and joined overlays/production/uk_001/deployment-env.yaml (value v1.0.1060, matching the overlay's image newTag). Base env is now POD_NAME + GITHUB_READ_TOKEN only (pure cluster-agnostic identity); base comment updated to list AGENT_VERSION among the overlay-injected values. Verified by merge simulation: base={POD_NAME,GITHUB_READ_TOKEN}, overlay adds the four (REQUESTS_TOPIC, CONSUMER_GROUP, GITHUB_API_BASE, AGENT_VERSION), merged=6; all files parse.
- COHERENCE CAVEAT recorded in deployment-env.yaml: AGENT_VERSION and the overlay image newTag must be kept in step at each release — MANUAL today, because deploy-agents seds only newTag, not this env value. Candidate future tidy: either drive AGENT_VERSION from the Downward API/image ref, or extend deploy-agents to sed both. Until then the two-line edit at release is the procedure.
- The overlay-heavy split now covers: image tag, the four env values (incl. AGENT_VERSION), and the per-cluster config copy — a new cluster edits only its overlay (kustomization newTag + deployment-env.yaml + configs/analyser-adapter.yaml), never the base.

## DONE 2026-06-12: makefile re-cut from a FRESH upload (IMAGE_TAG now v1.0.1063) + 4 analyser insertions
- User uploaded a fresh makefile with NO analyser entries (newer than the session's earlier .1061 copy — this one is v1.0.1063). Re-applied the four insertions by cloning thunder's exact tab-preserved blocks: build-analyser-adapter target with its .PHONY line (L140-144), build-adapters aggregate (L169, analyser appended after thunder), push-backend line (L207, inside push-backend target confirmed), deploy-agents guarded block (L911-915, sed on the overlay's single newTag + conditional kubectl apply -k).
- Verified: make -n expands build-analyser-adapter to the right docker build -f path; build-adapters dry-run includes analyser (3 mentions); NO missing-separator/tab errors; push line sits inside push-backend (L196). Stub .env satisfied the makefile's `include .env`.
- The deploy-agents sed runs on the overlay kustomization — the analyser overlay has exactly ONE newTag (the sed-trap guard from earlier holds), so the release tag-bump is safe.
- NOTE: AGENT_VERSION coherence caveat still stands — deploy-agents seds newTag only, not the overlay's AGENT_VERSION env value; keep them in step manually at release.

## FIXED 2026-06-12: deploy-agents kustomize error — overlay configs/ missing in repo + patchesStrategicMerge deprecation
- FATAL: "evalsymlink failure on .../overlays/production/uk_001/configs: no such file or directory" (loading KV pairs for the configMapGenerator). CAUSE: the overlay's configs/ subdir didn't survive the copy into the repo — staging HAS overlays/production/uk_001/configs/analyser-adapter.yaml (verified present + parses + generator path matches), so it's a copy gap, not a content bug. FIX: mkdir the overlay configs/ in the repo + cp the one file (or cp -r the whole tree). Added to RUNBOOK dry-build failure-modes with exact commands.
- NON-FATAL: "patchesStrategicMerge is deprecated" warning. Updated the overlay to the modern form: patches: [{path: deployment-env.yaml}]. Re-verified by simulation — patches: targets base by kind+name identically, merged env still 6, configMapGenerator + single-newTag unchanged, parses. (deploy-agents ran PAST this warning; only the missing configs/ stopped it.)
- This is the same class as the earlier RUNBOOK "no such file → overlay configs/ copy missing" prediction — it actually happened; the predicted fix was correct. cp -r the analyser-adapter kustomize tree wholesale is the durable avoidance.
- Both fixes are repo-side (the staging files were already correct except the now-modernised patches: key). User: create the dir + copy the file, re-run make deploy-agents.

## ═══ MILESTONE 2026-06-12: analyser-adapter DEPLOYED TO PRODUCTION ═══
- First piece of the contextkit thread to reach production (uk_001). The build-to-deploy arc is complete: empirical envelope contract → adapter lifecycle + main.go + config → registry insertions → code_symbols actions (+ owner/repo label composition) → kustomize (overlay-heavy: config, 4 env values incl. AGENT_VERSION, image tag all per-overlay) → makefile (4 insertions, re-cut at v1.0.1063) → KafkaTopic CRD → dockerfile name-swap → code-indexer agent row (INSERT 0 1). Deploy hiccups resolved along the way: domain_tags jsonb cast; missing overlay configs/ dir (copy gap, predicted by the runbook); patchesStrategicMerge → patches: modernisation.
- WHAT "DEPLOYED" ESTABLISHES: image built + pushed, kustomize applied, pod should be Running with the merged 6-env set and the mounted config. It does NOT by itself prove the request/response path or indexing — those are the post-deploy steps still to do.
- STILL TO DO (RUNBOOK "In-cluster path" steps 3-6), in order:
  * 3. Post-deploy smoke: pod 1/1 Running; logs show init then "starting message processing"; /health + /ready via port-forward; kcat envelope test (action in the BODY, expect in_response_to_request_id + status complete + real-bool is_complete).
  * 4. First indexing run: trigger code-indexer with input_data {owner, repo, ref}; watch BOTH adapter logs ("analysed repo" file_count → response sent) and indexer logs (request_repo_analysis → await → index_code_symbols counts). THE STANDING CAVEAT: if index_code_symbols errors "no analysis output at repo_analysis.output", inspect the awaited reply's nesting in collected_data and adjust analysis_field/commit_field/owner_field/repo_name_field (payload key "data" git/thunder vs "body" canonical) — do NOT guess; paste the collected_data shape.
  * 5. Verify code_symbols: counts/embedded/files + kind breakdown WHERE repo='gqls/agentchassis' (the composed label). 0 rows → check the repo label value before concluding failure.
  * 6. B4a — the one UNMEASURED risk: embedding quality on the code domain, now against the LIVE nomic path (sem.json from lookup_code_symbols, never -local); eval lex/sem/fused.
- PENDING DECISIONS unaffected by deploy: registry Category "code" vs "analysis" (whatever was pasted is now live — confirm which); 003 §832-890 → 035 §1 pointer still unapplied; AGENT_VERSION/newTag manual-sync at release (deploy-agents seds only newTag); the fork-divergence SQL check still draftable; the tools/docs thread (016 v2_34 + 001 + 019/020 + the 6 Go files + 2 migrations) still apply-ready per its own RUNBOOK rollout — SEPARATE from this deploy.

## 2026-06-13: B4a measurement underway — ollama URL (DNS) gotcha + early observations
- ERROR diagnosed: `embed` run from the LAPTOP against http://ollama-adapter.ai-persona-system.svc.cluster.local:11434 failed with "Temporary failure in name resolution" — DNS, not HTTP. svc.cluster.local names resolve only INSIDE the cluster (CoreDNS); the URL was well-formed, just unreachable from outside. The deployed adapter reaches ollama fine (it's a pod). Service confirmed present: kubectl get svc ollama-adapter → ClusterIP 10.21.234.10:11434, 86d old.
- FIX recorded in RUNBOOK B4a as two labelled options: (1) kubectl port-forward svc/ollama-adapter 11434:11434 (or deploy/ollama-adapter — targets the pod, sidesteps the Service) + point embed at http://localhost:11434 [the B4a default]; (2) run embed in-cluster (kubectl run/Job) where the original URL resolves. URL form reaffirmed: <service>.<namespace>.svc.cluster.local:<port>.
- SCALE measured: full chassis analysis = 4,494 symbols; embedding ~16/sec single-call → ~1hr end-to-end. That's the SPEED half of B4a (single-call indexing latency). User leaving it running. For faster iteration: analyse a subset.
- resolve_targets (lexical baseline) RAN on the real silent-no-op-rebuild task (gamesdesign.co.uk index.html): top hit SavePageSectionsAction (21), then saveSectionsLookupPageID/saveSectionsExtractFromHTML — i.e. lexical alone already points hard at save_page_sections, the exact area the bug write-up implicates. Good sign for the lexical spine; the embeddings run will show whether semantic adds recall beyond name-echo matches (resolve_targets itself flags that recall gap). NOTE this is the SAME silent-no-op-rebuild failure mode catalogued in 016 §9 — the contextkit B4a task and a real known bug coincide, which is a useful dogfood.
- This is all the local B4a measurement path — SEPARATE from the deployed adapter (which dials the same name correctly in-cluster). Production deploy not implicated.

## 2026-06-13: runbook early pipeline (1c/1d) — ollama access note added
- The early "## The pipeline" section's step 1c still showed the bare in-cluster ollama URL with no access note, so a top-to-bottom reader would hit the DNS failure ~140 lines before the B4a section that explains it. Added a concise two-option note inline at 1c (port-forward+localhost / run in-cluster) and switched the 1c+1d EXAMPLE commands to http://localhost:11434 so the copy-paste form actually works; full rationale + get-svc check stays single-sourced in §B4a (pointer only, no duplication).

## FOUND+FIXED 2026-06-13: B4a index contaminated by the repo's archived self-copies (analyser exclude gap)
- TWO problems in the uploaded sem.json/lex.json: (1) both ran -task "T" (the literal runbook placeholder) — NOT a real measurement; lexical correctly returned candidates:null ("T" tokenises to nothing — the matcher working, not broken), semantic returned 25 confident-but-meaningless NN hits (embeddings always return something). B4a is NOT yet measured. (2) The semantic index was built over ~/projects/agentchassis/ WHOLE, sweeping in docs/.../go_files_old/, docubundle/, thin_slice_run/, scripts/documentation_project/, and *(N).go download-duplicates: 15 of the top 25 were stale/dup, ranks 1-9 all the SAME func typeSignature from nine near-identical assembler*.go copies.
- ANALYSER GAP (real, located): analyse.go skipped vendor/testdata/hidden/_test.go but had NO concept of archived copies of the repo's own code. FIXED in contextkit/internal/analysis/analyse.go: new AnalyseWithExclude(root, []string) — caller-supplied substring path excludes (dirs pruned via SkipDir) + UNCONDITIONAL skip of *(N).go via dupSuffixRe. Analyse(root) kept as a no-exclude back-compat wrapper, so the adapter's analysis.Analyse(dir) call is UNTOUCHED and still compiles. analyser CLI gains -exclude (comma-sep, repeatable) with the chassis-specific default set documented in its header. COMPILED + behaviour-tested in-container: (N).go skipped even with no flag; dir copies caught by -exclude; real source survives both.
- RUNBOOK B4a step-0 updated: build the index with -exclude + a one-liner duplicate-survivor check (expect zero). The chassis exclude set used 2026-06-13: go_files_old/,go_files/,docubundle/,thin_slice_run/,scripts/documentation_project/,docs024_key_docs_latest/.
- PRODUCTION IMPLICATION (flagged, not yet acted): the code-indexer agent indexes via the ADAPTER, which calls the no-exclude analysis.Analyse — but it fetches a clean owner/repo tarball from GitHub, NOT the local docs-graveyard tree, so the production index does NOT have this contamination (the archived copies live only in the working checkout under docs/). The exclude matters for the LOCAL B4a harness over the on-disk tree; the deployed path is already clean. IF a repo ever ships archived self-copies in its committed tree, the adapter would need the same exclude — worth a future config knob on the analyse request. Recorded for B4b/later.
- B4a STILL TO RUN PROPERLY: rebuild chassis.json with -exclude, then run BOTH resolve_targets and embed query with a REAL task string (the gamesdesign silent-no-op sentence or a groundtruth task), then eval lex/sem/fused.

## DONE 2026-06-13: dedup tool built + engines-tree proposal
- contextkit/cmd/dedup/main.go — report-only by default; exact (SHA-256) always + optional near-dup (-near, shingled-token Jaccard ≥ -threshold, same-ext); canonical pick (non-archiveish > non-(N) > shallowest > shortest > newest > lex-first); MOVES non-canonical to <root>/_archive/<rel> preserving structure, never deletes; TSV manifest with undo guidance; -ext / -exclude scope guards; skips the archive dir so re-runs are idempotent. COMPILED + behaviour-tested in-container: report, near grouping, -move actually moving both exact+near, structure preserved, manifest correct, idempotent re-run = "no duplicate groups".
- BUG CAUGHT BY TEST (serious, fixed): Go's flag.Parse() STOPS at the first positional, so `dedup <root> -move` silently dropped -move (printed REPORT ONLY, moved nothing — a destructive flag silently inert). Fixed with value-flag-aware manual separation of the single positional <root> from flags, so order doesn't matter and -threshold's value isn't mistaken for root. This footgun is exactly the runbook usage form — worth remembering for the OTHER contextkit CLIs that may have the same pattern (they parse args by hand already, but check).
- contextkit/docs/engines_tree_proposal.md — target tree for the docs019 area: engines/ (one dir per engine: analyser/{analyser,code_symbols,adapter}.md, contextkit-cli/{pipeline,commands,B4a}.md, tool-docs/), runbooks/ (thin_slice, analyser_deploy), go_files/contextkit/ (live module, unchanged), and a SINGLE _archive/ root replacing the scattered go_files_old//docubundle//(N).go graveyard. Key payoff: analyser -exclude _archive/ becomes ONE entry instead of the six-pattern list. Migration order ties to the new tool: dedup report → dedup -move → hand-create engines//runbooks/ + editorial move (human-led, selective-carry not generative merge per the earlier discussion) → re-point links → re-index with -exclude _archive/.
- NOTE the dedup default archive dir (_archive) === the tree proposal's archive root === the analyser exclude target: one consistent name end to end.

## DONE 2026-06-13: docs019 migration STAGING script (not a blind mover)
- The uploaded directory_tree.txt revealed the real state: it IS the docs019 dir itself; 402 top-level files, 203 carrying (N) download-suffixes; families NOTES(53)/PLAN(57)/016(48)/FOCUS(29)/RUNBOOK(18). CRUCIALLY the migration is ALREADY PARTLY DONE — _archive/, engines/{analyser,contextkit-cli,tool-docs}/, go_files/contextkit/cmd/{...,dedup}/ all exist; _archive/ already holds archive_april_26/chassis-drafts/excellent_discussions/old. And engines/tool-docs/ has become a DUMPING GROUND (main.go×8, assembler(2/3/4).go, tarballs, json) — NOT clean engine docs. So a from-scratch mover would corrupt existing work.
- Built stage_docs019_migration.sh (contextkit/cmd/dedup/) — report-only by default, THREE move-classes split by safety: (1) AUTO archive-dir moves (go_files_old/thin_slice_run/working/stray archive_april_26 → _archive/, by name, --apply, collision-guarded); (2) duplicate collapse DELEGATED to the dedup tool (prints the command; optional --dedup runs it report-only); (3) editorial moves NEVER auto-applied — writes PROPOSED_MOVES.tsv (family→engines//runbooks/ heuristic, (N) files flagged "leave for dedup") with an ACTION column the USER edits, plus the awk one-liners to apply move/archive rows by hand. Tested: report, proposal generation, --apply moving ONLY the archive dirs and leaving loose docs untouched. Self-catch: excluded the script itself from its own proposal.
- WHY NOT a full auto-mover (the actual answer to the request): canonicality of 200+ FOCUS/PLAN/NOTES working docs is editorial — a filename can't tell a script which file IS the canonical code_symbols doc or whether a PLAN is current vs superseded. Auto-sorting them would re-create the silent-merge/wrong-canonical risk. Deterministic parts automated; editorial parts staged for human decision — same split as the dedup design and the docs-consolidation discussion.
- ORDER for the user: (a) ./stage_docs019_migration.sh <docs019> (report) → (b) --apply for archive dirs → (c) dedup -ext ... -move for the 200 (N) dupes → (d) edit PROPOSED_MOVES.tsv ACTION column, apply with the one-liners → (e) re-point links, re-index with -exclude _archive/. (a)-(c) reversible/mechanical; (d) human-led.
- engines/tool-docs/ needs hand-untangling (it's module files + docs mixed) — flag, not scriptable.

## DESIGN 2026-06-13: doc-drift claim classifier (contextkit/docs/DESIGN_doc_drift_classifier.md)
- User revisited doc-merging + proposed deeper verification (DB data not just \d; running tasks + logs; "a test suite for the state of play"). Drafted as a DESIGN NOTE (prompt contract is the bit that must be right before code).
- KEY REFRAME: the deep-verification idea is really TWO tools — (a) doc-drift classifier (one-off, "is this claim current", THIS note) and (b) a standing live-system CONFORMANCE suite ("does prod behave as documented", continuous, pages). Same techniques, different products/cadence/cost. Carved (b) OUT explicitly (§6, on the existing DiscoveryCheckContext/CheckResult rails) so the heavyweight always-on thing isn't built under a cleanup banner.
- TIERED evidence (all sources confirmed to exist): T1 static (code_symbols + \d), T2 state (dbcontext -rows, read-only SELECT), T3 behavioural = READ EXISTING LOGS (dbcontext -runtime-site already pulls agent_error_log + site_work_items; + system_events/entity_state_log/orchestration_states). T1 confirms code EXISTS; T3 confirms it RAN — by reading the trail it already left.
- TWO HARD STRUCTURAL RULES (the safety of the whole thing): (1) READ-ONLY — behavioural checks inspect what already happened, NEVER trigger a build/index/spawn/commit to test a sentence (that path spawns agents, hits GitHub, costs money; a doc-checker that mutates state is worse than a stale doc); fenced integration probes belong to (b) not the doc pass. (2) ABSTENTION ASYMMETRY — T1 fails safe (→unverifiable→keep); T3 adds MISATTRIBUTION risk (blame the doc for an unrelated bug/flaky run/stale logs → false "stale" rewrites a CORRECT doc), so behavioural evidence supports `stale` ONLY on DIRECT contradiction; logs merely consistent-with-staleness are `unverifiable`.
- PROMPT CONTRACT (load-bearing): not "judge if current" but "CITE the confirming/contradicting evidence or output UNVERIFIABLE". Verdict without citation = invalid → unverifiable. Model may NOT propose rewritten doc text (classify+cite; human authors). Evidence tagged tier+freshness. "Unsure→unverifiable→keep" is the stated DEFAULT. Per-claim JSON schema given.
- CLASSIFY ≠ MERGE (held firmest): grounding makes CHECKING tractable, NOT generative merging — LLM rewrite drops caveats silently and no code-check catches an OMISSION (code can't know what a human wrote that isn't in code). Output = per-claim annotated map; human consolidates with citations in hand.
- DATE/VERSION = TRIAGE not truth (user's refinement, inverted): old ≠ wrong, old = UNCHECKED; date orders the queue + breaks conflict ties (newer default), NEVER overrides the code check.
- DOGFOODS B4a (§7): each claim's T1 step IS a real retrieval, ground truth = the code, hit/miss = recall on real queries. MUST run over a CLEAN index (post-dedup, -exclude _archive/) or it inherits the duplicate contamination.
- RECOMMENDED build order (§8): ship T1+T2 first (cheap, fully safe), add T3 once trusted; plain report first (no auto update_doc work items yet); claim granularity = per falsifiable sentence with paragraph as context.

## DONE 2026-06-13: version-thinning tool built + tested (contextkit/cmd/thin_versions/)
- ANALYSIS of the real tree (main_docs_directory_tree.txt, 2,729 files / 184 dirs): the user's stem-derivation rules (strip .patch/.orig, ext, (N) bracket, _vX_Y version → subject stem; dir carries classification) VERIFIED to cluster correctly — 2,729 files → 1,917 (dir,stem) subjects. Duplication is CONCENTRATED not spread: 1,734 SINGLETONS (untouchable), and just 18 fat clusters (10+ versions) NOT already archived holding ~489 files, almost all under docs024_key_docs_latest/ (running_notes, traffic_probe_*, finetuning phase5, 016_debugging_guide). So keep-5-EVERYWHERE only archives 14%; the high-value move is thinning the 18 FAT clusters specifically. Already 574 files under _archive//old paths.
- TOOL: thin_versions — groups by (dir, subjectStem), ranks newest-first by (vMajor,vMinor) > (N) bracket > mtime (VERSION BEATS MTIME — tested: a v2_31(1) with an older mtime than v2_30 still ranks above it), keeps newest -keep (default 5), only thins groups >= -min-group (default 10, targets fat clusters), report-only by default, -move archives the tail to _archive/ preserving structure + TSV manifest, SKIPS singletons + already-archiveish paths (_archive//old/_old), -report-renames flags NNN-increment-same-stem pairs (the 004_/005_ mixup case) for review without merging. Value-flag-aware positional parsing so the dedup stop-at-positional footgun is absent (tested both orders).
- DISTINCT from dedup: dedup removes identical COPIES; thin_versions removes older VERSIONS (different content). Run order: dedup first (clear exact/(N) copies), then thin_versions (trim version history).
- TESTED in-container: report (versioned + bracket-only clusters both rank right), singleton/small-group/archived all untouched, -move keeps newest 5 + archives tail with manifest, idempotent re-run, flag-order robust, -report-renames flags the 004/005 pair. gofmt clean, go vet clean.
- USER OBJECTIVE met: reduce each subject to ~5-10 + archive the rest, to de-noise the analyser — but targeted at the 18 fat clusters (safe) rather than a blanket pass. Recommended real-tree run: thin_versions <docs-root> (report) → review → -move; AFTER dedup; then re-index with -exclude _archive/.

═══════════════════════════════════════════════════════════════════════
## PRINCIPLE 2026-06-13: untested code is a liability, surfaced by the dedup -move bug
═══════════════════════════════════════════════════════════════════════
The dedup tool shipped a SILENT destructive-flag bug: Go's flag.Parse() stops at
the first positional arg, so `dedup <root> -move` parsed -move into flag.Args()
and never set it — the tool printed "REPORT ONLY" and moved nothing, while
APPEARING to accept the flag. It was caught ONLY because the in-container
behaviour test asserted on the post-move tree state, not on "it compiled" or "it
printed sensible output".

The durable lessons (apply to every contextkit CLI and every chassis action):
- COMPILE/gofmt/vet prove syntax, NOT behaviour. A tool that builds clean can
  still do the opposite of what its flags say. Behaviour tests that assert on
  the RESULT (files moved? rows changed? tree state?) are the only thing that
  catches this class.
- DESTRUCTIVE operations must be REPORT-ONLY BY DEFAULT (dedup, thin_versions,
  stage_docs019 all are). That design is WHY the bug was benign: the failure
  mode was "did nothing", not "deleted the wrong thing". Report-first is not
  politeness — it is the safety margin for the bug you haven't found yet.
- The Go flag-stops-at-positional footgun specifically: any CLI taking a
  positional + flags after it needs value-flag-aware manual arg separation
  (dedup and thin_versions now both have it). resolve_targets/embed/assembler/
  fuse/eval_targets/dbcontext parse args by hand already — but AUDIT each for the
  same pattern; an untested -flag after a positional may be silently dead.
- "Untested code" in this project means: drafted, gofmt-clean, possibly run once
  on the happy path — but without an assertion on its actual effect. Treat such
  code as UNVERIFIED (item 24 again): the claim "this tool archives the tail" is
  falsifiable and must carry its [checked: behaviour test] evidence before it is
  trusted near real files.

═══════════════════════════════════════════════════════════════════════
## CONSOLIDATION 2026-06-13: the documentation thread, end to end
═══════════════════════════════════════════════════════════════════════
A single arc across several sessions, distinct from the analyser-deploy thread.
Two sub-threads that share one spine (don't restate code; verify claims; keep the
human as author):

### A. Tool-doc system (chassis-drafts/tool-docs/) — APPLY-READY, not yet applied
- The in-code tool-doc header (one sentinel block per tool), strip-at-deploy
  (three call sites), provenance columns, tool_health checks, prompt updates.
- 019/020 CONSOLIDATED wholesale (804 + 473 lines); staging splices retired in.
- Rollout order (load-bearing): provenance migration → prompts SQL → one binary
  release (6 Go files). Detail in entries above (2026-06-11) + the tool-doc
  rollout block in RUNBOOK_thin_slice.md.

### B. Doc-currency + archiving (THIS subproject) — tools built, cleanup not yet run
The chain of reasoning, in order:
1. TRIGGER: 019 carried a stale "isn't built yet" negative claim (the split WAS
   built); nearly caused a reuse-before-recreate violation. Same day, freshly
   written docs went stale within hours. → staleness is a COUPLING property, not
   an age property.
2. DISCIPLINE (016 v2_34 item 24 + 001 pointer): point-of-use verification;
   dated-claim convention [checked YYYY-MM-DD: evidence]; negative claims carry
   their falsifying command; NO whole-doc verified stamps; docs update in the
   same change as the decision. Demonstrated live in 019's tags.
3. MERGE QUESTION (twice): can an LLM merge/dedupe docs? Resolved: CLASSIFY,
   DON'T MERGE — grounding makes CHECKING tractable (the analyser exists now),
   but generative merge fails SILENTLY (drops caveats; code can't catch an
   omission). Date/version = TRIAGE not truth (old = unchecked, not wrong).
4. DESIGN: doc-drift claim classifier (contextkit/docs/DESIGN_doc_drift_
   classifier.md) — tiered evidence T1 static / T2 state / T3 behavioural-but-
   READ-ONLY (read existing logs, never trigger runs); evidence-or-UNVERIFIABLE
   prompt contract; the standing CONFORMANCE suite carved out as a separate
   future product. Scoped to T1+T2 first, enumerated docs first.
5. TOOLS BUILT (contextkit/cmd/, all report-only-by-default, tested):
   - dedup — exact (SHA) + optional near-dup (-near) COPIES → _archive/, manifest.
   - thin_versions — older VERSIONS of a subject (version>bracket>mtime rank),
     keep newest N, target fat clusters (-min-group), -report-renames.
   - stage_docs019_migration.sh — deterministic archive-dir moves + dedup
     delegation + an editorial PROPOSED_MOVES.tsv the human approves.
   - analyser -exclude / *(N).go auto-skip — keep the index clean at the source.
6. TREE TRUTH (main_docs_directory_tree.txt): 2,729 files → 1,917 subjects;
   1,734 singletons; noise concentrated in 18 fat clusters (10+ versions, not yet
   archived, ~489 files), almost all under docs024_key_docs_latest/. So thinning
   targets the 18, not a blanket pass.
- STATE: all tools built + tested; the actual cleanup RUN is still to do (the
  archiving subproject runbook covers it).

## DONE 2026-06-13: archiving-subproject runbook written (RUNBOOK_doc_archiving.md)
- Standalone operational runbook for the cleanup RUN (separate from RUNBOOK_thin_slice.md, which is the contextkit pipeline + analyser deploy). Sequence: pre-flight (build + tar snapshot) → Step 1 dedup (copies) → Step 2 thin_versions (versions) → Step 3 stage_docs019 + editorial PROPOSED_MOVES (human-led) → Step 4 re-point + re-index with -exclude _archive/ + zero-stale-paths verify. Report-first throughout; verification-gates table; reversibility section (manifests + _archive/ + tarball + git); explicit "what this does NOT do" (no merge, no content-currency judgement, no singletons). Grounded in the real numbers (2,729→1,917; 1,734 singletons; 18 fat clusters). Carries the read-the-report-first caution as the mitigation for the recency-heuristic risk and the untested-code lesson.

## 2026-06-13: gamesdesign silent-no-op-rebuild — MID-DIAGNOSIS + analyser-root trap
### Analyser-root trap (fixed in runbook known-limits)
- User's analyser run returned ENGINE files (docs024…/idea.uk/golang_files/) not FRAMEWORK files. Cause: $DOCS=~/projects/agentchassis/docs, so the analyser only saw the docs tree — the framework (platform/orchestration/actions/save_page_sections_action.go etc.) is NOT under docs/, so it could not appear (absent, not low-ranked). FIX: analyse the REPO ROOT (~/projects/agentchassis) with -exclude docs/ for framework tasks (inverse of the doc-cleanup run). SECOND trap: -exclude matches a SUBSTRING of the path RELATIVE to root; the user's absolute -exclude ~/projects/agentchassis/test matched nothing — use relative substrings (test/, docs/). Repeated -exclude DOES accumulate. Both recorded in RUNBOOK_thin_slice known-limits + a post-analysis presence check one-liner.
### The bug, grounded in /mnt/project code (mid-diagnosis, NOT yet confirmed which path fires)
- SYMPTOM (user-confirmed): gamesdesign.co.uk root index rebuild reports success, live page stays stale.
- save_page_sections_action.go: SavePageSectionsAction has TWO len(sections)==0 early returns — L188 (no HTML + no metadata: success/skipped:true/sections_saved:0) and L215 (no sections after enrichment: success/sections_saved:0). BOTH return "success": true persisting NOTHING — the exact "presents as success, changes nothing" signature in the task sentence.
- rerender_single_page_action.go L242-243 (comment): empty return → skipped=true → workflow check_skipped → complete_skipped. So the SKIP IS DESIGNED; the bug is UPSTREAM — why no sections for an already-deployed page. Reframes the scope from "why save skips" to "what generates sections and why is that output empty/missing in collected_data for this page".
- LEADING HYPOTHESIS (unconfirmed): workflow FIELD-NAME mismatch — save reads `sections` from a metadata field or `assembled_page.html` (L161); if the generator/plan step wrote sections to a differently-named collected_data field, save sees zero. This is the variable-names-in-workflow class on the standing watch-list.
- CONTENT-REGRESSION GUARD (L227): refuses to overwrite content-rich deployed pages with empty shells. IMPLICATION: the stale page is being PROTECTED, not clobbered — which is why it presents as success-with-no-change rather than success-then-blank.
- NEXT (user to run): (1) the cheap confirm SQL (page_components build_status/count/html_len for gamesdesign index) — content present + last rebuild sections_saved:0 ⇒ confirmed; (2) dbcontext -runtime-site gamesdesign.co.uk -runtime-page index for which empty-path fired; (3) assemble the debug bundle (command in RUNBOOK worked-example). Paste runtime+SQL to read the actual failing path together — do NOT conclude which return fires from static code alone.
### Assembler scope-of-responsibility (design question raised)
- User asked: shouldn't the assembler assemble the SQL calls + investigations? It DOES paste SQL OUTPUT (-schema/-runtime files) but does NOT run dbcontext or execute SQL — by design (pure composer, read-only-by-construction, same rule as the doc-drift classifier). THREE ways to close the gap: (a) naive = assembler grows -psql and runs queries — REJECTED (breaks read-only boundary; couples composition to the cluster); (b) orchestration wrapper (cmd/bundle/ or script) runs the standard dbcontext queries THEN calls assembler with the outputs — RECOMMENDED (composer stays pure, SQL stays in the bounded read-only dbcontext); (c) automatic table-SELECTION (infer tables from task) — the hard later step, propose-then-confirm not silent. Wrapper NOT yet built — flagged for user decision. Recorded as a runbook design note next to the assembler flags.

## DONE 2026-06-13: bundle orchestration wrapper built (contextkit/cmd/bundle/)
- The "assemble it all including the SQL" wrapper, built the SAFE way: it RUNS dbcontext (read-only) for schema/capabilities/runtime, writes each to a temp file, THEN invokes the assembler with those files wired in — composer stays pure, SQL stays in the bounded read-only dbcontext, nothing triggered. Chosen over putting -psql in the assembler (which would break read-only + couple composition to the cluster).
- WIRING (confirmed against the real flag definitions): dbcontext -schema <tables> → assembler -schema; dbcontext -capabilities [-df-filter] → assembler -dbfacts; dbcontext -runtime-site [-runtime-page] → assembler -doc (the assembler has NO -runtime flag — runtime evidence rides in as a -doc; the debug step otherwise prints only a placeholder. NOTE: the earlier runbook worked-example wrote `-runtime /tmp/runtime.md` which is WRONG — should be `-doc`; fix when next editing). -include forwarded as -scope (assembler force-includes files via -scope). Passes through -scope/-doc/-task/-step/-neighbour/-max-neighbour.
- SAFETY/ergonomics: -dry-run prints the dbcontext + assembler commands and runs nothing; without -psql the DB gather is skipped (plain assembler front-end); evidence temp files kept for inspection; hand-rolled arg parse so repeated -scope/-doc/-include accumulate. exec.Command passes []string directly (no shell word-split), so the kubectl-exec -psql string stays ONE arg and dbcontext splits it itself — verified.
- TESTED: go vet clean; -dry-run sequences 3 dbcontext calls then assembler with all files wired; real run with no -psql produces a valid bundle. Live DB calls need the cluster (not testable here).

## CORRECTED DIAGNOSIS 2026-06-14: gamesdesign silent-no-op is a CONTENT-REGRESSION + status-rollup bug, NOT "sections never reach save"
- The runtime evidence + SQL FALSIFIED the leading hypothesis. The earlier "freshly generated sections never reach save_page_sections" framing is WRONG.
- agent_error_log (15 rows): the rebuild IS reaching save_page_sections every time; each FAILS with "content regression blocked: new content has ~3000 chars vs ~13000-20000 existing" at step save_sections (agent page-build-handler). So sections DO arrive; the L227 content-regression guard BLOCKS the overwrite because every regenerated version is ~3k vs ~13-20k deployed.
- SQL confirms the page is content-rich + intact: deployed index = 5 page_components, 34,375 chars rendered_html. The guard is doing its job — protecting a 34k page from a 3k shell.
- TWO ACTUAL FAULTS: (1) THE REAL BUG = the page-content-writer regenerates ~3k chars when the page has ~13-20k — drastically short. Cause is UPSTREAM in section generation, not in save. The guard is a symptom-catcher. (2) VISIBILITY BUG = save_sections is logged as "step save_sections failed" (content regression), YET the site_work_items page_rerender rows show status=complete. A blocked save is rolled up as a completed rebuild → "presents as success". The disconnect is step-failure vs work-item-status, not total silence (the errors ARE logged).
- METHOD WIN: this is exactly why we don't conclude the failing path from static code — the code showed TWO success-with-zero early returns (L188/L215) as the suspected silent path; the logs showed the REAL path is a third thing (the regression guard erroring, caught + logged, but the work item completing anyway).
- NEXT: (1) why does regeneration produce ~3k vs ~13-20k — inspect the page-content-writer / plan_sections output for the index (the generation is the bug); (2) why does a content-regression step-failure roll up to work-item complete (the status-propagation path: page-build-handler workflow completion vs step error). Bundle for fault (1) with -scope on the generation side; the regression guard + save are now understood, not suspects.

## 2026-06-14: bundle wrapper — first LIVE-DB run (works) + two path snags
- cmd/bundle proven against the real cluster: all three dbcontext gathers succeeded (schema, capabilities/dbfacts, runtime all written to the temp dir). The wrapper's gather→assemble sequencing works end to end with a live -psql.
- SNAG 1 (path doubling, prior attempt): ran `./$CK/cmd/bundle` from INSIDE …/go_files/contextkit, but $CK holds the full repo-root-relative path, so it resolved to cwd + full path twice ("directory not found", contextkit prefix appearing twice). FIX: from inside contextkit use bare `./cmd/bundle` (and bare `thin_slice_constitution.md` etc.); the $CK-prefixed runbook form assumes you run from the REPO ROOT. Both valid, can't be mixed.
- SNAG 2 (this run): assembler failed with "read constitution: open thin_slice_constitution.md: no such file or directory" — the file isn't at the contextkit module root where the user is standing. The wrapper gathered all DB evidence FIRST then failed cleanly at the assemble step, temp evidence files intact (re-run repeats only the cheap gather). FIX pending: `find ~/projects/agentchassis -name thin_slice_constitution.md` then pass that path; if find returns nothing, the constitution is an uncommitted harness artefact to recreate/repoint (it's a REQUIRED assembler input).
- analysis6.json confirmed = the correct repo-root analysis (presence check: save_page_sections present = True). So once the constitution path is fixed the gamesdesign bundle will assemble over the right framework files.
- DIAGNOSIS UNCHANGED by this run (re-confirmed by the same runtime.md): 15× content-regression-block at save_sections (~3k vs ~13-20k), page_rerender work items still complete. Real bug = generation producing ~3k; visibility bug = step-failure rolling up to work-item complete. Next bundle scope should move to the generation side once it assembles.

## DONE 2026-06-14: gamesdesign bundle ASSEMBLED (cmd/bundle fully proven) + bug runbook written
- cmd/bundle ran end-to-end against the live cluster: all three gathers + assemble succeeded, wrote /tmp/bundle_gamesdesign.md (3921 lines). The wrapper is now proven complete (gather→assemble, live -psql). Constitution path was the only prior blocker; resolved.
- Bundle inspected + diagnosis SHARPENED from the actual code (save_page_sections_action.go in the bundle): the content-regression guard (L341-361) returns a HARD ERROR (return nil, fmt.Errorf) when existingTextLen>200 AND newTextLen < existingTextLen/4. ~3k < ~13k/4 trips it every rebuild. The guard returns BEFORE the history-snapshot (L372) and the DELETE (L399), so the deployed page is UNTOUCHED — protected, not clobbered. The guard's own comment (L328-330) names the cause: "LLM failures (credit exhaustion, timeouts, empty responses)".
- BUG RUNBOOK written: RUNBOOK_gamesdesign_silent_norebuild_bug.md. Two faults: (F1 REAL) page-content-writer generates ~3k for a ~13-20k page — upstream generation defect, guard is a correct symptom-catcher; fix surface = the content-writer step (NOT in this bundle's scope — next bundle scopes the generation side; plan_sections is the bridge). Lines of enquiry: truncated/empty LLM output vs subset-of-sections vs stale brief on recreate — each falsifiable, don't guess. (F2 VISIBILITY) save returns a hard error logged as step save_sections failed, yet page_rerender work item = complete — status rollup swallows the step error; fix = page-build-handler workflow step-error→work-item-status mapping; worth fixing regardless of F1. Runbook includes evidence, reproduce/inspect commands, read-only confirmations, verification-when-fixed, and an explicit "do NOT loosen the guard / do NOT guess the generation failure mode".
- NEXT: assemble the F1 bundle scoped on the content-writer generation side (find the step via the workflow/registry; add its agent_definitions workflow-prompt via dbcontext -rows). cmd/bundle makes this a one-command, different-scope re-run.
