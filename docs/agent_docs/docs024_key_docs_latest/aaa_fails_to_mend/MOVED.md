# MOVED — 2026-07-17 (owner decision)

This directory is now **`/bugs_open/`** (repo root, beside `docs/`).

It is the real-case queue of diagnosed-or-filed bugs awaiting a fixing thread.
23 documents across the repo still reference the old path — follow them here.
Numbering (001–009) is unchanged.
