# HANDOFF — travelling docs: Phase A proven · Stage 5 LIVE · next fronts
**Written:** 2026-07-10 · **updated every turn** (turn-stamp at the bottom) · supersedes `HANDOFF_2026-07-09_recreation_and_chassis.md`.
**Companions (current revs):** `RUNBOOK_travelling_docs(39).md` (rev 48, position tracker — read §0 first), `RUNNING_NOTES_travelling_docs(40).md` (chronological log), `PLAN_travelling_docs(7).md` (spec, rev 8), **`SUMMARY_2026-07-19_evening_the_delivery_gap.md` (plain-language read-out — START HERE for the story; supersedes `SUMMARY_readout_2026-07-19.md`, which is kept, corrected in place, because what it got wrong is the useful part)**, `STATUS_2026-07-16_where_we_are.md` (older snapshot), `PLAN_tool_acceptance_runner.md` (Tier-4 runner plan).

---

## 0. First actions in a new chat *(refreshed T30, 2026-07-19 — trust this block)*
1. **Read `/CLAUDE.md` first** (repo root, owner-maintained, changes often): many sessions share this tree/branch/cluster/tag-sequence. Commit per task with an explicit pathspec, forward-only, never `git add -A`. Then read **`SUMMARY_2026-07-19_evening_the_delivery_gap.md`**, **§7 (RESUME HERE)** below, this doc's turn log (newest first), and the RUNBOOK §0 position line.
2. Environment: repo `~/projects/agentchassis`, branch **`085_debug_and_feature_loops`**; DB via `kubectl exec -n ai-persona-system postgres-clients-0 -- psql -U clients_user -d clients_db`. Deploys are **USER-driven**. ⚠️ **The build rule INVERTED on 2026-07-17**: `make build-<service>` now builds from **committed HEAD** (git-archive; cannot bundle WIP) — *commit your task, then build*; `-tree` is the deliberate WIP escape hatch. Chassis + adapter are **SEPARATE images**; currently **v1.0.1139** (both, verified against the pods 2026-07-19 — it moved 1137→1138→1139 during one session, so re-check rather than trusting this line). Never infer a deployed binary's contents from git — grep the running pod (`kubectl exec … -- grep -ac "<a string from your change>" /app/agent-chassis`). Short (≤16B) Go literals compile to immediates and won't grep — pick a long, distinctive string (T14).
3. Migrations: home `docs/agent_docs/sql_for_agents/`, ledger `schema_migrations` (keyed on `filename`), runner `./scripts/migration/run-migrations.sh` (dry-run default). The old gripper-151 blockage is **RESOLVED** (ledger backfilled, T23). This workstream applied **157, 158, 159, 160, 161, 162, 168, 169**; **next free number is 178** (ledger max 177; other threads hold 163–177 and there are DUPLICATE 175s and 176s — collisions are routine, so always take `max+1` from the live ledger AND the directory, never from this line). Out-of-band `psql -f` is fine but **you must insert the ledger row in the same sitting** (`bugs_open/007`).
4. Sites: trial = gamesdesign.co.uk `e33263f4-74f8-494f-b191-546845dbbddf`; real-bug/Tier-4 = vonc.com `9ec3b9ee-5b08-461b-b4f8-9e1e03579c74`. **Benchmark tool** = `tool-loot-table-balancer` (gamesdesign), component `3862f72f-8a67-4dda-b0ef-6be83bc22fe6`, page `f25dd4d8-6e25-44eb-a021-689d3057d7a3`, page_component `45229c85-5600-4e8c-b4ae-e8058f74b185` — still RED on `mobile-fit@mobile`; **do not hand-fix it.** ⚠️ **T28 reframed WHY it is red:** not a fixer that cannot aim (`bugs_open/010`'s reading) but **`bugs_open/024` — no tool fix has ever been RENDERED to the page.** The component template carries a correct fix (`minmax(0, 2fr) …`, 10,626 chars); `page_components.rendered_html` is still **9,901 chars = the v1 born length**. Re-verification was RED every cycle because the page under test never changed.
5. Triggers live in the scratchpad, not the repo (the old `drafts/` workspace is gone): an 087-style acceptance trigger and an 085 toolgen trigger are reconstructed each session — mirror `scripts/initial_messages/320_tool_notes_generation_and_fixes/085_TRIGGER_toolgen_gamesdesign_v1.sh` (kcat → `system.agent.generic.requests`, `action=orchestrate`, `config.agent_type=<agent>`, **body compacted to ONE line** — kcat -P is line-delimited).
6. Our open bugs: **`bugs_open/024`** (**the live one** — tool fixes never render to the page; three defects in series; diagnosed with evidence, fix part-built, see §7), **`bugs_open/010`** (convergence guard, candidate b — open; **re-read it alongside 024**, its non-convergence evidence is explained by an unchanged page), **`bugs_open/012`** (truncation — guard + `stop_reason` + 168 + 169 all LIVE; **168 is now PROVEN** by a clean 10,626-char improver run), **`bugs_open/021`** (widen the write guard to `page_components.rendered_html` / `pages.rendered_*` — open), **`bugs_open/031`** (a wrong concept-register entry became a HIGH-severity council blocker — **filed by us, OWNED BY ANOTHER THREAD, do not work it**). `016b_debugging_guide_8_consolidated.md` §9 holds the patterns, §10 indexes every open bug.

---

## 1. Where the project stands (all proven in production)
> **CORRECTED 2026-07-19 (T28):** read this section with `bugs_open/024` in hand. Everything below is true of the mechanism, and the loop's green proof (the vonc footer arc) stands — but that proof ran through **site-chrome**, a different delivery path. For **tool** components the last mile is broken: a fix reaches the durable template and is never rendered onto the page. "Proven in production" does not currently extend to tool fixes reaching users.
- **Task 3 — PLANs at birth: PROVEN.** tool-generator runs `save_tool → compose_plan → write_plan → index_plan → complete`.
- **Task 4 — NOTES at every fix: PROVEN.** Two machine `fix` notes `('pipeline','build')` from tool-recreation-handler (economy-simulator recreation, 2026-07-09).
- **Task 5 — economy simulator recreated with both bugs FIXED**, live on gamesdesign.co.uk (influx rate map `[0,1,5,15,40,100]`; Players on its own `yPlayers` axis). Took 3 runs; lessons in §4.
- **Task 6 — OOM solved + index_plan proven.** Root cause was `chunkContent()` looping forever on content > chunk_size (NOT a leak, NOT a missing deadline). Fixed + regression-tested; proof run wrote the **first `knowledge_base` `tool_docs` rows** (4 chunks, 4 embeddings, ~5.5s).
- **Stage 5 — Tier-2 static acceptance checker: LIVE** (v1.0.1107, sweep `cd0d9731`). Discovery check `tool_acceptance` (`discovery_checks/check_tool_acceptance.go`); first sweep produced exactly the pre-verified findings: 2 improve_tool items + 2 acceptance-fail notes (see §2).
- **Migrations system live:** `schema_migrations` ledger + runner + README; arc renumbered 125–142 (128 is a reconstruction stub — original lost with an old workspace). Every applied file exists in git as of commit `ad39ec6e`.
- Both generator-created tool pages are **deployed and live**: `/tools/tool-xp-curve-designer.html`, `/tools/tool-drop-rate-tuner.html`.

## 2. RESOLVED — Tier-2's first findings + the delivery decision (Option B, 2026-07-10)
Tier-2's first sweep failed both tools: drop-rate-tuner on `["asset","slider-updates-stat"]` (composer-invented kebab-case anchors `#drop-chance`/`#stat-median` vs the real camelCase `#dropChance`/`#statMedian`) and both on `asset` (`asset_loads /tools/assets/<fn>.js` — the render path ships JS INLINE; the "Path 1 extraction" every PLAN declared was designed but never built).
**User decision: Option B — "surrender" to delivered reality.** Principle on record: *criteria describe what the system DELIVERS; aspirations live in roadmaps.* Executed:
- **143** superseded both PLANs (asset check removed; Delivery mechanism = inline + rationale; drop-rate's selectors corrected to the verified real ids) with correction notes in each tool's stream, and **cancelled the two work items** (PLAN-side resolution — tool-improver must not chase a stale contract).
- **144** fixed the composer: `compose_plan`'s standard checks five → four (asset_loads dropped), delivery instruction now inline (snapshot `1bca62f6`).
- Verified: `fence_has_asset_check=f` on both current PLANs; every remaining anchor probed present on the live pages; chains v1→v2→v3 with history intact.
- Residue: (i) the 7-day improve_tool cooldown counts CANCELLED items, so sweeps skip these two tools until ~2026-07-17 — future tweak: exclude cancelled in `check_tool_acceptance.go`'s recentItems query (and arguably tool_health's); (ii) if extraction ever ships (Option A benefits: cacheable JS, lighter pages), PLANs supersede forward again and the composer regains the check.
- Still unobserved: a tool-improver run driven by a Tier-2 `acceptance_test` item — the first genuine one will be the full criteria→check→item→fix→note loop. Next candidates come from future sweeps on other sites/tools.

## 4. Durable rules banked this arc (beyond 016b v8)
- **kcat -P is line-delimited**: multi-line JSON body → one message per line; the chassis can marry your headers to a NEIGHBOURING message's body (`input_data: null` + someone else's workflow). Compact to single line; 086 script enforces.
- **Prompt seams lose intent**: a spec field can survive analysis and be ignored at generation if the generation prompt never renders it (analyze_tool rendered `spec.interactive_features`; recreate_tool didn't → Opus recreated the bugs). Fixed by 138's "Mandatory Behaviour Requirements" section. When adding a spec field, grep EVERY prompt_template that should render it.
- **Verify deploys by ancestry** (`git merge-base --is-ancestor <commit> <release>`): untracked files survive any number of release commits (the checker missed TWO releases before ad39ec6e).
- **`chunkContent`-class bugs**: content ≤ threshold returning early can hide a non-terminating path for weeks. Regression tests with timeouts catch loop regressions.
- **The anchor rule (Stage 5, settled)**: validate a criteria selector's leftmost id/class token only; confirm, never refute; `-EDIT` ids skipped; class tokens are whitespace-delimited (Go `\b` wrongly splits on hyphens).
- **"Completeness + validation passed" ≠ working** — twice demonstrated (June recreation introduced the bugs; run 2 recreated them). Tier 4 is the missing behavioural layer.
- **Snapshot before every agent_definitions change** (`snapshot_agent(type, reason)`); migrations via the runner; every migration carries its own guard DO block; workflow-altering migrations leave a `('pipeline','build')` note.
- Older essentials: env vars same-line prefix or exported; `agent_error_log` first read (orchestration_id is TEXT); `EXECUTING_STEP` forever = worker died (check pod restarts + Last State, capture crash logs IMMEDIATELY); prompt templates text→bare string / json→map with `.result`; aborted psql needs `ROLLBACK;`.

## 5. Next fronts, in order
1. ~~The two improve_tool items + delivery decision~~ RESOLVED (§2, Option B). Small follow-up: exclude `cancelled` items from the tool_acceptance/tool_health cooldown queries (next chassis change).
2. ~~**Stage 6 — Tier-4 headless runner P0/P1/P2**~~ **DONE — P0 live (T6/T8), continuous (T12), P1 mobile + P2 interactions PROVEN LIVE on v1.0.1114 (T14: 9 passed / 0 failed / 1 correctly-skipped).** Remaining in this strand: the note-composer profile-labelling fix (T14), then P3 screenshots.
   <details><summary>original P0 deploy instructions (historical)</summary>
   Code/tests/packaging/makefile all done (T3). **User deploy sequence:**
   (a) `kubectl apply -f deployments/kustomize/services/browser-runner-adapter/overlays/production/uk_001/browser-runner-requests-topic.yaml` (ONCE — kafka ns);
   (b) `make build-browser-runner-adapter push-backend deploy-agents IMAGE_TAG=vX` (Chromium image ~1.2GB, slow);
   (c) §2.15 smoke: hand-produce a `run_checks` request against one live tool page (e.g. gamesdesign.co.uk/tools/tool-xp-curve-designer.html) with its PLAN's criteria_json, expect a response with `in_response_to_request_id=$REQUEST_ID`, `status=complete`, results matching manual inspection.
   Then build the **`tool-acceptance-agent`** orchestrator (load_doc_context criteria → resolve URLs → call adapter → judge → acceptance-run/fail notes + improve_tool items). P1 mobile+interpreter, P2 interactions, P3 screenshots.
   Full P0 contract: `PLAN_tool_acceptance_runner.md` rev 2. This is the only tier that catches economy-simulator-class behavioural bugs, and the only one that can see the adopted page-section tools Tier 2 structurally can't.
   </details>
3. ~~**Gap to close sometime**: tool creation never enqueues a `page_rerender` item~~ **CLOSED T24 (2026-07-17)** — `create_rerender_items` gained single-page mode + migration 162 wired the `enqueue_rerender` tail on tool-generator (`index_plan → enqueue_rerender → complete`). 162 is applied and inert; the Go single-page code activates it on the next chassis image.
4. **Watch items**: chassis memory slope (fresh-pod baseline ~25–29Mi; the loop explained the crashes, leak neither proven nor needed); `github-actions-runner-…-lhg9l` CrashLoopBackOff (node cgroup-driver mismatch, infra not app; healthy replica serves).
5. Standing opens (unchanged): `deploy_tool_to_site` `source_*` stamp; `rag_index` `source_type='scrape'` parameterisation; Tier-4 vocabulary needs the `select` verb defined; recreated/adopted tools are invisible to Tier 2 (no content_components rows) — Tier 4 reaches them via pages.

## 6. Key identifiers
site `e33263f4-74f8-494f-b191-546845dbbddf` (gamesdesign.co.uk) · tools `tool-xp-curve-designer`, `tool-drop-rate-tuner` (components exist); `game-economy-simulator` (page-sections only) · snapshots: tool-generator `1bca62f6`, tool-recreation-handler `8701375f`, design-discovery-agent `b294bf7d` · proof runs: Task-3 `1923badd`, Task-4 `e1018366`/`ca00a1dd`, index_plan `05d1fc97`, Stage-5 sweep `cd0d9731`, **Tier-4 P1/P2 run `af5a4ac5`** (9/0/1, 2026-07-14) · checker commit `ad39ec6e` · adapter P1/P2 commit `53a5b518` (live in v1.0.1114) · migrations applied through `147`.

---

## 7. RESUME HERE — `bugs_open/024`: request PROVEN correct, delivery blocked by a SIXTH defect *(rewritten T32, 2026-07-21)*

> **T32 SUPERSEDES T31.** Migration 180 is **APPLIED** (2026-07-20 19:13:22Z,
> ledger row written). A real proof run then drove the whole chain and proved the
> re-render request is now **correctly formed for the first time ever** — all four
> of 180's changes visible in one live row. **But the page still did not render**,
> because the proof exposed a **sixth defect** one layer below the fix: the
> per-page `page_rerender` item_key is not reason-scoped, so a stale reason-less
> request suppressed the correct one and then re-deployed stale HTML. Full
> evidence + fix candidates are in **`bugs_open/024`** under "UPDATE 2026-07-21".
> **This is where a new thread picks up.**

**The single sentence:** a tool-improver fix is written correctly to
`content_components.html_template` and **never rendered onto the page**. **Six**
defects in series; 5 fixed, the 6th open and blocking.

| # | defect | status |
|---|---|---|
| 1 | `update_component_html` only sets `page_components.build_status='pending'` — nothing scans it | **not a blocker** — the request goes via `create_work_item`→`insertWorkItem` (the normal path). Dead flag stays dead; 024 candidate 3 |
| 2 | rerender request carries no `spec.reason`/`component_id` → `scoped=false` → assemble-only re-ship of stale HTML | **FIXED & LIVE** — migration 180 (`spec_literal`+`spec_paths`) |
| 3 | content pre-check escalates any empty-`content_data` section; a tool has `content_data={}` by design → bypasses `save_sections` | **FIXED & LIVE** — `isSelfContainedSection` (`component_level='tool'`+empty `input_schema`) |
| 4 | `needs_rerender` `item_key` is site-wide → anti-churn counts SUCCESSFUL predecessors as strikes → born `unresolved` | **FIXED & LIVE** — `item_key_suffix_field`+`recurrence_expected`, activated by 180 |
| 5 | `loadComponentSchemas` DROPS tool components: `sectionTemplateValid` keys on `</section>`, a tool ends `</script>`, so the benchmark never entered the schemas map and defect 3's exemption never fired. **Two call sites** (`loadComponentSchemas` + `loadSingleComponentSchema`) — first fix covered ONE, `bug_historian` predicted the second | **FIXED**: `toolTemplateValid`+shared `componentTemplateValid`. Call site 1 LIVE in v1.0.1140; **call site 2 committed `3cb92dae4`, INERT until next image** |
| **6** | **per-page `page_rerender` item_key `page_rerender_<page>_<site>` is NOT reason-scoped** (`create_rerender_items_action.go:248`), so a stale reason-less `page_rerender` in the backlog suppressed the correct reason-bearing one via `ON CONFLICT DO NOTHING` (0 items created), then itself ran assemble-only and re-deployed stale HTML | **OPEN — this is the current blocker.** Full timeline + 3 fix candidates in `bugs_open/024` UPDATE 2026-07-21 |

**What was PROVEN this session (T32):** proof-run `improve_tool` `216ea5fe` →
`tool-improver` emitted `needs_rerender` `666619d1` with `item_key
=rerender_tool_fix_gamesdesign.co.uk_3862f72f-…`, `spec.reason=section_data_resolved`,
`spec.component_id=3862f72f-…`, `status=triaged`. **All four of migration 180's
changes, live, in one row.** The request half of the bug is fixed and evidenced.

**Do this next, in order:**
1. **Fix defect 6** — the delivery blocker. It is a Go change
   (`create_rerender_items_action.go:248`), so image-gated, and it is subtle:
   scoping the key alone lets both a section-render and an assemble-only run, and
   if assemble-only runs LAST it re-deploys stale (024 candidate 4 is the
   companion rule). **Recommended: diagnosis/council loop, not a rushed patch.**
   This is the same collision class as defect 4, one layer down.
2. Then re-run improve→rerender→acceptance and watch `tool-loot-table-balancer`
   go GREEN on `mobile-fit@mobile`. **Do not hand-fix the benchmark.** ⚠️ **The
   `bugs_open/024` verify query is STALE** — the improver now writes
   `display:flex; flex-wrap:wrap; min-width:0`, not `minmax(0, 2fr)`. Prove
   delivery by the rendered `.ltb-row-grid` rule leaving `display:grid;
   grid-template-columns:2fr 1fr 1fr auto` and `rendered_html` leaving 9,901
   chars. **`flex-wrap:wrap` is a FALSE marker** (present elsewhere in the v1
   render). Match the component's OWN rule.
3. **Ship call site 2 of defect 5** on the next chassis image (committed
   `3cb92dae4`, currently inert): `loadSingleComponentSchema` still rejects tool
   templates on `</section>` in the live binary.
4. **Watch for up to 8 new `needs_new_component` items** fleet-wide once an image
   ships call site 2: `toolTemplateValid` rejects 8 genuinely truncated tool
   templates the old guard admitted (`bugs_open/046`).

**Council round 6 = REVISE** (10 approve / 4 object / 2 abstained). Both
code-earning objections are already fixed in `3cb92dae4` (the second call site;
`item_key_suffix_field` now hard-fails like `spec_paths`). Remaining objections
answered by query in that commit message. **No `Council-Reviewed:` trailer** is
on any of this — the Go shipped on a REVISE (swept into `bca5d8255`), and applied
migration 180 while round 6 was still queued.

**Related bugs filed this arc:** `bugs_open/044` (sibling name-heuristic, latent),
`bugs_open/046` (8 tools serve unterminated JS on 6 live sites — truncation
casualties `bugs_closed/012` never swept). Both grep BOTH bug dirs; `044` and
`045` each collided with a concurrent thread's filing (see `bugs_closed/README`).

**Corrected figures** (both of mine were wrong, see `WRONG_CALLS.md`): the
exemption matches **13 of 123** active components, not 12 of 122; and **14 of 27**
active tool components **do** declare an `input_schema`, so the empty-schema half
of the predicate is load-bearing, not a formality.

**Sibling bug filed:** `bugs_open/044` — a second call site makes the same
"is this emptiness legitimate?" judgement by matching the function name against
`article/content/body/text/blog` (`plan_sections_action.go:1141-1160`). Latent:
0 of 27 tools trip it today.

Plus the enabler found on the way: `insertWorkItem`'s two-strike rule counted a
**successful** `complete` as a failed attempt, so two successful rerenders
poisoned a site-wide key and later rerenders were born `unresolved`.

### What is already done
- **Two-strike rule FIXED and committed** — `f6e3f3166`, `workItem.recurrenceExpected`
  skips both the two-strike label and the within-cycle suppression (the latter
  drops the item silently, which is worse). **Opt-in, default false, so it is
  INERT until a caller sets it.** 6 tests, none existed before.
- **Council gate: 4 rounds**, trail `7ef4de4e-3930-47fe-8ca6-ba40a2d440cc`,
  ended **8 approve / 2 object** (advisory REVISE — it cannot block). Submissions
  saved as `travelling_docs/submission_024_tool_render_path{,_r2,_r3,_r4}.json`.
  Round 4 is the good one; **reuse its `grounded_in` block**, it holds every
  quote and live figure you need.

### What is left (owner ruled: **fix the rule, do NOT lean on the bypass**)
1. Set `recurrenceExpected` at the rerender call site and route tool-improver's
   request through the **normal** `insertWorkItem` path. *(The council-reviewed
   plan had `update_component_html` call `createRerenderWorkItem`, whose raw SQL
   bypasses that machinery; `improvement_guardian` objected that reusing a
   contract-violating shape entrenches it, and the owner agreed. So edits 1 and 3
   of the reviewed plan are now materially different — **if you want the
   `Council-Reviewed:` trailer to be honest, put the reshaped plan through one
   more round before the Go lands.**)*
2. **Scope the `item_key`.** `create_work_item_action.go:128` builds
   `<prefix>_<domain>` — site-wide. Even with the rule fixed, two tools fixed
   close together collide on `idx_swi_dedup` and one request is lost.
3. **Stamp `spec.reason`** so the rerender re-renders instead of assembling stale
   HTML. Note `spec_data` is a *path* into collected data, not a literal, so this
   needs either a literal-spec affordance or a different carrier.
4. **The escalation-guard fix** — key on the explicit `component_level='tool'`
   marker + empty `input_schema` (12 of 122 active components; already SELECTed by
   `loadSectionComponents`, carried on `componentInfo.Raw`, so no new query).
   This one is unchanged from the reviewed plan and had 8 approvals.
5. Then: **image → verify against the pod → apply the migration → re-run
   improve→rerender→acceptance** and finally watch the benchmark go green.

### Traps that cost time here — do not re-pay them
- **Verify a fix by matching its SPECIFIC rule, never a generic CSS property.**
  `max-width`/`min-width`/`minmax(` all appear many times in unrelated site
  chrome, so the check finds a hit and can never fail. This produced three false
  "the fix is live" claims, including one in T24 and two of mine.
- **A council submission within ~300s of a chassis (re)start is SILENTLY
  DROPPED** — no artifact, no error, no log line. Lost twice around the v1.0.1139
  deploys. Confirm a `fix_plan` artifact appears within minutes or assume it died.
- **Council runs are stateless** — no `orchestration_states` row, so the 097
  banner's watch query returns nothing. Poll `diagnosis_artifacts` **filtered by
  `orchestration_id`**, never by correlation alone, or you read the PREVIOUS
  round's verdict as success. Queueing on the shared topic ran 6–27 min.
- **Verify a cited "contract" against code before revising your plan around it**
  (`bugs_open/031`): a reviewer quoted our own concept register as the pipeline's
  contract and blocked a correct plan at HIGH severity. `git log -S` proved the
  claim had never been true.

---

## Turn log (newest first — update EVERY turn)
- **T32 (2026-07-21):** **Migration 180 APPLIED; the request is PROVEN correct end-to-end; a SIXTH defect found by the proof run still blocks delivery. Round 6 = REVISE, both code objections fixed.**
  - **180 applied** 2026-07-20 19:13:22Z (pre-flight OK, post-condition OK, snapshot `1f3ebb4a`, ledger row same sitting, `('pipeline','build')` note written). Applied via `psql -f`, not the runner — `177`/`178` are unrecorded so the runner would try to re-apply another thread's migrations.
  - **PROOF RUN.** Cloned the real acceptance-driven `improve_tool` spec into item `216ea5fe` (do NOT hand-fix the benchmark; drive the loop). `tool-improver` ran and emitted `needs_rerender` `666619d1` carrying — for the first time in this bug's history — `item_key=rerender_tool_fix_gamesdesign.co.uk_3862f72f-…`, `spec.reason=section_data_resolved`, `spec.component_id=3862f72f-…`, `status=triaged`. **All four of 180's changes proven in one live row.** The improver also chose a DIFFERENT valid fix this cycle (`display:flex; flex-wrap:wrap; min-width:0`, not `minmax(0,2fr)`) — so the old `bugs_open/024` verify query is stale, and `flex-wrap:wrap` is a FALSE delivery marker (it lived in the v1 render already).
  - **DEFECT 6 — still no render.** `rendered_html` is still the 9,901-char v1 with the broken grid. `create_rerender_items` produced **zero** per-page items despite `scoped=true`, a valid dependent page, and a correct spec. Cause, from `claimed_at`/`completed_at` (NOT `updated_at` — `bugs_open/035`): a reason-less `page_rerender` (`b5dbd732`) born 18:50 sat in the backlog; my `needs_rerender` ran 21:45–21:46 and its `create_rerender_items` insert of `page_rerender_tool-loot-table-balancer_<site>` **collided with the open `b5dbd732` via `ON CONFLICT DO NOTHING` → 0 items**; then `b5dbd732` ran 22:51–22:53 with no reason → assemble-only → re-deployed stale HTML. The per-page key (`create_rerender_items_action.go:248`) is site+page scoped, **not reason-aware** — same collision class as defect 4, one layer down, untouched by 180. Full write-up + 3 fix candidates in `bugs_open/024` UPDATE 2026-07-21. **This is the resume point.**
  - **Round 6 REVISE** (10/4/2). `bug_historian` (MED) predicted a SECOND call site of the template filter from platform history — RIGHT: `loadSingleComponentSchema` still rejected tool templates on `</section>`. Fixed both via shared `componentTemplateValid` (`3cb92dae4`, inert until image). `editquality`/`bug_historian` flagged the `item_key_suffix_field` warn-and-fallback asymmetry vs `spec_paths`' hard-fail — now both hard-fail. `reuse_agent`/`improvement_guardian`/`prior_art_librarian` answered by query (no combined validator exists; only tool-improver sets the new keys; numbering confirmed). `tooling_provenance`+`debug_historian` flipped to approve.
  - **WRONG CALL logged:** `editquality` caught that edit 4's SQL sketch in the round-6 submission **cut off mid-statement** — because I truncated the sketch at 5,200 chars when building the JSON. In a workstream about truncated artifacts reported as complete, I submitted a truncated artifact and called it complete in the rationale. `WRONG_CALLS.md`.
  - **NEXT:** §7 — fix defect 6 (Go, `create_rerender_items_action.go:248`; council/diagnosis loop recommended, key-scoping alone reopens via last-writer-wins), ship call site 2 of defect 5, then prove delivery by the rendered `.ltb-row-grid` rule changing.
- **T31 (2026-07-20, evening):** **The Go half of 024 is LIVE in v1.0.1140 — swept in by another session, on a REVISE verdict. A FIFTH defect found on double-check, without which the council-reviewed plan would not have worked. Migration 180 written + committed, NOT applied.**
  - **The plan got SMALLER once I read the call site.** Round 4 moved the re-render request into `update_component_html_action.go` via `createRerenderWorkItem`'s raw-SQL bypass; the owner ruled against that and the two-strike fix landed (`f6e3f3166`, inert). Reading the call site made two of three reviewed edits unnecessary: **`create_work_item` ALREADY calls `insertWorkItem`** (`create_work_item_action.go:170`) — the normal path, not a bypass. And the propagation was **already wired**: `rerender-pages.create_rerender_items` maps `reason ← input_data.spec.reason` and `component_id ← input_data.spec.component_id`, `create_rerender_items_action.go:139` sets `scoped` from both, `:243` stamps the reason onto each per-page item, which is what `check_rerender_mode` gates on. The chain works the moment the item carries both values. It carried neither, and **could not**: `spec_data` resolves as a **path** only (`action_inputs.go:125-141`), so no step could stamp a constant. Hence `spec_literal`/`spec_paths`.
  - **THE FIFTH DEFECT — the reviewed plan was a no-op.** `loadComponentSchemas` DROPS any component failing `sectionTemplateValid`, which keys on `</section>`. A tool ends `</script>`. The benchmark never entered the schemas map, so the exemption's `schemas[slot]` lookup fails and **it never fires**. 6 of 27 active tools, incl. all three gamesdesign tools. Five council rounds reasoned about the guard; **none of us asked whether the component reached it.** `toolTemplateValid` reuses the write guard's own signals (`balancedPairs` + `endsCleanly`), calibrated against all 27 live templates: 19 pass, 8 truncated fail — **4 of those 8 contain `</section>` upstream of the cut**, so the OLD guard admits them and can render broken markup.
  - **Shipped without me.** Sweep commit `bca5d8255` ("v1.0.1140 - sweep") took my four uncommitted files; that commit is what v1.0.1140 was built from. Verified in pod `agent-chassis-5567d99bd6-5snzn`: positive control present, three strings my change CREATED present, negative control absent. **The CLAUDE.md hazard, live** — committing per task stops *me* sweeping *others'* WIP; it cannot stop a session running `git add -A` from sweeping mine. Nothing lost, forward-only held. **On a REVISE, so no `Council-Reviewed:` trailer is legitimate.** Only two changes are non-opt-in (the exemption, `toolTemplateValid`); the four `create_work_item` affordances are default-off with no caller until 180.
  - **Round 5 = 9 approve / 3 object / 3 abstained → REVISE.** Dispositions: `bug_historian` MED → sibling heuristic **filed as `bugs_open/044`** (latent: 0 of 27 tools trip it); `guardian` MED → blast radius settled, **9 agent types** call `create_work_item`, one step each; `guardian` MED ("could key scoping be config-only?") → **no**, `item_key_prefix` is a static string and the component varies per run, so it needs a *resolved* value; `debug_historian` MED → migration hardened (defensive `ROLLBACK`, counting pre-flight distinguishing "already applied" from "target missing", needle gate, `RETURNING` post-conditions, post-condition block, separate VERIFY + ROLLBACK files, rollback **strips keys** rather than restoring the snapshot since that would revert other threads' later changes — `40c8f00b4` moved this agent's `error_step` twice today); `tooling_provenance` MED → loaded the subject's travelling NOTES first (three `fix` notes, each ending *"Verified: pending rerender + acceptance"* — the docs record the loop believing it succeeded three times while the page never changed) and 180 now **writes** a `('pipeline','build')` note. Round 6 submitted, `RUN_ORCH_ID=5688a8b2-48cc-4285-9372-3eb753017751`.
  - **THREE WRONG CALLS OF MINE, two logged in `WRONG_CALLS.md`.** (i) Read the page's `build_status='needs_rebuild'` as a fifth blocker — `get_pages_for_rerender` filters on **`p.status`** (`:153`), which is `'active'`. Two different columns; caught before it reached the submission. (ii) **"12 of 122" was wrong (live: 13 of 123), and my justification "no tool has an `input_schema`" was FALSE — 14 of 27 do.** Carried forward unchecked from a **one-day-old** doc; caught by the council refusing to take a population on prose. (iii) Cited `plan_sections_action.go:1090-1108`; it is **1141-1160**, shifted partly **by my own edit to that file**, and it is **not "silent"** — it logs at Warn with function and section. New tally row: *re-resolve a file:line you carried across sessions — above all one you edited yourself*.
  - **Numbering moved twice in one day:** **179 was taken** by another thread mid-round (`179_tool_guide_intro_cta_integrity.sql`), so mine is **180**. `177`/`178` remain on disk with **no ledger rows** — `bugs_open/007` still armed. Next free bug was **044**.
  - **NEXT:** §7. Read the round-6 verdict, apply 180 + its VERIFY (ledger row same sitting), re-run improve→rerender→acceptance, watch the benchmark go green, and watch for up to 8 new `needs_new_component` items from `toolTemplateValid`'s reject direction.
- **T30 (2026-07-19, late):** **Owner ruled against the bypass; two-strike rule FIXED (`f6e3f3166`); §0 + §7 rewritten for a clean new-chat start.** The council's one open objection (`improvement_guardian`) was that edit 1 leaned on `createRerenderWorkItem`'s raw-SQL insert, which bypasses `insertWorkItem`'s dedup/two-strike machinery — and my rationale had *touted* that bypass. Owner: **"don't write the bypass, fix the two strike rule."** Done: **`workItem.recurrenceExpected`** skips BOTH anti-churn heuristics — the two-strike label and the **within-cycle (<3h) suppression**, the latter being the more dangerous since it drops the item entirely and returns no error, so a fix landing within 3h of a previous rerender loses its deploy silently. **Dedup is NOT waived** (`idx_swi_dedup` still refuses a second OPEN item for the same key). **Opt-in, default false → INERT until a caller sets it**; none of the 24 existing `insertWorkItem` call sites change. **6 tests added (there were none for this rule)** — and the first version of the "poison" test passed without ever proving the row was branded `unresolved`, so they now assert the written **status and summary** ($12/$6), not merely that a row landed. Live scale of the defect: **111 items born `unresolved` in 30 days across 9 item types** (`undeployed_asset` 53, `needs_internal_links` 14, `page_rerender` 12, `deactivated_component` 11, `needs_sprite_css` 10, `needs_rerender` 4) — only the tool path is opted in; the rest is a per-type judgement.
  - **`bugs_open/031` filed then handed over.** The register-staleness case (a seat quoting our own concept register as "the pipeline's own contract" and blocking a correct plan at HIGH severity). Evidence went further than "stale": `git log -S "content_hash"` over the three rerender files returns **no commits at all** — the mechanism was **never** true, so the originating thread most likely observed a real symptom (pages not updating) and inferred a wrong cause. **Owner says another thread now owns it — marked as such in the case file, the 016b §10 row and T29; do NOT open a parallel front** (the correction spans five files). ⚠️ Until it lands, **any submission touching the rerender path will draw the same objection** — the disproof is in T29 and round 4's `grounded_in`.
  - **Fleet moved under us twice:** chassis+adapter **v1.0.1137 → 1138 → 1139** during this session, and migrations reached **177** (with duplicate 175s and 176s). §0 re-grounded against the live pods and ledger; **next free migration 178**. Both my round-4 submissions were eaten by the ~300s post-restart dispatch hole.
  - **Commit hygiene, live instance:** three of my file sets were swept into other sessions' broad commits (`754577564`, `c8c3088fd`) before my own pathspec commit ran — the exact hazard CLAUDE.md documents. Nothing lost, forward-only held, remainder committed narrowly.
  - **NEXT:** §7 is the resume block — items 1–5 there, starting with routing tool-improver's rerender through the normal path now the rule is fixed. Consider one more council round first, since the reshaped plan differs materially from the reviewed one.
- **T29 (2026-07-19, evening):** **Bug 024's fix through the COUNCIL GATE — 4 rounds, converged to 8 approve / 2 object, still advisory REVISE. NO code written, NO migration applied, nothing deployed.** Trail correlation **`7ef4de4e-3930-47fe-8ca6-ba40a2d440cc`** (all rounds; submissions saved as `travelling_docs/submission_024_tool_render_path{,_r2,_r3,_r4}.json`). Plan = 3 edits: (1) `update_component_html_action.go` REQUESTS the re-render by reusing `markPagesPendingRebuild`+`createRerenderWorkItem` (stamps `reason=section_data_resolved`+`component_id`, per-component key, `triaged`) instead of the dead `build_status='pending'` flag; (2) `rerender_page_sections_action.go` stops escalating sections whose component needs no LLM content; (3) migration **171** drops tool-improver's redundant/mis-keyed/reason-less `create_rerender_item` step.
  - **The council materially improved the fix, twice.** (i) **bug_historian (round 1) was right**: my predicate (`missingRequiredLLMFields(schema,nil)` empty → skip) exempted a BROADER class than justified — it also covered components declaring OPTIONAL `source:"llm"` fields, a class I had never examined. Its "missing" note said use an EXPLICIT marker, and one exists: **`content_components.component_level`**, and the loot component is `component_level='tool'`. Re-keyed on the marker → **12 of 122** components change, not 19, and the 5 schemaless *sections* + 2 schemaless *site* components are now untouched. `component_level` is already SELECTed by `loadSectionComponents` (`v3_site_actions.go:3389`) and carried on `componentInfo.Raw` — no new query, no struct change. (ii) **The reviewers' own read-only checks** found `complete.config.output_fields` still lists `"rerender_item"` — removing the step without clearing it leaves a dangling reference, the exact class edit 3 exists to prevent.
  - **The council was WRONG once, at HIGH severity, and the cause matters.** `render_guardian` (round 3) blocked on: scoped rerender "SKIPS pages whose content hash is unchanged" — which would make this fix a no-op. **The sentence is real but is NOT from the code — it is quoted from the concept register** (`docs026_concept_register/register/styling-render-pipeline.md:389` + two `.buckets` copies). Disproof: `grep -rn content_hash --include=*.go platform/ internal/` → **zero** hits in `rerender_page_sections_action.go`/`create_rerender_items_action.go`/`save_page_sections_action.go`; the re-render loop has exactly **three** skip paths, none hash-based (component not found / `planSection` != "ready" / empty html_template); and probe `478c44c9` had already driven scoped mode through this page without skipping (`section_count:1`). Round 4 carried the evidence and **render_guardian APPROVED**. ⚠️ **A seat citing a stale register entry as fact produced a high-severity blocker on a correct plan — the register entry needs correcting. Filed as `bugs_open/031`; **OWNED BY ANOTHER THREAD as of 2026-07-19 (owner-confirmed) — this thread is NOT working it, do not start a parallel fix.** Note the register still carried the false claim when last checked, so until that lands, expect the same objection on any submission touching the rerender path — the disproof above is the answer.** *[UPDATE 2026-07-20: 031 is FIXED & LIVE → `bugs_closed/031`. Register + all copies corrected, and the claim was ALSO embedded in the live `render_guardian` seat prompts of BOTH council rows — patched (`PATCH_render_guardian_031_content_hash.sql` + 099 sync, verified). The content-hash objection cannot recur from that text; this note kept for the trail.]*
  - **Chasing that objection found a REAL adjacent risk nobody had named**, now disclosed in the submission: `plan_sections_action.go:1090-1108` defers an empty-schema component — so it is **CARRIED, not re-rendered** — when `comp.Function` contains `article`/`content`/`body`/`text`/`blog`. `tool-loot-table-balancer` matches none (returns "ready", re-renders); a future **`tool-content-*` would be silently carried**. Same silent-carry-by-heuristic shape as the bug being fixed, one function away.
  - **Round 4 hardening** (all debug_historian): migration 171 now gated on the pre-state marker (`steps ? 'create_rerender_item'`) so a re-run is a 0-row no-op (needle-gate rule); `RETURNING` asserts post-conditions inline; and an explicit **VERIFY-AGAINST-THE-POD** pre-flight (grep the running binary for `createRerenderWorkItem`) guards the sequencing hazard (**Go edits inert until an image ships; 171 is live immediately — applying 171 first removes the only rerender request and leaves NONE**).
  - **OPEN — owner decision.** `improvement_guardian` (MEDIUM, fair): edit 1 leans on a helper that inserts straight at `triaged` via raw SQL, **bypassing `insertWorkItem`'s dedup/two-strike machinery** — and my rationale *touted* that bypass, because it dodges the two-strike bug found in T28. Reusing a contract-violating shape to fix a bug entrenches it. **Either implement as reviewed, or first fix the two-strike rule (counts `complete` as a failed attempt) so edit 1 need not lean on the bypass.** Also open, low: both helpers only `logger.Warn` on failure, so a failed rerender request reverts silently to this exact bug.
  - **Council operational traps banked.** (a) A submission fired within **~300s of a chassis (re)start is SILENTLY DROPPED** — round 4 was lost TWICE around the **v1.0.1138** deploy (no artifact, no error, no log line); re-firing after the pod settled worked first time. Confirm a `fix_plan` artifact appears within minutes or assume it was dropped. (b) These runs are **stateless** — no `orchestration_states` row, so the 097 banner's watch query returns nothing; poll `diagnosis_artifacts` **filtered by `orchestration_id`**, never by correlation alone, or you read the PREVIOUS round's verdict and mistake it for success. (c) Queueing on the shared topic ran 6–27 min; absence from logs is not proof of a drop.
  - **NEXT:** owner's call on the two-strike question; then write the Go, build+roll an image, verify against the pod, apply 171, and finally re-run improve→rerender→acceptance to watch the benchmark go green. Next free migration still **171**.
- **T28 (2026-07-19):** **The benchmark cannot go green — no tool fix has EVER reached a tool page. Filed `bugs_open/024`.** Picked up T27's NEXT item 1. **168 is proven**: tool-improver re-ran clean on claude-sonnet-5, component 10,272 → **10,626 chars structurally whole** (`</script>`/`<fieldset>` intact), real `fix` note, correctly root-caused to the grid (`minmax(0,2fr) minmax(0,1fr) minmax(0,1fr) auto`) — the truncation fix works on a real repair. **But the live page has not changed since the tool was born on 07-17**: `page_components.rendered_html` is still 9,901 chars = the **v1 born length**, and all three repairs sit only in the durable template. Three defects in series, each hiding the next: (1) tool-improver's rerender item is **born `unresolved`** — the two-strike rule (`load_work_item_actions.go:1019`) counts `complete` as a failed attempt, but for a rerender `complete` = success, so two successful rerenders on 07-17 poisoned the key for 7 days; compounded by `create_work_item_action.go:128` keying `<prefix>_<domain>` site-wide (per-page keys would NOT fix it — the same tool fixed twice still self-poisons). (2) A hand-inserted rerender **deployed stale HTML and reported success** — `check_rerender_mode` gates on `spec.reason IN (image_landed, section_data_resolved, cta_links_stale)`; tool-improver sets **no** `spec_data`, so it always takes `else_step: render_page` = `rerender_single_page` = *"Simple concatenation - no template re-rendering"*. (3) Forcing the good branch (`reason=section_data_resolved`, item `478c44c9`) proved the gate diagnosis right — `rerender_sections` RAN — then **escalated** instead of rendering: `rerender_page_sections_action.go:186` refuses any section with empty `content_data` (the article-body blanking guard, `bugs_closed/004`/`005`), and a tool has `content_data={}` **by design** (no `input_schema` at all). `check_escalated` routes `true → complete`, bypassing `save_sections` — the **only** writer of `rendered_html`. The render is computed and discarded. Also: `page_components.build_status='pending'` is **dead state** — nothing reads it (`store_generated_component_action.go:455` says so).
  - **CORRECTION to T24.** Its claim that *"the durable fix reached the live page (`max-width` present ×10)"* is **wrong** — all ten are unrelated site-chrome rules; the tool's own `fieldset` rule has none. **This thread made the identical mistake twice** (matched `min-width:0`, which dates from attempt 1; then nearly repeated it with `minmax(`, of which the page has 4 unrelated instances). Durable rule now in 016b §9 + RUNBOOK §9: **verify a specific fix by matching its specific rule, never a generic CSS property**; and compare template vs stored render vs live bytes rather than assuming they agree.
  - **CONSEQUENCE for `bugs_open/010`.** Its non-convergence evidence — two RED re-verifications, two "materially identical" fixes — is explained by an **unchanged page**, not a fixer that cannot aim. Candidate (a) (drill-down) was still a real improvement; candidate (b) (convergence guard) would have fired on a loop never given a chance to converge. Re-read 010 with 024 alongside.
  - **Cleaned up:** the escalation emitted a `needs_page` **full rebuild** (`79203a68`) — the WRONG remedy for a tool page (a rebuild regenerates the tool and would destroy the benchmark; the re-plan-clobbers-built-pages landmine). Cancelled with a `result.resolution` (T9/T20 convention).
  - **Nothing deployed, no migration applied, no Go changed.** Next free migration still **171**. The benchmark tool is still RED and still the benchmark — **do not hand-fix it.**
  - **NEXT:** (1) decide the fix for 024 — the minimum is *both* a `spec_data.reason` on tool-improver's rerender AND teaching the guard that a section with no `input_schema` needs no `content_data`; either alone leaves the path dead. It touches shared rerender machinery used by every site, so **council gate first** is the suggested route (owner steer requested in `README_where_we_are.md`). (2) then re-run improve→rerender→acceptance and finally watch the benchmark go green. (3) `bugs_open/010` (b) convergence guard. (4) `bugs_open/021` widen the write guard.
- **T27 (2026-07-19):** **Protection chain COMPLETE; read-out summary written; start a new chat from here.** Verified against pods: **v1.0.1137** (chassis + adapter) now carries the drill-down (adapter `a grid item is not shrinking`; chassis `overflow_fix_hint`), the **component write guard** (`a collapse this large`), and **`stop_reason`** truncation detection — all three live. Applied **migration 169** (`tool_improver_refusal_path`, written by the guard's thread, flagged NOT-APPLIED): `update_component.error_step` was **null**, so a guard refusal hit `failWorkflow` — orchestration FAILED, item to the reaper, **no note**. Now routes `refuse_mangled_write → note_refusal → complete`, so a refusal reads as `needs_human_review` **with the guard's own reason**. Verified post-apply. That closes the loop opened by bugs_open/012. **Next free migration number: 171** (170 is another thread's `tool_list_card_image`).
  - **Corrections absorbed (I had two facts wrong; the guard's author fixed them in bugs_open/012).** (i) The guard is wired into `update_component_html` **ONLY** — NOT `store_generated_component`, which keeps its own schema-shaped gate (a proposed consolidation was withdrawn after council objection). (ii) There was **no** `error_step` route until 169. My independent verification against the REAL stored artifacts stands and is confirmed: wreck 10,280→1,253 blocked on all three checks; intermediate 10,280→6,771 blocked on **structure alone** (it passes the 50% size floor at 66% — size checking is NOT sufficient); restore allowed. Guard commit `cc7bcc881`; follow-up `bugs_open/021` (page_components.rendered_html + pages.rendered_* share the same unguarded shape).
  - **My 098 fix was kept and extended.** The stdin-theft fix (drop `-i`; it ate the `while read` loop's git-log stdin, reporting 4 of 41 in-scope commits) stands. My "defect 2" hypothesis (kind filter too narrow) was **wrong**: the real causes were v1 matching only `correlation_id` (now matches `orchestration_id` too) and council reports being **deleted** by 091's documented clear-for-a-fair-run practice — so "no row" ≠ "not approved". The script gained an **EVIDENCE GONE** bucket. Pattern is in 016b §9 (`stdin-theft`).
  - **Read-out written**: `SUMMARY_readout_2026-07-19.md` (what we're doing / where we came from / what we've done / where we are / where we're going — plain language, meant to be read aloud). Machine-authored totals as of today: **8 tool PLANs, 125 notes, 12 Tier-4 verdicts**.
  - **NEXT (in priority order):** (1) watch the benchmark tool (`tool-loot-table-balancer`, deliberately left overflowing) finally go GREEN — the last outstanding proof; the aimed-correctly repair was the run that truncated, so re-run improve+rerender+acceptance now that the guard and 169 are live; (2) `bugs_open/010` candidate (b) — a **convergence guard** (after N non-converging cycles → `needs_human_review` instead of retrying weekly forever); (3) `bugs_open/021` — widen the write guard to `page_components.rendered_html` / `pages.rendered_*`; (4) coordinate with the **Experience Loop** thread (it extends these travelling docs with `subject_type='experience'`; overlap assessment in T26).
- **T25 (2026-07-17):** **Drill-down overflow attribution BUILT + validated (bugs_open/010 candidate a).** The overflow signal named the widest ANCESTOR (fieldset), so the one-shot fixer kept constraining it while the real cause sat deeper (T24: two Sonnet-5 attempts both missed). `HorizontalOverflow` now descends from the widest offender through the children that cross the viewport edge, then names the outermost layout container (grid / flex-nowrap) as the fix target — else the deepest crossing leaf — and says WHY it won't shrink; new `forced_by`/`forced_reason` ride the CheckResult, the detail string (→ improve_tool issue), and both specs (`overflow_forced_by`/`overflow_fix_hint`; chrome suggestion gains "fix THAT element, not just its container"). **Probed against the live loot bug before shipping** (the 148/T16 "prove it live first" rule): signal went from `fieldset (426px)` → *"forced by div.ltb-row-grid [grid layout (228px 123px) — a grid item is not shrinking; set min-width:0 on the items or let the grid wrap]"* — the exact element+fix the fixer kept missing; confirmed the grid items keep default `min-width:auto`. Code: `run_checks_action.go` (JS drill-down + `evaluateOnPage`), `tool_acceptance_actions.go` (judge threading). 4 unit tests (adapter ×2, judge ×2) + the live probe; both packages green. **GATE: ships on the next browser-runner-adapter image (JS/adapter) + chassis image (judge threading).** Not re-verified end-to-end yet — needs the images, then a fresh acceptance run so tool-improver gets the pointed hint. Candidate (b) (convergence guard → needs_human_review after N failed cycles) still open. Migrations unchanged; next free **164**.
- **T24 (2026-07-17, v1.0.1128):** **New models proven through the REAL pipeline + the tool-birth deploy gap CLOSED + a genuine tool bug found by Tier-4.** Verified v1.0.1128 on the pods (adapter still `failure screenshots enabled`). Fired tool-generator for a new tool **tool-loot-table-balancer** on gamesdesign: `llm_call_log` confirms both steps ran **claude-sonnet-5** (generate 6094 tok / compose_plan 761 tok), component + PLAN-at-birth (fenced, `container` present) + `index_plan` (3 chunks/3 embeddings) all correct, 0 errors. **Sonnet 5 proven end-to-end through the actual generator.**
  - **Composer shape defect found + fixed at birth (migration 160).** Sonnet-5's compose_plan emitted the interaction check as `{"type":"click","expect":"<string>"}` — not a Tier-4 check type (types are page_status_ok/selector_exists/selector_count/no_console_errors/no_horizontal_overflow/**interaction**) and `expect` must be an object, so the runner skips it and the behaviour goes untested. Selectors were REAL (no-invention rule held on the new model); only the SHAPE was improvised, because the compose_plan prompt described interactions in prose and never showed the JSON — every prior well-formed interaction check was hand-written in a migration (143/148), so the gap had never been exercised. **160** added the exact JSON shape + allowed step actions to the prompt (snapshot taken). **161** superseded the one already-born PLAN to the real interaction shape — **probed passing in real Chromium against the live page FIRST** (the 148 rule): row-4 does NOT exist pre-click on either profile (expect not vacuous), the `#ltbAddItem` click produces it on desktop AND mobile.
  - **Tool-birth deploy gap CLOSED (Go + migration 162; activates on next image).** The long-standing §5-item-3 gap — tool-generator never enqueues a `page_rerender`, so a new tool page sits `build_status=planned` until an unrelated sweep (all 3 births to date needed a hand-inserted item). `create_tool_component`'s result even carries `needs_rerender:true` that nothing acted on. Taught `create_rerender_items` a **single-page mode** (`page_id_field`/`page_name_field`/`filename_field` scalar config, tolerates a leading `/`; 4 unit tests) so one action still owns the item shape/status/dedup key, then **migration 162** wired a tool-generator tail step `index_plan → enqueue_rerender → complete` feeding it from `create_result` (`error_step=complete`, snapshot taken). **Safe/no-op on the current image, activates on the one carrying single-page mode (the 147 pattern).** Trap re-learned while hand-deploying the loot page this turn: `create_rerender_items` inserts at **`triaged`**, NOT `detected` — detected sits unswept on gamesdesign; my first insert guessed `detected` and stalled. **GATE: the Go single-page code rides the next chassis image; 162 is already applied and inert until then.**
  - **REAL failure → tool-improver → fix, on Sonnet 5 (closes the T8/T9 open milestone).** Acceptance on the corrected PLAN: **8/9 passed** — incl. `add-item@desktop`+`@mobile`, so the reshaped interaction genuinely drove the tool — and **`mobile-fit@mobile` FAILED on a real defect**: a `fieldset` 419px in a 390px viewport, attributed **"inside the tool"** (NOT chrome) → routed to **improve_tool** with the P3 screenshot. First non-manufactured failure to reach tool-improver. Promoted it; **tool-improver ran on claude-sonnet-5** (improve_tool 4767 tok + compose_note 250 tok), constrained the fieldset (`max-width:100%` + `box-sizing` + `flex-wrap`/`min-width:0`), wrote a machine **`fix` note**, and enqueued its OWN rerender (`0743bfa9`) — the fix loop is autonomous on the new model. The durable fix reached the live page (rerender eventually drained the 83-deep backlog; `max-width` present ×10). **RE-VERIFY CAME BACK RED — TWICE — and that is the loop working, not failing.** The fixed template still overflows: `mobile-fit@mobile`, `fieldset (419px)`, unchanged. tool-improver (Sonnet 5) had constrained the fieldset the adapter NAMED (`width:100%; min-width:0`) but not the grid child inside `#ltbRows .ltb-row-grid` that actually forces the width. A second improve_tool cycle — Sonnet 5, loading its OWN prior fix note — produced a **materially identical** fix and stayed RED. So: the behavioural tier caught an insufficient fix twice ("passed checks ≠ working", now shown on the *fixer* itself, on the new model), and the fix loop **does not converge** on this defect. Two linked causes: (1) the overflow signal names the widest ANCESTOR (fieldset), so the one-shot fixer keeps targeting it and never reaches the forcing descendant — the SAME class T15 solved for chrome attribution; (2) nothing bounds a non-converging loop — each fail raises a fresh improve_tool item (the 3-attempt cap never engages), gated only by the 7-day cooldown, so it would re-fail weekly forever. **Filed as `bugs_open/010_HANDOFF_2026-07-17_fix_loop_stuck_on_intrinsic_overflow.md`** (candidates: drill-down overflow attribution in the adapter; a convergence guard → needs_human_review after N failed cycles). The tool is left overflowing ON PURPOSE as that finding's benchmark. (Two transient infra notes from these runs: one acceptance run hit `complete_error` on a `request_browser_run` timeout — re-fired cleanly; and `AWAITING_RESPONSES` is plural, so a terminal-state poll excluding the singular exits early — watch that.) Migrations 160/161/162 applied out of band. **Next free migration number: 164** (163 + a second 157 taken concurrently; collisions continue, always check max+1).
- **T23 (2026-07-16):** **MIGRATION RUNNER UNBLOCKED — the gripper-151 "failure" was a ledger omission, not a broken migration.** Owner identified: the **empty-sections/loop-integrity workstream** (`docs024_key_docs_latest/empty_sections_loop_integrity/HANDOFF_2026-07-16_continue_here.md` §7), whose handoff states 149–156 were APPLIED by that workstream — but 151–156 were never recorded in `schema_migrations`, so the runner replayed 151 into its own duplicate-component error and halted. Verified every file's artifacts live in the DB before touching the ledger: `gripper-spec-sheet` component (151), 5 gripper products (152), the detail-page slot layout incl. spec-sheet slot (153), the `gripper-spec-sheet` site_plan_sections row (154), `section_source_drift` in **completeness-discovery-agent**'s checks — NOT design-discovery, easy to mis-verify (155), and the active `product-spec-refresher` agent (156). Backfilled six ledger rows (`applied_by='ledger-backfill'`, note cites the owning handoff + verification). **Runner dry-run now reports "Up to date — no pending migrations."** Their handoff §7 updated so their next session doesn't re-investigate. Durable rule: out-of-band applies MUST insert their ledger row (as this workstream's 157/158/159 did) — an applied-but-unrecorded migration becomes a runner roadblock with someone else's name on it. Tooling fixes filed as **`aaa_fails_to_mend/007_HANDOFF_2026-07-16_migrations_ledger_omission_trap.md`** (recommended: `--record-only` flag + duplicate-key failure hint in run-migrations.sh). Next free number: **160**.
- **T22 (2026-07-16):** **recreate_tool → claude-opus-4-8 (migration 159).** Completes the model refresh: snapshot taken, guard demanded exactly 1 row, verified post-apply (recreate_tool = opus-4-8 @ 64k tokens; analyze_tool/compose_note untouched on sonnet-5). Applied out of band as before. The whole tool pipeline now runs the current generation: 7× claude-sonnet-5 + 1× claude-opus-4-8. **Next free migration number: 160.** Untracked: 159 file joins 157/158 + SUMMARY for the next `git add`.
- **T21 (2026-07-16):** **Summary doc written + tool pipeline upgraded to Sonnet 5 (migration 158).** New `SUMMARY_travelling_docs_2026-07-16.md` (what we've done / where we are / where we're going — the plain-language wrap of the whole arc). Model audit answered the user's question: models live in `agent_definitions.default_config → workflow.steps.<step>.config.ai_service.model` (NOT the workflow columns — all NULL for these agents; and the chassis default in `anthropic.go` is claude-sonnet-4-6). The travelling-docs agents ran **claude-sonnet-4-6** on all 7 Sonnet steps (tool-generator: generate_tool_html + compose_plan; tool-improver: improve_tool + compose_note; tool-recreation-handler: analyze_tool + compose_note; component-template-fixer: compose_note) + **claude-opus-4-6** on recreate_tool (64k). tool-acceptance-agent has NO LLM steps. **158** (snapshots ×4, guard requires exactly 7 updates) moved the 7 Sonnet steps → **claude-sonnet-5**; recreate_tool deliberately left on opus-4-6 (Opus 4.8 = separate call). Safe without rebuild: ResolveModelAlias passes unknown ids through; the client sends no temperature (Claude 5 rejects non-default); **diagnose-agent already ran sonnet-5 through this chassis in prod** (fixloop workstream) — the de-risking precedent. Applied out of band again (runner still blocked at gripper-151; 152–156 foreign + pending) — **next free number 159**. Also added claude-sonnet-5 / claude-opus-4-8 to `model_aliases.go` (documentation; rides next image). Fleet context left untouched: ~31 other agents on sonnet-4-6 (44 steps), 23 on haiku-4-5 — user's call, 158 is the template. Untracked files to `git add` before next release: 157, 158, the SUMMARY doc.
- **T20 (2026-07-16):** **P3 PROVEN LIVE on v1.0.1125 — and the proof caught a real dedup defect, fixed as migration 157.** User deployed BOTH images at v1.0.1125 + re-applied the adapter overlay. Verified against pods, not git: chassis binary (kubectl cp, sha256 match) carries `evidenceLine` + the `.response.data.screenshots` fallback string + the consumer's new empty-poll debug string (`shotsForSpec` is inlined away — verify by string constants, the T14 rule); adapter startup log says `failure screenshots enabled, bucket personae-prod-uk001-images`, `failure_screenshots: true`, and the idle ERROR spam is GONE. **Proof (T9-style controlled failure, fully reverted):** injected one failing check (`p3-proof`, `#p3-proof-not-present`) into drop-rate's PLAN (in-DB backup table first), fired an 087-style trigger (script rebuilt in scratchpad — the old drafts live in a dead workspace). Run 1 COMPLETED: adapter stored TWO full-page screenshots (desktop 142KB, mobile 443KB, keyed `acceptance-evidence/<site>/<fn>/<run>_<profile>.png`), and the acceptance-fail note carried the `Evidence:` line with both durable URIs — **but NO improve_tool item appeared.** Root cause: **`idx_swi_dedup` treated `cancelled` as OPEN**, so T9's cancelled 2026-07-12 test item still held the `(site_id, item_key)` slot and the judge's `ON CONFLICT DO NOTHING` insert vanished silently. This contradicted the recorded intent (cooldown queries already exclude cancelled; T15 parked the vonc quiz item as "regenerable" — it wasn't). **Migration 157** (`157_swi_dedup_excludes_cancelled.sql`) rebuilt the index with `cancelled` in the closed set (safe: new predicate is a strict subset — no violation possible) + left the conventional pipeline note. ⚠️ Numbering: other workstreams took 152–156 (gripper/product files, all still PENDING behind the failing gripper-151) — **next free number is 158**; 157 was applied OUT OF BAND (`psql -f` + manual ledger row, `applied_by='psql -f (runner blocked at gripper-151)'`) because running the runner would apply five other-workstream files as a side effect. Run 2 after 157: **improve_tool item created WITH `spec.screenshots`** (durable uri + 7-day presigned view_url per profile); **curl of the view_url returned HTTP 200, 442,982 bytes — byte-identical to the adapter's log — a real 1170×5457 full-page PNG of the live Drop Rate Tuner** (403 on HEAD is normal: the presign signs GET). **Cleanup, zero orphans:** PLAN restored byte-exact (3046 chars, md5 `c62b48bd…` = pre-test), both manufactured fail notes DELETED, ticket cancelled with `result.resolution` (the T9 convention — `site_work_items` has no notes/resolution column), backup table dropped. Remains deliberately: 157 + its note + ledger row; 4 inert evidence PNGs in B2. **P3 is fully live and proven. The screenshots feature has now paid for itself once already — its first run exposed the dedup bug.** NEXT: nothing gated; remaining polish unchanged (7 footer sites self-heal; optional per-site override; DEBUGaa sweep someday).
- **T19 (2026-07-16):** **P3 — SCREENSHOTS ON FAILURE — BUILT; deploy-gated.** Adapter: a failing (url, profile) run now captures a full-page PNG **while the page is still open** (post-interaction state) and uploads it via the imagegenerator's S3/B2 client — bucket `personae-prod-uk001-images`, key `acceptance-evidence/<site>/<function>/<run>_<profile>.png` — attaching `screenshots` refs to the run_checks response: **durable `s3://` URI + 7-day presigned `view_url`** + the `id@profile` instances it evidences. **Evidence, never a dependency:** no storage configured / capture error / upload error → a log line, the verdict untouched; nav-failed pages aren't photographed (no state worth keeping). Judge: extracts refs through the same response-shape fallback chain as results; fail notes (and pass-with-chrome notes) gain an `Evidence:` line carrying **ONLY durable URIs — presigned URLs never enter doc_notes** (notes are fed into LLM prompt contexts by load_doc_context; a signed URL is hundreds of expiring chars), while item specs get both forms: improve_tool takes all evidence, each chrome_overflow item takes its own profile's. Deploy wiring: overlay config + deployment-env carry `object_storage` + B2 secret refs (`personae-storage-secrets`, mirroring imagegenerator; S3_REGION/S3_USE_PATH_STYLE env); a missing secret degrades to "screenshots disabled" at startup, P0–P2 behaviour intact. Tests: 6 new adapter (capture-on-fail, none-on-pass, none-without-store, errors-never-fail-the-run, none-on-nav-failure, key sanitising) + 4 new judge (fallback extraction, URI-less refs dropped, **durable-URI-only note invariant**, per-profile spec filter); all three packages green. **ALSO fixed chassis-wide:** `platform/kafka` `FetchMessage` no longer ERROR-logs the normal empty-poll `DeadlineExceeded` (T14's log-drowning defect — every idle adapter, every 10s; `errors.Is`, not `==`, since kafka-go may wrap it); real fetch errors still ERROR. Checked and left alone: the old Task-C `DEBUGaa` logging is still in the tree (processor.go/agent.go — a wide sweep for its own turn). **GATE: next chassis image (judge + consumer fix) + next adapter image (capture/upload) + re-apply of the adapter overlay (new env/config).** Proof after deploy: fire 087 at a failing tool → the acceptance-fail note shows an `Evidence: s3://…` line and the item spec carries `screenshots`. NEXT: that proof, then the remaining polish (7 footer-4-column sites self-heal on cadence; optional per-site override).
- **T18 (2026-07-16):** **Redesigned durable-layer fixer DEPLOYED + docs rolled forward for a clean new-chat start.** Verified **v1.0.1123** against the pod (symbol table: `patched the DURABLE content_component template`, `shared_sites`, `chrome_overflow_fix`, `TRANSIENT`) — the part-2 fixer redesign (writes to `content_components.html_template`, falls back to rendered_html only when a slot has no backing component and reports it TRANSIENT, reports shared-site blast radius) is LIVE. No new code this turn. Rolled the four docs forward: **RUNBOOK rev 45** (`RUNBOOK_travelling_docs(39).md` — new §0 "THE LOOP IS COMPLETE" position line), **RUNNING_NOTES rev 40** (`(40).md` — appended the full 2026-07-14→16 chronological arc), **PLAN rev 7** (`(7).md` — new "Completed loop" section + status), and a fresh **`STATUS_2026-07-16_where_we_are.md`** (supersedes the 07-13 snapshot; milestone ladder now 11 rows through "Durable fix + re-verify GREEN"). Updated this handoff's §0 (v1.0.1123, migrations through 151 + the collision warning, vonc site_id added, companion revs bumped). **The self-verifying tool loop is COMPLETE and proven green end-to-end on a real bug; remaining work is polish.** The migration-number collisions and the blocking gripper-151 are being handled in a SEPARATE chat (per user). NEXT (polish, unblocked): P3 screenshots; let the other 7 footer-4-column sites self-heal on refresh cadence; optionally a per-site override so autonomous shared-template edits stay narrow.
- **T17 (2026-07-15):** **The targeted fixer works — and then the deploy exposed a deeper bug: the fixer edits the wrong LAYER.** v1.0.1119 verified live (chassis `chrome_overflow_fix`/`refusing to guess`; adapter `culprit_selector`). Re-ran 087 on the quiz (one run orphaned in the pod-rollover response-topic gap — re-fired cleanly). Adapter returned the full attribution: `scope=chrome, culprit_selector=div.footer-legal, slot=footer`. Judge raised a correct ticket — **`fix_type=chrome_overflow_fix, slot_name=footer, overflow_selector=div.footer-legal`**. Promoted it; `component-template-fixer` ran `fixChromeOverflow` and this time patched the **FOOTER** slot (header untouched) with the right CSS. Verified in DB: footer 3741→4017, `flex-wrap: wrap` targeting `div.footer-legal`. **The detect→attribute→route→fix-correct-target loop is proven**, and the CSS itself was proven live-effective in Chromium last turn (506→326px, overflow→0).
  - **BUT it did not go live, and the reason is the T-lesson of the vonc workstream.** I drove the deploy via the queued `stale_sc_footer` rerender (`refresh_site_components: true`) — which **regenerated the footer from its content_component template and WIPED the patch** (footer 4017→3935, `still_patched=f`). `site_components.rendered_html` is a **rendered artifact**; the durable source is `content_components.html_template`. The fixer (BOTH `chrome_overflow_fix` and the legacy `fixInjectResponsiveCSS`) writes to the artifact, so any `refresh_site_components` erases the fix. This is the memory's `[[vonc-spark-workstream]]` landmine ("runtime-fill templates are rendered artifacts"), now shown to bite the fixer path.
  - **Root bug located at the durable layer:** vonc's footer renders from content_component **`footer-4-column`** (id `09034086-a581-4bba-a5b4-760d863bb2df`), whose template carries `.footer-legal { display:flex; gap:2rem; }` with **no `flex-wrap`**. That template is **SHARED across 8 sites** (incl. gamesdesign `e33263f4`) — so this one flex-nowrap rule overflows the footer on 8 sites' mobile views. (Sibling `site-footer` template already has flex-wrap; `footer-4-column` never did.)
  - **DECISION NEEDED (surfaced to user):** where should the overflow fixer WRITE, and do we fix the shared template now? The core loop is proven; this is about durable-fix location + blast radius. Live state is CLEAN: DB patch was wiped by the refresh (nothing stale left in site_components), header revert from T16 holds, no fix is live.
  - **✅ CLOSED GREEN (user chose: fix template + redesign fixer).** Migration **151** (`151_footer4col_flexwrap`, numbering collided AGAIN — a `151_gripper_spec_sheet_component` from another workstream also exists and FAILS on a duplicate content_component, blocking the runner past it; **next free number is 152, and that gripper file needs its owner to fix/guard it**) added `flex-wrap:wrap; justify-content:center` to `.footer-legal` in the shared `footer-4-column` template, with the full pre-edit template backed up in a doc_note (`created_by='151_footer4col_flexwrap'`, subject_type must be tool|pipeline — `component` is rejected). Re-triggered `stale_sc_footer` (refresh now pulls the FIXED template) → footer regenerated WITH the wrap → live on vonc. **Re-ran 087: `mobile-fit@mobile` now PASSES** — note reads "all 9 of the tool's own checks passed", mobile-fit@mobile in the Verified list. **The real-bug loop is proven green end-to-end: system detected → attributed to chrome → routed → (caught the fixer's wrong-LAYER bug) → fixed the durable source → deployed → re-verified green.**
    - The other **7 sites** sharing `footer-4-column` now have the fixed template but STALE `rendered_html`; they self-heal on their next refresh/rerender. Not force-rerendered (7 live sites — left to natural cadence / user).
  - **✅ FIXER REDESIGNED (part 2).** `fixChromeOverflow` now resolves the slot's backing component (`site_components.component_id` → `content_components.html_template`) and patches the **DURABLE** layer — survives `refresh_site_components`. Falls back to `rendered_html` ONLY when a slot has no backing component, and then reports `durable:false` / "TRANSIENT" honestly rather than pretending. Reports `shared_sites` (blast radius) in the result + logs it: patching a shared template is correct for a genuine shared CSS defect, but never silent. Idempotent per-selector marker; guards (safe selector, slot required) unchanged. Proven against real data: the fixer's resolution query for (vonc, footer) → `footer-4-column`, shared_sites=8 — the exact durable target migration 151 fixed by hand. Builds clean; helper tests (safe-selector injection guard, CSS shape, marker) pass.
    - Edge note: 151 fixed the rule INLINE (no marker); the redesigned fixer keys idempotency on its own marker, so if it ever re-runs on vonc it would append a (harmless, redundant) media-query block. Not worth special-casing.
  - **GATE: one more chassis image** carries the redesigned fixer. No adapter change this time (T17's adapter culprit_selector/slot already shipped in v1.0.1119).
  - **The self-verifying tool loop is now COMPLETE and proven on a real bug: birth-PLAN → continuous discovery → Tier-4 behavioural run (desktop+mobile, interactions) → attribution (tool vs site-chrome) → routing (improve_tool vs responsive_fix) → durable fix at the right layer → deploy → re-verify GREEN.** Everything from here is polish (P3 screenshots; teach the composer to emit `container` — done in 149; self-heal the other 7 sites' footers on cadence).
- **T16 (2026-07-14):** **The fixer was the broken link — and Tier 4 caught it.** v1.0.1117 verified live (chassis: `routeChromeFailures`/`chrome_overflow`; adapter: `toolContainer`/attribution). Promoted ONLY the routed `responsive_fix` item (a full vonc triage would have released 10 unrelated items on a live site). It dispatched to `component-template-fixer`, which returned **`"fixed": true`** — and had changed **nothing** in the footer. Re-measured the live page: **identical 506px `div.footer-legal` overflow**. The behavioural tier caught a fixer that lied; "complete" ≠ fixed, exactly the illusion this ladder exists to puncture.
  - **ROOT CAUSE — a silent default.** `fixInjectResponsiveCSS` reads `spec.slot_name`, and when absent **defaults to `slotName := "header"`**, then injects a *hardcoded header-nav CSS block* (`.main-nav ul`, `.site-header__categories`…). My ticket named the component in `affected_component` (copying the design-audit shape), never `slot_name` — so it patched vonc's **HEADER** for a **FOOTER** defect and declared success.
  - **SYSTEMIC, not just mine: ALL 54 `responsive_fix` items ever raised have NO `slot_name`.** Every one defaulted to the header. The first on a site injects the canned block; the rest return `fixed:false, "already has responsive CSS"` — and are marked **complete** regardless. Page-section responsive findings (`container`, `features-container`, grid columns…) have therefore **never actually been fixed**, just closed. *(Left untouched by your call — it belongs to another workstream's backlog; flagged for you to schedule.)*
  - **REVERTED the collateral damage:** stripped the unrequested header injection from vonc's `site_components` (4170 → 3640 chars, marker-guarded). It had **not** gone live — but a `needs_rerender` was already queued that would have carried it.
  - **DIAGNOSED the real bug properly** (asked the browser, never guessed): `.footer-legal` is `display:flex` with the default **`flex-wrap:nowrap`** and `gap:2rem`, holding 6 links → intrinsic 506px in a 390px viewport. The rule lives in the **footer slot's own `<style>`** (`site_components`), not in `styles.css` (which has no `.footer-legal` rule at all).
  - **BUILT `chrome_overflow_fix`** (your call: new targeted fix type; legacy path untouched, so no mass edits fire across other sites). The adapter now emits machine handles — `culprit_selector` (`div.footer-legal`) and `slot` (`footer`, derived via `closest('footer')`) — the judge puts them in the spec, and the new fixer **REFUSES to run without them** rather than guess a target. Selector is regex-validated before it reaches a `<style>` block (it crosses a browser→HTML boundary); the patch is idempotent per-selector; appended after the slot's own style so it wins on document order without `!important`.
  - **PROVED THE FIX ON THE LIVE PAGE BEFORE SHIPPING IT** — injected the exact CSS `buildOverflowCSS` generates into vonc.com in real Chromium: **`footer-legal 506px → 326px, overflow 58px → 0px, flex-wrap: wrap`**. The fix is known-good before a byte is written to the site.
  - **GATE: needs a new chassis image AND a new browser-runner-adapter image** (judge spec + fixer are chassis; culprit_selector/slot are adapter). Then: re-run 087 on the quiz → a fresh `chrome_overflow` item with `fix_type=chrome_overflow_fix, slot_name=footer, overflow_selector=div.footer-legal` → fixer patches the FOOTER → rerender deploys → re-run acceptance → `mobile-fit@mobile` goes green. That closes the real-failure loop, autonomously, on a bug the system found by itself.
  - Housekeeping: `fix_component_template_chrome_overflow_test.go` is mine and **untracked**; several other `??` files (`complete_work_item_verification.go`, `check_required_fields_missing.go`, `verifiers.go`, …) belong to other workstreams — same T11/T13 trap, `git add` before the next release commit.
- **T15 (2026-07-14):** **FIRST REAL FAILURE — and it taught two lessons.** Fired Tier-4 at the third (and only unverified) tool carrying PLAN criteria: **`tool-archetype-taster-quiz` on vonc.com** — a different site and a different render path. Verdict: **3 of 7 failed**, and the two failures are of *opposite kinds*:
  - `boots` ×2 (desktop+mobile) — **FALSE failure, stale criteria.** The PLAN asserts `.tool-container` (the gamesdesign *generator* convention); vonc's page-section path delivers `.tool-archetype-taster-quiz-section`, and `.tool-container` occurs **zero** times on the live page. Pure Option-B/143 class: the criteria describe a delivery mechanism this page never used.
  - `mobile-fit` — **GENUINE failure.** The page really overflows at 390px.
  - **PARKED the improve_tool item** (`acceptance_fail:tool-archetype-taster-quiz:…`, cancelled with reason, regenerable by re-running 087). It bundled the false criterion with the real bug: dispatching it would have sent tool-improver chasing a stale contract — the exact §2 trap — and it might have "fixed" the tool by injecting a bogus `.tool-container` div.
  - **DIAGNOSED the real one in real Chromium: it is NOT the tool's bug.** Culprit is **`div.footer-legal` (506px in a 390px viewport, 58px overflow)** — vonc **site chrome**, identical on the homepage, i.e. **site-wide on every vonc page**. (Live bug for the vonc/Spark workstream: mobile horizontal scroll across the site.)
  - **DESIGN DEFECT this exposes:** `no_horizontal_overflow` measures the **DOCUMENT**, but the ticket it raises is **TOOL-scoped**. One overflowing footer ⇒ a bogus, unfixable `improve_tool` ticket for **every tool on the site**, re-created on every run, sending the fixer to edit a quiz that cannot possibly fix a template footer. A page-level check must not silently become a tool-level accusation.
  - **FIX BUILT — attribute, then route (user's call).** The adapter now locates the tool's container, names the widest offender, and stamps every result with `scope` = `tool` | `chrome` | `unknown` (+ `culprit`, `component`). The judge routes on it: `chrome` → a **`responsive_fix` item for `component-template-fixer`** (the *existing, dispatched* route for responsive defects — no invented handler), `tool`/`unknown` → `improve_tool` as before. **`unknown` never routes to chrome**: an unlocatable container falls back to the tool rather than guessing. The chrome item's dedup key is `chrome_overflow:<component>:<profile>` — deliberately WITHOUT the tool, so one overflowing footer is ONE site ticket, not one per tool (`idx_swi_dedup` is UNIQUE on `(site_id, item_key)` for open items, so repeat reports collapse and a regression can still re-raise it). A tool whose own checks pass now records **PASSED** with the chrome defect named and routed, instead of an eternal false failure.
    - Container resolution: per-check → PLAN-level `container` → convention fallback `.tool-container, [class*="tool-"][class*="-section"]`, which covers BOTH delivery paths (generator + page-section) so PLANs written before attribution still attribute correctly. **Verified live on vonc:** `scope="chrome" component="site-footer" culprit="div.footer-legal (506px)"`.
  - **Migration 148 APPLIED — the quiz PLAN surrenders to delivered reality (143 precedent).** `boots` re-anchored to the real `.tool-archetype-taster-quiz-section`; the pre-144 `asset_loads` check removed; `container` added; and **`quiz-flow-EDIT` replaced with a REAL interaction** (click `.quiz-option-btn` ×3 → `.result-archetype-name`) — **probed passing in real Chromium before being written into the PLAN** (never author a criterion you have not watched pass). The quiz's actual behaviour is now covered instead of skipped. Chain intact (old retired, new current); the parked ticket carries its resolution.
  - **Chassis fixes this turn (both ride the NEXT chassis image):** (1) **composer fix** — results are now labelled `id@profile` (`Passed`/`Failed`/`SkipList`/`Details`), the note gains `across profiles: …`, the stale "skipped at this phase" wording is gone; `failing_checks` stays **bare deduped ids** (the fixer matches PLAN ids, as Tier 2 does) and the new `failing_instances` carries the profile detail. 6 unit tests, incl. regressions pinned to the live payloads. (2) **cooldown scoped to `source='tool-acceptance'`** — Tier-2 writes `tool-acceptance-tier2`, and letting its static fails suppress a browser run had it backwards (a static failure is when you MOST want the behavioural tier to look).
  - **PROVEN LIVE on v1.0.1116** (user built chassis + adapter mid-turn; both verified to carry the work by symbol table — `routeChromeFailures`/`chromeSummary` in the chassis, `toolContainer`/`overflowInfo` in the adapter). Run `f0019bd6` on the quiz, with 148 applied:
    - **Note:** `Tier-4 acceptance PASSED — tool-archetype-taster-quiz` · *"all 8 of the tool's own checks passed across profiles: desktop, mobile (1 skipped: mobile-fit@desktop)"* · **`Root cause: site chrome, not this tool: mobile-fit@mobile — div.footer-legal (479px) in site-footer`** · *"Fix: 1 responsive_fix item raised for the site template (handler component-template-fixer); the tool itself needs no change"* · checks listed `id@profile` throughout.
    - **`quiz-flow` (the interaction 148 filled in) EVALUATED AND PASSED on BOTH profiles** — the quiz's real behaviour is under test for the first time.
    - **Routing correct:** ONE `responsive_fix` item (`chrome_overflow:site-footer:mobile`, handler `component-template-fixer`, status detected) and **NO new improve_tool item**. The tool was not blamed for the footer.
    - Every piece of this turn is now live and mutually confirming: composer labels (`id@profile`, honest skips), the corrected PLAN, attribution, and routing.
  - **Migration `149_composer_emits_container` APPLIED — the defect is now fixed AT BIRTH.** compose_plan emits a top-level `container` (the tool's outermost selector, **copied from the generated HTML, never assumed**) and anchors `boots` on that same root instead of the hardcoded `.tool-container`. Future PLANs are attributable by construction; the "NEVER invent a selector" rule is untouched. (Snapshot taken; guards anchored on single-line substrings — `prompt_template` holds REAL newlines, so a `\n` in a SQL literal never matches: 144's lesson, re-learned.)
  - ⚠️ **Migration numbering collided:** another workstream landed its own `149_page_build_handler_noop_flags` + `150_enable_required_fields_missing_check` while this turn was in flight, so **there are two 149s**. Harmless (the ledger keys on `filename`, and all applied cleanly) but **the next migration must take 151**.
  - Routing confirmed reachable: `triage_detect_items` promotes ALL `detected` items for a site (no source/type filter) → `triaged` + `pipeline=build`, and the dispatch loop then hands them to `handler_agent`. So the `responsive_fix` item flows to `component-template-fixer` on vonc's next triage pass — no extra wiring. Its free-text `affected_component` ("site-footer") matches how existing responsive_fix items name components ("site-header / main-nav", "container / layout").
  - Housekeeping: `platform/orchestration/actions/diagnose_triage_action.go` is **untracked (`??`)** — not this workstream's, but it is exactly the T11/T13 trap; `git add` it before the next release commit.
  - NEXT: let the `responsive_fix` flow to `component-template-fixer` (queued at `detected`) → the footer gets fixed → re-run acceptance and watch `mobile-fit@mobile` go green. That closes the real-failure loop end-to-end, with the fix landing where it belongs. Then: P3 screenshots.
- **T14 (2026-07-14):** **P1 (mobile) + P2 (interactions) PROVEN LIVE — the ladder now catches economy-simulator-class bugs in production.** The T13 deploy gate was *already satisfied*: the user's **v1.0.1114** browser-runner-adapter build carries P1/P2 (images build from the LOCAL WORKING TREE, so the 13:54 source landed in the 20:08 image regardless of commit order). Verified against the POD, not git — `go tool nm` on the copied binary shows `runInteraction`, `splitByProfile`, `(*chromiumPage).HorizontalOverflow`, `criteriaStep/criteriaExpect`. No rebuild needed. Fired 087 on xp-curve (correlation `af5a4ac5`): **passed=9 failed=0 skipped=1** vs T8's P0 baseline of "3 evaluated, 2 skipped". Adapter log is per-check/per-profile explicit: `curve-switch` "interaction produced the expected result (#tableWrap tr)" on **desktop AND mobile** (the select fired → the page's own JS rebuilt the table → rows asserted), `mobile-fit` "no horizontal overflow on mobile", and `mobile-fit` correctly SKIPPED on desktop ("not run on profile desktop"). Both profiles ran from the 147 agent config. Clean pass, no improve_tool item.
  - **DURABLE TRAP banked (cost a chase):** *grepping a Go binary for a short string literal proves nothing.* Go compiles ≤16-byte constants used only in equality comparisons into integer immediates — `page_status_ok` (14B) and `selector_exists` (15B) grep as ABSENT from a binary that plainly implements them, while `no_horizontal_overflow` (22B) greps present. Verify compiled-in code via the **symbol table** (`go tool nm`; these images ship unstripped) — pods carry no `strings`/`nm`, so `kubectl cp` the binary out (sha256 both ends). This supersedes the old "grep -ac the pod binary" habit.
  - **DEFECT found (cosmetic, not functional) — the note lies by omission.** `judge_acceptance_results` composes the note's skip line per *check id*, not per *(check, profile)*: the note reads "all 9 evaluated checks passed (1 skipped at this phase: **mobile-fit**)" — but mobile-fit RAN AND PASSED on mobile; only its desktop instance was (correctly) skipped. The `Verified:` list also repeats bare ids with no profile label (`boots, status, curve-switch, console, boots, status, mobile-fit, curve-switch, console`). A human reading the note concludes mobile was never checked — the exact opposite of the truth, in the one artifact the whole ladder exists to produce. Fix: label results `id@profile` and count skips per (check, profile). Rides the next chassis image.
  - Also seen: the adapter logs `Failed to fetch message from Kafka: context deadline exceeded` at **ERROR** every 10s while idle — that's the normal empty-poll timeout, not a fault (messages arrive fine). It drowns the real log; downgrade to debug.
  - NEXT: (1) note-composer fix above; (2) let a REAL failure flow to tool-improver and back; (3) scope the acceptance cooldown to `source='tool-acceptance'` verdicts; (4) P3 screenshots.
- **T13 (2026-07-13):** Wrote `STATUS_2026-07-13_where_we_are.md` (state-of-play) + refreshed `OVERVIEW`. Built **P1 (mobile) + P2 (interactions)** in the browser-runner runner behind a testable `browserPage` interface (P0 behavior preserved). Added mobile profile (390×844 touch), `no_horizontal_overflow`, and `interaction` (fill/click/select → expect selector/text). 9 unit tests pass. **Proven LIVE on xp-curve**: the `curve-switch` interaction really selected 'exponential' → JS rebuilt the table → rows asserted, on desktop AND mobile; mobile no-overflow passed at 390px. Migration **147** sets the agent to request both profiles (safe now, activates on the P1/P2 image). **Repo-completeness fix**: the ENTIRE browser-runner adapter was never git-tracked (prod worked via Docker `COPY . .`) — committed complete as **`53a5b518`**. GATE: P1/P2 go live on a new browser-runner-adapter image (from `53a5b518`+); 147 is DB-only, applied. NEXT: deploy the adapter image → verify mobile+interaction criteria evaluate live; then a real failure through tool-improver; cooldown scoping.
- **T12 (2026-07-13):** **FULL AUTONOMY PROVEN.** v1.0.1112 carries `tool_acceptance_due` (confirmed in-binary via the sweep's `checks_run`). A design-discovery sweep on gamesdesign → emitted `acceptance_run:tool-drop-rate-tuner` → dispatch loop drove `tool-acceptance-agent` → browser-runner-adapter (Chromium, live page) → `acceptance-run` note "PASSED, 3/3", item `complete`. **Zero manual triggers from the sweep onward** (and the sweep is a scheduled tick in prod). First sweep correctly emitted nothing (both tools in 7-day cooldown from my testing); freed drop-rate by deleting a STALE Tier-2 fail note (07-10 16:25, fixed by 143 at 16:51). Follow-up: cooldown counts Tier-2 verdicts too — consider scoping to `source='tool-acceptance'`. Discovery→verdict is now unattended. NEXT: real-failure flow through tool-improver + back; P1 mobile / P2 interactions.
- **T11 (2026-07-13):** v1.0.1111 deployed — **partial: the untracked-file trap struck again.** Verified by lineage (v1.0.1111 == commit `f2fb87a`, msg "acceptance loop runs in scheduled tasks"): the cooldown fix (`check_tool_acceptance.go`, tracked+modified) IS live, but `check_tool_acceptance_due.go` was UNTRACKED so `git commit -a` skipped it — the continuous sweep is NOT in the binary (design-discovery warn-skips the unknown check; 142-precedent safe). Committed the file as **`83ba9bd4`**. **GATE: continuous acceptance needs ONE MORE image (from 83ba9bd4+).** Durable guard banked: `git status` for `??` before every release (2nd occurrence — T4 was the checker itself). What's live now: Tier-4 self-driving (manual 087) + the cancelled-item cooldown fix. NEXT after redeploy: a design-discovery sweep → `acceptance_run` item → dispatch → fresh `acceptance-run` note, fully unattended.
- **T10 (2026-07-12):** **Tier 4 goes CONTINUOUS.** New discovery check `tool_acceptance_due` (emits `acceptance_run` items for deployed tools with PLAN criteria; 7-day verdict cooldown; skips open runs; status `triaged` directly — detected items were observed sitting unswept; priority 90 so acceptance tests the NEW page after rerenders). Post-creation/post-improve hooks deliberately NOT used — they'd fire before the page redeploys. Dispatch mapping verified (`spec` → `input_data.spec.function` = the 145 contract). Correct-while-touching: `check_tool_acceptance` cooldown now excludes cancelled items (recorded 07-10 follow-up). Migration **146** applied (snapshot `b294bf7d`). **GATE: the check + cooldown fix ride the NEXT chassis image** (warn-skip until then). Proof after deploy: trigger a design-discovery sweep on gamesdesign → expect an `acceptance_run` item → dispatch loop → a fresh `acceptance-run` note with zero manual triggers. Uncommitted: the new check, the cooldown fix, 146.
- **T9 (2026-07-12):** Wrote **`OVERVIEW_self_verifying_tools.md`** (plain-language explainer for talking about the whole mechanism). Then **proved the FAIL path live** (controlled, reverted — Stage-1 smoke precedent): injected one genuinely-failing criterion into drop-rate's PLAN, fired 087 → verdict `failed=1, improve_tool_created=true`; both artifacts correct (acceptance-fail note + `improve_tool` item with criteria as `acceptance_test`, handler tool-improver). Cleaned up completely: ticket cancelled (was still `detected`), PLAN reverted exactly (3046 chars, 5 checks, no asset check), test note deleted, zero orphans. **The loop now closes on BOTH outcomes with no human.** OPEN: let a REAL (non-manufactured) failure flow to tool-improver and back; wire trigger points; P1 mobile / P2 interactions.
- **T8 (2026-07-12):** **TIER 4 SELF-DRIVING — proven.** v1.0.1108 rebuilt from HEAD carries the actions (pod start 16:11 > commit 12:37; tag-reuse made the commit-message ancestry misleading — settled by timing + the run). Fired 087 on tool-xp-curve-designer → COMPLETED at `complete`, first machine `acceptance-run` note: PASSED, 3/3 checks, 2 skipped (mobile-fit, curve-switch), no improve_tool item. Results identical to the T6 hand-produced smoke → orchestrator path equivalent, zero humans. **The verification ladder is whole (Tier 0/1/2/4).** OPEN: fail-path (acceptance-fail note + improve_tool item) is unit-tested, not yet live (needs a genuinely-failing tool); wire trigger points (post-recreation/post-improve/periodic sweep); P1 mobile / P2 interactions.
- **T7 (2026-07-12):** **tool-acceptance-agent BUILT; migration 145 APPLIED.** Two Go actions (`tool_acceptance_actions.go`): `request_browser_run` (analyser-style await; resolves the tool URL from `pages` itself; NO-OP skips when the PLAN has no criteria — never a fake run) and `judge_acceptance_results` (response-shape fallback chain, unit-tested ×3 shapes; pass → acceptance-run note; fail → acceptance-fail note + ONE improve_tool item with criteria as `acceptance_test`; no item for component-less recreated tools — logged). Registry entries added. 145 = the agent definition (linear workflow, error_steps in config), applied via the runner + pipeline note. Trigger `087_TRIGGER_tool_acceptance.sh` written (dry-run default, prints the ancestry check). **GATE: the two actions ride the NEXT chassis image — do not fire 087 until a release built from the commit containing `tool_acceptance_actions.go` is deployed** (unknown action fails the workflow). Uncommitted: the actions+tests, registry edit, 087. NEXT after deploy: fire 087 on tool-xp-curve-designer → expect the first machine acceptance-run note; then wire trigger points (post-recreation, post-improve, periodic) + P1 mobile.
- **T6 (2026-07-11):** **Stage 6 P0 LIVE — smoke PASSED.** Third image build green; adapter deployed (v1.0.1107). Two deploy findings: requests-topic CR wasn't applied (classic fetch-deadline signature — applied it) and ~10 eviction husks from the image pull (cleaned). §2.15 smoke against production: `run_checks` with the xp-curve PLAN's real criteria → response matched on `in_response_to_request_id`, `status=complete`, REAL bool body headers, results = manual inspection (200/boots/console; mobile+interaction honestly skipped). Scratch topic deleted. **The verification ladder has a working Tier 4.** NEXT: `tool-acceptance-agent` orchestrator (criteria → URLs → adapter call via the 035 envelope → judge → acceptance-run/fail notes + improve_tool items), then P1 mobile / P2 interactions.
- **T5 (2026-07-11):** Second image-build failure, now trivial: CDN fix held (all browsers downloaded to /pw-browsers) but `chmod /pw-cache` failed — **v0.6100.0's driver dir is `$HOME/.cache` on Linux and IGNORES `XDG_CACHE_HOME`** (read from module source; the earlier local "pw-cache worked" was Go's build cache honouring the same env var — two caches, one name). Dockerfile fixed: `ENV HOME=/pw-home` (persists across `USER appuser` → install and runtime resolve the same driver path), appuser owns /pw-home (Chromium crashpad scratch), browsers root-owned a+rX, comment moved out of the RUN continuation. **User: re-run the make build — third time should be green.**
- **T4 (2026-07-11):** User's image build 404'd on the Playwright driver — **playwright.azureedge.net CDN is decommissioned**; all playwright-go tags ≤v0.6000.0 hardcode it. Fixed by requiring **v0.6100.0 under its DECLARED module path `github.com/mxschmitt/playwright-go`** (the tag's go.mod mistakenly kept the pre-rename path; replace directives conflict — import the declared path, swap back when upstream fixes). v0.6100.0 installs the driver from npmjs.org + nodejs.org/dist. Verified end-to-end locally: the image's install command succeeds (Chrome Headless Shell 149), and a NEW env-gated integration test (`BROWSER_RUNNER_IT=1`) drove real Chromium against the live xp-curve page — **`#tableWrap tr` → 20 elements in the live DOM** (the JS-built assertion Tier 2 can't make), status/boots/console pass, negative control fails, 4.7s. **User: re-run `make build-browser-runner-adapter …` — the build should now pass.**
- **T3 (2026-07-10):** **Stage 6 adapter BUILT** (code prep; user deploys). Read the mould (035 §1 envelope, 001/002/003, analyser adapter, runner PLAN rev 2). Wrote `cmd/browser-runner-adapter/main.go` + `internal/adapters/browserrunner/{adapter.go,run_checks_action.go}` (playwright-go headless Chromium, desktop-only, 3 checks: page_status_ok / selector_exists [real live-DOM assertion] / no_console_errors; everything else honestly skipped; 5 unit tests pass). Packaging: dockerfile (debian+Chromium), kustomize base+overlay, KafkaTopic CR, config YAML, `playwright-go v0.5200.0` pinned (latest tag has broken upstream module path), **all 5 makefile insertion points** (user asked me to wire the makefile, not deploy). `go build ./...` + vet clean. **User deploys**: apply the KafkaTopic CR once, `make build-browser-runner-adapter push-backend deploy-agents IMAGE_TAG=vX` (~1.2GB Chromium image, slow), then §2.15 smoke. §5.2 below has the deploy+exit-test detail. NEXT: after the smoke proves the contract → `tool-acceptance-agent` orchestrator.
- **T2 (2026-07-10):** User chose **Option B**. Executed end-to-end: migrations 143 (PLAN supersedes + selector fixes + item cancellations) and 144 (composer five→four checks, inline delivery) applied via the runner and verified (fences clean, anchors probed green on live pages, chains intact). §2/§3 of this doc collapsed into the resolved §2. Docs updated (NOTES 39 entry, RUNBOOK Stage-5 section, PLAN(6) new principle bullet). Migrations ledger now through **144**. NEXT FRONT: Stage 6 Tier-4 runner P0 (starting).
- **T1 (2026-07-10):** Handoff created. State: Stage 5 LIVE + first-sweep proven; docs (RUNBOOK 38 / NOTES 39 / PLAN 6) current; delivery-mechanism decision explained to user, awaiting their call.
