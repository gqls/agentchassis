We're building an agent-orchestration platform (Go "chassis", agent workflows stored as JSON in Postgres agent_definitions.default_config->'workflow', Kafka messaging, k8s namespaces ai-persona-system and kafka) whose purpose is to intelligently plan and build multipage websites from a domain name. The current feature is travelling docs: every tool and pipeline carries a PLAN (its intent, including a fenced criteria JSON block of acceptance checks) and an append-only NOTES stream (its history), both in Postgres and both written by the agents themselves rather than by hand. Where we are: the diagnosis loop already writes NOTES; Task 3 is applied and proven — tool-generator now writes a PLAN at every tool creation (it created tool-xp-curve-designer on gamesdesign.co.uk and documented it unaided); Task 4 is applied but unproven — component-template-fixer, tool-improver and tool-recreation-handler each append a NOTES entry after a successful fix, but no machine-written fix note exists yet. The immediate goal is to produce that first one by recreating the genuinely broken economy-simulator game at gamesdesign.co.uk through tool-recreation-handler (spec.mode = "recreate"), fixing two real bugs — the Player-Influx slider's raw index is used as the players-per-tick rate, and the Players series is bound to the gold axis so it renders flat — which then become that tool's first acceptance criteria. Two tool-recreation-handler fixes must land first (declare spec in its input contract; re-subject its note to pipeline/build, since it writes page sections rather than a component). A parallel track cleans up the chassis: an OOMKilled shared pod, full-params Info logging, and a missing embedding deadline that forced index_plan to be bypassed.

