We're building an agent-orchestration platform (Go "chassis", agent workflows stored as JSON in Postgres agent_definitions.default_config->'workflow', Kafka messaging, k8s namespaces ai-persona-system and kafka) whose purpose is to intelligently plan and build multipage websites from a domain name. The current feature is travelling docs: every tool and pipeline carries a PLAN (its intent, including a fenced criteria JSON block of acceptance checks) and an append-only NOTES stream (its history), both in Postgres and both written by the agents themselves rather than by hand. Where we are: the diagnosis loop already writes NOTES; Task 3 is applied and proven — tool-generator now writes a PLAN at every tool creation (it created tool-xp-curve-designer on gamesdesign.co.uk and documented it unaided); Task 4 is applied but unproven — component-template-fixer, tool-improver and tool-recreation-handler each append a NOTES entry after a successful fix, but no machine-written fix note exists yet. The immediate goal is to produce that first one by recreating the genuinely broken economy-simulator game at gamesdesign.co.uk through tool-recreation-handler (spec.mode = "recreate"), fixing two real bugs — the Player-Influx slider's raw index is used as the players-per-tick rate, and the Players series is bound to the gold axis so it renders flat — which then become that tool's first acceptance criteria. Two tool-recreation-handler fixes must land first (declare spec in its input contract; re-subject its note to pipeline/build, since it writes page sections rather than a component). A parallel track cleans up the chassis: an OOMKilled shared pod, full-params Info logging, and a missing embedding deadline that forced index_plan to be bypassed.

--

another one:
We're building an agent-orchestration platform (Go "chassis", agent workflows as JSON in Postgres agent_definitions, Kafka messaging, k8s namespaces ai-persona-system/kafka) that autonomously builds and maintains
websites, and the current workstream is travelling docs + the tool verification ladder: every interactive tool carries a machine-written PLAN (intent + acceptance criteria in a fenced JSON block, doc_plans) and
append-only NOTES (doc_notes), and a tiered verification ladder checks tools against their own criteria — Tier 2 static (tool_acceptance discovery check, anchor rule) and Tier 4 behavioural (browser-runner-adapter
driving headless Chromium via tool-acceptance-agent, migrations 145–147). Where we are: everything through full autonomy is proven live — PLANs at birth, NOTES at every fix, both Tier-4 verdict paths (pass →
acceptance-run note; fail → acceptance-fail note + improve_tool ticket), and the continuous loop (design-discovery sweep → acceptance_run item → dispatch → browser run → verdict, zero manual triggers, proven
2026-07-13 on gamesdesign.co.uk). P1 (mobile profile) and P2 (interaction checks — fill/click/select then assert the result, the tier that catches economy-simulator-class bugs) are built, unit-tested, and proven live
locally (xp-curve's select→#tableWrap tr interaction on desktop and mobile), committed as 53a5b518 after discovering the whole adapter package had been untracked (Docker COPY . . masked it). The immediate task:
deploy a new browser-runner-adapter image built from 53a5b518+ (make build-browser-runner-adapter push-backend deploy-agents IMAGE_TAG=vX), then verify mobile+interaction criteria evaluate live in an acceptance run
(migration 147 already requests both profiles). After that: let a real (non-manufactured) failure flow through tool-improver and back; scope the acceptance cooldown to Tier-4 verdicts (source='tool-acceptance'); P3
screenshots. Read first: docs/agent_docs/docs024_key_docs_latest/travelling_docs/HANDOFF_2026-07-10_stage5_live_and_next_fronts.md (turn log at the bottom, currently T13) — the every-turn-updated handoff — plus
STATUS_2026-07-13_where_we_are.md for the snapshot and RUNBOOK_travelling_docs(38).md §0 for the position line; migrations run via ./scripts/migration/run-migrations.sh (ledger through 147); trial site is
gamesdesign.co.uk (site_id e33263f4-74f8-494f-b191-546845dbbddf).
