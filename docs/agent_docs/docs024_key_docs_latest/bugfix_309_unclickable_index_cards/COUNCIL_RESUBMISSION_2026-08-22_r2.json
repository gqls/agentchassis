{
  "rationale": "bugs_open/309's LAST open half. CLC-018 shipped the BIRTH gate (sourceVocabularyIssues, live v1.0.1314) which refuses a generated component whose input_schema declares a source the platform cannot resolve. It is holding: MEASURED 2026-08-22, zero offending components created or updated since it went live. But nothing has ever asked the same question of the components ALREADY in the database, and the birth gate structurally never can — a component is routinely inserted or altered by a hand-written migration or hand SQL, which never passes through store_generated_component_action (a standing LANDMINE entry, and exactly how the motivating component blog-listing_pre_037 got there). MEASURED at the live DB today: 69 fields across 17 active components declare a source that resolves nowhere (51 phantom site_specs aspects, 14 unregistered query fields, 4 outside the vocabulary); SIX of those components are live on 46 page instances. The one runtime detector of this exact silence, STRUCTURAL_KEY_CARRY_MISS, is 28 rows all-history, last written 2026-08-17, and is one of the codes bugs_open/358 measures as having no automated consumer and a 30-day retention delete — and it only ever fires when a page is BUILT, so the eleven dormant components are invisible to it for ever. The concept register's own CLC-018 entry names this audit as the unbuilt half and prescribes its shape in as many words: 'build it ON sourceVocabularyIssues, not on a second predicate, or they drift'. This change does exactly that: a new --component-source-vocabulary mode in cmd/config-key-audit calling the birth gate's own function, on the estate's Go-native daily-check-image pattern (ten such images now, most recently capped-schedule-ordering-check). A Python mirror pinned by a parity test was considered and rejected: it IS a second predicate, and can only detect drift where calling the function makes drift unrepresentable. The 69 pre-existing findings cannot be zeroed in one commit — repairing ONE of them (migration 478) took an owner ruling and a shrink-guard incident — so they are grandfathered by a FROZEN baseline that can only shrink, keyed on the exact (component_id, field, source, class) tuple so it can never become an all-clear for a component, and dated so it cannot be appended to. The baseline governs the EXIT CODE and never the REPORT: the daily doc_notes row names all 69 every morning.",
  "plan": {
    "summary": "ROUND 2. Round 1 was REVISE, gated by editquality on edit 7, and the gating objection was RIGHT ABOUT THE SUBMISSION: squeezing to the 8-edit limit left one entry describing makefile and docs content under a cronjob.yaml filename. Four objections across two seats (editquality 0/1/3, debug_historian 0) followed from that one incoherence and named as MISSING three files that were shipped in the same commit - base/kustomization.yaml, the production overlay, and the RELEASE_IMAGES entries. This round describes what actually exists. The objections that were owed real work have had it: a deploy-verification recipe (debug_historian), the doc_notes subject_type constraint verified against the live CHECK (guardian), the call sites enumerated rather than asserted (guardian, and its guessed caller disproved), the baseline-vs-acks reuse question answered on lifecycle (reuse_agent), the 083 detector-with-no-consumer shape answered on the failed Job being the alarm (bug_historian), and the mutation proofs now RUN rather than promised (prior_art_librarian). THE CHANGE ITSELF: bugs_open/309's last half. The birth gate refuses a phantom data source at component BIRTH and is holding, but a component is routinely inserted or altered by a hand-written migration that never passes through the Go action - 69 fields across 17 active components are already offending, six of them live on 46 page instances. This adds a daily at-rest audit that CALLS the birth gate's own function, with a frozen shrink-only baseline that grandfathers the 69 without ever silencing a 70th. Committed 747e717a1 / effd08fff / 62f187442 under Council-Submitted: a092d7d8.",
    "edits": [
      {
        "file": "platform/orchestration/actions/component_source_guard.go",
        "symbol": "SourceVocabularyFindings",
        "operation": "modify",
        "rationale": "Export the rule so the audit CALLS it (CLC-018's own instruction) and return it as DATA so the audit need not parse a class out of a human-readable message. SourceVocabularyIssues is kept as a thin projection, so the birth gate's behaviour is unchanged. ROUND 1, guardian: the call sites are now ENUMERATED rather than asserted - `grep -rn` over all non-test .go gives exactly ONE production caller, store_generated_component_action.go :413 (LoadKnownSpecAspects) and :430 (SourceVocabularyIssues), plus my new one. create_tool_component_action.go, which the seat named as a possible caller, contains ZERO references - checked, not assumed. The gate's pre-existing tests pass untouched, which is the behaviour evidence.",
        "sketch": "+const (SourceIssuePrefixOutsideVocabulary|UnregisteredQuery|PhantomAspect)\n+type SourceIssue struct{ Field, Source, Class, Message string }\n func SourceVocabularyIssues(...) []string { // projects .Message over Findings }\n+func SourceVocabularyFindings(...) []SourceIssue\n // loadKnownSpecAspects -> LoadKnownSpecAspects (same-package rename, 2 call sites in the same commit)"
      },
      {
        "file": "cmd/config-key-audit/componentsourcevocabulary.go",
        "symbol": "emitComponentSourceVocabulary",
        "operation": "add",
        "rationale": "The audit. Reuses dbConn()/writeDocNote() from fleetdb.go and emitCommitShaExposure's exit-code discipline. ROUND 1, bug_historian (the 083 shape): the doc_notes row is the DETAIL, not the alarm - a red exits 1, which fails the Job, and a failed CronJob is a surface this estate does read. No work-item production is deliberate: detection->dispatch is the known-broken half (083 itself), so filing into it would be the very shape the objection warns about. ROUND 1, guardian: doc_notes.subject_type is CHECK-constrained to 8 values and writeDocNote sends 'pipeline', which is in the set - MEASURED, 1,895 existing rows.",
        "sketch": "[--baseline <file>] [--report] [--json]; exit 0 clean / 1 red / 2 could-not-run\nTWO vacuity refusals failing in OPPOSITE directions, both exit 2:\n  0 components -> the silence (0 findings reads clean)\n  0 aspects    -> the FLOOD (every site_specs.* source reads phantom)\nfindings := actions.SourceVocabularyFindings(...)  // the birth gate's own rule"
      },
      {
        "file": "docs/agent_docs/docs024_key_docs_latest/bugfix_309_unclickable_index_cards/component_source_baseline.json",
        "symbol": "component_source_baseline",
        "operation": "add",
        "rationale": "The frozen grandfather baseline, GENERATED from the census query, 69 entries. ROUND 1, reuse_agent (why not reuse optional_key_budget_acks.json's mechanism): checked, and the divergence is in the LIFECYCLE, not the format. The two existing ack files are ACKNOWLEDGEMENT registers - keyed by one subject with a scalar level, and they GROW as a human reviews and acknowledges each new subject. This is a frozen CENSUS - machine-generated, keyed by a composite per-finding tuple, and it may only ever SHRINK. Folding them into one abstraction would force one lifecycle on both and make this file appendable, which is the single property it exists to deny. What IS reused: the `_doc` key convention, the date/route fields, and the refuse-on-malformed loader shape from loadCommitShaExposureAcks.",
        "sketch": "{\"_doc\": \"...may only ever SHRINK...\", \"entries\":[{component_id, component, field, source, class, live_instances_at_baseline, baselined:\"2026-08-22\", route}] }"
      },
      {
        "file": "cmd/config-key-audit/componentsourcevocabulary_test.go",
        "symbol": "TestBaselineKeyIsNarrow",
        "operation": "add",
        "rationale": "ROUND 1, prior_art_librarian: the mutation-proof claim was made about tests not yet written, which was a fair objection to a pre-implementation submission. The tests now EXIST and the proofs have RUN: six mutations, each applied to the working tree, each KILLING its test, file restored byte-identical after - baselineKey ignoring field+source, the closure-date refusal removed, dormant-wake disabled, the stale pawl returning nothing, the audit diverging from the birth gate, and a route pointed at a non-existent file.",
        "sketch": "TestAuditRunsTheBirthGatesOwnRule / TestBaselineKeyIsNarrow / TestChangedSourceIsANewFinding /\nTestDormantWakingIsRed / TestRepairedEntryIsStale / TestBaselineIsClosed /\nTestRepoBaselineMatchesItsRecordedCensus (may shrink, never grow; every route must RESOLVE)"
      },
      {
        "file": "cmd/config-key-audit/main.go",
        "symbol": "main",
        "operation": "modify",
        "rationale": "Dispatch the mode. ROUND 1, guardian asked for a mechanism rather than an intention on this collision surface, and prior_art_librarian asked for the commit shas to be verified rather than taken on my word. Both done: 77865a223 committed this file from the working tree while my arm was in it and its symbol was untracked, 8664a7f96 removed the arm to restore HEAD - checkable in git log. The mechanism is that the arm and the file supplying its symbol are in ONE commit (747e717a1), and that HEAD was then verified by extracting `git archive HEAD` into a scratch tree and running `go build ./...` plus the affected packages' tests THERE, not in the working tree. That is the only check that catches this class.",
        "sketch": "+if len(os.Args) > 1 && os.Args[1] == \"--component-source-vocabulary\" { emitComponentSourceVocabulary(os.Args[2:]); return }\n// the blank actions import STAYS blank - Go scopes imports per file"
      },
      {
        "file": "build/docker/backend/component-source-vocabulary-check.dockerfile",
        "symbol": "component-source-vocabulary-check image",
        "operation": "add",
        "rationale": "The check image, cloned from capped-schedule-ordering-check.dockerfile. A PRE-BUILT image is what answers the owner's 2026-08-14 objection to Go-native checks, which was to COMPILING INSIDE THE JOB.",
        "sketch": "FROM golang:1.24-alpine AS builder ... go build -o config-key-audit ./cmd/config-key-audit\nFROM alpine:latest ... CMD [\"./config-key-audit\", \"--component-source-vocabulary\", \"--report\"]"
      },
      {
        "file": "deployments/kustomize/services/component-source-vocabulary-check/base/cronjob.yaml",
        "symbol": "component-source-vocabulary-check CronJob",
        "operation": "add",
        "rationale": "ROUND 1 GATING OBJECTION (editquality) WAS RIGHT ABOUT THE SUBMISSION AND WRONG ABOUT THE WORK, and that is my fault, not the seat's: squeezing to 8 edits left this entry's rationale describing makefile targets and docs under a cronjob.yaml filename. This entry now describes the actual manifest. The k8s YAML is real and complete - schedule, image, env, secret, resources, imagePullSecrets - and the sibling files editquality and debug_historian both correctly said were MISSING (base/kustomization.yaml and overlays/production/uk_001/kustomization.yaml) were shipped in the same commit; they were absent from the SUBMISSION, not from the change. `kubectl kustomize` builds the overlay to a CronJob at v1.0.1324.",
        "sketch": "schedule: \"20 7 * * *\"  # free in the REPO, not merely in the cluster: capped-schedule-ordering-check\n                        # is COMMITTED at \"5 7 * * *\" and undeployed, so kubectl shows that slot free\nimagePullSecrets: [docker-hub-creds]   # without it: ImagePullBackOff, which this fleet reports as RUNNING\nimage: docker.io/aqls/component-source-vocabulary-check:latest   # overlay pins the tag\nenv: PG_CLIENTS_HOST=postgres-clients + CLIENTS_DB_PASSWORD (no pods/exec RBAC here)\n+ base/kustomization.yaml and overlays/production/uk_001/kustomization.yaml (pinned v1.0.1324)"
      },
      {
        "file": "makefile",
        "symbol": "build-component-source-vocabulary-check",
        "operation": "modify",
        "rationale": "build/push/deploy targets AND the fleet lists. ROUND 1, editquality objected that the image is not added to RELEASE_IMAGES - it IS, and so is AGENT_DEPLOY_SERVICES; both were in the change and the submission buried it in a sketch comment. ROUND 1, debug_historian asked for a deploy-verification step, which was genuinely OWED and is now written: the RUNBOOK carries the ordered recipe - image before manifest, then the cronjob's image by jsonpath, then a binary probe with a MUST-BE-ABSENT control alongside the must-be-present one, then a manual Job read at the POD's terminated exitCode, then the doc_notes row as the positive control on the report path (writeDocNote is best-effort, so a silently refused insert otherwise looks like a healthy quiet run).",
        "sketch": "build-/push-/deploy-component-source-vocabulary-check targets\n+ added to RELEASE_IMAGES (line ~100) and AGENT_DEPLOY_SERVICES (line ~128)"
      }
    ],
    "grounded_in": [
      "component-lifecycle.md CLC-018: 'sourceVocabularyIssues (pure, reusable - a future daily audit of EXISTING config should call IT)' and 'bugs_open/309 sec5 candidate 2 (a daily audit of EXISTING config is the unbuilt other half - build it ON sourceVocabularyIssues, not on a second predicate, or they drift)'",
      "bugs_open/309 sec8: 'the guard is its birth-gate half; sourceVocabularyIssues is pure and deliberately callable from a CronJob check - build the audit on it rather than a second predicate, or the two will drift the way sec5.1's stale comment did'",
      "MEASURED 2026-08-22 live DB: 69 offending fields / 17 active components (phantom_aspect 51, unregistered_query 14, prefix_outside_vocabulary 4); 6 components live on 46 page instances; ZERO offending components created or updated since the birth gate went live 2026-08-19",
      "MEASURED 2026-08-22: agent_error_log STRUCTURAL_KEY_CARRY_MISS = 28 rows all-history, first 2026-08-11, last 2026-08-17; SOURCE_GUARD_ASPECT_SET_UNAVAILABLE = 0 rows all-history",
      "plan_sections_action.go:623 resolve(): a source with no dot returns (nil,false); resolveSpecAlias step 2 is 'if aspect != \"identity\" { return nil, false }', so a phantom aspect cannot be rescued by the alias - the 69 genuinely resolve nowhere",
      "build/docker/backend/ holds TEN *-check.dockerfile Go check images against THREE Python checks; the most recent check service added (capped-schedule-ordering-check, c81b73b9e) is Go-native with CMD [\"./config-key-audit\", \"--capped-schedule-ordering\", \"--report\"]",
      "information_schema: 56 tables carry an input_schema column; all but content_components and component_versions are dated backup snapshots - hence the explicit scope in activeComponentsQuery",
      "ROUND 1 verification, guardian obj 3: doc_notes_subject_type_check CHECK ((subject_type = ANY (ARRAY['tool','pipeline','experience','action','experience-pattern','landmine','component','decision']))) - writeDocNote sends 'pipeline', 1,895 existing rows",
      "ROUND 1 verification, guardian obj 1: grep over all non-test .go gives ONE production caller of the renamed symbols (store_generated_component_action.go:413,:430); create_tool_component_action.go contains ZERO references",
      "ROUND 1 verification: `kubectl kustomize deployments/kustomize/services/component-source-vocabulary-check/overlays/production/uk_001` builds a CronJob, schedule 20 7 * * *, image docker.io/aqls/component-source-vocabulary-check:v1.0.1324 - the overlay editquality and debug_historian reported missing"
    ],
    "risks": "(1) THE BASELINE IS THE RISK SURFACE: an allow-list that silences the detector it belongs to is a recorded failure on this estate (scripts/pattern-check.py's COMPONENT_WRITE_ALLOWED note). Three structural defences, all mutation-proven rather than asserted: the key is the exact (component_id, field, source, class) tuple, so a NEW bad field on an already-baselined component with 32 live instances is RED (mutation M1: drop `field` from baselineKey -> TestBaselineKeyIsNarrow fails); the loader REFUSES any entry not dated 2026-08-22, so the file cannot be appended to (M2: remove the date check -> TestBaselineIsClosed fails); and the baseline governs the EXIT CODE only — the daily doc_notes body names all 69 every run, so it cannot go quiet. Check these. (2) FAIL-OPEN ASYMMETRY, deliberate and worth a reviewer's eye: the birth gate SKIPS the aspect half when site_specs is unreadable (it must not block all generation); this audit exits 2 instead. An audit has nothing to block, and a report that silently skipped a third of its rule is the blind-pass landmine one rung higher — the trap this very lane shipped and had to correct on 2026-08-21 (85c70c24a). Do not 'make the callers consistent'. (3) VACUITY: two refusals that fail in OPPOSITE directions, both exit 2 — zero components is the silence (0 findings reads clean), zero aspects is the FLOOD (every site_specs.* source reads phantom). A reviewer should check that neither can return 0/1. (4) The exported rename touches the live birth gate; its behaviour must be unchanged. SourceVocabularyIssues is now a thin projection over SourceVocabularyFindings and the gate's pre-existing tests pass untouched, which is the evidence. (5) NOT DONE, deliberately: the 69 repairs (each a per-component judgement — repairing ONE took an owner ruling and a shrink-guard incident), and no work-item production, because detection->dispatch is the estate's known-broken half (bugs_open/083, and bugs_open/358 for this exact channel)."
  }
}