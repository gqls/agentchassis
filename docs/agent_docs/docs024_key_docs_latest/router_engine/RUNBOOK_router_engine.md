# RUNBOOK — router engine (commands, gotchas attached)

Nothing engine-specific yet. Reuse from `bugfix_277_required_fields_repair/RUNBOOK_…`:
- proving a seed's exact embedded SQL against live rows (extract from the file, never retype);
- the census pattern (read-only classification of a whole population before any assignment);
- the canary→fleet assignment discipline (pre-image, guarded DO block, ROW_COUNT assert);
- reading close-evidence from `orchestration_states` (the loop overwrites `result` on completed rows).
