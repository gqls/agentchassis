# Where we are — framework prompts, positive voice (owner's plain-prose log; append-only)

## 2026-09-03, afternoon

This thread exists because you asked, on 2 September, for prompts that say what we want in the
language we want back, instead of lists of things not to do. The finetuning thread wrote it up as
a handoff this morning, and you said you wanted to revisit the choice already made (candidate C)
and, when I asked, the house voice, the model, and the style reference too.

Two things had moved by the time I read the handoff. The 641 migration is already written with
candidate C, has been through the council (approved this morning), and has been rehearsed against
the live database twice. The only thing left was your read of the exact words. And you have since
proposed a different shape for those words, relayed from the finetuning thread: a sentence like
"If you'd like to prepare in advance of your hour, you might want to get these things ready".

The straight answer to your question: yes, that is possible today, but only as data. The template
can only drop strings into slots; it cannot write a sentence. Right now there is exactly one authored
string per section, the subject. So the sentence has to BE the subject. You chose that route: the
block prints the subject as written, then lists what the rest of the page covers. The finetuning
thread's three pages already have subjects in that form ("what to have ready before the hour").
For sites where the planner writes the subjects, the planner needs a small nudge to write them the
same way; that is a separate small change.

What happens next, in order: I redraft the block in that shape, test it against real page data,
and bring you two or three versions blind with the current one as a control. Then the same treatment
for the house voice text, with a measured before/after and a model comparison run on the new text
so the cost question has real numbers. The style reference gets a v4 written from the same table
of rules, as the human-readable record.
