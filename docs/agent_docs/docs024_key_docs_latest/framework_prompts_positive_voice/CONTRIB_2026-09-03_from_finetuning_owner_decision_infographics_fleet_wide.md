# CONTRIB 2026-09-03, from the finetuning.uk lane: OWNER DECISION — infographics are to be fleet-wide and framework-driven, which is the build-site-planner prompt change your lane is placed to make

**Owner, 2026-09-03 23:45 BST, verbatim:** *"Infographics should be fleetwide and framework driven not
narrow for now."* Context, same evening: *"including infographics wherever they will help the
understanding of the concepts"*.

**What that decides.** The editorial_design_uplift lane established today (their CONTRIB in the
finetuning dir, §2, measured 2026-09-02 at the live prompt `f263eaa1`, build-site-planner, 34,781 bytes)
that the planner's imagery vocabulary already permits `kind='infographic'` and that the prompt TELLS the
planner to produce almost none — verbatim *"Use sparingly in v1 — most plans will have zero
section-scope entries."* — with `infographic` appearing three times in rule text and never in the worked
example. Fleet census, all history: hero 399 · icon 211 · logo 50 · illustration 25 · **infographic 1**.
So the owner's decision is a PROMPT CHANGE to build-site-planner: infographics where they help a reader
understand a concept, not "sparingly"; and the uplift lane's caution rides with it — rule 16's *"each
entry produces exactly ONE image"* discipline must be in the same edit, or under-decomposition yields
unusable multi-panel images. The uplift lane deliberately did not make this edit because it reaches every
new site and 18 site remakes are queued behind it; the owner has now made that call.

**Why your lane.** You own prompt migrations to the framework's writers and planners (641 this week; 640
was the last edit to this planner's rules, by apis.uk, whose due-diligence note on 640's example strings
still applies). RFC_016 §5.2: the owner reads the exact bytes before they apply.

**Constraints the drawn artefacts must meet, from the uplift lane (VIZ-007/009/011), which the prompt
should state so the planner does not brief the impossible:** no arithmetic in the render funcmap
(pass values to CSS custom properties); words and figures as HTML text, never inside `<svg>` (invisible
to the claims gate); non-text contrast on chart furniture. And every figure resolves through a registered
fact — the uplift lane's example: finetuning.uk's £99 (exact) against ~$5,000 (`ft-market-anchor`,
approximate) must be drawn banded on the approximate side.

**What finetuning.uk needs from it first:** the homepage's `features` section (a concept diagram: your
documents → the trained part → a model you host; no quantities) and `differentiators` (the comparison
above, WITH the orange-left-border device, not replacing it). The uplift lane scoped those two and stays
off the case-studies slot. Tell the finetuning lane when the prompt lands so its next homepage rebuild
picks them up; the copy on that page is owner-approved and must not be regenerated by the imagery pass.
