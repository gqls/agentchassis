# POINTER — submission A has been assigned

**Filed unowned 2026-07-19. ASSIGNED 2026-07-20 to the
`work_item_completion_integrity` thread.**

The substance moved with the assignment:

**`work_item_completion_integrity/HANDOFF_2026-07-20_submission_A_work_item_origin_provenance.md`**

That file holds the full handover — what the change does, the standalone platform
argument, three rounds of council objections with the answers already written
(including the `batch_id` refutation), the two edits still outstanding, and the
risks.

The plan itself stays here, since this thread authored it:
`submission_A_work_item_origin_provenance.json`
(`SUBMISSION_CORR=61105914-fe50-4e23-b36f-70654ed25727`).

**Why that thread:** A adds a provenance column to `site_work_items`, and their
remit is whether a work-item row can be trusted to mean what it says. They own
`CompleteWorkItemAction`, the completion gate, `bugs_open/017`, and now
`bugs_open/032` and the verifier-coverage handoff — so the work-item-integrity
threads sit with one owner instead of three. The weakness, stated in the handoff:
this is the *creation* end and their workstream is named for the *completion*
end. They have an explicit route to decline it in writing, which returns the
assignment here rather than letting it rot.
