# SUMMARY — the experiment is closed, the lane is handed off

**2026-08-02, end of day.** Third summary today, each marking a different thing: the
morning's recorded an error, the evening's recorded a proof, this one closes the
session — the experiment phase is done, the decisions are executed, and the lane
continues in a fresh chat from a written handoff.

## What we are trying to do

Unchanged: ~150 finance and insurance domains as substantial, deliberately different
sites — built by the pipeline, differentiated by configuration the register controls.

## Where we have come from

One day, one arc. The morning summary named the error: the pipeline should be building
everything, and it had built nothing — loancash was hand-built and a milestone document
had credited the machine. By evening the pipeline had built lendzy.co.uk end to end
from the same brief, its tools carried the right regulatory arithmetic through the
platform's own tool pipeline, and the register's positioning was proven to steer
written copy — a marker sentence planted only in the seeded specification came out
woven into regenerated page copy.

## What we have done since

The owner's remaining ruling is executed: **no blog on this site.** Removed at all
three levels in one transaction — the two plan rows deleted, the two never-built pages
archived, the two work items rejected with the ruling recorded — so the reconciler
cannot resurrect it. The finding underneath survives on the seam list: the planner
imposes blog templates the page builder can never build, a filed platform bug class.
lendzy now has zero open work items. The chassis rolled again (v1.0.1231 — the third
binary the experiment touched in a day, all recorded), the marker-bearing about page
is rerendered and shipped, and one cleanup is flagged loudly rather than done
silently: the acceptance-marker instruction is still live in lendzy's seeded
specification and must be stripped before that site is ever more than a shadow.

Because this chat is at capacity, the lane's continuation is written down properly:
`HANDOFF_2026-08-02_continue_here.md` — the three proven facts a new session must not
re-derive, every live identifier, the evidenced seam backlog in priority order, the
command shapes that took effort to get right, and the day's shell traps. The memory
index now points cold starts at it.

## Where we are now

The experiment phase is closed, and it answered the question it was built to answer.
The pipeline can build a positioned, factually disciplined site whose navigation,
title, topics and numbers all trace to inputs we control — and the distance between
its output and the hand-built benchmark is no longer a mood but seven specific,
evidenced seams: the every-page invariant carrier (the largest gap), canonicals,
favicon and meta descriptions, the tool handler's missing rerender, links to unbuilt
pages, the imposed standard shape, and in-browser tool fixtures. lendzy stands as the
evidence artefact: eighteen pages, publicly unreachable by design.

## Where we are going

A fresh chat, starting from the handoff. The work is the seam backlog through the
council gate, one coherent task at a time, starting with the chrome-level carrier for
every-page compliance lines. On the owner's queue: the build order across the 43
propositions — now unblocked, because the machine is demonstrated — plus the two
residual insurance twins, the fleet-wide Always-Use-HTTPS and www decisions, and the
citation pass against live FCA text that both loancash's guides and lendzy's tools
still owe before any regeneration.
