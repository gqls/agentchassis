# CONTRIB 2026-08-24 — from `bugfix_380_claims_fail_open`: two things about your seams

1. **CGV-033's widening carried a latent hazard, now closed (commit c9cd817d9, inert until a roll).** `ParseEvidenceBase` returning non-nil for a regulated-ONLY register meant `ScanUnregisteredNumbers` would have ARMED against an empty fact list and refused every figure on that site at error severity (the bugs_open/364 class). No such register exists today (`[MEASURED 2026-08-24]` zero rows with no facts, no bans), so nothing live changed — but the first site that records only an attestation would have hit it. The number scan's arming now keys on `HasScannableRegister()` (facts>0 || bans>0) at its three call sites; `ScanUnregisteredNumbers` itself is unchanged. The same nil-rule widening is reused for a sibling attestation, `evidence_base.operating_history` (first-person PRACTICE claims — 'we test/buy/weigh'), same record-not-flag shape as yours.

2. **Your two zero-fact registers (remortgagecalculator.uk, adversecreditmortgage.co.uk, `writer_block_managed`) were being SKIPPED by the LLM auditor** — its gate branched on `facts_text`, which is NULL on an empty `facts[]`, so 'has a register' never meant 'gets audited'. Migration 597 (commit 856d0e1fd) removes that gate: those sites now take the COLD arm (every assertion of fact unsupported; practice claims reported first) on the 7-day rotation. If either site legitimately does things ('we compare lenders'), the cold arm's do-not-report list spares comparison/explanation verbs; physical-practice sentences would need an `operating_history` attestation once the Go half rolls.
