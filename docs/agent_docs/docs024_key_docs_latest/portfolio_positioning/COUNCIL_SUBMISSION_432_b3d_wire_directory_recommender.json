{
  "rationale": "Phase B3d (portfolio_positioning fleet buildout): evaluate_directory_features - the deterministic, no-LLM directory-kind recommender that has been in the binary since v1.0.1301 and dispatched by NOTHING (agent_definitions references measured 0 on 2026-08-15) - gets its two consumers. (1) improvement-loop: a new enrich_directory_features step between enrich_news_feed and load_audit_state, so every audited site's classification spec gains/refreshes its per-kind directory recommendation each cycle; mirrors the news enrichment step's exact shape including config.error_step continue-on-failure (an enrichment failure must never kill the audit loop - the property bug 291's re-point established, preserved through the new step on both the success and error paths). (2) domain-research-classifier: the same step immediately after write_classification_spec, so a GREENFIELD build carries the flag at plan time - without this a new finance site would only learn it wants a directory on its first improvement-loop cycle, after the site was already planned. The action is a proven mirror of evaluate_news_feed: reads the current classification spec, matches the vertical against the Phase B starter kinds only, deep-merges content_features.<spec_key>, supersede-then-insert; no match or no spec means NO WRITE, so wiring it fleet-wide cannot flip any existing site. Surgical jsonb_set edits pinned by drift guards on the LIVE edge values (the seed is history; the live row is fact - the handoff's explicit warning for this agent), not whole-workflow replacement, because both agents are other lanes' machinery. The ROLLBACK is likewise a surgical inverse rather than restore-from-backup, so it cannot clobber unrelated edits either agent gains after 432. Applied 2026-08-15 ~18:55Z; verify DO/RAISE (IS DISTINCT FROM throughout) passed in-transaction. Live note: improvement-sweep is currently DISABLED (enabled=f since 2026-08-14), so the loop consumer is wired but undriven until the sweep re-enables; the classifier consumer proves on the Phase C pilot. Submitted FORCE=1 per the 411/429 precedent for config migrations under docs/ that define unattended production workflows.",
  "submitter": "portfolio_positioning Phase B3d",
  "plan": {
    "summary": "Migration 432 (applied 2026-08-15): wire evaluate_directory_features into its two consumers - improvement-loop (new enrich_directory_features step between enrich_news_feed and load_audit_state, news edges re-pointed on both success and error paths) and domain-research-classifier (same step between write_classification_spec and write_content_direction_spec, so greenfield builds carry the directory flag at plan time). Surgical jsonb_set edits with drift guards pinning the live 291-shaped edges; surgical-inverse ROLLBACK; snapshot-first for both rows.",
    "risks": "(1) The improvement-loop consumer is wired but UNDRIVEN today: improvement-sweep is disabled (enabled=f, last fired 2026-08-14) - stated openly so nobody reads silence as failure; the classifier consumer self-proves on the next greenfield build (Phase C pilot). (2) A failure inside the new step routes to the next mainline step via config.error_step in both contexts, so enrichment can never kill an audit cycle or a greenfield classification - the trade is that a persistent action failure is only visible in step logs, matching the accepted news-enrichment posture. (3) The action supersede-then-inserts the classification spec; in the classifier it runs immediately after write_classification_spec writes it, so the enriched row becomes current before downstream spec writes of OTHER aspects - no same-aspect contention exists in either chain. (4) Both agents belong to other lanes; edits were two edges + one step each, guards refuse on any drift, and the surgical-inverse rollback cannot clobber later unrelated edits.",
    "edits": [
      {
        "file": "docs/agent_docs/sql_for_agents/432_wire_evaluate_directory_features_b3d.sql",
        "operation": "add",
        "rationale": "Snapshot-first (two-arg snapshot_agent for BOTH rows), dual-active + idempotency + drift-anchor guards pinned to the live 291-shaped edges, both UPDATEs one transaction, DO/RAISE verify with IS DISTINCT FROM (a missing jsonb path is NULL and plain <> can never fire).",
        "sketch": "-- improvement-loop: insert step + re-point BOTH news edges\njsonb_set(jsonb_set(jsonb_set(default_config,\n  '{workflow,steps,enrich_directory_features}',\n  $json${\"action\": \"evaluate_directory_features\", \"config\": {\"site_id\": \"site_record.site_id\", \"error_step\": \"load_audit_state\"}, \"next_step\": \"load_audit_state\", \"output_field\": \"directory_features_enrichment\"}$json$::jsonb),\n  '{workflow,steps,enrich_news_feed,next_step}', '\"enrich_directory_features\"'::jsonb),\n  '{workflow,steps,enrich_news_feed,config,error_step}', '\"enrich_directory_features\"'::jsonb)\n\n-- domain-research-classifier: insert step + re-point one edge\njsonb_set(jsonb_set(default_config,\n  '{workflow,steps,enrich_directory_features}',\n  $json${\"action\": \"evaluate_directory_features\", \"config\": {\"site_id\": \"input_data.site_id\", \"error_step\": \"write_content_direction_spec\"}, \"next_step\": \"write_content_direction_spec\", \"output_field\": \"directory_features_enrichment\"}$json$::jsonb),\n  '{workflow,steps,write_classification_spec,next_step}', '\"enrich_directory_features\"'::jsonb)"
      },
      {
        "file": "docs/agent_docs/sql_for_agents/432_wire_evaluate_directory_features_b3d_ROLLBACK.sql",
        "operation": "add",
        "rationale": "Surgical inverse, NOT restore-from-backup: removes the step (#- path) and re-points the edges back, guarded to refuse unless the rows are in the 432 shape - a full-row restore would clobber any unrelated edit either agent gains after 432. The 432 backups remain in agent_definitions_backup for a full restore if ever needed.",
        "sketch": "UPDATE agent_definitions SET default_config =\n  jsonb_set(jsonb_set(default_config #- '{workflow,steps,enrich_directory_features}',\n    '{workflow,steps,enrich_news_feed,next_step}', '\"load_audit_state\"'::jsonb),\n    '{workflow,steps,enrich_news_feed,config,error_step}', '\"load_audit_state\"'::jsonb)\nWHERE type = 'improvement-loop' AND is_active AND COALESCE(is_snapshot,false)=false AND deleted_at IS NULL;"
      }
    ],
    "grounded_in": [
      "Live improvement-loop row (read 2026-08-15 pre-432): enrich_news_feed = {action: evaluate_news_feed, config: {site_id: site_record.site_id, error_step: load_audit_state}, next_step: load_audit_state} - the exact shape and edges the new step mirrors and the guards pin.",
      "Live domain-research-classifier row (read 2026-08-15 pre-432): write_classification_spec.next_step = write_content_direction_spec; sibling steps reference input_data.site_id - the classifier context's idiom, kept.",
      "feed_directory_recommendation_action.go: EvaluateDirectoryFeaturesInputSpec Required=[site_id]; sql.ErrNoRows on the spec query returns {recommended:false, reason:'no classification spec available'} without error; no-match returns without WRITING ('a spec that never mentions the key is indistinguishable from one that was never evaluated, which is the safe default for an opt-in flag').",
      "verticalDirectoryMap covers ONLY the Phase B starter kinds (mortgage-lender, savings-provider, health-insurer) - wiring fleet-wide cannot recommend a directory for any non-finance vertical.",
      "agent_definitions references to evaluate_directory_features measured 0 on 2026-08-15 (HANDOFF_2026-08-15 landmine list) - the action had no dispatcher; this migration is the first.",
      "scheduled_tasks: improvement-sweep enabled=f, interval 900s, last_triggered 2026-08-14 16:34Z (read 2026-08-15 ~18:57Z) - the loop consumer is undriven until re-enabled; stated rather than discovered later.",
      "Both snapshots confirmed in agent_definitions_backup (reason '432_...: pre-update') before the UPDATEs ran; in-transaction verify DO passed; zero in-flight orchestrations for either agent type at apply time."
    ]
  }
}
