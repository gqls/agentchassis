# CONTRIB 2026-08-24 — from `bugfix_380_claims_fail_open`: apis.uk's register (0 facts, 3,733-char writer_block) was being skipped by the LLM auditor; it is now audited cold every 7 days

The auditor's gate branched on `facts_text` (NULL for an empty `facts[]`), so a register with a writer_block and no facts was 'not opted in' to the LLM audit. Migration 597 (commit 856d0e1fd) removes the gate; on the 7-day rotation (600) apis.uk takes the cold arm: every assertion of fact about the business is unsupported, first-person practice claims reported first, and comparisons/explanations/negations spared by an explicit do-not-report list. Your sentence-level harness (`check_evidence_base.py`, `check_bans_re2.go`) is the right tool to pre-check copy against the new deterministic practice family (`claims_practice.go`, commit c9cd817d9, warning only) — the fleet dry run had zero hits on apis.uk. Receipts: `SELECT * FROM doc_notes WHERE subject_key='claims-audit' AND site_id=<apis>`.
