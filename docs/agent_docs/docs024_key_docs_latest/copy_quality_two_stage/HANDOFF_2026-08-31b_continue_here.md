# HANDOFF 2026-08-31b — continue here

**Lane:** `copy_quality_two_stage`. **Supersedes `HANDOFF_2026-08-31_continue_here.md`** (whose
FIRST ACTION — the canary — and council trail are DONE and absorbed here). First reads: this
file → `OWNER_RULINGS_2026-08-25_six_decisions_on_the_copy_machinery.md` (the ledger; gained
the propagation BUILD GO tonight) → `NOTES_two_stage_copy.md` tail (the 08-31
afternoon→night entries are this session's full trail). The owner does NOT want this lane closed.

## What CLOSED tonight (all verified at the artefact)

- **667 council trail: r3 APPROVED** (corr `1c787532-…`); advisories checked against the
  committed 668 — both already satisfied (the `%s` bug was sketch-only; `target_id` writes from
  the joined PK). Nothing owed.
- **THE CANARY RAN AND THE STACK WORKS.** Approach page through v1.0.1349: `rather than` 6→1
  (P2 holds on the full stack after failing on the template-only half — the model-prior class
  is exactly what A+B caught), total 9→5, repairs read meaning-intact, both guard refusals
  correct and recorded. **Ruling-7 trial verdict: SUCCESS on undifferentiated prose**, with the
  offer lane's asterisk adopted as binding: truncation is structurally WRONG for
  `differentiated: true` points (the comparison IS the meaning; no length floor catches it —
  their measured 12-byte case). **The final wash + 668 re-base MUST carry their prefix test**
  (`isTruncationOf` / `judgeRegisterRewrite`).
- **Widening: nine pages scored** (NOTES night entry has the table): 30→6 where the stack
  could act; every residual read and attributed. Two structural escapes became bugs:
  - **`bugs_open/420`** (resolve by SLUG — the number COLLIDES with the delivery lane's
    billing-email 420, both filed tonight, CLAUDE.md list updated): the negation walker's
    `nonProseFieldRe` excludes `name$`, and feature-card names are HEADINGS — one-line fix
    candidate + walker test written into the file.
  - **`bugs_open/422`**: negation repairs on short negation-dense heroes trip the 50%
    section-shrink floor; about + services are TERMINALLY UNREBUILDABLE (3/3 attempts each)
    and serve pre-A+B copy. Fix candidate 1 = repair-side shrink budget inside
    `rewrite_negations` (the action already composes per-field splices). **Until fixed, do
    not re-fire needs_page at those two pages — it burns attempts into the same wall.**
  - Spec-fed sections (use-cases 6→6): `site_specs` aspect `portfolio` injects `use_cases[]`
    at build, bypassing writer AND gate. Deliberately NOT washed — "not AI slop" may be
    deliberate site voice → owner decision (below).
- **Contact page saga** (4 rolls, NOTES night entry): the identity spec stored the email
  TWICE, disagreeing; the render reads the half nothing validates. Spec superseded, live page
  correct, fleet census = exactly 1 site in the class. §9 pattern written (walk the ASSEMBLY
  inputs when a validator refuses a regeneration for a value no generation emits).
- **FARMER PARCEL step 1 DONE: migration 674 applied** — briefing/strategy/vertical_landscape
  superseded battery-clean, loanzy cross-verified, exchange closed. Council `53ea95f4` drew
  **REVISE r1** (right again: the verify never asserted successor SHAPE) — answered same night
  with measurements at the live rows (recursive key-set identity 33/44/16, zero lost; index
  confirmed; aspects re-checked) and **r2 is RUNNING at handoff — read its verdict**. The
  final-wash migration inherits three requirements from this round: in-verify shape check,
  aspect predicate on the flip, prefix test on differentiated points.
- **BEST-IN-CLASS PROPAGATION step 1 BUILT + APPROVED** (owner BUILD GO relayed, ledger):
  voicestyle generalised to named blocks (`8c62e9f1b`, INERT till roll), carrier 675 APPLIED
  (894 chars), opt-ins **677/678/679 are `_HOLD` — apply ONLY after a roll carrying
  8c62e9f1b** (probe recipe + snapshot-overload check in each header; the bundled 676 is
  SUPERSEDED unapplied). Council r2 APPROVED on corr `b5a642b7` after a RIGHT four-way REVISE
  (my withdrawn premise is in WRONG_CALLS). Register CQ-033. Steps 2–4 of
  `PLAN_2026-08-25_best_in_class_propagation.md` NOT built (strategy.benchmark at birth is
  step 2; boxingonline is the sharpest live test case — their suggestion, recorded).
- **Stage-2 editor validated on the farmer CTA class**: proposal `be23d897` (gate-PASS,
  claims verified at the destination, catches the labels + a compliance typo) is PARKED at
  needs_human_review for the OWNER per D2. My duplicate misfire is cancelled-with-reason +
  WRONG_CALLS.

## NEXT ACTIONS, in order

1. **Read the 674 r2 verdict** (`53ea95f4`, by payload — r1 REVISE already answered).
2. **Farmer parcel step 2**: the 52 CTA labels. The route is PROVEN (stage-2 editor catches
   the class unprompted post-674). The open question is POSTURE: ~17 more hand-fired proposals
   → 17 human reviews. **D2 makes batch-release the owner's call — ask him** whether to (a)
   release `be23d897` and judge the pattern, then batch, or (b) hand-fire all and let him
   review in bulk. Then step 3: the 14 body sentences.
3. **On the next fleet roll**: verify 8c62e9f1b in the stamp, apply 677/678/679 (headers carry
   the ladder), then canary ONE planner run and grep its `prompt_rendered` for the standard's
   needle ("stands comparison with the strongest sites") + battery the block's arrival.
4. **bugs_open/422 and 420 fixes** — both have one-mechanism fix candidates in-file; 422
   unblocks about/services (re-fire recipe in the bug); 420's fix moves brief-negation-check
   counts on its next rebuild (expected, not drift).
5. The E sequence stands as before (gate live+measured → re-census → final wash WITH the
   prefix test → re-base 668 → wiring, vetcomparison first).

## OWNER DECISIONS OUTSTANDING (updated; raise, don't re-derive)

1. Decision D (question hierarchy) — unchanged.
2. The axis confirmation — unchanged.
3. Fund the xAI team — unchanged (bugs_open/418).
4. brief-negation-check council scope — unchanged.
5. **BANNED_REGISTER v2 — now carries a BUILD STEP**: the offer lane's `registerwords.go`
   lockstep test goes RED on a v2 cut until the Go table updates in the same commit. Their
   finding sharpens the ask: `plainly`/`honest*` had NO mechanical reader anywhere until their
   gate tonight.
6. ~~Best-in-class BUILD go~~ — **GIVEN 2026-08-31 (relayed; ledger). Steps 2–4 still his to
   sequence or delegate.**
7. Model choice — unchanged (screen still blocked on xAI funding).
8. **NEW: the spec-fed register** — finetuning's `portfolio` spec carries the use-cases copy
   ("authoritative content, not AI slop" etc.) that no rebuild can wash. Wash it (674
   pattern), keep it as deliberate voice, or rule on the class (any collection-backed section
   is ungated today)?
9. **NEW: stage-2 batch posture for the farmer labels** (see next-action 2).
10. **NEW: release or reject proposal `be23d897`** (farmer /about labels + typo fix) — parked
    for him.

## Peer state

offer lane: gate BUILT (`f7156fb54`, inert till roll), REPAIR mode ruled, corr `4054f4d9`
running, 681_HOLD held; mint measured unmoved (24%). loanzy: cull complete all layers,
exchange closed. boxingonline: prereg `PREREG_2026-08-31_does_the_research_spec_carry_the_meta_copy.md`
filed on my four-arm design — the experiment is OURS to run (through the delivery lane or an
unpaid site; NOT on boxingonline directly). 414 lane: closed. Channels in the old handoff still
valid; a peer cannot grant escalation.

## Landmines this session added or re-proved

- A publisher re-run for OUTPUT is a re-run for EFFECT (`tee` first — WRONG_CALLS).
- Attribute an artefact to a run only after grepping THAT run's responses (WRONG_CALLS).
- A widening watcher's time filter must bracket the FILING time you actually used — foreground
  the first poll against known rows.
- Tool pages deploy to per-tool paths (`tools/<name>.html` vs `/index.html` shells at 2,685
  bytes) — read `result.files`, never guess the URL.
- Probe needles single-source-line; find council runs BY PAYLOAD; the corpus moves; a REVISE
  is usually right — all re-proved tonight, all still in the old handoff's list.
