# HANDOFF 2026-09-02 — continue here

**Lane:** `copy_quality_two_stage`. **Supersedes `HANDOFF_2026-08-31b_continue_here.md`.**
First reads: this file → `OWNER_RULINGS_2026-08-25…md` (the ledger — gained TEN direct rulings
on 2026-09-02, all executed or queued below) → `NOTES_two_stage_copy.md` tail (the 09-02
entries are the full trail). The owner does NOT want this lane closed.

## The stack as deployed (verified at artefacts, 2026-09-02 evening)

- **Chassis v1.0.1354** (rolled ~15:39Z): carries the voicestyle NAMED-BLOCK generalisation
  (capability-probed: `injectPlatformBlocks`, `config_name = $1`, `build_standard_block` all
  PRESENT; probing MY COMMIT SHA read absent and means nothing — the stamp is the build HEAD,
  not an ancestry list). Does NOT yet carry BANNED_REGISTER v2's Go half (next roll).
- **BEST-IN-CLASS PROPAGATION STEP 1 FULLY LIVE**: carrier 675 applied · injector aboard ·
  **677/678/679 APPLIED** (three snapshots, single-row guards, verified at the live rows).
  **OWED READ: the next organic planner/designer run's `prompt_rendered` must carry "stands
  comparison with the strongest sites"** — llm_call_log, agent_type build-site-planner /
  content-gap-planner / visual-designer, first run after 2026-09-02 ~18:45Z. Steps 2–4 of
  PLAN_2026-08-25 not built (strategy.benchmark at birth is next; boxingonline the test case).
- **BANNED_REGISTER v2 CUT AND LANDED** (`0c11a8818`, corr `fa9744cb` — verdict UNREAD at
  handoff): plain_words class + em-dash x_not_y (superset separator). Four lockstep files one
  commit; suites green; the offer lane [4628f9] verified independently. **CLI
  brief-negation-check v1.0.1354 already carries v2** (detector ahead of repairer =
  conservative skew until the chassis rolls again). First v2 nightly read: expect plain_words
  movement, labelled in-report as the arm arriving.
- **706 APPLIED** (finetuning portfolio wash, owner's "AI slop cheapens our offering"; corr
  `7f0c4adb` — verdict UNREAD): kept-utterance asserted; the post-706 use-cases rebuild was
  FILED (item_key `page_rerender:use-cases`) — **score it: expect the spec-fed 6 to drop**.
- **The ADMIN REVIEW BATCH is parked and the owner is told**: 15 farmer + 1 loanzy
  copy_edit_proposed items, all gated (one annotated FAIL = deliberate dead-button removal,
  his call; loanzy's carries the ruling-1b evidence note, trimmed to citations). The released
  /about pair (`section_edit:be23d897:*`) was applying at handoff — **verify at the served
  page** (labels + "Financial Conduct Authority" typo fix).

## The ten rulings of 2026-09-02 (ledger has verbatim; state here)

1 RELEASED · 1b DONE (evidence fetched, proposal trimmed, citations → loanzy for SVC facts) ·
2 QUEUED BUILD (veracity checkers in the framework — the MaPS 8-page fix flows through it or
through supplied-defect editor runs; owner says discussed elsewhere too, so CHECK who else
holds a piece before designing) · 3 DONE (batch parked) · 4 CAMPAIGN QUEUED (fleet brief wash:
**v2 battery only, register+version stamped into every record** — both requirements already
promised; start with the daily check's 11 finding-sites, canary ONE site) · 5 DONE ·
6 ANSWERED (console.x.ai, team d443dd72…) · 7 DONE · 8 DONE · 9 CONFIRMED-with-belt (the
offer lane [4628f9] put D's start to the owner IN THEIR SESSION — their relayed-authorisation
discipline; design settled: their production half, our consumption half, the JOIN is the
deliverable, "THROUGH THE GATE" is a STATED requirement held both ways, mostly-unanswered
first pass = correct) · 10 CONFIRMED (axis; implementation AFTER D per their H4 evidence).

## Also open

- Council verdicts to read: `7f0c4adb` (706), `fa9744cb` (v2). A REVISE is usually right.
- **HEAD is RED from another lane** (thunder/api build failure + livespec migration-reader
  test, present on bare HEAD at `11dda6c79`) — not ours; verify-head-builds judges relative.
- bugs 420 (walker name$ blindness) + 422 (repair-vs-shrink floor; about/services still
  unrebuildable) hold their fix candidates — 422 blocks two finetuning pages from the
  corrected stack.
- Title-promise gate + CQ-032 tell + guide rewrite: ruled and queued (see 08-31b's 1b item;
  start from `check_heading_promise.go`, reuse first).
- Peer routing: offer lane = **[4628f9] ONLY, by ref** (consolidated by the owner; [6c226c]
  stood down). loanzy + boxingonline channels unchanged.
- Statistical discipline for gate quotes: ~25 post-gate points before a halving is detectable;
  a widened regex is a different instrument, not a different result.

## This session's landmine harvest (short list; WRONG_CALLS + NOTES carry the detail)

- A probe for YOUR sha in a binary tests build-HEAD identity, not inclusion — probe the
  CAPABILITY (three literals, present+absent controls).
- A batch loop killed by timeout re-fires its boundary item — receipts per item, dedup at the
  queue, and the resume must skip what the log already confirms.
- A same-file passenger CAN be the tag bump itself: my `48180ffe1` says 1353, shipped truth is
  1354 (another session's bump swept in) — read the commit scope block, not the message.
- The offer lane's RE2 guard catches register patterns at the FILE; \u escapes are a
  Python/JS-ism Go rejects at init.
- A false-positive CONTROL can be a true positive in disguise — the control string itself must
  be run against the rule before it guards anything.
