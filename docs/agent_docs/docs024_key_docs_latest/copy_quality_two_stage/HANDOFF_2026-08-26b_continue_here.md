# HANDOFF 2026-08-26b — continue here

**Lane:** `copy_quality_two_stage`. **Supersedes `HANDOFF_2026-08-26_continue_here.md`** (same
day — the chassis roll, the owner's trial, and the canary's first scores landed after it).

> **08-31 UPDATE — read this before the 08-30 one:** the owner issued RULINGS 9–12
> (rulings doc tail): "plainly" banned; no internal-directive explanations in copy; MODEL TRIALS
> ordered (Fable benchmark → Grok → Gemini); benefit-led hero co-design with the offer/benefit
> thread, per-copy agent discussion licensed. **Fable benchmark RUN, pre-registered**
> (`AUDIT_prompts/EXPERIMENT_2026-08-31_model_trials.md`): NEG **0 and 0** vs production sonnet's
> 5 on the identical post-fix prompt, read passes decisively — promoted to a wider screen; Grok ×2
> + Gemini ×2 on the same prompt are the NEXT RUNS (keys on the pod, runner recipe in the canary/
> experiment docs). The co-design's first round landed (their CONTRIB in this dir): benefit set =
> 32 sites / 187 ranked points, NO writer reads it; 18/187 carry the banned register so the set
> gets a register pass before becoming a source; `AUDIT_prompts/BANNED_REGISTER_v1.json` ships the
> rulings as data. **DECISIONS NOW STACKED FOR THE OWNER: Q-A, Q-B, and C (wire offer_ordering
> into the writer after the register pass, finetuning first).** farmerinsurance.uk arrived as
> expected (loanzy CONTRIB trail): tone-VACUUM variant (no identity/tone → writer self-describes)
> joins the audit questions; farmer = worked case #2 + model-screen material; its record-mode
> verdicts stay human-released. Ruling 9's plainly-ban migration still queued. kubectl token works
> again.

> **08-30 UPDATE:** the owner has formally handed copy quality BACK to this lane alone
> (*"The copy team can take on the copy quality from here, we can go back to finetuning"* — via
> the finetuning lane, which stands down on register work; canary collaboration CLOSED, their
> baselines remain available). **Q-A/Q-B still await him and are the first thing to raise.**
> ⚠ kubectl token EXPIRED 08-30 — no DB measurement until he refreshes; an empty result is an
> auth artefact. Their parting lesson, adopted: a dated claim about an in-flight pipeline is a
> claim about a moving thing — re-measure before quoting.

> ## ▶ START HERE, IN THIS ORDER
> 1. **`OWNER_RULINGS_2026-08-25_six_decisions_on_the_copy_machinery.md`** — now EIGHT rulings +
>    two open questions (Q-A, Q-B at the tail). The owner is actively ruling in-chat; this file
>    is the ledger.
> 2. **`AUDIT_prompts/CANARY_2026-08-26_finetuning_nine_page_rebuild.md`** — the live experiment,
>    pre-registered, two partial scoring rounds appended. The next session's first real work is
>    round 3.
> 3. **`REFRESH_2026-08-25_deep_context_the_accumulated_copy_discussion.md`** — the assembled
>    context (ground truth, every claim sourced).
>
> **One-line state:** all six 08-25 rulings plus the 08-26 truncation trial are SHIPPED and
> council-APPROVED (migrations 627–630 + the Go trial, live on v1.0.1345, verified at the binary
> with removed-string controls); the finetuning canary is 6 of 9 pages through and its scoring
> shows the binding constraints are now **D3's per-section forgiveness** and the **gate's
> vocabulary** (`instead of`/`not just` invisible), not the prompts; **two questions await the
> owner (Q-A/Q-B)** and index/faq still await rebuild.

## What is TRUE as of 2026-08-26 ~21:15Z

**Live and verified at artefacts:**
- Writer template: July "say this instead" substitutes DELETED (627); bans + tool-fallibility
  mandate kept. House voice rewritten form-only, 17→0 demos (628). Planner: no unfillable
  social-proof slots (629) + mission-constraints/about-title format rule (630, replay-tested 3/3
  before shipping). Rendered writer prompt: 63–65 → **36** negation demos fleet-level.
- finetuning.uk's own brief de-demonstrated by their lane (their 646/647: rather_than 7→0).
- **The truncation trial (owner ruling 7)**: `negationRepairPrompt` is truncation-first ("end the
  sentence before the comparison… leave the compared alternative out"; shorter is the point).
  LIVE on v1.0.1345 (probe: added literals ×1, replaced instruction ×0, on a named pod);
  council **APPROVED all-reviewers** (`82b800e1`). Fleet mixed 19× 1344 / 106× 1345, converging.
- **The tone-route bound** (`pendingCopyEditForPage`, council `754dcffd`) is now live in the same
  binary (`items_copy_edit_bound_unevaluated` ×1) — CQ-030 fully armed; still 0 dispatched
  `needs_copy_edit` runs ever; first-run verification stands as queued work.

**The canary (finetuning.uk, nine pages + requested index/faq):**
- Owner's GO ~17:20 08-26 with the STANDING instruction: *"forward only corrections… keep
  rebuilding through the system until they are acceptable"* — baseline = diff base, never undo.
- 6 of 9 rebuilt (about, approach, case-studies, services, model-approach-selector,
  tool-ai-readiness-checker); pending careers, contact, use-cases; **index + faq requested from
  the finetuning lane (owner was reading the un-rebuilt HOMEPAGE when he called regression — his
  screenshot pinned it; every quoted phrase was pre-rebuild copy) — not yet rebuilt.**
- **Round-1 scoring:** the two tool pages failed P2b (3 and 5 vs ≤2) with the tool-fallibility
  mandate satisfied in the negation shape ("not a verdict" ×4) — partial P2a-class evidence the
  model's preferred form produces the shape without demonstrations.
- **Round-2 scoring (the decisive arithmetic):** about **10**, approach **10**, case-studies 1,
  services 3. Half the hits are `rather than` = D3's FORGIVEN-MILD class, and the budget is
  **PER SECTION** (305-lane landmine) → ~6/page ships without any repair attempted;
  `instead_of` + `not_just` are **outside the gate's five shapes** → invisible. The trial changed
  repair STYLE; forgiveness + vocabulary decide WHAT is repaired. Character is much better than
  the old floor (vendor-portability contrasts, not self-limiting candour; honesty-beat CLEAR).
- **Round 3 SCORED (8 of 9; contact pending): P2a REFUTED EMPHATICALLY — the pre-registered new
  finding is real.** A positive comparison-avoidance instruction entered the brief at 19:03,
  before all six clean builds; with the demonstration stack cleared AND the instruction present,
  the pages still carry ~5 constructions each (their hand count 30, our battery 34 on the same
  shapes — two instruments converge; both reads fixed per protocol). **The carrier is the model's
  own preference; the only remaining lever is the gate.** P1 clear, P4 confirmed, prose genuinely
  better in kind (their read, fixed first). All 30 truncate losslessly — the trial's approach
  validated on real instances. This makes Q-A the whole ballgame: repair-all (or per-PAGE
  allowance) + Q-B vocabulary is the only measured path to the owner's target.

## ⚠ OPEN QUESTIONS FOR THE OWNER (Q-A, Q-B in the rulings doc, with the numbers)

- **Q-A:** does the trial's "whenever" repeal D3's mild-forgiveness for `rather than` — repair
  every one (truncation is cheap and safe now), or keep an allowance, and per PAGE not section?
  **This is the single highest-leverage decision left**: it alone accounts for ~6 of 10 on the
  failing pages.
- **Q-B (already ordered in substance — he named "instead of" in the trial):** add `instead_of`
  (+ `not_just`?) to `ScanDefineByNegation`'s shapes (`platform/orchestration/datahelpers/
  negationtells.go`, tests beside it; council scope; classification mild-vs-sharp follows Q-A).
- Older owner items still open: best-in-class build go (`PLAN_2026-08-25_best_in_class_propagation.md`);
  retitle ~6 self-limiting about pages fleet-wide (630 is future-plans only); writer rules 16–17
  removal; parked `b0dea48e` (approve edit 3 at most). **Ruling 8 (hero length)**: census done
  (mortgagecalculator 1,380 chars, cv1 16,088 outlier) — fix shape not yet designed (hero
  `llm_guidance` + brief sweep; mcalc also carries the v2 voice FOSSIL, CONTRIB in their lane).

## Next work, in order

1. **Round-3 canary scoring** when finetuning pings (or check rebuilds yourself:
   `page_components.updated_at > '2026-08-26 17:20'` on finetuning.uk, battery in
   `audit_prompt_demonstrations.py` patterns / the canary doc's protocol).
2. **Execute Q-B** once Q-A is answered (one Go change + tests + council; inert till roll — note
   in the file, per this week's pattern).
3. **The brief sweep** (ruling 4 §5 + voice-fossil pass + hero-length rule design) — the briefs
   remain the dominant teacher fleet-wide (12–31 demos/site).
4. Best-in-class plan execution on the owner's go.
5. Phase 2 verdicts 2 of N (briefs → llm_guidance → copy-editor's own prompt).
6. Tone-route first-dispatch verification when a `tone` finding fires (bound now fully live).
7. **(added 08-27) Rebuild + newTag-bump `cmd/brief-negation-check`** — the 414 lane's
   `spec_supplies_claim` detector (CLM-030, accepted into our binary after verification, NOTES
   08-27) is inert until then; tag-pinned overlay, the component-render-check landmine verbatim
   (bump in the SAME commit, verify at the cronjob jsonpath + a run's pod exit code). NO urgency
   (414 stripped both planted rows — the correct first-run result is ZERO findings). **First-run
   check, theirs verbatim: the report's `N of M sites` must have M = the whole fleet AND per-site
   `scanned_fields` NON-ZERO — that pair is the only thing distinguishing "nothing to find" from
   "surface derivation returned empty"** (main() exits 2 on the vacuous shapes, so silent-empty
   should be impossible). Honest positive case if wanted: the TWO historical rows (lendzy
   content_direction 61ef7033 + strategy 96eaff0b — also the test fixtures), never a synthetic.
   Expect the CQ-027 report's "N mandated" to RISE on its next run — detector change, not brief
   drift. Spec content as an injection surface joins phase-2's brief-verdict questions, sharpened
   by 414's two details: the plant arrived as a MANUAL spec row (`source='manual'` — an
   agent-door guard would never see it), and the second hop was an agent PARAPHRASING it into a
   different aspect ten days later — so exact-string or key-shaped defences are worthless and the
   scanned surface must be the UNION across every agent's prompt (LANDMINES entry footprinted on
   `site_specs`).

## ⚠ Traps, freshest first (full accounts: NOTES 08-26 tail, LANDMINES)

- **D3's forgiveness budget is PER SECTION, not per page** (305 landmine) — page-level counts
  accumulate legitimately; do not read a 10-NEG page as gate failure.
- The gate's five shapes ≠ the battery's list — `instead_of`/`not_just` are battery-only today.
- **Watchers poll by PAYLOAD correlation, never a printed run id** (`orchestration_states` was
  restructured: `id`→`orchestration_id`; payload scans slow). Foreground-test watcher queries.
- Council dispatches can DROP silently (kcat shape) — missing row + later submission consumed =
  dropped; `RESUBMIT_CORR` re-uses the trail. A council run can also be STRANDED by mid-flight
  schema surgery (one FAILED at `review_guidelines` 08-26).
- Migration numbers COLLIDE (two `630_*.sql`); re-check at write time, resolve by FILENAME.
- Four agent types carry TWO active `agent_definitions` rows — prompt migrations assert
  `count(*)=1` in the DO block (LANDMINES 08-26).
- `formatted` is DERIVED — brief edits hit source keys AND formatted (or regenerate), else they
  expire at the next spec write. **Verify blocks assert the FULL battery**, never the headline
  needle (finetuning's 646/647 lesson).
- A carrier-row rewrite cannot reach pasted COPIES (mcalc's v2 voice fossil).
- Mixed fleet during builds: which pod built a section is not attributable — argue from
  arithmetic that precedes the repair, as round 2 does.

## Key docs, all in this directory unless pathed

`OWNER_RULINGS_2026-08-25_six_decisions_on_the_copy_machinery.md` ·
`AUDIT_prompts/CANARY_2026-08-26_finetuning_nine_page_rebuild.md` ·
`AUDIT_prompts/EXPERIMENT_2026-08-25_about_section_replay.md` ·
`REFRESH_2026-08-25_deep_context_the_accumulated_copy_discussion.md` ·
`PLAN_2026-08-25_prompt_audit.md` + `AUDIT_prompts/PHASE1_*` + `PHASE2_*` ·
`PLAN_2026-08-25_best_in_class_propagation.md` ·
`NOTES_two_stage_copy.md` (append-only log) · `README_where_we_are.md` (owner's log) ·
baseline: `../finetuning_uk_service/baselines/2026-08-26_pre_hero_rebuild/` ·
canonical owner reviews: `../loanzy_uk_example_site/OWNER_REVIEW_2026-08-25_homegarden_and_what_it_says_about_every_site.md`,
`CONTRIB_2026-08-25_OWNER_ESCALATION_finetuning_pages_fail_the_would_a_person_say_this_test_after_a_maximal_seed.md`
