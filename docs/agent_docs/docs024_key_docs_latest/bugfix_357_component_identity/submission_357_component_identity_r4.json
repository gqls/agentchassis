{
  "submitter": "bugfix_357_component_identity lane — RFC_046 PHASE 1, ROUND 4. The code is unchanged since round 3 and is now LIVE (chassis rolled 2026-08-22 15:10Z; capability probed in the running binary with both control arms). This round answers round 3's objections by CHECKING them — one of which was right and is corrected in the register, the lane PLAN and WRONG_CALLS.",
  "rationale": "ROUND 3, and the shape has changed because the ARCHITECTURE QUESTION WAS SETTLED BETWEEN ROUNDS. Round 1 was gated as unsafe; round 2 was gated by editquality (HIGH, seconded by bug_historian) as ineffective — 'the mistyping mechanism and the Layer 2 carry-forward both continue to fire exactly as before; the diagnosed corruption is not stopped by this plan', and the 079/083 'detected but never blocked' recurrence. That objection was accepted, not argued. The architecture seat's own recommendation in that round — 'recommend an RFC scoping a durable component-instance identity rather than a fifth heuristic layer' — was filed as RFC_046 and the OWNER RULED IT: stamp identity at the point of production.\n\nSo this round is not another attempt to fix 357 with an inference. It is step 1 of a ruled two-step, and I want the council to judge it as that.\n\nTHE DEFECT, unchanged and cited: a page_components row's identity, bytes and content are written and carried as one bundle and no seam asserts they agree. Identity is re-derived by inference at every hop — from a data-component attribute, from the sentinel 'section' meaning unknown, from planned[Position-1], from fuzzy name matching, and from slot-name equality during a rebuild. 22 live rows (as of 2026-08-22) declare the shared `hero` while storing a whole interactive tool, newest born 2026-08-22 on a site homepage, because hero is planned first on all 22.\n\nWHAT THIS CHANGE DOES: RenderTemplate — the estate's ONE render spelling, 15 non-test call sites as of 2026-08-22 — sets RenderContext.RenderedTemplateSHA to the SHA-256 of the template text it actually executed. That is the one fact only the seam knows, it is free (the seam has the text in hand), and it is REPORTED not acted on, because resolving it needs a database handle the seam does not have. RenderComponentAction emits it; extractSectionsFromMetadata carries it onto SectionData; resolveComponentVersionID turns it into a component_versions row id at the single INSERT INTO page_components, in the same statement as the bytes (IMP-052); carryStoredSection forwards an existing stamp so a carried row cannot silently downgrade to unknown.\n\nWHY THE VERSION RATHER THAN THE COMPONENT: component_id records which component was ASKED; it does not record which template text RAN, and the two diverge the moment a template is edited. bugs_closed/277 closed with 15 rows permanently unrepairable because the templates that made their bytes exist nowhere — component_versions held zero rows for any of the nine components involved. This does not repair those. It stops the class accruing.\n\nREUSE, NOT ADDITION, and this is the part I most want checked: page_components.component_version_id ALREADY EXISTS, purpose-built, and has never been wired. Measured 2026-08-22: 0 of 1,930 rows populated, no Go code writing it, no Go code reading it. No new column, no new table, no new concept. ⚠ site_plan_sections carries an identically-named column which IS live and accounts for nearly every grep hit — qualify by table or the dormant one looks busy.",
  "plan": {
    "summary": "ROUND 4 — no code change since round 3. Round 3 drew SIX clean approvals plus approve-with-notes from editquality and architecture, and was gated by debug_historian on a livespec/landmine concern. Every objection has now been answered by running the check rather than arguing: the gating one does not apply (this change touches no migration and no live DB object), and the reuse_agent objection was RIGHT — component_versions has five INSERT sites, not one, and my published claim was corrected at source.",
    "edits": [
      {
        "file": "platform/orchestration/actions/component_library.go",
        "symbol": "RenderContext.RenderedTemplateSHA, RenderTemplate",
        "operation": "modify",
        "rationale": "The seam reports the digest of the template text it executed, computed BEFORE execution so it describes the input rather than anything the render may mutate. An OUT-FIELD rather than a return value, on the exact precedent of AbsentRequiredFields added by bugs_open/342: the signature was unified to ONE spelling across sixteen call sites by owner ruling 2026-08-21 and churning it again would undo that. The seam does NO I/O. An empty template returns early WITHOUT setting it — 'there was no template' is a kind of unknown, and stamping the digest of the empty string would hand a caller a provenance token pointing at a template that renders nothing. json:\"-\" because it must never be settable from content, and unlike InputSchema this field IS a string and so IS reachable by the reflection-derived step contract — the tag is doing real work here, and render_context_derivation_test.go is what tells you if that stops being true.",
        "sketch": "if ctx != nil {\n    sum := sha256.Sum256([]byte(templateStr))\n    ctx.RenderedTemplateSHA = hex.EncodeToString(sum[:])\n}"
      },
      {
        "file": "platform/orchestration/actions/save_sections_component_version.go",
        "symbol": "resolveComponentVersionID, componentVersionSource (new file)",
        "operation": "add",
        "rationale": "Turns what a section knows about its own provenance into a component_versions id, or into nothing. It returns ('',false) far more readily than it answers, and that IS the design: the only outcomes that produce a stamp are the two where provenance is a FACT — a carried stamp, or a digest still matching the component's current template. A template edited between render and save yields NO stamp and must never fall back to 'the component's newest version', because that would rebuild bugs_open/357's defect (a confident answer manufactured from something that merely correlates) inside its own fix. Get-or-create is keyed on the TEMPLATE TEXT, not the version number, so two saves of an unchanged template resolve to the SAME row and the table does not become a render log. ON CONFLICT (component_id, version_number) DO NOTHING covers the race with a concurrent save on another pod, which on this estate is the normal case, not the edge.",
        "sketch": "if hex.EncodeToString(sum[:]) != src.renderedSHA {\n    // the digest describes text we no longer hold; we cannot create a version\n    // for bytes we cannot reproduce, and must not point at the new template,\n    // which did NOT produce them.\n    return \"\", false\n}"
      },
      {
        "file": "platform/orchestration/actions/save_page_sections_action.go",
        "symbol": "SectionData, extractSectionsFromMetadata, INSERT INTO page_components",
        "operation": "modify",
        "rationale": "SectionData gains RenderedTemplateSHA (fresh render) and ComponentVersionID (carried). BOTH EMPTY MEANS UNKNOWN and unknown is persisted as NULL — nothing in this file may infer a version, for the same reason nothing should have been inferring a component. The stamp is written in the SAME statement as the bytes, beside md5($3), on the IMP-052 principle that the stamp and the thing it describes must not be two writes.",
        "sketch": "INSERT INTO page_components (page_id, position, rendered_html, rendered_html_digest, slot_name, component_id, content_data, content_brief, build_status, component_version_id)\nVALUES ($1, $2, $3, md5($3), $4, $5, $6::jsonb, $7::jsonb, 'deployed', $8)"
      },
      {
        "file": "platform/orchestration/actions/rerender_page_sections_action.go",
        "symbol": "storedSection, loadStoredSections, carryStoredSection",
        "operation": "modify",
        "rationale": "A carry does not change the bytes, so it must not change what produced them. Without this the save re-inserts a carried row with no stamp and a fact the estate already owned is lost on EVERY rerender — and rerenders are frequent here (one affected page had six in four days), so the stamp would erode faster than it accrued. This is the same file whose Layer 2 carry-forward is the conservation loop in bugs_open/357; the stamp joins the bundle it already forwards.",
        "sketch": "if s.componentVersionID != \"\" {\n    m[\"component_version_id\"] = s.componentVersionID\n}"
      },
      {
        "file": "platform/orchestration/actions/save_sections_component_version_test.go",
        "symbol": "TestComponentVersion_* (new file)",
        "operation": "add",
        "rationale": "The half that matters is where it declines. Tests: a carried stamp is kept AND costs no query (asserted by sqlmock having no expectations at all); a malformed carried value is dropped rather than written on; no provenance means no stamp and no query; an unchanged template resolves to the existing row; first sight of a template records it. ⚠ THE DRIFT TEST USES A DECOY, and the obvious spelling of it does NOT work: written as 'expect no component_versions query' it passes against inferring code too, because sqlmock refuses the unexpected call, the resolver sees a query error, and it returns no-stamp for the wrong reason — a guard in series standing in for the one under test. So the decoy row is made AVAILABLE and attractive and the assertion is that the resolver does not take it, with TestComponentVersion_DecoyControl_AnInferringImplementationTakesIt as the paired control proving the decoy is genuinely on offer.",
        "sketch": "mock.ExpectQuery(\"SELECT id FROM component_versions\").\n    WillReturnRows(sqlmock.NewRows([]string{\"id\"}).AddRow(decoyVersionID))\n// ... assert the resolver returns \"\" anyway"
      },
      {
        "file": "platform/orchestration/actions/page_component_divergence_test.go",
        "symbol": "TestPageWritersStampDigestInSameStatement anchor",
        "operation": "modify",
        "rationale": "The bugs_open/229 pin ended at \"'deployed')\", which froze the statement's COLUMN LIST rather than the property it protects ('the digest is stamped in the same statement as the bytes', enforced by the separate `stamp` check). Adding a column reddened it without weakening anything it exists for, and a pin that reddens on a safe change trains the next author to widen it carelessly. Tail moved to \"'deployed'\". MUTATION-PROVEN rather than asserted: dropping md5($3) from INSIDE the matched span makes the check fail (anchor len 247→242, contains=false), and moving the statement makes it error on locate. The first mutation I ran was WRONG — I replaced the file's first md5($3), which is at line 994 and OUTSIDE this anchor, and the guard correctly still passed; the 'first occurrence wins' trap on source-scanning tests is exactly what that is.",
        "sketch": "anchor: `(?s)INSERT INTO page_components \\(page_id, position, rendered_html, rendered_html_digest,.*?'deployed'`"
      },
      {
        "file": "docs/agent_docs/docs026_concept_register/register/component-lifecycle.md",
        "symbol": "CLC-026 (+ its 000_concept_index.md row)",
        "operation": "add",
        "rationale": "Condition (2) of the platform-seams ruling: registered in the SAME commit that ships the seam. Records the landmine (the resolver refuses more than it answers, and why a fallback to 'newest version' would rebuild 357 inside its own fix), the decoy-test trap, the dormant-column reuse with the site_plan_sections name collision, and the verify-later with its control. CLC-025 was taken by another session mid-session, hence 026. The index ROW is added; the headline COUNT is deliberately not touched, because stored counts are retired and reintroducing one in the head of that file IS the finding.",
        "sketch": "| CLC-026 | Component provenance stamp — `RenderedTemplateSHA` at the render seam → `page_components.component_version_id` at the save | built 2026-08-22, INERT, not yet rolled | …the five inferences, the dormant-column reuse, and the refuse-unless-it-is-a-fact rule | component-lifecycle.md |"
      }
    ],
    "grounded_in": [
      "architecture_review/RFC_046 — RULED by the owner 2026-08-22, option 1: 'Option 1 please, we can change the existing pages once option 1 has been built.' The ruling was referred here by this gate's own architecture seat in round 2.",
      "MEASURED live 2026-08-22 — page_components.component_version_id: 0 of 1,930 rows populated; no Go writer; no Go reader. component_versions: 369 rows over 203 components, 2026-04-20 → 2026-08-22, html_template present on 369 of 369. Control that the same method sees a LIVE column: rendered_html_digest, 1,623 of 1,930 populated, 65 code references.",
      "platform/orchestration/actions/component_library.go — RenderContext.AbsentRequiredFields: 'an OUTPUT: RenderTemplate writes it, callers read it… It is an out-field rather than a fifth return value because the seam's signature was just unified to ONE spelling across sixteen call sites (2026-08-21).' The new field follows that precedent exactly.",
      "platform/orchestration/actions/save_page_sections_action.go:522-535 — the Layer 2 carry-forward: 'Same slot, non-interactive rebuild content (e.g. the hero regenerated as plain text). Keep the existing interactive markup in place rather than overwriting the tool with prose.' This is why a carried row must keep its stamp.",
      "bugs_closed/277 close — 15 rows unrepairable because 'component_versions holds 367 rows across 202 components and zero for any of the nine components involved'. That is the class this stamp stops accruing.",
      "Council round 2 on this trail, editquality HIGH: 'In production this edit changes nothing but writes a log/work-item — the mistyping mechanism and the Layer 2 carry-forward both continue to fire exactly as before. The diagnosed corruption is not stopped by this plan.' Accepted; this round does not repeat the claim.",
      "go test ./platform/orchestration/actions/ passes, including 11 pre-existing tests updated for the added INSERT argument and the added SELECT column — the sqlmock expectations were pinning the exact argument list, which is why they had to move."
    ],
    "risks": "ROUND 3'S OBJECTIONS, EACH ANSWERED BY A CHECK.\n\n1. debug_historian (HIGH, GATING) — 'page_component_divergence_test.go is a named landmine: a migration that edits a guarded live object leaves platform/livespec stale, and nothing tells you.' CHECKED, and it does not apply: this change ships NO migration and alters NO live DB object. `git show --name-only bbe178309` contains no .sql, nothing under sql_for_agents/, and no livespec file — it writes an existing column from Go. `grep -c component_version_id platform/livespec/livespec.go` returns 0, so livespec declares nothing about it, and `go test ./platform/livespec/...` passes. The landmine fires on migrations that move a guarded object; the test edit here widened a SOURCE-scanning regex whose subject is a Go file, which has no live-object sibling. If a seat still believes a sibling exists, name it and I will update it — but I could not find one and the two mechanical checks say there is none.\n\n2. reuse_agent (MEDIUM) — 'component_versions already has a live writer, snapshotComponentVersion in store_generated_component_action.go.' **RIGHT, AND MY CLAIM WAS WRONG.** The census is FIVE INSERT sites across FOUR files as of 2026-08-22: fix_component_template_action.go (x2, change_source='repair_template_slots'), store_generated_component_action.go (snapshotComponentVersion), update_component_html_action.go (changed_by='update_component_html'), and this one (change_source='render_stamp'). The live table would have told me for free — 26 distinct change_source values. I ran the grep and stopped at the first file. Corrected visibly in register CLC-026, in the lane PLAN §10.2 and in WRONG_CALLS.\n   RECONCILIATION, which is what the seat actually asked for: (a) change_source is already the discriminator and mine is uniquely 'render_stamp', so the new rows are separable from every existing writer's by a WHERE clause; (b) dedup semantics differ by design — the other four snapshot on EDIT (one row per edit, history), mine gets-or-creates on TEXT (one row per distinct template, not per render), which is why it cannot become a render log; (c) version_number allocation is the one genuinely shared surface, and every writer already does its own MAX+1 — so the race editquality raised against my get-or-create is a property of the TABLE that predates this change by four writers. I am not fixing that here (it would be a fifth-writer change to four other paths), but it is now named rather than discovered later.\n\n3. editquality (LOW) — 'confirm the actual query is SELECT id WHERE component_id=$1 AND html_template=$2 before the INSERT, inside a single tx.' It is exactly that SELECT, and it is NOT in a transaction. The insert carries ON CONFLICT (component_id, version_number) DO NOTHING and re-selects by template text on the empty return, so the losing side of a race resolves to the winner's row rather than erroring. What that does NOT prevent is two DIFFERENT texts racing for the same version_number, which is the table-wide issue in (2). Worst case is a dropped stamp (NULL, honest), never a wrong one.\n\n4. editquality (LOW) — 'confirm the mutation-proof was run against the FINAL statement, post column addition.' It was: the column was added to the INSERT first, which is what reddened the pin; the pin was then widened and mutation-proven against that final statement (anchor 247 chars -> 242 with md5($3) removed from inside the matched span, contains=false; statement moved -> locate error). The author's own wrong-occurrence misstep is written up in WRONG_CALLS and the corrected method is in the submission.\n\n5. bug_historian + guardian (MEDIUM) — 'other page_components writers (apply_section_edit, store_generated_component, create_report_page, rerender_single_page) will keep writing rows with no stamp — the 016b §9 one-call-site-gets-the-rigorous-fix shape.' TRUE, and I want to be precise about why it is not the same defect. An unstamped row is NOT a wrong row: absence means UNKNOWN, which is the honest state and the one this whole RFC exists to restore. The stamp's guarantee is 'a row that HAS one can be trusted', not 'every row has one'. That is a materially different contract from 093's shape, where the unguarded sibling produced WRONG answers. Accepted as scoped residual, named in the register's verify-later, and the other writers are the obvious phase-2 wiring once the seam is proven on real traffic.\n\n6. NEW, found while checking (5) and not raised by any seat: page_component_history has 15 columns and NONE is component_version_id, so the archive DROPS the stamp. A row's provenance is lost the moment it is archived — which is exactly the forensic case bugs_closed/277 needed and could not have. Named here as a phase-2 item rather than folded in.\n\nDEPLOY STATE, verified at the artefact not inferred: chassis rolled 2026-08-22 15:10:31Z; the running binary carries the capability, probed with four arms (two new literals PRESENT, a pre-existing literal PRESENT as positive control, a never-shipped literal ABSENT as negative control). **The stamp is NOT yet exercised**: zero page_components rows have been created fleet-wide since the roll, so since_roll_stamped is 0 of 0 — pending, not passing. The control half already holds: 0 of 1,940 pre-roll rows are stamped, confirming nothing backfills. This is stated because a capability with no live caller has an untested dependency on its environment.\n\nUNCHANGED FROM ROUND 3: inert (0 readers), does not fix bugs_open/357, phase 2 is where behaviour changes, phase 3 (the 22 rows) is owner-authorised in principle and still blocked on the slot-name matching hazard."
  }
}