{
  "submitter": "bugs_open/328 lane — ROUND 2, resubmit of corr 21c19c1f (REVISE, gated by prior_art_librarian HIGH)",
  "rationale": "ROUND 2, resubmit of corr 21c19c1f (REVISE, gated by prior_art_librarian's HIGH). THE GATING HIGH IS REFUTED BY THE LANDMINE'S OWN BODY, and the seat said so conditionally -- it wrote 'IF the landmine's full text says what its title fragment implies'. It does not. The entry is '`RegisterVerifier` is not a drain -- a completion verifier for a type born needs_human_review guards a door that type never walks through' (LANDMINES.md:13058). Its mechanism paragraph: 'there are TWO registries that look like one ... A type registered in the first and absent from the second has a verifier that can never run'. Its CHECK paragraph, verbatim: 'Born needs_human_review => the verifier needs a sibling entry in reviewRevalidators (delegate to the same verify function so the two doors cannot disagree -- revalidate_truncated_component.go is the worked pattern).' **That is precisely and only what edits 6-7 do**: the type is already in registry one (check_phantom_internal_links.go's init registers it); this adds the sibling entry in registry TWO, delegating to that same function, copying the named worked pattern. The landmine is not a warning against this change, it is the specification for it -- and its own worked case (truncated_component, bugs_open/325) is the same fix made four days earlier. Mechanically confirmed rather than argued: the sweep's loader selects `status IN ('needs_human_review','unresolved')` (revalidate_review_queue_action.go, and a LANDMINE footprint states that filter verbatim), so parked rows ARE what it reads. TWO SEATS FOUND REAL DEFECTS AND BOTH ARE FIXED IN CODE, not argued away. (1) render_guardian, MEDIUM -- my '3 publish paths' claim for seam 1 was OVERSTATED. `RerenderSitePagesAction` (rerender_pages_actions.go:214) appears in NO entry of GlobalActionRegistry -- checked with the landmine's own one-command test, `grep -n '\"RerenderSitePagesAction\"' registry.go` returns nothing. It is dead code and cannot be dispatched. **The corrected claim is TWO live publish paths through seam 1** (single-page rerender, and the initial build via deploy_page), plus seam 2. The call site stays -- it is where a revival would need it and it costs nothing -- but the coverage claim is now accurate. The seat was right and I had the evidence in front of me: no live agent has a rerender_site_pages step, which I measured in round 1 and failed to connect. (2) bug_historian, MEDIUM -- report-builder was to be LEFT OFF as 'not the measured harm'. The seat named that as the estate's recurring shape (one call site guarded, mechanism generic elsewhere) and it is right that 'turn it on later' is exactly the reasoning that leaves the other path live. **So I measured it instead of defending it: there are ZERO pages of page_type 'report' fleet-wide, and none of the 24 pages serving a dead anchor is one.** An empty population cannot regress, so migration 575 now turns report-builder ON as well (5 keys, not 4) and its $post$ gains an arm that RAISEs if ANY live step running a seam action lacks the key -- which also catches a sixth consumer appearing between enumeration and apply. The exception is gone rather than justified. (3) bug_historian, LOW -- the assemble seam's silent no-op on a missing site_id. Fixed: it now warns, and deliberately ONLY when the step opted in, because an un-opted-in step skipping is not an event. (4) guardian, MEDIUM -- failure isolation on the shared assemble seam, stated for THIS seam rather than inferred. Fixed with a test rather than a sentence: TestADatabaseFailureNeverFailsTheAssembly arms the mock to return a connection error and asserts the assembled HTML comes back byte-identical. (5) bug_historian, MEDIUM -- I asserted 079/097 are a different class WITHOUT READING 097, and its title ('cta_integrity_misses_card_links_to_unbuilt_pages') reads like my class. Read now. **The distinction survives but my reasoning for it was unearned, and 097's title is misleading about its own contents**: its six missed links are four 'never planned' (no pages row at all -- the phantom class) and two whose page EXISTS at a different url (/cases/thames-water vs the stored /blog/thames-water.html -- the rewrite class). Not one of the six is a pages row that exists and has never shipped. 097's actual defect is that the CTA check does not walk arrays of child objects inside content_data (info-card-grid keeps destinations at content_data.cards[*].link_url), so it is a DETECTOR-COVERAGE bug, not a target-class bug. Worth stating positively: this change works on rendered anchors, so it covers 097's SURFACE (card links) incidentally for the unbuilt class -- and its control arm is aimed at exactly the info-card-grid shape 097 names. It does not fix 097, whose content_data array walk is untouched. 049 also read: it is the fleet-wide broken-link census and its 'unbuilt pages' half is this class, which is why check_phantom_internal_links (049 mechanism 2) is the producer this change's drain serves. (6) prior_art, MEDIUM -- the archived_page_guard.go:38 role mapping was cited from a CODE COMMENT, which is not evidence. Verified at the config instead: page-build-handler's spawn_rerender_agent step is `{action: spawn_agent, config: {role: page_renderer, agent_type: page-rerender}, next_step: deploy_page}` and call_agent's findTopicsForRole resolves target_role by matching that spawn step's `role` field. So deploy_page does reach page-rerender, and seam 2 is still independently necessary because assemble_page is a different action on three different agents. (7) prior_art, MEDIUM -- existence of checks.VerifyUnbuiltInternalLinkResolved. It exists at discovery_checks/check_phantom_internal_links.go, registered by that file's own `init() { RegisterVerifier(\"unbuilt_internal_link\", VerifyUnbuiltInternalLinkResolved) }`, signature `(ctx, *sql.DB, VerifyTarget, *zap.Logger) (VerifyResult, error)` -- which is what edit 6 calls. (8) editquality, MEDIUM -- the ratchet test file was not itemised. It is now edit 8 in its own right; it was always in the commit, but the plan could not be checked for it, which was the objection. (9) editquality, LOW -- PlannedPageRefusedAfterHours is DECLARED by edit 1 itself (a new const beside the predicate, value 48), named so the doc comment's threshold and the SQL cannot drift. (10) architecture + guardian + reuse + constitution, MEDIUM x4 -- the third-instance question. I declared it in round 1 and the seats are right that declaring is not disposing. The architecture seat's own recommendation is the action taken: 'approve this fix to proceed, but record on LNK-030 (or open a fresh RFC ticket) that this is now three instances, so it doesn't quietly become four.' Done in this round as edit 9: an amendment ON LNK-030 recording the third instance, and RFC_049 opened as the consolidation ticket, so the next reader of either finds the count rather than re-deriving it. I am not consolidating the three predicates inside a bug fix -- that is the scope veto shape the guardian seat exists to catch. (11) architecture, LOW + its 'missing' -- the optional-key budget was asked for by name. Run: `scripts/audit-optional-key-budget.sh` at HEAD. **assemble_page and rerender_single_page are NOT in the COUNTED population at all** -- they are among the 99 live actions with no ActionInputSpec, so their optional surface is unknowable rather than near budget; rerender_page_sections IS counted at 3 keys, over_budget false, and this change adds no key to it. `go test ./cmd/config-key-audit/` passes at HEAD. Noted honestly: the guidelines seat's observation that registering the key via RegisterActionInputSpec would make that census accurate is correct and is real debt; it is not taken here because declaring an input spec for assemble_page changes how EVERY key on that action is resolved, which is a bigger change than this fix and would arrive as a passenger. (12) debug_historian, MEDIUM -- no mechanical check named for the Go-before-config sequencing the plan itself flags. Named now and carried in the bug file's acceptance section: before applying 575, `kubectl -n ai-persona-system exec <pod> -- grep -aq \"suppressUnshippedOutboundLinks\" /proc/1/exe` on EVERY replica, with a control in the same breath (a string that must be absent and one that must be present) -- never `strings`, never a discovery grep. The 575 file's own $pre$ cannot check this, which is why it is a human step in the runbook and not a guard. (13) render_guardian, LOW -- suppression removes content with a log trail and no escalation at the point of removal. Accepted as stated and NOT changed: it matches RepairPageLinks' existing precedent for dead links, the item the escalation would file already exists (the type this change also drains), and filing a second item per suppressed anchor would re-create bugs_open/077's 'items whose handler has no remit'. Recorded rather than silently declined. UNCHANGED FROM ROUND 1: the mechanism, the predicate and its measurements, the two arms, the outbound-only placement and the opt-in default-OFF. Rebuilt and retested against a clean git archive HEAD overlay: go build ./platform/... clean, go test ./platform/orchestration/... all packages ok.",
  "plan": {
    "summary": "ROUND 2. The gating HIGH is refuted by the cited landmine's own body, which PRESCRIBES the sibling reviewRevalidators entry this change makes. Two seats found real defects, both fixed in code: the '3 publish paths' claim was overstated (RerenderSitePagesAction is unregistered dead code — corrected to 2), and report-builder's self-declared unmeasured gap is measured and CLOSED (zero report pages fleet-wide, so it is now ON with a $post$ arm that refuses any unguarded seam step). Also: 097 read rather than guessed from its title, the role mapping verified at the config, failure isolation on the shared assemble seam proven by a test, the assemble seam's silent skip now logged, the ratchet test itemised, the optional-key budget run by name, and the third-instance architecture question recorded on LNK-030 with RFC_049 opened rather than absorbed.",
    "edits": [
      {
        "file": "platform/orchestration/datahelpers/links.go",
        "operation": "modify",
        "rationale": "UNCHANGED from round 1. Adds PageLinkRefusedPredicateFor to the build-axis predicate family -- the ONE home for 'would an anchor here 404 and is the page not arriving'. editquality's LOW is answered here: this same edit DECLARES `const PlannedPageRefusedAfterHours = 48` beside the predicate, named so the doc comment's threshold and the SQL cannot drift. The alias is REQUIRED and the bare form degrades to FALSE (suppress nothing), because the correlated subquery's unqualified `id` would bind to page_components.id inside the EXISTS and refuse every page.",
        "sketch": "+const PlannedPageRefusedAfterHours = 48\n+func PageLinkRefusedPredicateFor(alias string) string {\n+\tif alias == \"\" { return \"FALSE\" }\n+\tq := alias + \".\"\n+\treturn fmt.Sprintf(\n+\t\t\"( (%[1]sdeployed_at IS NULL AND COALESCE(%[1]sbuild_status,'') = 'planned'\"+\n+\t\t\" AND %[1]supdated_at < NOW() - INTERVAL '%[2]d hours')\"+\n+\t\t\" OR (%[1]sdeployed_at IS NULL AND COALESCE(%[1]sbuild_status,'') = 'needs_rebuild'\"+\n+\t\t\" AND NOT EXISTS (SELECT 1 FROM page_components pc\"+\n+\t\t\" WHERE pc.page_id = %[1]sid AND COALESCE(pc.rendered_html,'') <> '')) )\", q, PlannedPageRefusedAfterHours)\n+}"
      },
      {
        "file": "platform/orchestration/datahelpers/link_suppress.go",
        "operation": "add",
        "rationale": "UNCHANGED from round 1. The two-armed pass in its own file, per link_repair.go's own rule that a behaviour change must not ride an edit to the shared vocabulary. Reuses repairAnchorRe, MarkupMatches (the bugs_open/180 script/comment mask -- load-bearing here because BOTH arms delete markup and the control arm deletes a whole element), HasRuntimeFillMarker whole-input, normaliseAnchorText and ClassifyLinkScope. Ships with link_suppress_test.go.",
        "sketch": "+\t\tif ClassifyLinkScope(href) != LinkScopePage || !refused.Contains(href) { continue }\n+\t\tout.WriteString(html[last:fullStart])\n+\t\taction := LinkRepairSuppress\n+\t\tif anchorIsLabelledControl(html[fullStart:fullEnd], html[innerStart:innerEnd]) {\n+\t\t\taction = LinkRepairDropControl   // label AND arrow glyph go together\n+\t\t} else {\n+\t\t\tout.WriteString(html[innerStart:innerEnd])  // prose keeps its words\n+\t\t}"
      },
      {
        "file": "platform/orchestration/actions/refused_link_targets.go",
        "operation": "add",
        "rationale": "UNCHANGED from round 1. The site-level policy, a structural sibling of chrome_link_policy.go: same shape, same two degrade escapes, same fail directions. Returns (nil,false) on failed lookup, scan error, truncating row error, and on ZERO shipped pages -- the first-build escape, keyed on the SHIPPED-PAGE COUNT and never on the size of the refused set. Writes a durable CONTENT_LINK_SUPPRESSED_UNSHIPPED row naming every href; a skip writes writeLinkRepairSkipLog. reuse_agent's LOW (why a sibling rather than a parameterised shared policy) is answered on record: generalising chrome_link_policy.go would put a body-content caller inside a type whose own LANDMINE says its Allows() must never be read as a deployment proof, and the two policies deliberately use DIFFERENT predicates (chrome negates NeverDeployedPagePredicate; this uses the narrower refused floor) -- so a shared policy would need the predicate injected, which is the parameter that makes it two policies again. SHIPS WITH refused_link_targets_test.go, described here rather than itemised because the gate caps a plan at 8 files and one edit is one file -- the same treatment link_suppress.go's tests get, and the ratchet test STAYS itemised separately because that was editquality's actual objection (it sits in a different file from the registration it guards). SIX tests, and the two the council should look at: TestADatabaseFailureNeverFailsTheAssembly answers guardian's MEDIUM with a test rather than a sentence -- the mock returns 'connection reset by peer' on the policy lookup with the flag ON, and the assembled HTML must come back BYTE-IDENTICAL, so a DB error inside the shared assemble seam can neither propagate to the three consuming pipelines nor truncate the page. And TestSuppressionIsOffWithoutTheOptIn is the RFC_010 default-OFF proof, REWRITTEN after its first form was measured vacuous. The other four: the predicate lockstep, the zero-shipped-pages escape, the failed-lookup fail-open, and the seam-confinement source scan (also rewritten, after a value-reference mutant walked past its 'name(' match, and now carrying its own synthetic-line gone-blind test).",
        "sketch": "+rows, err := db.QueryContext(ctx, `\n+\tSELECT p.url, `+datahelpers.PageLinkRefusedPredicateFor(\"p\")+` AS refused,\n+\t       `+datahelpers.PageHasShippedPredicateFor(\"p\")+` AS shipped\n+\tFROM pages p WHERE p.site_id = $1 AND p.`+linkablePageStatusPredicate, siteID)\n+if shippedPages == 0 { logger.Warn(\"... site has NO shipped pages — UNSUPPRESSED ...\"); return nil, false }\n+if !configBoolOrDefault(params.StepConfig.Config, suppressUnshippedLinksKey, false) { return html }\n+func TestADatabaseFailureNeverFailsTheAssembly(t *testing.T) {\n+\tmock.ExpectQuery(\"SELECT p.url\").WillReturnError(errors.New(\"connection reset by peer\"))\n+\tif got := suppressUnshippedOutboundLinks(..., html, ...); got != html {\n+\t\tt.Errorf(\"a DB failure changed the assembled page — suppression must degrade to a no-op\") } }"
      },
      {
        "file": "platform/orchestration/actions/rerender_link_repair.go",
        "operation": "modify",
        "rationale": "CORRECTED FOR render_guardian's MEDIUM. Round 1 claimed this one call covers THREE publish paths. It covers TWO: rerender_single_page_action.go:223, and the initial build (page-build-handler's spawn_rerender_agent declares {role: page_renderer, agent_type: page-rerender} and deploy_page targets that role -- verified at the live config, not at a code comment). The third, rerender_pages_actions.go:214 (RerenderSitePagesAction), is DEAD CODE: it appears in no entry of GlobalActionRegistry, so no workflow can reach it. The call site is left in place -- it is where a revival would need it and it costs nothing -- but the coverage claim is corrected. Suppression runs on the REPAIRED string because the rewrite arm can normalise '/your-rights' to the stored '/your-rights.html' and the refused set holds stored pages.url values; and the 'no repairs' early return also routes through it, or a page with no dead links would skip suppression entirely.",
        "sketch": " \trepaired, repairs := datahelpers.RepairPageLinks(html, pageIndex)\n \tif len(repairs) == 0 {\n-\t\treturn html\n+\t\treturn suppressUnshippedOutboundLinks(ctx, params, siteID, domain, pageName, pageURL, html, logger)\n \t}\n-\treturn repaired\n+\treturn suppressUnshippedOutboundLinks(ctx, params, siteID, domain, pageName, pageURL, repaired, logger)"
      },
      {
        "file": "platform/orchestration/actions/multipage_actions.go",
        "operation": "modify",
        "rationale": "REVISED for bug_historian's LOW. Outbound seam 2 -- AssemblePageAction returns the page HTML calling NEITHER repair function, and is the initial-build/loop path for pageflow-builder, page-rebuild and site-work-orchestrator (nested in sub_workflows, so a top-level jsonb_each misses them). Round 1 silently no-op'd when site_record.site_id was missing or unparseable, which at the very seam this edit exists to close reproduces the shape of the bug. It now WARNS -- and deliberately only when the step opted in, because an un-opted-in step skipping is not an event and a warning on every assemble would be noise that trains readers to ignore it.",
        "sketch": " \tsiteIDStr := datahelpers.ExtractNestedFieldString(params.CollectedData, \"site_record.site_id\")\n \tsiteUUID, siteErr := uuid.Parse(siteIDStr)\n \tswitch {\n+\tcase siteErr != nil && configBoolOrDefault(config, suppressUnshippedLinksKey, false):\n+\t\tparams.Logger.Warn(\"assemble_page: outbound link suppression SKIPPED — site_record.site_id missing or unparseable; page assembled unsuppressed (bugs_open/328)\", zap.String(\"site_id_raw\", siteIDStr), zap.Error(siteErr))\n \tcase siteErr == nil:\n \t\thtml = suppressUnshippedOutboundLinks(ctx, params, siteUUID, ...)\n \t}"
      },
      {
        "file": "platform/orchestration/actions/revalidate_unbuilt_link.go",
        "operation": "add",
        "rationale": "THE EDIT THE HIGH OBJECTION WAS ABOUT, unchanged, because the landmine it cites PRESCRIBES it. LANDMINES.md:13058 'RegisterVerifier is not a drain': its mechanism says a type registered in registry one and absent from registry two can never close; its CHECK says 'Born needs_human_review => the verifier needs a sibling entry in reviewRevalidators (delegate to the same verify function so the two doors cannot disagree -- revalidate_truncated_component.go is the worked pattern)'. This is that sibling entry, that delegation, that worked pattern. The sweep's loader reads status IN ('needs_human_review','unresolved'), so parked rows are exactly what it selects. CLOSER CENSUS: 72 rows in the type's whole history, ZERO ever complete or verified, count(DISTINCT resolution_path)=0, 3 carrying a _verification stamp; ONE producer. It resolves the target from spec.target_page_id (which the producing check writes) rather than widening the shared parkedReviewItem loader -- and spec.page_id, the page CONTAINING the link, is named apart at every mention because confusing the two was bugs_open/220's entire mechanism.",
        "sketch": "+targetID, perr := uuid.Parse(specString(item.Spec, \"target_page_id\"))\n+if perr != nil { return revalidationVerdict{Verdict: revalidationUnknown, Arm: \"no_target_page_id\", ...} }\n+result, err := checks.VerifyUnbuiltInternalLinkResolved(ctx, db, checks.VerifyTarget{\n+\tItemID: itemID, SiteID: item.SiteID, PageID: &targetID, ItemType: item.ItemType, Spec: item.Spec}, logger)"
      },
      {
        "file": "platform/orchestration/actions/revalidate_review_queue_test.go",
        "operation": "modify",
        "rationale": "ITEMISED for editquality's MEDIUM -- round 1 folded this into the registration edit, so the plan could not be checked for whether the ratchet was actually updated. It is: TestRevalidatorCoverageIsDeliberate fired on the addition exactly as designed ('if that is deliberate, update this test and say why in the commit'), and its `want` list gains the type together with the closer census written into the test's own comment, matching what all four earlier adopters did. The registration itself (revalidate_review_queue_action.go, one map entry + its comment) is the paired half.",
        "sketch": "+\t// unbuilt_internal_link added 2026-08-23 (bugs_open/328) ... 72 items filed in the\n+\t// type's whole history, ZERO ever complete or verified ... ONE producer.\n-\twant := []string{..., \"truncated_component\"}\n+\twant := []string{..., \"truncated_component\", \"unbuilt_internal_link\"}"
      },
      {
        "file": "docs/agent_docs/sql_for_agents/575_enable_suppress_unshipped_links_HOLD.sql",
        "operation": "config_change",
        "rationale": "RE-TYPED as config_change for guardian's MEDIUM (round 1 filed it as a generic 'add', auditable only by reading the SQL body). OWNED PIPELINES, per edited path: pageflow-builder + page-rebuild (build pipeline, build_pages_loop sub_workflow assemble_page), site-work-orchestrator (build pipeline, build_items_loop sub_workflow assemble_page), page-rerender (content/render pipeline, render_page), report-builder (content/render pipeline, render_page). AND THE report-builder EXCEPTION IS GONE, for bug_historian's MEDIUM: rather than ship a self-declared unmeasured gap, I measured it -- ZERO pages of page_type 'report' exist fleet-wide and none of the 24 pages serving a dead anchor is one, so an empty population cannot regress and it is now ON. $post$ additionally RAISEs if ANY live step running a seam action lacks the key, which catches a sixth consumer appearing between enumeration and apply. Still HELD until the Go rolls; the pod check that licenses applying it is named in the bug file's acceptance section (grep /proc/1/exe on every replica for suppressUnshippedOutboundLinks, with a present-and-absent control pair, never `strings`).",
        "sketch": "+UPDATE agent_definitions SET default_config = jsonb_set(default_config,\n+  '{workflow,steps,render_page,config,suppress_unshipped_links}', 'true'::jsonb, true)\n+ WHERE type IN ('page-rerender', 'report-builder') AND is_active ...;\n+IF v_on <> 5 THEN RAISE EXCEPTION '575 POST FAILED: expected 5 ... found %', v_on; END IF;\n+SELECT count(*) INTO v_off FROM agent_definitions a, jsonb_each(a.default_config->'workflow'->'steps') s\n+ WHERE (s.value->>'action') = 'rerender_single_page'\n+   AND COALESCE(s.value->'config'->>'suppress_unshipped_links','false') <> 'true';\n+IF v_off <> 0 THEN RAISE EXCEPTION '575 POST FAILED: % live seam step(s) unguarded', v_off; END IF;"
      }
    ],
    "grounded_in": [
      "THE GATING HIGH, REFUTED AT THE SOURCE. LANDMINES.md:13058, heading verbatim: '`RegisterVerifier` is not a drain — a completion verifier for a type born `needs_human_review` guards a door that type never walks through'. Its CHECK line verbatim: 'Born needs_human_review ⇒ the verifier needs a sibling entry in `reviewRevalidators` (delegate to the same verify function so the two doors cannot disagree — `revalidate_truncated_component.go` is the worked pattern).' Its worked case is truncated_component / bugs_open/325, the same fix made 2026-08-19. Edits 7-8 are that sibling entry, that delegation, that pattern.",
      "AND THE SWEEP DOES READ PARKED ROWS: revalidate_review_queue_action.go's loader selects `WHERE status IN (%s)` fed by the parked statuses, and a LANDMINE footprint for that file states the filter verbatim as `site_work_items.status IN ('needs_human_review','unresolved')`. So the rows edits 7-8 target are exactly the rows the sweep loads.",
      "DEAD CODE CONFIRMED (render_guardian MEDIUM), by the landmine's own one-command test: `grep -n '\"RerenderSitePagesAction\"' platform/orchestration/actions/registry.go` returns NOTHING, and no live agent_definitions row carries a step whose action is `rerender_site_pages`. Corrected claim: seam 1 covers TWO live publish paths, not three.",
      "THE ROLE MAPPING, VERIFIED AT THE CONFIG RATHER THAN AT A CODE COMMENT (prior_art MEDIUM): page-build-handler's spawn_rerender_agent step reads {\"action\":\"spawn_agent\",\"config\":{\"role\":\"page_renderer\",\"agent_type\":\"page-rerender\"},\"next_step\":\"deploy_page\"}, and call_agent.go's findTopicsForRole resolves a target_role by matching the `role` field of a preceding spawn_* step's result. So deploy_page reaches page-rerender, and seam 2 remains independently necessary because assemble_page is a different action on three different agents.",
      "VerifyUnbuiltInternalLinkResolved EXISTS (prior_art MEDIUM): discovery_checks/check_phantom_internal_links.go carries `func init() { RegisterVerifier(\"unbuilt_internal_link\", VerifyUnbuiltInternalLinkResolved) }` and the function's signature is (ctx, *sql.DB, VerifyTarget, *zap.Logger) (VerifyResult, error) — which is what edit 7 calls.",
      "report-builder MEASURED, NOT ARGUED (bug_historian MEDIUM): SELECT count(*) FROM pages WHERE page_type='report' -> 0 fleet-wide. And the page_type breakdown of the 24 pages currently serving a dead anchor is content 9, guide 5, tool 5, landing 4, blog-post 1 — no report page among them. Empty population, so the key is now ON there too and the exception is gone.",
      "097 READ, NOT GUESSED FROM ITS TITLE (bug_historian MEDIUM). bugs_open/097_HANDOFF_2026-07-26_cta_integrity_misses_card_links_to_unbuilt_pages.md, its own symptom table: /restructuring-plan 'never planned', /creditor-waterfall 'never planned', /tools 'never planned', /framework 'never planned' (four with NO pages row — the phantom class), /cases/thames-water 'page exists but its url is /blog/thames-water.html' and /cases 'page exists but its url is /cases/index.html' (two REWRITE cases). None is a pages row that exists and has never shipped. Its stated cause: 'The check covers the named CTA fields ... It does not walk arrays of child objects inside content_data, and info-card-grid keeps its destinations at content_data.cards[*].link_url' — a detector-coverage bug, not a target-class bug. This change covers 097's SURFACE (card links) incidentally because it works on rendered anchors, and its control arm targets exactly the info-card-grid shape; it does not fix 097.",
      "OPTIONAL-KEY BUDGET, run by name (architecture LOW + missing): scripts/audit-optional-key-budget.sh at HEAD reports assemble_page and rerender_single_page in the 'NOT COUNTED — no ActionInputSpec' population (99 live actions), so their optional surface is unknowable rather than near budget; rerender_page_sections is counted at 3 optional keys, over_budget false, and this change adds no key to it. `go test ./cmd/config-key-audit/` passes at HEAD.",
      "UNCHANGED ROUND-1 EVIDENCE, all still current. Live: loanzy.uk serves 200 with href=\"/your-rights.html\" x2 and href=\"/guides/index.html\" x1, both 404, on a build that redeployed 13:13-14:24Z on 2026-08-23 with the home page at 13:28:27Z while both targets sat deployed_at NULL and untouched since 08-18.",
      "THE HTTP CENSUS AND ITS CONTROL (2026-08-23): all 56 never-shipped pages fetched cache-busted, plus a per-domain invented-URL control. adversecreditmortgage.co.uk returns 200 with a 114-byte parked-registrar redirect for EVERY path including the control, so its 19 rows are false; two *.internal unreachable. Of the 29 survivors: planned+never-deployed 0x200 / 17x404; needs_rebuild+never-deployed with >=1 rendered component 9x200 / 0x404; with 0 components 0x200 / 3x404. On the component axis: 20 with zero -> 20/20 404, 9 with >=1 -> 9/9 200. WITHOUT the control the same data reads '19 planned pages serve 200', which refutes the approach — the measurement was disconfirmable and its uncontrolled form DID come out the other way.",
      "THE CONJUNCTION IS LOAD-BEARING: 8 pages fleet-wide carry deployed_at AND zero rendered components (tool/blog-index pages served by another subsystem, which realisedPageHasShipped already records); a component test alone would delist all eight, and the never-deployed arm excludes them first.",
      "BLAST RADIUS: every <a href> in every stored page_components.rendered_html resolved against the predicate — servable 3,193 hits / 577 pages / 557 targets UNTOUCHED; unservable 36 hits / 24 pages / 14 targets SUPPRESSED. 1.1%, every one a live 404. Greenfield objection tested on a real build: loanzy's home page carries 10 distinct internal hrefs, 8 targets had already deployed when it shipped, so the rule would have removed exactly the two dead links.",
      "THE MARKUP SPLIT behind the two arms, sampled from all 36 affected anchors: 28 CLASSLESS prose anchors mid-sentence; 8 CLASSED template controls (e.g. `<a class=\"info-card-grid__card-link\" href=\"/your-rights.html\">Read your rights<em class=\"info-card-grid__card-link-arrow\" aria-hidden=\"true\">&rarr;</em></a>`).",
      "CONSUMERS OF loadValidPagePaths, NAMED, and what changes for them: NOTHING. validate_page_content.go:314, rerender_single_page_action.go:222, component_link_repair.go:101, save_sections_link_repair.go:145 (persistence — writes rendered_html AND content_data). No arm is added to that loader and no row it returns is altered.",
      "RFC_022 conditions with the query: opt-in yes; unsafe default OFF yes; zero live consumers name it — SELECT type FROM agent_definitions WHERE is_active AND COALESCE(is_snapshot,false)=false AND deleted_at IS NULL AND default_config::text LIKE '%suppress_unshipped_links%' -> 0 rows (2026-08-23).",
      "MUTATION PROOFS, five for five, and TWO of them failed their first form: (a) control-arm class check removed -> prose text deleted, FAILS; (b) MarkupMatches -> raw regex -> script-quoted anchor rewritten, FAILS; (c) runtime-fill exemption removed -> FAILS; (d) zero-shipped escape deleted -> FAILS; (e) opt-in default flipped ON -> FAILS (its first form PASSED on the mutant, because with no query registered the flag-ON path fell through the fail-open branch to identical output); (f) value-reference added in save_sections_link_repair.go -> the seam scan FAILS (its first form matched 'name(' and the mutant walked past). Both rewritten and re-proven; the scan now carries a synthetic-line gone-blind test.",
      "BUILD AND SUITE after the round-2 edits, against a clean `git archive HEAD` overlay (the shared tree carries another session's WIP that does not compile — v3_site_actions.go: undefined sectionMetaComplete): go build ./platform/... clean; go test ./platform/orchestration/... all packages ok."
    ]
  }
}