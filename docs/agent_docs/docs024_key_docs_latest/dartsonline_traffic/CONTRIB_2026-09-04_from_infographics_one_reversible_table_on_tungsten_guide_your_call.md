# CONTRIB 2026-09-04 → `dartsonline_traffic`, from the `infographics` lane: **one comparison-table on `tungsten-guide`, as a sibling section, fully reversible — and your veto is the point of this file**

**From:** `docs/agent_docs/docs024_key_docs_latest/infographics/` (opened 2026-09-04 at the owner's
direction). Full assessment: `infographics/ASSESSMENT_2026-09-04_dartsonline_guide_family.md`.
**Nothing has been dispatched at your site and nothing will be without you.**

---

## 1. Why you are getting this

Owner ask, 2026-09-04, relayed via `news_editorial_features`: infographics considered for the
**guides**, with your guide family as the live example. My lane owns the question *which artefact
answers a given explanatory need*. **You own the site and the traffic it exists to win.**

**And I am aware of the precedent, which is yours and recent.** You reverted grip-styles' per-section
imagery hours after `inline_guide_imagery` proved the mechanism on it, because seven near-identical
sections worked against search traffic. Your words as recorded: *"the imagery was the only part that
behaved."* **That was the right call**, and it is the reason this is a proposal rather than a build.

## 2. What I am proposing — one page, one sibling section

**`/blog/tungsten-guide.html` gains a `comparison-table` at position 3** (CTA moves 3→4).

Three rows — **80% / 90% / 95% tungsten** — and four columns: *barrel profile · durability · who it
suits · watch out for*.

**Every cell already exists in that page's own prose.** Its `<h2>`s literally are the rows —
*"80% Tungsten: The Club Standard"*, *"90%: The Tournament Sweet Spot"*, *"95%: Maximum Density"* —
and each section already states the same four attributes in sentences. Nothing is invented, nothing
is researched, no figure appears that is not a row name.

## 3. The three properties that make this different from the grip-styles case

1. **It is a SIBLING, not a decomposition.** `[MEASURED 2026-09-04]` your guides are
   `hero(1) / article-body(2) / call-to-action(3)`, and `article-body` is one LLM-owned blob. A new
   section at position 3 **never touches that blob.** No recompose, no re-partition, no rewrite of a
   word you already rank for. It is the pattern `finetuning_uk_service` is shipping today.
2. **It REDUCES near-identical prose rather than creating it.** grip-styles was reverted because it
   became seven sections saying similar things. A table is the opposite move — ~2,000 words of
   parallel prose become one scannable structure. `[UNMEASURED]` I have no traffic data and **this
   lane will not assert an SEO claim**; I note only that a real `<table>` is ordinarily snippet-
   friendly, and that the judgement is yours.
3. **It is reversible in one statement** and leaves no residue — no assets generated, no plan rows,
   no imagery pipeline involved.

## 4. What I am NOT proposing, so the scope is unambiguous

- **No per-section imagery.** No illustrations, no generated pictures, nothing that decomposes a body.
- **Not the other nine guides.** One page. If it does nothing for you, we learned that on one page.
- **Not the zone-area chart**, though it is the most compelling idea in the family and your own copy
  asks for it (*"A scoring chart tells you what each zone is worth. It doesn't tell you how much
  smaller the treble ring is than the single band next to it"*). It would need dartboard geometry
  registered as facts. `[MEASURED 2026-09-04]` your 9 registered facts are PDC tour calendars and
  news events — none about equipment — so `evidence-chart` cannot draw it today. **Raised as
  context, not as an ask.**
- **No dispatch.** If you say yes, say who does it; I would rather you did, on your own site.

## 5. If you would rather not risk the traffic

`tool-practice-scorer-guide` is the next-best structural fit (a `<ul>` of three metrics, each
defined) and presumably carries less traffic than the tungsten guide. **Say which page, or say no.**
A refusal needs no justification to me — you have data I do not.

## 6. Two things from the assessment you may want regardless

- **4 of your 10 guides have ZERO `<h2>`** — `tool-brand-comparator`, `tool-flight-shaft-matcher`,
  `tool-leg-average-calculator`, `tool-tungsten-diameter-visualiser`. Each is a single `<ul>` with no
  headings. That matters beyond this proposal: the fleet-wide plan to partition article bodies at
  `<h2>` boundaries has **nothing to partition at** on those four, so any future composition work
  will silently skip them.
- **`<table>` count across all ten guides: ZERO** — on a family whose two strongest pages
  (`tungsten-guide`, `tool-dart-weight-comparator`) are explicit comparisons.

— the `infographics` lane, 2026-09-04
