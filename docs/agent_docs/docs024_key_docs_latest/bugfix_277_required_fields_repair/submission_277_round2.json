{
  "rationale": "ROUND 2 on corr 7b0e2833 (REVISE, gating: the assignment UPDATE must be a reviewed edit). This round: (1) the assignment is now edit 2 — a needle-gated SQL file (pre-image step, execution-time re-derivation with refusal conditions, ROW_COUNT assertion, revert statement) committed to the workstream dir; the 4-row CANARY it prescribes was executed under round 1's stated sequencing and is VERIFIED PER-ARM (all four routes matched the census prediction; fleet assignment of the remaining ~40 rows is HELD for this verdict). (2) A round-1 figure is CORRECTED and logged in WRONG_CALLS.md: 'exactly one item ever terminal' was a 2026-07-25 code-comment measurement repeated as current; the lookup the prior_art seat asked for returns 50 complete (completed_at < 2026-08-15) — almost all the 033 drain's own closes since 07-27. The park-vs-checkpoint argument rests on checkpoint_for_review writing NO item_key (code: the INSERT at checkpoint_for_review_action.go:198-207 simply has no item_key column) plus the two-strike churn mechanics, not on that figure. (3) Route-count clarified for editquality: the workflow has SEVEN routes (stale, resolved, no_plan_owned, no_content_data, no_plan_generic, partial, and implicit malformed->mark_failed); the census showed five because resolved and malformed were empty that day. The no_plan_generic row IS converted now by file_recreate — the 'deliberately deferred' item is only the ensure_page_section_layout FALLBACK, committed to be added (one-line DB config) if that single row's rebuild no-ops for want of a same-role sibling layout. (4) Canary evidence now answers seats that asked for verification: the router executed 4 live items (orchestrations 0177ce18 stale-close, 61a71bbd blob-park, 8dd51e7e owned-park incl. the gas converter, plus the partial conversion) — this also supersedes reliance on the 397 precedent alone; the minted content_rewrite carries source AND created_by = 'required-fields-missing-handler' (guidelines seat: provenance is truthful, measured not assumed), spec.mode=edit_live, and depends_on linking the original (create_work_item's parent_item_id input feeds the depends_on dispatch gate — documented in CQ-023). (5) guardian's component_id objection is answered by analysis PLUS a measured instance now in CQ-023: the partial row's stored spec.component_id was ALREADY dead while (page,slot) classification found the live component — which is exactly why classification refuses stored ids; the CONVERSION KEY needs a different property, stability across FAILED repair cycles, and a failed repair by definition did not rerender the component so its id holds and strikes accumulate; a successful rerender mints a new id and correctly resets the budget. A page-level key was rejected: two failing slots on one page would share one strike counter.",
  "submitter": "bugfix-033/277 session (bugfix_277_required_fields_repair), RESUBMIT of 7b0e2833",
  "plan": {
    "summary": "required_fields_missing items park forever because no handler claims the type. Seed a pure-SQL router (required-fields-missing-handler) that classifies each item by live DB state — by (page_name, slot_name), never stored component ids — and converts/closes/parks per class; assign the existing rows via a needle-gated, canaried SQL file (now a reviewed edit); flip the producer to file future items routed; update the coverage-test roster and the two register entries. Canary (4 rows, one per executed arm) verified; fleet assignment held for this verdict.",
    "edits": [
      {
        "file": "docs/agent_docs/sql_for_agents/410_required_fields_missing_router.sql",
        "symbol": "required-fields-missing-handler",
        "operation": "config_change",
        "rationale": "The router (397 template): query_database classifier -> conditional_branch cascade -> arms; verify DO-block asserts branch wiring, park-arms park (not complete), conversions born triaged, retired create_work_item spec key ABSENT, and 0 rows assigned at seed time. APPLIED 2026-08-15 and canary-proven: 4 live executions, all routes matching the read-only census prediction.",
        "sketch": "classify (one-row CASE, params [input_data.site_id, input_data.work_item_id], spec re-read from site_work_items) -> route_stale/.../route_partial (== cascades, final else_step mark_failed) -> close_stale/close_resolved (update_work_item_status complete + evidence) | park_owned/park_blob (update_work_item_status needs_human_review + error_message with facts/options; HOLDS dedup key) | file_recreate (needs_content_page @ page-build-handler, triaged, mode=recreate) / file_rewrite (content_rewrite @ page-build-handler, triaged, mode=edit_live, spec_paths carries missing_fields, key content_rewrite:from_rfm:+spec.component_id) -> close_converted. The seed's exact embedded SQL string was extracted and executed against all five canary-class rows BEFORE apply — five/five routes correct."
      },
      {
        "file": "docs/agent_docs/docs024_key_docs_latest/bugfix_277_required_fields_repair/ASSIGN_2026-08-15_fleet_assignment.sql",
        "symbol": "fleet assignment (THE actual fix for the parked backlog — promoted from prose to a reviewed edit per round 1's gating objection)",
        "operation": "config_change",
        "rationale": "Needle-gate discipline per the debug_historian seat: STEP-0 pre-image dump command in the header; a DO block that re-derives the target set AT EXECUTION TIME and REFUSES on (a) handler not active, (b) zero unrouted parked rows, (c) any row of the type mid-claim; the UPDATE asserts ROW_COUNT equals the re-derived target; a revert UPDATE for unprocessed rows is in the header and 410_ROLLBACK refuses while non-terminal rows still route here.",
        "sketch": "UPDATE site_work_items SET handler_agent='required-fields-missing-handler', status='triaged', attempt_count=0, updated_at=NOW() WHERE item_type='required_fields_missing' AND status IN ('needs_human_review','unresolved') AND COALESCE(handler_agent,'')='' — inside a DO block with the three refusal preconditions and GET DIAGNOSTICS assert. Canary variant (id-filtered, 4 rows) EXECUTED 2026-08-15 ~11:05Z and verified per-arm: 332bb3f6 stale->complete (orch 0177ce18), e512af8a blob->parked with message+route on the row, 483fb749 gas-converter->parked (owned), 4fa5b019 partial->converted (content_rewrite:from_rfm: item minted, claimed by page-build-handler). Fleet run HELD for this verdict."
      },
      {
        "file": "platform/orchestration/actions/discovery_checks/check_required_fields_missing.go",
        "symbol": "RequiredFieldsMissingCheck.Run",
        "operation": "modify",
        "rationale": "Future items born routed. Precedent for triaged-from-check: check_integrity.go:141, check_tool_acceptance_due.go:134. improvement_guardian's claimable-status concern is sequenced away: the seed shipped and canary-proved BEFORE this Go change rides a roll, and the claim gate blocks any window where the handler were missing ('blocked', recoverable by feasibility-recheck).",
        "sketch": "const ( requiredFieldsHandlerAgent = \"required-fields-missing-handler\" ) // block form so handler_coverage_test's const resolver sees it\n- HandlerAgent: \"\",  - Status: \"needs_human_review\",\n+ HandlerAgent: requiredFieldsHandlerAgent,  + Status: \"triaged\",\nplus both flag-only rationale comments rewritten as superseded-by-owner-ruling (the human-decision reasoning survives inside the router's two park arms). COMMITTED 5ad81182b (Council-Submitted trailer); post-roll verification committed in RUNBOOK: buildinfo.GitCommit stamp + git merge-base --is-ancestor + per-replica literal probe with positive AND negative controls (the literal enters the binary only via this const)."
      },
      {
        "file": "platform/orchestration/actions/discovery_checks/handler_coverage_test.go",
        "symbol": "knownHandlerAgents",
        "operation": "modify",
        "rationale": "Roster entry; the seed was applied and verified live BEFORE the Go commit landed, satisfying the roster's own contract. prior_art's dormant-machinery check answered: the only other agent_definitions row whose config names required_fields_missing is completeness-discovery-agent — the producer's run_checks carrier, not a rival handler.",
        "sketch": "+ \"required-fields-missing-handler\": true, // seeded 2026-08-15 by 410_...sql (bugs_open/277); verified live BEFORE this line"
      },
      {
        "file": "docs/agent_docs/docs026_concept_register/register/content-quality.md",
        "symbol": "CQ-023",
        "operation": "add",
        "rationale": "Registered in the same commit as the seed (ordering-exemption condition 2). Now ALSO records, from this round's objections: the component-id key-vs-classification split with its measured instance; the depends_on (not parent_item_id) linkage semantics; and the PROLIFERATION TRIPWIRE both the reuse_agent and architecture seats asked for — this is the estate's third bespoke single-type router, and the entry states that whoever writes the FOURTH should propose the generalised engine instead (the classifier SQL is the only type-specific part).",
        "sketch": "status / what (six explicit routes + malformed) / producer+consumer set / conversion keys / landmines (verifier-later fail-closes converted arm; parked rows deliberately hold dedup keys; blob repair is decomposition's, tool-page repair is the tool lane's) / key-choice rationale / depends_on linkage / proliferation tripwire / verify-later incl. the +7d churn guard."
      },
      {
        "file": "docs/agent_docs/docs026_concept_register/register/page-build-pipeline.md",
        "symbol": "PBP-028 producer-set clause",
        "operation": "modify",
        "rationale": "The router is the third mode=edit_live emitter; the clause's 'ONLY two' sentence is corrected with strike-through (it was also already narrower than the fleet on plain content_rewrite emitters — noted in place). render_guardian's objection rests on a PRE-bugs_open/178-fix landmine: PBP-028 IS the channel that hands the writer the page's own current rendered_html under mode=edit_live — load_current_section_content_action.go (editLiveMode :98; passthrough for any other mode :139-140; existing_content_html attach :191), wired by migration 299, shipped v1.0.1247, running-binary-confirmed (238's fixes are ancestors of stamp a2a6912...).",
        "sketch": "Strike 'ONLY two', append the third emitter with its always-targets-an-existing-deployed-component guarantee and the component-id item_key note."
      }
    ],
    "grounded_in": [
      "CANARY, measured 2026-08-15 ~11:08-11:20Z: orchestration_states WHERE workflow_plan->>'start_step'='classify' -> 0177ce18 COMPLETED route=stale; 61a71bbd COMPLETED route=no_content_data (html_len 2404); 8dd51e7e COMPLETED route=no_plan_owned (n_missing 9, html_len 13248 — the gas converter); item 4fa5b019 complete route=partial with minted content_rewrite:from_rfm: item at page-build-handler",
      "PROVENANCE (guidelines seat), measured on the minted item: source='required-fields-missing-handler' AND created_by='required-fields-missing-handler'; depends_on={4fa5b019-...} (the original), spec mode=edit_live, missing_fields carried verbatim",
      "CORRECTED FIGURE (prior_art seat): SELECT status,count(*) FROM site_work_items WHERE item_type='required_fields_missing' AND completed_at<'2026-08-15' GROUP BY 1 -> complete|50. Round 1 said 'exactly one ever' from revalidate_review_queue_action.go:64-66, a 2026-07-25 measurement predating the 033 drain's closes. Logged in WRONG_CALLS.md 2026-08-15",
      "checkpoint_for_review_action.go:198-207 (prior_art seat): the work-item INSERT names columns (site_id, source, pipeline, item_type, severity, summary, spec, page_id, priority, handler_agent, status, created_by) — no item_key, hence no dedup; and handler_agent is the literal 'human-review', an unregistered agent (the 2026-07-28 handoff's 'do not add a tenth' landmine)",
      "IMG-071 precedent (prior_art seat), measured: agent_definitions rows image-url-404-handler + image-source-unsatisfiable-handler active (created 2026-08-12, updated 2026-08-14); site_work_items carrying them as handler_agent: 3 complete image_url_404 + 2 complete and 15 cancelled image_source_unsatisfiable — real terminal outcomes, and now superseded as load-bearing precedent by this router's own four live executions above",
      "SCHEDULED-DEPS (guardian seat): SELECT name FROM scheduled_tasks WHERE pre_query LIKE '%required_fields_missing%' OR pre_query LIKE '%needs_human_review%' -> 0 rows; the review-queue revalidator selects by covered type + status IN (needs_human_review, unresolved), so routed/triaged items simply exit its scope while PARKED rows remain inside it — the second drain is preserved by construction (work_items_common.go:139-142)",
      "KEY-CHOICE instance (guardian seat): the partial row's stored spec.component_id dda5fbdf-91cc-4461-bdef-3f5400f93ee4 resolves to NO page_components row while (page_name,slot) resolves live component 65739383-004d-4971-9bf9-c7154c182321 — stored ids are unfit for CLASSIFICATION; the conversion KEY only needs stability across failed repair cycles, where the component id by definition has not changed",
      "WRITER GATES on the exact conversion path (bug_historian + compliance seats): page-build-handler chain runs validate_content BETWEEN writer and save (PBP-008; ValidatePageContentAction: placeholders, unrendered templates, contamination, links, claims) and the render gate missingRequiredLLMFields refuses to render a section whose required source-llm fields are empty (v3_site_actions.go:2066-2079) while rerender_page_sections ESCALATES to the writer rather than blanking (rerender_page_sections_action.go:327-357) — a writer that leaves required fields unset produces a loud refusal, not silent empties; and the shrink guard (save_refused_incomplete) refuses saves that shrink a section past the floor",
      "EDIT_LIVE mechanism (render_guardian seat): PBP-028 + migration 299_edit_live_channel_for_content_rewrite_writer.sql — load_current_section_content sits between check_has_ready_sections and spawn_content_writer, and when spec.mode='edit_live' joins each ready section against page_components.slot_name and attaches rendered_html as existing_content_html (load_current_section_content_action.go:98,139-140,191); the writer prompt's {{if .current_section.existing_content_html}} block ships in 299. The cited landmine describes the PRE-178-fix world in which no such channel existed",
      "ROUTE COUNT (editquality seat): the workflow's verify block asserts SIX explicit branch pairs plus the final else_step mark_failed; census classes were five because 'resolved' and 'malformed' were empty on 2026-08-15. The single no_plan_generic row (leopardess blog listing) converts via file_recreate NOW; the committed fallback if it no-ops is one ensure_page_section_layout step (registered action, registry.go:717, refuses when any section exists from any source)"
    ],
    "risks": "1) The partial conversion's page rebuild was still executing at resubmission time — its artefact check (named fields populated in content_data, rendered_html length not collapsed, claims gate passed) is the one outstanding canary verification and is committed in the RUNBOOK; fleet assignment stays HELD until it lands clean AND this round approves. 2) The no_plan_generic single row may no-op for want of a same-role sibling — committed response: add the ensure_page_section_layout step (one-line DB config, second consumer of a registered action — will be noted in its register entry) rather than accept a silent no-op. 3) PBP-028's confirmed narrower gap (plan_sections resolving a DIFFERENT component than stored makes the edit_live join miss and the writer fabricate) is inherited on the partial arm — bounded by the shrink guard and by scale (one row today; every future conversion passes the same validate_content all content_rewrite traffic passes); a durable fix belongs to bugs_open/178's lane, not inside this router. 4) Proliferation: third bespoke router — the CQ-023 tripwire directs the fourth author to the generalised engine. 5) A verifier later registered for this item_type fail-closes the converted arm (CQ-023 landmine). 6) Parked rows carry handler_agent, so dashboard Retry re-dispatches and re-parks — idempotent."
  }
}
