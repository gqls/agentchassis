{
 "submitter": "bugs_open/329 fixing thread (docs024_key_docs_latest/bugfix_329_takeover_claim)",
 "rationale": "bugs_open/329: both takeover arms of handleOrchestrationStatus decide an orchestration is stuck by a CLOCK (time.Since(state.LastActivity) > StuckOrchestrationTimeout, 300s) and then resume it with nothing claiming the row. Raised by the council's own guardian seat on migration 465's round (corr 1c212b15, APPROVED) and filed because an objection living only inside a verdict is invisible.\n\nTWO PREMISES IN THE BUG FILE ARE FALSE AND I AM CORRECTING THEM RATHER THAN BUILDING ON THEM. (1) The file states [MEASURED 2026-08-19] that SetExecutingStep/ClearExecutingStep 'end in r.UpdateState \u2014 NOT UpdateStateWithVersion, so the write does no version check'. state.go:883-885 is a one-line wrapper: 'func (r *StateRepository) UpdateState(ctx, state) error { return r.UpdateStateWithVersion(ctx, state, state.Version) }'. UpdateState IS the version CAS, so the file's fix candidate (2) is a NO-OP. (2) I myself wrote, and have retracted in the file, that every agent binary runs the arms; only cmd/agent-chassis imports platform/agentbase.\n\nSO THE DEFECT IS NOT A MISSING GUARD ON THE WRITE. It is a CHECK-THEN-ACT ACROSS TWO READS: the arm judges staleness on the CALLER'S SNAPSHOT (coordinator.go:761, :796), and the write that follows does a FRESH GetState -> mutate -> CAS that never re-evaluates the predicate. Two takers seconds apart both win, because each CASes against the version it just read. Corollary that inverts the obvious test: exactly-SIMULTANEOUS takers do NOT double-execute today (the loser's CAS fails), so a WaitGroup start-line test shows the unfixed code passing. The disconfirming case is the SEQUENTIAL interleaving.\n\nHONEST SIZING, BECAUSE THE MEASUREMENTS DO NOT SUPPORT A LARGER CHANGE. This is a correctness gap, not a fire. The dispatch_throughput lane's double-handle census over 24h [MEASURED 2026-09-03 ~11:4xZ]: 3,044 handler orchestrations, 2,911 distinct work items, 71 items with >=2 handlers, and 0 overlapping pairs on one item. 0 rows with a repeated step name in one execution_path over 7d. 0 'Found stuck orchestration' / 'stalled between steps' lines in either chassis pod's reachable log window. There are THREE guards in series and the defect sits in the middle of them: (i) the chassis intake serialisation claim, keyed on orchestration_id, agent-chassis ONLY; (ii) THE ARMS, the defect; (iii) per-path CASes such as the work-item claim on the dispatch path. Guards (i) and (iii) absorb the outcome where they apply.\n\nWHY FIX IT ANYWAY, STATED AS WHAT IT REMOVES RATHER THAN WHAT IT PREVENTS TODAY: the absorption is a BY-PRODUCT. Guard (iii) exists to make work-item claiming exclusive and protects this defect by accident; a refactor could remove it without knowing it was load-bearing. Guard (i) is disabled STRUCTURALLY for spawned pods (intake.go refuses the mode when a.spawned) - and spawned Job pods of the chassis image are the MAJORITY of orchestration drivers: processing_node over 14d [MEASURED 2026-09-03 ~11:3xZ] gives agent-chassis 3,332 against 4,900+ from agent-page-rerender 2,215, agent-build-dispatch-loop 660, agent-page-build-handler 412 and 9 more families, every one confirmed absent from kubectl get deploy. That census could have come out agent-chassis:100% and did not.\n\nTHE DESIGN IS THE MINIMAL ONE AND DELIBERATELY DOES NOT CLOSE EVERYTHING. Move the staleness decision INSIDE the version-guarded write, composed from the existing ExecuteWithOptimisticLocking plus a sentinel. No new SQL, no new column, no touch on processing_node - so the council invariant that orchestration_states' two guarded mechanisms must never share a column (state_locks_test.go, corr 4a227ed9) is preserved trivially: the new method contains no UPDATE literal at all. What it makes unrepresentable: N takers resuming one stale row. What it does NOT close: a live driver versus one taker - defaultLocalActionTimeout is 7200s (coordinator.go:1246) and NOTHING refreshes last_activity during a local action, so a driver inside a 9-minute council seat is 'stuck' by a 5-minute clock while behaving correctly. No claim taken by the takeover side can exclude a driver that holds nothing; closing that needs a driver heartbeat, which is a separate seam I am deliberately not carrying. Post-fix bound per orchestration: 2 (driver + exactly one taker), from unbounded.\n\nNOT ARCHITECTURE-SCOPE by the 2026-07-29 \u00a71 test: the shared mechanism's guarantee is NARROWED (an unconditional resume becomes a claimed one), not widened. No opt-in field per 2026-08-02 \u00a72: the fix REMOVES authority from a branch, no caller-supplied predicate licenses a widest branch, and no action input spec changes (RFC_022's N=10 optional-key budget untouched). A 'revert to clock-only' switch would be a mechanism rotting unexercised, which 2026-07-29 \u00a72 declines to require. ExecuteWithOptimisticLocking has ZERO non-test callers today; this gives it its first, and the tests exercise its conflict-retry path.",
 "plan": {
  "summary": "A takeover is CLAIMED, never ASSUMED. Move the arms' staleness predicate from the caller's snapshot into the version-guarded write, so judging and claiming are one atomic step. The winner's write bumps version and refreshes last_activity, so every later taker's fresh read declines. Composed from existing machinery only: ExecuteWithOptimisticLocking + a sentinel error. No new SQL, no new column, processing_node untouched.",
  "edits": [
   {
    "file": "platform/orchestration/coordinator.go",
    "symbol": "ErrTakeoverLost (var block, ~line 50)",
    "operation": "modify",
    "rationale": "A typed 'lost the claim' outcome the arm can distinguish from a DB error. Without it the arm cannot tell 'another actor is driving this' (return nil, drop the message, exactly as the existing non-stale branch does) from 'the database is broken' (propagate).",
    "sketch": " var (\n \tErrWaitingForResponse   = errors.New(\"orchestration is waiting for responses\")\n \tErrVersionMismatch      = errors.New(\"optimistic lock failure: version mismatch\")\n \tErrLoopExpansionHandled = errors.New(\"loop expansion handled: outer continueExecution must not continue\")\n+\t// ErrTakeoverLost: the row this caller judged stale from its SNAPSHOT was no\n+\t// longer stale on the FRESH read inside the claim -- another actor resumed it,\n+\t// or it moved on. The caller returns without executing anything (bugs_open/329).\n+\tErrTakeoverLost = errors.New(\"stale-orchestration takeover lost: the row is being driven\")\n )"
   },
   {
    "file": "platform/orchestration/state.go",
    "symbol": "ClaimStaleOrchestration (new, after ExecuteWithOptimisticLocking)",
    "operation": "add",
    "rationale": "The check-and-claim primitive. The caller judged the row stuck from a SNAPSHOT; this re-judges it on the FRESH row INSIDE the version CAS and writes only if it is still stale. The write IS the claim -- UpdateStateWithVersion already stamps last_activity=now and version+1 unconditionally, so the next caller's fresh read sees a row that is no longer stale. A lost version race is re-read by ExecuteWithOptimisticLocking and RE-JUDGED, never retried blind. Contains no UPDATE literal of its own, so state_locks_test.go's setClauseOf never sees it and the two-guarded-mechanisms invariant is untouched.",
    "sketch": "func (r *StateRepository) ClaimStaleOrchestration(ctx context.Context, orchestrationID string,\n\texpectStatus OrchestrationStatus, staleAfter time.Duration, claimedBy string) (*OrchestrationState, error) {\n\tvar claimed *OrchestrationState\n\terr := r.ExecuteWithOptimisticLocking(ctx, orchestrationID, func(fresh *OrchestrationState) error {\n\t\tif fresh.Status != expectStatus || time.Since(fresh.LastActivity) <= staleAfter {\n\t\t\treturn ErrTakeoverLost\n\t\t}\n\t\tif expectStatus == StatusExecutingStep {\n\t\t\tif fresh.CurrentlyExecuting == nil {\n\t\t\t\treturn ErrTakeoverLost\n\t\t\t}\n\t\t\tfresh.CurrentlyExecuting = nil\n\t\t\tfresh.Status = StatusRunning // what ClearExecutingStep did -- now under the claim\n\t\t}\n\t\tfresh.ProcessingHistory = append(fresh.ProcessingHistory, ProcessingRecord{...Action: \"stale_takeover_claimed\"...})\n\t\tclaimed = fresh\n\t\treturn nil\n\t})\n\tif err != nil { return nil, err }\n\treturn claimed, nil\n}"
   },
   {
    "file": "platform/orchestration/coordinator.go",
    "symbol": "takeOverStaleOrchestration (new helper)",
    "operation": "add",
    "rationale": "One call site for both arms, with named log needles (STALE_TAKEOVER_CLAIMED / STALE_TAKEOVER_LOST) that are the post-roll field meters -- there is no durable needle today, which is why 'have the arms ever fired?' is currently unanswerable beyond the log window. Returns nil on a lost claim, not an error: the message was for an orchestration another actor is driving, which is exactly the outcome of the arms' existing non-stale branch.",
    "sketch": "func (s *SagaCoordinator) takeOverStaleOrchestration(ctx context.Context, repo *StateRepository,\n\tsnapshot *OrchestrationState, execCtx *types.ExecutionContext, from OrchestrationStatus) error {\n\tclaimed, err := repo.ClaimStaleOrchestration(ctx, snapshot.OrchestrationID, from, StuckOrchestrationTimeout, s.podName)\n\tif errors.Is(err, ErrTakeoverLost) {\n\t\ts.logger.Warn(\"STALE_TAKEOVER_LOST: stale on the snapshot, driven on the fresh row -- not resuming\", ...)\n\t\treturn nil\n\t}\n\tif err != nil { return fmt.Errorf(\"stale takeover claim: %w\", err) }\n\ts.logger.Warn(\"STALE_TAKEOVER_CLAIMED: resuming an orchestration idle past the stuck threshold\", ...)\n\treturn s.continueExecution(ctx, claimed, execCtx)\n}"
   },
   {
    "file": "platform/orchestration/coordinator.go",
    "symbol": "handleOrchestrationStatus, case StatusExecutingStep (758-777)",
    "operation": "modify",
    "rationale": "The snapshot saying 'stuck' becomes a reason to TRY a takeover, not to perform one. Replaces ClearExecutingStep + reload + continueExecution, whose three separate reads are the check-then-act.",
    "sketch": " \t\tif state.CurrentlyExecuting != nil && time.Since(state.LastActivity) > StuckOrchestrationTimeout {\n-\t\t\ts.logger.Warn(\"Found stuck orchestration, taking over\", zap.String(\"stuck_step\", *state.CurrentlyExecuting))\n-\t\t\tif err := repo.ClearExecutingStep(ctx, state.OrchestrationID); err != nil { return err }\n-\t\t\tstate, err := repo.GetState(ctx, state.OrchestrationID)\n-\t\t\tif err != nil { return fmt.Errorf(\"failed to reload state: %w\", err) }\n-\t\t\treturn s.continueExecution(ctx, state, execCtx)\n+\t\t\treturn s.takeOverStaleOrchestration(ctx, repo, state, execCtx, StatusExecutingStep)\n \t\t}"
   },
   {
    "file": "platform/orchestration/coordinator.go",
    "symbol": "handleOrchestrationStatus, case StatusRunning (780-805)",
    "operation": "modify",
    "rationale": "The RUNNING arm previously wrote NOTHING at all before resuming, so two arrivals seconds apart both resumed. The long explanatory comment above it is kept; only its stale reference to ClearExecutingStep needs adjusting.",
    "sketch": " \t\tif time.Since(state.LastActivity) > StuckOrchestrationTimeout {\n-\t\t\ts.logger.Warn(\"Found orchestration stalled between steps (RUNNING), resuming\", ...)\n-\t\t\treturn s.continueExecution(ctx, state, execCtx)\n+\t\t\t// Claimed, not assumed (bugs_open/329).\n+\t\t\treturn s.takeOverStaleOrchestration(ctx, repo, state, execCtx, StatusRunning)\n \t\t}"
   },
   {
    "file": "platform/orchestration/state.go",
    "symbol": "ClearExecutingStep",
    "operation": "remove",
    "rationale": "Its SOLE caller was the arm being replaced (repo-wide grep 2026-09-03: coordinator.go:765 plus one comment reference at :781). A helper with no callers reads as a finished refactor, which this estate has been bitten by before. SetExecutingStep is untouched -- it retains callers at :746 and :896.",
    "sketch": "-// ClearExecutingStep clears the currently executing step\n-func (r *StateRepository) ClearExecutingStep(ctx context.Context, orchestrationID string) error {\n-\t... GetState -> CurrentlyExecuting = nil -> Status = StatusRunning -> UpdateState\n-}"
   },
   {
    "file": "platform/orchestration/stale_takeover_claim_test.go",
    "symbol": "five arm-level tests (helper-level refusal tests ship in claim_stale_orchestration_test.go alongside)",
    "operation": "add",
    "rationale": "ROUND 2 \u2014 these are now RUN, not proposed. Written to reference only pre-existing symbols so they compile against unfixed HEAD, and watched to FAIL there: `scripts/verify-head-builds.sh --with platform/orchestration/stale_takeover_claim_test.go --test ./platform/orchestration/` at HEAD f9aa97a97 fails all four discriminating tests, and the failure messages name the write that must not have happened. Isolation from the guards in series: the tests call handleOrchestrationStatus directly (below the intake claim, above any action-level CAS) and sqlmock rejects any unexpected statement, so the expectation list IS the span's complete DB traffic.",
    "sketch": "AT UNFIXED HEAD (verbatim from the run):\n  TestStaleExecutingStepTakeoverIsRefusedWhenTheFreshRowIsBeingDriven FAIL --\n    'call to ExecQuery UPDATE orchestration_states ... {Ordinal:1 Value:RUNNING} ... {Ordinal:20 Value:10} {Ordinal:22 Value:9} was not expected'\n    i.e. the arm caught writing status=RUNNING version=10 over a row another actor claimed at v9.\n  TestStaleRunningResumeIsRefusedWhenTheFreshRowIsBeingDriven FAIL -- same, writing status=EXECUTING_STEP.\n  TestSimultaneousTakersOnlyOneClaims FAIL -- 'optimistic lock failure' propagates out: the INVERTED-TEST corollary demonstrated, not asserted.\n  TestStaleTakeoverClaimsAndResumesWhenNobodyElseHas FAIL -- needle absent; unfixed code writes action:'state_updated'.\n  TestFreshRowsAreNeverTakenOver PASSES at HEAD -- correct: the NEGATIVE CONTROL does not discriminate fixed from unfixed, it exists so a fix that refuses EVERYTHING cannot pass the other four.\nWITH THE FIX: ok github.com/gqls/agentchassis/platform/orchestration"
   },
   {
    "file": "docs/agent_docs/sql_for_agents/736_running_written_by_follows_the_329_claim.sql",
    "symbol": "orchestration_status_vocabulary.RUNNING.written_by",
    "operation": "add",
    "rationale": "ROUND 2, ADDED BECAUSE THE COUNCIL'S OBJECTION FOUND SOMETHING REAL. guardian [low] and prior_art_librarian [medium] both asked what a caller of the deleted ClearExecutingStep would look like if the compiler could not see it. `go build ./...` is clean and there is no reflection on StateRepository \u2014 but grepping OUTSIDE .go found a LIVE DATABASE ROW: migration 466's orchestration_status_vocabulary seeds a `written_by` column, and RUNNING's row names 'StateRepository.ClearExecutingStep (state.go:1428)'. It could never break a build and nothing would ever have flagged it. \u26a0 The row is DOCUMENTATION, not a dispatch target \u2014 the reaper and database-cleanup read is_terminal/is_pausable, untouched \u2014 so this is a correctness fix to a reference an engineer will trust, not a behavioural change. The verify block RAISEs rather than SELECTs, because ON_ERROR_STOP ignores a non-empty result and a block of bare SELECTs cannot stop a COMMIT.",
    "sketch": "UPDATE orchestration_status_vocabulary\n   SET written_by = 'StateRepository.ClaimStaleOrchestration (state.go, the EXECUTING_STEP arm of the claim)',\n       notes      = 'The inter-step gap. Written inside the stale-takeover CLAIM, under the version CAS (bugs_open/329, WFA-025) - previously by ClearExecutingStep, ... deleted. ...'\n WHERE status = 'RUNNING';\nDO $do$ ... IF got LIKE '%ClearExecutingStep%' THEN RAISE EXCEPTION ... END $do$;\n(+ _ROLLBACK sibling restoring 466's original text)"
   }
  ],
  "grounded_in": [
   "platform/orchestration/coordinator.go:761 (EXECUTING_STEP arm): 'if state.CurrentlyExecuting != nil && time.Since(state.LastActivity) > StuckOrchestrationTimeout {' -- followed by ClearExecutingStep, a fresh GetState, and continueExecution, none of which re-tests the predicate.",
   "platform/orchestration/coordinator.go:796 (RUNNING arm): 'if time.Since(state.LastActivity) > StuckOrchestrationTimeout {' -- followed directly by 'return s.continueExecution(ctx, state, execCtx)', writing NOTHING before resuming.",
   "platform/orchestration/state.go:883-885: 'func (r *StateRepository) UpdateState(ctx context.Context, state *OrchestrationState) error { return r.UpdateStateWithVersion(ctx, state, state.Version) }' -- REFUTES bugs_open/329's [MEASURED 2026-08-19] claim that these writes do no version check, and makes its fix candidate (2) a no-op.",
   "platform/orchestration/state.go:1378-1380 (TakeOverOrchestration doc): 'It deliberately leaves `version` alone: this is bookkeeping, not a state transition, and must never collide with UpdateStateWithVersion's optimistic lock.' -- why candidate (1), routing the arms through that CAS, is the wrong primitive here.",
   "platform/orchestration/state_locks_test.go header: 'orchestration_states has TWO guarded-update mechanisms and they must never govern the same column ... If either ever writes the other's columns, two locks race on one field and neither is authoritative.' -- the invariant this design preserves by containing no UPDATE literal at all.",
   "platform/orchestration/coordinator.go:1246: 'defaultLocalActionTimeout = 7200 * time.Second' with every last_activity refresh site (:1039,:2220,:2850,:2914,:3441,:3975) being a step transition -- so a live driver inside a long step is 'stuck' by a 300s clock, and this is the residual the fix explicitly does NOT close.",
   "platform/agentbase/intake.go:75 and :215-224: the intake serialisation claim is refused when a.spawned, and keys on the orchestration_id -- so the guard that absorbs this defect on static chassis pods is structurally absent on spawned Job pods.",
   "orchestration_states.processing_node over 14d [MEASURED 2026-09-03 ~11:3xZ]: agent-chassis 3,332 against agent-page-rerender 2,215, agent-build-dispatch-loop 660, agent-page-build-handler 412, agent-internal-link-resolver 392, agent-page-content-writer 392, agent-asset-deployer 236 and 9 more families -- every family below the first absent from 'kubectl get deploy', i.e. spawned pods, i.e. unguarded.",
   "dispatch_throughput lane's double-handle census [MEASURED 2026-09-03 ~11:4xZ]: 3,044 handlers, 2,911 items, 71 items with >=2 handlers, 0 overlapping pairs on one item -- the honest bound: this is a correctness gap, not a fire, and the fix is sized as one."
  ],
  "risks": "1) THE OBVIOUS TEST IS INVERTED, so a careless reviewer or a later maintainer will 'prove' the unfixed code correct. Exactly-simultaneous takers do NOT double-execute today -- the loser's version CAS fails and the arm returns the error. The disconfirming case is the SEQUENTIAL interleaving (taker A wins and refreshes last_activity; taker B arrives seconds later on a stale snapshot). Both test files are written around the sequential case and say so in their headers.\n\n2) A GUARD IN SERIES CAN PASS THE TEST FOR ME. There are three layers: the chassis intake serialisation claim above, the arms, and per-path CASes below (the work-item claim on the dispatch path). Any verification on the dispatch path is worthless -- guard (iii) absorbs a double-takeover even with the fix reverted. The tests call handleOrchestrationStatus directly, below the intake claim and above any action-level CAS, and sqlmock's reject-on-unexpected-statement makes the expectation list the complete DB traffic of the span.\n\n3) BEHAVIOURAL CHANGE VISIBLE TO OTHERS: a message arriving at a stale row that another actor has just resumed is now DROPPED with STALE_TAKEOVER_LOST (return nil, offset committed) instead of resumed. This is the same disposition as the existing non-stale branch, so it is not a new class of drop -- but if the claim predicate were ever wrong in the refusing direction, recovery of genuinely abandoned rows would silently stop. That is what the positive control test exists for, and why the CLAIMED needle is logged at Warn.\n\n4) WHAT IT DOES NOT CLOSE, stated so no one reads more into it: driver-versus-taker. defaultLocalActionTimeout is 7200s and nothing refreshes last_activity during a local action, so a live driver on a 9-minute step is 'stuck' by the 300s clock. No claim on the takeover side can exclude a driver that holds nothing. Post-fix bound is 2 (driver + one taker), down from unbounded. Closing it needs a driver heartbeat -- a separate seam, deliberately not carried here.\n\n5) ExecuteWithOptimisticLocking gains its FIRST non-test caller (0 today, grep 2026-09-03). Its retry loop is 12 attempts; a hot row could in principle exhaust them, which surfaces as an error rather than a double-execution -- the safe direction. The tests exercise the conflict-retry path explicitly.\n\n6) NOT MINE TO TOUCH: claim_work_item_action.go (another lane's seam, now carrying an opt-in honour_spend_governor pre-claim read) and intake_workers.go (the 180s lease). Neither is in the edit list. The 180s-lease-vs-300s-timeout ordering is recorded as a finding in bugs_open/329 \u00a75; the dispatch_throughput lane was offered it and declined it on the ground that they have never measured it, so it is attributed to nobody.",
  "revision_notes": "ROUND 2, answering round 1 (REVISE, gated by prior_art_librarian; 8 of 11 seats approved, INCLUDING architecture \u2014 so the not-architecture-scope reading held). Every objection is answered with evidence or with work; none is argued down.\n\n1) prior_art_librarian [HIGH, edit 2] \u2014 'the design's entire correctness rests on ExecuteWithOptimisticLocking reloading AND re-invoking the mutator closure each attempt; the sibling UpdateStateWithRetry is landmined for the OPPOSITE behaviour; if it shares that defect the plan ships the bug it claims to close.' THE SEAT WAS RIGHT TO DEMAND THE LINE, AND THE ANSWER IS A CITATION, NOT A CHANGE. state.go:1417-1437: `for i := 0; i < maxRetries; i++ { state, err := r.GetState(ctx, orchestrationID)` (:1421, FRESH read every attempt) ... `currentVersion := state.Version` (:1426) ... `if err := fn(state); err != nil { return err }` (:1429, the closure is RE-INVOKED on that fresh state) ... `err = r.UpdateStateWithVersion(ctx, state, currentVersion)` (:1434, CAS against THAT attempt's version). Contrast the landmined sibling, state.go:1079-1100: UpdateStateWithRetry calls UpdateState on the CALLER'S pointer and, on conflict, reloads into `reloaded` and overwrites \u2014 which is exactly why local mutations are discarded there, and exactly why this design uses the other function. Two further points the seat did not ask for but which close the question: ClaimStaleOrchestration's closure recomputes `claimed = fresh` on every attempt, so a retry can never return a stale pointer; and the retry path is not merely asserted safe, it is EXERCISED by TestSimultaneousTakersOnlyOneClaims (CAS returns 0 rows -> re-read -> predicate now false -> stand down), which FAILS against unfixed HEAD.\n\n2) prior_art_librarian [MEDIUM] + guardian [LOW] \u2014 the ClearExecutingStep 'sole caller' absence claim, and specifically a caller the compiler cannot see. Evidence, three ways: `go build ./...` across the WHOLE repo is clean with the function deleted; `grep -rn ClearExecutingStep --include=*.go` returns only four COMMENT lines and no call; and there is no reflection on StateRepository (`MethodByName` / `reflect.ValueOf(repo)` : zero hits under platform/, internal/, cmd/). \u26a0 BUT THE OBJECTION FOUND A REAL ONE. Grepping outside .go turned up a live DATABASE row \u2014 orchestration_status_vocabulary.RUNNING.written_by names 'StateRepository.ClearExecutingStep (state.go:1428)', seeded by migration 466. Not a caller, invisible to the compiler, and now false. New edit 8 fixes it. This is what the round bought.\n\n3) prior_art_librarian [LOW] \u2014 'ExecuteWithOptimisticLocking has ZERO non-test callers' is an absence claim. Confirmed and now superseded: `grep -rn 'ExecuteWithOptimisticLocking(' --include=*.go .` excluding the declaration returns exactly ONE hit, state.go:1506 \u2014 this fix's own call. It had none before; it has one now.\n\n4) bug_historian [MEDIUM, edit 4] \u2014 the residual (a live driver inside a long local action judged stuck) is an incomplete fix of a class this platform has documented repeatedly (bugs_closed/038, /040, 016b \u00a79), and on a content-writing workflow a driver mid-write racing a taker is a plausible silent-content-loss vector. ACCEPTED IN FULL, AND ACTED ON RATHER THAN RE-DISCLOSED: the residual is now bugs_open/461, filed with the mechanism read at the code (the 24x gap between defaultLocalActionTimeout 7200s and StuckOrchestrationTimeout 300s; all six last_activity refresh sites are step transitions), with the seat's argument quoted, with widening the threshold REJECTED on the ground that no value is both safe and useful, and with the heartbeat candidate flagged as ARCHITECTURE-SCOPE (a third guarded mechanism on orchestration_states) rather than a bug patch. \u26a0 One thing that changes the seat's calculus and is recorded in 461: the instrument to SIZE this did not exist until this fix. processing_history action 'stale_takeover_claimed' is the first durable record a takeover has ever left \u2014 before it, 'have the arms ever fired?' was answerable only inside a live log window. 461 says explicitly not to size the bug until that query has data, and that today it necessarily returns 0 because the fix is not yet rolled.\n\n5) guardian [MEDIUM, edit 4] \u2014 wants the 'has this core site been sent upward repeatedly' precedent confirmed rather than assumed. Two things: the ARCHITECTURE seat approved this round, on the 2026-07-29 \u00a71 reading that the shared mechanism's guarantee is NARROWED, not widened; and the change was deliberately sized to the minimum for exactly this reason \u2014 no new column, no new SQL in the Go path, no third guarded mechanism, processing_node untouched, and the one deletion is of a function whose only caller was the code being replaced. The larger option (a lease column) is declined IN THIS PLAN and routed to bugs_open/461 as architecture-scope.\n\n6) guardian [LOW, edit 1] \u2014 STALE_TAKEOVER_LOST returns nil where the arm previously resumed. Correct, and it is the one behavioural change visible outside the diff. Why it is not a new class of drop: the arms' pre-existing non-stale branch ALREADY returned nil on exactly this shape (a message for a row somebody else is driving), so the disposition is unchanged and only the branch reaching it is new. Two controls stand behind it: TestStaleTakeoverClaimsAndResumesWhenNobodyElseHas fails if the predicate ever refuses a genuinely abandoned row, and TestFreshRowsAreNeverTakenOver fails if it claims one it should not. And the drop is now OBSERVABLE \u2014 STALE_TAKEOVER_LOST at Warn \u2014 where before this branch left no trace at all.\n\n7) editquality [approve, with a note on edit 6] \u2014 same ClearExecutingStep evidence question; answered by (2).\n\nALSO CORRECTED IN ROUND 2, found while answering (1) and not raised by any seat: a comment I wrote in ClaimStaleOrchestration said UpdateStateWithVersion 'bumped Version and LastActivity on this pointer'. It bumps Version in memory (state.go:1074) and stamps last_activity in the DATABASE from a local `now` (state.go:1051) \u2014 it does NOT write LastActivity back onto the struct. Harmless to the design (the next taker reads the database, which is where the refresh lives, and that is all the exclusion needs) but false as written, in the one place a reader would rely on it. Fixed.\n\nSTATE OF THE CODE: rounds 1 and 2 are BOTH already committed (b55f837ef, then 9b7197866 for the comment fix and 461), per the 2026-07-30 rule that a shared HEAD cannot hold code for a verdict; the commit carries Council-Submitted. The migration in edit 8 is written and NOT yet applied."
 }
}