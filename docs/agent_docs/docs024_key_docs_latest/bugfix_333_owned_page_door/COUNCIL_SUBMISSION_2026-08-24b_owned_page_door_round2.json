{
  "submitter": "bugs_open/333 lane \u2014 policy routability at the door, ROUND 2 (2026-08-24), resubmitted on the same correlation",
  "rationale": "ROUND 2. Round 1 came back REVISE on a gating HIGH from `editquality`: the door keys on the site_work_items.page_id COLUMN, and a standing landmine says that column is NULL on most rows \u2014 so the door might silently skip the majority of the population the diagnosis measured. That was the right question and it is now measured, not argued.\n\n**THE HIGH, ANSWERED WITH A CENSUS [MEASURED 2026-08-24, live + archive, all history at page-build-handler].** 3,819 of 5,259 rows carry page_id (72.6%) \u2014 so the landmine is correct about the column. But split by producer, the CONTENT-finding producers this bug is about are at or near 100%: completeness-discovery-agent 597/597, content-gap-planner 433/433, generic 247/247, tool-generator 230/230, tool-suggester 126/126, tool-deployer 77/77, backfill-353 74/74, quality-discovery-agent 57/57, offer-analysis 41/41, design-audit 1469/1508 (97%). The 0% producers are a different KIND of item \u2014 name-only ACTION REQUESTS filing needs_page for a page identified by name (page-rerender 0/399, reconcile_site_plan 0/384, image-build-handler 0/262, render_directory 0/92, render_news_section 0/80, plan_sections 0/33). On the actual measured defect population the door sees 83 of 88 (94%). Of the 1,440 page_id-NULL rows, 1,438 carry spec.page_name and so are resolvable \u2014 a bounded, quantified gap now stated as a named non-scope item rather than left as prose. **The same objection's MEDIUM is answered by construction: the door reads page_id and the cited figures join `pages p ON p.id = w.page_id`. Same column, same population, by construction rather than by coincidence.**\n\n**TWO OBJECTIONS CHANGED THE CODE (commit 1789489bf).**\n(1) `guardian` [medium] \u2014 the declaration probe is the NOVEL SQL (jsonb_path_exists over default_config) and it was running on every page-bearing dispatchable write through a seam ~26 producers share; a bad jsonpath on an older Postgres, or a permissions change, would abort the transaction for ALL of them. **The probe order is now inverted: the boring statement gates the novel one.** A pages PK read runs first; the jsonpath runs only for an owned page \u2014 176 pages estate-wide, so a small minority of writes. Cost is a 2.7 ms PK read on the common path where there was a 0.278 ms index scan, which is precisely the trade the objection asked for.\n(2) `bug_historian` [low] \u2014 fail-open is now COUNTABLE: both stand-down branches log the stable literal `OWNED_PAGE_DOOR_PROBE_FAILED`. Deliberately a log line and NOT an agent_error_log row, because that write would go through the same transaction \u2014 the one failure mode it must survive is exactly the one that would break it.\n\n**THREE MORE WERE CHECKABLE AND WERE CHECKED, NOT ARGUED.**\n\u2022 `prior_art_librarian` [medium]: the jsonpath is blind to sub_workflow-nested config, so 'exactly ONE live agent declares it' may be false. **REFUTED by the widest probe available** \u2014 `$.**.refuse_owned_page ? (@ == true)`, with no is_active and no snapshot filter, returns the SAME single agent (page-build-handler). The positive control holds under a probe that could not have missed a nested declaration.\n\u2022 `guardian` [medium] on raiseToolContentItem's new return string: **no caller switches on it.** Both call sites (create_tool_component_action.go:571, deploy_tool_action.go:557) assign it and pass it straight into a result map key 'content_item'; verified by grepping every use of the variable, not just the call.\n\u2022 `reuse_agent` [medium]: **no generic 'handler declares boolean flag X in workflow.steps.*.config' helper exists** \u2014 grepped jsonb_path_exists and 'workflow,steps' across platform/, internal/ and cmd/. This is the first of its kind; the register entry now says that a SECOND such flag should parameterise this helper rather than copy it.\n\u2022 `constitution` [low]: handlerExpr is a placeholder or column reference at every call site, never user data \u2014 the same posture as workItemHandlerRegisteredSQL, which it mirrors.\n\n**TWO OBJECTIONS I AM ANSWERING WITH A RECORD RATHER THAN CODE, AND SAYING SO.**\n\u2022 `bug_historian` [medium] \u2014 the 9 raw-INSERT bypassers keep the identical failure mode and disclosure is not a fix. Agreed that it is not a fix. It is now a named non-scope item in bugs_open/333 with its measured size, and the register entry lists the bypassers BY PATH with the warning that the set grows unobserved. A promoter-side backstop for the ones born `detected` is the real remedy and wants its own round; taking it here would widen a shared-seam change while a REVISE is open on the seam itself.\n\u2022 `architecture` [medium] \u2014 `deferred` is now overloaded a third way, disambiguated only by an error-string prefix and spec keys. Recorded in the register entry in the terms the seat asked for: **a FOURTH overload must introduce a real reason-code column instead of another string convention.**\n\n**AND THE MISSING ARTEFACT `guardian` NAMED \u2014 the other consumers were TOLD, not merely measured** (owner ruling 2026-07-29 \u00a73). Notified with the mechanism and what changes for them: the bugs_open/326 lane (colliding in this same function \u2014 sequenced, they landed on top and pinned both of my constraints in their own tests), bugs_open/367 (their from_rfm rows now park; they verified my claims first-hand and froze their own '28 of 31 failed' figure in five documents), the webdesign tool-rebuilds lane (largest producer), and the staged_component_build/353 lane (whose reply then gave me a demand-control warning I did not have: cross-link emission has been at ZERO since 08-21, so an empty parked bucket after the roll will not mean the door is inert). Recorded in the bug file's 'Consumers told' section and the lane RUNBOOK.\n\nEverything below is unchanged from round 1 except edit 4 (probe order + the countable fail-open) and edit 6 (the test file follows the new order; both mutations re-run and both still discriminate \u2014 disarming the door fails 3 tests, re-typing the parked row fails the retraction assertions). Verified against committed HEAD 47d9d9198 with verify-head-builds.sh --test.\n\n--- ROUND 1 RATIONALE FOLLOWS, UNCHANGED ---\n\nbugs_open/333: ~26 producers file content findings at handler_agent='page-build-handler' and none of them, nor the shared writeWorkItem seam most pass through, reads pages.rebuild_policy. A finding on a rebuild_policy='owned' page is therefore routed at the one handler forbidden to touch it. Since bugs_closed/301 the refusal is cheap, but the OUTCOME is the defect: the item terminates 'wont_fix' \u2014 the status meaning 'we decided not to fix this' \u2014 on a page with a real detector-found defect; idx_swi_dedup excludes wont_fix so the detector re-files it; and the only human-visible trail (owned_page_review) dedups per PAGE, so the SECOND distinct finding on an already-reviewed page leaves no trace at all. [MEASURED 2026-08-24 14:15Z, live table] 83 findings ended that way since 2026-08-19, five of them that day, the most recent at 11:48Z. This is the untaken fix candidate 3 of TWO closed files (bugs_closed/295, bugs_closed/301) and the owner filed it as its own bug on 2026-08-19 (decision (a)).\n\nTHE FIX is candidate 1: a policy check at the shared write door, beside the registration check bugs_open/291 already put there. Three design decisions the council should weigh:\n\n(1) THE DOOR READS THE HANDLER'S OWN DECLARATION, NOT A GO LIST OF NAMES. page-build-handler opts into refusing owned pages through config (migration 488 sets refuse_owned_page on its load_page_record step). A Go slice of handler names would be another hand-kept copy of a fact the DB holds, and wrong in the dangerous direction the day a handler opts in. Positive control, and it could have come out otherwise: [MEASURED 2026-08-24] exactly ONE live agent declares it (page-build-handler), while page-rerender (5,040 completions on owned pages, live+archive), section-editor (44 complete/1 failed) and tool-generator (43) do not \u2014 which is what makes them safe to leave alone.\n\n(2) THE PARKED ROW KEEPS ITS OWN item_type AND item_key, taking only bugs_closed/077's SIGNAL (status='deferred', empty handler, gap_kind, builder_needed). The first draft followed 077 literally and re-typed the row to capability_gap. That was WRONG and a red-team pass caught it before any code was written: detectors retract their own findings via resolveWorkItems, which matches (item_type, item_key), and 'deferred' is NOT in workItemClosedStatuses \u2014 so a row keeping its identity IS retracted normally when the page is later repaired, whereas a re-typed one would sit at deferred for ever holding its dedup slot, undispatchable and un-refilable at once. That is the exact hole two council seats caught on bugs_open/342 one day earlier. Keeping the key also delivers the bug's stated requirement \u2014 counted PER FINDING, not per page \u2014 for free. The roadmap sweep (diagnose_triage_action.go:355-365, fixloop_digest_action.go:358) reads '(item_type=capability_gap OR status=deferred)' grouped by builder_needed, so the consumer still sees it.\n\n(3) 'deferred', NOT 'blocked' AND NOT 'needs_human_review'. blocked self-heals \u2014 feasibility-recheck promotes any blocked row whose handler exists, every 600s, wiping error. needs_human_review holds 1,087 rows and the owner has ruled that queue should not fill (bugs_open/033). deferred is the estate's parking state: nothing promotes it, it holds its dedup slot so the detector cannot churn, and it already has a reader.\n\nSCOPE HONESTY: this does NOT repair owned pages. It converts 'silently refused and forgotten' into 'visibly parked with its reason and the route that does work'. What repairs an owned page is bugs_open/277's question and the owner ruled 08-19 that the two must not be merged. Nine raw-INSERT writers bypass the seam (admin handlers, tool guide pages, write_audit_findings) \u2014 for those the handler's own cheap refusal remains the backstop, exactly as claim's branch is 291's. Named, not silently dropped.",
  "plan": {
    "summary": "Park a content finding at 'deferred' with no handler, keeping its item_type/item_key, when its page is rebuild_policy='owned' AND its handler declares refuse_owned_page \u2014 checked once at the shared writeWorkItem door instead of in ~26 producers. Ships ARMED with a redeploy-free kill switch.",
    "edits": [
      {
        "file": "platform/orchestration/actions/discovery_checks/remit.go",
        "operation": "add",
        "rationale": "One definition of the declaration predicate, in the package that already owns the routability renderer (import direction: actions imports discovery_checks, never the reverse). A scalar subquery, not an EXISTS, because the question is the VALUE of a config property, not the existence of a row \u2014 and it is deliberately NOT the registration predicate, which work_item_routability_single_definition_test.go requires to have exactly one definition.",
        "sketch": "const OwnedPageRefusalDeclarationPath = `$.workflow.steps.*.config.refuse_owned_page ? (@ == true)`\nfunc HandlerDeclaresOwnedPageRefusalSQL(handlerExpr string) string {\n  return \"COALESCE((SELECT bool_or(jsonb_path_exists(ad.default_config, '\" + OwnedPageRefusalDeclarationPath + \"')) FROM agent_definitions ad WHERE ad.type = \" + handlerExpr + \" AND ad.deleted_at IS NULL AND ad.is_active AND COALESCE(ad.is_snapshot, false) = false), false)\"\n}"
      },
      {
        "file": "platform/orchestration/actions/work_items_common.go",
        "operation": "add",
        "rationale": "Thin delegation (mirroring workItemHandlerRegisteredSQL) plus the status set the door triggers on. workItemStatusHeadsForDispatch is a NEW function, not a widening of workItemStatusRequiresRegisteredHandler: widening that one demotes the flag-only findings to blocked, which is bugs_closed/284's regression and is pinned by 291's own test. 'detected' is IN the new set and OUT of the old because registration is re-judged at promotion (WDS-017) while ownership is re-judged by NOBODY.",
        "sketch": "func workItemHandlerRefusesOwnedPagesSQL(e string) string { return checks.HandlerDeclaresOwnedPageRefusalSQL(e) }\nfunc workItemStatusHeadsForDispatch(status string) bool {\n  return status == \"detected\" || workItemStatusRequiresRegisteredHandler(status)\n}"
      },
      {
        "file": "platform/orchestration/actions/owned_page_guard.go",
        "operation": "modify",
        "rationale": "Extract the ownership SQL so a caller holding a *sql.Tx runs the SAME statement inside its own transaction \u2014 the file's 'ONLY place a pipeline may read ownership policy' comment stays literally true. It returns the ERROR rather than a checked bool because the two callers want opposite things from ErrNoRows: the composition guards treat any error as unreadable (ERROR log, counted), while the door treats a page that does not resolve as 'not owned' (a work item can legitimately carry a stale spec.page_id). The nil check stays in the *sql.DB wrapper, before boxing, to avoid the typed-nil trap.",
        "sketch": "func readRebuildPolicy(ctx context.Context, q dbQueryer, pageID uuid.UUID) (string, error)\n// pageIsOwnedForGuard keeps its signature and posture, delegating."
      },
      {
        "file": "platform/orchestration/actions/load_work_item_actions.go",
        "operation": "modify",
        "rationale": "ROUND 2 CHANGE. The door itself, plus the pure transform. It runs FIRST, before the anti-churn brake: the item_key's history is full of this finding's own refusals (112 failed rows on owned pages at that handler), so brake-first would brand the third refusal '[unresolved after 2 attempts]' \u2014 terminal, slot freed \u2014 before the door ever saw a dispatchable status. 291's registration block runs after both and correctly skips a 'deferred' row. Probe order is declaration-first so the common case pays one indexed lookup and never reads pages. Demote, never refuse (291's rule): discovery sweeps log-and-continue on insert error, so a refusal here loses the finding to a pod log. The error column reuses the existing conditional extraCols/argN append, so the 16-arg callers stay byte-identical. PROBE ORDER INVERTED after guardian's medium: the pages PK read (the boring statement) now gates the jsonb_path_exists (the novel one), so the novel SQL runs only for an owned page \u2014 176 estate-wide \u2014 instead of on every page-bearing write through a seam ~26 producers share. Both stand-down branches now log the stable literal OWNED_PAGE_DOOR_PROBE_FAILED so a fail-open is countable; deliberately a log line and not an agent_error_log row, since that write would ride the same transaction.",
        "sketch": "ownedParked := false; ownedParkedError := \"\"\nif item.pageID != nil && *item.pageID != uuid.Nil && item.handlerAgent != \"\" &&\n   workItemStatusHeadsForDispatch(item.status) && os.Getenv(\"DISABLE_OWNED_PAGE_DOOR_DEMOTION\") == \"\" {\n\tpagePolicy, policyErr := readRebuildPolicy(ctx, tx, *item.pageID)   // BORING SQL FIRST\n\tswitch {\n\tcase errors.Is(policyErr, sql.ErrNoRows):\n\t\tlogger.Debug(\"policy door \u2014 page_id does not resolve, treating as generic\")\n\tcase policyErr != nil:\n\t\tlogger.Warn(\"OWNED_PAGE_DOOR_PROBE_FAILED rebuild_policy unreadable \u2014 standing down\")\n\tcase pagePolicy == ownedRebuildPolicy:\n\t\tvar refusesOwned bool                                            // NOVEL SQL, owned pages only\n\t\tif probeErr := tx.QueryRowContext(ctx,\n\t\t\t\"SELECT \"+workItemHandlerRefusesOwnedPagesSQL(\"$1\"), item.handlerAgent,\n\t\t).Scan(&refusesOwned); probeErr != nil {\n\t\t\tlogger.Warn(\"OWNED_PAGE_DOOR_PROBE_FAILED declaration probe failed \u2014 standing down\")\n\t\t} else if refusesOwned {\n\t\t\titem, ownedParkedError = ownedPageParkedItem(item)\n\t\t\townedParked = true\n\t\t}\n\t}\n}"
      },
      {
        "file": "platform/orchestration/actions/work_item_owned_page_door_test.go",
        "operation": "add",
        "rationale": "ROUND 2: follows the new probe order; the two shared helpers now script one PK read for the generic case (the declaration probe is no longer reached). Mutation-proven both ways: disarming the door fails the positives; re-typing/re-keying the parked row fails the retraction-contract assertions (both run, both caught). Discriminating negative: an owned page at page-rerender (the 5,040-completion route) is untouched. THE TRAP THIS FILE DOCUMENTS: the door fails open, so sqlmock's unexpected-query error is swallowed and a test that omits the probe expectations passes having proved nothing. Instrumenting the door found 21 such cases across 8 existing files; all are re-scripted here via two shared helpers, and every discriminating negative calls ExpectationsWereMet.",
        "sketch": "TestWriteWorkItem_OwnedPage_ParkedAtDeferred (detected+triaged)\nTestWriteWorkItem_OwnedPage_NonDeclaringHandler_Untouched\nTestOwnedPageParkedItem_KeepsIdentityTakesTheSignal\nTestStatusHeadsForDispatch_IsRegistrationsSetPlusDetected\nTestOwnedPageRefusalPathMatchesMigration488 (reads the migration's own SQL; resolves it by SLUG because 488 is a DUPLICATED number)"
      },
      {
        "file": "platform/orchestration/actions/create_work_item_action.go",
        "operation": "modify",
        "rationale": "Producer honesty (bugs_open/177's lesson: a producer whose metric counts a FILING as success reports 100% while filing items unsatisfiable at birth). Two additive result keys beside the existing born_blocked, so a config-driven producer can tell 'filed and dispatchable' from 'filed and parked' without a second query. handler_agent keeps reporting what the CONFIG asked for \u2014 that is the contract every existing consumer reads.",
        "sketch": "\"owned_page_parked\": w.OwnedPageParked,\n\"row_status\":        effectiveRowStatus(status, w),\n\nfunc effectiveRowStatus(requested string, w workItemWrite) string {\n\tswitch {\n\tcase w.BornBlocked:\n\t\treturn \"blocked\"\n\tcase w.OwnedPageParked:\n\t\treturn ownedPageParkedStatus\n\tdefault:\n\t\treturn requested\n\t}\n}"
      },
      {
        "file": "platform/orchestration/actions/create_tool_cross_link_items.go",
        "operation": "modify",
        "rationale": "withWorkItemTx collapses the write outcome to a bool, so a producer cannot see a park. Added as a SIBLING that returns the full workItemWrite, with the bool version delegating \u2014 a widened signature would land this change in the diffs of three files owned by other lanes for no benefit to them.",
        "sketch": "func withWorkItemTx(ctx context.Context, db *sql.DB, logger *zap.Logger, item workItem) (bool, error) {\n\tw, err := withWorkItemTxWrite(ctx, db, logger, item)\n\treturn w.Inserted, err\n}\n\nfunc withWorkItemTxWrite(ctx context.Context, db *sql.DB, logger *zap.Logger, item workItem) (workItemWrite, error) {\n\ttx, err := db.BeginTx(ctx, nil)\n\t...\n\tw, err := writeWorkItem(ctx, tx, item, dropOnConflict, logger)\n\t...\n\treturn w, nil\n}"
      },
      {
        "file": "platform/orchestration/actions/tool_content_item.go",
        "operation": "modify",
        "rationale": "The tool pipeline is the LARGEST single producer of owned-page findings at page-build-handler (57 of ~260 lifetime, the bug's census): it asks the generic builder for prose around a widget on a page the same pipeline marked owned. That design conflict is the bug's fix candidate 2 and belongs to the tool lane \u2014 not taken here. What IS taken: the step result stops calling the filing a raise, because 'raised' is what made this class look healthy while nothing happened.",
        "sketch": "w, err := withWorkItemTxWrite(ctx, params.DB, logger, workItem{...})\nif w.OwnedPageParked {\n\tlogger.Warn(\"raiseToolContentItem: page is rebuild_policy=owned \u2014 content item PARKED, not raised\")\n\treturn \"parked_owned_page\"\n}\ninserted := w.Inserted"
      }
    ],
    "grounded_in": [
      "bugs_open/333: 'none of them, and not the shared write seam they mostly pass through, reads pages.rebuild_policy'",
      "bugs_open/333 fix candidate 1: 'Policy routability at the door \u2014 in writeWorkItem, beside the 291 check ... Demote, never refuse (291's rule) ... counted per finding, not per page'",
      "work_items_common.go resolveWorkItems: 'UPDATE site_work_items ... WHERE site_id=$3 AND item_type=$4 AND ($5=\\'\\' OR item_key=$5) AND status NOT IN (workItemClosedStatuses)' \u2014 deferred is not a closed status, which is why keeping the identity preserves retraction",
      "work_items_common.go workItemStatusRequiresRegisteredHandler: 'Widening this set to those statuses would demote every one of them to blocked \u2014 recreating bugs_closed/284 inside 291's fix'",
      "load_work_item_actions.go (291 door): 'Demote, never refuse: a refusal here would lose the finding to a pod log \u2014 discovery sweeps log-and-continue on insert error'",
      "load_work_item_actions.go: 'WIDENING A SHARED STATEMENT BREAKS CALLERS NOBODY EDITED ... sqlmock matches the argument COUNT'",
      "diagnose_triage_action.go:361: 'WHERE (item_type = \\'capability_gap\\' OR status = \\'deferred\\')' \u2014 the parked row's consumer",
      "migration 488: jsonb_set(default_config, '{workflow,steps,load_page_record,config,refuse_owned_page}', 'true')",
      "[MEASURED 2026-08-24, live DB] declaration probe EXPLAIN ANALYZE: Index Scan using idx_agent_definitions_type_version, Execution Time 0.278 ms warm / 4.204 ms cold, 33 shared buffer hits, 216 rows in table",
      "[MEASURED 2026-08-24, live+archive] outcomes on rebuild_policy='owned' pages: page-build-handler 112 failed/85 wont_fix/36 unresolved; page-rerender 5040 complete; section-editor 44 complete/1 failed",
      "[MEASURED 2026-08-24, live DB] SELECT type FROM agent_definitions WHERE jsonb_path_exists(default_config,'$.workflow.steps.*.config.refuse_owned_page ? (@ == true)') AND is_active AND NOT COALESCE(is_snapshot,false) AND deleted_at IS NULL -> exactly one row: page-build-handler",
      "[MEASURED 2026-08-24] jsonb_typeof(default_config->'workflow'->'steps') across live agents: object 194, absent 5, array 0 \u2014 the jsonpath's object assumption",
      "owner ruling 2026-07-29 \u00a73: a shared mechanism's other consumers must be told, not merely measured"
    ],
    "risks": "COVERAGE IS 94% OF THE MEASURED POPULATION, NOT 100%, AND THE RESIDUE IS NOW QUANTIFIED. The door keys on the page_id column. [MEASURED 2026-08-24] 3,819 of 5,259 rows at page-build-handler carry it; the content-finding producers are at 97-100% while the name-only action-request producers are at 0%, and 1,438 of the 1,440 NULL rows carry spec.page_name instead. Resolving by name at the door is a named non-scope item with its size attached, not an unstated gap.\n\nPARKING SOMETHING REPAIRABLE. If a handler declared refuse_owned_page but could in fact repair owned pages, the door would park findings that would have been fixed. Bounded by the declaration being the handler's own opt-in (it refuses at load_page_record today, so the park changes the record, not the outcome), and by the kill switch. The failure direction to watch is the declaration query returning a handler that does NOT refuse \u2014 the RUNBOOK names it.\n\nTHE PROBES RUN INSIDE THE CALLER'S TRANSACTION. Fail-open covers driver/context/scan errors; a server-side SQL error aborts the tx and the INSERT fails with it, which no fall-through can undo. The comment says this rather than claiming a safety the code lacks; the jsonpath literal is pinned by a parity test against migration 488 and was verified against the live estate.\n\n'deferred' IS OFF THE ADMIN ATTENTION QUEUES. site_admin_handlers.go's retry/resolve lists are needs_human_review/failed/blocked/(triaged)/unresolved \u2014 a parked high-severity finding is visible to the roadmap sweep and the OWNED_PAGE_GUARD marker query, not to those. Stated in the register entry; it is the same posture 077's 45 live rows already have.\n\nCOUNTING REGRESSION FOR EXISTING QUERIES. A census of 'error LIKE OWNED_PAGE_GUARD%' now counts parked rows alongside handler refusals. A LANDMINE entry and a dated correction to the 301 lane's NOTES query say to add AND status <> 'deferred'.\n\nTHE 9 RAW-INSERT WRITERS BYPASS THE DOOR (admin handlers, deploy_tool/create_tool_component guide pages, create_blog_posts, apply_adoption_plan, write_audit_findings). Backstop unchanged; listed by path in the register entry so the set is enumerable rather than folklore.\n\nLEGACY ROWS ARE UNTOUCHED. 142 open rows (59 failed/36 unresolved/16 needs_human_review) still hold the old handler; the post-roll verification splits by created_at vs roll time precisely so their continued refusals are not read as the fix failing."
  }
}