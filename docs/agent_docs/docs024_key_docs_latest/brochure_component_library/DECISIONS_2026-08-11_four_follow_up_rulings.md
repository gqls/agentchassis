# Owner rulings 2026-08-11 — the four follow-up decisions after the APPROVED round

Companion to `DECISIONS_2026-08-10_owner_rulings_after_relook.md` (the §6 set,
all executed). These four accumulated from the APPROVED verdict's advisory
notes (corr `a06ff850`) and the compliance read's follow-up ledger. Ruled by
the owner in session, 2026-08-11 morning.

## 1. The payoff replan runs NOW — phantom cleanup accepted as a known cost
The replan of fundamentallyai (the census that measures whether three weeks of
fact-assignment work actually lands) proceeds without waiting for the
phantom-404 quiet mode to be fixed. The sweep front hand-archives the phantom
pages afterwards, as it already does; coordination via its handoff is still
mandatory before dispatch. The phantom-mode fix stays in `bugs_open/215`'s
remaining scope on its own merits.

## 2. `features_open/012` is SCHEDULED: immediately after the census
The field-based recompose fix (surface `recompose_pages` to the planner as a
prompt-visible field) is this lane's next round once the census read-out
exists. This answers the council bug_historian's tracking ask directly — the
deferral now has a date-shaped trigger, not a vague intention. Recorded on
`features_open/012` too.

## 3. PLATFORM RULING — nested additions to a declared object input: register-named, not re-declared
Resolving the guidelines seat's flagged ambiguity (corr `a06ff850`): a
nested-field addition to an already-declared object input (the worked case:
`facts_scoped`/`assigned_writer_block` riding inside the already-passed
`section_plan`) does **not** require contract re-declaration — but it **must be
named in the seam's concept-register entry in the commit that ships it**. Same
shape as the 2026-08-02 RFC_010 §1 ruling (producer set named in the register
keeps extension cheap and honest). Applied to its own motivating case today:
PBP-037 now carries the named nested contract of `section_plan.sections_ready`
entries. **Still owed: a guidelines-corpus seed** so the council seat reads the
ruling natively (the `sql_for_agents/247` pattern) — specced in the 08-11
handoff, next session.

## 4. The invented-commitments clause is APPROVED — "we don't want these claims"
The compliance read's finding 3.4: nothing squarely bans the writer inventing
figure-free commitments ("money-back guarantee", "free no-obligation
consultation", "we'll always pick up the phone"). The owner approves the
one-line extension to the writer prompt's STRICT RULE 5:
> …and do NOT invent commitments, guarantees, warranties, or service promises
> (response times, refunds, availability) not stated in this prompt.
Ships as its own small seed on `page-content-writer`'s nested template path,
derived byte-exact from the live row (the seed-330 machinery), through the
council gate. Specced in the 08-11 handoff, next session. Findings 3.5/3.6
(edit-mode legacy claims; testimonial trade-dress) stay recorded, unscheduled.
