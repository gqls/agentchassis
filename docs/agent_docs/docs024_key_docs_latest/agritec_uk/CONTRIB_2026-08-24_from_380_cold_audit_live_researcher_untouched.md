# CONTRIB 2026-08-24 — from `bugfix_380_claims_fail_open`: the claims-auditor now runs COLD on every site; `evidence-researcher` is untouched

What changed that touches your lane: (1) `claims-auditor` no longer skips sites without facts — it audits every site with a shipped page on a 3600s/7-day rotation (migrations 597/600/601, commit 856d0e1fd). agritec.uk has 105 facts so it takes the ROSTER arm as before; the receipts it now leaves are in `doc_notes` (`pipeline`/`claims-audit`). (2) Nothing in the greenfield chain dispatches `evidence-researcher` — deliberately. Your RUNBOOK §9 (every research run needs the human read) is why: unattended research on a greenfield build was named as a follow-on for an OWNER decision, not shipped. Your `TRIGGER_evidence_research.sh` stays the only dispatch path. One note on it: it uses the racing `kubectl run -i --rm … kcat -P` form (LANDMINES: ~4 in 5 publishes dropped at exit 0); my `bugfix_380_claims_fail_open/TRIGGER_claims_audit.sh` shows the `kafka_publish_checked` form with an asserted receipt, if you want to copy it.

Also for you: the Go practice-claims family (commit c9cd817d9, inert until a roll) records first-person physical-practice sentences at WARNING unless the site's `evidence_base` carries an `operating_history` attestation (`attested_by`, `attested_at`, `evidence`). agritec's copy had zero hits in the fleet dry run, so nothing is owed today.
