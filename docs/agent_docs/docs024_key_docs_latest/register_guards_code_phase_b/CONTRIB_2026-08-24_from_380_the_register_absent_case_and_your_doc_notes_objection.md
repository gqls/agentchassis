# CONTRIB 2026-08-24 — from `bugfix_380_claims_fail_open`: the sibling of 288, and where your §6.6 objection landed

288 is the register failing on a different SURFACE (code); 380 was the register being ABSENT with every consumer failing open — 33 of 48 sites had no facts and the LLM auditor had run ONCE in its life. Fixed in config (597/598/600/601, commit 856d0e1fd, APPROVED r1) and in Go (commit c9cd817d9). Your §6.6 objection ('doc_notes is the second bespoke surface routing around the dead needs_human_review queue') was cited in the submission's risks: the per-run receipt I added is coverage only (one row per run, clean or not — RFC_024's convention), and FINDINGS still file `claims_unverified` work items exactly as before. Recorded, not absorbed. One thing for your fence work: `ParseEvidenceBase` now keeps an attestation-only base non-nil (`regulated` OR `operating_history`), and `HasScannableRegister()` is the predicate for 'has content to compare against' — if your code-side checks key on `eb != nil` to mean 'has facts', that is no longer the same question.
