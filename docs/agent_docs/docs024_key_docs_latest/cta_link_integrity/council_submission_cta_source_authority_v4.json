{
  "submitter": "leopardess3 / cta_link_integrity workstream (round 4 — answers round-3 REVISE; trail: r1 voided by 019, r2 REJECTED with direction, r3 REVISE 9-approve/2-object)",
  "rationale": "BE CONCISE — a token-capped reviewer voids the whole round (bugs_open/019). Decision + strongest objection, one or two sentences each.\n\nFIRST, A MATERIAL DISCOVERY — editquality's round-3 check ('verify the WHERE against live data') was run and it changes the plan: THE MIGRATION IS A NO-OP. The exact WHERE (ctaFieldNames functions ∩ site_specs.cta.% sources) matches ZERO live rows — migrations 091/098 already flipped every mapped component's cta url field to renderer. All 16 remaining site_specs.cta.* fossils sit on UNMAPPED components (bayesian-ranking-hero-tool_pre_037, product-hero_pre_037, header-minimal-tool_pre_037, archetype-result-card, game-master-explanation, content-sidebar, system-stats, product-specs, tool-ai-agent-roi-estimator, header-with-categories_pre_037), where the resolver never writes — flipping their source would orphan the value, not fix it. So Option A is DROPPED, not deferred: there is nothing left for it to do. The staged derivation (observe-only now, flip after delta review) is the sole route to those 16, exactly as the round-2 direction anticipated with 'then retire A's pattern' — A's pattern turns out to be already retired for the mapped set.\n\nROUND-3 OBJECTIONS, each addressed:\n- tooling_provenance (medium ×2): doc_notes entries are now edit 5 — one row per subject (pipeline/resolve_internal_links, pipeline/plan_sections) recording 'observe-only this round; flip gated on delta-log review; flip must honour on_missing/required', so the staged context survives outside this plan's prose.\n- debug_historian (medium): moot this round — no migration ships. Recorded as a binding constraint on the follow-up: any future jsonb surgery goes as separate apply/verify/rollback files with a mechanically-derived expected-count needle-gate and RETURNING post-condition.\n- editquality (low): 'FIXED' relabelled — the required:true/on_missing behaviour is DEFERRED BY DESIGN, not fixed; nothing ships that could hit it this round. And the live-data verification was performed; it is what killed the migration.\n- reuse_agent (low): links.go was read in full this session before ctafields.go was proposed — it contains href extraction/classification only (ExtractHrefs, ExtractAnchors, ClassifyLinkScope, IsNoopHref, NormalizePagePath, PageURLSet); no schema-field pairing exists there or elsewhere in datahelpers. ctafields.go is additive, not duplicative.\n- bug_historian (low, Warn-only guard): accepted as staged; the follow-up flip returns to this gate with the Warn-log evidence attached before anything blocks.",
  "plan": {
    "summary": "Go-only, observe-only round. The scoped migration was verified against live data and matches zero rows (091/098 already covered the mapped set), so it is dropped; the 16 remaining site_specs.cta.* fossils are all on unmapped components and are reachable only via derivation coverage. This round lands: the shared schema-derived CTA pairing helper with a loud-fail guard for unconventional fields, observe-only wiring in resolve_internal_links (derivation delta + uncovered-field Warns) and plan_sections (ownership-conflict log), and doc_notes rows preserving the staged design for the next agent. Zero behaviour change; the write-path flip and applyCTARecompute widening return to this gate with delta-log evidence.",
    "edits": [
      {
        "file": "platform/orchestration/datahelpers/ctafields.go",
        "symbol": "DeriveCTAURLFields / UncoveredCTAURLFields",
        "operation": "add",
        "rationale": "One definition of 'this field is a button destination', plus the loud-fail guard round 2 required. Reuse check done: links.go (read in full) holds href extraction/classification only — no schema-field pairing exists in datahelpers to extend.",
        "sketch": "type CTAField struct{ URLField, LabelField, Source string }\n\n// DeriveCTAURLFields: fields ending \"_url\" with a sibling \"<stem>_label\" or\n// \"<stem>_text\". Sibling test is load-bearing: 32 site_assets image/logo\n// fields also end in _url and must never be touched. Sorted output.\nfunc DeriveCTAURLFields(inputSchema map[string]interface{}) []CTAField { ... }\n\n// UncoveredCTAURLFields: fields ending \"_url\" whose source is query-resolved\n// (site_specs./pages./config. — site_assets. excluded) with NO sibling label.\n// The map's old silent failure mode, made loud. Callers log each at Warn.\nfunc UncoveredCTAURLFields(inputSchema map[string]interface{}) []string { ... }"
      },
      {
        "file": "platform/orchestration/datahelpers/ctafields_test.go",
        "symbol": "TestDeriveCTAURLFields / TestUncoveredCTAURLFields",
        "operation": "add",
        "rationale": "Pins the exclusion case (image/logo urls never derive), _text suffix, nil-schema safety, deterministic ordering, and the loud-fail guard semantics.",
        "sketch": "schema := fields{cta_primary_url:{source: site_specs.cta.primary_url},\n  cta_primary_label:{source: static}, guide_image_url:{source: site_assets.image},\n  orphan_cta_url:{source: site_specs.cta.secondary_url}}\nDerive -> exactly [cta_primary_url ↔ cta_primary_label]\nUncovered -> exactly [orphan_cta_url]   // NOT guide_image_url\n+ nil/empty schema -> nil, no panic; ordering deterministic."
      },
      {
        "file": "platform/orchestration/actions/resolve_internal_links_action.go",
        "symbol": "ResolveInternalLinksAction",
        "operation": "modify",
        "rationale": "Observe-only (no seat objected to this staging in round 3): log derived-vs-map delta per component + one Warn per uncovered field. ctaFieldNames still decides every write; its doc comment gains 'now an override for non-conforming components; stage-2 inverts precedence after delta review'.",
        "sketch": "  fields, isCTA := ctaFieldNames[function]\n+ if schema := sectionInputSchema(section); schema != nil {\n+   if d := ctaDerivationDelta(function, fields, isCTA, datahelpers.DeriveCTAURLFields(schema)); d != \"\" {\n+     logger.Info(\"resolve_internal_links: cta derivation delta (observe-only)\", zap.String(\"component\", function), zap.String(\"delta\", d))\n+   }\n+   for _, f := range datahelpers.UncoveredCTAURLFields(schema) {\n+     logger.Warn(\"resolve_internal_links: uncovered cta url field\", zap.String(\"component\", function), zap.String(\"field\", f))\n+   }\n+ }\n  if !isCTA { continue }"
      },
      {
        "file": "platform/orchestration/actions/plan_sections_action.go",
        "symbol": "planSection",
        "operation": "modify",
        "rationale": "Observe-only, per the round-2 direction: logs a would-be ownership conflict when query re-resolution is about to overwrite a differing resolved_data value on a derived CTA field. No skip, no write change, no queries. The follow-up flip (separate submission) skips re-resolution for resolver-owned fields and honours the schema's on_missing/required — that behaviour is DEFERRED, not shipped, and guidelines asked that the flip round be reviewed specifically for it.",
        "sketch": "// per-field loop, query-resolved branch, AFTER value resolves:\n+ if ctaOwned[fieldName] {   // built once per section from DeriveCTAURLFields\n+   if prev, ok := resolvedData[fieldName]; ok && prev != value {\n+     logger.Info(\"plan_sections: cta ownership conflict (observe-only)\",\n+       zap.String(\"field\", fieldName), zap.String(\"source\", source))\n+   }\n+ }\n  resolvedData[fieldName] = value   // UNCHANGED this round"
      },
      {
        "file": "docs/agent_docs/docs024_key_docs_latest/cta_link_integrity/scripts/persist_staged_design_notes.sql",
        "symbol": "n/a (doc_notes inserts)",
        "operation": "add",
        "rationale": "tooling_provenance's fix: the staged 'observe-only now, flip pending delta review' context must outlive this plan's prose. Two doc_notes rows, keyed by the travelling-docs subject convention, so the next agent touching either file finds the prior decision.",
        "sketch": "INSERT INTO doc_notes (subject_type, subject_key, body, categories, source, created_by) VALUES\n('pipeline','resolve_internal_links',\n '2026-07-20 staged rollout: schema-derived CTA pairing lands OBSERVE-ONLY — ctaFieldNames still decides writes. Precedence inverts only after one real build''s derivation-delta log is reviewed; that flip returns to the council gate. Trail 2525f980.',\n '[\"cta-link-integrity\",\"staged-rollout\"]'::jsonb,'council-gate','leopardess3'),\n('pipeline','plan_sections',\n '2026-07-20 staged rollout: cta ownership-conflict logging is OBSERVE-ONLY — re-resolution behaviour unchanged. The follow-up flip must skip re-resolution for resolver-owned fields AND honour on_missing/required (never a bare continue on a required field); guidelines asked that flip round be re-reviewed for exactly this. Trail 2525f980.',\n '[\"cta-link-integrity\",\"staged-rollout\"]'::jsonb,'council-gate','leopardess3');"
      }
    ],
    "grounded_in": [
      "Live verification 2026-07-20 (the check editquality requested): SELECT over content_components WHERE function IN (the 6 ctaFieldNames functions) AND fields source LIKE 'site_specs.cta.%' → 0 rows. The same query unscoped → 16 rows, every one on an unmapped component (list in rationale).",
      "Round-3 verdict (trail 2525f980): decision revise, decided_by 'objection from tooling_provenance'; 9 approvals incl. guardian ('one map lookup + conditional Info/Warn per CTA field is negligible hot-path cost and acceptable as-is') and guidelines ('correctly deferred, not silently fixed-in-name-only … recommend the follow-up submission be reviewed again specifically for the on_missing branch').",
      "tooling_provenance (round 3): 'the travelling PLAN/NOTES convention (doc_plans/doc_notes keyed by subject_type+subject_key) is exactly the mechanism for carrying observe-only-this-round context forward instead of relying on the plan JSON's prose'.",
      "links.go read in full this session: ExtractHrefs, ExtractAnchors, ClassifyLinkScope, IsNoopHref, DeadControlAnchors, IsAssetPath, NormalizePagePath, PageURLSet — extraction/classification only, no schema-field pairing (reuse_agent's requested evidence).",
      "Diagnosis c84940fb (CONFIRMED): query-resolved sources re-run their lookups on every plan_sections call and merge last; the code-level mechanism stands even though the mapped set's config exposure is now closed."
    ],
    "risks": "1. No migration ships — nothing is live-immediately; all Go is inert until an image roll, then observe-only. Deploy verification against the RUNNING POD: `strings /app/agent-chassis | grep -c \"cta derivation delta\"` ≥ 1, never inferred from tag/commit.\n2. The 16 unmapped fossils remain live-broken until the flip round — accepted: the alternative (flipping their source without resolver coverage) orphans the value, and the flip round is gated on delta-log evidence by design.\n3. Binding constraint carried to the follow-up (debug_historian): any future jsonb surgery ships as separate apply/verify/rollback files with a mechanically-derived expected-count needle-gate and RETURNING post-condition.\n4. doc_notes inserts are plain rows (no schema change); dedupe by re-running is acceptable (duplicate note < missing note), and the script is idempotent-enough via its dated body."
  }
}
