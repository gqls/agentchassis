{
  "submitter": "leopardess3 / cta_link_integrity workstream (round 6 — incorporates a concurrent thread's verified corrections + closes round-5's asks; trail: r1 voided/019, r2 REJECTED, r3-r5 REVISE)",
  "rationale": "BE CONCISE — a token-capped reviewer voids the whole round (bugs_open/019).\n\nTHE HEADLINE: tooling_provenance's load-before-write protocol (its round-5 MISSING item) was executed and FOUND PRIOR CONTEXT — two doc_notes rows written today by a concurrent thread (created_by bugfix-023, commit b6e374fc2) containing verified corrections to this very plan:\n\n1. THE PLAN_SECTIONS OBSERVE LOG WAS DEAD CODE. resolvedData is a fresh local map in planSection and the per-field loop writes each field at most once, so `prev, ok := resolvedData[fieldName]` is never true. The observe round would have reported a structural ZERO and green-lit the flip on false evidence — the exact failure the staging was designed to prevent. Independently re-verified against my own read of planSection. FIX: the observe log moves to the true loss site — rerender_page_sections_action.go's merge loop (:281-307), where stored content_data (the resolver's last write) merges FIRST and fresh plan.ResolvedData merges LAST and wins. plan_sections receives NO edit this round.\n2. THE SIBLING RULE WAS LOSSY BOTH WAYS. (a) It misses 3 of ctaFieldNames' own 10 fields — hero.secondary_cta_url pairs with bare `secondary_cta`; call-to-action's primary/secondary_cta_url pair with bare `primary_cta`/`secondary_cta` (verified live). Bare stem is now a third sibling form; measured fleet-wide it adds exactly those 3 fields and zero images. (b) It derives header-leopardess.logo_url (source site_assets.logo, sibling logo_text) — the resolver could overwrite a logo with a page link. FIX: a *_url field whose own source is site_assets.* never derives.\n\nCredit where due: five council rounds and this author missed the dead-code defect; a concurrent thread reading the actual function caught it. The load-before-write protocol is what surfaced it here.\n\nROUND-5 ITEMS: reuse_agent (low) — the string-or-map input_schema parse is now a single shared helper, datahelpers.ParseInputSchemaValue, used by the new code; converging loadSingleComponentSchema onto it is queued for the flip round (keeps this round observe-only). The parallel-path period remains bounded: map becomes override now, retires at the flip. prior_art_librarian (medium) — doc_notes existence corroborated from repo DDL, not session state: CREATE TABLE doc_notes at docs/agent_docs/sql_for_agents/125_doc_plans_and_notes.sql:46. prior_art (low, needs_human_review 'write-only pile') — evidenced: grep of platform/ for unresolved_cta/cta_names_unknown_destination/dead_control returns emission sites only; TriageDetectedItemsAction promotes status='detected' only; load_work_item_actions.go:804 excludes needs_human_review from re-open; live: 21+13 such items open on leopardess, oldest 2026-07-13. prior_art (low, sectionInputSchema novelty) — superseded: the new code calls the shared ParseInputSchemaValue; nothing equivalently-shaped exists (loadSingleComponentSchema is a DB loader whose inline parse is the duplication being extracted).",
  "plan": {
    "summary": "Go-only, observe-only, zero behaviour change — reshaped by a concurrent thread's verified corrections. The derivation helper gains a third sibling form (bare stem) and a site_assets source guard; the shared ParseInputSchemaValue helper replaces the would-be duplicated parse; the ownership-conflict observe log moves from plan_sections (where it was provably dead code) to the true loss site in rerender_page_sections' merge loop, carrying spec.reason to distinguish deliberate cta_links_stale recomputes; doc_notes rows preserve the staged design alongside the concurrent thread's correction rows. Flip-round constraints accumulate unchanged.",
    "edits": [
      {
        "file": "platform/orchestration/datahelpers/ctafields.go",
        "symbol": "DeriveCTAURLFields / UncoveredCTAURLFields / ParseInputSchemaValue",
        "operation": "add",
        "rationale": "Corrected derivation: three sibling forms and a source guard, both measured live (bare stem adds exactly the 3 mapped fields, zero images; the guard excludes logo_url despite its logo_text sibling). ParseInputSchemaValue extracts the string-or-map branching once (reuse_agent r5).",
        "sketch": "type CTAField struct{ URLField, LabelField, Source string }\n\n// ParseInputSchemaValue: input_schema arrives as a JSON string (DB round-trip)\n// or an already-parsed map (in-process). One parse, all callers.\nfunc ParseInputSchemaValue(v interface{}) map[string]interface{} { ... }\n\n// DeriveCTAURLFields: fields ending \"_url\" with a sibling of the SAME stem in\n// any of THREE forms: \"<stem>_label\", \"<stem>_text\", or bare \"<stem>\"\n// (hero.secondary_cta_url ↔ secondary_cta — 3 of the map's own 10 fields pair\n// this way). GUARD: a field whose own source is site_assets.* NEVER derives\n// (logo_url has sibling logo_text but is an image, not a button). Sorted.\nfunc DeriveCTAURLFields(inputSchema map[string]interface{}) []CTAField { ... }\n\n// UncoveredCTAURLFields: *_url, query-resolved source (site_specs./pages./\n// config. — site_assets. excluded), NO sibling in any form. Warn-only THIS\n// ROUND; the flip must replace this with a consumed detection path (named\n// handler) or the flip does not ship.\nfunc UncoveredCTAURLFields(inputSchema map[string]interface{}) []string { ... }"
      },
      {
        "file": "platform/orchestration/datahelpers/ctafields_test.go",
        "symbol": "TestDeriveCTAURLFields / TestUncoveredCTAURLFields / TestParseInputSchemaValue",
        "operation": "add",
        "rationale": "Pins the corrected semantics, including the two defects the concurrent thread found — they become regression tests.",
        "sketch": "cases:\n- cta_primary_url ↔ cta_primary_label (suffix form)\n- hero-style secondary_cta_url ↔ secondary_cta (BARE-STEM form — regression for correction 2a)\n- logo_url with sibling logo_text but source site_assets.logo -> NOT derived (regression for 2b)\n- guide_image_url (site_assets.image, no sibling) -> not derived, not uncovered\n- orphan_cta_url (site_specs.cta.*, no sibling) -> uncovered\n- ParseInputSchemaValue: string form, map form, garbage -> nil; nil-schema safety; deterministic ordering."
      },
      {
        "file": "platform/orchestration/actions/resolve_internal_links_action.go",
        "symbol": "ResolveInternalLinksAction / sectionInputSchema (new, thin)",
        "operation": "modify",
        "rationale": "Observe-only, unchanged in intent since r3 (no seat has objected to this staging). sectionInputSchema is now a two-line wrapper over datahelpers.ParseInputSchemaValue(section[\"component\"][\"input_schema\"]) — no duplicated parse. Sole consumer: internal-link-resolver.",
        "sketch": "+ func sectionInputSchema(section map[string]interface{}) map[string]interface{} {\n+   comp, _ := section[\"component\"].(map[string]interface{})\n+   if comp == nil { return nil }\n+   return datahelpers.ParseInputSchemaValue(comp[\"input_schema\"])\n+ }\n\n  fields, isCTA := ctaFieldNames[function]\n+ if schema := sectionInputSchema(section); schema != nil {\n+   if d := ctaDerivationDelta(function, fields, isCTA, datahelpers.DeriveCTAURLFields(schema)); d != \"\" {\n+     logger.Info(\"resolve_internal_links: cta derivation delta (observe-only)\", zap.String(\"component\", function), zap.String(\"delta\", d))\n+   }\n+   for _, f := range datahelpers.UncoveredCTAURLFields(schema) {\n+     logger.Warn(\"resolve_internal_links: uncovered cta url field\", zap.String(\"component\", function), zap.String(\"field\", f))\n+   }\n+ }\n  if !isCTA { continue }\n// ctaFieldNames doc comment gains: 'now an override; stage-2 inverts precedence after delta review'."
      },
      {
        "file": "platform/orchestration/actions/rerender_page_sections_action.go",
        "symbol": "merge loop (:281-307 region)",
        "operation": "modify",
        "rationale": "REPLACES the dead plan_sections log (correction 1): this merge loop is where the loss actually happens — stored content_data (the resolver's last write) merges FIRST, fresh plan.ResolvedData merges LAST and wins. Observe-only: log each derived CTA field where the fresh value differs from the stored one, carrying spec.reason so deliberate cta_links_stale recomputes are distinguishable from silent clobbers. plan_sections_action.go is NOT edited this round.",
        "sketch": "// in the merge loop, per section, before fresh ResolvedData overwrites:\n+ derived := datahelpers.DeriveCTAURLFields(sectionSchema) // via ParseInputSchemaValue\n+ for _, cf := range derived {\n+   stored, hasStored := s.contentData[cf.URLField]\n+   fresh, hasFresh := plan.ResolvedData[cf.URLField]\n+   if hasStored && hasFresh && stored != fresh {\n+     logger.Info(\"rerender_page_sections: cta ownership conflict (observe-only)\",\n+       zap.String(\"field\", cf.URLField), zap.String(\"source\", cf.Source),\n+       zap.String(\"reason\", spec.Reason))\n+   }\n+ }\n  // merge behaviour UNCHANGED this round"
      },
      {
        "file": "docs/agent_docs/docs024_key_docs_latest/cta_link_integrity/scripts/persist_staged_design_notes.sql",
        "symbol": "n/a (doc_notes inserts)",
        "operation": "add",
        "rationale": "Load-before-write was performed (tooling_provenance r5) and found the bugfix-023 correction rows — which reshaped this plan. These two design rows are APPENDED alongside them (append-only convention), each referencing the corrections it incorporates.",
        "sketch": "-- protocol: SELECT existing rows for these subject_keys FIRST (done 2026-07-20;\n-- found 2 correction rows from bugfix-023, incorporated in trail v6), then append:\nINSERT INTO doc_notes (subject_type, subject_key, body, categories, source, created_by) VALUES\n('pipeline','resolve_internal_links',\n '2026-07-20 staged rollout v6: schema-derived CTA pairing (3 sibling forms + site_assets source guard, per bugfix-023 corrections) lands OBSERVE-ONLY — ctaFieldNames still decides writes; precedence inverts only after delta-log review, via the council gate. Trail 2525f980.',\n '[\"cta-link-integrity\",\"staged-rollout\"]'::jsonb,'council-gate','leopardess3'),\n('pipeline','rerender_page_sections',\n '2026-07-20 staged rollout v6: cta ownership-conflict observe log lives in THIS action''s merge loop (the true loss site — stored content_data merges first, fresh ResolvedData wins), NOT in plan_sections, whose sketched log was dead code (bugfix-023 correction). Flip constraints: skip-with-on_missing honoured, needle-gated migrations, Warn replaced by consumed detection path. Trail 2525f980.',\n '[\"cta-link-integrity\",\"staged-rollout\"]'::jsonb,'council-gate','leopardess3');"
      }
    ],
    "grounded_in": [
      "doc_notes corrections (created_by bugfix-023, 2026-07-20 12:44, commit b6e374fc2), independently re-verified this session: (1) planSection's resolvedData is a fresh local map, each field written at most once — the sketched conflict log could never fire; true loss site is rerender_page_sections_action.go:281-307 (stored merges first, fresh wins). (2) hero has fields secondary_cta + secondary_cta_url; call-to-action has primary_cta/primary_cta_url + secondary_cta/secondary_cta_url (live query) — bare-stem pairs the suffix rule missed. (3) header-leopardess has logo_url (source site_assets.logo) + logo_text (live query) — derives without the source guard.",
      "doc_notes DDL in-repo (prior_art_librarian r5): CREATE TABLE doc_notes at docs/agent_docs/sql_for_agents/125_doc_plans_and_notes.sql:46 — corroborated from the codebase, not session state.",
      "needs_human_review write-only evidence (prior_art r5): grep of platform/ for unresolved_cta / cta_names_unknown_destination / dead_control → emission sites only; TriageDetectedItemsAction promotes WHERE status='detected' only (triage_detect_items_action.go:91-104); load_work_item_actions.go:804 excludes needs_human_review from re-open; live 2026-07-19: 21 unresolved_cta + 13 cta_names_unknown_destination open on leopardess, oldest 2026-07-13.",
      "Consuming workflows (live agent_definitions): plan_sections → build-site-planner, diagnose-agent, image-build-handler, page-build-handler, page-content-writer (UNEDITED this round); resolve_internal_links → internal-link-resolver; rerender_page_sections → the page-rerender path (cta_links_stale recompute host, per migration 092/098 history).",
      "Round-4 editquality: 'if those confirm, this converges to approve' — the checks confirmed and are cited above."
    ],
    "risks": "1. All Go inert until an image roll, then observe-only. Pod verification: `strings /app/agent-chassis | grep -c \"cta derivation delta\"` ≥ 1 and `grep -c \"cta ownership conflict\"` ≥ 1.\n2. BINDING on the flip round (accumulated r3–r6): (a) needle-gated separate apply/verify/rollback for any jsonb surgery [debug_historian r3]; (b) Warn-only guard replaced by a consumed detection path — named handler, not needs_human_review [bug_historian r4, now evidenced]; (c) guidelines re-reviews the on_missing/required skip branch [guidelines r3]; (d) loadSingleComponentSchema converges onto ParseInputSchemaValue [reuse_agent r5].\n3. The rerender observe log runs only on rerender paths; initial builds produce no conflict signal — accepted: the loss mechanism is the rerender merge, which is exactly where evidence is needed, and spec.reason separates deliberate recomputes.\n4. Concurrent-thread coordination: bugfix-023 is feeding findings to this trail (docs-only commits), not implementing — no work-item conflict; their corrections are incorporated and credited."
  }
}
