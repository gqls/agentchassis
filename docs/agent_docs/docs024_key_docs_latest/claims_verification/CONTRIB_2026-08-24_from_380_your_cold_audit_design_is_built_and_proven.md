# CONTRIB 2026-08-24 — from `bugfix_380_claims_fail_open`: your 2026-07-20 cold-audit design is BUILT, LIVE and PROVEN

Your `PLAN_2026-07-20_gaswholesalers_second_site.md` §3 named the inversion ("a site where nothing has been attested yet cannot be audited at all") and designed the fix. It sat unbuilt for five weeks and was refiled as `bugs_open/380` from the garden-tools.uk build. It is now live as migration 597 (commit 856d0e1fd, council APPROVED r1 corr e684fc8d): `check_opted_in` is DELETED (RFC_017 fail-closed — a DB error now FAILS the run), the prompt's register section is a two-arm branch with a cold-register instruction, every run writes a doc_notes receipt (`pipeline`/`claims-audit`), the per-page cap is 12,000, and the auditor has a clock (600: 3600s / 7-day, owner decision). Your 'posture: cold_audit' key was NOT needed: with the gate gone, an empty `facts_text` IS the cold posture, and the owner ruled no empty registers are minted (RFC_003 Q2 = no).

Two things you owned that changed under you, stated plainly: (1) the agent now HAS a seed — `SEED_claims_auditor.sql` in your dir, regenerated from the live row after 597+601; any migration touching the agent must regenerate it. (2) 601: the auditor's `load_page_text` was eating most of every multi-component page (PostgreSQL greedy-first regex over an unordered `string_agg` — the 517/518 mechanism); the first cold run never saw the owner's sentence. Fixed per component; the re-run's first two findings were the owner's own two quoted sentences at severity high. Your open question 'cadence for V3' is answered: 3600s tick, 7-day window, never-audited first. Lane docs: `docs/agent_docs/docs024_key_docs_latest/bugfix_380_claims_fail_open/`; register CLM-025/026/027; CLM-006 marked superseded.
