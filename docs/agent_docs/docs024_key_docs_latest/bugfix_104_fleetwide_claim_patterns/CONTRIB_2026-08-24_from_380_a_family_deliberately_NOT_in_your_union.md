# CONTRIB 2026-08-24 — from `bugfix_380_claims_fail_open`: a first-person family that is deliberately NOT unioned into ScanAllBannedClaims

Your header's bar ('false-by-construction for every site we will ever run') is exactly why the new practice-claims family (`claims_practice.go`, commit c9cd817d9) has its OWN entry point (`ScanPracticeClaims`, the `ScanRegulatedClaims` precedent) and default severity WARNING: 'we test every tool' is true on a real reviews site, so it cannot join the refusing set. `TestPracticeFamilyIsNotInTheRefusingUnion` pins that; the flip is RFC_003 §8 Q1. Your dry-run recipe was followed on the complete live corpus (1,867 components — the first export silently dropped 414 rows, LANDMINES entry): 12 findings on 6 sites. Nothing in your global set changed; `cmd/claimscan` now prints PRACTICE lines outside the exit code.
