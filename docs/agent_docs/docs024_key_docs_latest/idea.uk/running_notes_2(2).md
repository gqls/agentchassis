# RUNNING NOTES — PART 2 (idea.uk chassis + scheme/design work)

Part 1 archived at running_notes.md (~5690 lines, checkpoints (a)…(kkk)). This file continues the journal. Memory is OFF; this doc is the journal. **Present this file at the END OF EVERY TURN.**

═══════════════════════════════════════════════════════════════════════
CARRY-OVER STATE (still valid as of 2026-06-26)
═══════════════════════════════════════════════════════════════════════

## Standing preferences (STRICT)
Go not Python; plain human language, no LLM-hype, no flattery; confirm live API/schema/product facts before asserting/coding (`0 rows` not decisive — check the query/state first); reuse before rebuild; fix the framework structurally over one-offs; honest caveats + pushback incl. correcting my own reads; British English; low risk appetite; reasonable steps; ≤1 question per reply; don't create summary docs unless asked; minimal formatting (prose not bullets); banned words "perfect"/"critical"/"excellent"; no `logger.Debug` (use `logger.Info`); don't call a fix "final/last". Keep the runbook + this journal fresh. SQL run as FILES via `kubectl … < file` (pasting mangles `\set`/`\echo`/blank lines).

## Project facts
- **idea.uk** — LIVE Go service selling £29 "verified AI product idea" reports. Single Go binary under systemd (`idea`) on a Hetzner VM, nginx + LetsEncrypt, 127.0.0.1:8080. Orders in /var/lib/idea/orders.json (no DB). Live Stripe webhook → https://idea.uk/stripe/webhook (money path). Reserved tool paths: /request /confirm /approve /decline /stripe/webhook /internal/* /order/*. DNS (Cloudflare) → the VM, so chassis B2 deploys are INVISIBLE to the live site (safe staging-in-place). UNCHANGED/earning.
- **Chassis website-builder** — multi-agent (Go/Kafka/Postgres in k8s). domain → multipage site → static → Backblaze B2 (github → GH Actions → B2). Rebuilding idea.uk's front site; go-live is a deliberate VM cutover (NOT done).
- k8s namespaces: `-n ai-persona-system` (app pods) + `-n kafka`. Cluster `personae-kafka-cluster`. Bootstrap `personae-kafka-cluster-kafka-bootstrap.kafka.svc.cluster.local:9092`.
- DB access: `kubectl -n ai-persona-system exec -i postgres-clients-0 -- psql -U clients_user -d clients_db`. Run migration FILES via `< file`.
- agent_definitions: `type`+`version` UNIQUE; `processing_mode` is nested in `default_config` (NOT a top-level column). snapshot_agent backs up before edits.

## Chassis design model (three layers)
1. **Direction** — domain-research-classifier writes identity/classification/content_direction/design_intent (design_intent carries structured palette.reference_values + typography.reference_values + style_direction → light/dark scheme).
2. **Composition** — site-design-planner resolves layout (scheme-aware weighted matcher) / typography / palette, then install_site_composition writes css_themes + style_collections + sites.style_collection_id + a `resolved_composition` POINTER spec (palette_id/name/source, NOT colour values). Refuses to overwrite an installed composition.
3. **Execution** — webdesign-agent renders styles.css from the installed composition (+ overlay) and git-commits it (→ GH Actions → B2). Sole styles.css deployer.

## CURRENT STATE — idea.uk scheme fix
- Scheme-aware weighted layout matcher is LIVE in production (merged into fork_theme_composition.go + resolve_composition_layout_action.go). Added layouts.scheme column + a new tool-portal-light layout via migration_layouts_scheme_and_light_tool_portal.sql (applied).
- idea.uk (site **1244516d-014d-421c-88c6-090bb1e9552a**, domain idea.uk) RE-RESOLVED in place onto **tool-portal-light (scheme light)** + **parchment palette** (palette-idea-uk: text/primary/border #1A1816, accent #A8391A, surface #E8DFCC, secondary #4A4540, background #EFE7D6, text_muted #837C72). New chain: style_collection 6e7d98fb → css_theme 4734d51c (theme-idea-uk) → layout 278ae068 (tool-portal-light). No needs_new_layout_candidate.
- styles.css RENDERED + DEPLOYED via webdesign-agent (commit 05ef817). VERIFIED correct: exactly tool-portal-light, parchment :root, NO LLM drift.
- **OPEN — the page-layer gap (Step 7):** the deployed PAGES still render dark. Page HTML is unchanged from the 2026-06-21 build and bakes per-component inline `<style>` (dark gradient header `.site-header--gradient` #1A1816→#4A4540; dark stock-photo hero with primary button `var(--accent-color, #0f3460)` → falls back to NAVY because styles.css defines `--color-accent` not `--accent-color`; dark CTA `.cta-section` var(--color-primary)=#1A1816; dark footer #1A1816). Only `.tool-list-section` + `.latest-news-section` read `--color-*` vars → only they go parchment. Inline hardcoded colours beat the external stylesheet, so a styles.css swap can't flip the pages. The PAGE layer needs rebuilding — but only if page-build component selection is scheme-aware AND the components defer to the palette; else a rebuild reproduces the same dark chrome. INVESTIGATING how pages are built (this turn).

## Key runbook + files (/mnt/user-data/outputs/, mirrored by user at docs/agent_docs/docs024_key_docs_latest/idea.uk/)
RUNBOOK_idea_uk_chassis_site_and_vm_deploy.md (re-resolve Steps 1–7, Step 7 = page rebuild/component investigation = current). reresolve_idea_uk_0{1,1b,2,2b,4}*.sql + 03_trigger.sh (planner) + 05_render.sh (webdesign-agent). migration_layouts_scheme_and_light_tool_portal.sql. RUNBOOK_idea_uk_vm_cutover.md. 002_system_architecture.md (rewritten). 016_debugging_guide_v2_55.md.

## Backlog (post page-fix)
(b) build-standard classifier migration (proven, NOT applied). (c) dead-slot hardening Go (design_reference fingerprint fallback). (d) improver-not-rewriter overlay for webdesign-agent analyze_design. (e) review built site (CTAs→/request, reserved paths) → VM cutover. (f) populate remaining layouts.scheme. Known chassis hazard: a content rebuild de-tools a tool page (page-content-writer regenerates from plan_sections which doesn't know the interactive tool) — fix pending.

═══════════════════════════════════════════════════════════════════════
## CHECKPOINT 2026-06-26 (lll) — established HOW pages are built (from 027/003/page-build-handler def); idea.uk dark-page root cause is structural (component layer, not styles.css)
═══════════════════════════════════════════════════════════════════════
Read: page-build-handler agent_definition (inline), 027_design_and_site_planner_v2.md (= "026 Design Composition & Site Design Planner"), 003_contracts_and_standards_7_.md. 

HOW PAGES ARE BUILT:
- Components live in content_components (function = kebab-case identifier + html_template carrying its OWN scoped CSS). Variants = distinct functions (hero, hero-split, header-professional-dark, header-minimal-light, footer-4-column).
- SITE-LEVEL header/footer/head: chain = layouts.default_{header,footer}_component_id -> install_site_composition copies onto style_collections.{header,footer}_component_id -> update_site_defaults copies onto site_components.component_id -> renderAndStoreSiteComponent renders template -> site_components.rendered_html. If style_collections.header_component_id NULL or site_components unlinked -> RenderFallbackHeader (hardcoded generic). (003 Site Component Linkage Contract.)
- PER-PAGE sections: page-build-handler.plan_sections picks section functions from a GLOBAL component library (027 §10 + §312: layout-aware section selection is FUTURE work, "025 Phase 4"). page-content-writer writes prose per section -> save_page_sections persists to page_components (slot_name/component_id/rendered_html) -> page-rerender concatenates page_components.rendered_html in order, wraps with site header/footer, commits to git -> B2.
- COLOUR MODEL (003 CSS Colour Inheritance): styles.css body{color:var(--color-text)}; h1-6 use var(--section-heading,var(--color-primary)); p/li use var(--section-text,inherit). A DARK section sets --section-* on its OWN container (SANCTIONED override). Light sections inherit the palette automatically.

WHY idea.uk PAGES RENDER DARK despite tool-portal-light + correct styles.css:
The re-resolve changed composition + re-rendered styles.css, but did NOT touch page HTML or site header/footer wiring, and page assembly is NOT scheme-aware. Specifically:
1. Header/footer: pages still carry the original 2026-06-21 gradient DARK header + dark footer. The re-resolve installed a new style_collection but (a) tool-portal-light almost certainly declares NO default_{header,footer}_component_id (I only set css_template/structure_tokens/tags/scheme when I added it) -> new collection's ids likely NULL, and (b) site-design-planner does NOT run update_site_defaults -> site_components never re-pointed. So a re-render would keep the OLD header or drop to RenderFallbackHeader — NOT tool-portal-light's light header.
2. Hero + CTA: global `hero` (dark image overlay) + `call-to-action` (dark) components. plan_sections isn't layout-aware -> a re-render picks the SAME dark sections, even though tool-portal-light's own hero is light/typographic.
3. Hero component BUG: button background var(--accent-color, #0f3460) — system var is --color-accent (NOT --accent-color) -> falls back to NAVY. The stray blue is baked into the `hero` content_components template, NOT styles.css. (003 New-component checklist #3: use render-context vars not hardcoded; the var name is also just wrong.)

CONCLUSION: a plain page re-render will NOT make idea.uk light — it reproduces the dark chrome + navy button. Fix is STRUCTURAL at the component layer (matches "fix framework not patch one page"): (a) author + assign light header/footer components for tool-portal-light (+ ensure update_site_defaults runs to push them into site_components) and a light/typographic hero; OR (b) make section/component selection scheme-aware. The --accent-color->--color-accent hero fix lands regardless.

NEED FROM USER (queries + code) to pin the exact fix:
1. layouts: SELECT name, default_header_component_id, default_footer_component_id FROM layouts WHERE name IN ('tool-portal-light','tool-portal-dark','brochure-formal');
2. idea.uk wiring: SELECT header_component_id, footer_component_id FROM style_collections WHERE id='6e7d98fb-843c-4931-8626-1af5ffb237c0'; + SELECT slot_name, component_id FROM site_components WHERE site_id='1244516d-014d-421c-88c6-090bb1e9552a';
3. component inventory + buggy templates: SELECT function, is_active FROM content_components WHERE function LIKE 'header-%' OR function LIKE 'footer-%' ORDER BY function; + SELECT function, html_template FROM content_components WHERE function IN ('hero','call-to-action');
4. code: plan_sections (section selection) + update_site_defaults (header/footer -> site_components) + page-rerender agent definition. Have 026_component_regeneration_flow_2_.md uploaded — will read for plan_sections; action code/def would confirm.

NOTE other uploads now available for this work: install_site_composition_action.go, render_css_from_spec_action.go + render_css_composition_{loader,helpers}.go, emit_design_items_action.go, fork_theme_from_site_action.go, 026_component_regeneration_flow_2_.md, 030_phase1_plan_and_reconciler_5_.md, 021_site_spec_and_classifier.md, 003/002/001 latest. The 081*_trigger_rerender_*.sh scripts (gaswholesalers/robot-hands) are existing page-rerender trigger examples to reuse for idea.uk.

═══════════════════════════════════════════════════════════════════════
## CHECKPOINT 2026-06-26 (mmm) — CONFIRMED at code+data level: component selection is NOT scheme-aware; header/footer not re-derived. Root cause pinned.
═══════════════════════════════════════════════════════════════════════
Got: queries (layouts/style_collections/site_components/content_components) + component_selector.go + flag_page_image_rebuild_action.go + check_unresolved_sections.go + nav_tables.go + write_site_plan_action.go (uploaded) + page-rerender agent_definition (inline). User correction: a component html_template's inline <style> is an OPTIONAL OVERRIDE, not the main CSS — main CSS is styles.css (the layout css_template). Consistent with observed: .tool-list-section/.latest-news-section use --color-* vars -> render parchment; .hero/.cta-section/.site-header--gradient carry dark overrides.

DATA FINDINGS:
- layouts: tool-portal-light, tool-portal-dark, brochure-formal ALL have NULL default_header_component_id + default_footer_component_id. NO layout declares header/footer defaults.
- style_collections 6e7d98fb (idea.uk new): header_component_id NULL, footer_component_id NULL (inherited nothing, as expected).
- site_components (idea.uk): header=9644c86f-18b0-4f75-b086-5b79a74a48d7, footer=09034086-a581-4bba-a5b4-760d863bb2df, head=116c5f91-bc0d-439d-9e13-a3ba2d145571. These are the ORIGINAL build's components (dark gradient header + dark footer). The re-resolve did NOT touch site_components.
- content_components header-*/footer-*: footer-with-disclaimer(t), header-docs(f), header-docs(t), header-minimal-tool(t), header-with-cart-or-nav(t), header-with-categories(t), header-with-search(t). No 'footer-4-column' / 'site-header--gradient' under those names -> idea.uk's header/footer (9644c86f/09034086) functions UNKNOWN (need lookup); likely site-specific/forked or differently named. Whether any header/footer here is LIGHT = unknown (need is_dark_section).
- content_components hero: TWO rows. (A) BUGGY: uses var(--primary-color/--secondary-color/--accent-color ...) -> WRONG names -> navy #0f3460 fallback (this is what deployed). (B) FIXED: uses var(--color-primary/--color-secondary/--color-accent ...) -> correct, rust accent. Which is_active = UNKNOWN (uniqueness index allows only 1 active/function, so exactly one). If (B) is active, a page-build-handler rebuild yields the rust button automatically.
- content_components call-to-action: TWO rows, BOTH dark (background var(--color-primary,#1a1a2e) + dark --section-*). CTA is a dark callout section by design (sanctioned per 003 CSS Colour Inheritance). Differ only in CTA-button {{if}} guards.

CODE FINDINGS:
- component_selector.go (SelectComponentByType/ByTypes; called by plan_sections): scores candidates by suitable_site_types(0.35) + suitable_page_types(0.15) + avg_quality_score*0.3 + specificity + usage_count. WHERE section_type=$1 AND component_level='section' AND is_active AND forked_from IS NULL ORDER BY score DESC. **It SELECTs is_dark_section into the struct (IsDarkSection) but NEVER uses it in scoring.** => section selection is site_type/page_type/quality/usage-aware but NOT scheme/light-dark-aware. A LIGHT site gets DARK sections if they score well for its site_type. THIS IS THE STRUCTURAL GAP (sections).
- page-rerender agent_def (type page-rerender, processing_mode task): start check_rerender_mode -> if reason in {image_landed, section_data_resolved} -> rerender_sections (re-resolve fields from stored content_data, no LLM) -> save -> render_page; else -> render_page (rerender_single_page = assemble STORED page_components.rendered_html) -> deploy_page (git_commit repo "sites") -> update_status. So page-rerender RE-ASSEMBLES stored HTML; it does NOT re-select components. page-build-handler (plan_sections->component_selector->content-writer->save) is the one that RE-SELECTS.
- flag_page_image_rebuild_action.go: emits needs_page (handler page-build-handler, item_key page_rerender:<page>, priority 99) to force a plan_sections re-resolve. = the reusable pattern to trigger a real page rebuild for idea.uk.
- nav_tables.go: GetNavItems (nav tables -> pages fallback); header/footer NAV only, not styling.

ROOT CAUSE (pinned): two scheme-blind spots. (1) section component selection (component_selector) ignores scheme + the already-loaded is_dark_section. (2) header/footer are not scheme-derived: no layout declares defaults, site-design-planner doesn't run update_site_defaults, and idea.uk's site_components still point at the original dark header/footer. => even a full page-build-handler rebuild reproduces dark chrome on the light site (dark hero/cta re-picked; header/footer untouched).

FIX DIRECTION (structural, matches "fix framework not patch one page"):
1. Make component_selector scheme-aware: add site scheme to SelectorContext; score is_dark_section against it (light site -> prefer is_dark_section=false; dark site -> prefer true), or penalize scheme-mismatched dark sections. The field is already loaded -> small, contained change. Plumb scheme via plan_sections_action.go (needs that file).
2. Header/footer scheme-appropriateness: (immediate) point idea.uk's site_components.header/footer (+ style_collections 6e7d98fb) at a LIGHT header/footer component; (structural) have layouts declare default_{header,footer}_component_id and the build copy them to site_components via update_site_defaults, so header/footer track the layout's scheme. Needs: do light header/footer components exist (is_dark_section)? else author them.
3. Hero navy button: confirm which hero is_active; if buggy(A) is active, deactivate it / fix its var names to (B). If fixed(B) active, a rebuild fixes the button.
4. After fixes: trigger page-build-handler rebuild (reuse flag_page_image_rebuild pattern: needs_page per page) so plan_sections re-selects with scheme + re-renders; re-render site components for the new header/footer.

STILL NEED:
- plan_sections_action.go (the caller of component_selector — where SelectorContext is built; where to plumb scheme; how the per-page section list is derived).
- queries: SELECT function, is_active, is_dark_section, section_type, component_level, suitable_site_types FROM content_components WHERE function IN ('hero','call-to-action') ORDER BY function, is_active;  AND  SELECT id, function, is_active, is_dark_section FROM content_components WHERE id IN ('9644c86f-18b0-4f75-b086-5b79a74a48d7','09034086-a581-4bba-a5b4-760d863bb2df');  AND a scan for any light header/footer: SELECT function, is_active, is_dark_section FROM content_components WHERE (function LIKE 'header-%' OR function LIKE 'footer-%') ORDER BY function;
- (later) how the ORIGINAL build chose idea.uk's header (9644c86f) given no layout default — which build step assigns site_components.header. update_site_defaults action + where it's invoked.

═══════════════════════════════════════════════════════════════════════
## CHECKPOINT 2026-06-26 (nnn) — CORRECTION: the scheme-aware-selector fix (mmm) does NOT help idea.uk. The real lever is the COMPONENTS, not the selector. Library lacks light variants.
═══════════════════════════════════════════════════════════════════════
Read plan_sections_action.go fully + the active-component query. Two corrections to (mmm):

1. SECTION RESOLUTION IS DIRECT-FUNCTION (Path 1), NOT the selector, for idea.uk. plan_sections: Path 1 = components[sectionName] direct lookup by content_components.function (loadComponentSchemas); the comment says "All current sites hit this path — their planners output function names." Path 2 (component_selector, the scheme-blind scorer) ONLY runs when a section NAME doesn't match a function directly (section_type variants). idea.uk's sections are named hero/call-to-action/tool-list/latest-news = functions -> Path 1 -> THE single active component per function (uniqueness index = exactly one active/function). So making component_selector scheme-aware would NOT change idea.uk's sections — the selector isn't even invoked. (mmm's "make component_selector scheme-aware" fix is necessary for variant selection generally but does NOT fix idea.uk on its own.)

2. THE LAYOUT css_template (styles.css) MOSTLY DOESN'T STYLE THE SECTIONS — COMPONENTS SELF-STYLE. tool-portal-light styles .hero-section/.tools-grid/.tool-card/etc; the COMPONENTS emit DIFFERENT classes (.hero, .cta-section, .tool-list-section .tl-card, .latest-news-section .news-card) and carry their OWN inline <style>. So the layout's section rules largely don't apply (class-name mismatch); the page's section appearance is driven by each COMPONENT's inline <style>. What the layout DOES contribute that reaches components: the :root palette vars (--color-*) + generic element rules (body, h1-h6, a). That's WHY tool-list/latest-news render parchment (their inline <style> reads var(--color-background)/var(--color-text)) and hero/cta/header/footer render dark (their inline <style> hardcodes dark or sets dark --section-* or uses wrong var names). Overlap cases: .site-footer + .latest-news-section ARE styled by both layout and component; the component's later inline rule wins. So: COMPONENT inline <style> is the lever for section/header/footer light-vs-dark; the layout mainly sets the palette + element defaults.

DATA: active components (is_active=t): hero -> is_dark_section=t (DARK); call-to-action -> is_dark_section=t (DARK); both have an inactive twin (the two hero rows differ only by var name: buggy --accent-color vs fixed --color-accent; STILL don't know which is active — need is_active paired with html_template). idea.uk site_components: header=9644c86f function "site-header" is_active=f is_dark_section=f (renders dark gradient anyway -> the is_dark_section flag is unreliable here / dark is hardcoded in its template); footer=09034086 function "site-footer" is_active=f is_dark_section=f (renders #1A1816 dark). Header/footer library: LIGHT (is_dark_section=f) options = header-with-cart-or-nav, header-with-categories (+ inactive header-docs light). NO light footer exists (only footer-with-disclaimer, dark). NO light hero or light cta exists (only the dark actives).

REAL ROOT CAUSE (final): the component library is DARK-ORIENTED. The active hero/cta/header/footer are all dark, there are NO light variants, and section->component is a direct per-function lookup (one active component per function). So there is nothing light to select. tool-portal-light set the palette correctly (parchment :root -> the var-reading components went light) but the dark components override with hardcoded dark. The lever to make idea.uk light is the COMPONENTS, not the layout/selector.

THE FORK (needs user decision — scope + idea.uk go-live timing; £29 VM tool unaffected, no urgency):
A. STRUCTURAL scheme dimension (the framework's real gap, matches "fix framework"): author light variants (light hero, light cta, light footer; confirm/author a tool-portal-light-matching light header) as distinct functions under shared section_types; move section planning to section_type references so Path 2 (selector) is used; make component_selector scheme-aware (score is_dark_section vs site scheme — the field is already loaded); derive header/footer from the layout's scheme (layouts.default_{header,footer}_component_id + update_site_defaults in the build). Large; touches library + write_site_plan + selector + install + site-component wiring.
B. PRAGMATIC for idea.uk now: fix the active hero's var-name bug (-> rust button); author a light footer (none exists) + re-point idea.uk site_components.footer to it; re-point site_components.header to a light header (header-with-categories/-cart-or-nav, or author a tool-portal-light header); ACCEPT the dark hero + dark cta as dark callout sections on an otherwise light site (header/footer/content light). Smaller; idea.uk becomes "light with dark hero/cta accents". Still needs ≥1 new light component (footer) + per-site header/footer wiring; does NOT make hero/cta light.
C. MINIMUM: just fix the navy-button bug + accept the rest. Not a light site.

RECOMMENDATION: lean A (it's the actual gap and the only path to truly-light sites), but A is a project; B gets idea.uk presentable sooner. The hero/cta being dark "callout" sections on a light body is a legitimate look — so the question is whether idea.uk must be fully light or whether dark hero/cta on a light (header/footer/content) site is acceptable. The --accent-color->--color-accent hero fix is in scope for all three.

STILL NEED: which hero is active (pair is_active with the var name): SELECT function, is_active, (html_template LIKE '%--accent-color%') AS buggy_var, (html_template LIKE '%--color-accent%') AS correct_var FROM content_components WHERE function='hero' ORDER BY is_active;
