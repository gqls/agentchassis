{
  "submitter": "bugfix_302_design_repair_verification lane, acting on bugs_open/317 (owner decision 3, 2026-08-19)",
  "rationale": "ROUND 2. Round 1 was REVISE on a GATING HIGH from editquality, and it was right \u2014 not about the code, but about what my submission demonstrated. Every objection is answered below by a measurement or a code change, none by argument.\n\nTHE GATING OBJECTION, and what it correctly spotted. It observed that my own doc note said \"this migration's own comment tripped it\" about spelling the exclusion clause in prose above the real declaration, and that the plan never showed whether that was an unfixed defect or a corrected near-miss. It was a corrected near-miss \u2014 during authoring, a sentence containing the literal clause sat above the declaration and the regex parsed '...' as the only 'item_type'. I caught it, fixed the ordering, and verified exactly one occurrence parsing 14 types. But the reviewer is right that a fix resting on my having noticed is not a fix, and that the guard reads the newest migration by design, so the NEXT author inherits the same trap with only a comment to protect them.\n\nSO THE PROSE WARNING IS NOW A CHECK. The guard asserts that every entry it parsed is a well-formed item_type (^[a-z][a-z0-9_]*$). Prose therefore fails LOUDLY and with the true cause named \u2014 'the exclusion clause parsed to \"...\", which is not an item_type; the regex takes the FIRST match in the file, so a COMMENT above the real declaration is read instead of it' \u2014 rather than producing 14 confusing 'this type is not excluded' errors about the roster, which is what the unaided guard would have said. MUTATION M10 REPRODUCES THE EXACT TRAP: insert a prose line spelling the clause above the declaration, and the guard fails with that message; restored, control green. The guard now defends itself against the trap it was written beside.\n\nTHE MEDIUM (how the test locates the file and resolves ties) is specified in code and was simply absent from my plan: `filepath.Glob` over `*_claimed_item_timeout_generic_evidence.sql`, `sort.Strings`, take the last \u2014 numeric order, because every file in sql_for_agents carries a zero-padded 3-digit prefix. The chosen filename is named in every failure message, so a wrong pick is visible rather than silent. A later migration that touches this predicate must therefore follow the same naming convention; that is now stated in the register entry and in the migration's own header.\n\nTHE MISLABEL, which two seats caught independently and which I am glad they did: I marked the `verifier_coverage_test.go` edit `remove`. It is a function-level MODIFY. [MEASURED] that file retains its three other tests \u2014 TestEveryItemTypeIsVerifiedOrAnAcknowledgedGap, TestEveryCheckProducedItemTypeIsClassified, TestVerifierCoverageIsReported \u2014 none of which is touched. Read as written, my label said I had deleted the estate's verifier coverage guard. Corrected below.\n\nGUARDIAN'S SIBLING-SWEEP QUESTION, answered with the query rather than asserted: exactly ONE scheduled task's pre_query contains the `item_type NOT IN` convention \u2014 `claimed-item-timeout` itself. No sibling sweep reads this fragment or shares the convention, so widening it cannot reach another sweep.\n\nGUARDIAN'S CIRCULARITY QUESTION: confirmed non-circular by construction and by the compiler \u2014 `actions` already imports `discovery_checks` in production code (complete_work_item_verification.go), so the test adds no new edge; the reverse edge does not and cannot exist, which is precisely why the guard had to move.\n\nDEBUG_HISTORIAN'S BACKUP POINT, accepted and acted on: a rollback sidecar reverses the KNOWN edit, which is not the same insurance as a snapshot of the prior text. The pre-image row is now recorded in the lane (PREIMAGE_2026-08-19_*.txt), with a note that it is a record and not a restore script.\n\nARCHITECTURE'S WATCH ITEM, recorded rather than actioned: at a THIRD completion-gate class this pairwise test should become a single registry rather than another pairwise comparison. Noted in the register beside the existing two-gate watch item.\n\nUnchanged from round 1: the defect, the measurements, the live-column verification, and the scope argument (council gate, not an RFC \u2014 this makes the implementation match a guarantee the mechanism already claimed, for one item_type, and widening a refusal list is the safe direction).",
  "plan": {
    "summary": "Widen the claimed-item-timeout exclusion contract from 'has a registered verifier' to 'has a registered verifier OR a noChangeGates entry', apply it to the live scheduled_tasks.pre_query via a guarded migration, and move the parity guard to the one package that can read both rosters.",
    "edits": [
      {
        "file": "docs/agent_docs/sql_for_agents/482_claimed_item_timeout_generic_evidence.sql",
        "symbol": "scheduled_tasks.claimed-item-timeout.pre_query",
        "operation": "config_change",
        "rationale": "Owning pipeline: the work-item dispatch/completion path (site-work-orchestrator and the dispatch loops), whose completions this sweep short-circuits. Adds dark_section_audit to the exclusion list on the LIVE column. Guarded as a DO block rather than a bare UPDATE: a read-before-write anchor aborts if the predicate has moved since this was written, an idempotence check aborts on a second application rather than appending a duplicate, and the post-write verification RAISEs \u2014 because ON_ERROR_STOP ignores a non-empty result set, so a verification block built from SELECTs cannot stop the COMMIT. 220 is deliberately NOT edited: it is applied history and editing it would make its recorded checksum a lie.",
        "sketch": "+DO $$\n+DECLARE old_tail text := '...decision_regression, needs_brand_head_assets';\n+        new_tail text := '...decision_regression, needs_brand_head_assets, dark_section_audit';\n+BEGIN\n+  -- abort unless the live predicate is the shape this was written against\n+  IF n <> 1 THEN RAISE EXCEPTION 'ABORT: ... predicate has changed ...'; END IF;\n+  -- abort if already applied (no duplicate entry)\n+  UPDATE scheduled_tasks SET pre_query = replace(pre_query, old_tail, new_tail) WHERE name='claimed-item-timeout';\n+  -- RAISE-based post-write verification\n+END $$;"
      },
      {
        "file": "docs/agent_docs/sql_for_agents/482_ROLLBACK_claim_timeout_exclusion.sql",
        "symbol": "rollback sidecar",
        "operation": "add",
        "rationale": "Removes the added type and verifies the removal by RAISE. Its own comment states the cost of using it: applying the rollback re-opens bugs_open/317.",
        "sketch": "+UPDATE scheduled_tasks SET pre_query = replace(pre_query, ', dark_section_audit', '') ...\n+IF n <> 0 THEN RAISE EXCEPTION 'ABORT: rollback did not remove it'; END IF;"
      },
      {
        "file": "platform/orchestration/actions/claim_timeout_exclusion_lockstep_test.go",
        "symbol": "TestClaimTimeoutExclusionCoversBothCompletionGates",
        "operation": "add",
        "rationale": "The widened guard, in the only package that can see both rosters. Asserts the contract in BOTH directions over the union, and asserts each roster is non-empty SEPARATELY so a failure names which one went missing \u2014 without that, emptying noChangeGates would silently narrow the guard back to the gate-2-only contract this change exists to widen. Reads the NEWEST matching migration, because 220 stays as history and a later migration owns the live predicate.",
        "sketch": "+gated := map[string]string{}\n+for _, t := range checks.RegisteredVerifierItemTypes() { gated[t] = \"a registered verifier (gate 2)\" }\n+for t := range noChangeGates { gated[t] = \"a noChangeGates entry (gate 1b)\" }\n+for t, why := range gated { if !excluded[t] { t.Errorf(\"%q has %s but is NOT excluded\", t, why) } }\n+for t := range excluded { if _, ok := gated[t]; !ok { t.Errorf(\"%q is excluded but NO gate can grade it\") } }"
      },
      {
        "file": "platform/orchestration/actions/discovery_checks/verifier_coverage_test.go",
        "symbol": "TestRegisteredVerifiersMatchClaimTimeoutExclusion (function removed; file retains its 3 other tests)",
        "operation": "modify",
        "rationale": "FUNCTION-LEVEL modify, not a file removal \u2014 my round-1 label said 'remove' and two seats correctly objected. [MEASURED] the file retains TestEveryItemTypeIsVerifiedOrAnAcknowledgedGap, TestEveryCheckProducedItemTypeIsClassified and TestVerifierCoverageIsReported, none touched. The one lockstep function is deleted because once the exclusion list carries a type with no registered verifier its reverse direction fails BY DESIGN \u2014 proven to fail before deletion, not assumed. A comment is left in its place explaining where the guard went and the import-direction reason it could not stay, so no reader concludes a guard was silently dropped.",
        "sketch": "-func TestRegisteredVerifiersMatchClaimTimeoutExclusion(t *testing.T) { ... }\n+// The claimed-item-timeout lockstep guard USED TO LIVE HERE. It moved 2026-08-19 to ... because noChangeGates lives in `actions`, which imports this package."
      },
      {
        "file": "docs/agent_docs/docs026_concept_register/register/work-item-integrity.md",
        "symbol": "WII-019",
        "operation": "modify",
        "rationale": "Registers the widened seam in the same commit that ships it, per the 2026-07-28 ordering exemption condition (2). Records the package-move reason, the newest-migration rule, the latency measurement, the mutation matrix, and the authoring trap (a migration in this family must not spell the exclusion clause in prose above its real declaration \u2014 the regex takes the first occurrence, and this migration's own comment tripped it).",
        "sketch": "+### WII-019 \u2014 The claim-timeout exclusion is the lockstep twin of BOTH completion gates, not just the verifier registry"
      },
      {
        "file": "platform/orchestration/actions/claim_timeout_exclusion_lockstep_test.go",
        "symbol": "itemTypeShapeRe + the well-formedness assertion",
        "operation": "modify",
        "rationale": "ROUND 2, answering the gating objection. Turns the prose warning into a check: every parsed entry must match ^[a-z][a-z0-9_]*$, so prose above the declaration fails with the true cause named instead of 14 misleading roster errors. Mutation M10 reproduces the original authoring trap and the guard catches it.",
        "sketch": "+var itemTypeShapeRe = regexp.MustCompile(`^[a-z][a-z0-9_]*$`)\n+for itemType := range excluded {\n+\tif !itemTypeShapeRe.MatchString(itemType) {\n+\t\tt.Fatalf(\"the exclusion clause in %s parsed to %q, which is not an item_type. The regex takes the\\n\"+\n+\t\t\t\"FIRST match in the file, so a COMMENT above the real declaration is read instead of it.\", ...)\n+\t}\n+}"
      }
    ],
    "grounded_in": [
      "Migration 220's own comment, which states the contract being widened: 'The item_type exclusion is the LOCKSTEP TWIN of the RegisterVerifier() calls in platform/orchestration/actions/discovery_checks/: those item types have a Go verifier that\u2026'",
      "MEASURED 2026-08-19, the live predicate BEFORE: 13 item types, exactly matching the 13 RegisterVerifier/RegisterVerifierWithPolicy call sites \u2014 so the gate-2 lockstep was intact and the gap was specifically gate 1b.",
      "MEASURED 2026-08-19, archive-inclusive (site_work_items UNION site_work_items_archive, because the live table is a ~7-day window): 0 of 30 dark_section_audit and 0 of 498 hardcoded_section_colors completions carry the sweep's completed_by_step shape. Never fired.",
      "MEASURED 2026-08-19: scheduled_tasks 'claimed-item-timeout' enabled=true interval_seconds=120; 'improvement-sweep' enabled=false; 'site-discovery-rotation-design' enabled=false. The sweep runs constantly; it is the CLAIMING that is absent, which is why this is latent rather than structural.",
      "VERIFIED at the live column after applying: the exclusion list holds 14 item types and its set is IDENTICAL to the one parsed out of the declared migration file; dark_section_audit appears exactly once; a second application ABORTS with 'this migration has been applied'.",
      "Mutation matrix, exit-status gated with an unmutated control and both files restored byte-identical (diff -q): drop the type from the list -> RED ('has a noChangeGates entry (gate 1b) but is NOT excluded'); add a type no gate can grade -> RED ('NO gate can grade it'); empty noChangeGates -> RED ('gate 1b is inert').",
      "The import direction that forced the package move: platform/orchestration/actions imports platform/orchestration/actions/discovery_checks (verified in complete_work_item_verification.go's import block), so a test in discovery_checks cannot read noChangeGates at any cost.",
      "bugs_open/006 \u00a7C is why the reverse direction must stay: the sweep's generic branch exists because 15 of 18 types had no evidence test and were re-running finished work, so an over-wide exclusion list reintroduces that churn.",
      "MEASURED 2026-08-19, answering guardian: SELECT name, enabled FROM scheduled_tasks WHERE pre_query ILIKE '%item_type NOT IN%' returns exactly ONE row, claimed-item-timeout itself. No sibling sweep shares the convention.",
      "MEASURED 2026-08-19, answering editquality and guardian on the mislabel: verifier_coverage_test.go still contains 3 test functions after the edit (TestEveryItemTypeIsVerifiedOrAnAcknowledgedGap, TestEveryCheckProducedItemTypeIsClassified, TestVerifierCoverageIsReported). The edit is a function-level modify, not a file removal.",
      "Mutation M10 (round 2), the gating objection made falsifiable: inserting a prose line containing the literal exclusion clause above the real declaration makes the guard FAIL with 'the exclusion clause in 482_... parsed to \"...\", which is not an item_type'. Restored byte-identical, control green. Run on a clean `git archive HEAD` overlay carrying only this file, because another session holds an uncommitted duplicate-symbol build break in the package (reWindowOnload).",
      "The import edge the guardian asked to see confirmed rather than asserted: platform/orchestration/actions already imports platform/orchestration/actions/discovery_checks in PRODUCTION code (complete_work_item_verification.go's import block), so the test adds no new edge and the reverse edge cannot exist \u2014 which is the reason the guard had to move package at all."
    ]
  }
}