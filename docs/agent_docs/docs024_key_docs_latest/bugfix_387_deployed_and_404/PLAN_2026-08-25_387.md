# Plan — `bugs_open/387` (session `bugs_open/387`)

## Context — what 387 claimed, and what is actually true (all first-hand, 2026-08-25)

**Ownership check.** `who-owns 387` → the `site_ai_agent_orchestration` lane is *cited* (it received a CONTRIB) but has not acted: no commits in its dir since the CONTRIB, no dirty files, no live session named for it. The filing session (`bugs_open/364`) is live, has **stood down** on these pages and messaged me. Nobody is working 387 → resumed here. (Replied to 364 with the corrections below and its requested build result.)

**Claim 1 — "three pages deployed and 404" — REFUTED, with controls.**
- `pages.url` for the three is `/adoption-tracker.html`, `/protocol-tracker.html`, `/model-directory.html`; all **200** at that URL. Every page on the site 404s in the extensionless form the filer curled (`/about`, `/pricing`, `/contact`, `/services` → 404), because the B2/Cloudflare worker deliberately does not resolve slashless paths (`scripts/cloudflare/worker.js:40-44`). Nav, `sitemap.xml`, `<link rel=canonical>`, `og:url` all say `.html`. Fleet: 100% of active `pages.url` carry `.html`/`index.html` (one exception on idea.uk).
- The filer's invented-URL control proved the domain *discriminates*; it could not prove the URL *form* because it had the same form. The missing control was a known-good page at the same form.
- **Third occurrence of the identical wrong call**: `WRONG_CALLS.md` 2026-07-27 (`/sale` vs `/sale.html`), `LANDMINES.md:7890` "A page's served URL is NOT derivable from `pages.name`" (2026-08-09, bugfix_220), now 387 (2026-08-24). Recorded twice, still repeated by a third lane ⇒ automate it (WP4).
- **Fleet demand control** (sweep 2026-08-25 ~10:40Z, `pages.build_status='deployed' AND status='active'`, curled at `pages.url`): **709 pages, 0 × 404** (698 × 200, 7 × 302 webdesign.uk's deliberate off-domain redirect, 1 × 301 idea.uk/privacy, 3 transient TLS `000` on fundamentallyai that 200 on re-probe). The "deployed-and-404" class has **no live instance today**.
- The linked case is already covered fleet-wide by `dead_internal_link_live` (`check_site_structural_validity.go:534`; 16 items `detected` fleet-wide) and the seam "non-200 in the divergence check is a skip, availability is `site_unreachable`'s" is a *recorded design decision* (register DGH-015 line 95, IMP-053 D4). ⇒ **Do NOT build a per-page reachability check.** Zero demand + existing coverage + a documented seam. Record this decision with the dated measurement in the bug file.

**Claim 2 (387 §3) — the `NNN+` placeholder — REAL, and WORSE than filed: it is PUBLIC.**
`https://ai-agent-orchestration.com/model-directory.html` serves *"…against the NNN+ agent types already running in production"* (curled 2026-08-25 10:2xZ). The hero was **regenerated today 06:30Z and still says NNN+** — repeatable, not a one-off.

**Root cause — established first-hand, evidence in NOTES (the 07-31 ruling's substitution for a 090 run: the deciding evidence is the executed prompt itself, read from `llm_call_log`, not an inference):**
1. Migration `557` (aiao lane, applied 2026-08-22) rewrote the site's `evidence_base.writer_block` to read: *'take the live value from the `aao-agent-definitions` fact. Phrase it as "NNN+ AI agents" or "NNN+ agents in the registry"'* — `NNN` meant "the number goes here". Its own guard **requires** that literal (`557…sql:227`). There is no substitution machinery behind `NNN` anywhere.
2. The writer's template (`page-content-writer` `default_config`) injects **only `evidence_base.writer_block`** under "## Verified Facts" on the unscoped path; the facts list with values is **not** in the prompt. Prompt `9ba94176` (today's hero): hand-written block present, composed `{value}` block absent, writer_line absent, **zero occurrences of `200`** (the fact's value). The writer was told to read a value it was never shown.
3. Measured over `llm_call_log` since 08-22: **137** generate_content calls carried the instruction → **14 copied `NNN` verbatim (10%)**, **0 wrote the agents value**, 1 wrote the types value. No writer response contained `\mNNN\M` before 08-22. So the owner-ruled lower bound has been effectively unstated for three days and the placeholder ships ~1 in 10 rebuilds — and `model-directory` rebuilds every ~6h (`needs_page` "model data refreshed": 08-24 12:24, 18:24; 08-25 00:24, 06:26).
4. Three detectors miss it: `placeholderPatterns` is a substring list with no numeric shape (`validate_page_content.go:105-174`); `templateVarRegex` wants `{{`; `model-directory` is in `editorialPageTypes` so `ScanUnregisteredNumbers` never runs there (`claims.go:840-849`) — and `NNN` has no digit anyway. *(364 session, 2026-08-25 ~11:00Z: it is removing the three tracker page types from `editorialPageTypes` — uncommitted — so the third reason lapses; the first two stand and the numeric-placeholder blocker is still the only detector that has the shape.)*
5. The machinery that would have prevented it **already exists and this site never opted in**: `composeWriterBlock` (`refresh_evidence_base_action.go:1187`) substitutes `{value}` into human `writer_line`s when `writer_block_managed: true` (6 sites); the scoped path `composeScopedWriterBlock` does the same per section (13 of 891 current plan sections, 3 sites). aiao's own NOTES name why it can't opt in: managed mode **drops the hand-written NEVER-STATE list** (composeWriterBlock emits only NUMBERS / CAPABILITIES / allowed_entities). That gap is the fleet-wide door left open: **13 unmanaged sites have a hand-typed writer_block as their only channel for numbers**, which is exactly where a human types a stand-in.

**Fleet census of the candidate detector regex** (`\mN{2,}\+|\mNNN\M|\mX{2,}\+|\mN,NNN\M`) over `rendered_html` of every active page + all `site_components`, 2026-08-25: **exactly 1 hit** (the aiao hero), **0 false positives**. Two shapes deliberately excluded because they convict honest copy today: bare `XX` (`siglo XX`, relojistas) and `[number]` (a quoted fill-in template on an idea.uk guide).

## Decisions (recorded in PLAN with reasons)
- **D1** 387's headline is a wrong call, not a defect: correct the record; do not "fix" the deploy. Bug stays OPEN on §3 only, re-scoped.
- **D2** No new reachability check (measured 0/709; linked case covered; seam recorded). The class fix for the *claim* is WP4 (make the right probe one command).
- **D3** Fix the live defect at **source first** (config, live immediately), then the **detector** (Go, inert until a roll) — the reverse order would refuse rebuilds while the NNN+ page stays live.
- **D4** Never hand-edit the page (owner ruling 2026-08-04). The 6-hourly `needs_page` rebuild regenerates the hero once the block is fixed; verify at the artefact.
- **D5** The door-closer (WP5) is a framework change on a file the `bugs_open/288` session is editing **right now** → CONTRIB + message first; build only if they agree or hand it over. Not architecture-scope (opt-in key, unsafe default OFF, zero consumers — RFC_022), but it changes what managed mode carries, so both consumers (aiao, 386 lane) are told.

## Work packages (in order)

### WP0 — Standing five + tell the lanes (first, before any code)
- Create `docs/agent_docs/docs024_key_docs_latest/bugfix_387_deployed_and_404/` with `PLAN_2026-08-25_387.md` (this content + decisions), `RUNBOOK_387.md` (the queries above — regex census, copy-rate query, prompt-read by id, sweep command), `NOTES_387.md` (append-only; include my own misstep: I curled composed URLs for the two other census hits before reading `pages.url` — same trap, caught in-session), `README_where_we_are.md`, `SUMMARY_2026-08-25_387.md`.
- CONTRIB files: `site_ai_agent_orchestration/CONTRIB_2026-08-25_from_387_your_557_block_ships_NNN_and_the_pages_were_never_404.md` (mechanism, the 14/137/0 measurement, the interim migration I will apply to their site's spec, the managed-mode path and its blocker, ⚠ `aao-agent-types` writer_line carries a literal `as of 2026-07-26` beside `{value}`); `bugfix_364_claims_whose_number_is_it/CONTRIB_2026-08-25_from_387_the_pages_serve_and_your_5a_6f_rest_on_it.md` (do not edit their words); message `bugs_open/386` (counting-fact drift is the sibling mechanism) and `bugs_open/288` (WP5 touches their file).

### WP1 — Correct the record
- `bugs_open/387…md`: append `> **CORRECTED 2026-08-25**` block: headline refuted (evidence + controls), §3 promoted to the live incident with root cause, D2 with the 0/709 sweep, what remains open. Forward-only: never rewrite §1–§4.
- `WRONG_CALLS.md`: one entry — third occurrence; the control that was missing (known-good page, same form); tally → automated (WP4). Plus two short entries of my own: (a) I curled composed URLs for the two other census hits before reading `pages.url` (same trap, caught in-session); (b) **I offered the 364 lane a clean `model-directory` build (06:26Z) as confirmation of its fix; its roll landed 09:27Z — the build predates the binary by 3h.** Caught by the peer, not by me. The check I skipped: date the build against the service's `build provenance` stamp before calling it evidence of any fix (MEMORY: "a roll is not evidence your fix shipped — per SERVICE").
- `LANDMINES.md:7890` entry: dated addendum — "the check is now a command: `scripts/probe-page-url.sh`"; then `./scripts/landmines-verify-dispatch.sh`.
- Memory: extend `a-parked-domain-200s-every-path.md` (sibling lesson: curl the RECORDED `pages.url`; a known-good page at the same form is the second control); update its MEMORY.md line within budget. New memory `a-quoted-exemplar-in-a-prompt-is-copied-verbatim.md` (feedback/project: a stand-in token in a writer-visible field ships ~10% of the time; the value must reach the prompt by code).
- 016b §9: pointer entry "a stand-in token in a writer_block is copied literally and no detector has its shape" → points at 387.

### WP2 — Source fix (config, live immediately; aiao's site, told in WP0)
- Migration `docs/agent_docs/sql_for_agents/<next free, 611 at time of writing>_aiao_writer_block_no_stand_in_tokens.sql` in 557's style (snapshot, close current `evidence_base` row, insert successor, `DO … RAISE` guards). The successor `writer_block`:
  - removes every `NNN` token and every quoted exemplar containing a stand-in;
  - says plainly: *no letters standing in for digits, ever; if a count is not written in this block, write no count*;
  - carries the two human-authored stale-proof floors verbatim from the facts' own `writer_line`s ("more than 150 active agent definitions…", "more than 150 distinct agent types") — floors don't go stale as the value rises, and 150/200 is inside the owner's 07-27 tolerance (the objection was 70 vs 175); flag this wording to the aiao lane as theirs to change;
  - keeps every ban (70+, 30+, NEVER-STATE list, rolling-window rules) — guards assert each is still present, and assert `writer_block !~ '\mN{2,}\M'` and no `NNN`.
  - `_ROLLBACK.sql` sidecar restores the 557 row.
- Apply with the runner **scoped to this one file** (LANDMINE: `MIGRATIONS_DIR=` on the same line); dry-run first.
- Let the next 6-hourly `needs_page` rebuild `model-directory` (≈12:24Z), or fire one by inserting a `needs_page` row of the same shape as `c73d25b1` (read its spec first); also rebuild the two trackers (their 08-24 18:24/18:25Z `needs_page` items sit at `needs_human_review`) — the 364 lane needs a build that **starts after 09:27:24Z 2026-08-25** on `agent-chassis` (stamp `4c996e1b5` at 10:44Z; re-read the stamp at the time, per service). **Verify at the artefact**: `content_data` hero has no `NNN`; live `model-directory.html` has no `NNN`; message 364 with the orchestration id, its start time vs the stamp, and the `unregistered_number` count in `agent_error_log` (their prediction: zero).

### WP3 — Detector (Go, council-gated, inert until a roll)
- `platform/orchestration/actions/validate_page_content.go`: add `placeholderRegexes` (numeric stand-ins: `\bN{2,}\+`, `\bNNN\b`, `\bX{2,}\+`, `\bN,NNN\b`, label "numeric stand-in placeholder"), scanned over the same prose blocks `checkPlaceholderPatterns` already uses (`ExtractAssertionText` + `headProseBlocks`), severity `blocker`, `Type: placeholder_text`, `Value` = matched text. Keep the substring list untouched.
- `validate_page_content_placeholder_test.go`: positives — the live hero sentence, "NN+ clients", "X,XXX+"; negatives — `siglo XX`, `N+1 queries`, `[number]` inside a quoted template, `XXL`, roman numerals, `NNN` inside `<script>`. Mutation check: delete the regex list → the positive test must fail.
- Measured control in the submission: the census above (1/1 true positive, 0 FP fleet-wide, dated) — and re-run it after WP2 lands (expect 0).
- Council: submission JSON (`rationale`, `submitter`, `plan` ≤8 edits with real hunks, `grounded_in`), `097_TRIGGER`, commit with `Council-Submitted: <corr>`; `Council-Reviewed:` only on a read APPROVED. Tell the `218`/mcalc lane (placeholder-scan owner) via CONTRIB — this widens the scan they narrowed, in the prose-only scope they set.
- `scripts/verify-head-builds.sh --with … --test` before commit; commit by pathspec; bump `IMAGE_TAG` only if I roll (owner runs `make release` — do not one-service apply).

### WP4 — Make the right probe one command
- `scripts/probe-page-url.sh <domain> [<page-name>…|--all]`: reads `sites`/`pages.url` from the DB, curls each recorded URL verbatim, runs **both** controls per domain (invented `/zzz-control-not-a-page-$RANDOM.html` must be non-200; a sibling `deployed` page must 200), refuses a composed URL, exit 0/1/2 like the check fleet (2 = could not run, never a pass), `--self-test`. Register entry (new; say "built, exercised on 709 pages 2026-08-25"). Cross-link from LANDMINES 7890 addendum and the RUNBOOK.

### WP5 — Door-closer (framework; CONTRIB-first, owner of file busy today)
- `composeWriterBlock` (`refresh_evidence_base_action.go:1187`) gains an opt-in `writer_block_guidance` (string, verbatim, appended as its own section) so a site can flip `writer_block_managed: true` without losing its NEVER-STATE list; test; register entry; RFC_022 conditions enumerated (opt-in, default OFF, `SELECT count(*) FROM site_specs WHERE data ? 'writer_block_guidance'` = 0). Council. Then aiao (their call) opts in and the interim block regenerates from `writer_line`s with live values — the number reaches the prompt by code, never by a human typing it.
- If `288` is still on the file when WP1–4 are done: leave WP5 as a CONTRIB with the diff and stop.

## Files
- New: `docs/…/bugfix_387_deployed_and_404/{PLAN,RUNBOOK,NOTES,README_where_we_are,SUMMARY}`; 2 CONTRIBs; `sql_for_agents/611_aiao_writer_block_no_stand_in_tokens.sql` + `_ROLLBACK`; `scripts/probe-page-url.sh`; 1 memory file.
- Edit (append only): `bugs_open/387…md`, `WRONG_CALLS.md`, `LANDMINES.md`, `016b…md`, `MEMORY.md`, `a-parked-domain-200s-every-path.md`, concept register (2 entries + index count).
- Edit (code): `validate_page_content.go`, `validate_page_content_placeholder_test.go`; WP5 only with agreement: `refresh_evidence_base_action.go` + test.
- Not touched: the page itself; 364's or aiao's words; the discovery checks; the divergence/unreachable seam.

## Verification
1. WP2: `SELECT data->>'writer_block' … !~ 'NNN'`; next `model-directory` build completes (`orchestration_states` COMPLETED, `agent_error_log` count for that orchestration); `content_data` hero and live `model-directory.html` contain no `NNN`; regex census → 0.
2. WP3: `go test ./platform/orchestration/actions/ -run Placeholder` green; mutation (regex list removed) red; `verify-head-builds.sh --with`; council verdict read; after the next fleet roll, `kubectl logs … 'build provenance'` + `git merge-base --is-ancestor` on `agent-chassis` specifically; then bug 387 → `bugs_closed` (fixed AND live).
3. WP4: `--self-test`; run on aiao (3 × 200 + both controls) and on a site whose control 200s if any still exists; exit codes proven in both directions.
4. Comms: 364 gets orchestration id + error count; aiao, 386, 288, 218 lanes have their CONTRIB/message.

---
*This PLAN is the owner-approved plan of 2026-08-25 (session `bugs_open/387`), verbatim. Decisions D1–D5 live in it; corrections land below as dated blocks, never as edits above.*
