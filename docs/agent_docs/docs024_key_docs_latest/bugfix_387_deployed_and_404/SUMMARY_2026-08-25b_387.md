# SUMMARY — 2026-08-25b — bug 387 (second of the day: the build work is done and approved)

**What we're trying to do.** Resolve bug 387: filed as "three pages published but returning
not-found", actually a public unrendered "NNN+" stand-in plus a repeated measurement mistake.

**Where we've come from.** This morning's summary: headline refuted with the missing control,
mechanism traced to instruction text quoting "NNN+" over a number the writer is never shown,
plan approved.

**What we've done since.** The instruction fix (migration 611) applied and PROVEN — the page's
own scheduled rebuild at 12:41 produced honest floors ("more than 150 distinct agent types")
and the public page now serves them with zero stand-ins. The publish-time checker change went
through FOUR council rounds and is APPROVED — each round genuinely improved it (an unmeasured
pattern cut; the scope claim corrected to what the gate actually covers, with the unguarded
edit paths pinned as a known gap; double-counting eliminated; per-block reporting from the
approval's own advisory). The durable piece — hand-written "never say this" guidance that
survives regeneration — is built, APPROVED in two rounds (the hard question, whether a typed
round-trip would delete the new field, was answered at the code and pinned by tests of the real
write paths), registered as CLM-029, and built inside the owning thread's window with its
agreement. The wrong-call automation (probe-page-url.sh, OPP-010) is live and proven both ways.
Four peer threads coordinated: the filer corrected its records and caught one mistake of ours;
the site's own lane fixed its adjacent instruction defects the same day; a duplicate
investigation stood down and contributed a dated check.

**Where we are now.** Everything buildable is built, committed, reviewed and approved. The
checker and the guidance carry are inert until the next software release (v1.0.1338, pinned,
owner's to run — it ships another thread's fixes under the same tag, so verification is
per-service by binary probe, negative control already run). The bug stays open until that
release makes the checker live.

**Where we're going.** Tonight: the two tracker pages rebuild (~18:24) — the discriminating
test for the filer thread's own fix, reported to them as promised. Tomorrow ~09:06: confirm the
instruction fix survives the daily register refresh (query pinned in NOTES). On the release:
binary probe, re-run the stand-in census (expect zero), then move 387 to closed as
fixed-and-live, re-scoped to what it actually was.
