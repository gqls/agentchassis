# NOTES — bugfix_387 (append-only, newest at the bottom)

## 2026-08-25 — session `bugs_open/387` picks the bug up

- Ownership: `who-owns 387` → aiao lane cited but inactive since 08-24; filer (`bugs_open/364`) live and STOOD DOWN on these pages (its message, ~10:5xZ). Nobody working it → resumed.
- **Headline refuted in the first hour, with controls.** `pages.url` carries `.html` for all three; all 200. `/about`, `/pricing`, `/contact`, `/services` extensionless → 404 (the known-good-page control the filing lacked). Worker deliberately does not resolve slashless paths (`scripts/cloudflare/worker.js:40-44`). Nav/sitemap/canonical/og:url all `.html`. Fleet: 100% of active `pages.url` end `.html`/`index.html` bar one idea.uk row.
- **MISSTEP (mine), caught in-session:** verifying the two other census hits (idea.uk `[number]`, relojistas `XX`) I curled `https://idea.uk/guides/testing-it.html` and `https://relojistas.com/glosario-cronografo.html` — COMPOSED urls → 404/404, momentarily read as two more damaged pages. `pages.url` says `/guides/testing-it/index.html` and `/glosario/cronografo.html` → 200/200. The exact trap under investigation, made while investigating it. Check: read `pages.url` first, always (RUNBOOK §1).
- Both other census hits are FALSE positives for a placeholder detector: `siglo XX` is Spanish prose; `[number]` sits inside a deliberately quoted fill-in template in a guide. Recorded so the detector regex never grows those shapes without a fresh census.
- **NNN+ is PUBLIC** on model-directory.html (curled 10:2xZ), regenerated 06:30Z today and still present ⇒ repeatable.
- **Mechanism, first-hand** (090 substituted per the 2026-07-31 ruling — the deciding evidence is the executed prompt read from `llm_call_log`, not an inference; stated here as required):
  - 557's writer_block QUOTES the exemplar 'Phrase it as "NNN+ AI agents"' and its own verify guard REQUIRES that literal (`557…sql:227`).
  - The writer template injects ONLY `evidence_base.writer_block` on the unscoped path. Prompt `9ba94176`: handwritten block t, composed block f, writer_line f, occurrences of `200` (the fact's value) = **0**. The writer was told to read a value it was never shown.
  - Copy rate since 08-22: **137 instructed / 14 copied NNN / 0 stated the agents value** (1 stated the types value). Zero `\mNNN\M` in writer responses before 08-22.
  - Detector blindness: `placeholderPatterns` = substrings, no numeric shape; `templateVarRegex` wants `{{`; `editorialPageTypes` excludes model-directory from `ScanUnregisteredNumbers` (and NNN has no digit). *(364 session, ~11:00Z: tracker page types leaving `editorialPageTypes`, uncommitted — third reason lapses, first two stand.)*
  - The preventing machinery EXISTS: `composeWriterBlock` `{value}` substitution (managed, 6 sites) and `composeScopedWriterBlock` (13/891 sections, 3 sites). aiao unmanaged; its NOTES name the blocker (managed mode drops the NEVER-STATE list). 13 unmanaged sites share the exposure.
- **Demand control for a reachability check: 0 of 709** deployed pages 404 at `pages.url` (sweep ~10:40Z; 698×200, 7×302 webdesign.uk by design, 1×301, 3 transient TLS 000 that 200 on re-probe). Linked case covered by `dead_internal_link_live` (16 items fleet-wide). Seam recorded (DGH-015 line 95). ⇒ D2: no new check.
- **MISSTEP (mine), caught by the PEER:** I offered 364 the clean 06:26Z model-directory build as confirmation of its fix. Its roll landed **09:27:24Z** — the build predates the binary by 3h; it ran on old code. Logged in WRONG_CALLS under this session. Check: date `execution_started_at` against the service's build-provenance stamp before calling any build evidence of any fix.
- Chassis stamp moved again intra-day (`635f2d32` → `4c996e1b5` by 10:44Z, per 364) — re-read the stamp at use time, never pin it in a doc.

## 2026-08-25 (continued) — execution

- Council submission fired: **corr `6cfaa8f0-ce1b-43de-a995-c1536b425d02`** (both halves: migration 611 + the detector). Trailer `Council-Submitted:` on commits `e1f84bc15` (611) and `29ca1b35e` (detector). Budget ~30 min dispatch latency; find the run by payload, not the printed id.
- **Migration 611 APPLIED and LIVE** (~11:20Z): current `evidence_base` row `created_by='611_migration'`, `writer_block ~ 'NNN'` = false, floors present, facts byte-identical (guards passed; probe ran the whole file in a doomed transaction first). ⚠ Prior rows are `created_by='evidence-refresher'` — the refresher SUPERSEDES this row on each fact refresh, carrying writer_block forward when unmanaged. Verify after the next refresh that the 611 block survived it.
- **MISSTEP (mine), caught immediately:** after the mutation test I "restored" with `git restore` — which reverts to HEAD and WIPED the entire uncommitted patch, not just the mutation. Re-applied from the scripted patch (anchored asserts made that safe). The check: to undo a scripted mutation on an UNCOMMITTED file, invert the mutation (re-run the patch script); `git restore` is only safe when the base you want is actually HEAD.
- Detector: tests green (both new suites), mutation-verified (emptied regex list fails the positive suite), `verify-head-builds.sh --with … --test` OK against HEAD `30d223291`.
- `probe-page-url.sh` (OPP-010): self-test green; live: 3/3 tracker pages SERVING with both controls held (exit 0); unknown-name refusal proven (exit 2 + near-match hint).
- **⚠ Verification protocol for the rebuilds, from the 364 lane (~11:45Z), verified relevant:** `aao-orchestrations` is a ROLLING-WINDOW gte fact (35 snapshots, 1,494–7,281, fallen 17/34 transitions, below 1,600 on 3). The site publishes "over 1,600 orchestrations a day"; at the fact's historic low, claimscan at HEAD produces 4 ERROR findings on HONEST copy (3 on trackers — silenced on today's binary; 1 on enterprise-reference-deployment — live now). So: **read the fact's value in the same breath as any rebuild verdict and record it with the run** — a refusal with the counter dipped is the counter, not the fix (mine or theirs). Today: 7,281 → clear. Mechanism owned by `bugs_open/386`.
- **Closure condition added (raised by a second `bugs_open/387` session, 13:5xZ, which correctly stood down):** the evidence-refresher supersedes the `evidence_base` row DAILY at ~09:05Z (6/6 consecutive days measured). By code (`refresh_evidence_base_action.go:36`) an unmanaged site's writer_block is "left exactly as it is" — but that is a claim about behaviour, so: **on or after 2026-08-26 ~09:06Z, re-run** `SELECT created_by, (data->>'writer_block' ~ 'NNN') FROM site_specs WHERE site_id='2a8ebf9c-20a2-4c39-b191-840b012371da' AND aspect='evidence_base' AND is_current;` — expect a fresh `evidence-refresher` row with `has_nnn = f`. If NNN returns, the refresher rebuilds from somewhere other than the current row and 611 has an expiry nobody is watching. Layered backstop either way: once the detector rolls, a resurrected stand-in is REFUSED at validate_content rather than published.

## 2026-08-25 afternoon — source fix PROVEN, WP5 built, council rounds

- **WP2 PROVEN END-TO-END (12:41Z):** first build through the 611 block — hero now "…more than 150 distinct agent types spread across the platform's 8 departments…", `content_data ~ NNN` = f, page re-stamped deployed 12:41:25Z, live page serves it (0 `\bNNN\b`, both floors present, cache-busted). Build window error rows: **0** `unregistered_number` (counter read in the same breath: 7,281). The one FAILED orchestration in the window is tool-automation-savings-estimator's pre-existing floor refusal — unrelated. 364 messaged with the three-line datum; **their re-measurement: the model-directory pass is weak by MEASUREMENT (0 findings gated AND ungated — the writer happened to produce clean copy), and adoption-tracker carries 17 ungated findings today ⇒ the ~18:24Z tracker rebuilds are the discriminating test for their fix.**
- **Detector council trail (corr 6cfaa8f0):** r1 REVISE → X-family cut + asymmetry argued in the comment (commit 805481518); r2 REVISE → conceded the provenance-recipe error (agent-chassis's startup line scrolls; replaced with the binary probe, negative control run TODAY: new label 0/rc1, existing label 2/rc0 on pod 67fd9c76f5-2g8kw), census re-run at the WIDEST population (no page filter, content_data included, site_components): still exactly 1 hit; migration_backups shape confirmed (old_value jsonb, 611 row object); fleet-wide framing stated in the comment (commit 5cc11f03b). r3 submitted 12:5xZ.
- **WP5 BUILT as agreed with 288 (their message: option b, window open):** `writer_block_guidance` — ONE composer serves both paths, so the carry seats once in composeWriterBlock + a one-line key copy in composeScopedWriterBlock (without which a fact-scoped section's prompt LOSES the guidance — pinned by its own test). 4 tests incl. byte-identity for the absent key; both carry sites mutation-verified (each deletion fails its test). Register **CLM-029** same commit. Council corr **0de22385-729c-493c-8422-8bda9bc056f9**. Zero consumers at ship (enumerated). Adoption is each site's call; aiao retires 611's interim reword when it opts in.
- Same-file passengers today (recorded, per the honest-account convention): my LANDMINES addendum and the OPP-010 index row were committed by OTHER sessions' commits (4d8711397/099bd5fd6 era); both verified present at HEAD. And my own commit b8e05d471 took no passengers.
- **288 lane cross-verified the carry (their message, ~13:4xZ):** one additive hunk, disjoint from their paths, no passenger either direction; HEAD builds (their own verify-head-builds run); c17a18620 an ancestor of HEAD (e933d98f5 at their read). ⚠ **The pending release is SHARED: v1.0.1338 is pinned in the makefile (6902665ff, owner has not run `make release` yet) and will carry their two fixes + this lane's detector and carry under ONE tag** — verify per SERVICE by binary probe at roll time (their three discriminator literals + my "numeric stand-in placeholder" discriminate independently on the same roll; all grep 0 pre-roll with positive controls). bugs_open/249's straddling-tag case is exactly this shape.
- **Council, later rounds (both trails):** detector r3 REVISE (bug_historian HIGH: one guarded call site — section-editor/chrome render without validate_content, per LANDMINES:13341) → r4: scope claim corrected to the true page-BUILD path, KNOWN GAP pinned in the bug file, regex list collapsed to ONE alternation (the pre-r4 list double-counted "NNN+" — a blocker count is read as a measurement), count test + mutation 2/2, single caller grep-verified (ec7cc0907). Carry r1 REVISE (reuse_agent HIGH: would the typed EvidenceBase struct's write-back strip the unlisted key?) → answered at the code, demonstrate branch: 9 ParseEvidenceBase callers enumerated (readers + a guard that stores client bytes); both REAL write paths (refresher raw-map marshal; siteSpecDeepMerge) pinned by new round-trip tests; struct deliberately NOT extended (writer_block itself is unlisted and never struct-stripped across 19 sites — the struct is the scanner's read model); field contract restricted to NEGATIVE/PROHIBITIVE guidance only (comment + register ⚠ CONTRACT bullet) per the compliance seat (14ec48b89). r4 and carry-r2 in flight. ⚠ Transients seen twice in the shared package this afternoon: another session's mid-edit test file broke package compile for minutes (`audit_tone_pending_proposal_bound_test.go`, missing import, actively being edited) — a FAIL from `go test` on this tree must be attributed before it is believed.
- **Detector APPROVED, round 4, 13:06:45Z** — 12 approve / 1 advisory objection (none high) / 4 abstain. The advisory (a `break` across the block loop hid a second-block stand-in until the next build cycle — never shipped, page refused either way, but one extra human iteration) was real and is FIXED in the follow-up commit carrying the `Council-Reviewed:` trailer (verdict read first): one issue per matching block, two-block case test-pinned. Detector remains INERT until the shared v1.0.1338 roll; bug stays OPEN until then (fixed-AND-live bar). Post-roll verification: the binary probe pair (new label present / controls) per service.
- **Carry APPROVED, round 2, 13:13:02Z** — 8 approve / 3 advisory / 6 abstain. Advisories: (1) the marshal-cycle test didn't call the PRODUCTION function — acted on: sqlmock pin of `writeRefreshedEvidenceBase` (the inserted JSON must carry the key), 9 guidance tests total now; (2) `banned_claims` overlap — acted on in the register: DETECTIVE vs PREVENTIVE stages of one purpose, a scannable prohibition belongs in banned_claims regardless, sites hold both; (3) two seats misread the register edit as creating a new claims-verification.md and omitting the index row — both wrong (the file predates this lane, the entry was appended, the index row shipped in the r1 commit): recorded here, not acted on; (4) a doc_notes travelling-docs row for writer_block composition — accepted-not-acted: the design lives in CLM-029 + this lane's docs + the 288 lane's own trail, and hand-writing doc_notes outside a sanctioned writer is exactly what the landmines rules warn about for their corpus; if a sanctioned design-note channel exists the 288 lane (file owner) is the right author. Both trailers now `Council-Reviewed:` on read verdicts (5aed9ca74 detector, cbadcba71 carry).
