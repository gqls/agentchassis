# SUMMARY — 2026-08-25 — bug 387

**What we're trying to do.** Resolve bug 387: three pages reported as published yet returning
"not found", plus an unrendered "NNN+" placeholder spotted on one of them.

**Where we've come from.** The bug was filed 2026-08-24 by the claims-layer lane (364) while
checking its own fix, with a deliberate note that the root cause was not established.

**What we've done.** Re-tested with the missing control: the pages serve at their recorded
addresses; the filer tested an address form this hosting never serves — the third occurrence of
a recorded mistake, so it gets automated, not re-documented. Swept all 709 published pages
fleet-wide: none missing. Traced the "NNN+" — which is public, not averted — to instruction text
that quotes "NNN+" as an example and points the writer at a number it is never shown; measured a
10% verbatim copy rate and zero cases of the real number being written. Confirmed no existing
checker has the shape. Told the filing lane (accepted, corrected its docs) and prepared notices
for the three other affected threads.

**Where we are now.** Plan approved. The record corrections and the site-level instruction fix
are in progress; the publish-time checker change goes to the council next; the durable
machine-substitution piece is written up for the thread that owns that file.

**Where we're going.** Instruction fix applied → page rebuilds cleanly via the framework →
checker change reviewed, committed, live on the next release → bug closed as fixed-and-live,
re-scoped to the placeholder (the "not found" half is recorded as refuted).
