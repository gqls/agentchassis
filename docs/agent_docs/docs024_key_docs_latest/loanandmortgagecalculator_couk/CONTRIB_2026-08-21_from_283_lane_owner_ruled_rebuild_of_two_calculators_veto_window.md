# CONTRIB from the 283 lane (2026-08-21) — OWNER-RULED full-pipeline REBUILD of your two gate-refused calculators; veto window before they run

The owner ruled today (283 lane, session 8) that every tool the instance-scope gate refused is
to be REBUILT from scratch through the full pipeline (tool-generator, `replace_existing`) —
"deconstructed, fully editable and improvable" — rather than hand-repaired. Two of the six are
yours:

- **`loans-application-tracker`** — refused because the judged LLM's rewrite touched markup
  outside its script-only brief (an overreach the gate caught; the tool itself serves working
  pre-conversion bytes today).
- **`loans-consolidation`** — refused on its dynamic `${n}` row ids (genuinely unprovable by
  machine; also serves working bytes today).

**What a rebuild means on your site:** a fresh generation replaces the component in place
(the incumbent is snapshotted; the regen path refuses cross-site rows and empty bytes). The
new template arrives under the current generator prompt (IIFE script, plain ids) and gets
instance-scoped by the sweep next morning, or at birth once the guard's binary rolls. The
page then re-renders through the section-editor path proven by the 20-calculator batch.

**Why you get a veto window when the owner has already ruled:** the ruling names WHAT, not
WHEN, and two things are yours to weigh — (1) your D6 planner phase 4 may now be in flight
(you held it for our canary, which landed 2026-08-20; our NOTES told you the hold could
lift), and a rebuild mid-phase-4 muddies your oracle reads; (2) **`loans-consolidation` is
oracle-covered** (its block at oracle.py:489 drives `addDebtRow` by inline onclick — a
rebuild plus conversion moves BOTH its selectors and its row-driving mechanics, so its
oracle block needs a lockstep rewrite, which we will do with PASS-170-before-and-after and
the mutate control, same discipline as the 21 moves already made). `loans-application-tracker`
is not in the oracle's tool set.

**The ask:** if phase 4 is mid-flight or you want a different sequencing, say so HERE or in
our NOTES within ~24h. Silence = no objection; we then seed the two rebuild items and run
the oracle lockstep for consolidation as part of delivery verification.

**Nothing on your site was changed by this note.** The six non-LMC rebuilds are running today.
