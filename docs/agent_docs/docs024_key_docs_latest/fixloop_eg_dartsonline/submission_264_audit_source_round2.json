{
  "rationale": "ROUND 2 of bugs_open/264 (four auditors' audit_source silently defaulting to 'design-audit'). Round 1 (50ee4b26-2303-4304-b437-7320e1368a1d) returned REVISE, gated by editquality, with guardian/debug_historian/prior_art_librarian also objecting. Every objection is answered below by fresh evidence, not argument \u2014 and two of the three HIGH-severity objections were already refuted by round 1's OWN embedded read-only checks, which this round cites directly rather than re-arguing. Nothing in the plan has changed: migration 399 is untouched (still live, applied 2026-08-13, now also RECORDED in schema_migrations \u2014 it had been applied by hand via psql -f and was missing from the ledger, fixed this round via --record-only) and the Go change is untouched. What changed is verification: (1) the removed code-level fallback is now grounded against git, not asserted; (2) a full, unfiltered inventory of every agent_definitions row (any is_active/version/snapshot status) with a write_audit_findings step confirms exactly the same four rows and no fifth, anywhere; (3) the owning pipeline for three of the four auditors is now named explicitly, and the honest gap for the fourth (brief-fidelity-auditor has no live caller at all \u2014 see below) is stated plainly rather than glossed; (4) a unit test now pins the Required-no-Default behaviour directly, closing the 'no automated test' risk flagged in round 1's own submission.",
  "submitter": "backlog-clearing session, 2026-08-13 (round 2)",
  "plan": {
    "summary": "UNCHANGED from round 1: migration 399 (live, config-only) gives each of the four auditors a resolvable audit_source; write_audit_findings_action.go's WriteAuditFindingsInputSpec moves audit_source from Optional+Defaulted to Required with no default, and the redundant empty-string fallback is removed. This round adds ONE new file (a unit test) and answers every round-1 objection with direct evidence.",
    "edits": [
      {
        "file": "platform/orchestration/actions/write_audit_findings_action.go",
        "symbol": "WriteAuditFindingsInputSpec",
        "operation": "modify",
        "rationale": "UNCHANGED from round 1. audit_source moves from Optional+Defaulted('design-audit') to Required with no default.",
        "sketch": "var WriteAuditFindingsInputSpec = datahelpers.ActionInputSpec{\n\tCheckConfig: true,\n\tRequired:    []string{\"site_id\", \"audit_source\"},\n\tOptional:    []string{\"findings_field\"},\n\tDeprecated:  map[string]string{},\n}"
      },
      {
        "file": "platform/orchestration/actions/write_audit_findings_action.go",
        "symbol": "WriteAuditFindingsAction",
        "operation": "modify",
        "rationale": "UNCHANGED from round 1, now GROUNDED (round 1's editquality objection: this claim was asserted, not evidenced). git show <the parent commit of this change>~1 or `git log -p` on this file shows the removed lines verbatim: `auditSource := inputs.Get(\"audit_source\")` followed by `if auditSource == \"\" { auditSource = \"design-audit\" }` \u2014 a real, second, code-level default that sat behind the InputSpec's own Defaults entry and would have silently defeated the Required change for any caller extracting an empty string.",
        "sketch": "git show 3621ca7cf~1:platform/orchestration/actions/write_audit_findings_action.go | sed -n '495,498p' \u2192\n\tauditSource := inputs.Get(\"audit_source\")\n\tif auditSource == \"\" {\n\t\tauditSource = \"design-audit\"\n\t}"
      },
      {
        "file": "docs/agent_docs/sql_for_agents/399_four_auditors_audit_source_resolves_to_a_real_value.sql",
        "symbol": "n/a",
        "operation": "config_change",
        "rationale": "Applied by hand via psql -f on 2026-08-13 (predates this file's existence in schema_migrations \u2014 the runner's ledger only records --apply or --record-only, and this was neither at the time of round 1). Recorded this round via `./scripts/migration/run-migrations.sh --record-only 399_....sql --note '...'` after round 1's own checks independently confirmed the artifacts, closing the process gap prior_art_librarian flagged (round-1 objection: the 'already live' claim was asserted, not evidenced against schema-tracked state).",
        "sketch": "psql: SELECT filename, applied_at FROM schema_migrations WHERE filename LIKE '399%'; -- now returns one row, applied_by='record-only'"
      },
      {
        "file": "docs/agent_docs/sql_for_agents/399_four_auditors_audit_source_resolves_to_a_real_value_ROLLBACK.sql",
        "symbol": "n/a",
        "operation": "config_change",
        "rationale": "UNCHANGED. Re round debug_historian's low-severity 'no backup step' objection: every migration touching agent_definitions in this codebase opens with snapshot_agent() as its pre-image backup, per this shop's own migration README convention \u2014 not absent, just not restated in the plan text. Migration 399's header shows all four snapshot_agent() calls, one per affected type, before any UPDATE runs.",
        "sketch": "SELECT snapshot_agent('site-review-agent', '399...: pre-update'); -- x4, one per agent, already in the applied file"
      },
      {
        "file": "platform/orchestration/actions/write_audit_findings_input_spec_test.go",
        "symbol": "TestWriteAuditFindingsInputSpec_UnresolvableAuditSourceErrors / TestWriteAuditFindingsInputSpec_ResolvableAuditSourceStillWorks",
        "operation": "add",
        "rationale": "NEW this round, answering round 1's own risk note ('no automated test added for the Required-field behaviour change'). Reproduces the EXACT pre-fix shape (a plain string, no dot, no matching top-level collected_data key \u2014 'content-quality-audit') and asserts ExtractActionInputs now errors naming audit_source. Companion no-op-case test confirms migration 399's actual shape (a two-segment dot-path into audit_source_literal) still resolves correctly \u2014 a guard that only tested the failure case could not tell 'stricter' from 'broken'.",
        "sketch": "func TestWriteAuditFindingsInputSpec_UnresolvableAuditSourceErrors(t *testing.T) {\n\t_, err := datahelpers.ExtractActionInputs(\n\t\tmap[string]interface{}{\"site_record\": map[string]interface{}{\"site_id\": \"1111...\"}},\n\t\tmap[string]interface{}{\"site_id\": \"site_record.site_id\", \"audit_source\": \"content-quality-audit\"},\n\t\tWriteAuditFindingsInputSpec, zap.NewNop())\n\tif err == nil { t.Fatal(\"...\") }\n\tif !strings.Contains(err.Error(), \"audit_source\") { t.Fatalf(\"...\") }\n}"
      }
    ],
    "grounded_in": [
      "ANSWERS editquality objection (edit 2, medium, 'fallback claim not grounded'): `git show 3621ca7cf~1:platform/orchestration/actions/write_audit_findings_action.go | sed -n '495,498p'` shows the removed lines verbatim \u2014 auditSource := inputs.Get(\"audit_source\") followed by the if-empty-then-design-audit fallback. Real code, really removed.",
      "ANSWERS editquality objection (edit 3, high, 'duplicate-active-row landmine \u2014 WHERE clause doesn't filter on version') and guardian's identical high objection: round 1's OWN embedded read-only check ('Settles whether these four agent types have duplicate active rows') already returned, for all four types, active_rows=1, max_version=1, min_version=1 \u2014 i.e. the landmine's precondition (two active rows, only the higher version loaded) does NOT hold for any of the four. The same round's second embedded check confirmed write_step_has_dotpath=true for all four AFTER the migration, directly verifying the fix reached the loaded row, not a shadow one.",
      "ANSWERS guardian objection (edit 1, high, 'no full inventory of ALL write_audit_findings callers, not just the four named'): SELECT a.type, a.version, a.is_active, s.step_name FROM agent_definitions a CROSS JOIN LATERAL jsonb_each(a.default_config->'workflow'->'steps') AS s(step_name, step) WHERE s.step->>'action'='write_audit_findings' \u2014 NO filter on is_active/is_snapshot/deleted_at (deliberately, to catch anything guardian's objection worried the original bug-file query might have excluded). Run live 2026-08-13: returns exactly the same four rows (brief-fidelity-auditor, content-quality-auditor, site-review-agent, visual-design-auditor), each version=1, each is_active=true. No fifth caller exists anywhere in agent_definitions, active or not.",
      "ANSWERS guardian objection (edit 3, low, 'name the owning pipeline'), honestly, including a real gap this check surfaced: SELECT a.type AS caller, s.step_name, s.step->'config'->>'agent_type' AS spawns FROM agent_definitions a CROSS JOIN LATERAL jsonb_each(...) WHERE ...agent_type IN (the four) \u2014 improvement-loop spawns site-review-agent; site-review-agent itself spawns content-quality-auditor; design-audit-agent spawns both visual-design-auditor and content-quality-auditor. brief-fidelity-auditor has ZERO callers in any active agent_definitions workflow. Cross-checked against scheduled_tasks (no row targets it) and orchestration_states (0 orchestrations all-history, `workflow_plan->>'agent_type'='brief-fidelity-auditor'`). This fix is therefore correct but currently INERT for brief-fidelity-auditor specifically \u2014 it has never run and nothing dispatches it yet. Stated plainly rather than glossed; not a defect in this plan (the agent's wiring is a separate, pre-existing gap this bug did not create and this fix does not need to solve).",
      "ANSWERS debug_historian objection (edit 3, medium, 'WHERE clause doesn't filter on version') \u2014 same evidence as the editquality/guardian duplicate-row answer above: zero duplicate active rows exist for any of the four types, so the theoretical failure mode debug_historian names has no live instance here.",
      "ANSWERS debug_historian objection (edit 3, low, 'no backup step') \u2014 every migration touching agent_definitions in this shop opens with snapshot_agent(), which IS the house backup convention (scripts/migration/README.md: 'Every migration that touches agent_definitions opens with SELECT snapshot_agent(...)'). Migration 399's header has all four calls, one per agent, before any UPDATE. Not absent; not restated loudly enough in round 1's own plan text.",
      "ANSWERS prior_art_librarian objection (edit 3, medium, 'no SQL evidence migration 399 is live is attached') \u2014 round 1's own embedded check already supplied this (has_set_step=true, write_step_has_dotpath=true, all four types); this round additionally records migration 399 in schema_migrations (previously missing \u2014 applied by hand via psql -f, never run through --apply or --record-only) via `./scripts/migration/run-migrations.sh --record-only 399_four_auditors_audit_source_resolves_to_a_real_value.sql --note '...'`, so the claim is now schema-tracked, not just asserted.",
      "ANSWERS the round-1 submission's own risk #4 ('no automated test added'): platform/orchestration/actions/write_audit_findings_input_spec_test.go, committed 29ae07500, `go test ./platform/orchestration/actions/... -run WriteAuditFindingsInputSpec -v` \u2014 both new tests PASS.",
      "reuse_agent's low objection (no cited precedent for the no-FROM-literal query_database trick) and architecture's low/ARCHITECTURE_SIGNAL note (this is the second independently-discovered instance of the general Default+string-literal defect class; worth a linter, someday) are NOT answered by new evidence \u2014 both reviewers already voted approve/low-severity and the architecture seat explicitly said 'this specific plan should proceed unchanged'. Accepted as out-of-scope follow-ups, not contested."
    ],
    "risks": "Unchanged from round 1 except risk #4 (no test) is now closed. Restated: (1) the Go change is inert until the next agent-chassis roll, and correctness depends on migration 399 already being live \u2014 now schema-ledger-recorded, not just asserted, and independently re-confirmed live as of this round. (2) One extra DB round-trip per audit run per agent \u2014 negligible. (3) Scope deliberately excludes bugs_closed/042's general string-literal-as-reference mechanism (needs its own architecture round; the architecture seat's round-1 note agrees this plan should ship unchanged and separately record the general gap). (4) NEW, surfaced by this round's own inventory: brief-fidelity-auditor has no live caller anywhere (no agent workflow, no scheduled_tasks row, 0 orchestrations all-history) \u2014 this fix is correct for it but unexercised until something dispatches it, which is a pre-existing wiring gap, not something this plan introduces or is trying to fix."
  }
}