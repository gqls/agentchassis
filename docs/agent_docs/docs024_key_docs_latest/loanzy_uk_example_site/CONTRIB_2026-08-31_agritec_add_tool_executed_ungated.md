# CONTRIB (from the agritec_uk lane, 2026-08-31): a fourth worked case for §6's growth-refusal decision — and the first where the growth EXECUTED

For `OWNER_REVIEW_2026-08-31_farmerinsurance_first_review_and_routing.md` §6 (seven unresearched
`add_tool` rows; per-site growth refusal, RFC_056 FOLLOW-UP #1). Your three worked cases show the
mechanical seat *requesting* ungated growth. agritec.uk shows what happens when the request RUNS —
and it is a controlled comparison, because the same site carries both routes side by side.

**What happened (all first-hand, measured 2026-08-31 at the served artefacts + live DB):**

- 2026-08-26 00:24 the discovery visit filed `evaluate_tools`; by 15:25 six `add_tool` rows had
  completed (5 `_novel` + 1 library); by 17:32 on 08-27 **five new calculators + five companion
  guides were LIVE** on a domain whose standing owner rule (2026-08-21) is *"no unsourced figure
  is published anywhere — not in copy, not as a tool default, not in a spec."*
- **The register never moved: 104 facts before, 104 after.** The path consulted no evidence,
  registered none, and installed no `artifact_check` fences or PLAN declarations.
- Measured at the served pages: **0 external source links across all five tools** (the
  evidence-route SFI stacker on the same site carries its gov.uk anchor + capture date).
  Specifics: `blue-carbon-estimator` encodes unsourced empirical constants in its JS (per-species
  dry-matter % 10–15, carbon % 25–30, sink/burial scenario fractions 10/50/90) on the site's most
  claims-sensitive subject; `bsf-waste-converter` is well-shaped (rates are user inputs) but its
  help copy asserts uncited trial ranges ("Typical trials report 40 to 60%"). The claims scanner
  cannot see inside JavaScript (our ledger's standing note, from the `bugs_open/288`
  collaboration), so nothing downstream catches the encoded ones.

**Why this strengthens FOLLOW-UP #1 specifically:** the same framework, on the same site, in the
same week, produced a fenced, cited, reconciled tool when driven through the evidence-first route
(24/24 fact fence, gov.uk citation, Tier-4 acceptance) — and five unfenced, citation-free tools
when driven through `evaluate_tools → add_tool`. The gap is the ROUTE, not the site config, which
is exactly the shape a per-site (or per-route) refusal/gate decides.

**What the agritec lane is doing regardless of the ruling** (site-level repair, our own scope):
evidence runs to source the encoded figures (blue carbon first), then citation-bearing copy and
fences per the SFI pattern. So the worked case will also show the *cost* of ungated growth: each
tool's figures have to be re-sourced after the fact.

Contact: session "agritek", lane `docs024_key_docs_latest/agritec_uk/`
(COLD-START = `agritec_uk/HANDOFF_2026-08-30_continue_here.md`; the 08-31 NOTES tail carries the
measurements quoted here).
