# PLAN — Imagery: best-in-class sites (imagery programme, stage 3)

**What this project is about (read this first).** This repo (agentchassis) is an
autonomous agent platform that plans, builds, and operates a fleet of content
websites — tools, guides, games, news feeds, and articles. Sites are planned by
LLM agents (`build-site-planner`), content and imagery are generated by worker
agents through a Kafka-orchestrated work-item pipeline, and finished assets are
committed to each site's git repo for static deployment. An imagery pipeline
already exists and is verified end-to-end: the planner emits a structured
imagery plan (`site_plan_imagery`, kinds `logo|hero|illustration|icon|infographic`),
discovery checks turn unfulfilled plan rows into work items, `image-build-handler`
routes generation to one of two providers (Stability SDXL for photographic work,
Google "Banana" `gemini-3-pro-image-preview` for flat icons/illustrations), and
`asset-deployer` commits optimised images to the site's git repo, after which the
page re-renders. **robot-hands.com** (an industrial-gripper site, currently being
rebuilt fresh with a news section) is the testbed. This document plans the next
stage: raising the sites' visual quality to *best in class* — data-accurate
infographics, brand-consistent graphic language, content-linked card imagery,
illustrated bullets and lists, copyright-safe product sketches, news-item
imagery, and image performance budgets — with an audit loop that enforces the
standard rather than hoping for it.

Companion documents:
- `RUNNING_NOTES_imagery_best_in_class.md` — turn-by-turn discussion log, updated every session.
- `RUNBOOK_imagery_best_in_class.md` — the tasks the human operator performs (credentials, backups, approvals, verification).

Naming note: earlier imagery work used Phase 0–6 and 2A–2H. To avoid collision,
this plan's phases are numbered **I0–I8**.

---

## 1. The brief (as given, then expanded)

The user's brief (2026-07-08): make the sites *look and feel* best in class.
Infographics illustrating major news points, latest data, and historical trends —
especially for issues current in users' minds. Graphic artefacts that help
readability and promote the domain brand (professional / usable / friendly, per
site). One consistent good logo throughout the life of the site. Images above
linking/descriptive cards that reflect the content, and similar themed images on
the content itself. Interesting design treatment of bullets and lists (relevant
graphics as bullet points). Sketch illustrations of products where copyright is
a risk — altered viewpoint, product shown in a relevant setting; pencil sketch,
watercolour, or CAD-like diagram depending on how technical the product is. News
feed and individual news items illustrated so the reader gets background at a
glance (e.g. a price graph above an article about a sudden price change).
Appropriately sized images (sprite sheets for bullets). robot-hands.com is the
working domain while building. Few limits — this is exploratory.

Expanded into concrete capability goals:

| # | Goal | What "done" looks like |
|---|------|------------------------|
| G1 | **Brand kit & logo permanence** | Each site has one approved logo used everywhere (header, favicon, OG cards) for its whole life. A machine-readable per-site imagery style guide (palette, medium, mood, reference images) that every generation honours. |
| G2 | **Data-accurate infographics** | Charts of *real* data (latest figures, historical trends) rendered by code — never by a diffusion model — with an LLM editorial layer for titles, callouts, and annotations. Used on tools, guides, and news. |
| G3 | **Content-linked card imagery** | Every linking card (blog index, news feed, tool directory, related-content blocks) carries an image that reflects the content behind it, and the content page carries the same or a sibling image — visually one family. |
| G4 | **Graphic design artefacts** | Bullets/list markers as relevant themed glyphs (sprite sheets), section dividers, borders, background motifs — readability-first, brand-consistent, cheap to serve. |
| G5 | **Copyright-safe product sketches** | Stylised product illustrations (pencil sketch / watercolour / CAD-like per product type), viewpoint deliberately altered from source photos, product placed in a relevant setting. Clearly derivative works, never facsimiles. |
| G6 | **News imagery** | Every news item gets an at-a-glance image chosen by kind: a data chart when the story is data-driven (price moves, trends), an illustration/photo otherwise. Feed and article views share the treatment. |
| G7 | **Performance budgets** | Per-kind byte and dimension budgets, modern formats (WebP), responsive sizes, lazy loading, sprites for repeated small art. Enforced at deploy time and policed by a discovery check. |
| G8 | **Accessibility & sharing surface** | Alt text on every meaningful image; OG/social card images; favicon derived from the logo. Part of what "professional" means. |
| G9 | **Quality loop** | A vision-capable imagery auditor that checks delivered images against the brand style guide and the prompt intent, with bounded fix attempts and respect for locked/approved assets. |

---

## 2. Where we are — current state (verified from docs and code as of 2026-07-08)

**Shipped and verified in production:**
- `site_plan_imagery` table — scope `site|page|section`, `chk_kind` constraint currently `logo|hero|illustration|icon|infographic`, `style_hints`/`constraints` JSONB, locking columns. Mirrored in Go as `validImageryKinds` (`write_site_plan_action.go`). **New kinds = migration + Go change together.**
- Planner prompt emits the structured `imagery` block (sql_for_agents/053 patches): one-entry-one-image decomposition rule, `style_hints.aspect_ratio`, icon style wording, max_tokens 8000.
- Loop closure: `emit_imagery_items` → `image-build-handler` → `store_asset` → `asset-deployer` (git commit) → `flag_page_image_rebuild` (page re-resolves through `plan_sections`). Policed by discovery checks: `unfulfilled_imagery_plan`, `unfulfilled_image_prompt`, `placeholder_image_in_use`, `image_url_404`, `undeployed_assets`.
- Provider abstraction: `dynamic_adapter.go` routes by kind — icon → Banana, everything else → Stability SDXL. Banana confirmed good for illustrations, heroes, decorative infographics. The provider `Request` already carries `ReferenceImageURIs` (Banana can take reference images — key lever for brand consistency).
- Icon lessons locked in: SDXL can't do flat icons; true transparency is impossible (models paint checkerboards) → flat-grey chip + CSS; `directionAppliesToKind` keeps photographic direction off icons/logos.
- Lucide webfont icons are the *correct* path for UI chrome (features grids use `<i data-lucide=…>`). `lucide_icons.go` validator written, **not yet wired** into the content-generation step.
- `ImagePurposes` sizing map (hero 1600×900 jpg, logo 400×400 png, icon 240×240) + `OptimizeImageForWeb` on deploy.

**Planned but not built (design work already done — reuse it):**
- **Sprite sheets** — `CONTEXT_PACK_imagery_sprite_sheet.md`: decisions locked (one sheet per site, N×M grid at 768², CSS `background-position` web sprite, Banana model, bullets via `::before`). Phase 0 (schema confirm + geometry lock) not run.
- **Product illustrations** — `old/illustration/PLAN_product_illustration.md`: five pieces (discovery check `needs_product_illustration`, handler agent, `link_asset_to_product` action, renderer preference for `custom_image_id`, stylisation-as-constraint prompt template).
- **Data graphs** — `old/FUTURE_data_graph_pipeline.md`: three stages (fetch real series → code-render chart (go-echarts leading candidate) → LLM annotation layer). Hard constraint documented: diffusion models cannot plot real data.

**Known open items carried forward (status verified 2026-07-08):**
- ~~SECURITY: API-key scrub/rotate~~ — **done**; keys rotated (user confirmed 2026-07-08).
- Logo-in-header resolution gap — **still open**; header is a site component rendered by `render_site_components`, untouched by the page-level `plan_sections` fix; `site_assets.logo` resolution in the header path is an I0 item.
- Lucide validator — **still unwired** (verified: no callers outside `lucide_icons.go`); wire into the rebuild's content-generation step.
- robot-hands.com rebuild — **not done** (verified: 30 pages, 18/87 components with NULL content, adoption residue intact). Decision 2026-07-08: rebuild from scratch with news scope, as the closing step of Phase I0. Full latitude on this site.
- Stability provider timeout (60s vs old 120s), provider circuit breaker not threaded, asset→rerender coupling for site-level components — still open, non-blocking.
- Ground-truth notes: live `chk_kind` matches docs; planner prompt carries the Imagery Block + decomposition rule; planner max_tokens has since been bumped to 16000; `Pipeline: "build"` fix is in the code.

---

## 3. The key structural expansion: two lanes of imagery

Everything built so far is **plan-driven**: `build-site-planner` decides at plan
time what images the site needs, and the loop fulfils that fixed list. Most of
what this brief asks for is **content-driven**: a card image belongs to an
article; a price chart belongs to a news item; a product sketch belongs to an
`affiliate_products` row. These arrive continuously *after* the plan is fixed,
at content-creation time, and their imagery must reflect the specific content.

Proposal:

- **Lane A — planned imagery** (exists): `site_plan_imagery` rows → work items →
  assets. Extended with new kinds (`sprite_sheet`, decorative artefacts) and
  brand-guide-aware prompts. The planner keeps requesting site/page/section
  imagery.
- **Lane B — content imagery** (new): imagery attached to content entities
  (articles / news items / products), requested at content-creation time or by
  discovery checks that sweep content tables for image-less items. The pattern
  already exists in miniature: `affiliate_products.custom_image_id` FK → `assets`.
  Generalise it: content entities get an image reference (or an
  `entity_type + entity_id` column pair on `assets`), a per-entity work item
  type, and a handler that composes the prompt *from the content itself* plus
  the site's brand guide.

Cards then follow for free: a card is a *view* of a content entity, so the card
image is the entity's image (or a purpose-built `card` crop of it) — which is
exactly the "image above the card reflects the content, and the content page
carries the same theme" requirement.

---

## 4. Design decisions (D1–D8 confirmed with user 2026-07-08 — see running notes Turn 2)

| # | Decision | Rationale |
|---|----------|-----------|
| D1 | **Data graphics are code-rendered, never diffusion.** Charts come from real series + a charting library (go-echarts first candidate), LLM only does editorial annotation. | Diffusion models fabricate data; documented hard constraint. Best-in-class means *accurate*. |
| D2 | **Two lanes** (plan-driven + content-driven imagery) per §3, sharing the same generation/deploy/audit machinery downstream of the work item. | Reuse before recreate; only the *requesting* side is new. |
| D3 | **New kinds land as one migration + one Go change per batch**, `text` + CHECK (not enums), per repo convention. First batch: `sprite_sheet`, `card`, `product_sketch`, `og_card`. `chart` is deliberately NOT a `site_plan_imagery` kind — charts are Lane B artefacts rendered by code (D1) and stored as assets with their own purpose. | Keeps `chk_kind` and `validImageryKinds` in lockstep; keeps the diffusion pipeline away from data. |
| D4 | **Per-site brand guide as data.** A `site_specs` aspect (e.g. `imagery_style_guide`) holding palette, medium, mood words, negative directions, and 1–3 approved reference-image asset keys. Prompt composition reads it for every generation (extending the existing `imagery_direction` + `directionAppliesToKind` mechanism); Banana generations pass the reference images via `ReferenceImageURIs`. | This is the single biggest lever for "consistent, professional look". The plumbing (reference images, prompt composition, kind gating) already exists. |
| D5 | **Logo permanence = generate → human-approve → lock.** Approved logos (and any human-uploaded asset) are locked via the existing asset-locking columns; auditors and regeneration paths must skip locked assets. Favicon and OG card are *derived* from the approved logo, not independently generated. | "One consistent good logo throughout the life of the site" is a policy, not a generation feature. |
| D6 | **Icon strategy stays dual.** Lucide webfont for UI chrome (wire the validator — carried-over item); generated raster/sprites for decorative and topical glyphs. Bullets use the sprite sheet, not per-bullet images. | Locked lessons from 2026-05-20; sprites keep request count and bytes down. |
| D7 | **Product sketch styling is a constraint, not a knob**: stylised medium chosen per product category (technical → CAD-like line drawing; lifestyle → watercolour/pencil), viewpoint altered from source, in-context setting, "no brand markings/logos/text" hard-coded. Img2img from the cached photo is v2, only with the derivative-work framing. | Copyright safety by construction; matches the existing product illustration plan. |
| D8 | **Budgets enforced at deploy, policed by discovery.** Extend `ImagePurposes` with per-kind byte ceilings and WebP output where safe; add `image_weight_over_budget` discovery check; sprite sheet counts as one download for N glyphs. | Sizing was explicitly in the brief; enforcement beats intention. |

---

## 5. Phases

Each phase is independently shippable and verified on robot-hands.com before
moving on. Order within the table is the default sequence; I2/I3/I4 can overlap
once I0–I1 are done.

### Phase I0 — Verify ground truth, housekeeping, and the testbed rebuild
Everything below depends on knowing what's actually live, and on a clean testbed.
- ~~Refresh cluster credentials~~ ✅ 2026-07-08 (RUNBOOK A1).
- ~~Confirm deployed state vs docs~~ ✅ 2026-07-08: chk_kind matches, planner prompt carries Imagery Block + decomposition rule (max_tokens now 16000), `Pipeline: "build"` fix in code.
- ~~Confirm API-key scrub/rotation~~ ✅ keys rotated (user, 2026-07-08).
- ~~Confirm robot-hands.com rebuild status~~ ✅ not rebuilt; content layer still broken (18/87 components NULL content).
- Close the logo-in-header resolution gap (site component render path resolves `site_assets.logo`).
- Wire the Lucide validator into the content-generation step (identify the step; apply prompt allowlist + pre-store sanitise) — naturally lands with the rebuild's content generation.
- **Rebuild robot-hands.com from scratch with news scope** (decision 2026-07-08). Route: re-plan via `build-site-planner` (not patch-in-place), per `HANDOFF_robot_hands_rebuild.md`: first verify `site_specs` (identity/classification/briefing/strategy) are current; ensure `classification.content_features.news_feed.recommended=true` (or roadmap equivalent) so RULE 11 puts `latest-news` on the homepage; then trigger the rebuild. This exercises the imagery block end-to-end on a clean site and gives every later phase its acceptance surface.
- **Acceptance:** robot-hands.com is a freshly-built site with a populated content layer, a news section on the index, its generated logo rendering in the header, and valid Lucide glyphs in the features grid.

**Status 2026-07-10 — PHASE I0 SUBSTANTIALLY COMPLETE.** Scorecard against the
acceptance line:
- ✅ Freshly-built site with a populated content layer (33-page plan, content
  healthy; 3 corrupted component templates found and regenerated).
- ✅ News on the index: `latest-news` at position 5 (RULE 11 fired), dedicated
  news-index page in header nav, **9 content_sources seeded, 0 erroring**.
- ✅ Per-page imagery renders: 16 distinct heroes, one per page, committed git
  paths, 0 S3 URLs, 1 empty slot site-wide (Phase I3 scope).
- ⬜ Logo rendering in the header — still open (header renders via
  `render_site_components`, untouched by the page-level resolver; pairs with
  the B6 approve-and-lock flow in Phase I1).
- ⬜ Lucide glyphs validated in the features grid — validator still unwired;
  verify what the rebuilt features sections actually emit, then wire.
- Cleanup queue: orphan pre-rebuild pages (how-it-works, selection-guide,
  learning-center sprawl); "no-op complete" dispatch anomaly; hunt for the
  writer that originally saved rendered output as templates.
The two remaining ⬜ items close out I0 alongside the start of I1.

**⚠️ Cross-workstream finding (2026-07-10, from the leopardess rebuild —
affects this testbed):** the asset-deployer's git-commit step fails on
`agent-chassis` because `IMAGE_BUCKET` is not set in that deployment (storage
client never builds) — platform-wide, 83 of 102 active assets are expiring
presigned URLs; **robot-hands.com has 34 such assets and its logo 404s**. The
16 verified heroes ARE committed to git (deployed via the image-build-handler
→ spawned asset-deployer path, which injects storage env), so the I0 render
acceptance stands — but the logo (B6/I1 work) needs the underlying deploy fix
first. Fix + workaround are owned/documented by the leopardess workstream:
`docs/leopardessconsulting/AUDIT_verified_facts.md` D8 (add
IMAGE_BUCKET/S3_ENDPOINT/S3_REGION to the agent-chassis deployment) and
`scripts/commit_brand_assets.sh`. Also from that workstream: a routing change
(logo/illustration/infographic → Banana) is committed but not yet deployed.

**Status 2026-07-09 — build essentially DONE; imagery-render gap diagnosed.**
Content build all but complete (31 page rerenders done, 14 needs_page queued);
all 20 needs_imagery complete; **14 hero assets generated + deployed to git
overnight**; product-detail/services content layers healthy (0 null content).
BUT heroes are not rendering on some pages — diagnosed as a structural
key-mismatch, not timing. See "I0 finding" below.

**I0 FINDING (2026-07-09) — component image-source ↔ asset-key mismatch.**
Blocks the I0 acceptance line ("logo/heroes render"). Two problems:
1. *Empty `src=""` on 4 pages* (about, gripper-detail, learning-center-index,
   product-detail). Generic preset components declare image fields sourced from
   `site_assets.background`, `site_assets.product_screenshot`,
   `site_assets.illustration`, `site_assets.image` — but the plan generates
   page heroes under key `hero_<page>`, and the page-aware resolver
   (FOCUS §5.1) only maps `site_assets.hero`. The other four sources resolve to
   nothing → empty `src`. (LLM alt text still fills, so alt present + src empty.)
2. *Orphan hero* — `services-hero` template has no `<img>`; the generated
   `hero_services.jpg` is never consumed.

   **Fix options (decide before implementing):**
   (a) Broaden the page-aware resolver so the generic single-image sources
       (`background`, `illustration`, `image`) also resolve to the page-hero
       asset — smallest change, fixes 3 of the 4 pages immediately.
   (b) Treat `product_screenshot` as a genuine distinct need → Lane B (I3):
       a product/hero-secondary image, not the page hero. Interim: fall back to
       the page hero so nothing renders empty.
   (c) Services orphan: either give `services-hero` an image slot, or stop the
       planner emitting a page hero for pages whose selected component can't
       show one (add a "component consumes a hero image?" check before emitting
       the page-hero imagery row). Prefer the check — avoids wasted generation.
   Recommended: (a) now (unblocks I0 acceptance), fold (b) into I3, add the
   (c) check as a small planner/emit guard. This finding also strengthens the
   case for I8's `imagery_direction`/delivery audit and an
   `image_source_unsatisfiable` discovery check (component asks for a
   `site_assets.X` no generator produces).

   **BUILT 2026-07-09 (commit on 083_imagery, pending deploy).** Option (a)
   plus the discovery check, with two design corrections found during the
   guideline re-check (full detail: running notes Turn 7): the same-image
   symptom was actually the render-context's site-wide `hero_url` winning
   `{{or .hero_url .background_image}}` — fixed by injecting the per-page hero
   under the legacy alias keys into `resolved_data`, which the renderer merges
   last as the designed authoritative overlay; and the planner guard (c) is
   impossible at plan time (component selected later) — substituted by the
   discovery check, which runs when components are known. Shared alias table
   in `imageryplan.ImageRoleForPath` so resolver and check cannot drift.
   Post-deploy: run `SQL_2026-07-09_register_image_source_unsatisfiable.sql`,
   let the 14 queued re-resolves drain, spot-check pages.

   **RESOLVED 2026-07-10 — the finding took THREE fixes, all deployed/applied
   (running notes Turns 8–14 carry the full evidence trail):**
   1. *Image-role resolver + authoritative aliases* (above) — deployed
      2026-07-09; proven when pages started selecting their own heroes.
   2. *Deployed-path fix* — verification exposed that the resolver surfaced
      `assets.url` (an expiring presigned S3 URL; the committed git path is
      stored nowhere). Added `storage.DeployedWebPath(asset_key, purpose)` as
      the single source of truth shared with the deployer; resolver now emits
      `/assets/images/hero-<page>.jpg` paths. Deployed 2026-07-09 (2nd deploy).
   3. *Corrupted-template regeneration* — the residual empty-`src` pages were
      NOT resolver failures: 14 components fleet-wide (11 active) had
      html_templates saved as RENDERED OUTPUT (literal `<no value>`, zero
      `{{…}}` vars). Regenerated the three robot-hands ones via the existing
      `needs_component_regeneration` → component-creator path (the
      field-preservation guard rejected one attempt — correctly; retry with
      explicit preserve-instructions in `spec.description` succeeded). Built
      `check_component_template_corrupted` as the missing quality→regeneration
      bridge (committed; registration = RUNBOOK B8 after next deploy) to
      auto-regenerate the remaining 8 on other sites.
   **Final acceptance sweep:** 16 distinct hero files, each referenced by
   exactly one page; 0 presigned S3 URLs; 1 empty image slot site-wide
   (learning-center-index listing card — orphan residue + Phase I3 scope).

**Status 2026-07-08 evening — re-plan DONE, content build IN PROGRESS:**
- Prep executed (SQL artifacts beside this plan): specs backed up (42 rows), adoption-residue aspects superseded, classification news-enabled, `mission_brief` news hint added, 5 stale pre-2G items retired, `needs_site_plan` trigger inserted and manually promoted to `triaged` (manual items are not auto-triaged — record this pattern).
- New current plan `7a40a0f9-a1cd-4259-8654-cc0922e942aa` (complete 16:46 BST, ~6 min): **33 pages** — `news-index` in header nav + `news-post` template (mission hint landed), 5 `tool`-role pages — and **29 imagery rows** (19 page heroes, 8 section icons, site logo + hero).
- Downstream queue: 27 `needs_page` (1 in flight, rest queued), 20 `needs_imagery` (queued behind pages), `needs_composition` complete, `needs_design`/`needs_rerender` queued. Runs unattended via build-dispatch-loop; expect hours.
- Composition surfaced a layout gap (`scheme=dark` → fallback `brochure-formal` applied) — human decision queued as RUNBOOK **B7**.
- Still open in I0: logo-in-header fix; Lucide wiring; post-build verification (content layer populated, old pages/assets superseded not orphaned, learning-center page sprawl check).

### Phase I1 — Brand consistency layer (G1, G8 groundwork)
- `imagery_style_guide` site-spec aspect + schema (palette, medium, mood, avoid, reference asset keys).
- Prompt composition reads the guide for all kinds (per-kind gating like `directionAppliesToKind`).
- Banana path passes approved reference images (`ReferenceImageURIs`) for style-anchored generations.
- Logo lock policy (D5): approval flow + locked-asset respect in handlers/auditors; favicon + OG card derived from logo.
- **Acceptance:** two consecutive generations of different kinds on robot-hands.com visibly share palette/mood; logo asset locked; regeneration attempt refuses to touch it.

### Phase I2 — Sprite sheets & list treatment (G4)
Build the already-locked sprite plan: `kind='sprite_sheet'` (migration batch D3), one sheet per site (3×3 @ 768², 256px cells), Banana generation, CSS `background-position` classes emitted as a css_snippet, bullets/nav via `::before`.
- Add a fulfilment check beside `check_unfulfilled_imagery_plan`.
- Main risk (cell-content alignment) handled per the context pack: ordered-grid prompt, then assign meanings after eyeballing; vision-verify comes with I8.
- **Acceptance:** robot-hands.com lists render themed sprite bullets from one ≤80KB sheet; no per-bullet image requests.

### Phase I3 — Content-linked card imagery (G3; Lane B foundation)
- Generalise the content-entity → asset link (D2/Lane B): entity image reference + `needs_content_image` work item + handler composing prompts from the content item + brand guide.
- `card` kind/purpose with its own size profile (e.g. 800×450 WebP); card components resolve the entity's image. **Confirmed 2026-07-08: the card image is the article's asset re-cropped** (purpose-specific crops of one generation), not a sibling generation — one source image yields article hero, card crop, and OG crop.
- Blog-index / news-feed / tool-directory cards consume it.
- **Acceptance:** robot-hands.com blog index shows cards whose images match their articles; clicking through shows the same visual family on the article.

### Phase I4 — Data graphics pipeline (G2 — the flagship)
Per `FUTURE_data_graph_pipeline.md` and D1:
- `chart-renderer` capability — **confirmed 2026-07-08: go-echarts, in-chassis**; static SVG/PNG export through the existing asset path; interactive HTML embed where the component supports it.
- Data-fetch step for real series. **Confirmed 2026-07-08: sources are chosen per domain** (each domain gets the data source appropriate to its vertical); free-tier sources only for now, paid sources possible later. Series stored for reproducibility + attribution.
- LLM editorial layer: annotations, callouts, titles — never the data values.
- An `infographic`/chart component template that renders the artefact with caption + source line.
- **Acceptance:** a robot-hands.com article carries a chart of a real series (e.g. industrial robot shipments or component price index) with accurate labelled axes, source attribution, and an LLM-written callout.

### Phase I5 — News imagery (G6; builds on I3+I4)
- News ingestion attaches an imagery decision per item: `chart` (data-driven story → I4 pipeline), `illustration`/photo (I3 pipeline), or none (opinion/short items) — a small LLM classification step with the story text as input.
- Feed cards and article pages share the artefact (I3 mechanics).
- Freshness rule (confirmed 2026-07-08): no SLA — news ingests roughly twice a day. Imagery is requested at ingest time; a configurable `news_image_grace_interval` applies, after which the item falls back to a **default image derived from the site's brand kit** (I1) so the feed never shows a broken/empty slot. Interval value to be proposed at I5 design time (working suggestion: 6h, i.e. comfortably inside the ingest cadence).
- **Acceptance:** robot-hands.com news feed shows a price/trend graph above a data-driven story and a themed illustration above a non-data story, both at feed and article level.

### Phase I6 — Product sketches (G5)
Build the existing product illustration plan with the D7 styling constraints:
- Discovery check + `product-illustration-handler` + `link_asset_to_product` + renderer preference for `custom_image_id` (fallback `cached_image_url`).
- Medium selection by product category (CAD-like / pencil / watercolour); viewpoint + in-setting phrasing in the prompt template; per-pass cap (~20) against feed floods.
- **Acceptance:** hand-inserted gripper product row gets a CAD-style sketch, viewpoint distinct from the source photo, committed to git and rendered on the product card in preference to the cached photo.

### Phase I7 — Performance & delivery (G7, G8)
- Extend `ImagePurposes`: per-kind byte ceilings, WebP output for photographic kinds, explicit dimensions for new kinds (`card`, `sprite_sheet`, `og_card`).
- Responsive `srcset` + `loading="lazy"` in image-bearing component templates; alt-text required at generation time (prompt emits alt text alongside image spec) + `image_alt_text_missing` check.
- New discovery check `image_weight_over_budget` → `asset-deployer` re-optimise.
- **Acceptance:** robot-hands.com index page image payload under an agreed budget (propose ≤600KB above the fold); Lighthouse-style pass recorded in notes.

### Phase I8 — Quality loop (G9)
- `imagery-quality-auditor` (sibling of visual-design-auditor, vision-capable): checks delivered images against brand guide + prompt intent; respects locked assets and `origin_type='uploaded'`; `max_fix_attempts: 2`; findings feed the existing triage/dispatch loop.
- Sprite-sheet cell verification (the deferred vision check from I2).
- Brand-consistency check: sampled page images compared against the style guide reference images.
- **Acceptance:** auditor flags a deliberately-mismatched test image, the loop regenerates it within the attempt cap, and does not touch locked assets.

---

## 6. Explicitly NOT in this plan
- 3D product reconstruction from photos (was already parked; sketches cover the need).
- Per-vertical LoRA fine-tuning (kept as a future consistency upgrade once I1's reference-image approach shows its limits).
- Video/animation assets.
- Replacing Lucide with generated UI icons (settled: Lucide for chrome).
- Interactive-chart-only strategy — static SVG/PNG must always exist as fallback so charts survive in feeds/OG cards.

## 7. Risks
| Risk | Mitigation |
|---|---|
| Diffusion output quality drifts (multi-panel, contamination) | Locked prompt rules (one-entry-one-image, kind gating) already in planner; I8 auditor closes the loop. |
| Brand guide makes everything samey across sites | The guide is *per-site*, derived from design_intent; robot-hands vs a games site should diverge deliberately. |
| Chart pipeline scope creep (data sourcing per vertical) | I4 ships with ONE real source wired for the testbed; per-vertical sources added as content demands. |
| Copyright exposure on product sketches | D7 constraints hard-coded; no img2img in v1; audit gate before scale-out. |
| Cost creep (Banana paid tier, more images per site) | Per-pass caps; sprites amortise small art; budget review is a RUNBOOK task. |
| robot-hands rebuild not finished → acceptance checks blocked | I0 confirms state; if blocked, finish rebuild first (it's the testbed for everything). |

## 8. Open questions — ANSWERED 2026-07-08 (kept for the record)
1. Lane B storage → **generic `entity_type + entity_id` columns on `assets`**.
2. Chart runtime → **go-echarts in-chassis**.
3. Data sources → **per domain, appropriate to each vertical; free tier for now, paid later**.
4. Card vs article hero → **same asset, re-cropped per purpose**.
5. Logo approval → **human eyeball via the RUNBOOK** for v1.
6. News imagery → **no SLA** (ingest ~2×/day); configurable grace interval then brand-derived default image.
7. Byte budgets → **confirmed as proposed** (hero ≤180KB, card ≤60KB, sprite sheet ≤80KB, index above-the-fold ≤600KB).

Newly open:
- Grace-interval value for news imagery (propose at I5 design; working figure 6h).
- First data source for robot-hands.com's vertical (propose at I4 start).
