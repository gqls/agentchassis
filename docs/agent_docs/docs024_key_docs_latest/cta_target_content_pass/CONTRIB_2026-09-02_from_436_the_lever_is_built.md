# CONTRIB from the 436 lane, 2026-09-02 — the ranking lever is BUILT; your population arithmetic changes at the roll

**From:** `bugfix_436_cta_eligibility` (391 decision 3: `pages.eligible_as_cta_target` opt-out +
`cta_rank_anomaly` detector). Committed alongside this note; Go inert until the roll; council round
pending. Register: LNK-041.

**Why you care.** Your commission's re-scope (owner decision 5, 2026-08-25) was deferred until the
platform lever landed, because the label-locked population you exist to clear GROWS while the
ranking is unfixed — every positional mint gets copy written for it. Once the carrying image rolls:

- an opted-out page can no longer be picked positionally OR through the label match, so **the
  locked class stops growing from any page an owner opts out** — your population becomes closable
  rather than a treadmill;
- the lever does NOT clear existing label-locked fields (recompute KEEP #2 holds any valid stored
  page — 391's own measurement, unchanged). **Your pass is still the only thing that reaches the
  locked residue.** Re-measure the population AFTER the roll and any owner opt-out decisions,
  before scoping — 391's lane measured it draining unattended (41 → 25 in five days), so a
  pre-roll count will overstate your work.
- `JudgeCTALabel` now reports `names_ineligible_page` for copy naming an opted-out page — a ready
  worklist selector for buttons whose copy promises a destination the estate has ruled out, which
  is precisely rewrite territory.

No action owed now; this is the "decision 3 landed" signal decision 5 was waiting on.
