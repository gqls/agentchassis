apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-persona-system

resources:
  - ../../../base

patches:
  - path: patch-deployment.yaml

images:
  - name: docker.io/aqls/agent-chassis
    newTag: v1.0.883