apiVersion: v1
kind: Service
metadata:
  name: webscrape-adapter
  labels:
    app: webscrape-adapter
spec:
  type: ClusterIP
  selector:
    app: webscrape-adapter
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP