apiVersion: v1
kind: Service
metadata:
  name: web-scrape-adapter
  labels:
    app: web-scrape-adapter
spec:
  type: ClusterIP
  selector:
    app: web-scrape-adapter
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP