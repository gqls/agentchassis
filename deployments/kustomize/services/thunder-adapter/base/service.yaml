apiVersion: v1
kind: Service
metadata:
  name: thunder-adapter
  labels:
    app: thunder-adapter
spec:
  type: ClusterIP
  selector:
    app: thunder-adapter
  ports:
    - port: 8080
      targetPort: 8080
      name: http
