apiVersion: v1
kind: Service
metadata:
  name: admin-dashboard
  labels:
    app: admin-dashboard
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: admin-dashboard