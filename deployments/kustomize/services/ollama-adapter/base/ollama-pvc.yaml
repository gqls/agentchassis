apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models-pvc
  labels:
    app: ollama-adapter
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi