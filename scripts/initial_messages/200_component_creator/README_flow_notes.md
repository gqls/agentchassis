The generation path is confirmed: component-creator — it's the only agent that calls store_generated_component (which runs separateInlineJS). Its workflow is ensure_site_record → read_site_spec → generate_template (LLM) → store_component → complete, it processes needs_new_component work items, and the trigger script (080) shows the spawn+call pattern with section_type plus optional site_type/page_context/description/design_direction.
But reading the actual prompt surfaces two things I need to resolve before we regenerate anything — one is the confirmed Gap 2, and one is a regeneration-mechanics question I can answer from the uploaded store action. Let me read the store action's insert-vs-update logic first, because it determines whether re-running the creator replaces the broken row or just skips it.Synthesizing regeneration mechanics and component lookup logicSynthesizing regeneration mechanics and component lookup logicThis answers the regeneration-mechanics question well. Let me see the decision point above it — how it decides regenerate-vs-create:Synthesized regeneration mechanics, confirmed Gap 2 dependency, formulated sequencing strategySynthesized regeneration mechanics, confirmed Gap 2 dependency, formulated sequencing strategyThis is good news for the fix — the regeneration mechanics are clean. Here's the full picture.
Generation path: component-creator (a builder agent, not a rerender agent and not a build/site agent). It processes needs_new_component work items; the LLM step produces the template and store_generated_component persists it.
Regeneration is UPDATE-in-place, and it's well-behaved. store_generated_component looks up an existing component by function (ignoring is_active, ordered active-first), and if found sets isRegeneration=true. On that path it: snapshots the current row to component_versions (MAX+1) before the update so history is preserved; UPDATEs in place, preserving component_id so every page_components/site_components FK keeps resolving with no relink; refreshes html_template, input_schema, js_content, render_mode (derived), and sets is_active=true; then marks dependent pages pending and raises a needs_rerender work item per affected site so the rerender pipeline actually rebuilds. So regenerating these three is clean — no orphaned references, history kept, pages auto-rerendered.
The system already knows about the <no value> failure and now blocks it. The pre-store validation rejects any template containing <no value>, with a comment describing exactly our Mode-B cause ("the unresolved variables produced <no value>, and that output was stored back as the template… can't be restored without regeneration"). That validation post-dates these components — which fits our root cause (they predate the current guards) and means a fresh regeneration can't re-store a broken template. The one gap that remains is the <script>-balance check I flagged earlier (it validates unclosed <style> but not <script>), so that hardening still stands.
But Gap 2 is now confirmed from the actual prompt, and it splits the three components. The prompt's field tiers are A (voice/LLM), B (tunable labels), C (site data), D (derived lists resolved by a plan-time DB query), plus a "renderer" source for single JS-filled values like a timer. There is no tier for content fetched client-side from a JSON file at runtime — the daily-feed pattern. Consequences:

brief-explanation (static): regenerate via component-creator as-is. Static content is exactly Tier A/B/C; no gap. Clean win, and it proves the regeneration path end-to-end on the simplest case.
provocation-card & lobby-grid (runtime feed): regenerating as-is would lose the loader — the LLM would classify the provocation text as Tier A and bake a build-time provocation into the template, which is the wrong shape for daily content. It would fix the <no value>/empty-schema/extraction problems but regress the proven Option-2 design.

So the structural fix for the two dynamic ones is to give component-creator a runtime-feed tier — a Tier E with source: "feed.{name}" that instructs the LLM to emit a stable-selector DOM shell plus an inline <script> loader that fetches /data/{feed}.json and fills it, declaring the data contract. That mirrors exactly how the prompt already encodes Tier D (it hands the LLM a canonical range-block to copy); we'd hand it a canonical loader pattern — and we have a proven one to base it on. Then these regenerate with the loader inline, separateInlineJS extracts it to js_content, and it ships as /tools/assets/{function}.js on rerender — the durable Path 1, no snippet needed.
My recommended sequencing: regenerate brief-explanation now (no gap, validates the path), then design the Tier E prompt extension for the two dynamic components. I'd want your decision on the Tier E approach before writing it, since it's a deliberate change to the creator's contract.
Two things to confirm before regenerating, both about determinism:Ran 3 commands, read a fileRan 3 commands, read a fileTo pull it together: the generation pathway is component-creator, not any rerender or site-builder agent. The regeneration mechanics are reassuring — it UPDATEs in place keyed by function, preserves the component_id so nothing needs relinking, snapshots the old version into history, reactivates the row, and raises the rerender work items itself. And the system already rejects <no value> templates at store time, so a regeneration can't reintroduce the broken shape. The <script>-balance hardening is the one validation gap left.
