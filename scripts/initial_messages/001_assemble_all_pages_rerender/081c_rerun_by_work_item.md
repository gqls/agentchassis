the dynamic agents need a wrapper to spawn them; a direct orchestrate to system.agent.generic.requests won't materialize a page-build-handler. Let me find the wrapper pattern we've used rather than guess at it.Find the wrapper/spawn pattern used to launch dynamically-created agentsFind the wrapper/spawn pattern used to launch dynamically-created agentsThe multipage-wrapper agent type exists, and there's a spawn_agent action. Let me look at how the wrapper is used to spawn a handler — the multipage-wrapper is the precedent for launching a dynamically-created agent.Read the comment block about wrapping a handler in a dedicated spawned podRead the comment block about wrapping a handler in a dedicated spawned podThe doc reference is key: "Every pod-running agent needs a parent that spawned it." So the wrapper is a parent orchestrator that issues a spawn_agent step. Let me look at the multipage-wrapper's actual workflow and how it spawns its child, since that's the closest precedent to wrapping page-build-handler.Read the spawn_agent action to learn the config contract for spawning a dynamic agentRead the spawn_agent action to learn the config contract for spawning a dynamic agentThis is the spawn mechanism — a parent orchestration issues spawn_agent, which sends an initialize message to a freshly-created agent_id on the child's requests topic. So I need the wrapper that does this for page-build-handler. The cleanest precedent is whatever already wraps page-build-handler in production. Let me check how the build pipeline normally launches it — the build-dispatch-loop or a multipage-wrapper.Ran 2 commandsRan 2 commandsThat clarifies the topology. build-dispatch-loop is a scheduler-triggered agent with a fixed .process topic, and it spawns handlers via the spawn→job topic pattern. So the production path to page-build-handler is: a site_work_items row → build-dispatch-loop claims it → spawns page-build-handler in a job pod. The handler is dynamic (job topic), which is exactly why a direct orchestrate to generic.requests won't reach it — your point.
So the cleanest, lowest-risk way to launch our test build through the real machinery is to insert a work item and let the existing build-dispatch-loop claim and spawn it. That uses the production wrapper (build-dispatch-loop) exactly as a real build would — no hand-rolled wrapper, no guessing the spawn contract. Let me confirm the work-item shape build-dispatch-loop claims, so the inserted row matches what it expects.Ran 2 commandsRan 2 commandsNow I can see the real INSERT pattern that production uses to queue a page-build-handler item. Let me read one of these in full to get the exact column set and spec shape — line 33344 (the content-gap-planner creating a page-build work item) is the closest analog to "build this new page."Read the exact work-item INSERT the gap planner uses to queue a page-build-handler buildRead the exact work-item INSERT the gap planner uses to queue a page-build-handler buildNow I have the exact production contract. The gap planner queues a page-build by inserting a site_work_items row with pipeline='build', item_type='needs_content_page', handler_agent='page-build-handler', status='triaged', and a spec carrying page_name/page_type/title/purpose/sections/source. The build-dispatch-loop (already running on its scheduler) then claims status IN ('triaged','approved') items and spawns the handler via the job pattern — the real wrapper, no hand-rolled spawn.
So the test launch is: insert one work item matching this shape, pointing at our faq-test page, and let the existing dispatch loop spawn it. Let me verify the work-items column names/types against the live schema before writing the INSERT (per the schema-check discipline).List the columns the production INSERT uses for confirmation against live schemaList the columns the production INSERT uses for confirmation against live schemaThe launch path is settled: insert a site_work_items row exactly as the gap planner does, and the already-running build-dispatch-loop claims it and spawns page-build-handler through the real job-spawn wrapper. No custom wrapper needed — we reuse the production dispatch path, which is also more faithful to "what a real build does" than any hand-rolled trigger.
Before I write the INSERT, two schema confirmations (per the check-before-SQL discipline), since I'm reading these column names from the Go source not the live table:

