{
  "rfc009": {
    "binary": "/app/agent-chassis",
    "deployment": "agent-chassis",
    "expect": [
      "a component must not assert a contact detail, price or address nobody supplied=2",
      "template invents=1",
      "library_fabricated_hours=1"
    ],
    "negative": [],
    "note": "RFC_009 B+C. Control is SYNTHETIC on purpose, not by omission: 87ea0a5e7 is purely additive Go and removes no string literal, so no strong control exists to pass here (pick-pod-marker says so itself). Do not 'fix' the nag by inventing one."
  }
}
