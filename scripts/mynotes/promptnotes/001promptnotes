[parse all ai responses for confusions and restated bugs and reasoning steps]
produce detailed sequence diagram with all function calls using trace
follow particular struct throughout lifespan
look at each page separately perhaps so we miss less
try not to rely on missing values to determine that we are at the start of a chain of agents for example
keep things simple
prioritise complexity
prioritise what we can add at the end once we've determined basic flow
set up proper logging so we can turn off parts of it easily
maybe create tree/diagram of all code and flows
maybe create life cycle of all variables, structs and values
represent overall objectives time and time again like each agent is an orchestrator, agents are dynamic, etc etc
step names must always be unique in a workflow - first_calculation, second_calculation. process?, complete? etc as they act as keys, maybe should have compound keys
get where params are initiated throughout the code
track executivecontext throughout agent to agent and messaging
agents are dynamic
every agent is an orchestrator
messages contain full context, state is preserved in db in case of container failure
kafka - principle of unidirectional message flow
agent versioning and agent groups, evolution

keep your answers brief, to the point and use my existing code as a base wherever possible and don't over engineer things for now please claude.
If you think that parts of my code might be missing in your context, ask first before rewriting it.

if we have a structure in place like for instance a validation package, then don't suggest hardcoded inline validation as option 1 - this is hacky.
I understand sometimes it is necessary or shortcuts very longwinded solutions and it can be suggested but not without considering the "proper" flow.

Please always think things through such that you look for simpler solutions in preference to complicated ones.

to jsut detect if a function exists, don't call it.

/tmp directory was full - errors showed when reading topics
kubectl -n kafka exec -i personae-kafka-cluster-combined-pool-prod-0 -- \
  /opt/kafka/bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 \
  --topic job.42eab5fa-7fe55079-calculator-spawn_adder.requests \
  --from-beginning --property print.headers=true --max-messages 5
java.io.IOException: No space left on device ...

do I have the latest code and initial messages and database contents and schema available to me?

help me look at this step by step, function by function to see where it is failing.

lets create a sequence diagram with each function and agent included to determine what is happening and what should be happening

the calculator _should_ commence it's own workflow.

It should have received the parameters from the request (which numbers to add. 12345, 1234) and then should initialize, complete it's own workflow and complete.
