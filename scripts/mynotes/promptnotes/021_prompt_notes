_field suffix conventions are per-action — not universal
input_mapping fields are required by default (use ? suffix to mark optional)
Empty logs ≠ action didn't run (check orchestration_states FIRST)
DB rows can be partial (write may precede the failure)
SQL is immediate; Go requires rebuild + rollout
Sibling functions in the same file are the canonical pattern
Token budgets scale with structured output
Every $VAR referenced must be declared (set -u)
jq --slurpfile silently produces null
Manual triggers vs dispatch — different failure modes
Parent and child orchestrations are separate rows
? suffix placement matters — destination key, not source path